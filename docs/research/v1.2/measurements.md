# Замеры перед v1.2 (2026-09-06)

Стенд: Playwright MCP, headed Chrome, dpr 1, WebGL-вкладки закрыты. Мобильная эмуляция: viewport 390×844,
CPU-троттлинг ×4 через CDP `Emulation.setCPUThrottlingRate`. fps = средний интервал rAF за 2,5 с в секции.

## fps по секциям

| Секция | Прод 1440×900 | Прод 390×844, CPU×4 | Оригинал 390×844, CPU×4 |
|---|---|---|---|
| hero | 65 | 30 (max 133 мс) | 116,8 (max 18 мс) |
| карта | 66 | 30 | 116,8 |
| форма | 69 | 30 | — |
| about | 76 | 73 | — |
| остальное | 120 | 116–120 | — |

Long tasks на проде: 2 × 220 мс (десктоп), 615 мс (CPU×4). На странице 14 CSS-анимаций,
7 `backdrop-filter`, 1 canvas (глобус, 2200 точек на кадр, dpr до 2), 3109 узлов SVG
(из них 1884 круга огоньков + 5 корзин с анимацией opacity).

## Мобильный аудит прода (390×844)

- `meta viewport` есть, `scrollWidth` документа 390: горизонтальной прокрутки нет. За край выходят
  только `.starfield__glow` (hero с `overflow: hidden`) и SVG карты внутри `.map-shell`.
- H1 44px, Onest 400–900 и Noto Sans 400/700 загружены.
- Цели касания ниже 44px: ссылки футера 22px высотой (две), чекбокс согласия 18×18, скрытые radio 1×1
  (визуально скрытые, норма), логотип 146×43.
- Кегль ниже 12px: подпись логотипа «МИССИЯ ДЛЯ ВСЕХ» 9px/10px (словесный знак).
- Картинки: 7 превью YouTube `hqdefault.jpg` 480×360, все `loading="lazy"`, атрибуты `width`/`height`
  есть только у первой.
- Трафик первой загрузки 354 КБ: JS 124 + 65 КБ, CSS 17 КБ, шрифты 4 файла до 33 КБ.

## Hero оригинала

Видео `<video autoplay muted loop playsinline preload="auto">` 1920×1080, 12,79 с, webm 1 914 635 Б,
mp4 2 856 265 Б (копии в `public/hero-globe.webm` и `public/hero-globe.mp4`). На 1440×900 hero 600px,
видео 1067×600 справа вверху. На 390×844 hero 844px, видео cover по центру. Частицы: `<canvas>` 1440×600,
opacity .72, `mix-blend-mode: screen`, скрипт `orig-hero-motion.js` (30 fps). Скриншоты
`orig-hero-1440.jpeg`, `orig-hero-390.jpeg`.
