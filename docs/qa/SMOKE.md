# Приёмочный smoke «Единый голос 27»

Финальный чеклист фазы 5. Прогон идёт двумя заходами: сначала по локальному preview, затем по проду. Результаты вписываются в колонки «Preview» и «Прод» планом 05-07.

| Поле | Значение |
|---|---|
| Дата | 2026-09-05 |
| Коммит | b3a7009 (деплой), сборка идентична c8915d4 |
| Исполнитель | |
| Инструмент | |
| Прогон workflow | [runs/33985010587](https://github.com/thevladoss/esd-onevoice27/actions/runs/33985010587) — success; предыдущий [runs/33984796698](https://github.com/thevladoss/esd-onevoice27/actions/runs/33984796698) на c8915d4 тоже зелёный |

В ячейку результата пишется «да», «нет» или измеренное число. Пустая ячейка означает, что проверку ещё не прогоняли.

## Как запустить

```bash
npm test                                   # vitest run: зелёный, без предупреждений act
npm run build                              # tsc -b && vite build: без предупреждений о размере чанков
npm run check:dist                         # 11 проверок собранного dist
npx vite preview --port 4173 --strictPort  # preview поднимается в фоне
```

Preview открывается по адресу http://localhost:4173/esd-onevoice27/, прод живёт по адресу https://thevladoss.github.io/esd-onevoice27/.

Браузерный заход делается дважды на вьюпорте 1440×900: первый раз с обычными настройками движения, второй раз с `emulateMedia({ reducedMotion: "reduce" })`. Ширины для замеров вёрстки: 320, 390, 768, 1024, 1440.

## Проверки

| № | Проверка | Критерий | Preview | Прод |
|---|---|---|---|---|
| 1 | Секции | `#hero`, `#map`, `#light-form`, `#about`, `#involve`, `#news`, `#resources`, `#quote` присутствуют и видимы, всего 8; плюс `header` и `footer` в DOM | | |
| 2 | Консоль | 0 сообщений уровня `error`, 0 необработанных исключений | | |
| 3 | Сеть | 0 ответов со статусом 404 и 5xx, включая шрифты и превью YouTube; внешние хосты только `fonts.googleapis.com`, `fonts.gstatic.com`, `img.youtube.com`, `youtube-nocookie.com` | | |
| 4 | Горизонтальный скролл | `document.documentElement.scrollWidth <= window.innerWidth + 1` на 320, 390, 768, 1024 и 1440, до и после прокрутки страницы до конца | | |
| 5 | Reveal | После прокрутки до низа у каждой обёртки `getComputedStyle(el).opacity === "1"` и `transform === "none"` | | |
| 6 | Reduced motion | Прогон с `emulateMedia({ reducedMotion: "reduce" })`: все восемь секций видны, `opacity` обёрток равна 1, высота документа отличается от обычного прогона не больше чем на 2%, у элементов `[data-anim]` `animationName === "none"` | | |
| 7 | Якоря | Клик по четырём пунктам меню («О проекте», «Участие», «Новости», «Ресурсы»): верх целевого `<h2>` ниже нижней грани пилюли минимум на 16px | | |
| 8 | Фокус | Обход табом от начала страницы: первый стоп skip-link «Перейти к содержимому», кольцо видно на каждом шаге, порядок совпадает с DOM | | |
| 9 | Мобильное меню | На 390 бургер открывает диалог «Меню», Esc закрывает, фокус возвращается на бургер | | |
| 10 | FPS | Сэмпл через `page.evaluate` при прокрутке на 1440, ориентир ≥ 45. Информативно, приёмку не блокирует | | |

## Контраст

Пары из UI-SPEC фазы 5. Значение измеряется по итоговому композиту, а не по цвету слоя.

| Пара | Ожидание | Preview | Прод |
|---|---|---|---|
| `paper / .8` на `GlassCard` поверх midnight-950 | ≥ 4.5:1 (расчёт даёт ~8.9:1) | | |
| `paper / .72` на карточках новостей и материалов | ≥ 4.5:1 | | |
| Дата и заголовок новости поверх обложки со скримом | ≥ 4.5:1 в самой светлой точке фото | | |
| Текст цитаты на градиенте секции `#quote` | ≥ 4.5:1 в самой светлой точке градиента | | |
| `#fca5a5` на фоне поля формы | ≥ 4.5:1 (расчёт даёт ~9.8:1) | | |
| Кольцо фокуса `#aad9dc` на midnight-950 и на стекле | ≥ 3:1 (нетекстовый элемент) | | |

## Скриншоты

Файлы кладутся в `docs/qa/`. Playwright пишет их в корень проекта, оттуда переносятся.

| Файл | Вьюпорт | Снят |
|---|---|---|
| `docs/qa/final-desktop.jpeg` | 1440×900, первый экран | |
| `docs/qa/final-mobile.jpeg` | 390×844, первый экран | |
| `docs/qa/final-full.jpeg` | 1440, `fullPage: true` | |
| `docs/qa/final-reduced-motion.jpeg` | 1440×900, `reducedMotion: "reduce"` | |

`final-full.jpeg` кладётся рядом с `docs/research/orig-full.jpeg` и сравнивается по ритму секций, скосам, стеклу и градиентам. Расхождения записываются в «Итог».

## Прод

```bash
git push origin main
gh run watch --exit-status
curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1   # ожидается 200
```

| Проверка | Критерий | Результат |
|---|---|---|
| Заголовок вкладки | Совпадает с preview: «Единый голос 27 — Евро-Азиатский дивизион» | да, ровно 1 совпадение в живом HTML |
| Число секций | 8 | |
| Консоль | 0 сообщений уровня `error` | |
| Сеть | 0 ответов со статусом 404 | |
| Код ответа | `curl -sI` по корню Pages отдаёт 200 | да, `HTTP/2 200` |
| Побайтная сверка | sha256 живых файлов равен локальному `dist` на том же коммите | да, 4 из 4 |
| Ассеты | Список `/esd-onevoice27/assets/*` на живой странице совпадает с `dist/index.html` того же коммита | да, `diff` пуст: 3 файла, каждый отвечает 200 |

Строки «Число секций», «Консоль» и «Сеть» заполняет браузерный заход: у CLI-прогона нет доступа к DOM и к логу консоли.

Побайтная сверка прода и локального `dist` (сверка снята на `c8915d4`, повторена после деплоя `b3a7009` с тем же результатом). Коммиты фазы после `c8915d4` меняют только `.planning/` и `docs/`, на собранный `dist` они не влияют, поэтому имена и sha256 ассетов не меняются:

| Файл | Код ответа | sha256 |
|---|---|---|
| `/esd-onevoice27/` (`index.html`) | 200 | `de8579d4da367fdbf165b9a0fff556e2749b151b0855a307cc319c235bffc3d5`, равен локальному |
| `/esd-onevoice27/assets/index-BkuRj91m.js` | 200 | равен локальному |
| `/esd-onevoice27/assets/index-HFIOJlwW.css` | 200 | равен локальному |
| `/esd-onevoice27/assets/vendor-map-BjCgd77U.js` | 200 | равен локальному |

Расхождение preview и прода означает проблему с `base` или с кэшем Pages, а не с вёрсткой.

## Итог

| Поле | Значение |
|---|---|
| Вердикт | |
| Замечания | |
| Отложено на v2 | |
