# Интеграционный аудит milestone v1.0

**Дата:** 2026-09-05. **Метод:** чтение исходников и экспортов фаз 01–05, `npm test` (43 файла / 346 тестов), `npm run build`, `node scripts/check-dist.mjs` (11/11), результаты браузерного smoke из `docs/qa/SMOKE.md`.

**Итог:** проверено 20 межфазовых связей, 19 WIRED, 1 частичная (WARNING). Полностью оборванных E2E-сценариев нет.

## Связи

| # | From | To | Via | Статус |
|---|---|---|---|---|
| 1 | Hero CTA (P2) | `#light-form` (P3) | `scrollToSection` | WIRED |
| 2 | LightForm (P3) | `LightsProvider.addLight` (P2) | `useLights` | WIRED |
| 3 | `lights[]` (P2/P3) | EsdMap (P2) | проп `lights`, `is-new`/`pulse` | WIRED |
| 4 | `counts` (P2/P3) | Counters (P2) | `useLights().counts` → `useCountUp` | WIRED |
| 5 | LightForm успех (P3) | SuccessToast + live region (P3) | `role="status"` | WIRED |
| 6 | `copy.ts` nav (P1) | секции P3/P4 | id совпадают буквально | WIRED |
| 7 | `--header-offset` (P1/P5) | `scrollToSection`, `headerOffset` | одна переменная | WIRED |
| 8 | `useActiveSection` (P1) | Header `aria-current` | IntersectionObserver | WIRED |
| 9 | CountryChips (P2) | useMapZoom → EsdMap | `selection`, тест на ресайз | WIRED |
| 10 | Reveal* (P5) | секции P2–P4 | ref и aria насквозь | WIRED |
| 11 | News пагинация (P4) | RevealGroup (P5) | `key={result.page}` | WIRED (CR-01 закрыт) |
| 12 | VideoEmbed (P3) | VideoGrid (P4) | единый компонент | WIRED |
| 13 | `lib/geo` (P2) | EsdMap, WorldSilhouette (P4) | импорт | WIRED |
| 14 | `usePrefersReducedMotion` (P2) | Hero, GlobeCanvas, EsdMap, Counters, Reveal | единственный источник | WIRED |
| 15 | реестр `data-anim` (P5) | Footer, Button, Starfield, GlobeCanvas, EsdMap, Resources | grep в тесте | WIRED |
| 16 | Involve «Скачать материалы» (P3) | deep link `#resources-materials` (P4) | `href="#resources"` | **PARTIAL** |
| 17 | `codeSplitting.groups.vendor-map` (P5) | d3/topojson/world-atlas (P2) | отдельный чанк 182,7 КБ | WIRED |
| 18 | deploy.yml (P1, WR-06 P5) | `check:dist` (P5) | шаг перед upload | WIRED |
| 19 | select страны (P3) | `ESD_COUNTRIES` (P2) | импорт | WIRED |
| 20 | MaterialsList (P4) | внешние домены | `noopener noreferrer`, тест | WIRED |

## E2E-сценарии

Все сценарии завершены: hero → форма → карта и счётчик → тост; навигация по шапке; зум карты чипами; reveal и reduced motion; пагинация новостей; панели ресурсов; VideoEmbed; деплой. Единственный разрыв: из триптиха «Участие» карточка «Скачать материалы» приводит к секции ресурсов, но панель материалов не раскрывается сама (нужен второй клик).

## Находка

**WARNING, INVOLVE-01 / RES-02.** `src/data/copy.involve.ts` держит `toolkit.href: "#resources"`, хотя фаза 4 построила приёмник `#resources-materials` (`Resources.tsx`, тест `Resources.test.tsx`), а `04-REVIEW-FIX.md` отложил перевод ссылки на фазу 5, где его не сделали. Исправление: `href: "#resources-materials"` и расширение типа. Запланировано в фазе 6 (план 06-04).

## Техдолг

- Комментарий в `Resources.tsx` про «фазу 5» устарел.
- `Button.tsx` ставит `data-anim="beam"` на любую primary-кнопку, включая submit формы; в оригинале луч есть и на submit, поэтому поведение оставлено.
