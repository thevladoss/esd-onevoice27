---
phase: 06-original-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Header.css
  - src/components/layout/BurgerButton.tsx
  - src/components/layout/MobileMenu.tsx
  - src/components/layout/Wordmark.tsx
  - src/components/layout/Header.test.tsx
  - src/lib/useHeaderHide.ts
  - src/styles/global.css
  - src/styles/motionPolicy.test.ts
autonomous: true
requirements: [FID-01]

must_haves:
  truths:
    - "На 1440 и 1920 пилюля шапки повторяет оригинал: контейнер fixed на всю ширину с отступом 16px, пилюля max-width 72rem, два псевдослоя (градиентная рамка 1.5px и стекло rgb(7 2 16 / .77) с blur 18px saturate 135%), на ≥1280 оба слоя skewX(-20deg) с радиусом 18px и drop-shadow"
    - "Пункты меню на ≥1024: 16px, weight 650, letter-spacing -0.015em, uppercase, текст в span с градиентом background-size 230% и background-position 100% 50%, на hover и focus-visible background-position 0 50% за 360ms cubic-bezier(.22,1,.36,1); активный пункт (aria-current) тоже в позиции 0 50%"
    - "При прокрутке вниз дальше высоты шапки контейнер получает класс is-header-hidden (translateY(calc(-100% - 2rem)), opacity 0, transform 420ms cubic-bezier(.32,.72,0,1)), при прокрутке вверх класс снимается; при открытом меню шапка всегда видна"
    - "Ниже 1024 показан бургер 48×48 с SVG 64×28 из трёх rect, в открытом состоянии линии складываются в крест за 240ms; мобильное меню — полноэкранный оверлей с радиальными пятнами, linear-gradient(145deg) и blur 24px, пункты 750 веса с разделителями и линией справа; Esc, фокус-ловушка, inert и возврат фокуса работают как раньше"
    - "Логотип белый (rgb 255 255 255), ширина 146/169/159px по брейкпоинтам"
  artifacts:
    - path: "src/components/layout/Header.css"
      provides: "CSS шапки, меню, бургера и оверлея по оригиналу"
      contains: "skewX(-20deg)"
    - path: "src/lib/useHeaderHide.ts"
      provides: "хук скрытия шапки при прокрутке вниз"
      contains: "is-header-hidden"
  key_links:
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/useHeaderHide.ts"
      via: "класс на контейнере"
      pattern: "useHeaderHide"
---

## Phase Goal

Из ROADMAP: «Посетитель видит шапку, меню, фоны секций и кнопку hero, совпадающие с оригиналом onevoice27.org вплоть до цветов и анимаций».

<objective>
Переписать стили и разметку шапки, десктоп-меню, бургера и мобильного оверлея по CSS оригинала (все правила ниже сняты с живого onevoice27.org 2026-09-05, переменные развёрнуты в RGB), сохранив наши a11y-механики и тексты. Purpose: FID-01, прямая просьба пользователя. Output: Header.css с правилами оригинала, хук скрытия при прокрутке, обновлённые тесты.
</objective>

<context>
@.planning/phases/06-original-fidelity/06-CONTEXT.md
@docs/research/orig-fidelity-css.md (если файл уже есть — сверить; правила ниже приоритетны)
Текущие файлы: src/components/layout/Header.tsx, Header.css, BurgerButton.tsx, MobileMenu.tsx, Wordmark.tsx, Header.test.tsx, src/lib/useActiveSection.ts, scrollLock.ts, src/styles/global.css (единый блок reduced motion и реестр data-anim), src/styles/motionPolicy.test.ts, src/data/copy.ts (nav).
Отступления от оригинала, принятые сознательно: бургер остаётся `<button>` (в оригинале `<a role="button">`); кольцо фокуса `focus-visible` остаётся нашим (оригинал ставит `outline: none`, у нас это запрещено тестом); шрифт Onest вместо Figtree; активный пункт меню подсвечен как hover.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Пилюля, логотип и десктоп-меню по CSS оригинала</name>
  <files>src/components/layout/Header.tsx, src/components/layout/Header.css, src/components/layout/Wordmark.tsx, src/components/layout/Header.test.tsx</files>
  <action>
    Разметка: `<header class="site-header">` → `<div class="site-header__content">` (grid) → логотип (Wordmark, белый, `width: min(146px, 100%)`, ≥768 169px, ≥1024 159px, grid-column 1) и `<nav class="site-nav">` с `<ul class="site-nav__list">` и ссылками `<a class="site-nav__link"><span>Текст</span></a>`; бургер (`site-header__toggler`) в колонке 2, ниже 1024 показан, ≥1024 `display: none`; nav ниже 1024 не рендерится в пилюле (его роль играет MobileMenu). Заменить прежний clip-path-скос и рамку на правила ниже (дословно, селекторы наши):

    ```css
    .site-header { position: fixed; inset-inline: 0; top: 0; width: 100%; z-index: 40; transform: none; opacity: 1; transition: transform 420ms cubic-bezier(.32,.72,0,1), opacity 302ms ease; }
    @media (min-width: 768px) { .site-header { width: calc(100% - 32px); margin: 16px; } }
    .site-header.is-header-hidden { transform: translateY(calc(-100% - 2rem)); opacity: 0; }
    .site-header.is-menu-open { transform: none; opacity: 1; }
    .site-header__content { position: relative; isolation: isolate; display: grid; width: 100%; max-width: 72rem; margin-inline: auto; grid-template-columns: minmax(0, 1fr) auto; align-items: center; min-height: 72px; padding-inline: 20px; border-radius: 0; box-shadow: 0 12px 28px rgb(5 3 20 / .22); }
    .site-header__content::before, .site-header__content::after { content: ""; position: absolute; pointer-events: none; }
    .site-header__content::before { z-index: 0; inset: auto 0 0; height: 1.5px; background: linear-gradient(90deg, rgb(210 142 190 / .82), rgb(105 120 195 / .42) 43%, rgb(255 255 255 / .12) 68%, rgb(84 164 172 / .76)); }
    .site-header__content::after { z-index: -1; inset: 0; background: linear-gradient(180deg, rgb(255 255 255 / .07), transparent 42%), rgb(7 2 16 / .77); -webkit-backdrop-filter: blur(18px) saturate(135%); backdrop-filter: blur(18px) saturate(135%); }
    @media (min-width: 768px) {
      .site-header__content { min-height: 88px; padding-inline: 28px; border-radius: 12px; box-shadow: 0 3px 7px rgb(5 3 20 / .28), 0 18px 42px rgb(5 3 20 / .30), 0 8px 34px rgb(79 93 175 / .13); }
      .site-header__content::before { z-index: -2; inset: 0; height: auto; border-radius: inherit; background: linear-gradient(112deg, rgb(210 142 190 / .82), rgb(105 120 195 / .42) 43%, rgb(255 255 255 / .12) 68%, rgb(84 164 172 / .76)); }
      .site-header__content::after { inset: 1.5px; border-radius: calc(12px - 1.5px); }
    }
    @media (min-width: 1024px) { .site-header__content { grid-template-columns: auto minmax(0, 1fr); } }
    @media (min-width: 1280px) {
      .site-header { --header-skew: 20deg; --header-skew-comp: 22px; }
      @supports (width: calc(1px * tan(1deg))) { .site-header { --header-skew-comp: calc(80px * tan(20deg) / 2); } }
      .site-header__content { min-height: 80px; padding-inline: calc(32px + var(--header-skew-comp)); border-radius: 18px; box-shadow: none; }
      .site-header__content::before { inset-inline: var(--header-skew-comp); transform: skewX(calc(-1 * var(--header-skew))); filter: drop-shadow(0 3px 7px rgb(5 3 20 / .28)) drop-shadow(0 18px 30px rgb(5 3 20 / .24)) drop-shadow(0 8px 24px rgb(79 93 175 / .10)); }
      .site-header__content::after { inset-block: 1.5px; inset-inline: calc(var(--header-skew-comp) + 1.5px); border-radius: calc(18px - 1.5px); transform: skewX(calc(-1 * var(--header-skew))); }
    }
    .site-nav { display: none; }
    @media (min-width: 1024px) {
      .site-nav { display: block; grid-column: 2; min-width: 0; }
      .site-nav__list { display: flex; justify-content: flex-end; align-items: center; gap: clamp(1.25rem, 2.6vw, 2.75rem); margin: 0; padding: 0; list-style: none; }
      .site-nav__link { position: relative; display: flex; align-items: center; min-height: 2.75rem; padding: .5rem 0; color: rgb(248 247 251 / .9); font-family: var(--font-display); font-size: 1rem; font-weight: 650; letter-spacing: -0.015em; text-transform: uppercase; text-decoration: none; }
      .site-nav__link > span { background-image: linear-gradient(90deg, rgb(210 142 190 / .82) 0%, rgb(105 120 195 / .42) 18%, rgb(84 164 172 / .76) 36%, rgb(248 247 251 / .9) 48%, rgb(248 247 251 / .9) 100%); background-size: 230% 100%; background-position: 100% 50%; -webkit-background-clip: text; background-clip: text; color: transparent; transition: background-position 360ms cubic-bezier(.22,1,.36,1); }
      .site-nav__link:hover > span, .site-nav__link:focus-visible > span, .site-nav__link[aria-current] > span { background-position: 0 50%; }
    }
    ```
    `--header-offset` (scroll-padding и `headerOffset.ts`) пересчитать под новую высоту: 72px + 16px отступа на мобильном, 88 + 32 на планшете, 80 + 32 на десктопе (переменная уже есть в global.css). Обновить Header.test.tsx: 4 якоря, span внутри ссылок, класс контейнера, бургер скрыт на десктопе через CSS (в jsdom проверять наличие элемента и класс).
  </action>
  <verify><automated>npx vitest run src/components/layout && npm run lint</automated></verify>
  <acceptance_criteria>
    - `grep -c "skewX(calc(-1 \* var(--header-skew)))" src/components/layout/Header.css` = 2; `grep -c "background-clip: text" src/components/layout/Header.css` ≥ 1; `grep -c "blur(18px) saturate(135%)" src/components/layout/Header.css` ≥ 1
    - `grep -c "clip-path" src/components/layout/Header.css` = 0 (скос теперь transform)
    - Header.test.tsx зелёный, `npx vitest run src/components/layout` зелёный
  </acceptance_criteria>
  <done>Пилюля и десктоп-меню собраны по правилам оригинала.</done>
</task>

<task type="auto">
  <name>Task 2: Скрытие шапки при прокрутке</name>
  <files>src/lib/useHeaderHide.ts, src/lib/useHeaderHide.test.ts, src/components/layout/Header.tsx</files>
  <action>
    Хук `useHeaderHide({ menuOpen, threshold = 80 })`: слушает `scroll` (passive) с rAF-троттлингом, хранит lastY; если `scrollY > lastY + 4 && scrollY > threshold` → hidden=true; если `scrollY < lastY - 4` или `scrollY <= threshold` → hidden=false; при `menuOpen` всегда false; возвращает boolean. Header ставит классы `is-header-hidden` и `is-menu-open` на `<header>`. Тест хука с фейковым `scrollY` и `requestAnimationFrame`: вниз на 300 → true, вверх на 100 → false, при menuOpen → false.
  </action>
  <verify><automated>npx vitest run src/lib/useHeaderHide.test.ts src/components/layout/Header.test.tsx</automated></verify>
  <acceptance_criteria>
    - `grep -c "is-header-hidden" src/components/layout/Header.tsx` ≥ 1; тест хука зелёный (≥ 3 кейса)
  </acceptance_criteria>
  <done>Шапка прячется при прокрутке вниз и возвращается при прокрутке вверх.</done>
</task>

<task type="auto">
  <name>Task 3: Бургер-крест и мобильный оверлей по оригиналу</name>
  <files>src/components/layout/BurgerButton.tsx, src/components/layout/MobileMenu.tsx, src/components/layout/Header.css, src/styles/global.css, src/styles/motionPolicy.test.ts</files>
  <action>
    BurgerButton: `<button class="burger" aria-expanded aria-controls aria-label>` с SVG `viewBox="0 0 64 28" width="64" height="28" fill="none" aria-hidden="true" focusable="false"` и тремя `<rect>`: `width=64 height=4 rx=2`, `x=6 y=12 width=52 height=4 rx=2`, `y=24 width=64 height=4 rx=2`. CSS:
    ```css
    .burger { display: grid; width: 48px; height: 48px; place-items: center; border: 0; border-radius: 0; color: rgb(248 247 251); background: transparent; -webkit-tap-highlight-color: transparent; transition: color 240ms cubic-bezier(.22,1,.36,1), opacity 240ms ease; }
    .burger:hover { color: rgb(170 217 220); }
    .burger__icon { display: block; width: 28px; height: auto; overflow: visible; }
    .burger__icon rect { fill: currentColor; transform-box: fill-box; transform-origin: center center; transition: transform 240ms cubic-bezier(.22,1,.36,1), opacity 168ms ease; }
    .is-menu-open .burger__icon rect:nth-child(1) { opacity: 0; transform: translateY(-8px) scaleX(.72); }
    .is-menu-open .burger__icon rect:nth-child(2) { transform: scaleX(1.2308) rotate(45deg); }
    .is-menu-open .burger__icon rect:nth-child(3) { transform: translateY(-12px) rotate(-45deg); }
    @media (min-width: 1024px) { .site-header__toggler { display: none; } }
    ```
    MobileMenu (разметка: `<div class="mobile-menu" role="dialog" aria-label="Меню" aria-modal отсутствует, inert когда закрыто>` → `<ul class="mobile-menu__list">` → `<li><a class="mobile-menu__link">`). CSS:
    ```css
    .mobile-menu { position: fixed; z-index: 40; inset: 0; display: flex; flex-direction: column; align-items: center; justify-content: center; height: 100svh; min-height: 0; padding: calc(72px + 1rem) clamp(24px, 7vw, 56px); overflow-y: auto; overscroll-behavior: contain; color: rgb(248 247 251 / .9); background: radial-gradient(circle at 14% 18%, rgb(158 67 154 / .22), transparent 38%), radial-gradient(circle at 88% 78%, rgb(67 139 150 / .18), transparent 42%), linear-gradient(145deg, rgb(7 2 16 / .98), rgb(22 29 61 / .97)); -webkit-backdrop-filter: blur(24px) saturate(125%); backdrop-filter: blur(24px) saturate(125%); visibility: hidden; opacity: 0; pointer-events: none; transform: translateY(-12px); transition: opacity 240ms ease, transform 240ms cubic-bezier(.22,1,.36,1), visibility 0s linear 240ms; }
    .mobile-menu.is-open { visibility: visible; opacity: 1; pointer-events: auto; transform: translateY(0); transition-delay: 0s; }
    @media (min-width: 768px) { .mobile-menu { padding-block: calc(88px + 1rem); } }
    @media (min-width: 1024px) { .mobile-menu { display: none; } }
    .mobile-menu__list { display: flex; flex-direction: column; width: min(100%, 36rem); margin: 0 auto; padding: 0; gap: 0; list-style: none; }
    .mobile-menu__list > li { border-bottom: 1px solid rgb(184 192 230 / .18); }
    .mobile-menu__list > li:first-child { border-top: 1px solid rgb(184 192 230 / .18); }
    .mobile-menu__link { position: relative; display: flex; align-items: center; min-height: 4.75rem; padding-inline: .25rem 2.5rem; color: rgb(248 247 251 / .9); font-family: var(--font-display); font-size: clamp(1.65rem, 8vw, 2.6rem); font-weight: 750; line-height: 1.08; letter-spacing: -.035em; text-decoration: none; transition: color 240ms ease, padding 240ms cubic-bezier(.22,1,.36,1); }
    .mobile-menu__link::after { content: ""; position: absolute; right: .25rem; width: 1.5rem; height: 1px; background: linear-gradient(90deg, rgb(210 142 190 / .82), rgb(84 164 172 / .76)); transform: scaleX(.42); transform-origin: right center; transition: transform 240ms cubic-bezier(.22,1,.36,1); }
    .mobile-menu__link:hover, .mobile-menu__link:focus-visible { color: rgb(231 245 245); padding-left: .75rem; }
    .mobile-menu__link:hover::after, .mobile-menu__link:focus-visible::after { transform: scaleX(1); }
    ```
    Открытие/закрытие: класс `is-open` + `inert`/`aria-hidden` как сейчас; переход visibility требует, чтобы элемент оставался в DOM (не unmount), фокус-ловушка и Esc остаются; блокировка скролла как в `scrollLock.ts`. Reduced motion: в единый блок `@media (prefers-reduced-motion: reduce)` в global.css добавить `.site-header, .site-header *, .site-header ::before, .site-header ::after, .mobile-menu, .mobile-menu * { transition-duration: .01ms !important; }`; если motionPolicy.test.ts требует регистрации, добавить `data-anim="header"` на `<header>` и в реестр теста с комментарием. Обновить тесты MobileMenu/Header (открытие через класс, крест бургера через класс на header).
  </action>
  <verify><automated>npm test && npm run lint && npm run build</automated></verify>
  <acceptance_criteria>
    - `grep -c 'viewBox="0 0 64 28"' src/components/layout/BurgerButton.tsx` = 1; `grep -c "rotate(45deg)" src/components/layout/Header.css` ≥ 1
    - `grep -c "blur(24px) saturate(125%)" src/components/layout/Header.css` ≥ 1; `grep -c "min(100%, 36rem)" src/components/layout/Header.css` ≥ 1
    - `npm test` зелёный (все файлы), `npm run lint` чист, `npm run build` без предупреждений
  </acceptance_criteria>
  <done>Бургер, оверлей и reduced motion по оригиналу, a11y сохранена.</done>
</task>

</tasks>

<verification>
- Скриншот шапки на 1440 и 1920 визуально совпадает с `docs/research`-снимком оригинала: параллелограмм, градиентная рамка, белый логотип, uppercase-пункты
- Ховер пункта меню сдвигает градиент; прокрутка вниз прячет шапку, вверх возвращает
- 390: бургер-крест, оверлей, Esc, фокус на бургере
</verification>

<output>
Create `.planning/phases/06-original-fidelity/06-01-SUMMARY.md` when done
</output>
