---
phase: 06-original-fidelity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/layout/Button.tsx
  - src/components/layout/Button.test.tsx
  - src/styles/motionPolicy.test.ts
autonomous: true
requirements: [FID-03]

must_haves:
  truths:
    - "Кнопка .btn[data-beam] повторяет оригинал: поверхность linear-gradient(125deg, rgb(108 44 104), rgb(59 77 161) 50%, rgb(57 114 126)) в padding-box и конический луч в border-box на рамке 1.5px, угол --ov-hero-beam крутится 3s linear infinite"
    - "::after кнопки — сетка точек radial-gradient .8px шагом 7px с opacity .42, замаскированная conic-gradient(from var(--ov-hero-beam) …) с той же анимацией: точки проявляются в секторе луча и гаснут за ним"
    - "::before — блик radial-gradient(circle at 20% 12%, rgb(248 234 244 / .16), transparent 34%) + linear-gradient(110deg, transparent 30%, rgb(255 255 255 / .08) 50%, transparent 70%)"
    - "Hover: поверхность светлее (signal-700/unity-600/horizon-600) и translateY(-1px); reduced motion: animation none у кнопки и ::after, точки статичны"
    - "Submit формы получает те же правила (в оригинале луч и точки есть и там), с width 100%, min-height 54px, padding 14px 28px"
  artifacts:
    - path: "src/styles/global.css"
      provides: "правила .btn[data-beam], ::before, ::after, @property, @keyframes"
      contains: "mask-image: conic-gradient(from var(--ov-hero-beam)"
---

## Phase Goal

Из ROADMAP: «…кнопка hero с точками точно по CSS оригинала».

<objective>
Заменить наш луч на кнопке точной копией правил оригинала (сняты с живого onevoice27.org 2026-09-05, переменные развёрнуты в RGB): луч в border-box, блик ::before, точки ::after под конической маской на той же переменной угла. Purpose: FID-03. Output: обновлённые правила в global.css, тесты.
</objective>

<context>
@.planning/phases/06-original-fidelity/06-CONTEXT.md
Текущее: `src/components/layout/Button.tsx` (variant primary ставит `data-beam` и `data-anim="beam"`), `.btn[data-beam]` и `@property` в `src/styles/global.css`, реестр data-anim и единый блок reduced motion там же, `src/styles/motionPolicy.test.ts`, `src/components/form/LightForm.tsx` (submit через Button primary).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Правила кнопки оригинала</name>
  <files>src/styles/global.css, src/components/layout/Button.tsx, src/components/layout/Button.test.tsx, src/styles/motionPolicy.test.ts</files>
  <action>
    В global.css заменить блок `.btn[data-beam]` (и его `::before`, если луч был на псевдоэлементе) на:
    ```css
    @property --ov-hero-beam { syntax: "<angle>"; inherits: false; initial-value: 0deg; }
    @keyframes hero-beam { to { --ov-hero-beam: 360deg; } }
    .btn[data-beam] {
      --ov-hero-beam: 0deg;
      position: relative; z-index: 0; isolation: isolate;
      display: inline-flex; align-items: center; justify-content: center;
      min-height: 52px; padding: 16px 40px; border-radius: 999px;
      border: 1.5px solid transparent;
      background:
        linear-gradient(125deg, rgb(108 44 104) 0%, rgb(59 77 161) 50%, rgb(57 114 126) 100%) padding-box,
        conic-gradient(from var(--ov-hero-beam), transparent 0deg 238deg, rgb(170 217 220) 270deg, rgb(248 234 244) 294deg, white 304deg, rgb(240 211 231) 316deg, transparent 348deg 360deg) border-box;
      box-shadow: inset 0 0 0 1px rgb(217 222 244 / .34), 0 10px 30px rgb(59 77 161 / .34), 0 2px 10px rgb(3 2 18 / .28);
      color: rgb(248 247 251);
      font-family: var(--font-body); font-size: .875rem; font-weight: 700; line-height: 1.2; letter-spacing: .08em; text-transform: uppercase; text-decoration: none;
      overflow: hidden; transform: translateY(0);
      animation: hero-beam 3s linear infinite;
      transition: box-shadow 320ms, transform 320ms cubic-bezier(.22,1,.36,1);
    }
    .btn[data-beam]::before, .btn[data-beam]::after { content: ""; position: absolute; border-radius: inherit; pointer-events: none; }
    .btn[data-beam]::before { inset: 1px; z-index: 0; background: radial-gradient(circle at 20% 12%, rgb(248 234 244 / .16), transparent 34%), linear-gradient(110deg, transparent 30%, rgb(255 255 255 / .08) 50%, transparent 70%); }
    .btn[data-beam]::after {
      inset: 2px; z-index: 1; opacity: .42;
      background-image: radial-gradient(circle, rgb(248 247 251 / .72) .8px, transparent 1.1px);
      background-size: 7px 7px;
      -webkit-mask-image: conic-gradient(from var(--ov-hero-beam), transparent 0deg 242deg, rgb(0 0 0 / .18) 260deg, black 286deg 316deg, rgb(0 0 0 / .18) 338deg, transparent 356deg 360deg);
      mask-image: conic-gradient(from var(--ov-hero-beam), transparent 0deg 242deg, rgb(0 0 0 / .18) 260deg, black 286deg 316deg, rgb(0 0 0 / .18) 338deg, transparent 356deg 360deg);
      animation: hero-beam 3s linear infinite;
    }
    .btn[data-beam] > * { position: relative; z-index: 2; }
    .btn[data-beam]:hover, .btn[data-beam]:focus-visible {
      background:
        linear-gradient(125deg, rgb(132 53 127) 0%, rgb(79 93 175) 50%, rgb(67 139 150) 100%) padding-box,
        conic-gradient(from var(--ov-hero-beam), transparent 0deg 238deg, rgb(170 217 220) 270deg, rgb(248 234 244) 294deg, white 304deg, rgb(240 211 231) 316deg, transparent 348deg 360deg) border-box;
      transform: translateY(-1px);
      box-shadow: inset 0 0 0 1px rgb(217 222 244 / .34), 0 14px 34px rgb(59 77 161 / .40), 0 2px 10px rgb(3 2 18 / .28);
    }
    .btn[data-beam][data-size="form"] { width: 100%; min-height: 54px; padding: 14px 28px; }
    ```
    Псевдоэлемент `::after` перекрывает текст — поэтому обёртка текста внутри кнопки (span в Button.tsx) получает `position: relative; z-index: 2`. В едином блоке reduced motion заменить прежнее гашение луча на `.btn[data-beam], .btn[data-beam]::after { animation: none; }` (точки остаются статично в секторе 286–316deg от 0deg). Если старая @property называлась иначе — удалить, оставить одну. LightForm submit: передать `data-size="form"` через проп Button (добавить проп `size?: "form"`). Обновить Button.test.tsx (data-beam и обёртка текста), motionPolicy.test.ts (если он ищет старое имя keyframes/переменной). Проверить в jsdom, что `getComputedStyle` не требуется; тест CSS-текста: `global.css` содержит `mask-image: conic-gradient(from var(--ov-hero-beam)` и `background-size: 7px 7px`.
  </action>
  <verify><automated>npm test && npm run lint && npm run build && node scripts/check-dist.mjs</automated></verify>
  <acceptance_criteria>
    - `grep -c "mask-image: conic-gradient(from var(--ov-hero-beam)" src/styles/global.css` ≥ 1; `grep -c "background-size: 7px 7px" src/styles/global.css` = 1; `grep -c "@property --ov-hero-beam" src/styles/global.css` = 1
    - `npm test` зелёный, lint чист, build без предупреждений, `check:dist` OK
  </acceptance_criteria>
  <done>Кнопка hero и submit формы светят лучом и точками как оригинал.</done>
</task>

</tasks>

<verification>
- В браузере на 1440: точки на кнопке появляются в секторе за лучом и гаснут, период 3s; при reduced motion статичны
</verification>

<output>
Create `.planning/phases/06-original-fidelity/06-02-SUMMARY.md` when done
</output>
