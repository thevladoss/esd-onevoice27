---
phase: 02-hero-and-map
plan: 03
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/data/copy.map.ts
  - src/components/map/EsdMap.tsx
  - src/components/map/EsdMap.test.tsx
  - src/components/map/map.css
  - src/components/map/Counters.tsx
  - src/components/map/Counters.test.tsx
  - src/components/map/MapSection.tsx
  - src/components/map/MapSection.test.tsx
  - src/main.tsx
  - src/App.test.tsx
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]
must_haves:
  truths:
    - "Посетитель видит секцию #map со скошенными верхним и нижним краями, надзаголовком «Все вместе» и H2 «Зажигаем свет по всему дивизиону»"
    - "На SVG-карте 12 стран ЕАД подсвечены на фоне соседних стран, Россия не рвётся на антимеридиане"
    - "На карте 694 маджентовых и 248 бирюзовых огонька с гало, пульсируют не более 40, ни один не использует filter"
    - "Счётчики «ЧЕЛОВЕК» и «ГРУПП» показывают значения из состояния огоньков с разделителем тысяч U+202F"
    - "При пустом списке огоньков виден блок «Пока ни одного огонька», при размере контейнера 0×0 виден блок ошибки role=status"
  artifacts:
    - path: src/components/map/EsdMap.tsx
      provides: "SVG-карта: проекция, страны, огоньки, пустое и ошибочное состояния"
      contains: "role=\"img\""
    - path: src/components/map/Counters.tsx
      provides: "две стеклянные карточки со значениями из useLights"
      contains: "aria-live=\"polite\""
    - path: src/components/map/MapSection.tsx
      provides: "секция #map со скосами, заголовком, счётчиками и картой"
      contains: "id=\"map\""
    - path: src/components/map/map.css
      provides: "стили секции, карты, огоньков, счётчиков, keyframes light-pulse и light-arrive"
      contains: "@keyframes light-pulse"
    - path: src/data/copy.map.ts
      provides: "тексты секции карты"
      exports: [mapCopy]
    - path: src/main.tsx
      provides: "LightsProvider вокруг App"
      contains: "<LightsProvider"
  key_links:
    - from: src/components/map/EsdMap.tsx
      to: src/lib/geo.ts
      via: "makeProjection, worldFeatures, isEsd"
      pattern: "makeProjection\\("
    - from: src/components/map/MapSection.tsx
      to: src/state/lights.tsx
      via: "useLights().lights → EsdMap"
      pattern: "useLights\\("
    - from: src/components/map/Counters.tsx
      to: src/lib/format.ts
      via: "formatCount(counts.people)"
      pattern: "formatCount\\("
    - from: src/main.tsx
      to: src/state/lights.tsx
      via: "<LightsProvider><App /></LightsProvider>"
      pattern: "LightsProvider"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** увидеть эффектный hero с вращающимся глобусом и кнопкой «Зажечь свой свет», а ниже живую карту ЕАД с огоньками, счётчиками и переключением стран, **so that** почувствовать масштаб движения по дивизиону и перейти к форме, чтобы зажечь свой свет.

Этот план даёт первый видимый срез карты: посетитель видит секцию со скосами, 12 подсвеченных стран, 942 огонька и счётчики с реальными числами. Зум, чипы и count-up добавляют планы 02-04 и 02-05 поверх этого же кода.

<objective>
Заменить заглушку `src/components/map/MapSection.tsx` на секцию карты: скошенные края, заголовок, статичная SVG-карта ЕАД на d3-geo с огоньками из `LightsProvider`, стеклянные счётчики с форматированными значениями, пустое и ошибочное состояния. Обернуть `App` в `LightsProvider` в `main.tsx`.

Purpose: карта это Core Value проекта и самый рискованный компонент; статичный срез сразу проверяет проекцию, антимеридиан и производительность 942 `<circle>` на реальном билде.
Output: `EsdMap.tsx`, `Counters.tsx`, `MapSection.tsx`, `map.css`, `copy.map.ts`, тесты, правка `main.tsx` и `App.test.tsx`, зелёные `npm test` и `npm run build`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/02-hero-and-map/02-UI-SPEC.md
@.planning/phases/02-hero-and-map/02-RESEARCH.md
@.planning/phases/02-hero-and-map/02-02-SUMMARY.md
@.planning/research/PITFALLS.md
@src/App.tsx
@src/App.test.tsx
@src/main.tsx
@src/components/map/MapSection.tsx
@src/components/layout/Eyebrow.tsx
@src/components/layout/GradientTitle.tsx
@src/lib/geo.ts
@src/data/lights.ts
@src/data/countries.ts
@src/state/lights.tsx
@src/lib/format.ts
@src/test/setup.ts

<interfaces>
<!-- Из плана 02-02 (уже в кодовой базе). Использовать напрямую, не исследовать. -->

```typescript
// src/lib/geo.ts
export type CountryFeature = Feature<Geometry, { name: string }>; // feature.id строка вида "004"; сравнивать через Number(f.id)
export const worldFeatures: CountryFeature[];   // 177
export const esdFeatures: CountryFeature[];     // 12
export function featureById(id: number): CountryFeature | undefined;
export function isEsd(id: number): boolean;
export function makeProjection(width: number, height: number, pad?: number): GeoProjection;

// src/data/lights.ts
export type LightType = "person" | "group";
export interface Light { id: string; type: LightType; countryId: number; lon: number; lat: number; isNew?: boolean }
export function generateLights(seed?: number, people?: number, groups?: number): Light[];

// src/state/lights.tsx
export function LightsProvider(props: { children: ReactNode; initialLights?: Light[] }): JSX.Element;
export function useLights(): { lights: Light[]; counts: { people: number; groups: number }; addLight: (input: { type: LightType; countryId: number }) => void };

// src/lib/format.ts
export function formatCount(n: number): string; // "4\u202F268"
```

Контракт этого плана (потребляют 02-04 и 02-05):
```typescript
// src/data/copy.map.ts
export const mapCopy = {
  eyebrow: "Все вместе",
  title: "Зажигаем свет по всему дивизиону",
  mapTitle: "Карта Евро-Азиатского дивизиона",
  counters: { people: "ЧЕЛОВЕК", groups: "ГРУПП" },
  chips: { all: "Весь дивизион", groupLabel: "Показать страну на карте" },
  hint: { pointer: "Масштаб: ⌘ / Ctrl + колесо. Сдвиг: перетаскивание", touch: "Два пальца: масштаб и сдвиг" },
  srDescription: (people: number, groups: number) => `Карта Евро-Азиатского дивизиона: ${people} огоньков людей и ${groups} групповых маяков в 12 странах.`,
  empty: { title: "Пока ни одного огонька", bodyBefore: "Зажгите первый свет: заполните ", link: "форму ниже", bodyAfter: ".", linkHref: "#light-form" },
  error: "Карта не загрузилась. Обновите страницу, чтобы попробовать снова.",
} as const;

// src/components/map/EsdMap.tsx
export interface EsdMapProps {
  lights: readonly Light[];
  selectedCountryId?: number | null;      // подсветка страны здесь; полёт камеры добавляет 02-04
  onUserZoomAway?: () => void;            // реализует 02-04 (ручной зум > 15% от масштаба выбранной страны)
  onError?: (hasError: boolean) => void;  // MapSection отключает чипы при ошибке
  size?: { width: number; height: number }; // для тестов; без него размер берётся из getBoundingClientRect + ResizeObserver
}
export const LIGHT_CORE_RADIUS = 2.2;
export const LIGHT_HALO_RADIUS = 6;
export const PULSE_EVERY = 24;
export function EsdMap(props: EsdMapProps): JSX.Element;

// src/components/map/Counters.tsx
export function Counters(): JSX.Element; // читает useLights(), пропсов нет (фаза 3 рендерит его рядом с формой внутри LightsProvider)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: статичная SVG-карта ЕАД с огоньками, пустым и ошибочным состояниями (MAP-02, MAP-03)</name>
  <files>src/data/copy.map.ts, src/components/map/EsdMap.tsx, src/components/map/EsdMap.test.tsx, src/components/map/map.css</files>
  <read_first>
    - src/lib/geo.ts, src/data/lights.ts, src/data/countries.ts (реальные экспорты 02-02)
    - src/test/setup.ts (моки ResizeObserver и getBoundingClientRect: в jsdom размер 0×0, поэтому тесты передают `size`)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md: «Карта: геометрия и стили», «Слои секции карты», «Accessibility Contract» (SVG-карта, огоньки), «States» (Пустое, Ошибка карты), «Copywriting Contract» (полный список строк), «Motion Contract» (пульсация, новый огонёк)
    - .planning/phases/02-hero-and-map/02-CONTEXT.md: «Секция карты → EsdMap», «Огоньки»
    - .planning/research/PITFALLS.md, Pitfall 6 (без filter, ≤40 анимированных)
  </read_first>
  <behavior>
    - Test 1: `render(<EsdMap lights={generateLights()} size={{ width: 1200, height: 700 }} />)`: `getByRole("img", { name: "Карта Евро-Азиатского дивизиона" })` существует
    - Test 2: `path.country` ровно 177, `path[data-esd="true"]` ровно 12, и множество их `data-country-id` равно 12 id ЕАД
    - Test 3: `.light-core` ровно 942, `.light--person` 694, `.light--group` 248, `.light-halo` 942
    - Test 4: `.light.pulse` от 1 до 40 включительно для сгенерированных огоньков, плюс один при добавленном `isNew`-огоньке (рендер с провайдером после `addLight`); `querySelectorAll("[filter]").length === 0`; ни у одного `.light-core` в `style` нет `filter`
    - Test 5: скрытое описание содержит текст «694 огоньков людей и 248 групповых маяков»; `svg` имеет `aria-describedby`, указывающий на его id
    - Test 6: `lights={[]}` рендерит «Пока ни одного огонька» и ссылку «форму ниже» с `href` на `#light-form`; страны при этом остаются (`path.country` 177)
    - Test 7: `size={{ width: 0, height: 0 }}` рендерит `getByRole("status")` с текстом «Карта не загрузилась. Обновите страницу, чтобы попробовать снова.», `svg` отсутствует, `onError` вызван с `true`
    - Test 8: `selectedCountryId={643}` даёт ровно один `path[data-selected="true"]` с `data-country-id="643"` и классом `country--selected`
  </behavior>
  <action>
    Сначала `EsdMap.test.tsx` (красный), затем реализация.

    `src/data/copy.map.ts`: `mapCopy` ровно по контракту `<interfaces>` (тексты из UI-SPEC Copywriting Contract). Фаза 1 держит свои строки в `src/data/copy.ts`; его не трогать.

    `src/components/map/EsdMap.tsx`:
    Размер: `containerRef` на `<div className="esd-map">`; state `size: {width, height} | null`; если передан `props.size`, использовать его; иначе в `useLayoutEffect` взять `getBoundingClientRect()` и подписать `ResizeObserver` (если определён) на контейнер, обновлять state при изменении; cleanup `disconnect()`.
    Проекция: `useMemo(() => makeProjection(width, height), [width, height])`, `path = geoPath(projection)` из `d3-geo`. Ошибка (UI-SPEC State «Ошибка карты»): `width < 1 || height < 1`, либо `projection([37.6, 55.7])` вернул `null`/не конечные числа. Статус сообщать через `useEffect(() => onError?.(hasError), [hasError])`.
    Пути стран: `useMemo(() => worldFeatures.map(f => ({ id: Number(f.id), d: path(f) ?? "" })), [path])`.
    Позиции огоньков: `useMemo` над `lights` → `projection([lon, lat])`; точки с `null` пропускать.
    Разметка при готовности: `<div className="esd-map" ref>` → `<svg className="esd-map__svg" role="img" aria-describedby={descId} viewBox={\`0 0 ${width} ${height}\`} width="100%" height="100%" preserveAspectRatio="xMidYMid meet">` → `<title>{mapCopy.mapTitle}</title>` → `<g className="map-viewport">` (transform подключит 02-04) → `<g className="map-countries" aria-hidden="true">` с `<path key d className={"country" + (isEsd ? " country--esd" : "") + (selected ? " country--selected" : "")} data-country-id={id} data-esd={isEsd || undefined} data-selected={selected || undefined} vectorEffect="non-scaling-stroke" />` → `<g className="map-lights" aria-hidden="true">` с элементом на каждый огонёк: `<g key={light.id} className={"light light--" + type + (pulse ? " pulse" : "") + (isNew ? " is-new" : "")} style={pulse ? { animationDelay: \`${((i / PULSE_EVERY) % 12) * 200}ms\` } : undefined}>` содержит `<circle className="light-halo" cx cy r={LIGHT_HALO_RADIUS / scale} />`, `<circle className="light-core" cx cy r={LIGHT_CORE_RADIUS / scale} />` и при `isNew` третий `<circle className="light-ring" cx cy r={LIGHT_HALO_RADIUS / scale} />`. `pulse = light.isNew || i % PULSE_EVERY === 0` (942 / 24 даёт 40 штук из сгенерированных; новый огонёк из формы всегда пульсирует, как требует 02-CONTEXT). Переменная `scale` объявляется как масштаб viewport и в этом плане равна 1; 02-04 заменит её на `transform.k` d3-zoom, поэтому радиусы уже делятся на неё. Никаких `filter` и SVG-фильтров на огоньках (Pitfall 6).
    Скрытое описание: `<p id={descId} className="sr-only">{mapCopy.srDescription(people, groups)}</p>` где `people`/`groups` считаются по `lights`; `descId` через `useId()`.
    Пустое состояние: при `lights.length === 0` поверх svg `<div className="map-empty"><p className="map-empty__title">{mapCopy.empty.title}</p><p className="map-empty__body">{bodyBefore}<a href={linkHref}>{link}</a>{bodyAfter}</p></div>`.
    Ошибка: вместо svg `<div role="status" className="map-error">{mapCopy.error}</div>` на всю высоту контейнера.
    Импорт `./map.css` в EsdMap.tsx.

    `src/components/map/map.css` (обычный CSS; цвета литералами из UI-SPEC, см. примечание о Tailwind v4 в 02-01):
    `.map-section { --light-person: #d28ebe; --light-group: #7bc2c7; }` (семантические алиасы из UI-SPEC живут здесь, а не в tokens.css, чтобы не трогать файл фазы 1).
    `.esd-map { position: relative; width: 100%; height: 100%; }`, `.esd-map__svg { display: block; width: 100%; height: 100%; }`.
    `.country { fill: rgb(33 26 62 / .55); stroke: rgb(184 192 230 / .28); stroke-width: 0.6px; }`, `.country--esd { fill: rgb(59 77 161 / .55); }`, `.country--selected { fill: rgb(59 77 161 / .78); stroke: rgb(170 217 220 / .55); stroke-width: 1px; }`.
    `.light--person { color: var(--light-person); } .light--group { color: var(--light-group); }`, `.light-core { fill: currentColor; opacity: .9; }`, `.light-halo { fill: currentColor; opacity: .22; }`, `.light-ring { fill: none; stroke: currentColor; stroke-width: 1px; opacity: .5; }`.
    `.light.pulse { transform-box: fill-box; transform-origin: center; animation: light-pulse 2.4s ease-in-out infinite; }`, `@keyframes light-pulse { 0%, 100% { transform: scale(1); opacity: .9; } 50% { transform: scale(1.8); opacity: .45; } }`.
    `.light.is-new .light-ring { transform-box: fill-box; transform-origin: center; animation: light-arrive 900ms cubic-bezier(0.16, 1, 0.3, 1) forwards; }`, `@keyframes light-arrive { from { transform: scale(1); opacity: .5; } to { transform: scale(3.4); opacity: 0; } }`.
    `.map-empty { position: absolute; inset: 0; display: grid; place-content: center; text-align: center; gap: 8px; padding: 16px; pointer-events: none; } .map-empty a { pointer-events: auto; }`; заголовок Onest 900 20px paper, тело 16px `rgb(248 247 251 / .82)`, ссылка `#aad9dc` с подчёркиванием и `focus-visible` кольцом `2px solid #aad9dc`.
    `.map-error { display: grid; place-items: center; height: 100%; padding: 16px; text-align: center; font-size: 16px; color: rgb(248 247 251 / .86); }`.
    `@media (prefers-reduced-motion: reduce) { .light.pulse { animation: none; opacity: .9; } .light.is-new .light-ring { display: none; } }`.
    Стили секции, контейнера и счётчиков добавят Task 2 и Task 3 в этот же файл.
  </action>
  <verify>
    <automated>npm test -- src/components/map/EsdMap.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/copy.map.ts` содержит `export const mapCopy`, `"Все вместе"`, `"Зажигаем свет по всему дивизиону"`, `"Карта не загрузилась. Обновите страницу, чтобы попробовать снова."`, `"Пока ни одного огонька"`
    - `src/components/map/EsdMap.tsx` содержит `role="img"`, `<title>`, `aria-describedby`, `makeProjection(`, `geoPath(`, `vectorEffect="non-scaling-stroke"`, `data-country-id`, `data-esd`, `data-selected`, `className="map-viewport"`, `PULSE_EVERY`, `LIGHT_CORE_RADIUS`, `LIGHT_HALO_RADIUS`, `role="status"`, `ResizeObserver`, `onError`
    - `grep -c "filter" src/components/map/EsdMap.tsx` выводит 0
    - `src/components/map/map.css` содержит `@keyframes light-pulse`, `@keyframes light-arrive`, `transform-box: fill-box`, `rgb(59 77 161 / .55)`, `rgb(33 26 62 / .55)`, `stroke-width: 0.6px`, `--light-person: #d28ebe`, `--light-group: #7bc2c7`, `prefers-reduced-motion: reduce`
    - `npm test -- src/components/map/EsdMap.test.tsx` завершается с кодом 0, не меньше 8 тестов
  </acceptance_criteria>
  <done>SVG-карта рисует 177 стран с 12 подсвеченными, 942 огонька двух цветов с гало, 40 пульсирующих, без filter; пустое и ошибочное состояния и доступное описание покрыты тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: стеклянные счётчики со значениями из контекста (MAP-04, форматирование)</name>
  <files>src/components/map/Counters.tsx, src/components/map/Counters.test.tsx, src/components/map/map.css</files>
  <read_first>
    - src/state/lights.tsx, src/lib/format.ts (экспорты 02-02)
    - src/components/map/map.css (результат Task 1, дописывать в конец)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md, раздел «Счётчики» (таблица) и строка «Счётчик» в Accessibility Contract, «Typography» (число счётчика)
    - .planning/phases/02-hero-and-map/02-CONTEXT.md, пункт «Счётчики (MAP-04)»
    - .planning/phases/03-form-about-involve/03-CONTEXT.md, строка про тест формы с `Counters` рядом (Counters читает контекст сам)
  </read_first>
  <behavior>
    - Test 1: `<LightsProvider initialLights={generateLights(27, 1150, 12)}><Counters /></LightsProvider>` показывает текст `"1\u202F150"` и текст `"12"`
    - Test 2: видны подписи «ЧЕЛОВЕК» и «ГРУПП»
    - Test 3: ровно два элемента с `aria-live="polite"`, их textContent равен форматированным значениям
    - Test 4: карточки имеют классы `counter--people` и `counter--groups`
  </behavior>
  <action>
    Сначала `Counters.test.tsx` (красный), затем реализация.

    `src/components/map/Counters.tsx`: без пропсов; `const { counts } = useLights()`. Разметка: `<div className="counters">` с двумя `<article className="counter counter--people">` и `counter--groups`; внутри `<p className="counter__label"><span className="counter__dot" aria-hidden="true" />{mapCopy.counters.people}</p>` и `<p className="counter__value"><span aria-live="polite">{formatCount(counts.people)}</span></p>`. Число это текст, не canvas. План 02-05 заменит прямой вывод на count-up, не меняя разметку подписи и классы.

    `map.css`, дописать: `.counter { width: 240px; min-height: 112px; padding: 16px; border-radius: 16px; border: 1px solid rgb(184 192 230 / .22); box-shadow: 0 30px 72px rgb(18 12 52 / .62), 0 10px 24px rgb(18 12 52 / .42), inset 0 1px 0 rgb(248 247 251 / .08); color: #f8f7fb; }` (без backdrop-filter по UI-SPEC). `.counter--people { background: radial-gradient(circle at 12% 0%, rgb(158 67 154 / .16), transparent 52%), linear-gradient(145deg, rgb(48 63 131 / .86), rgb(7 2 16 / .76)); --counter-accent: var(--light-person); }`, `.counter--groups { background: radial-gradient(circle at 88% 0%, rgb(84 164 172 / .15), transparent 52%), linear-gradient(145deg, rgb(48 63 131 / .82), rgb(7 2 16 / .76)); --counter-accent: var(--light-group); }`. `.counter__label { display: flex; align-items: center; gap: 4px; margin: 0; font-family: "Noto Sans", sans-serif; font-weight: 700; font-size: 12px; line-height: 1.4; letter-spacing: .18em; text-transform: uppercase; color: rgb(248 247 251 / .8); }`, `.counter__dot { width: 6px; height: 6px; border-radius: 50%; background: var(--counter-accent); color: var(--counter-accent); box-shadow: 0 0 8px currentColor; }`, `.counter__value { margin: 12px 0 0; font-family: "Onest", "Noto Sans", sans-serif; font-weight: 900; font-size: clamp(3.5rem, 9vw, 4.5rem); line-height: 1.05; letter-spacing: -0.02em; font-variant-numeric: tabular-nums; color: var(--counter-accent); }`.
    Раскладка: до 768px `.counters { display: flex; gap: 12px; margin-bottom: 16px; } .counters .counter { flex: 1; width: auto; min-height: 96px; }`; `@media (min-width: 768px) { .counters { position: absolute; top: 88px; left: 50%; transform: translateX(-50%); z-index: 3; display: flex; flex-direction: row; gap: 16px; margin: 0; } .counters .counter { flex: none; width: 240px; min-height: 112px; } }`. Контейнер `.map-stage` с `position: relative` создаст Task 3.
  </action>
  <verify>
    <automated>npm test -- src/components/map/Counters.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/map/Counters.tsx` содержит `useLights(`, `formatCount(`, `aria-live="polite"`, `counter--people`, `counter--groups`, `mapCopy.counters.people`, `mapCopy.counters.groups`
    - `src/components/map/map.css` содержит `.counter {`, `top: 88px`, `width: 240px`, `clamp(3.5rem, 9vw, 4.5rem)`, `tabular-nums`, `letter-spacing: .18em`, `box-shadow: 0 0 8px currentColor`
    - `grep -c "backdrop-filter" src/components/map/map.css` выводит 0
    - `npm test -- src/components/map/Counters.test.tsx` завершается с кодом 0, не меньше 4 тестов
  </acceptance_criteria>
  <done>Счётчики читают контекст, показывают отформатированные значения с U+202F в двух стеклянных карточках, разметка готова к count-up из 02-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: секция #map со скосами, провайдер в main.tsx и полный прогон (MAP-01)</name>
  <files>src/components/map/MapSection.tsx, src/components/map/MapSection.test.tsx, src/components/map/map.css, src/main.tsx, src/App.test.tsx</files>
  <read_first>
    - src/App.tsx (как импортируется MapSection: сохранить вид экспорта; App.tsx не редактировать)
    - src/App.test.tsx (рендерит ли `<App />` без провайдера; после этого плана Hero и MapSection живут под `LightsProvider`, MapSection читает контекст)
    - src/main.tsx (структура: StrictMode, createRoot)
    - src/components/map/MapSection.tsx (заглушка фазы 1, заменяется целиком)
    - src/components/layout/Eyebrow.tsx, src/components/layout/GradientTitle.tsx (пропсы)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md: «Секция карты» (таблица), «Слои секции карты (z-index)», «Брейкпоинты»
    - .planning/phases/02-hero-and-map/02-CONTEXT.md: «Оболочка #map», «Контейнер карты»
    - .planning/research/PITFALLS.md, «Скошенные секции оставляют дырки фона»
  </read_first>
  <behavior>
    - Test 1 (MapSection.test.tsx): `<LightsProvider><MapSection /></LightsProvider>` рендерит `section#map`, текст «Все вместе» и `getByRole("heading", { level: 2 })` с текстом «Зажигаем свет по всему дивизиону»
    - Test 2: видны подписи счётчиков «ЧЕЛОВЕК» и «ГРУПП» и число `"694"`
    - Test 3: в jsdom контейнер имеет размер 0×0, поэтому виден `getByRole("status")` с текстом ошибки карты (фиксирует состояние ошибки на уровне секции)
    - Test 4: внутренний слой со скосами присутствует (`.map-section__skew`)
    - Test 5 (App.test.tsx): существующие проверки фазы 1 (восемь секций по id) проходят при рендере под `LightsProvider`
  </behavior>
  <action>
    Сначала `MapSection.test.tsx` (красный), затем реализация.

    `src/components/map/MapSection.tsx`: `const { lights } = useLights()`. Разметка: `<section id="map" className="map-section">` → `<div className="map-section__skew">` → `<div className="map-section__inner">` (контейнер `max-width: 72rem; margin-inline: auto; padding: 64px 16px` и `96px 32px` от 768px, как у примитива `Section`; сам `Section` не используется, потому что он рендерит `<section id>`, а якорь `#map` должен стоять на внешнем элементе секции) → `<header className="map-section__header">` с `<Eyebrow>{mapCopy.eyebrow}</Eyebrow>` и `<GradientTitle as="h2" variant="section">{mapCopy.title}</GradientTitle>` → `<div className="map-stage">` с `<Counters />` и `<div className="map-container"><EsdMap lights={lights} /></div>`. Ряд чипов и подсказку добавит 02-04 в эту же разметку. Импорт `./map.css`. Экспорт того же вида, что ждёт `src/App.tsx`.

    `map.css`, дописать: `.map-section { position: relative; background: #070210; color: #f8f7fb; overflow: hidden; background-image: radial-gradient(circle at 50% -42%, rgb(48 63 131 / .24), transparent 46%), radial-gradient(circle at 88% -22%, rgb(84 164 172 / .2), transparent 40%); }`. `.map-section__skew { clip-path: polygon(0 46px, 100% 0, 100% calc(100% - 46px), 0 100%); background: #070210; }` (фон родителя тот же тёмный, дырок на скосах нет). `.map-section__header { text-align: center; max-width: 42rem; margin-inline: auto; display: grid; gap: 24px; }`. `.map-stage { position: relative; margin-top: 32px; }`. `.map-container { position: relative; height: clamp(520px, 70vh, 880px); touch-action: pan-y; cursor: grab; overflow: hidden; border-radius: 16px; } .map-container.is-dragging { cursor: grabbing; }`. `@media (min-width: 768px) { .map-section__inner { padding: 96px 32px; } }`.

    `src/main.tsx`: импортировать `LightsProvider` из `./state/lights` и обернуть `<App />` внутри `StrictMode`: `<StrictMode><LightsProvider><App /></LightsProvider></StrictMode>`. Это единственная правка вне папки `components/map` кроме `App.test.tsx`.

    `src/App.test.tsx`: если тест рендерит `<App />` напрямую, обернуть рендер в `<LightsProvider>` (иначе `useLights()` в MapSection бросит ошибку). Остальные проверки фазы 1 не менять.

    Затем `npm test` (весь набор) и `npm run build`.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/map/MapSection.tsx` содержит `id="map"`, `className="map-section"`, `map-section__skew`, `map-stage`, `map-container`, `<Counters`, `<EsdMap`, `useLights(`, `variant="section"`, `mapCopy.eyebrow`, `mapCopy.title`
    - `src/components/map/map.css` содержит `clip-path: polygon(0 46px, 100% 0, 100% calc(100% - 46px), 0 100%)`, `height: clamp(520px, 70vh, 880px)`, `touch-action: pan-y`, `cursor: grab`, `cursor: grabbing`
    - `src/main.tsx` содержит `<LightsProvider>` и `</LightsProvider>`; `src/App.tsx` не изменён (`git diff --quiet HEAD -- src/App.tsx` выходит с кодом 0)
    - `grep -c "Все вместе\|Зажигаем свет" src/components/map/MapSection.tsx` выводит 0
    - `npm test` завершается с кодом 0 (в том числе `App.test.tsx` и `MapSection.test.tsx` с не меньше 4 тестами)
    - `npm run build` завершается с кодом 0
  </acceptance_criteria>
  <done>Секция #map со скосами, заголовком, счётчиками и картой заменила заглушку; App работает под LightsProvider; весь набор тестов и билд зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Бандл world-atlas → SVG `d` | Геометрия генерируется d3-geo из данных билда, пользовательского ввода нет |
| `LightsProvider` → `EsdMap`/`Counters` | Данные из памяти страницы; в фазе 3 сюда приходит ввод формы как `{ type, countryId }` |
| Ссылка в пустом состоянии → `#light-form` | Внутренний якорь |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | EsdMap рендер путей и текста | mitigate | Только React-атрибуты и текстовые узлы, без `dangerouslySetInnerHTML` (`grep -r dangerouslySetInnerHTML src/components/map` пуст) |
| T-02-08 | Denial of Service | 942 `<circle>` × 2 и пульсация | mitigate | Анимируются только 40 групп (`PULSE_EVERY = 24`), `filter` запрещён, при reduced motion анимация снята |
| T-02-09 | Information Disclosure | srDescription с числами | accept | Агрегаты синтетических данных |
| T-02-10 | Denial of Service | ResizeObserver без размера | mitigate | При 0×0 показывается блок ошибки, проекция не считается, `onError(true)` отключает чипы в 02-04 |
| T-02-SC | Tampering | npm installs | accept | Новых пакетов план не ставит; зависимости проверены в 02-02 |
</threat_model>

<verification>
- `npm test` зелёный, включая `EsdMap.test.tsx`, `Counters.test.tsx`, `MapSection.test.tsx`, `App.test.tsx`.
- `npm run build` собирает бандл с JSON world-atlas (ожидаемо около 40 КБ gzip дополнительно).
- Визуальная проверка (не блокирующая): `npm run preview`, на 1440px карта заполняет контейнер, Россия целая от Калининграда до Чукотки, огоньки внутри границ, две карточки счётчиков поверх карты сверху по центру; на 390px карточки строкой над картой. Playwright smoke выполняет фаза 5.
</verification>

<success_criteria>
- MAP-01: секция со скосами 46px, eyebrow «Все вместе», H2 «Зажигаем свет по всему дивизиону» через примитивы фазы 1.
- MAP-02: 12 стран ЕАД подсвечены `rgb(59 77 161 / .55)` на фоне `rgb(33 26 62 / .55)`, проекция `rotate([-90, 0])` без разрыва России.
- MAP-03: 694 маджентовых и 248 бирюзовых огоньков с гало, пульсируют 40, filter отсутствует.
- MAP-04 (значения и формат): счётчики показывают числа из `useLights().counts` через `formatCount`; count-up добавляет 02-05.
- `App.tsx` не изменён; `LightsProvider` оборачивает `App` в `main.tsx`.
</success_criteria>

<output>
Создать `.planning/phases/02-hero-and-map/02-03-SUMMARY.md` после выполнения.
</output>
