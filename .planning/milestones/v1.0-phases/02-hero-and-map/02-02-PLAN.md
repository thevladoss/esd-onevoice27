---
phase: 02-hero-and-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types/world-atlas.d.ts
  - src/data/countries.ts
  - src/lib/rng.ts
  - src/lib/rng.test.ts
  - src/lib/geo.ts
  - src/lib/geo.test.ts
  - src/data/lights.ts
  - src/data/lights.test.ts
  - src/lib/format.ts
  - src/lib/format.test.ts
  - src/lib/easing.ts
  - src/lib/easing.test.ts
  - src/lib/useReducedMotion.ts
  - src/lib/useReducedMotion.test.tsx
  - src/state/lights.tsx
  - src/state/lights.reducer.test.tsx
autonomous: true
requirements: [QA-01, MAP-02, MAP-03, MAP-04]
must_haves:
  truths:
    - "Vitest подтверждает детерминизм генератора огоньков: два вызова generateLights(27) дают одинаковый массив из 694 person и 248 group, каждая точка внутри своей страны по geoContains"
    - "Vitest подтверждает форматирование чисел: 694 → «694», 4268 → «4 268» (U+202F), 1150 → «1 150»"
    - "Vitest подтверждает редьюсер огоньков: add увеличивает нужный счётчик на 1, id уникальны, координата внутри выбранной страны"
    - "Проекция geoMercator().rotate([-90, 0]) с fitExtent по 12 странам ЕАД проецирует все центры стран внутрь вьюбокса, а точки по обе стороны 180° ложатся рядом (Чукотка не рвётся)"
  artifacts:
    - path: src/data/countries.ts
      provides: "12 стран ЕАД: id, code, name, weight, center"
      exports: [ESD_COUNTRIES, ESD_IDS, countryById]
    - path: src/lib/geo.ts
      provides: "world-atlas → GeoJSON, проекция, rejection sampling"
      exports: [worldFeatures, esdFeatures, esdCollection, featureById, isEsd, makeProjection, randomPointIn]
    - path: src/data/lights.ts
      provides: "детерминированный генератор огоньков"
      exports: [generateLights, allocateByWeight]
    - path: src/lib/rng.ts
      provides: "mulberry32"
      exports: [mulberry32]
    - path: src/lib/format.ts
      provides: "formatCount с U+202F"
      exports: [formatCount]
    - path: src/lib/easing.ts
      provides: "easeOutCubic, easeOutQuint"
      exports: [easeOutCubic, easeOutQuint]
    - path: src/lib/useReducedMotion.ts
      provides: "хук и синхронное чтение prefers-reduced-motion"
      exports: [usePrefersReducedMotion, prefersReducedMotion, REDUCED_MOTION_QUERY]
    - path: src/state/lights.tsx
      provides: "LightsProvider, useLights, lightsReducer, createLight, countLights"
      exports: [LightsProvider, useLights, lightsReducer, createLight, countLights]
    - path: src/types/world-atlas.d.ts
      provides: "тип импорта world-atlas/countries-110m.json"
      contains: "declare module \"world-atlas/countries-110m.json\""
  key_links:
    - from: src/lib/geo.ts
      to: world-atlas/countries-110m.json
      via: "import topology + topojson-client feature()"
      pattern: "world-atlas/countries-110m.json"
    - from: src/data/lights.ts
      to: src/lib/geo.ts
      via: "randomPointIn(featureById(id), rng)"
      pattern: "randomPointIn"
    - from: src/state/lights.tsx
      to: src/data/lights.ts
      via: "generateLights() как initialLights по умолчанию"
      pattern: "generateLights\\("
    - from: src/state/lights.tsx
      to: src/data/countries.ts
      via: "countryById(countryId).center в createLight"
      pattern: "countryById"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** увидеть эффектный hero с вращающимся глобусом и кнопкой «Зажечь свой свет», а ниже живую карту ЕАД с огоньками, счётчиками и переключением стран, **so that** почувствовать масштаб движения по дивизиону и перейти к форме, чтобы зажечь свой свет.

Этот план закрывает QA-01 (критерий успеха 5 в ROADMAP: «Vitest-тесты подтверждают детерминизм генератора огоньков, форматирование чисел и редьюсер огоньков») и даёт планам 02-03, 02-04, 02-05 готовые контракты: данные стран, проекцию, генератор, контекст огоньков, форматирование. Порядок «тесты → реализация» обязателен для каждой задачи.

<objective>
Поставить зависимости d3-geo, d3-zoom, d3-selection, topojson-client, world-atlas с типами; создать типизированные моки стран и огоньков, проекцию с поворотом против разрыва на антимеридиане, генератор с rejection sampling, форматирование чисел, easing-функции, хук reduced motion и `LightsProvider` с редьюсером. Каждая функция покрыта Vitest в той же задаче.

Purpose: это фундамент карты и счётчиков; QA-01 требует тестов именно на эти функции, а фаза 3 уже рассчитывает на `useLights().addLight({ type, countryId })`.
Output: 18 файлов из `files_modified`, зелёные `npm test` и `npm run build`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/02-hero-and-map/02-RESEARCH.md
@.planning/phases/02-hero-and-map/02-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@package.json
@tsconfig.app.json
@vite.config.ts
@src/test/setup.ts

<interfaces>
<!-- Контракт, который создаёт этот план. Планы 02-03, 02-04, 02-05 и фаза 3 импортируют ровно эти имена. -->

```typescript
// src/data/countries.ts
export type CountryCode = "RU" | "KZ" | "BY" | "MD" | "UZ" | "KG" | "TJ" | "GE" | "AM" | "AZ" | "TM" | "AF";
export interface EsdCountry {
  id: number;                 // ISO 3166-1 numeric, как Number(feature.id) в world-atlas
  code: CountryCode;
  name: string;               // по-русски, для чипов и select формы
  weight: number;             // доля огоньков, сумма 1.00
  center: [number, number];   // [lon, lat]; для России Москва
}
export const ESD_COUNTRIES: readonly EsdCountry[]; // 12 стран по убыванию weight (порядок чипов)
export const ESD_IDS: ReadonlySet<number>;
export function countryById(id: number): EsdCountry | undefined;

// src/lib/rng.ts
export function mulberry32(seed: number): () => number; // детерминированный PRNG, значения в [0, 1)

// src/lib/geo.ts
import type { Feature, FeatureCollection, Geometry } from "geojson";
import type { GeoProjection } from "d3-geo";
export type CountryFeature = Feature<Geometry, { name: string }>; // feature.id это строка вида "004"
export const worldFeatures: CountryFeature[];        // 177 стран из countries-110m
export const esdFeatures: CountryFeature[];          // 12 стран ЕАД в порядке ESD_COUNTRIES
export const esdCollection: FeatureCollection<Geometry, { name: string }>;
export function featureById(id: number): CountryFeature | undefined;
export function isEsd(id: number): boolean;
export function makeProjection(width: number, height: number, pad?: number): GeoProjection; // pad по умолчанию 24
export function randomPointIn(feature: CountryFeature, rng: () => number, maxTries?: number): [number, number]; // [lon, lat]

// src/data/lights.ts
export type LightType = "person" | "group";
export interface Light { id: string; type: LightType; countryId: number; lon: number; lat: number; isNew?: boolean }
export const DEFAULT_SEED = 27;
export const DEFAULT_PEOPLE = 694;
export const DEFAULT_GROUPS = 248;
export function allocateByWeight(total: number, weights: readonly number[]): number[]; // сумма ровно total
export function generateLights(seed?: number, people?: number, groups?: number): Light[];

// src/lib/format.ts
export function formatCount(n: number): string; // 4268 → "4\u202F268"

// src/lib/easing.ts
export function easeOutCubic(t: number): number;  // 1 - (1 - t)^3
export function easeOutQuint(t: number): number;  // 1 - (1 - t)^5, эквивалент cubic-bezier(0.22, 1, 0.36, 1)

// src/lib/useReducedMotion.ts
export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
export function prefersReducedMotion(): boolean;      // синхронное чтение matchMedia, false без window
export function usePrefersReducedMotion(): boolean;   // подписка на change

// src/state/lights.tsx
import type { ReactNode } from "react";
export interface LightsState { lights: Light[] }
export interface AddLightInput { type: LightType; countryId: number }
export type LightsAction = { type: "add"; input: AddLightInput };
export interface LightCounts { people: number; groups: number }
export function countLights(lights: readonly Light[]): LightCounts;
export function createLight(lights: readonly Light[], input: AddLightInput): Light; // чистая, детерминированная
export function lightsReducer(state: LightsState, action: LightsAction): LightsState;
export function LightsProvider(props: { children: ReactNode; initialLights?: Light[] }): JSX.Element;
export function useLights(): { lights: Light[]; counts: LightCounts; addLight: (input: AddLightInput) => void };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: зависимости карты, тип JSON-импорта, страны ЕАД и PRNG</name>
  <files>package.json, package-lock.json, src/types/world-atlas.d.ts, src/data/countries.ts, src/lib/rng.ts, src/lib/rng.test.ts</files>
  <read_first>
    - package.json (какие пакеты фаза 1 уже поставила: d3-geo, d3-zoom, d3-selection, topojson-client, world-atlas и типы могли быть установлены по 01-RESEARCH; повторная установка идемпотентна)
    - tsconfig.app.json (`include`, наличие `resolveJsonModule`; `.d.ts` в `src/types/` должен попадать в `include`)
    - .planning/phases/02-hero-and-map/02-RESEARCH.md, таблица «Проверено скриптом» (numeric id, центроиды) и раздел «Импорт JSON в Vite/TS»
    - .planning/phases/02-hero-and-map/02-CONTEXT.md, раздел «Данные и состояние» (веса стран)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md, таблица «Чипы стран → Порядок»
  </read_first>
  <behavior>
    - Test 1 (rng.test.ts): `mulberry32(27)` и второй `mulberry32(27)` дают одинаковые первые 10 значений
    - Test 2: 1000 значений `mulberry32(27)` лежат в `[0, 1)`
    - Test 3: `mulberry32(27)` и `mulberry32(28)` различаются хотя бы в одном из первых 5 значений
  </behavior>
  <action>
    Гейт легитимности пакетов перед установкой: для каждого из `d3-geo d3-zoom d3-selection topojson-client world-atlas` выполнить `npm view <pkg> 'maintainers[0].name'` и убедиться, что ответ `mbostock`; для `@types/d3-geo @types/d3-zoom @types/d3-selection @types/topojson-client @types/topojson-specification @types/geojson` ответ должен быть `types`. При любом другом ответе установку прервать и записать в SUMMARY. (Планировщик проверил 2026-09-05: d3-geo 3.1.1, d3-zoom 3.0.0, d3-selection 3.0.0, topojson-client 3.1.0, world-atlas 2.0.2, все mbostock, install-скриптов нет; типы 3.1.1 / 3.0.8 / 3.0.11 / 3.1.5 / 1.0.5 / 7946.0.16, все DefinitelyTyped.)

    Установка: `npm i d3-geo@^3.1.1 d3-zoom@^3.0.0 d3-selection@^3.0.0 topojson-client@^3.1.0 world-atlas@^2.0.2` и `npm i -D @types/d3-geo @types/d3-zoom @types/d3-selection @types/topojson-client @types/topojson-specification @types/geojson`. Пакет `d3-transition` не ставим: полёт к стране делается собственным rAF-интерполятором по D-решению из CONTEXT (первичный вариант «через rAF и линейную интерполяцию transform»), это на один непроверенный пакет меньше.

    `src/types/world-atlas.d.ts`: `declare module "world-atlas/countries-110m.json"` с `const topology: Topology<{ countries: GeometryCollection<{ name: string }>; land: GeometryCollection }>` и `export default topology`, типы из `topojson-specification`. Если в `tsconfig.app.json` уже есть `resolveJsonModule: true`, декларация не мешает: в `geo.ts` импорт всё равно приводится через `as unknown as Topology`. Проверить, что `src/types/**` входит в `include` tsconfig (по умолчанию `["src"]`).

    `src/data/countries.ts`: типы и константы по контракту `<interfaces>`. Данные (порядок по убыванию weight, как в UI-SPEC «Порядок чипов»):
    643 RU Россия .55 center [37.6, 55.7] (Москва, по RESEARCH: геометрический центроид России лежит в Арктике);
    398 KZ Казахстан .08 [67.2, 48.4]; 112 BY Беларусь .07 [28.0, 53.5]; 498 MD Молдова .06 [28.4, 47.2]; 860 UZ Узбекистан .05 [63.4, 41.8]; 417 KG Кыргызстан .04 [74.6, 41.5]; 762 TJ Таджикистан .03 [71.0, 38.6]; 268 GE Грузия .03 [43.5, 42.2]; 51 AM Армения .03 [45.0, 40.2]; 31 AZ Азербайджан .02 [47.6, 40.2]; 795 TM Туркменистан .02 [59.3, 39.1]; 4 AF Афганистан .02 [66.0, 33.8].
    `ESD_IDS = new Set(ESD_COUNTRIES.map(c => c.id))`; `countryById` через `Map`. Массив объявить `as const satisfies readonly EsdCountry[]` или типизировать явно; сумма весов 1.00 (проверяется в geo.test Task 2).

    `src/lib/rng.ts`: `mulberry32(seed)` стандартный: `a = seed | 0`; на каждом вызове `a = (a + 0x6D2B79F5) | 0; t = Math.imul(a ^ (a >>> 15), 1 | a); t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t; return ((t ^ (t >>> 14)) >>> 0) / 4294967296`.
  </action>
  <verify>
    <automated>npm test -- src/lib/rng.test.ts && npx tsc -p tsconfig.app.json --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` содержит `"d3-geo"`, `"d3-zoom"`, `"d3-selection"`, `"topojson-client"`, `"world-atlas"` в dependencies и `"@types/d3-geo"`, `"@types/d3-zoom"`, `"@types/d3-selection"`, `"@types/topojson-client"`, `"@types/topojson-specification"`, `"@types/geojson"` в devDependencies; `"d3-transition"` отсутствует
    - `src/types/world-atlas.d.ts` содержит `declare module "world-atlas/countries-110m.json"`
    - `src/data/countries.ts` содержит `export const ESD_COUNTRIES`, `export const ESD_IDS`, `export function countryById(`, строки `643`, `398`, `112`, `498`, `860`, `417`, `762`, `268`, `51`, `31`, `795`, `4` как id и 12 русских названий из UI-SPEC
    - `src/lib/rng.ts` содержит `export function mulberry32(`
    - `npm test -- src/lib/rng.test.ts` завершается с кодом 0 (не меньше 3 тестов)
    - `npx tsc -p tsconfig.app.json --noEmit` завершается с кодом 0
  </acceptance_criteria>
  <done>Зависимости установлены после проверки maintainers, JSON world-atlas типизирован, 12 стран ЕАД и PRNG готовы и протестированы.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: геометрия (проекция, rejection sampling) и детерминированный генератор огоньков</name>
  <files>src/lib/geo.ts, src/lib/geo.test.ts, src/data/lights.ts, src/data/lights.test.ts</files>
  <read_first>
    - src/data/countries.ts, src/lib/rng.ts, src/types/world-atlas.d.ts (результат Task 1)
    - .planning/phases/02-hero-and-map/02-RESEARCH.md: пункты про `Number(f.id)`, проекцию (scale 320 при 1200×700, Чукотка x=1017 против x=1026), rejection sampling для bbox России через 180°
    - .planning/phases/02-hero-and-map/02-CONTEXT.md, пункты `lib/geo.ts` и `data/lights.ts`
    - .planning/research/PITFALLS.md, Pitfall 1 (антимеридиан)
  </read_first>
  <behavior>
    - Test 1 (geo.test.ts): `worldFeatures.length === 177`; `esdFeatures.length === 12`; множество `Number(f.id)` у esdFeatures равно `ESD_IDS`; `esdCollection.features.length === 12`
    - Test 2: `makeProjection(1200, 700)`: для каждой из `ESD_COUNTRIES` `projection(center)` возвращает конечные `x ∈ [0, 1200]`, `y ∈ [0, 700]` (нет NaN)
    - Test 3: у той же проекции `|projection([179.5, 66])[0] - projection([-179, 66])[0]| < 40` (обе точки Чукотки рядом, разрыв ушёл в Атлантику)
    - Test 4: для каждой страны `geoContains(featureById(c.id), c.center) === true` (центры внутри границ, на них опирается fallback)
    - Test 5: `randomPointIn(featureById(643), mulberry32(1))` 200 раз подряд: каждая точка внутри России по `geoContains`, долгота в `[-180, 180]`
    - Test 6: `isEsd(643) === true`, `isEsd(840) === false`; `featureById(999) === undefined`
    - Test 7 (lights.test.ts): `allocateByWeight(694, ESD_COUNTRIES.map(c => c.weight))` даёт 12 неотрицательных целых с суммой 694; то же для 248
    - Test 8: `generateLights()` даёт 942 огонька: 694 `person`, 248 `group`; `generateLights(27)` дважды даёт `toEqual`-равные массивы; `generateLights(28)` отличается
    - Test 9: все id уникальны; для каждого огонька `ESD_IDS.has(countryId)` и `geoContains(featureById(countryId), [lon, lat]) === true`
    - Test 10: количество огоньков России равно `allocateByWeight(694, weights)[0] + allocateByWeight(248, weights)[0]`
  </behavior>
  <action>
    Сначала оба тестовых файла (красные), потом реализация.

    `src/lib/geo.ts`: `import topology from "world-atlas/countries-110m.json"`, `import { feature } from "topojson-client"`, `import { geoMercator, geoBounds, geoContains, geoCentroid } from "d3-geo"`, типы из `geojson` и `topojson-specification`. На уровне модуля один раз: `const topo = topology as unknown as Topology`, `const world = feature(topo, topo.objects.countries as GeometryCollection<{ name: string }>)` (это FeatureCollection), `worldFeatures = world.features as CountryFeature[]`. `esdFeatures` строится в порядке `ESD_COUNTRIES` через `Map<number, CountryFeature>` по `Number(f.id)` (id в атласе это строки с ведущими нулями, `"004"`). `esdCollection = { type: "FeatureCollection", features: esdFeatures }`. `featureById(id)` через ту же Map, `isEsd(id)` через `ESD_IDS`.
    `makeProjection(width, height, pad = 24)`: `geoMercator().rotate([-90, 0]).fitExtent([[pad, pad], [width - pad, height - pad]], esdCollection)` (D: проекция; поворот убирает разрыв на 180°).
    `randomPointIn(feature, rng, maxTries = 50)`: `[[x0, y0], [x1, y1]] = geoBounds(feature)`; если `x0 > x1` (bbox пересекает 180°, случай России), долгота `lon = x0 + rng() * ((180 - x0) + (x1 + 180))`, при `lon > 180` вычесть 360; иначе `lon = x0 + rng() * (x1 - x0)`; широта `lat = y0 + rng() * (y1 - y0)`; принимать первую точку с `geoContains(feature, [lon, lat])`; после `maxTries` неудач вернуть `geoCentroid(feature)` (fallback по CONTEXT).

    `src/data/lights.ts`: типы и константы по контракту. `allocateByWeight(total, weights)`: метод наибольших остатков: `raw = w * total`, `base = floor(raw)`, остаток `total - sum(base)` раздать по одной единице странам с наибольшей дробной частью (при равенстве по индексу). `generateLights(seed = 27, people = 694, groups = 248)`: `rng = mulberry32(seed)`; `weights = ESD_COUNTRIES.map(c => c.weight)`; `peopleAlloc = allocateByWeight(people, weights)`, `groupAlloc = allocateByWeight(groups, weights)`; обход стран в порядке `ESD_COUNTRIES`: сначала `peopleAlloc[i]` огоньков типа `person`, затем `groupAlloc[i]` типа `group`, для каждого `[lon, lat] = randomPointIn(featureById(country.id)!, rng)`; id `p${k}` для person и `g${k}` для group с глобальными счётчиками k от 0. Порядок обхода фиксирован, потому что детерминизм зависит от последовательности вызовов rng.
  </action>
  <verify>
    <automated>npm test -- src/lib/geo.test.ts src/data/lights.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/geo.ts` содержит `rotate([-90, 0])`, `fitExtent(`, `geoContains(`, `geoBounds(`, `geoCentroid(`, `Number(`, `export function makeProjection(`, `export function randomPointIn(`, `export const esdFeatures`, `export const esdCollection`, `export const worldFeatures`, `export function featureById(`, `export function isEsd(`
    - `src/data/lights.ts` содержит `export function generateLights(`, `export function allocateByWeight(`, `export const DEFAULT_SEED = 27`, `export const DEFAULT_PEOPLE = 694`, `export const DEFAULT_GROUPS = 248`, `export type LightType`, `export interface Light`
    - `npm test -- src/lib/geo.test.ts src/data/lights.test.ts` завершается с кодом 0; суммарно не меньше 10 тестов
  </acceptance_criteria>
  <done>Проекция без разрыва России, генератор 694+248 огоньков внутри границ, детерминированный по seed 27; всё подтверждено тестами.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: formatCount, easing, хук reduced motion и LightsProvider с редьюсером</name>
  <files>src/lib/format.ts, src/lib/format.test.ts, src/lib/easing.ts, src/lib/easing.test.ts, src/lib/useReducedMotion.ts, src/lib/useReducedMotion.test.tsx, src/state/lights.tsx, src/state/lights.reducer.test.tsx</files>
  <read_first>
    - src/data/lights.ts, src/data/countries.ts, src/lib/geo.ts (результаты Task 1 и 2)
    - src/test/setup.ts (мок matchMedia: форма объекта и наличие `addEventListener`)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md, абзац «Формат чисел» в Copywriting Contract (U+202F как `\u202F`, без Intl.NumberFormat) и раздел «Motion Contract»
    - .planning/phases/02-hero-and-map/02-CONTEXT.md, пункт `state/lights.tsx`
    - .planning/phases/03-form-about-involve/03-RESEARCH.md, строка про `addLight` (контракт для фазы 3)
  </read_first>
  <behavior>
    - Test 1 (format.test.ts): `formatCount(694) === "694"`, `formatCount(4268) === "4\u202F268"`, `formatCount(1150) === "1\u202F150"`, `formatCount(0) === "0"`, `formatCount(1000000) === "1\u202F000\u202F000"`; `formatCount(12.7) === "13"` (округление)
    - Test 2 (easing.test.ts): `easeOutCubic(0) === 0`, `easeOutCubic(1) === 1`, `easeOutCubic(0.5) === 0.875`; `easeOutQuint(0.5) === 0.96875`; обе монотонно растут на сетке 0..1 шагом 0.1
    - Test 3 (useReducedMotion.test.tsx): при `matchMedia` с `matches: true` для запроса `(prefers-reduced-motion: reduce)` `renderHook(usePrefersReducedMotion).result.current === true`; при `matches: false` даёт `false`; `prefersReducedMotion()` возвращает то же синхронно
    - Test 4 (lights.reducer.test.tsx): `countLights(generateLights())` даёт `{ people: 694, groups: 248 }`
    - Test 5: `lightsReducer({ lights }, { type: "add", input: { type: "group", countryId: 51 } })` даёт `lights.length + 1`, `groups` вырос на 1, `people` не изменился; новый огонёк имеет `isNew: true`, `countryId: 51`
    - Test 6: два последовательных add дают разные id, и все id в состоянии уникальны
    - Test 7: координата добавленного огонька внутри страны (`geoContains(featureById(51), [lon, lat])`) или равна `countryById(51).center`
    - Test 8 (компонентный): `LightsProvider` с `initialLights={[]}` и тестовым потребителем `useLights()`: изначально `counts` равны `{ people: 0, groups: 0 }`; после `act(() => addLight({ type: "person", countryId: 643 }))` `counts.people === 1`
    - Test 9: `useLights()` вне провайдера бросает ошибку с текстом, содержащим `LightsProvider`
  </behavior>
  <action>
    Сначала все четыре тестовых файла (красные), затем реализация.

    `src/lib/format.ts`: `formatCount(n)`: `Math.round(n)` в строку, разделитель тысяч через регулярное выражение `/\B(?=(\d{3})+(?!\d))/g` заменяется на `"\u202F"`; `Intl.NumberFormat` не использовать (UI-SPEC: результат зависит от ICU). Литеральный символ U+202F в исходники не попадает, только escape.

    `src/lib/easing.ts`: `easeOutCubic(t) = 1 - (1 - t) ** 3`, `easeOutQuint(t) = 1 - (1 - t) ** 5`; входы клампить в `[0, 1]`.

    `src/lib/useReducedMotion.ts`: `REDUCED_MOTION_QUERY`; `prefersReducedMotion()` возвращает `false`, если `typeof window === "undefined"` или нет `window.matchMedia`, иначе `.matches`; `usePrefersReducedMotion()` через `useSyncExternalStore(subscribe, prefersReducedMotion, () => false)`, где `subscribe` вешает `change` через `mq.addEventListener?.("change", cb)` и снимает в cleanup (мок фазы 1 может не иметь метода, поэтому optional chaining). UI-SPEC упоминает `useReducedMotion` из `motion/react`; используем собственный хук с тем же контрактом (boolean), потому что motion кэширует matchMedia в модульном синглтоне и вызывает устаревший `addListener`, что делает reduced-motion непроверяемым в jsdom. Поведение (без анимации сразу конечное состояние) сохраняется.

    `src/state/lights.tsx` по контракту `<interfaces>`:
    `countLights(lights)` одним проходом. `createLight(lights, { type, countryId })`: `country = countryById(countryId)`; если нет, бросить `Error("Unknown ESD country: <id>")`; `n = число огоньков этой страны в lights`; смещение от центра по золотому углу: `a = n * 2.39996`, `r = 0.3 + 0.9 * ((n % 4) / 3)` (в градусах, не больше 1.2° по D-решению), `lon = center[0] + r * cos(a)`, `lat = center[1] + r * sin(a)`; если `feature = featureById(countryId)` есть и `!geoContains(feature, [lon, lat])`, вернуть точку центра; id `n${lights.length}` (генератор использует префиксы `p`/`g`, коллизий нет); `isNew: true`. `lightsReducer`: для `add` вернуть `{ lights: [...state.lights, createLight(state.lights, action.input)] }`. `LightsContext = createContext<LightsValue | null>(null)`. `LightsProvider({ children, initialLights })`: `useReducer(lightsReducer, undefined, () => ({ lights: initialLights ?? generateLights() }))`; `counts = useMemo(() => countLights(state.lights), [state.lights])`; `addLight = useCallback(input => dispatch({ type: "add", input }), [])`; value мемоизировать. `useLights()`: `useContext`, при `null` бросить `Error("useLights must be used within <LightsProvider>")`.

    После реализации: `npm test` (весь набор) и `npm run build`. Если билд упал из-за одновременной записи `dist/` планом 02-01 в той же волне, повторить один раз.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/format.ts` содержит `export function formatCount(` и `\u202F`; `grep -c "Intl.NumberFormat" src/lib/format.ts` выводит 0
    - `src/lib/easing.ts` содержит `export function easeOutCubic(` и `export function easeOutQuint(`
    - `src/lib/useReducedMotion.ts` содержит `export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)"`, `export function prefersReducedMotion(`, `export function usePrefersReducedMotion(`, `useSyncExternalStore`
    - `src/state/lights.tsx` содержит `export function LightsProvider(`, `export function useLights(`, `export function lightsReducer(`, `export function createLight(`, `export function countLights(`, `useReducer`, `generateLights(`, `countryById(`, `geoContains(`, `isNew: true`
    - `npm test` завершается с кодом 0; в четырёх новых тестовых файлах суммарно не меньше 9 тестов
    - `npm run build` завершается с кодом 0
  </acceptance_criteria>
  <done>formatCount, easing, reduced-motion хук и LightsProvider с addLight готовы и покрыты тестами; QA-01 выполнен для генератора, формата и редьюсера.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Единственный внешний ввод плана: пакеты d3/topojson/world-atlas и типы |
| world-atlas JSON → бандл | Данные границ парсятся на билде, в рантайме сети нет |
| Форма (фаза 3) → `addLight` | Ввод пользователя доходит только как `{ type, countryId }`; координата и id вычисляются внутри редьюсера |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (d3-geo, d3-zoom, d3-selection, topojson-client, world-atlas, @types/*) | mitigate | Проверка `npm view <pkg> 'maintainers[0].name'` (mbostock / types) перед установкой, версии с `^` от проверенных, lockfile коммитится; d3-transition не ставится |
| T-02-04 | Tampering | `createLight` вход `countryId` | mitigate | Неизвестный id бросает ошибку, координата берётся из `ESD_COUNTRIES`, а не из ввода |
| T-02-05 | Denial of Service | `randomPointIn` цикл | mitigate | Жёсткий лимит `maxTries = 50` и fallback на центроид |
| T-02-06 | Information Disclosure | Моки огоньков | accept | Данные синтетические, персональных нет |
</threat_model>

<verification>
- `npm test` зелёный, включая `rng.test.ts`, `geo.test.ts`, `lights.test.ts`, `format.test.ts`, `easing.test.ts`, `useReducedMotion.test.tsx`, `lights.reducer.test.tsx`.
- `npm run build` проходит `tsc -b` с типизированным импортом world-atlas.
- `npm ls d3-geo d3-zoom d3-selection topojson-client world-atlas` показывает установленные версии без ошибок.
</verification>

<success_criteria>
- QA-01: тесты на детерминизм генератора (одинаковый seed, точки внутри стран по `geoContains`), форматирование чисел (`4 268` с U+202F) и редьюсер (счётчик +1, уникальные id) зелёные.
- MAP-02 (фундамент): `makeProjection` проецирует 12 центров внутрь вьюбокса, точки по обе стороны 180° рядом.
- MAP-03 (фундамент): `generateLights()` даёт ровно 694 person и 248 group.
- MAP-04 (фундамент): `formatCount` готов для счётчиков.
- Фаза 3 может вызывать `useLights().addLight({ type, countryId })` без изменений контракта.
</success_criteria>

<output>
Создать `.planning/phases/02-hero-and-map/02-02-SUMMARY.md` после выполнения.
</output>
