---
phase: 03-form-about-involve
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/about.ts
  - src/data/about.test.ts
  - src/data/copy.about.ts
  - src/components/about/About.tsx
  - src/components/about/About.test.tsx
  - src/components/about/VideoEmbed.tsx
  - src/components/about/VideoEmbed.test.tsx
  - src/components/about/StepCard.tsx
  - src/components/about/about.css
  - src/components/about/video-embed.css
autonomous: true
requirements: [ABOUT-01, ABOUT-02, ABOUT-03]
tags: [about, youtube, lite-embed, react]

must_haves:
  truths:
    - "Посетитель видит секцию #about с eyebrow «Глобальное влияние», H2 «Что такое Единый голос 27?» (GradientTitle variant=section) и абзацем о сентябре 2027 года и 2000-летии крещения Иисуса"
    - "Посетитель видит обложку ролика YpLD6p-z00g с img.youtube.com и кнопку play с aria-label «Смотреть видео: Единый голос 27»; до клика iframe в DOM нет"
    - "По клику на play постер и кнопка заменяются iframe https://www.youtube-nocookie.com/embed/YpLD6p-z00g?autoplay=1&rel=0 с title, allow=\"autoplay; encrypted-media; picture-in-picture\" и allowfullscreen; фокус переходит на iframe"
    - "Если постер не загрузился, показывается градиентная заглушка с названием ролика, кнопка play продолжает работать"
    - "Посетитель видит три стеклянные карточки «1 Проект», «2 Подготовка», «3 Цель» с градиентными номерами, списками по три пункта, разделителем и итоговой фразой; тексты совпадают с 03-CONTEXT"
    - "VideoEmbed принимает { videoId, title, className? } и не зависит от секции About: фаза 4 может импортировать его напрямую"
  artifacts:
    - path: "src/data/about.ts"
      provides: "aboutSteps: три шага с number, title, items[3], summary"
      exports: ["aboutSteps"]
      contains: "Желание веков"
    - path: "src/data/copy.about.ts"
      provides: "тексты секции About и параметры видео"
      exports: ["aboutCopy"]
      contains: "YpLD6p-z00g"
    - path: "src/components/about/VideoEmbed.tsx"
      provides: "фасад YouTube: постер, play, iframe nocookie по клику, fallback постера"
      min_lines: 50
    - path: "src/components/about/StepCard.tsx"
      provides: "стеклянная карточка шага: номер, заголовок, список, разделитель, итог"
      min_lines: 25
    - path: "src/components/about/About.tsx"
      provides: "секция #about: текст, VideoEmbed, три StepCard"
      min_lines: 30
    - path: "src/components/about/about.css"
      provides: "фон секции, сетка шагов, стили StepCard, токены фазы 3 на .ab-section"
      min_lines: 80
    - path: "src/components/about/video-embed.css"
      provides: "16:9 контейнер, постер, скрим, кнопка play, iframe, fallback, hover/focus, reduced motion"
      min_lines: 50
    - path: "src/components/about/VideoEmbed.test.tsx"
      provides: "тесты фасада: постер, отсутствие iframe до клика, iframe после клика, fallback"
      min_lines: 40
    - path: "src/components/about/About.test.tsx"
      provides: "тесты секции: тексты, 3 заголовка шагов, 9 пунктов, 3 итога, кнопка видео"
      min_lines: 30
    - path: "src/data/about.test.ts"
      provides: "тесты данных шагов"
      min_lines: 15
  key_links:
    - from: "src/components/about/About.tsx"
      to: "src/components/about/VideoEmbed.tsx"
      via: "рендер <VideoEmbed videoId title>"
      pattern: "<VideoEmbed"
    - from: "src/components/about/VideoEmbed.tsx"
      to: "https://www.youtube-nocookie.com/embed/"
      via: "src iframe после клика"
      pattern: "youtube-nocookie\\.com/embed/"
    - from: "src/components/about/VideoEmbed.tsx"
      to: "https://img.youtube.com/vi/{id}/hqdefault.jpg"
      via: "src постера"
      pattern: "img\\.youtube\\.com/vi/"
    - from: "src/components/about/About.tsx"
      to: "src/data/about.ts"
      via: "aboutSteps.map → StepCard"
      pattern: "aboutSteps\\.map"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** понять суть «Единого голоса 27», увидеть путь от убеждения к действию и зажечь свой свет через форму, **so that** мой огонёк сразу появляется на карте и в счётчиках.

<objective>
Заменить заглушку `About` фазы 1 на секцию «Что такое Единый голос 27?»: текстовый блок, видео-фасад `VideoEmbed` (обложка YouTube, iframe `youtube-nocookie` по клику) и три карточки шагов «1 Проект / 2 Подготовка / 3 Цель» с данными в `src/data/about.ts`. `VideoEmbed` делается переиспользуемым: фаза 4 импортирует его для 16 роликов раздела «Ресурсы».

Purpose: секция отвечает на вопрос посетителя «что это?» (ABOUT-01..03) и даёт общий видео-фасад без внешних скриптов.
Output: данные и тексты в `src/data/`, компоненты и стили в `src/components/about/`, три тестовых файла.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-form-about-involve/03-CONTEXT.md
@.planning/phases/03-form-about-involve/03-UI-SPEC.md
@.planning/phases/03-form-about-involve/03-RESEARCH.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Ожидаемые контракты фазы 1 по 01-UI-SPEC. Код фазы 1 пишется параллельно: открой реальные файлы примитивов и сверь пропсы перед использованием. -->

From src/components/layout/* (фаза 1):
```typescript
Section: { id: string; eyebrow?: string; title?: string; className?: string; children }   // <section id> max-w 72rem, py-16 md:py-24 px-4 md:px-8
Eyebrow: { children }
GradientTitle: { as: "h1" | "h2"; variant: "hero" | "section"; children }               // H2 секций только через него, без локальных размеров и весов
GlassCard: { className?: string; children }                                             // стекло, radius 16px, padding 24/32px, ::before занят внутренним светом
```

Контракт VideoEmbed (фиксирован оркестратором, фаза 4 использует тот же API):
```typescript
export interface VideoEmbedProps { videoId: string; title: string; className?: string }
export function VideoEmbed(props: VideoEmbedProps): JSX.Element;
// постер: https://img.youtube.com/vi/${videoId}/hqdefault.jpg
// iframe:  https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0
// кнопка:  aria-label = `Смотреть видео: ${title}`; iframe title = title
```

Данные шагов:
```typescript
export interface AboutStep { number: 1 | 2 | 3; title: string; items: readonly [string, string, string]; summary: string }
export const aboutSteps: readonly AboutStep[];
```
</interfaces>
</context>

<class_contract>
Классы разметки, под которые пишется CSS в Task 3. В Task 2 раскладку задавай утилитами Tailwind, цвета и псевдоэлементы оставь CSS.

| Элемент | Класс |
|---------|-------|
| `Section` корень | `ab-section` |
| Заголовочный блок / лид | `ab-head` / `ab-lead` |
| `VideoEmbed` в секции (через `className`) | `ab-video` |
| Сетка карточек | `ab-steps` |
| `GlassCard` шага / шапка / номер / заголовок / список / разделитель / итог | `ab-step` / `ab-step-head` / `ab-step-num` / `ab-step-title` / `ab-step-list` / `ab-step-rule` / `ab-step-summary` |
| `VideoEmbed`: корень / постер / заглушка / скрим / кнопка / иконка / iframe | `ve` / `ve-poster` / `ve-fallback` / `ve-scrim` / `ve-play` / `ve-play-icon` / `ve-frame`; активное состояние `ve--active` на корне |
</class_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Красные тесты данных шагов, видео-фасада и секции</name>
  <files>src/data/about.test.ts, src/components/about/VideoEmbed.test.tsx, src/components/about/About.test.tsx</files>
  <read_first>
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (раздел «О проекте (#about)»: тексты абзаца, три шага с пунктами и итогами, параметры VideoEmbed)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Видео-фасад», «Копирайтинг: Секция „Что такое Единый голос 27?“», «Accessibility Contract»)
    - .planning/phases/03-form-about-involve/03-RESEARCH.md (раздел «Проверено»: в jsdom тест проверяет только смену DOM)
    - src/test/setup.ts (глобальные моки; `css: false`, поэтому импорт CSS в компонентах тесты не ломает)
    - src/components/about/About.tsx (заглушка фазы 1: имя экспорта, которое импортирует App.tsx)
  </read_first>
  <behavior>
    about.test.ts:
    - Test 1: `aboutSteps.length === 3`, `aboutSteps.map(s => s.number)` равно `[1, 2, 3]`, `titles` равно `["Проект", "Подготовка", "Цель"]`.
    - Test 2: у каждого шага `items.length === 3` и непустой `summary`; второй пункт второго шага содержит «Желание веков»; итог первого шага равен «Крупнейшая евангельская инициатива Церкви адвентистов седьмого дня в нашем поколении».

    VideoEmbed.test.tsx (`render(<VideoEmbed videoId="YpLD6p-z00g" title="Единый голос 27" />)`):
    - Test 1: есть `button` с именем «Смотреть видео: Единый голос 27» и `type="button"`; `img` с `src === "https://img.youtube.com/vi/YpLD6p-z00g/hqdefault.jpg"`, `alt === ""`, `loading="lazy"`, атрибутами `width` и `height`; `container.querySelector("iframe") === null`.
    - Test 2: после `fireEvent.click(button)` появляется `iframe` с `src === "https://www.youtube-nocookie.com/embed/YpLD6p-z00g?autoplay=1&rel=0"`, `title === "Единый голос 27"`, `getAttribute("allow") === "autoplay; encrypted-media; picture-in-picture"`, атрибут `allowfullscreen` присутствует; кнопки и `img` больше нет; в документе ровно один `iframe`.
    - Test 3: `fireEvent.error(img)` убирает `img` и показывает текст «Единый голос 27» внутри элемента с классом `ve-fallback`; кнопка play остаётся и по клику всё равно монтирует `iframe`.
    - Test 4: `className="ab-video"` попадает на корневой элемент вместе с классом `ve`.

    About.test.tsx (`render(<About />)`):
    - Test 1: `container.querySelector("section#about")` не null; заголовок уровня 2 с именем «Что такое Единый голос 27?»; текст «Глобальное влияние»; текст, совпадающий с `/сентябре 2027 года/` и `/2000-летие крещения Иисуса/`.
    - Test 2: `button` с именем «Смотреть видео: Единый голос 27» присутствует, `iframe` отсутствует.
    - Test 3: три заголовка уровня 3, имена содержат «Проект», «Подготовка», «Цель» именно в этом порядке; `getAllByRole("listitem").length === 9`; три текста итогов из `aboutSteps` присутствуют; тексты «1», «2», «3» присутствуют внутри элементов с классом `ab-step-num`.
  </behavior>
  <action>
    Создай три файла по behavior. Тексты в утверждениях бери из `aboutCopy` (`../../data/copy.about`) и `aboutSteps` (`../../data/about`), кроме проверок, где важна буквальная строка (Test 2 в about.test.ts, aria-label кнопки). Запусти тесты, убедись, что они падают из-за отсутствующих модулей (RED). Закоммить `test(03-02): add failing tests for about section and video embed`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/data/about.test.ts src/components/about 2>&1 | tail -20; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Три тестовых файла существуют; `VideoEmbed.test.tsx` содержит строки `youtube-nocookie.com/embed/YpLD6p-z00g?autoplay=1&rel=0`, `hqdefault.jpg`, `fireEvent.error`, `allowfullscreen`
    - `About.test.tsx` содержит `section#about`, `listitem`, `ab-step-num`
    - `npx vitest run src/data/about.test.ts src/components/about` завершается ненулевым кодом из-за отсутствующих модулей
    - `git log -1 --format=%s` начинается с `test(03-02)`
  </acceptance_criteria>
  <done>Три тестовых файла падают на отсутствующей реализации и закоммичены.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Данные, тексты, VideoEmbed, StepCard и секция About</name>
  <files>src/data/about.ts, src/data/copy.about.ts, src/components/about/VideoEmbed.tsx, src/components/about/StepCard.tsx, src/components/about/About.tsx</files>
  <read_first>
    - src/components/about/About.tsx (заглушка фазы 1; сохранить имя экспорта, которое использует App.tsx)
    - src/components/about/VideoEmbed.test.tsx, About.test.tsx, src/data/about.test.ts (ожидания Task 1)
    - src/components/layout/Section.tsx, Eyebrow.tsx, GradientTitle.tsx, GlassCard.tsx (реальные пропсы и проброс `className`)
    - src/data/copy.ts (стиль объявления текстов фазы 1; повторить в отдельном файле, `copy.ts` не менять)
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (раздел «О проекте»)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Видео-фасад», «Карточка шага», «Layout Contract: #about», «Accessibility Contract»)
  </read_first>
  <behavior>
    - Все тесты Task 1 проходят.
    - `VideoEmbed` не импортирует ничего из `About`, `copy.about` или `data/about`: только React и свой CSS (CSS подключается в Task 3).
  </behavior>
  <action>
    1. `src/data/copy.about.ts`: экспорт `aboutCopy` (`as const`): `eyebrow` «Глобальное влияние», `title` «Что такое Единый голос 27?», `lead` (абзац из 03-CONTEXT: «В сентябре 2027 года, в 2000-летие крещения Иисуса, … вместе со всей всемирной семьёй.»), `video: { id: "YpLD6p-z00g", title: "Единый голос 27" }`. Название ролика «Единый голос 27» выбрано так, чтобы aria-label кнопки совпал с зафиксированным в 03-CONTEXT «Смотреть видео: Единый голос 27»; суффикс «: видео о проекте» из UI-SPEC для title iframe не применяется, потому что контракт пропсов даёт один `title` (зафиксировать в SUMMARY).

    2. `src/data/about.ts`: `interface AboutStep` и `aboutSteps` с тремя шагами ровно по 03-CONTEXT (номера 1–3, заголовки «Проект», «Подготовка», «Цель», по три пункта, итоговые фразы). Кавычки-ёлочки и тире внутри строк сохраняй как в CONTEXT.

    3. `src/components/about/VideoEmbed.tsx`: именованный экспорт `VideoEmbed` с пропсами `{ videoId, title, className? }`. Состояние `active` и `posterFailed` (`useState`). Корень `div.ve` (+ `className`, + `ve--active` при `active`). Пока не `active`: `img.ve-poster` с `src={`https://img.youtube.com/vi/${videoId}/hqdefault.jpg`}`, `alt=""`, `loading="lazy"`, `width={480}`, `height={360}`, `decoding="async"`, `onError={() => setPosterFailed(true)}`; при `posterFailed` вместо `img` рендерится `div.ve-fallback[aria-hidden="true"]` с `<span>{title}</span>`; далее `div.ve-scrim[aria-hidden="true"]` и `button.ve-play[type="button"][aria-label={`Смотреть видео: ${title}`}]` с инлайн-SVG треугольником `aria-hidden="true" focusable="false"` (класс `ve-play-icon`), `onClick={() => setActive(true)}`. При `active`: `iframe.ve-frame` с `ref`, `src={`https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0`}`, `title={title}`, `allow="autoplay; encrypted-media; picture-in-picture"`, `allowFullScreen`, `loading="lazy"`, `referrerPolicy="strict-origin-when-cross-origin"`; `useEffect` при `active` вызывает `iframeRef.current?.focus()`. Никакого `dangerouslySetInnerHTML`, никаких сторонних скриптов (T-03-06).

    4. `src/components/about/StepCard.tsx`: пропсы `{ number: 1 | 2 | 3; title: string; items: readonly string[]; summary: string }`. `GlassCard className="ab-step"` → `div.ab-step-head` (`flex items-center gap-2`) с `span.ab-step-num` `{number}` и `h3.ab-step-title` `{title}`; `ul.ab-step-list` из `li` по `items`; `hr.ab-step-rule`; `p.ab-step-summary`. Номер внутри шапки, но вне `h3`, чтобы имя заголовка было «Проект», а число читалось отдельно. Карточка не интерактивна: без `tabIndex`, без обработчиков.

    5. `src/components/about/About.tsx`: `Section id="about" className="ab-section"`; `div.ab-head` (`max-w-[42rem]`) с `Eyebrow` `{aboutCopy.eyebrow}`, `GradientTitle as="h2" variant="section"` `{aboutCopy.title}` (никаких своих размеров и весов заголовка), `p.ab-lead`; затем `<VideoEmbed videoId={aboutCopy.video.id} title={aboutCopy.video.title} className="ab-video" />` с обёрткой `mt-12`; затем `div.ab-steps` (`grid gap-6 lg:grid-cols-3 lg:items-stretch mt-12`) с `aboutSteps.map(step => <StepCard key={step.number} {...step} />)`.

    Запусти `npm test` (все тесты этого плана зелёные) и `npm run build`. Закоммить `feat(03-02): about section with video facade and step cards`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/data/about.test.ts src/components/about 2>&1 | tail -8 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/data/about.test.ts src/components/about` завершается кодом 0
    - `npm run build` завершается кодом 0
    - `grep -c "youtube-nocookie.com/embed/" src/components/about/VideoEmbed.tsx` = 1; `grep -c "img.youtube.com/vi/" src/components/about/VideoEmbed.tsx` = 1
    - `grep -c 'allow="autoplay; encrypted-media; picture-in-picture"' src/components/about/VideoEmbed.tsx` = 1; `grep -c "allowFullScreen" src/components/about/VideoEmbed.tsx` = 1
    - `grep -c "\.focus()" src/components/about/VideoEmbed.tsx` ≥ 1
    - `grep -c "dangerouslySetInnerHTML" src/components/about/*.tsx` = 0 для каждого файла
    - `grep -c "aboutSteps.map" src/components/about/About.tsx` = 1; `grep -c "<VideoEmbed" src/components/about/About.tsx` = 1
    - `grep -c -E "from \"\./About\"|copy\.about|data/about" src/components/about/VideoEmbed.tsx` = 0
  </acceptance_criteria>
  <done>Секция About рендерит текст, видео-фасад и три карточки; клик по play монтирует nocookie iframe; тесты и билд зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Визуальный контракт секции About и видео-фасада</name>
  <files>src/components/about/about.css, src/components/about/video-embed.css, src/components/about/About.tsx, src/components/about/VideoEmbed.tsx</files>
  <read_first>
    - src/components/about/About.tsx, StepCard.tsx, VideoEmbed.tsx (классы из class_contract, куда добавить import CSS)
    - src/styles/tokens.css и src/styles/global.css (имена токенов `--color-*`, `--font-display`, `--font-body`, `--shadow-card`, `--glass-border`; утилита `.glass`; что уже делает `GlassCard::before`)
    - src/components/map/MapSection.tsx (как фаза 2 делает full-bleed фон при контейнере `Section` шириной 72rem; повторить тот же приём для фона #about)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Новые токены фазы 3», «Layout Contract: #about», «Видео-фасад», «Карточка шага», «Motion Contract», «Color»)
    - docs/research/orig-custom-styles.css строки 1142–1170 (`#ov-about` фон) и 1357–1470 (`.ov-about-step-card`, номер, разделитель) как референс значений
  </read_first>
  <action>
    1. `src/components/about/video-embed.css` (импортируется в `VideoEmbed.tsx`, чтобы фасад был стилизован и вне секции About). Собственные переменные на `.ve`: `--ve-ring: #aad9dc`, `--ve-transition: 420ms cubic-bezier(0.32, 0.72, 0, 1)`. `.ve`: `position: relative; aspect-ratio: 16 / 9; border-radius: 24px; overflow: hidden; isolation: isolate; background: #120c34; box-shadow: var(--shadow-card)`. `.ve-poster`: `position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; display: block`. `.ve-fallback`: `position: absolute; inset: 0; display: grid; place-items: center; background: linear-gradient(140deg, #211a3e, #303f83); color: #f8f7fb; font: 400 16px/1.6 var(--font-body); text-align: center; padding: 16px`. `.ve-scrim`: `position: absolute; inset: 0; background: linear-gradient(180deg, rgb(7 2 16 / .25), rgb(7 2 16 / .55)); pointer-events: none; transition: background var(--ve-transition)`. `.ve-play`: `position: absolute; inset: 0; margin: auto; width: 72px; height: 72px; border: 0; border-radius: 999px; background: linear-gradient(135deg, #6c2c68, #3b4da1); display: grid; place-items: center; cursor: pointer; color: #f8f7fb; transition: transform var(--ve-transition)`; `.ve-play-icon` 28px белый треугольник. Hover (`@media (hover: hover)`): `.ve:hover .ve-play { transform: scale(1.06) }`, `.ve:hover .ve-scrim` со скримом `.18/.45`. `.ve-play:focus-visible { outline: 2px solid var(--ve-ring); outline-offset: 4px }`. `.ve-frame`: `position: absolute; inset: 0; width: 100%; height: 100%; border: 0; display: block`. `@media (prefers-reduced-motion: reduce)`: `.ve-play, .ve-scrim { transition: none }`.

    2. `src/components/about/about.css` (импортируется в `About.tsx`). Токены на `.ab-section`: `--focus-ring: #aad9dc; --ui-transition: 420ms cubic-bezier(0.32, 0.72, 0, 1)`. Фон секции: `radial-gradient(circle at 8% -10%, rgb(158 67 154 / .3), transparent 42%), radial-gradient(circle at 104% 30%, rgb(48 63 131 / .9), transparent 46%), var(--color-midnight-900)`; full-bleed тем же приёмом, что `MapSection` (если приёма нет, псевдоэлемент `.ab-section::before` с `position: absolute; inset: 0 auto 0 50%; width: 100vw; translate: -50% 0; z-index: -1` при `position: relative; isolation: isolate` на секции). `.ab-lead { color: rgb(248 247 251 / .8); max-width: 42rem }`. `.ab-steps`: `display: grid; gap: 24px; @media (min-width: 1024px) { grid-template-columns: repeat(3, minmax(0, 1fr)); align-items: stretch }`. `.ab-step`: `position: relative; isolation: isolate; display: flex; flex-direction: column; gap: 16px; height: 100%; transition: border-color var(--ui-transition)`; `.ab-step::after` (у `GlassCard` занят `::before`): `content: ""; position: absolute; top: 0; left: 24px; width: 96px; height: 1px; background: linear-gradient(90deg, transparent, #bb6cae 28%, #7bc2c7 62%, transparent); opacity: .76; transition: opacity var(--ui-transition); pointer-events: none`. `.ab-step-num`: `font: 900 56px/1 var(--font-display); font-variant-numeric: lining-nums tabular-nums; background: linear-gradient(145deg, rgb(248 247 251 / .92), rgb(217 222 244 / .9) 52%, rgb(143 157 214 / .88)); -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; padding-inline-end: 4px`. `.ab-step-title`: `font: 800 24px/1.15 var(--font-display); text-wrap: balance; letter-spacing: -.01em`. `.ab-step-list`: `list-style: disc; padding-left: 20px; display: grid; gap: 8px; color: rgb(248 247 251 / .8); font: 400 16px/1.6 var(--font-body)`; `.ab-step-list li::marker { color: #d28ebe }`. `.ab-step-rule`: `margin-top: auto; border: 0; height: 1px; width: 34%; background: linear-gradient(90deg, rgb(217 222 244 / .55), rgb(248 247 251 / .32) 30%, rgb(248 247 251 / .16)); transform-origin: left; transition: width var(--ui-transition)`. `.ab-step-summary { color: rgb(248 247 251 / .8) }`. Hover `@media (hover: hover) and (pointer: fine)`: `.ab-step:hover { border-color: rgb(184 192 230 / .34) }`, `.ab-step:hover::after { opacity: 1 }`, `.ab-step:hover .ab-step-rule { width: 100% }`. `@media (prefers-reduced-motion: reduce)`: `.ab-step, .ab-step::after, .ab-step-rule { transition: none }`. Никаких правил для `h2` и `GradientTitle`: размер и вес H2 задаёт фаза 1.

    3. Добавь `import "./video-embed.css"` в `VideoEmbed.tsx` и `import "./about.css"` в `About.tsx`. Запусти `npm test` и `npm run build`. Закоммить `style(03-02): about section and video facade visual contract`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npm run build 2>&1 | tail -3 && npx vitest run src/components/about 2>&1 | tail -5 && grep -c "prefers-reduced-motion" src/components/about/about.css src/components/about/video-embed.css</automated>
    <human-check>`npm run dev`, секция #about на 1440 и 390px: постер 16:9 с радиусом 24px и круглой кнопкой play; после клика видео играет в iframe; три карточки в ряд на 1440 и стопкой на 390, номера с градиентом, при наведении разделитель растягивается на всю ширину.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` и `npm test` завершаются кодом 0
    - `grep -c 'import "./about.css"' src/components/about/About.tsx` = 1; `grep -c 'import "./video-embed.css"' src/components/about/VideoEmbed.tsx` = 1
    - `grep -c "prefers-reduced-motion" src/components/about/about.css` ≥ 1 и то же для `video-embed.css`
    - `grep -c "aspect-ratio: 16 / 9" src/components/about/video-embed.css` = 1; `grep -c "width: 72px" src/components/about/video-embed.css` = 1
    - `grep -c "font: 900 56px" src/components/about/about.css` = 1; `grep -c "background-clip: text" src/components/about/about.css` ≥ 1
    - `grep -c "repeat(3" src/components/about/about.css` = 1
    - `grep -c -w "h2" src/components/about/about.css` = 0 (H2 не переопределяется)
    - `grep -c "!important" src/components/about/about.css src/components/about/video-embed.css` = 0 для каждого файла
  </acceptance_criteria>
  <done>Секция About и видео-фасад оформлены по UI-SPEC без переопределения примитивов фазы 1, hover и reduced motion учтены, билд зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Страница → img.youtube.com | Загрузка статичного постера (GET картинки), без cookie-контекста YouTube |
| Страница → www.youtube-nocookie.com (iframe) | Сторонний контент монтируется только после клика пользователя |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | VideoEmbed iframe | mitigate | Домен `youtube-nocookie.com`, монтирование только по клику, `referrerPolicy="strict-origin-when-cross-origin"`; до клика внешних запросов к YouTube кроме постера нет |
| T-03-07 | Elevation of Privilege | VideoEmbed iframe | mitigate | Минимальный `allow="autoplay; encrypted-media; picture-in-picture"`, без `allow-same-origin`/`camera`/`microphone`; iframe cross-origin и не имеет доступа к родителю |
| T-03-08 | Tampering | Данные about.ts, StepCard | accept | Статичные типизированные строки из репозитория рендерятся текстовыми узлами; `dangerouslySetInnerHTML` запрещён (grep-гейт) |
| T-03-09 | Denial of Service | Постер img.youtube.com | mitigate | `onError` показывает локальную градиентную заглушку, кнопка play работает без постера |
| T-03-SC | Tampering | npm installs | accept | План не устанавливает пакеты; `package.json` не меняется |
</threat_model>

<verification>
- `npm test` зелёный: `src/data/about.test.ts`, `src/components/about/VideoEmbed.test.tsx`, `src/components/about/About.test.tsx`.
- `npm run build` зелёный.
- Изменённые файлы ограничены `src/data/about*.ts`, `src/data/copy.about.ts`, `src/components/about/*`.
- `VideoEmbed` импортируется без побочных зависимостей от About (готов для фазы 4).
</verification>

<success_criteria>
- Секция `#about` показывает eyebrow, H2 через `GradientTitle`, абзац о сентябре 2027 и 2000-летии крещения (ABOUT-01).
- Видео-фасад с постером `hqdefault.jpg` и play; по клику iframe `youtube-nocookie` с автоплеем, фокус на iframe; fallback постера работает (ABOUT-02).
- Три стеклянные карточки с градиентными номерами, списками и итогами из 03-CONTEXT (ABOUT-03).
- Три атомарных коммита (`test`, `feat`, `style`), тесты и билд зелёные.
</success_criteria>

<output>
Create `.planning/phases/03-form-about-involve/03-02-SUMMARY.md` when done. Отметь в SUMMARY: путь `src/components/about/VideoEmbed.tsx` и сигнатуру пропсов для фазы 4; решение по `title` ролика («Единый голос 27») и отказ от суффикса «: видео о проекте»; приём full-bleed фона, если он отличался от MapSection.
</output>
