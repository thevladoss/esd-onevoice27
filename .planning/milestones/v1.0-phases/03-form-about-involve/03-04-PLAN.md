---
phase: 03-form-about-involve
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/form/light-form.css
  - src/components/form/LightForm.tsx
  - src/components/form/SuccessToast.tsx
  - src/components/form/LightForm.test.tsx
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03]
tags: [form, css, tokens, reduced-motion]

must_haves:
  truths:
    - "Поля и select формы высотой 54px с фоном rgb(18 12 52 / .58), рамкой rgb(248 247 251 / .18), radius 12px, плейсхолдером paper/.56; в фокусе рамка horizon-400/.46, фон rgb(33 26 62 / .7) и кольцо 2px #aad9dc"
    - "Выбранная радио-карточка типа света получает рамку rgb(123 194 199 / .72), фон rgb(84 164 172 / .16) и точку-индикатор 12px (маджента для индивидуального, бирюза для группового) через :has(input:checked) без JS-классов; фокус клавиатурой рисует кольцо на всей карточке"
    - "Ошибка валидации показана под полем цветом #fca5a5 размером 13px, рамка невалидного поля красная"
    - "Тост фиксирован снизу по центру (bottom 24px, z-index 60), появляется за 240ms со сдвигом 12px, уходит за 200ms; при prefers-reduced-motion появляется и исчезает без анимации; автозакрытие 4000ms сохраняется"
    - "Контейнер формы GlassCard max-width 56rem с padding 24px на мобильном и 40px от 768px; поля в 2 колонки от 768px, email/согласие/кнопка на всю ширину; заголовочный блок центрирован; на 390px нет горизонтального скролла от декоративного шара"
    - "H2 «Зажгите свой свет» не переопределяется: размер и вес задаёт GradientTitle фазы 1"
  artifacts:
    - path: "src/components/form/light-form.css"
      provides: "токены формы фазы 3 на .lf-section, стили полей, радио-карточек, чекбокса, ошибок, кнопки, тоста, reduced motion"
      min_lines: 150
      contains: ":has(input:checked)"
    - path: "src/components/form/SuccessToast.tsx"
      provides: "фаза закрытия 200ms (мгновенно при reduced motion) перед onClose"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/components/form/LightForm.tsx"
      to: "src/components/form/light-form.css"
      via: "import"
      pattern: "import \"\\./light-form\\.css\""
    - from: "src/components/form/light-form.css"
      to: "разметка LightTypeChoice"
      via: "селекторы .lf-type:has(input:checked) и [data-type]"
      pattern: "\\.lf-type:has\\(input:checked\\)"
    - from: "src/components/form/light-form.css"
      to: "разметка SuccessToast"
      via: ".lf-toast / .lf-toast-card"
      pattern: "\\.lf-toast-card"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** понять суть «Единого голоса 27», увидеть путь от убеждения к действию и зажечь свой свет через форму, **so that** мой огонёк сразу появляется на карте и в счётчиках.

<objective>
Довести форму «Зажгите свой свет» из плана 03-01 до визуального контракта 03-UI-SPEC: локальные токены полей и радио-карточек, состояния hover/focus/invalid, кастомный чекбокс, стрелка select, кнопка во всю ширину с состоянием busy, тост с анимацией появления и ухода, reduced motion, адаптивная сетка и декоративный шар атмосферы без горизонтального скролла.

Purpose: без этого слоя форма работает, но не выглядит как оригинал onevoice27.org (FORM-01..03 в части визуала и доступности фокуса).
Output: `src/components/form/light-form.css`, импорт в `LightForm.tsx`, фаза закрытия в `SuccessToast.tsx`, обновлённые тайминги в `LightForm.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-form-about-involve/03-CONTEXT.md
@.planning/phases/03-form-about-involve/03-UI-SPEC.md
@.planning/phases/03-form-about-involve/03-01-SUMMARY.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Разметка формы уже создана планом 03-01 по его class_contract. CSS пишется строго под эти классы; менять разметку не нужно, кроме одной строки import. -->

Классы из 03-01: `.lf-section`, `.lf-head`, `.lf-lead`, `.lf-card`, `.lf-form`, `.lf-types`, `.lf-legend`, `.lf-type[data-type="individual"|"group"]`, `.lf-type-title`, `.lf-type-text`, `.lf-type-dot`, `.lf-grid`, `.lf-span`, `.lf-field`, `.lf-label`, `.lf-control`, `.lf-error`, `.lf-consent`, `.lf-check`, `.lf-check-box`, `.lf-check-text`, `.lf-submit`, `.lf-toast`, `.lf-toast-card`, `.lf-toast-icon`.
Радио и чекбокс: нативные `input` с классом `sr-only` внутри `label`, поэтому состояния описываются через `:has(input:checked)`, `:has(input:focus-visible)`, `:has(input[aria-invalid="true"])`.
Тост рендерится через `createPortal` в `document.body`, поэтому переменные для него объявляются на `.lf-toast`, а не наследуются от `.lf-section`.

From src/components/form/SuccessToast.tsx (03-01):
```typescript
export function SuccessToast(props: { open: boolean; message: string; onClose(): void; duration?: number }): JSX.Element | null;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Токены, поля, радио-карточки, чекбокс, кнопка и раскладка формы</name>
  <files>src/components/form/light-form.css, src/components/form/LightForm.tsx</files>
  <read_first>
    - src/components/form/LightForm.tsx, LightTypeChoice.tsx, FormField.tsx, ConsentCheckbox.tsx (фактическая разметка и классы после 03-01)
    - .planning/phases/03-form-about-involve/03-01-SUMMARY.md (расхождения, отмеченные исполнителем 03-01)
    - src/styles/tokens.css и src/styles/global.css (токены `--color-*`, `--font-*`, `--glass-border`; утилиты `.glass`, `.focus-ring`)
    - src/components/layout/Button.tsx и GlassCard.tsx (какие свойства уже заданы примитивами: padding GlassCard, стили Button, чтобы переопределять только необходимое)
    - src/components/map/MapSection.tsx (приём full-bleed фона и `.ov-map-skew-mask--bottom::after`-аналог: как фаза 2 рисует «шар» атмосферы без горизонтального скролла)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Новые токены фазы 3», «Layout Contract: #light-form», «Радио-карточки типа света», «Поля ввода и select», «Валидация», «Accessibility Contract», «Color»)
    - docs/research/orig-custom-styles.css строки 695–706 (`--ov-map-field-*`, `--ov-map-option-*`) и 1110–1140 (радио-карточки) как референс значений
  </read_first>
  <action>
    Создай `src/components/form/light-form.css` и добавь `import "./light-form.css"` первой строкой импортов `LightForm.tsx`. Разметку не меняй.

    Токены на `.lf-section` (значения из 03-UI-SPEC «Новые токены фазы 3»): `--field-height: 54px; --field-radius: 12px; --field-surface: rgb(18 12 52 / .58); --field-surface-focus: rgb(33 26 62 / .7); --field-border: rgb(248 247 251 / .18); --field-border-focus: rgb(123 194 199 / .46); --field-placeholder: rgb(248 247 251 / .56); --field-error: #fca5a5; --option-surface: rgb(18 12 52 / .42); --option-surface-active: rgb(84 164 172 / .16); --option-border: rgb(248 247 251 / .16); --option-border-active: rgb(123 194 199 / .72); --focus-ring: #aad9dc; --ui-transition: 420ms cubic-bezier(0.32, 0.72, 0, 1)`. Токены объявляются здесь, а не в `global.css` (файл фазы 1 не трогаем).

    Секция: `.lf-section { position: relative; isolation: isolate; overflow-x: clip; background: var(--color-midnight-950) }`. Декоративный шар `.lf-section::before`: `content: ""; position: absolute; top: 50%; right: 0; width: clamp(520px, 72vw, 1100px); aspect-ratio: 1; border-radius: 50%; background: radial-gradient(circle, rgb(59 77 161 / .28), transparent 70%); transform: translate(38%, -50%); pointer-events: none; z-index: -1`. Если `MapSection` использует full-bleed обёртку для таких слоёв, повтори её приём, чтобы шар не обрезался по контейнеру 72rem; `overflow-x: clip` оставь, чтобы на 390px не было горизонтального скролла.

    Заголовочный блок: `.lf-head { text-align: center; margin-bottom: 48px }`, `.lf-lead { color: rgb(248 247 251 / .8); max-width: 42rem; margin-inline: auto }`. Никаких правил для `h2`: H2 целиком задаёт `GradientTitle`.

    Контейнер: `.lf-card { max-width: 56rem; margin-inline: auto; padding: 24px }`, `@media (min-width: 768px) { .lf-card { padding: 40px } }`. Блоки формы: `.lf-form { display: flex; flex-direction: column; gap: 32px }`.

    Радио-карточки: `.lf-types { display: grid; gap: 16px; border: 0; padding: 0; margin: 0 }`, `@media (min-width: 768px) { .lf-types { grid-template-columns: repeat(2, minmax(0, 1fr)) } }`, `.lf-legend { font: 700 14px/1.35 var(--font-body); margin-bottom: 8px; padding: 0 }`. `.lf-type { position: relative; display: block; padding: 16px; padding-right: 40px; border-radius: 16px; background: var(--option-surface); border: 1px solid var(--option-border); cursor: pointer; min-height: 44px; transition: border-color var(--ui-transition), background-color var(--ui-transition) }`; `@media (hover: hover) { .lf-type:hover { border-color: rgb(123 194 199 / .4) } }`; `.lf-type:has(input:checked) { border-color: var(--option-border-active); background: var(--option-surface-active) }`; `.lf-type:has(input:focus-visible) { outline: 2px solid var(--focus-ring); outline-offset: 3px }`; `.lf-type-dot { position: absolute; top: 16px; right: 16px; width: 12px; height: 12px; border-radius: 50%; opacity: 0; transition: opacity var(--ui-transition) }`; `.lf-type:has(input:checked) .lf-type-dot { opacity: 1 }`; `.lf-type[data-type="individual"] .lf-type-dot { background: var(--color-signal-400) }`; `.lf-type[data-type="group"] .lf-type-dot { background: var(--color-horizon-400) }`; `.lf-type-title { display: block; font: 800 24px/1.15 var(--font-display); text-wrap: balance; margin-bottom: 8px }`; `.lf-type-text { display: block; color: rgb(248 247 251 / .8); font: 400 16px/1.6 var(--font-body) }`.

    Поля: `.lf-grid { display: grid; gap: 16px }`, `@media (min-width: 768px) { .lf-grid { grid-template-columns: repeat(2, minmax(0, 1fr)) } .lf-span { grid-column: 1 / -1 } }`. `.lf-label { display: block; font: 700 14px/1.35 var(--font-body); margin-bottom: 8px }`. `.lf-control { width: 100%; height: var(--field-height); padding: 0 16px; border-radius: var(--field-radius); background-color: var(--field-surface); border: 1px solid var(--field-border); color: rgb(248 247 251 / .94); font: 400 16px/1.6 var(--font-body); transition: border-color 180ms ease, background-color 180ms ease }` (используй `background-color`, а не `background`, чтобы не затирать стрелку select). `.lf-control::placeholder { color: var(--field-placeholder) }`; `@media (hover: hover) { .lf-control:hover { border-color: rgb(123 194 199 / .3) } }`; `.lf-control:focus { border-color: var(--field-border-focus); background-color: var(--field-surface-focus); outline: 2px solid var(--focus-ring); outline-offset: 2px }`; `.lf-control[aria-invalid="true"] { border-color: var(--field-error) }`. Select: `select.lf-control { appearance: none; padding-right: 44px; background-image: url("data:image/svg+xml,...") }` с инлайн-SVG шевроном 16px цвета `#aad9dc` (URL-кодированный, без внешних файлов), `background-repeat: no-repeat; background-position: right 16px center; background-size: 16px`; `select.lf-control option { background: #120c34; color: #f8f7fb }`. Ошибка: `.lf-error { margin-top: 8px; color: var(--field-error); font: 400 13px/1.35 var(--font-body) }`.

    Чекбокс: `.lf-check { display: flex; align-items: flex-start; gap: 12px; min-height: 44px; cursor: pointer }`; `.lf-check-box { flex: 0 0 20px; width: 20px; height: 20px; margin-top: 2px; border-radius: 6px; border: 1px solid var(--field-border); background: var(--field-surface); display: grid; place-items: center; transition: border-color var(--ui-transition), background-color var(--ui-transition) }`; `.lf-check-box svg { width: 12px; height: 12px; opacity: 0; transition: opacity var(--ui-transition); color: var(--color-paper) }`; `.lf-check:has(input:checked) .lf-check-box { border-color: var(--option-border-active); background: var(--option-surface-active) }`; `.lf-check:has(input:checked) .lf-check-box svg { opacity: 1 }`; `.lf-check:has(input:focus-visible) .lf-check-box { outline: 2px solid var(--focus-ring); outline-offset: 2px }`; `.lf-check:has(input[aria-invalid="true"]) .lf-check-box { border-color: var(--field-error) }`; `.lf-check-text { color: rgb(248 247 251 / .8); font: 400 16px/1.6 var(--font-body) }`.

    Кнопка: `.lf-submit { width: 100% }`; `.lf-submit[aria-busy="true"] { opacity: .72; cursor: progress }` (значения UI-SPEC «Отправка и тост»; `disabled`-стиль остаётся от примитива, переопределяем только непрозрачность и курсор).

    Запусти `npm run build` и `npm test`. Закоммить `style(03-04): light form tokens, fields, type cards and consent`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npm run build 2>&1 | tail -3 && npx vitest run src/components/form 2>&1 | tail -5 && grep -c ":has(input:checked)" src/components/form/light-form.css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` и `npx vitest run src/components/form` завершаются кодом 0
    - `grep -c 'import "./light-form.css"' src/components/form/LightForm.tsx` = 1
    - `grep -c "\-\-field-height: 54px" src/components/form/light-form.css` = 1; `grep -c "\-\-option-border-active: rgb(123 194 199 / .72)" src/components/form/light-form.css` = 1
    - `grep -c ":has(input:checked)" src/components/form/light-form.css` ≥ 3 (карточка, точка, чекбокс); `grep -c ":has(input:focus-visible)" src/components/form/light-form.css` ≥ 2
    - `grep -c '\[aria-invalid="true"\]' src/components/form/light-form.css` ≥ 2
    - `grep -c "appearance: none" src/components/form/light-form.css` = 1; `grep -c "data:image/svg+xml" src/components/form/light-form.css` = 1
    - `grep -c "max-width: 56rem" src/components/form/light-form.css` = 1; `grep -c "padding: 40px" src/components/form/light-form.css` = 1
    - `grep -c "overflow-x: clip" src/components/form/light-form.css` = 1
    - `grep -c -w "h2" src/components/form/light-form.css` = 0; `grep -c "!important" src/components/form/light-form.css` = 0
  </acceptance_criteria>
  <done>Форма оформлена по UI-SPEC: поля 54px, радио-карточки с активным состоянием через :has, кастомный чекбокс, стрелка select, кнопка во всю ширину, адаптивная сетка, шар атмосферы без горизонтального скролла.</done>
</task>

<task type="auto">
  <name>Task 2: Тост с анимацией появления и ухода, reduced motion</name>
  <files>src/components/form/light-form.css, src/components/form/SuccessToast.tsx, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - src/components/form/SuccessToast.tsx (реализация 03-01: пропсы, таймер, портал)
    - src/components/form/LightForm.test.tsx (Test 5 про тост: точки, где после 4000ms и после клика проверяется исчезновение)
    - src/components/form/light-form.css (Task 1)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Отправка и тост», «Motion Contract», «Accessibility Contract» строка про тост)
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (решение: при reduced motion без анимации появления; длительность анимации на усмотрение)
    - src/test/setup.ts (как замокан `window.matchMedia`, чтобы фаза закрытия в тестах отработала детерминированно)
  </read_first>
  <action>
    1. `SuccessToast.tsx`: добавь фазу закрытия. Состояние `closing: boolean`; функция `requestClose()` при `prefers-reduced-motion: reduce` (`window.matchMedia("(prefers-reduced-motion: reduce)").matches`, с защитой на отсутствие `matchMedia`) вызывает `onClose()` сразу, иначе ставит `closing = true` и через `setTimeout(onClose, 200)`. Автотаймер `duration` (4000 по умолчанию) вызывает `requestClose`, клик по тосту тоже. Все таймеры очищаются в cleanup и при смене `open`. При `open` снова `true` сбрасывай `closing`. На корень `.lf-toast` добавь `data-state={closing ? "closing" : "open"}`. Никаких кнопок внутри.

    2. `light-form.css`: `.lf-toast { --focus-ring: #aad9dc; --ui-transition: 420ms cubic-bezier(0.32, 0.72, 0, 1); position: fixed; inset-inline: 0; bottom: 24px; z-index: 60; display: flex; justify-content: center; padding-inline: 16px; pointer-events: none }`; `.lf-toast-card { pointer-events: auto; display: flex; align-items: center; gap: 12px; padding: 16px 20px; max-width: 26rem; cursor: pointer; animation: lf-toast-in 240ms cubic-bezier(0.32, 0.72, 0, 1) both }`; `.lf-toast[data-state="closing"] .lf-toast-card { animation: lf-toast-out 200ms ease both }`; `@keyframes lf-toast-in { from { opacity: 0; transform: translateY(12px) } to { opacity: 1; transform: none } }`; `@keyframes lf-toast-out { to { opacity: 0 } }`; `.lf-toast-icon { flex: 0 0 20px; width: 20px; height: 20px; border-radius: 50%; background: linear-gradient(125deg, #6c2c68 0%, #3b4da1 50%, #39727e 100%); display: grid; place-items: center; color: var(--color-paper) }`; `.lf-toast-icon svg { width: 12px; height: 12px }`; текст тоста `font: 400 16px/1.6 var(--font-body)`.

    3. Блок `@media (prefers-reduced-motion: reduce)` в конце файла: `.lf-toast-card { animation: none }`, `.lf-type, .lf-type-dot, .lf-control, .lf-check-box, .lf-check-box svg { transition: none }`. Автозакрытие через 4000ms не зависит от режима движения.

    4. `LightForm.test.tsx`: в Test 5 после `advanceTimersByTime(4000)` добавь `advanceTimersByTime(200)` перед проверкой отсутствия `role="status"`; после клика по тосту так же `advanceTimersByTime(200)`. Добавь утверждение: сразу после клика (до 200ms) элемент `role="status"` ещё в DOM с `data-state="closing"`. Добавь Test 6: при `matchMedia` с `matches: true` для reduced motion клик убирает тост без ожидания (0ms).

    Запусти `npm test` и `npm run build`. Закоммить `style(03-04): toast motion and reduced-motion rules`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/form 2>&1 | tail -6 && npm run build 2>&1 | tail -3 && grep -c "prefers-reduced-motion" src/components/form/light-form.css src/components/form/SuccessToast.tsx</automated>
    <human-check>`npm run dev`, отправить валидную форму: кнопка на 1.2 с становится «Зажигаем…», затем снизу по центру всплывает стеклянный тост с галочкой и через 4 с плавно гаснет; клик по тосту закрывает его; с включённым «Уменьшить движение» в системе тост появляется и исчезает без анимации. На 390px радио-карточки и поля стопкой, горизонтального скролла нет; Tab проходит тип света → поля → согласие → кнопка с видимым кольцом фокуса.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` и `npm run build` завершаются кодом 0
    - `grep -c "prefers-reduced-motion" src/components/form/light-form.css` ≥ 1; `grep -c "prefers-reduced-motion" src/components/form/SuccessToast.tsx` = 1
    - `grep -c "z-index: 60" src/components/form/light-form.css` = 1; `grep -c "bottom: 24px" src/components/form/light-form.css` = 1
    - `grep -c "@keyframes lf-toast-in" src/components/form/light-form.css` = 1; `grep -c "@keyframes lf-toast-out" src/components/form/light-form.css` = 1
    - `grep -c 'data-state=' src/components/form/SuccessToast.tsx` = 1; `grep -c "clearTimeout" src/components/form/SuccessToast.tsx` ≥ 1
    - `grep -c "advanceTimersByTime(200)" src/components/form/LightForm.test.tsx` ≥ 2
    - `grep -c "<button" src/components/form/SuccessToast.tsx` = 0
  </acceptance_criteria>
  <done>Тост появляется и уходит по Motion Contract, при reduced motion без анимаций, автозакрытие 4 с сохранено; весь набор тестов и билд зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → DOM | Только статичные стили и инлайн data-URL SVG шеврона из репозитория |
| Тост → document.body | Портал рендерит только текст `formCopy.success`, пользовательский ввод в тост не попадает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Tampering | light-form.css data-URL | accept | Статичный SVG-шеврон, закодированный в репозитории; внешних `url()` нет |
| T-03-14 | Information Disclosure | SuccessToast | accept | Текст тоста фиксированный, без данных полей |
| T-03-15 | Denial of Service | Таймеры тоста | mitigate | Все `setTimeout` очищаются в cleanup и при смене `open`; повторное открытие сбрасывает `closing` |
| T-03-SC | Tampering | npm installs | accept | План не устанавливает пакеты; `package.json` не меняется |
</threat_model>

<verification>
- `npm test` и `npm run build` зелёные.
- Изменённые файлы ограничены `src/components/form/light-form.css`, `LightForm.tsx` (одна строка import), `SuccessToast.tsx`, `LightForm.test.tsx`.
- В CSS нет правил для `h2`, `!important`, внешних `url()`.
- Визуальная проверка на 390/768/1024/1440 остаётся за смоуком фазы 5 (MOTION-03, QA-04); `human-check` выше даёт сценарий.
</verification>

<success_criteria>
- Радио-карточки, поля, select, чекбокс, ошибки и кнопка соответствуют значениям 03-UI-SPEC; активные и фокусные состояния через `:has` (FORM-01, FORM-02).
- Тост фиксирован снизу по центру над header, анимации 240/200ms, reduced motion без анимаций, автозакрытие 4 с (FORM-03).
- Два атомарных коммита `style`, тесты и билд зелёные.
</success_criteria>

<output>
Create `.planning/phases/03-form-about-involve/03-04-SUMMARY.md` when done. Отметь приём full-bleed для шара атмосферы и любые отличия от UI-SPEC.
</output>
