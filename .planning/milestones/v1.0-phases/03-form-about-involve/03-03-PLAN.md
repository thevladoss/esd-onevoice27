---
phase: 03-form-about-involve
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/copy.involve.ts
  - src/components/involve/Involve.tsx
  - src/components/involve/Involve.test.tsx
  - src/components/involve/InvolveCard.tsx
  - src/components/involve/art/PersonalArt.tsx
  - src/components/involve/art/ToolkitArt.tsx
  - src/components/involve/art/SharingArt.tsx
  - src/components/involve/involve.css
autonomous: true
requirements: [INVOLVE-01]
tags: [involve, triptych, svg, react]

must_haves:
  truths:
    - "Посетитель видит секцию #involve с eyebrow «Как включиться», H2 «От убеждения к действию» (GradientTitle variant=section) и абзацем «Подготовьтесь лично, снабдите свою общину и делитесь «Единым голосом 27» единым голосом.»"
    - "Посетитель видит триптих из трёх карточек «Личное преображение», «Материалы для церкви», «Делиться», каждая с SVG-иллюстрацией 4:3, заголовком и ссылкой-действием"
    - "Ссылки «Начать путь →», «Скачать материалы →», «Узнать, как делиться →» ведут на #about, #resources и #news соответственно; кликабельна только ссылка, не вся карточка"
    - "SVG-иллюстрации декоративны (aria-hidden, role=presentation, focusable=false), без внешних шрифтов и растровых вставок, каждая меньше 3 КБ в исходнике"
    - "На ≥1024px три карточки стоят в одной стеклянной рамке без зазоров со швами 1px, на <768px стопкой с радиусом 24px у каждой"
  artifacts:
    - path: "src/data/copy.involve.ts"
      provides: "тексты секции и три карточки { id, title, action, href }"
      exports: ["involveCopy"]
      contains: "#resources"
    - path: "src/components/involve/Involve.tsx"
      provides: "секция #involve: текст и триптих"
      min_lines: 30
    - path: "src/components/involve/InvolveCard.tsx"
      provides: "карточка: медиа 4:3, заголовок, ссылка-действие"
      min_lines: 20
    - path: "src/components/involve/art/PersonalArt.tsx"
      provides: "SVG свеча, конус света, книга (signal тона)"
      min_lines: 20
    - path: "src/components/involve/art/ToolkitArt.tsx"
      provides: "SVG веер баннеров и телефон (unity тона)"
      min_lines: 20
    - path: "src/components/involve/art/SharingArt.tsx"
      provides: "SVG узел, дуги, узлы соцсетей (horizon тона)"
      min_lines: 20
    - path: "src/components/involve/involve.css"
      provides: "токены триптиха, три раскладки, швы, медиа, hover, ссылка-действие, свечение, reduced motion"
      min_lines: 80
    - path: "src/components/involve/Involve.test.tsx"
      provides: "тесты секции: тексты, 3 заголовка, 3 ссылки с href, 3 декоративных svg"
      min_lines: 30
  key_links:
    - from: "src/components/involve/Involve.tsx"
      to: "src/data/copy.involve.ts"
      via: "involveCopy.cards.map → InvolveCard"
      pattern: "involveCopy\\.cards"
    - from: "src/components/involve/InvolveCard.tsx"
      to: "#about | #resources | #news"
      via: "href ссылки-действия"
      pattern: "href=\\{"
    - from: "src/components/involve/Involve.tsx"
      to: "src/components/involve/art/*.tsx"
      via: "art по id карточки"
      pattern: "PersonalArt|ToolkitArt|SharingArt"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** понять суть «Единого голоса 27», увидеть путь от убеждения к действию и зажечь свой свет через форму, **so that** мой огонёк сразу появляется на карте и в счётчиках.

<objective>
Заменить заглушку `Involve` фазы 1 на секцию «От убеждения к действию»: текстовый блок и триптих карточек «Личное преображение / Материалы для церкви / Делиться» с SVG-иллюстрациями, заголовками и ссылками-действиями на `#about`, `#resources`, `#news`.

Purpose: секция показывает посетителю три конкретных шага участия и связывает разделы лендинга между собой (INVOLVE-01).
Output: `src/data/copy.involve.ts`, компоненты и три SVG-иллюстрации в `src/components/involve/`, `involve.css`, `Involve.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-form-about-involve/03-CONTEXT.md
@.planning/phases/03-form-about-involve/03-UI-SPEC.md
@.planning/phases/03-form-about-involve/03-RESEARCH.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Ожидаемые контракты фазы 1 по 01-UI-SPEC. Открой реальные файлы примитивов и сверь пропсы. -->

From src/components/layout/* (фаза 1):
```typescript
Section: { id: string; eyebrow?: string; title?: string; className?: string; children }
Eyebrow: { children }
GradientTitle: { as: "h1" | "h2"; variant: "hero" | "section"; children }   // H2 только через него
GlassCard: { className?: string; children }                                 // рамка триптиха; padding и radius переопределяются в involve.css
```

Данные секции:
```typescript
export type InvolveCardId = "personal" | "toolkit" | "sharing";
export interface InvolveCardCopy { id: InvolveCardId; title: string; action: string; href: "#about" | "#resources" | "#news" }
export const involveCopy: { eyebrow: string; title: string; lead: string; cards: readonly InvolveCardCopy[] };
```

Компонент карточки:
```typescript
export function InvolveCard(props: { id: InvolveCardId; title: string; action: string; href: string; art: React.ReactNode }): JSX.Element;
```
</interfaces>
</context>

<class_contract>
| Элемент | Класс |
|---------|-------|
| `Section` корень | `inv-section` |
| Заголовочный блок / лид | `inv-head` / `inv-lead` |
| Обёртка триптиха (несёт свечение) | `inv-triptych-wrap` |
| `GlassCard` рамка триптиха | `inv-triptych` |
| Карточка `article` (атрибут `data-card="personal|toolkit|sharing"`) | `inv-card` |
| Медиа-блок / svg внутри | `inv-media` / `inv-art` |
| Контент / заголовок / ссылка / стрелка | `inv-body` / `inv-title` / `inv-action` / `inv-arrow` |
</class_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Красный тест секции «От убеждения к действию»</name>
  <files>src/components/involve/Involve.test.tsx</files>
  <read_first>
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (раздел «Участие (#involve)»: тексты, ссылки, требования к SVG)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Карточка триптиха», «SVG Illustrations», «Копирайтинг: Секция „От убеждения к действию“», «Accessibility Contract»)
    - src/components/involve/Involve.tsx (заглушка фазы 1: имя экспорта для App.tsx)
    - src/test/setup.ts (глобальные моки)
  </read_first>
  <behavior>
    Involve.test.tsx (`render(<Involve />)`):
    - Test 1: `container.querySelector("section#involve")` не null; заголовок уровня 2 «От убеждения к действию»; текст «Как включиться»; текст лида `involveCopy.lead`.
    - Test 2: три заголовка уровня 3 с именами «Личное преображение», «Материалы для церкви», «Делиться» в этом порядке; `container.querySelectorAll("article").length === 3`.
    - Test 3: ссылки `getByRole("link", { name: /Начать путь/ })` с `href="#about"`, `/Скачать материалы/` с `href="#resources"`, `/Узнать, как делиться/` с `href="#news"`; всего ровно 3 ссылки в секции; ни один `article` сам не имеет `onClick`/`href`/`role="link"`.
    - Test 4: `container.querySelectorAll('svg[aria-hidden="true"][role="presentation"]').length === 3`; у каждого `viewBox="0 0 400 300"` и `focusable="false"`; внутри svg нет элементов `text` и `image`.
    - Test 5: в каждой карточке стрелка `→` вынесена в `span[aria-hidden="true"]`, поэтому имя ссылки не содержит символ `→`.
  </behavior>
  <action>
    Создай тест по behavior, тексты бери из `involveCopy` (`../../data/copy.involve`), кроме буквальных проверок href. Запусти, убедись в падении из-за отсутствующих модулей. Закоммить `test(03-03): add failing test for involve triptych`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/involve 2>&1 | tail -15; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/involve/Involve.test.tsx` существует, содержит `section#involve`, `#about`, `#resources`, `#news`, `role="presentation"`, `viewBox`
    - `npx vitest run src/components/involve` завершается ненулевым кодом из-за отсутствующих модулей
    - `git log -1 --format=%s` начинается с `test(03-03)`
  </acceptance_criteria>
  <done>Тест написан, падает на отсутствующей реализации, закоммичен.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Тексты, три SVG-иллюстрации, InvolveCard и секция Involve</name>
  <files>src/data/copy.involve.ts, src/components/involve/art/PersonalArt.tsx, src/components/involve/art/ToolkitArt.tsx, src/components/involve/art/SharingArt.tsx, src/components/involve/InvolveCard.tsx, src/components/involve/Involve.tsx</files>
  <read_first>
    - src/components/involve/Involve.tsx (заглушка фазы 1; имя экспорта сохранить)
    - src/components/involve/Involve.test.tsx (ожидания Task 1)
    - src/components/layout/Section.tsx, Eyebrow.tsx, GradientTitle.tsx, GlassCard.tsx (реальные пропсы, проброс `className`)
    - src/data/copy.ts (стиль объявления текстов; `copy.ts` не менять)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (раздел «SVG Illustrations»: композиция и цвета трёх иллюстраций; «Карточка триптиха»)
    - docs/research/orig-vp-4500.jpeg (визуальный ориентир триптиха оригинала)
  </read_first>
  <behavior>
    - Все тесты Task 1 проходят.
    - Каждый SVG-файл меньше 3072 байт в исходнике, содержит только `svg`, `defs`, `linearGradient`, `radialGradient`, `stop`, `rect`, `circle`, `ellipse`, `path`, `g`, `line`.
  </behavior>
  <action>
    1. `src/data/copy.involve.ts`: экспорт `involveCopy` (`as const`): `eyebrow` «Как включиться», `title` «От убеждения к действию», `lead` «Подготовьтесь лично, снабдите свою общину и делитесь «Единым голосом 27» единым голосом.», `cards`: `{ id: "personal", title: "Личное преображение", action: "Начать путь", href: "#about" }`, `{ id: "toolkit", title: "Материалы для церкви", action: "Скачать материалы", href: "#resources" }`, `{ id: "sharing", title: "Делиться", action: "Узнать, как делиться", href: "#news" }`. Стрелка «→» рендерится компонентом отдельным `span[aria-hidden]`, поэтому в `action` её нет; ссылка «Скачать материалы» ведёт на `#resources` по 03-CONTEXT (фаза 4 сама решает про хэш панели).

    2. Три SVG-компонента в `src/components/involve/art/` с именованными экспортами `PersonalArt`, `ToolkitArt`, `SharingArt`. Общие атрибуты корня: `viewBox="0 0 400 300"`, `preserveAspectRatio="xMidYMid slice"`, `aria-hidden="true"`, `role="presentation"`, `focusable="false"`, `className="inv-art"`. Идентификаторы градиентов уникальны в пределах страницы: префиксы `inv-personal-`, `inv-toolkit-`, `inv-sharing-`. Обводки 2px, палитра фазы 1. Композиции по 03-UI-SPEC «SVG Illustrations»:
       - `PersonalArt` (signal): фон `linearGradient` 160° `#2b1436 → #120c34`; `radialGradient` пятно `rgb(158 67 154 / .34)` за свечой; свеча `rect` 28×110 `rx=6` `#f8f7fb` при opacity .9 справа от центра; пламя `ellipse` 14×24 `#d28ebe`; конус света треугольный `path` с градиентом от `rgb(210 142 190 / .5)` к прозрачному; слева снизу раскрытая книга: два `path` разворота с обводкой `rgb(248 247 251 / .7)` и три короткие линии текста; три искры `circle` r 1.5–2.5 вокруг пламени.
       - `ToolkitArt` (unity): фон 150° `#303f83 → #120c34`; слева веер из трёх `rect` 96×140 `rx=8` с поворотами -8°, -2°, 5° и заливками `rgb(59 77 161 / .9)`, `rgb(48 63 131 / .9)`, `rgb(33 26 62 / .95)`, на верхнем полоса заголовка и две строки; справа телефон `rect` 92×170 `rx=16` с обводкой `rgb(248 247 251 / .55)`, экран с градиентом `#6c2c68 → #3b4da1` и волной-`path`; между ними две пунктирные линии (`stroke-dasharray`).
       - `SharingArt` (horizon): фон 140° `#1b3a44 → #120c34`; узел слева снизу `circle` r 7 `#7bc2c7`; четыре дуги `path` (или `circle` с обрезкой по viewBox) радиусами 60, 110, 160, 210 с opacity .6, .42, .28, .16; пять узлов `circle` r 3–6 на дугах, соединённых `line` `rgb(170 217 220 / .5)` толщиной 1.5; у двух дальних узлов свечение `radialGradient` `rgb(123 194 199 / .35)`.
       Внутри SVG нет `text`, `image`, `foreignObject`, `script`.

    3. `src/components/involve/InvolveCard.tsx`: пропсы `{ id, title, action, href, art }`. Рендер `article.inv-card[data-card=id]` → `div.inv-media[aria-hidden="true"]` с `{art}`; `div.inv-body` (`flex flex-col gap-4 p-8`) с `h3.inv-title` `{title}` и `a.inv-action[href]` с текстом `{action}` и `span.inv-arrow[aria-hidden="true"]` «→». Никаких обработчиков клика на `article`; ссылка единственная цель (UI-SPEC: вложенные цели ломают навигацию с клавиатуры).

    4. `src/components/involve/Involve.tsx`: `Section id="involve" className="inv-section"`; `div.inv-head` (`max-w-[42rem]`) с `Eyebrow`, `GradientTitle as="h2" variant="section"` `{involveCopy.title}` (без собственных размеров и весов), `p.inv-lead`; затем `div.inv-triptych-wrap` (`mt-12`) → `GlassCard className="inv-triptych"` → `involveCopy.cards.map` в `InvolveCard` с `art` по `id` через объект `{ personal: <PersonalArt />, toolkit: <ToolkitArt />, sharing: <SharingArt /> }`. Раскладка утилитами: `grid gap-4 md:gap-0 lg:grid-cols-3` (точную раскладку и швы доводит CSS в Task 3).

    Запусти `npm test` и `npm run build`, проверь размер SVG-файлов `wc -c src/components/involve/art/*.tsx` (каждый < 3072). Закоммить `feat(03-03): involve triptych with svg illustrations`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/involve 2>&1 | tail -6 && npm run build 2>&1 | tail -3 && wc -c src/components/involve/art/*.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/involve` и `npm run build` завершаются кодом 0
    - `wc -c` каждого файла в `src/components/involve/art/` меньше 3072
    - `grep -c -E "<text|<image|<foreignObject|<script" src/components/involve/art/*.tsx` = 0 для каждого файла
    - `grep -c 'role="presentation"' src/components/involve/art/*.tsx` = 1 для каждого файла; `grep -c 'viewBox="0 0 400 300"'` = 1 для каждого
    - `grep -c 'href="#about"\|href="#resources"\|href="#news"' src/data/copy.involve.ts` = 3
    - `grep -c "onClick" src/components/involve/InvolveCard.tsx` = 0
    - `grep -c "involveCopy.cards" src/components/involve/Involve.tsx` ≥ 1
    - `grep -c "dangerouslySetInnerHTML" src/components/involve/*.tsx src/components/involve/art/*.tsx` = 0 для каждого файла
  </acceptance_criteria>
  <done>Секция Involve рендерит текст и три карточки с SVG, заголовками и ссылками на нужные секции; тесты и билд зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Визуальный контракт триптиха</name>
  <files>src/components/involve/involve.css, src/components/involve/Involve.tsx</files>
  <read_first>
    - src/components/involve/Involve.tsx, InvolveCard.tsx (классы из class_contract)
    - src/styles/tokens.css и src/styles/global.css (токены `--color-*`, `--font-*`, `--glass-border`, `--glass-surface`, `--shadow-card`; что делает `GlassCard` и его `::before`)
    - src/components/layout/GlassCard.tsx (какие свойства нужно переопределить: padding, radius)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Новые токены фазы 3: Триптих», «Layout Contract: #involve», «Карточка триптиха», «Motion Contract», «Accessibility Contract»)
    - docs/research/orig-custom-styles.css строки 1556–1700 (`#ov-involve` слои и заголовки) как референс значений
  </read_first>
  <action>
    Создай `src/components/involve/involve.css` и добавь `import "./involve.css"` в `Involve.tsx`.

    Токены на `.inv-section`: `--triptych-radius: 24px; --triptych-seam: rgb(248 247 251 / .12); --triptych-surface: linear-gradient(180deg, rgb(33 26 62 / .82), rgb(18 12 52 / .9)); --triptych-media-saturation: .85; --focus-ring: #aad9dc; --ui-transition: 420ms cubic-bezier(0.32, 0.72, 0, 1)`. Фон секции `var(--color-midnight-900)`. `.inv-lead { color: rgb(248 247 251 / .8); max-width: 42rem }`.

    Рамка `.inv-triptych` (переопределяет `GlassCard`): `padding: 0; border-radius: var(--triptych-radius); overflow: hidden; display: grid`. Свечение `.inv-triptych-wrap { position: relative; isolation: isolate }`, `.inv-triptych-wrap::before { content: ""; position: absolute; inset: -18px; z-index: -1; filter: blur(28px); pointer-events: none; display: none; background: radial-gradient(circle at 12% 8%, rgb(158 67 154 / .28), transparent 40%), radial-gradient(circle at 50% 110%, rgb(59 77 161 / .28), transparent 42%), radial-gradient(circle at 92% 30%, rgb(84 164 172 / .22), transparent 40%) }`, показывается только `@media (min-width: 1024px) { display: block }`.

    Раскладки по UI-SPEC:
    - `< 768px`: `.inv-triptych { background: none; border: 0; box-shadow: none; overflow: visible; gap: 16px; backdrop-filter: none }`; `.inv-card { background: var(--triptych-surface); border: 1px solid var(--glass-border); border-radius: var(--triptych-radius); overflow: hidden }`.
    - `768–1023px`: `.inv-triptych { grid-template-columns: minmax(0, 1fr) }`; `.inv-card { display: grid; grid-template-columns: minmax(220px, 2fr) minmax(0, 3fr) }`; `.inv-media { aspect-ratio: auto; min-height: 220px }`; `.inv-card + .inv-card { border-top: 1px solid var(--triptych-seam) }`.
    - `≥ 1024px`: `.inv-triptych { grid-template-columns: repeat(3, minmax(0, 1fr)) }`; `.inv-card { display: flex; flex-direction: column }`; `.inv-card + .inv-card { border-left: 1px solid var(--triptych-seam); border-top: 0 }`; медиа 4:3.

    Медиа: `.inv-media { position: relative; aspect-ratio: 4 / 3; overflow: hidden; background: #120c34 }`; `.inv-art { display: block; width: 100%; height: 100%; filter: saturate(var(--triptych-media-saturation)); transition: transform var(--ui-transition), filter var(--ui-transition); transform-origin: center }`; `.inv-media::after { content: ""; position: absolute; inset: 0; background: linear-gradient(180deg, transparent 45%, rgb(7 2 16 / .55)); pointer-events: none }`.

    Контент: `.inv-body { padding: 32px; display: flex; flex-direction: column; gap: 16px; flex: 1 }`; `.inv-title { font: 800 24px/1.15 var(--font-display); text-wrap: balance; letter-spacing: -.01em; color: var(--color-paper) }`; `.inv-action { position: relative; margin-top: auto; display: inline-flex; align-items: center; gap: 8px; width: fit-content; min-height: 44px; padding-block: 10px; font: 700 12px/1.4 var(--font-body); letter-spacing: .1em; text-transform: uppercase; color: var(--color-paper); text-decoration: none }`; `.inv-action::before { content: ""; position: absolute; left: 0; right: 0; bottom: 5px; height: 1px; background: linear-gradient(90deg, #bb6cae, #7bc2c7, transparent); transform: scaleX(.34); transform-origin: left; transition: transform var(--ui-transition) }`; `.inv-action:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 4px; border-radius: 4px }`; `.inv-action:focus-visible::before { transform: scaleX(1) }`.

    Hover только `@media (hover: hover)`: `.inv-card:hover .inv-art { transform: scale(1.025); filter: saturate(1) }`, `.inv-card:hover .inv-action::before { transform: scaleX(1) }`.

    `@media (prefers-reduced-motion: reduce) { .inv-art, .inv-action::before { transition: none } }`. Никаких правил для `h2`.

    Запусти `npm test` и `npm run build`. Закоммить `style(03-03): involve triptych visual contract`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npm run build 2>&1 | tail -3 && npx vitest run src/components/involve 2>&1 | tail -5 && grep -c "prefers-reduced-motion" src/components/involve/involve.css</automated>
    <human-check>`npm run dev`, секция #involve: на 1440 три карточки в одной стеклянной рамке с тонкими швами и мягким свечением под рамкой; на 768 три строки с медиа слева; на 390 стопка карточек с радиусом 24px; при наведении иллюстрация чуть увеличивается, линия под ссылкой растягивается; Tab доходит до трёх ссылок с видимым кольцом фокуса.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` и `npx vitest run src/components/involve` завершаются кодом 0
    - `grep -c 'import "./involve.css"' src/components/involve/Involve.tsx` = 1
    - `grep -c "prefers-reduced-motion" src/components/involve/involve.css` ≥ 1; `grep -c "(hover: hover)" src/components/involve/involve.css` ≥ 1
    - `grep -c "aspect-ratio: 4 / 3" src/components/involve/involve.css` = 1; `grep -c "min-height: 44px" src/components/involve/involve.css` ≥ 1
    - `grep -c "repeat(3" src/components/involve/involve.css` = 1; `grep -c "min-width: 1024px" src/components/involve/involve.css` ≥ 1; `grep -c "min-width: 768px" src/components/involve/involve.css` ≥ 1
    - `grep -c -w "h2" src/components/involve/involve.css` = 0; `grep -c "!important" src/components/involve/involve.css` = 0
  </acceptance_criteria>
  <done>Триптих оформлен по трём раскладкам UI-SPEC, hover и фокус работают, reduced motion учтён, билд зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Статичные данные → DOM | Тексты и SVG из репозитория, пользовательского ввода нет |
| Внутренние якоря | Ссылки ведут только на `#about`, `#resources`, `#news` в том же документе |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | InvolveCard, art/*.tsx | accept | Инлайн-SVG статичны, без `script`/`foreignObject`/`image` (grep-гейт); тексты рендерятся текстовыми узлами |
| T-03-11 | Spoofing | Ссылки-действия | mitigate | Только внутренние якоря из типизированного `href: "#about" | "#resources" | "#news"`, внешних URL нет, `target` не используется |
| T-03-12 | Information Disclosure | Секция Involve | accept | Нет данных пользователя, нет сетевых запросов |
| T-03-SC | Tampering | npm installs | accept | План не устанавливает пакеты; `package.json` не меняется |
</threat_model>

<verification>
- `npm test` зелёный, включая `src/components/involve/Involve.test.tsx`.
- `npm run build` зелёный.
- Изменённые файлы ограничены `src/data/copy.involve.ts` и `src/components/involve/**`.
- Каждый SVG-файл меньше 3 КБ, без `text`/`image`.
</verification>

<success_criteria>
- Секция `#involve` показывает eyebrow, H2 через `GradientTitle`, лид и три карточки с SVG-иллюстрациями, заголовками и ссылками на `#about`, `#resources`, `#news` (INVOLVE-01).
- Только ссылка кликабельна; фокус виден; SVG декоративны.
- Три атомарных коммита (`test`, `feat`, `style`), тесты и билд зелёные.
</success_criteria>

<output>
Create `.planning/phases/03-form-about-involve/03-03-SUMMARY.md` when done. Отметь, что «Скачать материалы →» ведёт на `#resources` без хэша панели (фаза 4 при желании распознаёт `#resources-materials` сама).
</output>
