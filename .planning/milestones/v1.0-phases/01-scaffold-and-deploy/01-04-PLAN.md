---
phase: 01-scaffold-and-deploy
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/Footer.tsx
  - src/components/layout/Footer.css
  - src/components/layout/Footer.test.tsx
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "Посетитель видит footer с фиолетовым градиентом, дрейфующими волнами и гало, вордмарком «Единый голос 27 / МИССИЯ ДЛЯ ВСЕХ», подписью «Официальный сайт Церкви христиан адвентистов седьмого дня» и строкой «© 2026 Евро-Азиатский дивизион Церкви христиан-адвентистов седьмого дня»"
    - "Ссылки «Евро-Азиатский дивизион» → https://esd.adventist.org и «OneVoice27 (глобальный сайт)» → https://onevoice27.org открываются в новой вкладке с rel=\"noopener noreferrer\""
    - "При prefers-reduced-motion: reduce волны и гало неподвижны"
  artifacts:
    - path: "src/components/layout/Footer.tsx"
      provides: "footer с Wordmark, подписью, nav внешних ссылок и юридической строкой"
      exports: ["Footer"]
      min_lines: 30
    - path: "src/components/layout/Footer.css"
      provides: "клин верхней грани, градиент, четыре слоя волн, гало, раскладка, reduced-motion"
      contains: "footer-wave-drift"
    - path: "src/components/layout/Footer.test.tsx"
      provides: "тесты состава footer и атрибутов внешних ссылок"
      min_lines: 25
  key_links:
    - from: "src/components/layout/Footer.tsx"
      to: "src/data/copy.ts"
      via: "copy.footer.*"
      pattern: "copy\\.footer\\."
    - from: "src/components/layout/Footer.css"
      to: "src/styles/global.css"
      via: "animation: footer-wave-drift / footer-halo-drift и var(--footer-wedge)"
      pattern: "footer-halo-drift"
    - from: "src/components/layout/Footer.tsx"
      to: "src/components/layout/Wordmark.tsx"
      via: "import Wordmark"
      pattern: "<Wordmark"
---

## Phase Goal

**Как** посетитель, **я хочу** открыть опубликованный на GitHub Pages лендинг с рабочим header, footer и визуальным языком оригинала, **чтобы** с первого экрана узнавать «Единый голос 27» и переходить к разделам по меню, пока каждый пуш в main сам прогоняет тесты, билд и деплой.

<objective>
Заменить заглушку `Footer.tsx` на footer оригинала: клиновидная верхняя грань, градиент `#211a3e → #120c34`, четыре слоя дрейфующих волн, гало, вордмарк с подписью, две внешние ссылки и юридическая строка; обе анимации замирают при reduced motion.

Purpose: footer закрывает SHELL-04 и завершает «оболочку» страницы, к которой фазы 2–4 добавляют содержимое.

Output: `Footer.tsx`, `Footer.css`, `Footer.test.tsx`; `npm test`, `npm run build`, `npm run lint` зелёные.

Ограничение параллельности: план идёт одновременно с 01-02 и 01-03. Не редактировать `src/App.tsx`, `src/main.tsx`, `index.html`, `src/styles/*.css` (keyframes `footer-wave-drift` и `footer-halo-drift` уже объявлены в `global.css`), `src/data/copy.ts`, `src/test/setup.ts`, `package.json`, `Header*.tsx`, `Wordmark.tsx`, `SkipLink.tsx`, файлы секций и примитивов.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-deploy/01-CONTEXT.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md
@.planning/phases/01-scaffold-and-deploy/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Готовые контракты из плана 01-01. Использовать напрямую, не менять. -->

```ts
// src/data/copy.ts
export const copy = {
  footer: {
    caption: "Официальный сайт Церкви христиан адвентистов седьмого дня",
    linksLabel: "Внешние ссылки",
    newTabHint: "(откроется в новой вкладке)",
    links: [
      { label: "Евро-Азиатский дивизион", href: "https://esd.adventist.org" },
      { label: "OneVoice27 (глобальный сайт)", href: "https://onevoice27.org" },
    ],
    legal: "© 2026 Евро-Азиатский дивизион Церкви христиан-адвентистов седьмого дня",
  },
  // ...
} as const;

// src/components/layout/Wordmark.tsx — <span class="wordmark"><span class="wordmark__title text-gradient-brand">Единый голос 27</span><span class="wordmark__tagline">МИССИЯ ДЛЯ ВСЕХ</span></span>
export function Wordmark({ className }: { className?: string }): ReactElement;
```

```css
/* Из src/styles/global.css: */
@keyframes footer-wave-drift { from { background-position: 0 0, 0 0, -2vw 0, 2vw 0; } to { background-position: 2vw 1vw, -2vw 1vw, 2vw -1vw, -2vw 1vw; } }
@keyframes footer-halo-drift { from { transform: translate3d(-52%, 0, 0) scale(0.98); } to { transform: translate3d(-48%, 7%, 0) scale(1.06); } }
/* :root: --footer-wedge: clamp(24px, 3vw, 44px); --dur-ui; --ease-ui; универсальный reduced-motion блок (animation-duration 1ms) */
/* tokens: --color-midnight-800 (#211a3e), --color-midnight-900 (#120c34), --color-paper, --color-horizon-200, --text-sm, --text-xs, --font-body; утилита sr-only из Tailwind */
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Разметка Footer с тестами состава и внешних ссылок</name>
  <files>src/components/layout/Footer.tsx, src/components/layout/Footer.test.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md (строки 381–406 Footer; 475–489 копирайт оболочки; 542–557 Accessibility: ландмарк footer, минимальная область нажатия, скрытый текст «(откроется в новой вкладке)»)
    - .planning/phases/01-scaffold-and-deploy/01-CONTEXT.md (раздел «Footer»: состав и раскладка)
    - src/components/layout/Footer.tsx (текущая заглушка), src/components/layout/Wordmark.tsx, src/data/copy.ts
    - .planning/research/PITFALLS.md (раздел «Security Mistakes»: rel="noopener noreferrer")
  </read_first>
  <behavior>
    - `render(<Footer />)`: `getByRole("contentinfo")` присутствует и содержит тексты «Единый голос 27», «МИССИЯ ДЛЯ ВСЕХ», «Официальный сайт Церкви христиан адвентистов седьмого дня», «© 2026 Евро-Азиатский дивизион Церкви христиан-адвентистов седьмого дня»
    - `getByRole("navigation", { name: "Внешние ссылки" })` содержит ровно две ссылки: с `href="https://esd.adventist.org"` и текстом, начинающимся с «Евро-Азиатский дивизион», и с `href="https://onevoice27.org"` и текстом, начинающимся с «OneVoice27 (глобальный сайт)»
    - У обеих ссылок `target="_blank"` и `rel="noopener noreferrer"`, внутри каждой есть текст «(откроется в новой вкладке)» в элементе с классом `sr-only`
    - В footer нет заголовков `h1`–`h6`
  </behavior>
  <action>
Сначала `Footer.test.tsx` по `<behavior>` (названия на русском), убедиться, что тест падает на заглушке, затем реализовать.

1. `Footer.tsx` (`export function Footer`, импортирует `./Footer.css`): `footer.site-footer` → `div.site-footer__inner` (`mx-auto max-w-[72rem] px-4 md:px-8`) → `div.site-footer__grid` с двумя колонками: слева `div.site-footer__brand` с `Wordmark` и `p.site-footer__caption` (`copy.footer.caption`); справа `nav.site-footer__links` с `aria-label={copy.footer.linksLabel}` и `ul` из `copy.footer.links`: каждая `a` с `href`, `target="_blank"`, `rel="noopener noreferrer"`, текстом `label` и `span.sr-only` с `copy.footer.newTabHint` (пробел перед скрытым текстом, чтобы имя ссылки читалось «Евро-Азиатский дивизион (откроется в новой вкладке)»). Ниже `div.site-footer__legal` с `border-top` и `p` (`small` внутри необязателен) с `copy.footer.legal`. Все тексты только из `copy.footer`, русских литералов в файле нет. Заголовков в footer нет.

2. Стили этой задачи минимальны (полные слои в Task 2), но классы должны существовать заранее: создать `Footer.css` только если Task 2 выполняется отдельно; иначе Task 2 создаёт его целиком.

3. `npm test -- src/components/layout/Footer.test.tsx`, `npm run lint` зелёные.
  </action>
  <verify>
    <automated>npm test -- src/components/layout/Footer.test.tsx && npm run lint && grep -q 'rel="noopener noreferrer"' src/components/layout/Footer.tsx && grep -q 'target="_blank"' src/components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Footer.tsx` содержит `export function Footer`, `<Wordmark`, `copy.footer.caption`, `copy.footer.links`, `copy.footer.linksLabel`, `copy.footer.newTabHint`, `copy.footer.legal`, `target="_blank"`, `rel="noopener noreferrer"`, `sr-only`, `aria-label`
    - `grep -Ec '[А-Яа-яЁё]' src/components/layout/Footer.tsx` = 0 (тексты только из copy.ts)
    - `grep -Ec '<h[1-6]' src/components/layout/Footer.tsx` = 0
    - `npm test -- src/components/layout/Footer.test.tsx` проходит не меньше 4 тестов, код 0
    - `npm run lint` код 0
  </acceptance_criteria>
  <done>Footer содержит все элементы SHELL-04 с корректными атрибутами внешних ссылок и покрыт тестами.</done>
</task>

<task type="auto">
  <name>Task 2: Визуальные слои Footer: клин, градиент, волны, гало, раскладка, reduced motion</name>
  <files>src/components/layout/Footer.css, src/components/layout/Footer.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md (строки 381–406 Footer: таблица свойств и keyframes; 284–305 Motion и Reduced motion; 143–169 Spacing; 194–210 типографика подписи, ссылок и юридической строки)
    - .planning/phases/01-scaffold-and-deploy/01-CONTEXT.md (раздел «Footer»)
    - docs/research/orig-custom-styles.css (строки 2486–2560: `#ov-main-footer`, `::before` волны, `::after` гало, размеры и позиции слоёв)
    - src/styles/global.css (`--footer-wedge`, keyframes `footer-wave-drift`, `footer-halo-drift`; их не дублировать)
    - src/components/layout/Footer.tsx (разметка из Task 1)
  </read_first>
  <action>
1. `Footer.css`, блок `.site-footer`: `position: relative; isolation: isolate; overflow: hidden; margin-top: calc(-1 * var(--footer-wedge)); padding-block: calc(var(--footer-wedge) + clamp(72px, 10vw, 124px)) clamp(72px, 10vw, 124px); color: var(--color-paper); background-color: var(--color-midnight-900); background-image: radial-gradient(circle at 18% 12%, rgb(158 67 154 / .14), transparent 32%), radial-gradient(circle at 82% 24%, rgb(59 77 161 / .13), transparent 34%), radial-gradient(ellipse at 50% 100%, transparent 0 46%, rgb(48 63 131 / .16) 46.4% 47%, transparent 47.4% 100%), radial-gradient(ellipse at 54% 106%, transparent 0 56%, rgb(48 63 131 / .16) 56.4% 57%, transparent 57.4% 100%), linear-gradient(180deg, #211a3e, #120c34); background-size: auto, auto, 108% 108%, 112% 112%, auto; background-position: 0 0, 0 0, -2vw 0, 2vw 0, 0 0; animation: footer-wave-drift 28s linear infinite alternate; clip-path: polygon(0 var(--footer-wedge), 100% 0, 100% 100%, 0 100%)`. Keyframes `footer-wave-drift` анимируют четыре первые позиции, пятая (линейный градиент) остаётся `0 0` через `background-position` из keyframes с добавлением `, 0 0`? Нет: keyframes в `global.css` содержат четыре позиции, поэтому линейный градиент выносится в `background-color` плюс отдельный псевдоэлемент `.site-footer::after` с `linear-gradient(180deg, #211a3e, #120c34)` на `z-index: -2`, а четыре анимируемых слоя остаются на самом `.site-footer` с `background-position` из четырёх значений. Итог: `.site-footer` несёт четыре слоя волн и гало-пятен, `::after` несёт базовый градиент, `::before` несёт большое гало.

2. Гало `.site-footer::before`: `content: ""; position: absolute; z-index: -1; left: 50%; top: calc(var(--footer-wedge) - 34%); width: min(760px, 92vw); aspect-ratio: 1.8; pointer-events: none; background: radial-gradient(circle at 34% 46%, rgb(158 67 154 / .25), transparent 38%), radial-gradient(circle at 66% 42%, rgb(84 164 172 / .10), transparent 40%); filter: blur(32px); opacity: .78; transform: translateX(-50%); animation: footer-halo-drift 22s ease-in-out infinite alternate`. Базовый градиент `.site-footer::after`: `content: ""; position: absolute; inset: 0; z-index: -2; background: linear-gradient(180deg, #211a3e, #120c34); pointer-events: none`.

3. Раскладка: `.site-footer__inner { position: relative; z-index: 1 }`; `.site-footer__grid { display: grid; gap: 32px }`, от `768px` `grid-template-columns: 1fr auto; align-items: start`; `.site-footer__caption { margin-top: 16px; max-width: 42ch; font-size: var(--text-sm); line-height: 1.6; color: rgb(248 247 251 / .82) }`; `.site-footer__links ul { display: grid; gap: 8px }`; `.site-footer__links a { display: inline-block; padding-block: 8px; font-size: var(--text-sm); font-weight: 700; line-height: 1.6; color: var(--color-paper); text-decoration: none; transition: color var(--dur-ui) var(--ease-ui) }`, `:hover`, `:focus-visible` → `color: var(--color-horizon-200)`; `.site-footer__legal { margin-top: 48px; padding-top: 24px; border-top: 1px solid rgb(248 247 251 / .12); font-size: var(--text-xs); line-height: 1.7; color: rgb(248 247 251 / .72); text-wrap: balance }`. Вордмарк в footer наследует `.wordmark` из `global.css`, дополнительных правил не нужно.

4. Reduced motion (в дополнение к универсальному блоку `global.css`): `@media (prefers-reduced-motion: reduce) { .site-footer, .site-footer::before { animation: none } .site-footer::before { transform: translateX(-50%) } }`.

5. Убедиться, что `Footer.tsx` импортирует `./Footer.css` и классы совпадают. `npm test`, `npm run build`, `npm run lint` зелёные; в `dist/assets/*.css` встречаются `footer-wave-drift`, `footer-halo-drift`, `site-footer`.
  </action>
  <verify>
    <automated>npm test && npm run build && npm run lint && grep -q 'animation: footer-wave-drift 28s' src/components/layout/Footer.css && grep -q 'animation: footer-halo-drift 22s' src/components/layout/Footer.css && grep -q 'prefers-reduced-motion' src/components/layout/Footer.css && grep -l 'site-footer' dist/assets/*.css</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Footer.css` содержит `.site-footer`, `isolation: isolate`, `overflow: hidden`, `clip-path: polygon(0 var(--footer-wedge), 100% 0, 100% 100%, 0 100%)`, `margin-top: calc(-1 * var(--footer-wedge))`, `linear-gradient(180deg, #211a3e, #120c34)`, `rgb(158 67 154 / .14)`, `rgb(59 77 161 / .13)`, `rgb(48 63 131 / .16)`, `108% 108%`, `112% 112%`, `animation: footer-wave-drift 28s linear infinite alternate`, `.site-footer::before`, `min(760px, 92vw)`, `aspect-ratio: 1.8`, `filter: blur(32px)`, `opacity: .78`, `animation: footer-halo-drift 22s ease-in-out infinite alternate`, `grid-template-columns: 1fr auto`, `@media (min-width: 768px)`, `border-top: 1px solid rgb(248 247 251 / .12)`, `padding-block: 8px`, `@media (prefers-reduced-motion: reduce)`, `animation: none`
    - `grep -c '@keyframes' src/components/layout/Footer.css` = 0 (keyframes живут в global.css)
    - `grep -c "import \"./Footer.css\"" src/components/layout/Footer.tsx` = 1
    - `npm test` код 0; `npm run build` код 0; `npm run lint` код 0
    - `grep -l 'footer-halo-drift' dist/assets/*.css` находит файл
  </acceptance_criteria>
  <done>Footer выглядит как оригинал: клин сверху, фиолетовый градиент, дрейфующие волны и гало, две колонки на десктопе и стек на мобильном; анимации замирают при reduced motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Страница → внешние сайты (esd.adventist.org, onevoice27.org) | Переход в новую вкладку на чужой домен |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering (reverse tabnabbing) | Footer внешние ссылки | mitigate | `target="_blank"` всегда вместе с `rel="noopener noreferrer"`; проверяется тестом Task 1 и grep в acceptance |
| T-01-11 | Information Disclosure | Footer ссылки | accept | `noreferrer` не передаёт Referer; адреса статичны из `copy.ts`, только https |
| T-01-SC | Tampering | npm installs | mitigate | План не устанавливает пакетов; аудит в 01-01 |
</threat_model>

<verification>
- `npm test` зелёный, `Footer.test.tsx` ≥ 4 тестов.
- `npm run build`, `npm run lint` без ошибок.
- `grep -c 'target="_blank"' src/components/layout/Footer.tsx` = `grep -c 'rel="noopener noreferrer"' src/components/layout/Footer.tsx` (каждая новая вкладка защищена).
</verification>

<success_criteria>
- SHELL-04: footer с волнами, логотипом, подписью, © строкой и двумя внешними ссылками на esd.adventist.org и onevoice27.org.
- Анимации `footer-wave-drift` и `footer-halo-drift` активны и отключаются при reduced motion.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-and-deploy/01-04-SUMMARY.md` when done. Зафиксировать распределение слоёв footer (сам элемент = четыре слоя волн, `::before` = гало, `::after` = базовый градиент) для фазы 5 (MOTION-02).
</output>
