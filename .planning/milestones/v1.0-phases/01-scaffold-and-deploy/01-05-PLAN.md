---
phase: 01-scaffold-and-deploy
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - README.md
autonomous: true
requirements: [QA-03]

must_haves:
  truths:
    - "https://thevladoss.github.io/esd-onevoice27/ отдаёт 200 и собранную версию с header, footer и восемью секциями после пуша всех планов фазы"
    - "Workflow deploy.yml на последнем коммите main завершился success после npm ci, npm test, npm run build"
    - "Каждый ассет, на который ссылается живой index.html, отвечает 200; в браузере нет ошибок консоли и 404 (если доступен Playwright MCP)"
    - "Локальный vite preview и живой сайт отдают одинаковый index.html (совпадают ссылки на ассеты по хэшам)"
  artifacts:
    - path: "README.md"
      provides: "инструкции запуска, тестов, деплоя и проверки живого сайта на русском"
      contains: "https://thevladoss.github.io/esd-onevoice27/"
  key_links:
    - from: "main (origin)"
      to: "GitHub Pages"
      via: ".github/workflows/deploy.yml"
      pattern: "actions/deploy-pages@v4"
---

## Phase Goal

**Как** посетитель, **я хочу** открыть опубликованный на GitHub Pages лендинг с рабочим header, footer и визуальным языком оригинала, **чтобы** с первого экрана узнавать «Единый голос 27» и переходить к разделам по меню, пока каждый пуш в main сам прогоняет тесты, билд и деплой.

<objective>
Свести результаты волны 2 (примитивы и секции, header, footer) в один живой деплой: полный локальный прогон, пуш в main, ожидание workflow, проверка живого URL и ассетов, браузерный smoke на 1440 и 390px через Playwright MCP при его доступности.

Purpose: планы волны 2 коммитили, но не пушили, чтобы параллельные исполнители не гонялись за `origin/main`; этот план публикует итог фазы и подтверждает критерии успеха 1–5 из ROADMAP на живом сайте.

Output: зелёный прогон workflow на финальном коммите фазы, 200 с живого URL, отчёт о проверке в SUMMARY, README с разделом о проверке деплоя.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-deploy/01-CONTEXT.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md
@.planning/phases/01-scaffold-and-deploy/01-01-SUMMARY.md
@.planning/phases/01-scaffold-and-deploy/01-02-SUMMARY.md
@.planning/phases/01-scaffold-and-deploy/01-03-SUMMARY.md
@.planning/phases/01-scaffold-and-deploy/01-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Полный локальный гейт, пуш и проверка живого деплоя</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-CONTEXT.md (раздел «Проект и CI», пункт «Проверка деплоя»)
    - .planning/phases/01-scaffold-and-deploy/01-01-SUMMARY.md, 01-02-SUMMARY.md, 01-03-SUMMARY.md, 01-04-SUMMARY.md (что сделано и какие отклонения зафиксированы)
    - .github/workflows/deploy.yml (что именно прогоняет CI)
    - README.md (текущее содержимое из плана 01-01)
    - .planning/research/PITFALLS.md (чеклист «Looks Done But Isn't»)
  </read_first>
  <action>
1. Локальный гейт из чистого состояния: `git status --porcelain` пуст (все планы волны 2 закоммичены; если есть незакоммиченные файлы из планов 01-02…01-04, закоммитить их с сообщением `feat(01): finish wave 2 shell`); `npm ci`; `npm run lint`; `npm test` (ожидается 7 файлов тестов: App, Wordmark, primitives, placeholders, scrollToSection, Header, Footer); `npm run build`. Запустить `npm run preview` в фоне на порту 4173, `curl -s http://localhost:4173/esd-onevoice27/` должен вернуть HTML с `lang="ru"` и `/esd-onevoice27/assets/`; сохранить список ссылок на ассеты из локального HTML; остановить preview.

2. `README.md`: добавить раздел «Проверка деплоя» с командами `gh run list --workflow deploy.yml --limit 1`, `gh run watch <id>`, `curl -sI https://thevladoss.github.io/esd-onevoice27/` и коротким описанием, что проверять (200, ассеты по `/esd-onevoice27/assets/`, header и footer в DOM). Раздел на русском, без em-dash внутри абзацев.

3. Закоммитить README, `git push origin main`. `gh run list --workflow deploy.yml --limit 1 --json databaseId,headSha,status` → `headSha` должен совпасть с `git rev-parse HEAD`; `gh run watch <databaseId> --exit-status`. При падении `npm test`/`npm run build` в CI (расхождение с локальным Node 25 против Node 22): починить, закоммитить, повторить пуш.

4. Проверка живого сайта: до 10 попыток с паузой 15 с `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` содержит `200`; `curl -s https://thevladoss.github.io/esd-onevoice27/` содержит `lang="ru"`, `<title>Единый голос 27 — Евро-Азиатский дивизион</title>`, `og:url`, и тот же набор ссылок на `/esd-onevoice27/assets/*` (по хэшам), что и локальный preview из п. 1; каждый такой ассет плюс `/esd-onevoice27/favicon.svg` отвечает 200 через `curl -sI https://thevladoss.github.io<путь>`. Результаты (id прогона, sha, список проверенных URL) записать в SUMMARY.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && gh run list --workflow deploy.yml --limit 1 --json conclusion,headSha --jq '.[0] | select(.conclusion=="success") | .headSha' | grep -q "$(git rev-parse HEAD)" && curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1 | grep -q 200 && curl -s https://thevladoss.github.io/esd-onevoice27/ | grep -q '/esd-onevoice27/assets/'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint`, `npm test`, `npm run build` локально завершаются кодом 0; `npm test` показывает 7 файлов тестов passed
    - `git status --porcelain` пуст; `git rev-parse HEAD` = `git rev-parse origin/main`
    - `gh run list --workflow deploy.yml --limit 1 --json conclusion,headSha --jq '.[0].conclusion'` = `success`, а `.[0].headSha` = `git rev-parse HEAD`
    - `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` содержит `200`
    - Живой HTML содержит `lang="ru"`, `Единый голос 27 — Евро-Азиатский дивизион`, `og:url`, ссылки на `/esd-onevoice27/assets/`; каждая ссылка на ассет и `/esd-onevoice27/favicon.svg` отвечают `200`
    - Набор `/esd-onevoice27/assets/*` в живом HTML совпадает с набором из локального `vite preview` того же коммита
    - `README.md` содержит заголовок «Проверка деплоя», `gh run watch` и `curl -sI https://thevladoss.github.io/esd-onevoice27/`
  </acceptance_criteria>
  <done>Финальный коммит фазы задеплоен, живой сайт отдаёт актуальный билд с проверенными ассетами, README описывает проверку.</done>
</task>

<task type="auto">
  <name>Task 2: Браузерный smoke живого сайта на 1440 и 390px</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md (строки 569–583 Verification: чеклист сдачи фазы)
    - .planning/phases/01-scaffold-and-deploy/01-CONTEXT.md (пункт «Проверка деплоя»: нет 404 на ассеты, консоль без ошибок, header и footer в DOM)
    - .planning/research/PITFALLS.md (чеклист «Looks Done But Isn't»)
  </read_first>
  <action>
1. Если в сессии доступны инструменты Playwright MCP (`browser_navigate`, `browser_resize`, `browser_snapshot`, `browser_console_messages`, `browser_click`, `browser_press_key`, `browser_network_requests`): открыть `https://thevladoss.github.io/esd-onevoice27/` на 1440×900; проверить, что в снапшоте доступности есть `banner`, `navigation "Основная навигация"` с четырьмя ссылками, `main`, восемь секций (по заголовкам из `copy.sections.*.title`), один `heading level 1` «Вместе, единым голосом», ссылка «Зажечь свой свет», `contentinfo` с `navigation "Внешние ссылки"`; консоль без `error`, сетевые запросы без статусов 404/5xx (кроме допустимых `fonts.googleapis.com`/`fonts.gstatic.com` таймаутов, которых быть не должно); клик по «Новости» меняет прокрутку (снапшот показывает секцию «Каждая платформа становится голосом» в вьюпорте). Затем 390×844: снапшот содержит кнопку «Открыть меню»; клик по ней даёт `dialog "Меню"` с четырьмя ссылками и кнопку «Закрыть меню»; `Escape` закрывает диалог; горизонтального скролла нет (ширина `document.documentElement.scrollWidth` равна 390 через `browser_evaluate`, если инструмент доступен). Сделать по одному скриншоту на каждую ширину в scratchpad для SUMMARY (в репозиторий не коммитить).

2. Если Playwright MCP недоступен: не имитировать браузерную проверку. Выполнить только curl-проверки из Task 1 и записать в SUMMARY явный пункт «Браузерный smoke не выполнен: Playwright MCP недоступен в сессии; перенесено в фазу 5 (QA-04)».

3. Любую найденную проблему (404 ассета, ошибка консоли, отсутствующая секция, не закрывающееся меню) починить в файлах, принадлежащих фазе 1, закоммитить, запушить, дождаться `gh run watch --exit-status` и повторить проверку. Итоги (список проверенных пунктов чеклиста UI-SPEC с результатом, id прогона) записать в SUMMARY; README не менять, если проблем не было.
  </action>
  <verify>
    <automated>curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1 | grep -q 200 && gh run list --workflow deploy.yml --limit 1 --json conclusion --jq '.[0].conclusion' | grep -q success</automated>
    <human-check>Открыть https://thevladoss.github.io/esd-onevoice27/ на десктопе и телефоне: пилюля header со скосом уплотняется при скролле, клик по четырём пунктам ведёт к секциям, бургер открывает и закрывает меню (Esc тоже), волны footer дрейфуют, при включённом «уменьшении движения» замирают.</human-check>
  </verify>
  <acceptance_criteria>
    - Если Playwright MCP доступен: в SUMMARY зафиксированы результаты по каждому пункту: banner, navigation с 4 ссылками, main, 8 секций, 1 h1, CTA, contentinfo, консоль без ошибок, сеть без 404, меню на 390px открывается и закрывается по Escape, `scrollWidth` = 390
    - Если недоступен: в SUMMARY есть пункт «Браузерный smoke не выполнен: Playwright MCP недоступен в сессии; перенесено в фазу 5 (QA-04)»
    - Последний прогон `deploy.yml` на `origin/main` завершён `success`, живой URL отвечает 200
    - `git status --porcelain` пуст, `HEAD` = `origin/main`
  </acceptance_criteria>
  <done>Живой сайт проверен в браузере (или честно отмечено, что проверка отложена), все правки задеплоены, фаза готова к verify.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Локальная машина → origin/main → GitHub Actions → Pages | Публикация кода через OIDC-деплой |
| Браузер → живой сайт | Публичный статический контент |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Repudiation | deploy.yml прогон | mitigate | Записать в SUMMARY id прогона и `headSha`, чтобы деплой был привязан к коммиту |
| T-01-13 | Tampering | Расхождение локального билда и живого | mitigate | Сравнение набора хэшированных ассетов `vite preview` и живого HTML на одном коммите (Task 1, п. 4) |
| T-01-14 | Information Disclosure | Скриншоты Playwright | accept | Хранятся в scratchpad, в репозиторий не коммитятся; на страницах нет личных данных |
| T-01-SC | Tampering | npm installs | mitigate | Только `npm ci` по lockfile; новых пакетов план не добавляет |
</threat_model>

<verification>
- `gh run list --workflow deploy.yml --limit 1` → `success` на `headSha` = `HEAD`.
- `curl -sI https://thevladoss.github.io/esd-onevoice27/` → 200; все ассеты 200.
- SUMMARY содержит результаты браузерной проверки или явную пометку о её переносе.
</verification>

<success_criteria>
- Критерий 5 ROADMAP: сайт доступен по адресу, Actions прогоняет npm ci, npm test, npm run build перед деплоем.
- Критерии 1–4 ROADMAP подтверждены на живом сайте (Playwright MCP) или перенесены с явной пометкой в фазу 5.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-and-deploy/01-05-SUMMARY.md` when done. Включить: id и sha финального прогона, список проверенных URL ассетов, результаты браузерного чеклиста (или пометку о переносе), ссылки на скриншоты в scratchpad.
</output>
