---
phase: 04-news-resources-quote
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/paginate.ts
  - src/lib/paginate.test.ts
  - src/data/news.ts
  - src/data/copy.news.ts
  - src/components/news/News.tsx
  - src/components/news/NewsCard.tsx
  - src/components/news/NewsPagination.tsx
  - src/components/news/News.test.tsx
autonomous: true
requirements: [NEWS-01, NEWS-02]
tags: [news, pagination, react, vitest]

must_haves:
  truths:
    - "Посетитель видит в секции #news eyebrow «На каждом канале», H2 «Каждая платформа становится голосом» и сетку карточек новостей с обложкой, датой и заголовком"
    - "На первой странице 6 карточек, после клика по кнопке «Страница 2» их 3; страница не перезагружается и скролл не прыгает"
    - "Каждая карточка целиком является ссылкой, открывающейся в новой вкладке с rel=\"noopener noreferrer\""
    - "Активная кнопка пагинации несёт aria-current=\"page\", стрелка «Следующая страница» на последней странице disabled, скрытый role=\"status\" объявляет «Страница N из M»"
    - "Если обложка не загрузилась, карточка показывает плашку «Обложка недоступна», ссылка и заголовок остаются"
  artifacts:
    - path: "src/lib/paginate.ts"
      provides: "paginate<T>(items, page, perPage = 6) → { items, page, totalPages } с клампом page"
      exports: ["paginate"]
    - path: "src/data/news.ts"
      provides: "9 типизированных мок-новостей NewsItem, отсортированных от новых к старым"
      exports: ["news", "NewsItem"]
    - path: "src/data/copy.news.ts"
      provides: "строки секции новостей (eyebrow, title, body, pagination labels, empty/error state)"
      exports: ["newsCopy"]
    - path: "src/components/news/News.tsx"
      provides: "секция #news со скошенным верхом, шапкой, сеткой и пагинацией на useState"
      min_lines: 60
    - path: "src/components/news/NewsCard.tsx"
      provides: "article → a карточка 4:5 с обложкой, датой <time>, заголовком h3 и fallback обложки"
      exports: ["NewsCard", "formatNewsDate"]
    - path: "src/components/news/NewsPagination.tsx"
      provides: "nav[aria-label] с кнопками страниц и стрелкой"
      exports: ["NewsPagination"]
  key_links:
    - from: "src/components/news/News.tsx"
      to: "src/lib/paginate.ts"
      via: "paginate(items, page, 6) в рендере"
      pattern: "paginate\\("
    - from: "src/components/news/News.tsx"
      to: "src/data/news.ts"
      via: "items = news по умолчанию"
      pattern: "from \"\\.\\./\\.\\./data/news\""
    - from: "src/components/news/NewsCard.tsx"
      to: "img.youtube.com"
      via: "item.cover в <img src>"
      pattern: "src=\\{.*cover"
    - from: "src/components/news/NewsPagination.tsx"
      to: "src/components/news/News.tsx"
      via: "onChange(page) → setPage"
      pattern: "onChange"
---

## Phase Goal

**As a** посетитель лендинга ЕАД, **I want to** просматривать новости дивизиона, находить нужные материалы через раскрывающиеся панели ресурсов и читать цитату из «Евангелизма», **so that** узнать, чем живёт движение в дивизионе, и сразу забрать реальные материалы для своей церкви.

Этот план закрывает первую часть истории: лента новостей с постраничным просмотром.

<objective>
Заменить заглушку `News` из фазы 1 на рабочую секцию новостей: 9 мок-новостей ЕАД в сетке карточек с обложками из YouTube-превью, датой и заголовком, клиентская пагинация по 6 на страницу через `useState` и чистую функцию `paginate`.

Purpose: NEWS-01 и NEWS-02. Посетитель листает новости дивизиона без перезагрузки, а `paginate` покрыта unit-тестами (вклад в QA-01 из фазы 2, где пагинация новостей уже перечислена).
Output: `src/lib/paginate.ts` + тест, `src/data/news.ts`, `src/data/copy.news.ts`, `News.tsx`, `NewsCard.tsx`, `NewsPagination.tsx`, `News.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-resources-quote/04-CONTEXT.md
@.planning/phases/04-news-resources-quote/04-UI-SPEC.md
@.planning/phases/04-news-resources-quote/04-RESEARCH.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Примитивы фазы 1 (по 01-CONTEXT.md и 01-UI-SPEC.md). Перед использованием прочитать реальные файлы: сигнатуры могли уточниться при исполнении фазы 1. -->

From src/components/layout/Section.tsx:
```typescript
// Props: { id: string; eyebrow?: string; title?: string; children: ReactNode; className?: string }
// Рендерит <section id> с max-width 72rem, py-16 md:py-24 px-4 md:px-8. При eyebrow/title рендерит Eyebrow и GradientTitle as="h2".
```

From src/components/layout/Eyebrow.tsx:
```typescript
// <p> uppercase, Noto Sans 700, 12px, letter-spacing .1em, цвет horizon-200
export function Eyebrow(props: { children: ReactNode; className?: string }): JSX.Element;
```

From src/components/layout/GradientTitle.tsx:
```typescript
export function GradientTitle(props: { as: "h1" | "h2"; variant: "hero" | "section"; children: ReactNode; className?: string }): JSX.Element;
```

From src/components/layout/Button.tsx:
```typescript
// variant: "primary" | "ghost"; as: "a" | "button"; остальные атрибуты пробрасываются
```

Tailwind v4 токены фазы 1 (утилиты): bg-midnight-950 / bg-midnight-900 / bg-midnight-800, text-paper, text-horizon-200, text-horizon-400, text-signal-300, rounded-card, shadow-card, font-display (Onest), font-body (Noto Sans), ease-header (cubic-bezier(0.32, 0.72, 0, 1)).
CSS-переменные из :root global.css: --glass-border, --glass-surface, --gradient-action (125deg #6c2c68 0%, #3b4da1 50%, #39727e 100%), --radius-card.

Vitest окружение фазы 1 (src/test/setup.ts): jest-dom, моки IntersectionObserver, ResizeObserver, window.matchMedia, HTMLCanvasElement.getContext, Element.prototype.scrollIntoView, window.scrollTo. `globals: true`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Падающие тесты пагинации и happy path секции новостей (RED)</name>
  <files>src/lib/paginate.test.ts, src/components/news/News.test.tsx</files>
  <read_first>
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «NewsCard», «NewsPagination», «Copywriting Contract», «Contract Checks»)
    - .planning/phases/04-news-resources-quote/04-CONTEXT.md (раздел «Новости (#news)»)
    - .planning/phases/04-news-resources-quote/04-RESEARCH.md (раздел «Пагинация»)
    - src/test/setup.ts (какие моки уже есть)
    - src/components/layout/Header.test.tsx или любой существующий *.test.tsx (стиль импортов Testing Library и Vitest в проекте)
    - src/components/news/News.tsx (заглушка фазы 1: default или named export; тест импортирует тем же способом)
  </read_first>
  <action>
    Написать тесты до реализации. Оба файла падают на импорте отсутствующих модулей, это ожидаемое RED-состояние.

    `src/lib/paginate.test.ts` импортирует `paginate` из `./paginate` и проверяет контракт `paginate<T>(items: T[], page: number, perPage = 6): { items: T[]; page: number; totalPages: number }`:
    массив из 9 чисел, страница 1 → 6 элементов, `totalPages === 2`, `page === 1`; страница 2 → элементы с индексами 6..8 (3 штуки), `page === 2`; `page = 0` зажимается в 1; `page = 99` зажимается в 2 и отдаёт 3 элемента; пустой массив → `items: []`, `totalPages: 1`, `page: 1`; `perPage = 4` для 9 элементов → `totalPages === 3`; `perPage` по умолчанию равен 6 (вызов без третьего аргумента совпадает с `perPage = 6`).

    `src/components/news/News.test.tsx` рендерит `News` (тем же видом импорта, что в заглушке) и проверяет happy path из UI-SPEC «Contract Checks»:
    1. `screen.getAllByRole("article")` даёт 6 элементов; в DOM есть заголовок уровня 2 с текстом «Каждая платформа становится голосом» и текст «На каждом канале».
    2. У каждой ссылки внутри статей `target="_blank"` и `rel="noopener noreferrer"`; в каждой статье есть `<time>` с атрибутом `dateTime`, совпадающим с `/^\d{4}-\d{2}-\d{2}$/`.
    3. Кнопка `getByRole("button", { name: "Страница 1" })` имеет `aria-current="page"`; после `fireEvent.click(getByRole("button", { name: "Страница 2" }))` статей 3, `aria-current="page"` переехал на «Страница 2», `getByRole("button", { name: "Следующая страница" })` имеет атрибут `disabled`, `getByRole("status")` содержит текст «Страница 2 из 2», `document.activeElement` остаётся кнопкой «Страница 2» (для этого перед кликом вызвать `.focus()` на ней). `window.scrollTo` и `scrollIntoView` не вызывались (моки из setup.ts, проверить через `vi.spyOn` или `expect(...).not.toHaveBeenCalled()` если они `vi.fn`).
    4. Кнопка «Следующая страница» на первой странице не disabled; клик по ней переводит на страницу 2.
    5. `formatNewsDate("2026-09-05")`, импортированная из `./NewsCard`, возвращает строку «5 сентября 2026» (без «г.»).
    6. `nav` имеет `aria-label="Пагинация новостей"`.

    Используй `render`, `screen`, `within`, `fireEvent` из `@testing-library/react`. Не добавляй `@testing-library/user-event`: новых зависимостей в фазе нет.

    Запусти `npm test -- paginate News` и убедись, что оба файла падают именно из-за отсутствующих модулей. Закоммить: `test(04-01): add failing tests for news pagination and grid`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/lib/paginate.test.ts src/components/news/News.test.tsx 2>&1 | grep -E "FAIL|failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/paginate.test.ts` содержит минимум 6 `it(`/`test(` блоков; grep -c "totalPages" даёт ≥ 4
    - `src/components/news/News.test.tsx` содержит строки `getAllByRole("article")`, `"Страница 2"`, `"Следующая страница"`, `aria-current`, `noopener noreferrer`, `formatNewsDate`
    - `npx vitest run src/lib/paginate.test.ts src/components/news/News.test.tsx` завершается с ненулевым кодом (RED), причина в выводе: `Failed to resolve import` или `Cannot find module` для `./paginate` / `./NewsCard`
    - Коммит с сообщением, начинающимся на `test(04-01):`, существует в `git log -1`
  </acceptance_criteria>
  <done>Два тестовых файла закоммичены и падают из-за отсутствия реализации.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Тончайший срез: данные, paginate и секция новостей с пагинацией (GREEN)</name>
  <files>src/lib/paginate.ts, src/data/news.ts, src/data/copy.news.ts, src/components/news/News.tsx, src/components/news/NewsCard.tsx, src/components/news/NewsPagination.tsx</files>
  <read_first>
    - src/components/news/News.tsx (заглушка фазы 1: сохранить вид экспорта, `id="news"`)
    - src/components/layout/Section.tsx, src/components/layout/Eyebrow.tsx, src/components/layout/GradientTitle.tsx (реальные пропсы и классы контейнера)
    - src/styles/global.css и src/styles/tokens.css (имена токенов, утилиты `.glass`, `.text-gradient-brand`, `.focus-ring`, значение `--ease-header`)
    - src/data/copy.ts (форма объекта копирайта фазы 1, чтобы `copy.news.ts` повторял стиль; сам `copy.ts` НЕ редактировать)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «Layout Contract → Новости», «NewsCard», «NewsPagination», «Typography», «Color», «Interaction States»)
    - .planning/phases/04-news-resources-quote/04-RESEARCH.md (таблица 16 id и названий для обложек)
  </read_first>
  <behavior>
    - paginate: 9 элементов, perPage 6 → страница 1 из 6 элементов, страница 2 из 3, totalPages 2; page вне [1, totalPages] зажимается; пустой массив → totalPages 1
    - News: первая страница рендерит 6 article, клик по «Страница 2» рендерит 3; aria-current переезжает; стрелка disabled на последней странице; role="status" объявляет «Страница 2 из 2»; фокус остаётся на кнопке
    - NewsCard: ссылка с target="_blank" rel="noopener noreferrer", `<time dateTime="YYYY-MM-DD">` с текстом вида «5 сентября 2026»
  </behavior>
  <action>
    Реализовать всё, что нужно для GREEN по тестам Task 1. Никаких новых зависимостей, `App.tsx` и `src/data/copy.ts` не трогать.

    1. `src/lib/paginate.ts`: экспорт `paginate<T>(items: T[], page: number, perPage = 6)`. `totalPages = Math.max(1, Math.ceil(items.length / perPage))`, `safePage = Math.min(Math.max(1, Math.trunc(page) || 1), totalPages)`, `items.slice((safePage - 1) * perPage, safePage * perPage)`. Возвращает `{ items, page: safePage, totalPages }`.

    2. `src/data/copy.news.ts`: `export const newsCopy = { eyebrow: "На каждом канале", title: "Каждая платформа становится голосом", body: "Единый голос 27 встречает людей там, где они уже есть: на каждом экране и каждой частоте.", paginationLabel: "Пагинация новостей", nextPage: "Следующая страница", pageLabel: (n: number) => `Страница ${n}`, pageStatus: (page: number, total: number) => `Страница ${page} из ${total}`, emptyTitle: "На этой странице новостей нет", emptyAction: "Вернуться к первой странице", coverFailedTitle: "Обложка недоступна", coverFailedBody: "Заголовок и ссылка на месте — откройте новость" } as const`. Ключи из UI-SPEC «Copywriting Contract»; отдельный файл вместо `copy.ts`, потому что фаза идёт параллельно с фазой 3 (фаза 5 сольёт).

    3. `src/data/news.ts`: `export type NewsItem = { id: string; title: string; date: string; cover: string; href: string; source: string }` (D: CONTEXT «Новости»). Хелпер-константа `const cover = (videoId: string) => `https://img.youtube.com/vi/${videoId}/hqdefault.jpg``. Массив `news: NewsItem[]` из 9 записей, отсортирован от новых к старым, даты ISO `YYYY-MM-DD`:
       - `day-of-prayer`, «5 сентября — всемирный день молитвы «Единый голос 27»», 2026-09-05, cover `YpLD6p-z00g`, href `https://www.youtube.com/watch?v=YpLD6p-z00g`, source «YouTube ЕАД»
       - `kaminsky`, «Михаил Каминский: почему дивизион присоединяется к движению», 2026-08-28, cover `qQsgK18gKCU`, href `https://www.youtube.com/watch?v=qQsgK18gKCU`, source «YouTube ЕАД»
       - `koehler`, «Эртон Келер о «Едином голосе 27»: одна весть на всех языках», 2026-08-21, cover `dnQS3tFmCNU`, href `https://www.youtube.com/watch?v=dnQS3tFmCNU`, source «YouTube ЕАД»
       - `leaders`, «Руководители Адвентистской церкви — о проекте», 2026-08-14, cover `GK_RXxwZxEc`, href `https://www.youtube.com/watch?v=GK_RXxwZxEc`, source «YouTube ЕАД»
       - `expectations`, «Что мы ожидаем от проекта «Единый голос 27»?», 2026-08-07, cover `xH3MMwox8cU`, href `https://www.youtube.com/watch?v=xH3MMwox8cU`, source «YouTube ЕАД»
       - `departments`, «Как отделы церкви поддерживают проект», 2026-07-31, cover `gwqe_QH6KX0`, href `https://www.youtube.com/watch?v=gwqe_QH6KX0`, source «YouTube ЕАД»
       - `how-to-support`, «Как я могу поддержать «Единый голос 27»?», 2026-07-24, cover `1MN5Gml00QU`, href `https://www.youtube.com/watch?v=1MN5Gml00QU`, source «YouTube ЕАД»
       - `banners`, «Опубликованы баннеры к 5 сентября 2026», 2026-07-10, cover `-Eo--61cx90`, href `https://esd.onevoice27.org/materials/banners`, source «esd.onevoice27.org»
       - `desire-of-ages`, «Вопросы по книге «Желание веков»: материалы для малых групп», 2026-06-26, cover `VjwJfHAqIxQ`, href `https://esd.onevoice27.org/materials/desire-of-ages`, source «esd.onevoice27.org»

    4. `src/components/news/NewsCard.tsx`: named export `formatNewsDate(iso: string): string`: `new Intl.DateTimeFormat("ru-RU", { day: "numeric", month: "long", year: "numeric", timeZone: "UTC" }).format(new Date(iso))` с последующим удалением хвоста ` г.` через `.replace(/\s?г\.$/u, "")`. `timeZone: "UTC"` обязателен: `new Date("2026-09-05")` парсится как UTC-полночь и без него в западных зонах даёт 4 сентября. Компонент `NewsCard({ item }: { item: NewsItem })`: `<article>` → единственная `<a href={item.href} target="_blank" rel="noopener noreferrer">` на всю карточку (`relative block overflow-hidden rounded-card border border-[var(--glass-border)] bg-midnight-900`, группа `group`), внутри обложка `aspect-[4/5]` с `<img src={item.cover} alt="" loading="lazy" decoding="async" className="h-full w-full object-cover object-center transition-transform duration-[520ms] ease-header group-hover:scale-[1.04] motion-reduce:transform-none">`, скрим `pointer-events-none absolute inset-0 bg-[linear-gradient(180deg,transparent_38%,rgb(7_2_16/.82)_100%)]`, текстовый блок абсолютом `inset-x-4 bottom-4`: `<time dateTime={item.date}>` (Label 12px 700 uppercase tracking-[0.08em], text-horizon-200) и `<h3>` (font-display font-extrabold text-[22px] leading-[1.15] tracking-[-0.03em] text-paper, `line-clamp-4`, `[text-wrap:balance]`), зазор 8px. Hover-рамка `hover:border-[rgb(123_194_199/.4)]`, focus `focus-within:border-[rgb(123_194_199/.4)]` плюс `focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-[3px]` на ссылке. Состояние ошибки обложки добавит Task 3, сейчас только `<img>`.

    5. `src/components/news/NewsPagination.tsx`: `NewsPagination({ page, totalPages, onChange }: { page: number; totalPages: number; onChange: (page: number) => void })`. `<nav aria-label={newsCopy.paginationLabel}>` → `<ul className="flex items-center justify-center gap-2">`: по кнопке на страницу `<button type="button" aria-label={newsCopy.pageLabel(n)} aria-current={n === page ? "page" : undefined} onClick={() => onChange(n)}>` с классами `min-h-11 min-w-11 rounded-lg text-xs font-bold uppercase tracking-[0.08em]`; неактивная `text-paper/60 hover:bg-[rgb(33_26_62/.44)] hover:text-paper`, активная `bg-[image:var(--gradient-action)] text-paper shadow-[0_10px_24px_rgb(59_77_161/.34)]`; затем стрелка `<button type="button" aria-label={newsCopy.nextPage} disabled={page >= totalPages} onClick={() => onChange(page + 1)} className="... disabled:opacity-[.38] disabled:cursor-not-allowed">` с inline SVG chevron 20×20 (`stroke="currentColor" stroke-width="1.5" fill="none" aria-hidden="true"`). Все кнопки с `focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-2`. Никаких `scrollIntoView`/`scrollTo` в обработчиках.

    6. `src/components/news/News.tsx`: тот же вид экспорта, что у заглушки. Пропс `items?: NewsItem[]` со значением по умолчанию `news` (нужен тестам пустого состояния в Task 3). `const [page, setPage] = useState(1); const result = paginate(items, page, 6);`. Разметка: `<section id="news" className="relative -mt-6 bg-midnight-900 [clip-path:polygon(0_24px,100%_0,100%_100%,0_100%)] md:-mt-12 md:[clip-path:polygon(0_48px,100%_0,100%_100%,0_100%)]">`, фон-атмосфера через `before:` псевдоэлемент или вложенный `aria-hidden` div с `bg-[radial-gradient(circle_at_88%_18%,rgb(84_164_172/.14),transparent_38%),radial-gradient(circle_at_8%_72%,rgb(48_63_131/.22),transparent_42%)]`. Внутренний контейнер повторяет `Section` фазы 1: `mx-auto max-w-[72rem] px-4 md:px-8 pb-16 md:pb-24`, верхний отступ `pt-[calc(4rem+24px)] md:pt-[calc(6rem+48px)]` (скос плюс ритм секции). Собственный `<section>` вместо `Section` потому, что фон и clip-path нужны на всю ширину, а `Section` ограничен 72rem. Шапка слева `max-w-[34rem]`: `<Eyebrow>{newsCopy.eyebrow}</Eyebrow>`, `<GradientTitle as="h2" variant="section">{newsCopy.title}</GradientTitle>` (зазор 8px), `<p className="mt-4 text-base leading-[1.5] text-paper/80">{newsCopy.body}</p>`. Сетка `<ul className="mt-12 grid gap-6 md:grid-cols-2 lg:grid-cols-3 lg:gap-8">` из `<li key={item.id}><NewsCard item={item} /></li>`. Под сеткой `<p role="status" className="sr-only">{newsCopy.pageStatus(result.page, result.totalPages)}</p>` и `<div className="mt-12"><NewsPagination page={result.page} totalPages={result.totalPages} onChange={setPage} /></div>`.

    Прогнать `npm test` (весь набор, включая App.test фазы 1) и `npm run build`. Закоммить: `feat(04-01): news grid with client-side pagination`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/lib/paginate.test.ts src/components/news/News.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/paginate.test.ts src/components/news/News.test.tsx` завершается с кодом 0
    - `npm test` (весь набор) завершается с кодом 0; `npm run build` завершается с кодом 0
    - `grep -c "img.youtube.com/vi/" src/data/news.ts` ≥ 1 и в `src/data/news.ts` ровно 9 объектов с полем `date:` (`grep -c "date: \"2026-" src/data/news.ts` = 9)
    - `grep -n "rel=\"noopener noreferrer\"" src/components/news/NewsCard.tsx` находит строку; `grep -n "timeZone: \"UTC\"" src/components/news/NewsCard.tsx` находит строку
    - `grep -n "aria-current" src/components/news/NewsPagination.tsx` и `grep -n "role=\"status\"" src/components/news/News.tsx` находят строки
    - `grep -n "scrollIntoView\|scrollTo" src/components/news/*.tsx` ничего не находит
    - `grep -n "id=\"news\"" src/components/news/News.tsx` находит строку; `src/App.tsx` и `src/data/copy.ts` не изменены (`git diff --name-only HEAD~1 -- src/App.tsx src/data/copy.ts` пуст)
    - В `src/components/news/NewsCard.tsx` нет `dangerouslySetInnerHTML`
  </acceptance_criteria>
  <done>Секция новостей рендерит 9 новостей по 6 на страницу, пагинация переключает страницы без скролла, тесты Task 1 зелёные, билд проходит.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Состояния ошибки и пустой страницы, доступность и reduced motion</name>
  <files>src/components/news/NewsCard.tsx, src/components/news/News.tsx, src/components/news/News.test.tsx</files>
  <read_first>
    - src/components/news/NewsCard.tsx и src/components/news/News.tsx (результат Task 2)
    - src/components/layout/Button.tsx (пропсы `variant`, `as`, проброс `onClick`)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (строки «Empty state (пустая страница новостей)», «Error state (обложка новости не загрузилась)», «Motion Contract», «Accessibility Contract»)
  </read_first>
  <behavior>
    - Test: `fireEvent.error(img)` на обложке первой карточки → в карточке появляется текст «Обложка недоступна» и «Заголовок и ссылка на месте — откройте новость», `<img>` больше не в DOM, ссылка и `<h3>` остаются
    - Test: `render(<News items={[]} />)` → нет `article`, есть текст «На этой странице новостей нет» и кнопка «Вернуться к первой странице»; пагинация показывает одну страницу
    - Test: заголовок `<h3>` внутри каждой карточки содержит текст новости; у `<img>` атрибут `alt=""` и `loading="lazy"`
  </behavior>
  <action>
    Сначала дописать три теста из `<behavior>` в `News.test.tsx` и убедиться, что они падают. Затем:

    1. `NewsCard.tsx`: `const [coverFailed, setCoverFailed] = useState(false)`; у `<img>` `onError={() => setCoverFailed(true)}`. При `coverFailed` вместо `<img>` рендерится плашка `aria-hidden="true"` на ту же область 4:5: `bg-[linear-gradient(145deg,rgb(48_63_131/.86),rgb(18_12_52/.76))]`, внутри две строки текстом `newsCopy.coverFailedTitle` (Label 12px 700 uppercase text-paper/60) и `newsCopy.coverFailedBody` (text-xs text-paper/80). Плашка декоративная, поэтому `aria-hidden`; заголовок и ссылка уже несут смысл. Тексты плашки всё же должны присутствовать в DOM (тест ищет через `getByText`, который видит aria-hidden узлы).

    2. `News.tsx`: если `result.items.length === 0`, вместо `<ul>` рендерить блок `mt-12 rounded-card border border-[var(--glass-border)] p-8 text-center` с `<p className="text-base text-paper/80">{newsCopy.emptyTitle}</p>` и `<Button variant="ghost" as="button" type="button" onClick={() => setPage(1)}>{newsCopy.emptyAction}</Button>` (`mt-6`). Пагинация остаётся под блоком.

    3. Доступность и motion: у ссылки карточки `focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-[3px]`; масштаб обложки при hover только `motion-safe:group-hover:scale-[1.04]` (Tailwind `motion-safe:` = `@media (prefers-reduced-motion: no-preference)`), в reduced motion картинка неподвижна. Порядок заголовков: один `<h2>` секции, `<h3>` в каждой карточке. Проверить, что `<time>` идёт перед `<h3>` в DOM (дата над заголовком).

    Прогнать `npm test` и `npm run build`. Закоммить: `feat(04-01): news cover fallback, empty state and a11y polish`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/news/News.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/news/News.test.tsx` завершается с кодом 0 и в выводе ≥ 9 passed
    - `grep -n "onError" src/components/news/NewsCard.tsx` находит строку; `grep -n "coverFailedTitle" src/components/news/NewsCard.tsx` находит строку
    - `grep -n "emptyTitle\|emptyAction" src/components/news/News.tsx` находит обе строки
    - `grep -n "motion-safe:group-hover:scale" src/components/news/NewsCard.tsx` находит строку
    - `npm test` завершается с кодом 0; `npm run build` завершается с кодом 0
    - `git diff --name-only HEAD~3 -- src | grep -v -E "^src/(lib/paginate|data/news|data/copy\.news|components/news/)"` пуст (план не тронул чужие файлы)
  </acceptance_criteria>
  <done>Карточка переживает 404 обложки, пустая страница объясняет себя и ведёт на первую, фокус видим, reduced motion отключает зум обложки.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер → внешние сайты (youtube.com, esd.onevoice27.org) | Переход по ссылке карточки в новую вкладку |
| Браузер ← img.youtube.com | Сторонние изображения обложек |
| Статические данные `src/data/news.ts` → React | Заголовки и URL из типизированного TS-модуля |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `NewsCard` `<a target="_blank">` | mitigate | `rel="noopener noreferrer"` на каждой ссылке, проверяется тестом |
| T-04-02 | Information Disclosure | `<img src="https://img.youtube.com/...">` | accept | Утечка только реферера страницы GitHub Pages третьей стороне (YouTube); персональных данных нет |
| T-04-03 | Denial of Service | Недоступность img.youtube.com | mitigate | `onError` → плашка «Обложка недоступна», ссылка остаётся рабочей |
| T-04-04 | Tampering (XSS) | Рендер заголовков и дат | mitigate | Только JSX-интерполяция, без `dangerouslySetInnerHTML`; данные статические |
| T-04-SC | Tampering | npm installs | accept | В плане нет установки пакетов; появление `npm install` в исполнении считается отклонением и останавливает задачу |
</threat_model>

<verification>
- `npm test` зелёный, включая `App.test.tsx` фазы 1 (восемь секций с id на месте).
- `npm run build` проходит без ошибок TypeScript.
- Ручной smoke (`npm run dev`): в `#news` 6 карточек, клик «2» показывает 3, стрелка гаснет на второй странице, страница не прыгает; обложки грузятся с img.youtube.com; при отключённой сети карточка показывает плашку.
</verification>

<success_criteria>
- NEWS-01: eyebrow, H2, сетка из 9 новостей с обложкой YouTube-превью, датой, заголовком и внешней ссылкой (3/2/1 колонки).
- NEWS-02: по 6 на страницу, кнопки «1», «2», стрелка, `aria-current`, `role="status"`, без роутинга и без скролла.
- `paginate` покрыта unit-тестами, `News` покрыта component-тестами (счётчики карточек, aria, ошибка обложки, пустая страница).
- Ни один файл вне списка `files_modified` не изменён.
</success_criteria>

<output>
Create `.planning/phases/04-news-resources-quote/04-01-SUMMARY.md` when done
</output>
