---
phase: 04-news-resources-quote
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/videos.ts
  - src/data/materials.ts
  - src/data/copy.resources.ts
  - src/components/resources/VideoFacade.tsx
  - src/components/resources/VideoGrid.tsx
  - src/components/resources/VideoFacade.test.tsx
autonomous: true
requirements: [RES-02, RES-03]
tags: [resources, video, youtube, facade, vitest]

must_haves:
  truths:
    - "Посетитель видит фасад ролика с обложкой YouTube и кнопкой play; до клика iframe не загружается"
    - "Клик по фасаду заменяет его на плеер youtube-nocookie.com с автозапуском выбранного ролика"
    - "Сетка видео содержит все 16 роликов ЕАД с подписями названий из PROJECT.md"
    - "Если обложка не загрузилась, кнопка play и aria-label остаются, показывается градиентная плашка"
  artifacts:
    - path: "src/data/videos.ts"
      provides: "16 роликов { id, title } из PROJECT.md/RESEARCH"
      exports: ["videos", "VideoItem"]
    - path: "src/data/materials.ts"
      provides: "5 материалов { id, title, caption, href, kind } с реальными ссылками ЕАД"
      exports: ["materials", "MaterialItem", "MaterialKind"]
    - path: "src/data/copy.resources.ts"
      provides: "строки секции ресурсов: eyebrow, title, body, карточки (label/title/description/cta/accent), панель, заглушка музыки"
      exports: ["resourcesCopy", "ResourceKey", "resourceKeys"]
    - path: "src/components/resources/VideoFacade.tsx"
      provides: "фасад YouTube: постер hqdefault → iframe youtube-nocookie по клику"
      exports: ["VideoFacade"]
    - path: "src/components/resources/VideoGrid.tsx"
      provides: "ul 2/3/4 колонки из 16 фасадов с подписями"
      exports: ["VideoGrid"]
  key_links:
    - from: "src/components/resources/VideoFacade.tsx"
      to: "img.youtube.com"
      via: "постер hqdefault.jpg"
      pattern: "img\\.youtube\\.com/vi/"
    - from: "src/components/resources/VideoFacade.tsx"
      to: "youtube-nocookie.com"
      via: "iframe src после клика"
      pattern: "youtube-nocookie\\.com/embed/"
    - from: "src/components/resources/VideoGrid.tsx"
      to: "src/data/videos.ts"
      via: "items = videos по умолчанию"
      pattern: "from \"\\.\\./\\.\\./data/videos\""
---

## Phase Goal

**As a** посетитель лендинга ЕАД, **I want to** просматривать новости дивизиона, находить нужные материалы через раскрывающиеся панели ресурсов и читать цитату из «Евангелизма», **so that** узнать, чем живёт движение в дивизионе, и сразу забрать реальные материалы для своей церкви.

Этот план даёт ядро панели «Видео» (16 фасадов с lite-embed) и данные всех трёх панелей ресурсов; секцию с карточками и переключением панелей собирает план 04-04.

<objective>
Создать `VideoFacade` (постер `img.youtube.com/vi/{id}/hqdefault.jpg`, кнопка play, `iframe youtube-nocookie.com` только после клика), сетку `VideoGrid` из 16 роликов ЕАД и типизированные данные ресурсов (`videos.ts`, `materials.ts`, `copy.resources.ts`), которые план 04-04 подключит к карточкам и панелям.

Purpose: RES-03 (16 роликов как фасады с lite-embed) и данные для RES-02 (5 материалов с реальными ссылками). Фасад создаётся локально (`components/resources/VideoFacade.tsx`) с API `{ videoId, title, className? }`, идентичным `VideoEmbed` фазы 3; фаза 5 сведёт их к одному. Импортов из `src/components/about/` нет: фаза 3 может исполняться параллельно в другом worktree.
Output: `src/data/videos.ts`, `src/data/materials.ts`, `src/data/copy.resources.ts`, `VideoFacade.tsx`, `VideoGrid.tsx`, `VideoFacade.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-resources-quote/04-CONTEXT.md
@.planning/phases/04-news-resources-quote/04-UI-SPEC.md
@.planning/phases/04-news-resources-quote/04-RESEARCH.md

<interfaces>
<!-- Контракты, которые этот план СОЗДАЁТ. План 04-04 реализуется против них, поэтому имена и формы фиксированы. -->

src/data/copy.resources.ts:
```typescript
export type ResourceKey = "music" | "materials" | "video";
export const resourceKeys: readonly ResourceKey[]; // ["music", "materials", "video"]
export const resourcesCopy: {
  eyebrow: string; title: string; body: string;
  cards: Record<ResourceKey, { label: string; title: string; description: string; cta: string; accent: string }>;
  panel: { close: string; closeLabel: string };
  music: { emptyTitle: string; emptyBody: string };
  video: { watchLabel: (title: string) => string }; // «Смотреть видео: {title}»
};
```

src/data/videos.ts:
```typescript
export type VideoItem = { id: string; title: string };
export const videos: readonly VideoItem[]; // 16 записей
```

src/data/materials.ts:
```typescript
export type MaterialKind = "document" | "image" | "book" | "phone" | "folder";
export type MaterialItem = { id: string; title: string; caption: string; href: string; kind: MaterialKind };
export const materials: readonly MaterialItem[]; // 5 записей
```

src/components/resources/VideoFacade.tsx:
```typescript
export function VideoFacade(props: { videoId: string; title: string; className?: string }): JSX.Element;
```

src/components/resources/VideoGrid.tsx:
```typescript
export function VideoGrid(props: { items?: readonly VideoItem[] }): JSX.Element; // по умолчанию videos
```

Токены фазы 1: text-paper, bg-midnight-950, rounded-card, font-body; утилиты `.glass`, `.focus-ring`. React 19: `ref` передаётся обычным пропсом, `forwardRef` не нужен.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Падающие тесты фасада, сетки видео и данных ресурсов (RED)</name>
  <files>src/components/resources/VideoFacade.test.tsx</files>
  <read_first>
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (раздел «VideoFacade (или VideoEmbed фазы 3)», «Asset Contract», строка Error state превью видео)
    - .planning/phases/04-news-resources-quote/04-RESEARCH.md (таблица 16 id и названий, заметка об id `-Eo--61cx90`, ссылки материалов)
    - .planning/phases/04-news-resources-quote/04-CONTEXT.md (подразделы «Материалы (data/materials.ts)» и «Видео (data/videos.ts)»)
    - src/test/setup.ts (моки)
  </read_first>
  <action>
    Один тестовый файл `VideoFacade.test.tsx`, импортирует `VideoFacade` из `./VideoFacade`, `VideoGrid` из `./VideoGrid`, `videos` из `../../data/videos`, `materials` из `../../data/materials`, `resourcesCopy` из `../../data/copy.resources`. Падает на отсутствующих модулях (RED). Проверки:

    Фасад:
    1. `render(<VideoFacade videoId="qQsgK18gKCU" title="Единый голос-27: Михаил Каминский" />)`: кнопка `getByRole("button", { name: "Смотреть видео: Единый голос-27: Михаил Каминский" })` есть; `<img>` с `src === "https://img.youtube.com/vi/qQsgK18gKCU/hqdefault.jpg"`, `alt === ""`, `loading === "lazy"`; `document.querySelector("iframe")` равен null.
    2. После `fireEvent.click(button)`: `iframe` есть, `src` начинается с `https://www.youtube-nocookie.com/embed/qQsgK18gKCU?autoplay=1&rel=0`, `title === "Единый голос-27: Михаил Каминский"`, атрибут `allow` содержит `autoplay` и `encrypted-media`, атрибут `allowfullscreen` присутствует; кнопки больше нет.
    3. `fireEvent.error(img)` до клика: `<img>` исчезает, кнопка с тем же aria-label остаётся, у корня фасада `data-cover="failed"`.
    4. Id с дефисом в начале: `render(<VideoFacade videoId="-Eo--61cx90" title="Единый голос-2027: Иван Вельгоша" />)` → `img.src` содержит `/vi/-Eo--61cx90/`.

    Данные:
    5. `videos.length === 16`; `new Set(videos.map(v => v.id)).size === 16`; каждый `id` совпадает с `/^[A-Za-z0-9_-]{11}$/`; каждый `title.length > 0`; `videos[0]` равен `{ id: "YpLD6p-z00g", title: "Международный день молитвы – Единый голос27" }`.
    6. `materials.length === 5`; каждый `href` начинается с `https://`; `kind` входит в `["document","image","book","phone","folder"]`; ровно один `href` заканчивается на `.docx`; hrefs содержат `esd.onevoice27.org/materials/banners`, `/materials/desire-of-ages`, `/materials/wallpapers` и `sharepoint.com`.
    7. `resourcesCopy.cards.materials.cta === "Открыть материалы"`, `resourcesCopy.cards.music.label === "МУЗЫКА"`, `resourcesCopy.music.emptyTitle === "Песня ещё в работе"`.

    Сетка:
    8. `render(<VideoGrid />)`: `getAllByRole("button", { name: /^Смотреть видео: / })` даёт 16; `getAllByRole("listitem")` даёт 16; текст «Единый голос-27: Дмитрий Зубков» присутствует как подпись.

    Запустить, убедиться в RED, закоммить: `test(04-03): add failing tests for video facade, grid and resources data`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/resources/VideoFacade.test.tsx 2>&1 | grep -E "FAIL|failed|Failed to resolve" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит строки `youtube-nocookie.com/embed/qQsgK18gKCU`, `img.youtube.com/vi/qQsgK18gKCU/hqdefault.jpg`, `fireEvent.error`, `data-cover`, `videos.length`, `materials.length`, `Смотреть видео:`
    - `npx vitest run src/components/resources/VideoFacade.test.tsx` завершается с ненулевым кодом; причина `Failed to resolve import` / `Cannot find module`
    - Коммит `test(04-03):` в `git log -1`
  </acceptance_criteria>
  <done>Тест закоммичен и падает из-за отсутствия реализации.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Данные ресурсов, VideoFacade и VideoGrid (GREEN)</name>
  <files>src/data/videos.ts, src/data/materials.ts, src/data/copy.resources.ts, src/components/resources/VideoFacade.tsx, src/components/resources/VideoGrid.tsx</files>
  <read_first>
    - src/data/copy.ts и src/data/countries.ts (стиль типизированных моков фазы 1–2: `as const`, экспорт типов; сами файлы НЕ редактировать)
    - src/styles/global.css (утилиты `.glass`, `.focus-ring`, токены цветов)
    - .planning/phases/04-news-resources-quote/04-RESEARCH.md (таблица id/названий, ссылки материалов, заметка про hqdefault)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md («VideoFacade», «Copywriting Contract» ключи `resources.*`, «Color» акценты карточек)
    - docs/research/esd-snapshot.md строка с `gcsda.sharepoint.com` (точная SharePoint-ссылка)
  </read_first>
  <behavior>
    - VideoFacade: до клика button + img hqdefault, без iframe; после клика iframe youtube-nocookie с autoplay=1&rel=0 и title; onError постера → data-cover="failed", кнопка остаётся
    - VideoGrid: 16 li, 16 кнопок «Смотреть видео: …», подпись под каждым фасадом
    - videos: 16 уникальных 11-символьных id; materials: 5 https-ссылок с kind из перечисления; resourcesCopy содержит строки копирайт-контракта
  </behavior>
  <action>
    1. `src/data/videos.ts`: `export type VideoItem = { id: string; title: string }`, `export const videos: readonly VideoItem[]` из 16 записей в порядке таблицы RESEARCH: `YpLD6p-z00g` «Международный день молитвы – Единый голос27»; `qQsgK18gKCU` «Единый голос-27: Михаил Каминский»; `dnQS3tFmCNU` «Единый голос-27: Эртон Келер»; `GK_RXxwZxEc` «Единый голос-2027: руководители Адвентистской церкви — о проекте»; `xH3MMwox8cU` «Единый голос-2027: что мы ожидаем от этого проекта?»; `gwqe_QH6KX0` «Единый голос-2027: как различные отделы Адвентистской церкви поддерживают проект?»; `1MN5Gml00QU` «Единый голос-2027: как я могу поддержать проект?»; `-Eo--61cx90` «Единый голос-2027: Иван Вельгоша»; `VjwJfHAqIxQ` «Единый голос-2027: Даниил Ловска»; `uZ4XxENU0C4` «Единый голос-27: Олег Воронюк»; `OeU68sjhbGY` «Единый голос-27: Вячеслав Бучнев»; `S0xNs-LPztg` «Единый голос-27: Андрей Качалаба»; `9BNTd67GNy8` «Единый голос-27: Андрей Молдавану»; `Rvjnk_GGQF4` «Единый голос-27: Александр Жуков»; `gcyRYg-Wc2k` «Единый голос-27: Эдуард Булавчик»; `mpymrj-hOec` «Единый голос-27: Дмитрий Зубков». Id хранить как строки (первый начинается с дефиса).

    2. `src/data/materials.ts`: типы `MaterialKind`, `MaterialItem` из `<interfaces>`; `materials` из 5 записей (D: CONTEXT «Материалы»):
       - `project-description`, «Описание проекта (на русском)», caption «DOCX», `https://hope-documents.fra1.digitaloceanspaces.com/65e8ec8ed9988b1907692c05/mGS1787822852554.docx`, kind `document`
       - `banners`, «Баннеры к 5 сентября 2026», caption «Страница ЕАД», `https://esd.onevoice27.org/materials/banners`, kind `image`
       - `desire-of-ages`, «Вопросы по книге «Желание веков»», caption «Страница ЕАД», `https://esd.onevoice27.org/materials/desire-of-ages`, kind `book`
       - `wallpapers`, «Заставка для телефона», caption «Страница ЕАД», `https://esd.onevoice27.org/materials/wallpapers`, kind `phone`
       - `english`, «Материалы (на английском)», caption «SharePoint, английский», `https://gcsda.sharepoint.com/:f:/s/digitalmediateam/EtNk5tgXbZRMiJ7Nk1nOlRsBhAq-Xx8Oc7vfWiJzI86OJA?e=sSsYtg`, kind `folder`

    3. `src/data/copy.resources.ts` по контракту `<interfaces>`: `eyebrow` «Ресурсы», `title` «Всё, что нужно для старта», `body` «Музыка, видео и материалы, которые помогут рассказать об инициативе в вашей церкви и городе.»; `cards.music` = label «МУЗЫКА», title «Пойте вместе», description «Официальная песня и версии для общинного пения.», cta «Открыть музыку», accent `#8f9dd6`; `cards.materials` = «МАТЕРИАЛЫ», «Будьте готовы», «Скачайте материалы для церкви, малых групп и соцсетей.», «Открыть материалы», accent `#7bc2c7`; `cards.video` = «ВИДЕО», «Смотрите и делитесь», «16 роликов дивизиона: от приветствий руководителей до свидетельств.», «Открыть видео», accent `#d28ebe`; `panel` = `{ close: "Свернуть", closeLabel: "Свернуть панель" }`; `music` = `{ emptyTitle: "Песня ещё в работе", emptyBody: "Официальная песня «Единого голоса 27» скоро появится здесь. Следите за новостями дивизиона." }`; `video.watchLabel = (title) => `Смотреть видео: ${title}``; `resourceKeys = ["music", "materials", "video"] as const`. Акцент музыки `#8f9dd6` соответствует токену `--color-unity-200` из UI-SPEC; сам токен в `tokens.css` фаза 4 не добавляет (файл фазы 1 недоступен при параллельном исполнении), hex живёт в данных, фаза 5 поднимет его в `@theme`.

    4. `src/components/resources/VideoFacade.tsx`: `const [playing, setPlaying] = useState(false); const [coverFailed, setCoverFailed] = useState(false)`. Корень `<div className={cn("relative aspect-video overflow-hidden rounded-xl bg-midnight-950", className)} data-cover={coverFailed ? "failed" : "ok"}>` (без библиотеки `clsx`: простая конкатенация через шаблонную строку). Если `playing`: `<iframe className="absolute inset-0 h-full w-full" src={`https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0`} title={title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share" allowFullScreen loading="lazy" referrerPolicy="strict-origin-when-cross-origin" />`. Иначе `<button type="button" aria-label={resourcesCopy.video.watchLabel(title)} onClick={() => setPlaying(true)} className="group absolute inset-0 block h-full w-full focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-2">`: если не `coverFailed`, `<img src={`https://img.youtube.com/vi/${videoId}/hqdefault.jpg`} alt="" loading="lazy" decoding="async" onError={() => setCoverFailed(true)} className="h-full w-full object-cover" />`, иначе `<span aria-hidden="true" className="absolute inset-0 bg-[linear-gradient(145deg,rgb(48_63_131/.86),rgb(18_12_52/.76))]" />`; поверх слой `<span aria-hidden="true" className="absolute inset-0 bg-[rgb(7_2_16/.28)] transition-colors group-hover:bg-[rgb(7_2_16/.16)]" />` и круг play `<span aria-hidden="true" className="absolute left-1/2 top-1/2 flex h-14 w-14 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full bg-[rgb(248_247_251/.14)] backdrop-blur-[8px] transition-transform motion-safe:group-hover:scale-[1.08]">` с inline SVG треугольником 20px `fill="currentColor"` цветом paper. Постер `hqdefault` (480×360) по RESEARCH: `maxresdefault` есть не у всех роликов вообще, `hqdefault` есть у всех 16.

    5. `src/components/resources/VideoGrid.tsx`: `VideoGrid({ items = videos })` → `<ul className="grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">`, для каждого `<li key={item.id}><VideoFacade videoId={item.id} title={item.title} /><p className="mt-2 line-clamp-2 text-xs font-bold uppercase tracking-[0.08em] text-paper/80">{item.title}</p></li>`.

    Прогнать `npm test` и `npm run build`. Закоммить: `feat(04-03): resources data, video facade and 16-video grid`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/resources/VideoFacade.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/resources/VideoFacade.test.tsx` завершается с кодом 0 (8 проверок)
    - `npm test` и `npm run build` завершаются с кодом 0
    - `grep -c "id: \"" src/data/videos.ts` = 16; `grep -c "href: \"https://" src/data/materials.ts` = 5; `grep -c "\.docx" src/data/materials.ts` = 1
    - `grep -n "youtube-nocookie.com/embed/" src/components/resources/VideoFacade.tsx` и `grep -n "img.youtube.com/vi/" src/components/resources/VideoFacade.tsx` находят строки; `grep -n "hqdefault" src/components/resources/VideoFacade.tsx` находит строку
    - `grep -n "from \"../about\|components/about" src/components/resources/*.tsx` ничего не находит
    - `grep -n "dangerouslySetInnerHTML" src/components/resources/*.tsx` ничего не находит
    - `grep -n "\"Открыть материалы\"\|\"Песня ещё в работе\"" src/data/copy.resources.ts` находит обе строки
    - `git diff --name-only HEAD~2 -- src | grep -v -E "^src/(data/(videos|materials|copy\.resources)\.ts|components/resources/(VideoFacade|VideoGrid)\.tsx|components/resources/VideoFacade\.test\.tsx)$"` пуст; `package.json` не изменён
  </acceptance_criteria>
  <done>16 роликов рендерятся фасадами с обложками, клик грузит плеер youtube-nocookie, данные материалов и копирайт готовы для плана 04-04, тесты и билд зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер ← img.youtube.com | Сторонние постеры до клика |
| Браузер ← www.youtube-nocookie.com (iframe) | Сторонний плеер с JS, монтируется только после клика |
| Статические данные `videos.ts` / `materials.ts` → React | Id и URL из типизированных модулей |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | `<iframe youtube-nocookie>` | mitigate | Домен `youtube-nocookie.com`, iframe монтируется только по клику (без него YouTube не получает ни запроса), `referrerPolicy="strict-origin-when-cross-origin"` |
| T-04-09 | Elevation of Privilege | `allow` атрибут iframe | mitigate | Явный список `accelerometer; autoplay; clipboard-write; encrypted-media; picture-in-picture; web-share`, без `camera`, `microphone`, `geolocation` |
| T-04-10 | Tampering (injection в URL) | `videoId` в `src` постера и iframe | mitigate | Id только из статического `videos.ts`, формат проверяется тестом `/^[A-Za-z0-9_-]{11}$/`; интерполяция через JSX-атрибут |
| T-04-11 | Denial of Service | Недоступность img.youtube.com | mitigate | `onError` → градиентная плашка, кнопка play и `aria-label` остаются, iframe грузится по клику независимо от постера |
| T-04-12 | Repudiation | Внешние ссылки материалов (SharePoint, DigitalOcean Spaces) | accept | Ссылки статические, проверены исследователем (200 для DOCX; страницы ЕАД открываются в браузере); рендер ссылок в плане 04-04 с `rel="noopener noreferrer"` |
| T-04-SC | Tampering | npm installs | accept | Новых пакетов нет |
</threat_model>

<verification>
- `npm test` и `npm run build` зелёные.
- Ручной smoke после интеграции в план 04-04: 16 постеров грузятся с img.youtube.com, до клика в DevTools Network нет запросов к youtube-nocookie.com, после клика ролик стартует.
</verification>

<success_criteria>
- RES-03 (часть): 16 роликов ЕАД с YouTube ID и названиями из PROJECT.md как фасады с обложкой и lite-embed по клику.
- RES-02 (данные): 5 материалов с реальными ссылками ЕАД, типом иконки и подписью формата.
- `VideoFacade` имеет API `{ videoId, title, className? }`, идентичный `VideoEmbed` фазы 3, без импортов из `components/about`.
- Ни один файл вне `files_modified` не изменён; новых зависимостей нет.
</success_criteria>

<output>
Create `.planning/phases/04-news-resources-quote/04-03-SUMMARY.md` when done
</output>
