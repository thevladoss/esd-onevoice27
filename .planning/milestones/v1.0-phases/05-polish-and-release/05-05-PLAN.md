---
phase: 05-polish-and-release
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - src/App.test.tsx
  - src/components/layout/Header.test.tsx
  - src/components/map/CountryChips.test.tsx
  - src/components/form/LightForm.test.tsx
  - src/components/resources/Resources.test.tsx
  - src/test/setup.ts
autonomous: true
requirements: [QA-02]

must_haves:
  truths:
    - "App.test.tsx подтверждает: восемь секций с ожидаемыми id и aria-labelledby, один h1, skip-link первым в DOM с текстом «Перейти к содержимому» и href #main, main#main, nav «Основная навигация», footer, ни одной ссылки target=_blank без noopener"
    - "Header.test.tsx подтверждает: четыре якоря (#about, #involve, #news, #resources) с текстами меню, клик вызывает window.scrollTo с behavior smooth или auto, бургер открывает диалог «Меню» с aria-expanded=true и блокировкой скролла body, Esc закрывает и возвращает фокус на бургер"
    - "CountryChips, LightForm и Resources покрыты component-тестами: aria-pressed при выборе страны, 6 ошибок при пустой отправке и рост счётчика при валидной, панели ресурсов с aria-expanded, Esc и фокусом на панели"
    - "npm test зелёный без предупреждений act и без Warning в выводе"
  artifacts:
    - path: "src/App.test.tsx"
      provides: "Интеграционный тест оболочки и восьми секций"
      contains: "light-form"
    - path: "src/components/layout/Header.test.tsx"
      provides: "Тест навигации и бургера"
      contains: "Основная навигация"
  key_links:
    - from: "src/App.test.tsx"
      to: "src/App.tsx"
      via: "render(<App />) в провайдерах из main.tsx"
      pattern: "from \"./App\""
    - from: "src/components/layout/Header.test.tsx"
      to: "window.scrollTo (мок из setup.ts)"
      via: "expect(window.scrollTo).toHaveBeenCalledWith(expect.objectContaining({ behavior: ... }))"
      pattern: "scrollTo"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Этот план не меняет поведение страницы: он фиксирует контракт оболочки и секций тестами, чтобы деплой 05-07 шёл по зелёному CI.

<objective>
Дописать недостающие component-тесты фазы 5: `App.test.tsx` (восемь секций, skip-link, ландмарки), `Header.test.tsx` (навигация, бургер, Esc, фокус), добрать пробелы в `CountryChips.test.tsx`, `LightForm.test.tsx`, `Resources.test.tsx`; убрать предупреждения `act`.

Purpose: закрыть QA-02. Component-тесты покрывают форму, чипы стран, панели ресурсов и навигацию header.
Output: два новых тестовых файла, дополненные три существующих, `npm test` зелёный без предупреждений.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/research/PITFALLS.md

Тесты пишутся против контракта, который уже реализовали планы 05-02, 05-03, 05-04 (a11y-атрибуты, порядок DOM, тексты). Файлы компонентов в этой волне не править: если тест вскрывает несоответствие контракту, задокументировать в SUMMARY как расхождение для verifier, а тест оставить красным только в случае, когда несоответствие подтверждено кодом (не ошибкой теста). План 05-06 работает параллельно и трогает `vite.config.ts`, `scripts/`, `README.md`, `docs/qa/` и вычищает `console.*` в `src/**` вне тестов: пересечений с этим планом нет.

<interfaces>
<!-- Контракт разметки, против которого пишутся тесты. Источник: 05-UI-SPEC.md «Accessibility Contract», 01-UI-SPEC.md Header/MobileMenu/SkipLink. -->

Skip-link: первый <a> документа, текст «Перейти к содержимому», href="#main".
main: <main id="main">, содержит восемь <section id="hero|map|light-form|about|involve|news|resources|quote" aria-labelledby="hero-title|map-title|form-title|about-title|involve-title|news-title|resources-title|quote-title">.
Header: <nav aria-label="Основная навигация">; ссылки «Что это?»→#about, «Участвовать»→#involve, «Новости»→#news, «Материалы»→#resources.
Бургер: <button aria-expanded aria-controls="mobile-menu" aria-label="Открыть меню" | "Закрыть меню">; оверлей <div id="mobile-menu" role="dialog" aria-modal="true" aria-label="Меню">.
Чипы: <div role="group" aria-label="Показать страну на карте"> с <button aria-pressed>.
Форма: ошибки «Введите имя», «Введите фамилию», «Выберите страну», «Укажите город», «Введите корректный email», «Нужно согласие на обработку данных»; тост role="status" с «Ваш свет зажжён».
Ресурсы: карточки <button aria-expanded aria-controls>; панель tabIndex=-1; кнопка aria-label="Свернуть панель"; фасады <button aria-label="Смотреть видео: …">.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: App.test.tsx: восемь секций, skip-link, ландмарки</name>
  <files>src/App.test.tsx, src/test/setup.ts</files>
  <read_first>
    - src/main.tsx (какими провайдерами обёрнут `App`: `LightsProvider`, `StrictMode`)
    - src/App.tsx (порядок `SkipLink`, `Header`, `main`, секции, `Footer`)
    - src/test/setup.ts (моки `IntersectionObserver`, `ResizeObserver`, `matchMedia` с `addEventListener`, `getContext`, `scrollTo`)
    - Существующие `*.test.tsx` в src/components (стиль: `render` из Testing Library, `screen`, `user-event`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Accessibility Contract»
  </read_first>
  <behavior>
    - Рендер `<App />` внутри тех же провайдеров, что в `main.tsx`, не бросает исключений и не пишет `console.error` (шпион на `console.error` не вызван)
    - Для каждого id из `hero, map, light-form, about, involve, news, resources, quote` существует элемент `section#${id}`; его `aria-labelledby` указывает на существующий элемент с непустым текстом
    - Набор `aria-labelledby` восьми секций равен `hero-title, map-title, form-title, about-title, involve-title, news-title, resources-title, quote-title`
    - `document.querySelectorAll("h1").length === 1`
    - Первый `<a>` документа имеет текст «Перейти к содержимому» и `href`, оканчивающийся на `#main`; `document.querySelector("main#main")` не null; skip-link расположен в DOM раньше `header` (`compareDocumentPosition`)
    - Существуют `nav[aria-label="Основная навигация"]`, `footer`, `nav[aria-label="Внешние ссылки"]`
    - `document.querySelectorAll('a[target="_blank"]:not([rel~="noopener"])').length === 0`
    - Все значения `[data-anim]` в DOM входят в множество `stars, globe, beam, pulse, new-light, particles, atmosphere, wave, halo`
  </behavior>
  <action>
    Создать `src/App.test.tsx` по списку поведений. Рендерить `App` так же, как `main.tsx` (если провайдер уже внутри `App`, рендерить `<App />` напрямую). Проверить, что `src/test/setup.ts` даёт `window.matchMedia` объект с `addEventListener`/`removeEventListener` (motion подписывается на `change`), `HTMLCanvasElement.prototype.getContext` возвращает `null` без исключений, `ResizeObserver` и `IntersectionObserver` объявлены; недостающее добавить в `setup.ts`. Шпион `vi.spyOn(console, "error")` ставить в `beforeEach`, снимать в `afterEach`, проверять `not.toHaveBeenCalled()`; предупреждения React (`act`, ключи, невалидный DOM nesting) тем самым тоже ловятся.

    Если карта в jsdom получает контейнер 0×0 и рендерит блок ошибки `role="status"`, тест это допускает: проверяется наличие секции, а не карты.
  </action>
  <verify>
    <automated>npx vitest run src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/App.test.tsx` завершается 0
    - `grep -c "light-form" src/App.test.tsx` не меньше 1; `grep -c "Перейти к содержимому" src/App.test.tsx` не меньше 1; `grep -c 'main#main' src/App.test.tsx` не меньше 1; `grep -c "Основная навигация" src/App.test.tsx` не меньше 1; `grep -c "form-title" src/App.test.tsx` не меньше 1
    - `grep -c 'querySelectorAll("h1")' src/App.test.tsx` не меньше 1
    - `grep -cE "noopener" src/App.test.tsx` не меньше 1
    - `npx vitest run src/App.test.tsx` завершается 0 и показывает не меньше 6 passed
    - `npx vitest run src/App.test.tsx 2>&1 | grep -ci "not wrapped in act"` выводит 0
  </acceptance_criteria>
  <done>Интеграционный тест оболочки зелёный и без предупреждений.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Header.test.tsx: якоря, прокрутка, бургер, Esc, фокус</name>
  <files>src/components/layout/Header.test.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx (разметка nav, бургера, оверлея; как вычисляется `top` и `behavior`; куда уходит фокус при открытии; что делает Esc и клик по фону; как восстанавливается `document.body.style.overflow`)
    - src/lib/scrollToSection.ts, если существует
    - src/test/setup.ts (мок `window.scrollTo` как `vi.fn()`, мок `matchMedia`)
    - Существующие тесты Header фазы 1 (не дублировать проверки, дополнять)
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md, разделы `Header`, `BurgerButton`, `MobileMenu`
  </read_first>
  <behavior>
    - Рендер `<Header />` вместе с четырьмя заглушками `<section id="about|involve|news|resources">` в одном контейнере; `getByRole("link", { name: "Что это?" })` имеет `href` `#about`, «Участвовать» → `#involve`, «Новости» → `#news`, «Материалы» → `#resources`; всего в `nav[aria-label="Основная навигация"]` четыре ссылки меню
    - Клик по «Что это?» вызывает `window.scrollTo` один раз с объектом, где `top` число и `behavior` соответствует `/^(smooth|auto)$/`; `location.hash` не меняется на несуществующий якорь
    - Бургер `getByRole("button", { name: "Открыть меню" })` имеет `aria-expanded="false"` и `aria-controls="mobile-menu"`; после клика `aria-expanded="true"`, имя кнопки «Закрыть меню», в DOM есть `role="dialog"` с именем «Меню» и `aria-modal="true"`, `document.body.style.overflow === "hidden"`
    - Нажатие `Escape` закрывает: `aria-expanded="false"`, `document.activeElement` равен бургеру, `document.body.style.overflow` равен исходному значению (пустая строка)
    - Клик по пункту меню внутри открытого оверлея закрывает оверлей и вызывает `window.scrollTo`
  </behavior>
  <action>
    Создать `src/components/layout/Header.test.tsx` на `@testing-library/user-event` (`userEvent.setup()`), все взаимодействия через `await user.click`/`await user.keyboard("{Escape}")`. `window.scrollTo` брать из мока `setup.ts` и сбрасывать `mockClear()` в `beforeEach`. Проверку прокрутки делать при `matchMedia` с `matches: false` (ожидаем `smooth`) и отдельным кейсом при `matches: true` для запроса `prefers-reduced-motion` (ожидаем `auto`); если `useReducedMotion`/`matchMedia` кэшируется на уровне модуля, второй кейс вынести в отдельный файл `Header.reduce.test.tsx` по тому же приёму, что в 05-01, и добавить его в `files_modified` SUMMARY. Если оверлей остаётся в DOM в закрытом состоянии с `visibility: hidden` через CSS (в тестах `css: false`), проверять состояние по `aria-expanded` и по `aria-hidden`/`hidden` оверлея, а не по `toBeVisible()`.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Header</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/layout/Header.test.tsx` завершается 0
    - `grep -c '"#about"' src/components/layout/Header.test.tsx` не меньше 1; `grep -c '"#involve"' src/components/layout/Header.test.tsx` не меньше 1; `grep -c '"#news"' src/components/layout/Header.test.tsx` не меньше 1; `grep -c '"#resources"' src/components/layout/Header.test.tsx` не меньше 1
    - `grep -c "Открыть меню" src/components/layout/Header.test.tsx` не меньше 1; `grep -c "Escape" src/components/layout/Header.test.tsx` не меньше 1; `grep -c "toHaveFocus" src/components/layout/Header.test.tsx` не меньше 1; `grep -c "scrollTo" src/components/layout/Header.test.tsx` не меньше 1; `grep -c 'aria-expanded' src/components/layout/Header.test.tsx` не меньше 1
    - `npx vitest run src/components/layout/Header` завершается 0 и показывает не меньше 5 passed
    - `npx vitest run src/components/layout/Header 2>&1 | grep -ci "not wrapped in act"` выводит 0
  </acceptance_criteria>
  <done>Навигация и бургер покрыты тестами, зелёные, без предупреждений.</done>
</task>

<task type="auto">
  <name>Task 3: Пробелы в CountryChips, LightForm, Resources и нулевой шум act</name>
  <files>src/components/map/CountryChips.test.tsx, src/components/form/LightForm.test.tsx, src/components/resources/Resources.test.tsx, src/test/setup.ts</files>
  <read_first>
    - src/components/map/CountryChips.test.tsx, src/components/form/LightForm.test.tsx, src/components/resources/Resources.test.tsx (что уже проверено фазами 2–4)
    - src/components/map/CountryChips.tsx, src/components/form/LightForm.tsx, src/components/form/SuccessToast.tsx, src/components/resources/Resources.tsx, src/components/resources/ResourcePanel.tsx (контракт после 05-03/05-04: `aria-pressed`, фокус на первое невалидное поле, таймер тоста, `tabIndex={-1}` панели, Esc)
    - src/state/lights.tsx (`LightsProvider`, `useLights`, `counts`)
    - .planning/phases/05-polish-and-release/05-CONTEXT.md, раздел «Тесты (QA-02)»
  </read_first>
  <action>
    Дополнить существующие файлы только недостающими кейсами (при отсутствии файла создать):

    `CountryChips.test.tsx`: по умолчанию `aria-pressed="true"` у «Весь дивизион»; клик по «Казахстан» переносит `aria-pressed="true"` на него и вызывает `onSelect` (или эквивалентный колбэк) с id 398; Enter на сфокусированном чипе срабатывает как клик (нативная кнопка); контейнер `role="group"` с именем «Показать страну на карте»; всего 13 кнопок.

    `LightForm.test.tsx`: пустая отправка показывает шесть текстов ошибок из контракта и переводит фокус на первое невалидное поле (поле «Имя»), у него `aria-invalid="true"` и `aria-describedby` указывает на элемент с текстом «Введите имя»; валидная отправка внутри `LightsProvider` с рендером `Counters` рядом увеличивает счётчик «Человек» на 1 (или «Групп» при типе «Групповой маяк») и показывает `role="status"` с текстом, начинающимся с «Ваш свет зажжён»; таймер тоста прогонять через `vi.useFakeTimers()` и `act(() => vi.advanceTimersByTime(4000))`, чтобы не ловить предупреждений.

    `Resources.test.tsx`: клик по «Материалы» открывает панель с пятью ссылками (`getAllByRole("link")` внутри панели, у каждой `rel` содержит `noopener`), карточка `aria-expanded="true"`, панель получает фокус (`toHaveFocus()` на элементе с `tabIndex=-1`); повторный клик закрывает (`aria-expanded="false"`); клик по «Видео» показывает 16 кнопок с именем, начинающимся на «Смотреть видео:»; `Escape` внутри открытой панели закрывает её и возвращает фокус на активную карточку; кнопка «Свернуть панель» закрывает панель.

    Шум: прогнать `npm test 2>&1 | tee /tmp/vitest.log` и добиться нуля строк с `not wrapped in act` и `Warning:`. Типичные исправления: `await` у `user.*`, `findBy*` вместо `getBy*` после асинхронных обновлений, фейковые таймеры для тостов и count-up, `matchMedia` в `setup.ts` с `addEventListener`. Компоненты не править (владелец в этой волне: 05-06 для `console.*`, иначе никто); несоответствие контракту записать в SUMMARY как расхождение.
  </action>
  <verify>
    <automated>npm test 2>&1 | tee /tmp/vitest-05-05.log >/dev/null; test "${PIPESTATUS[0]}" = "0" && test "$(grep -ci "not wrapped in act" /tmp/vitest-05-05.log)" = "0" && test "$(grep -c "Warning:" /tmp/vitest-05-05.log)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/map/CountryChips.test.tsx && test -f src/components/form/LightForm.test.tsx && test -f src/components/resources/Resources.test.tsx` завершается 0
    - `grep -c "aria-pressed" src/components/map/CountryChips.test.tsx` не меньше 1; `grep -c "398" src/components/map/CountryChips.test.tsx` не меньше 1
    - `grep -c "Введите имя" src/components/form/LightForm.test.tsx` не меньше 1; `grep -c "Ваш свет зажжён" src/components/form/LightForm.test.tsx` не меньше 1; `grep -c "aria-invalid" src/components/form/LightForm.test.tsx` не меньше 1
    - `grep -c "Смотреть видео" src/components/resources/Resources.test.tsx` не меньше 1; `grep -c "Escape" src/components/resources/Resources.test.tsx` не меньше 1; `grep -c "toHaveFocus" src/components/resources/Resources.test.tsx` не меньше 1; `grep -c "Свернуть панель" src/components/resources/Resources.test.tsx` не меньше 1
    - `npm test` завершается 0
    - `npm test 2>&1 | grep -ci "not wrapped in act"` выводит 0; `npm test 2>&1 | grep -c "Warning:"` выводит 0
  </acceptance_criteria>
  <done>Четыре области QA-02 (форма, чипы, панели, навигация) покрыты, `npm test` зелёный и тихий.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Тесты → компоненты | Тесты выполняются в jsdom локально и в CI, сети нет; внешние домены (YouTube) в тестах не запрашиваются, `img` в jsdom не грузится |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | Внешние ссылки без `noopener` | mitigate | `App.test.tsx` и `Resources.test.tsx` проверяют `rel` на каждой `target="_blank"`; регрессия ломает CI |
| T-05-12 | Information Disclosure | `console.error` в рендере | mitigate | Шпион в `App.test.tsx` требует нулевых вызовов, предупреждения React не доходят до продакшна незамеченными |
| T-05-SC | Tampering | npm installs | mitigate | Установок в плане нет (`@testing-library/user-event` установлен фазой 1); `git diff -- package.json` без новых зависимостей |
</threat_model>

<verification>
- `npx vitest run src/App.test.tsx` и `src/components/layout/Header*` зелёные
- `npm test` завершается 0, в выводе нет `not wrapped in act` и `Warning:`
- Тесты на чипы, форму и панели содержат кейсы клавиатуры и aria
</verification>

<success_criteria>
- QA-02 закрыт: component-тесты покрывают форму (огонёк и счётчик), чипы стран, панели ресурсов и навигацию header
- `App.test.tsx` фиксирует восемь секций, skip-link и ландмарки
- CI-прогон `npm test` зелёный и без предупреждений
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-05-SUMMARY.md` when done
</output>
