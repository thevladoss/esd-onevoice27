---
phase: 05-polish-and-release
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/layout/Section.tsx
  - src/components/hero/Hero.tsx
  - src/components/map/MapSection.tsx
  - src/components/map/EsdMap.tsx
  - src/components/map/Counters.tsx
  - src/components/map/CountryChips.tsx
  - src/components/map/ZoomHint.tsx
  - src/components/form/LightForm.tsx
  - src/components/form/LightTypeChoice.tsx
  - src/components/form/FormField.tsx
  - src/components/form/ConsentCheckbox.tsx
  - src/components/form/SuccessToast.tsx
  - src/components/about/About.tsx
  - src/components/about/StepCard.tsx
  - src/components/involve/Involve.tsx
  - src/components/involve/InvolveCard.tsx
  - src/data/copy.ts
  - src/components/layout/GradientTitle.tsx
autonomous: true
requirements: [MOTION-03]

must_haves:
  truths:
    - "Секции hero, map, light-form, about, involve несут aria-labelledby на id своего заголовка: hero-title, map-title, form-title, about-title, involve-title; на странице один h1"
    - "Карта доступна как role=img с title; счётчики читаются реальным текстом с aria-live=polite; чипы стран несут aria-pressed и работают с клавиатуры; поля формы связаны с label, ошибки через aria-invalid и aria-describedby, фокус уходит на первое невалидное поле; тост role=status"
    - "Видео-фасад и декоративные SVG триптиха не мешают скринридеру: кнопка с aria-label, иллюстрации aria-hidden"
    - "На 390px счётчики стоят строкой над картой, чипы переносятся, форма в одну колонку, StepCard и триптих стеком, горизонтального скролла нет: секции с декоративными слоями держат overflow hidden, медиа не шире контейнера, flex- и grid-дети с min-width 0"
    - "Текст на стекле не бледнее .72 (кроме плейсхолдера .56), у каждого поля видимый label"
  artifacts:
    - path: "src/components/layout/Section.tsx"
      provides: "Проп titleId и aria-labelledby на section"
      contains: "aria-labelledby"
    - path: "src/components/map/EsdMap.tsx"
      provides: "role=img, title, aria-describedby на SVG"
      contains: "role=\"img\""
    - path: "src/components/form/FormField.tsx"
      provides: "label for, aria-invalid, aria-describedby"
      contains: "aria-describedby"
  key_links:
    - from: "src/components/{hero,map,form,about,involve}/*.tsx"
      to: "id заголовка секции"
      via: "aria-labelledby на <section>"
      pattern: "aria-labelledby=\"(hero|map|form|about|involve)-title\""
    - from: "src/components/map/CountryChips.tsx"
      to: "активная страна"
      via: "aria-pressed"
      pattern: "aria-pressed"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Срезы: после задачи 1 скринридер называет каждую секцию по заголовку, после задачи 2 карта и форма проходимы без мыши и не ломаются на 390px, после задачи 3 то же верно для «О проекте» и «Участия».

<objective>
Аудит и доводка доступности и адаптива секций hero, map, light-form, about, involve по «Accessibility Contract» и «Responsive Checklist» 05-UI-SPEC.md: `aria-labelledby`, иерархия заголовков, aria по элементам карты и формы, стековые раскладки на 390px, защита от горизонтального скролла в секциях, порог непрозрачности текста.

Purpose: закрыть MOTION-03 для первых пяти секций. Остальные три секции закрывает 05-04, оболочку закрыл 05-02.
Output: правки атрибутов и классов в файлах перечисленных секций, `Section.tsx` с пропом `titleId`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/03-form-about-involve/03-CONTEXT.md
@.planning/research/PITFALLS.md

Файлы `src/components/{news,resources,quote}/**` и `src/components/about/VideoEmbed.tsx` в этой волне принадлежат плану 05-04: здесь их не трогать. `global.css` принадлежит 05-02 (уже выполнен): правки адаптива делать классами и стилями в файлах секций, а не в глобальном CSS. `src/data/copy.ts` принадлежит этому плану: новые aria-строки (описание карты, подписи) кладутся в него по паттерну фаз 1–4. Обёртки `Reveal` из 05-01 уже стоят: не удалять и не переставлять, `aria-labelledby` ставить на `<section>`, а `id` на сам `<h2>` внутри обёртки.
</context>

<tasks>

<task type="auto">
  <name>Task 1: aria-labelledby и иерархия заголовков для hero, map, light-form, about, involve</name>
  <files>src/components/layout/Section.tsx, src/components/hero/Hero.tsx, src/components/map/MapSection.tsx, src/components/form/LightForm.tsx, src/components/about/About.tsx, src/components/involve/Involve.tsx</files>
  <read_first>
    - src/components/layout/Section.tsx (пропы `id`, `eyebrow`, `title`; как рендерится `GradientTitle as="h2"`; проп `reveal` из 05-01)
    - src/components/layout/GradientTitle.tsx (пробрасывает ли `id` и остальные атрибуты на заголовок)
    - src/components/hero/Hero.tsx, src/components/map/MapSection.tsx, src/components/form/LightForm.tsx, src/components/about/About.tsx, src/components/involve/Involve.tsx (где `<section id>`, где заголовок, какого уровня)
    - src/components/about/StepCard.tsx, src/components/involve/InvolveCard.tsx (уровень заголовков карточек)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Accessibility Contract» → «Ландмарки и заголовки»
  </read_first>
  <action>
    Расширить `Section.tsx` пропом `titleId?: string`: при наличии `title` заголовок получает `id={titleId}`, а `<section>` получает `aria-labelledby={titleId}`; `GradientTitle` должен пробрасывать `id` на элемент заголовка (если не пробрасывает, добавить проброс остальных атрибутов). Секции, рендерящие заголовок сами, получают `id` на `<h2>` и `aria-labelledby` на `<section>` напрямую.

    Идентификаторы строго по таблице: `#hero` → `hero-title` на единственном `<h1>` страницы; `#map` → `map-title`; `#light-form` → `form-title` (не `light-form-title`); `#about` → `about-title`; `#involve` → `involve-title`. Заголовки карточек (`StepCard`, `InvolveCard`) уровня `<h3>`; пропусков уровней нет; `<h1>` ровно один. Если `Hero` рендерит H1 через `GradientTitle as="h1"`, `id` передаётся туда же.
  </action>
  <verify>
    <automated>npm test && test "$(grep -rhoE 'aria-labelledby="(hero|map|form|about|involve)-title"' src/components | sort -u | wc -l | tr -d ' ')" = "5"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "titleId" src/components/layout/Section.tsx` не меньше 2; `grep -c "aria-labelledby" src/components/layout/Section.tsx` не меньше 1
    - `grep -rhoE 'aria-labelledby="(hero|map|form|about|involve)-title"' src/components | sort -u | wc -l` выводит 5 (передача через проп `titleId="map-title"` засчитывается, если `grep -rhoE 'titleId="(hero|map|form|about|involve)-title"' src/components | sort -u` дополняет набор до пяти)
    - `grep -rhoE 'id="(hero|map|form|about|involve)-title"' src/components | sort -u | wc -l` плюс число уникальных `titleId="..."` из пяти имён даёт 5
    - `grep -rhoE 'id="light-form-title"' src | wc -l` выводит 0
    - `grep -rcE '<h1\b|as="h1"' src/components --include=*.tsx | awk -F: '{s+=$2} END {print s}'` выводит 1
    - `grep -rc "<h3" src/components/about/StepCard.tsx src/components/involve/InvolveCard.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 2
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Пять секций именованы по заголовкам, один h1, карточки на h3, тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Карта и форма: aria по элементам, клавиатура, раскладка 390px</name>
  <files>src/components/map/MapSection.tsx, src/components/map/EsdMap.tsx, src/components/map/Counters.tsx, src/components/map/CountryChips.tsx, src/components/map/ZoomHint.tsx, src/components/form/LightForm.tsx, src/components/form/LightTypeChoice.tsx, src/components/form/FormField.tsx, src/components/form/ConsentCheckbox.tsx, src/components/form/SuccessToast.tsx, src/data/copy.ts</files>
  <read_first>
    - src/components/map/*.tsx (SVG-карта: `role`, `<title>`, `aria-describedby`; счётчики: `aria-live`, анимируемый span, скрытое итоговое значение; чипы: `role="group"`, `aria-label`, `aria-pressed`, обработка клавиатуры; `touch-action`; позиционирование счётчиков по брейкпоинтам; подсказка)
    - src/components/form/*.tsx (`<label htmlFor>`, `aria-invalid`, `aria-describedby`, фокус на первое невалидное поле после submit, `role="status"` тоста, `aria-busy` кнопки, сетка полей и радио-карточек по брейкпоинтам)
    - src/data/copy.ts (ключи `map.*`, `form.*`, есть ли `map.srDescription`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «aria по элементам» (строки Карта, Счётчики, Чипы стран, Поля формы, Тост), «Responsive Checklist» → «390 × 844» (Карта, Форма), «Защита от горизонтального скролла», «Color» → «Контраст»
  </read_first>
  <action>
    Карта: у `<svg>` `role="img"`, вложенный `<title>` «Карта Евро-Азиатского дивизиона», `aria-describedby` на визуально скрытый абзац с текстом из `copy.map.srDescription` (при отсутствии ключа добавить: «Карта двенадцати стран Евро-Азиатского дивизиона с огоньками участников: {people} человек и {groups} групп»). Счётчики: число реальным текстом, контейнер `aria-live="polite"`, анимируемый span `aria-hidden="true"`, рядом визуально скрытое итоговое значение. Чипы: контейнер `role="group" aria-label="Показать страну на карте"`, каждая `<button type="button" aria-pressed={active}>`, Enter и Space срабатывают нативно (не заменять `button` на `div`); «Весь дивизион» сбрасывает зум. Раскладка: ниже 1024px счётчики рендерятся строкой над картой (не абсолютом поверх), высота контейнера карты на 390px не меньше 520px, чипы `flex-wrap` с зазором 8px и `min-width: 0`, `ZoomHint` виден и не наезжает на карту, контейнер карты держит `touch-action: pan-y`, секция-хост `#map` держит `overflow: hidden` (или `clip`) вокруг скошенных слоёв.

    Форма: у каждого поля видимый `<label htmlFor>` (плейсхолдер `paper/.56` смыслa не несёт); при ошибке `aria-invalid="true"` и `aria-describedby` на элемент текста ошибки с уникальным `id`; после неудачного submit фокус на первое невалидное поле в порядке DOM; нативные `<input type="radio">` радио-карточек доступны с клавиатуры (визуально скрыты, не `display: none`); чекбокс согласия связан с текстом через `label`; тост `role="status"` с текстом «Ваш свет зажжён» (допускается продолжение «! Огонёк уже на карте.»), без перехвата фокуса; кнопка отправки с `aria-busy` в состоянии «Зажигаем…». Раскладка ниже 768px: одна колонка, радио-карточки стеком, поля высотой 54px, у полей и карточек `min-width: 0`, текст ошибки не сдвигает соседние поля (резерв высоты или абсолют внутри поля).

    Контраст по таблице «Color → Контраст»: `paper/.8` на `GlassCard` (~8.9:1) и `#fca5a5` на `rgb(18 12 52 / .58)` (~9.8:1) проходят расчётом; убрать любые непрозрачности текста ниже .72 в файлах карты и формы, кроме плейсхолдера. Расчёты записать в SUMMARY.
  </action>
  <verify>
    <automated>npm test && test "$(grep -rnE "text-paper/([0-6][0-9]?|7[01])\b|248 247 251 / \.?(0?[0-6][0-9]?|7[01])\)" src/components/map src/components/form | grep -v placeholder | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'role="img"' src/components/map | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc "<title>" src/components/map | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc "aria-describedby" src/components/map | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -c 'aria-live="polite"' src/components/map/Counters.tsx` не меньше 1; `grep -c 'aria-hidden' src/components/map/Counters.tsx` не меньше 1
    - `grep -c "aria-pressed" src/components/map/CountryChips.tsx` не меньше 1; `grep -c 'role="group"' src/components/map/CountryChips.tsx` не меньше 1; `grep -c "Показать страну на карте" src/components/map/CountryChips.tsx src/data/copy.ts | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "touch-action: ?pan-y|touch-pan-y" src/components/map | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "overflow-hidden|overflow: ?hidden|overflow-clip|overflow: ?clip" src/components/map/MapSection.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rc "htmlFor" src/components/form | awk -F: '{s+=$2} END {print s}'` не меньше 5; `grep -rc "aria-invalid" src/components/form | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc "aria-describedby" src/components/form | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -c 'role="status"' src/components/form/SuccessToast.tsx` выводит не меньше 1; `grep -c "Ваш свет зажжён" src/components/form/SuccessToast.tsx src/data/copy.ts | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rc "aria-busy" src/components/form | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rnE "text-paper/([0-6][0-9]?|7[01])\b|248 247 251 / \.?(0?[0-6][0-9]?|7[01])\)" src/components/map src/components/form | grep -v placeholder | wc -l` выводит 0
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Карта и форма проходимы с клавиатуры и читаемы скринридером, раскладка 390px стековая, текст не бледнее .72.</done>
</task>

<task type="auto">
  <name>Task 3: О проекте и Участие: aria, медиа не шире контейнера, стек на мобильном</name>
  <files>src/components/about/About.tsx, src/components/about/StepCard.tsx, src/components/involve/Involve.tsx, src/components/involve/InvolveCard.tsx</files>
  <read_first>
    - src/components/about/About.tsx, src/components/about/StepCard.tsx (сетка `StepCard` по брейкпоинтам, разделители, непрозрачности текста; `VideoEmbed` только импортируется, его файл принадлежит 05-04)
    - src/components/involve/Involve.tsx, src/components/involve/InvolveCard.tsx, src/components/involve/art/*.tsx (SVG-иллюстрации: `aria-hidden`, `preserveAspectRatio`, ширина; ссылки-действия и их фокус; хвост триптиха на мобильном; `@media (hover: hover)`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «aria по элементам» (Декоративная графика, Внешние ссылки), «Responsive Checklist» → «390 × 844» (О проекте, Участие), «768 × 1024», «1024 × 900», «Защита от горизонтального скролла», «Фокус»
    - .planning/phases/03-form-about-involve/03-CONTEXT.md, разделы «О проекте» и «Участие»
  </read_first>
  <action>
    About: `StepCard` стеком ниже 1024px и в три колонки от 1024px; у карточек `min-width: 0`; заголовки карточек `<h3>`; итоговая фраза `paper/.8`, разделитель `paper/.12` (фон, не текст); контейнер видео-фасада `max-width: 100%` и `aspect-ratio: 16 / 9`. Внутренности `VideoEmbed.tsx` не трогать.

    Involve: SVG-иллюстрации в `art/*.tsx` с `aria-hidden="true"`, `max-width: 100%`, медиа-блок `aspect-ratio: 4 / 3` с `overflow: hidden`; триптих стеком ниже 768px, в ряд от 1024px; ссылки-действия обычные `<a href="#about|#resources|#news">` с видимым текстом, кольцо фокуса через `focus-ring` (если карточка обрезает содержимое `overflow: hidden`, ссылке ставится `outline-offset: -3px` через класс `focus-visible:-outline-offset-3` или локальный стиль); hover-зум медиа только внутри `@media (hover: hover)`; секция-хост `#involve` держит `overflow: hidden` вокруг декоративных слоёв; `min-width: 0` у карточек в grid/flex.

    Непрозрачность текста в обоих разделах не ниже .72 (тот же grep-гейт, что в задаче 2). Тесты фазы 3 на About/Involve остаются зелёными.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'aria-hidden' src/components/involve | awk -F: '{s+=$2} END {print s}'` не меньше 3 (три иллюстрации)
    - `grep -rcE "aspect-\[16/9\]|aspect-video|aspect-ratio: ?16 ?/ ?9" src/components/about | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "aspect-\[4/3\]|aspect-ratio: ?4 ?/ ?3" src/components/involve | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "overflow-hidden|overflow: ?hidden|overflow-clip" src/components/involve/Involve.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE 'href="#(about|resources|news)"' src/components/involve | awk -F: '{s+=$2} END {print s}'` не меньше 3
    - `grep -rcE "lg:grid-cols-3|grid-cols-3" src/components/about/About.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rnE "text-paper/([0-6][0-9]?|7[01])\b|248 247 251 / \.?(0?[0-6][0-9]?|7[01])\)" src/components/about src/components/involve | grep -v placeholder | grep -v VideoEmbed | wc -l` выводит 0
    - `npm test` завершается 0; `npm run build` завершается 0
  </acceptance_criteria>
  <done>Иллюстрации скрыты от скринридера, медиа не выходят за контейнер, стековые раскладки на мобильном, тесты и сборка зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Посетитель → форма | Ввод остаётся в браузере, на сервер не уходит; валидация на клиенте задаёт только UX, не безопасность |
| Скринридер → DOM | Aria-атрибуты описывают состояние честно; ложные `aria-live`/`role` вводят пользователя в заблуждение |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Форма «Зажечь свет» | accept | Данные не покидают вкладку (FORM-03), сети нет; в тост и на карту попадает только тип и страна |
| T-05-07 | Repudiation | `aria-live` счётчиков | accept | Обновление объявляется скринридеру один раз после добавления огонька; ложных объявлений нет, спам исключён `polite` |
| T-05-SC | Tampering | npm installs | mitigate | Установок в плане нет; `git diff -- package.json` без новых зависимостей |
</threat_model>

<verification>
- Пять `aria-labelledby` с правильными id, один `<h1>`
- Карта `role="img"` + `<title>`, счётчики `aria-live`, чипы `aria-pressed`, поля `htmlFor`/`aria-invalid`/`aria-describedby`, тост `role="status"`
- Grep-гейт непрозрачности текста даёт 0 в пяти секциях
- `npm test` и `npm run build` зелёные
</verification>

<success_criteria>
- «Accessibility Contract» выполнен для hero, map, light-form, about, involve по всем строкам таблицы «aria по элементам», относящимся к этим секциям
- «Responsive Checklist» 390/768/1024 для этих секций выполнен на уровне классов; визуальное подтверждение даёт smoke 05-07
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-03-SUMMARY.md` when done
</output>
