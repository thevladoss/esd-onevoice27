---
phase: 05-polish-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/reveal.constants.ts
  - src/components/layout/Reveal.tsx
  - src/components/layout/Reveal.test.tsx
  - src/components/layout/Reveal.motion.test.tsx
  - src/components/layout/Section.tsx
  - src/components/hero/Hero.tsx
  - src/components/map/MapSection.tsx
  - src/components/form/LightForm.tsx
  - src/components/about/About.tsx
  - src/components/involve/Involve.tsx
  - src/components/news/News.tsx
  - src/components/resources/Resources.tsx
  - src/components/quote/Quote.tsx
autonomous: true
requirements: [MOTION-01]

must_haves:
  truths:
    - "Заголовочные блоки и сетки карточек секций map, light-form, about, involve, news, resources, quote появляются при скролле один раз: opacity 0 → 1, сдвиг 24px (16px ниже 768px) → 0, 0.7s, cubic-bezier(0.22, 1, 0.36, 1)"
    - "Сетки карточек (счётчики, StepCard ×3, триптих ×3, новости ×6, ресурсы ×3) появляются каскадом с шагом 0.08s через variants, а не через delay на каждом элементе"
    - "При prefers-reduced-motion: reduce весь контент виден сразу: Reveal, RevealGroup и RevealItem рендерят обычные элементы без motion-обёртки и без inline opacity 0"
    - "Hero не обёрнут в Reveal; eyebrow, H1 и подзаголовок hero проявляются fade 0.6s с задержками 0 / 0.08 / 0.16s при монтировании, при reduce видны сразу"
    - "Reveal анимирует только opacity и y; после завершения у обёртки transform: none, стекло под ней не ломается"
  artifacts:
    - path: "src/components/layout/reveal.constants.ts"
      provides: "Единственный источник длительности, кривой и stagger"
      contains: "REVEAL_DURATION = 0.7"
    - path: "src/components/layout/Reveal.tsx"
      provides: "Reveal, RevealGroup, RevealItem"
      exports: ["Reveal", "RevealGroup", "RevealItem"]
      min_lines: 60
    - path: "src/components/layout/Reveal.test.tsx"
      provides: "Тест reduce: контент виден сразу, обёртки без motion"
    - path: "src/components/layout/Reveal.motion.test.tsx"
      provides: "Тест без reduce: начальное состояние opacity 0, группа рендерит детей"
  key_links:
    - from: "src/components/layout/Reveal.tsx"
      to: "motion/react"
      via: "import { motion, useReducedMotion }"
      pattern: "from \"motion/react\""
    - from: "src/components/layout/Reveal.tsx"
      to: "src/components/layout/reveal.constants.ts"
      via: "REVEAL_DURATION, REVEAL_EASE, REVEAL_STAGGER, REVEAL_DELAY_CHILDREN"
      pattern: "from \"./reveal.constants\""
    - from: "src/components/{map,form,about,involve,news,resources,quote}/*.tsx"
      to: "src/components/layout/Reveal.tsx"
      via: "обёртки Reveal / RevealGroup / RevealItem вокруг заголовочных блоков и сеток"
      pattern: "<Reveal(Group|Item)?"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story. Историю здесь не придумываем; для переформатирования запустите `/bm:mvp-phase 5`. Задачи ниже всё равно построены вертикальными срезами: после каждой задачи посетитель видит больше анимированных секций, чем до неё.

<objective>
Ввести контракт движения фазы 5: компонент `Reveal` с `RevealGroup`/`RevealItem` на `motion/react`, константы reveal, тесты на reduced motion, обёртки вокруг заголовочных блоков и сеток карточек семи секций, fade текста hero при монтировании.

Purpose: закрыть MOTION-01. Секции появляются при скролле один раз и уважают `prefers-reduced-motion`.
Output: `src/components/layout/reveal.constants.ts`, `Reveal.tsx`, два тестовых файла, обёртки в восьми файлах секций (hero получает только fade).
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/05-polish-and-release/05-RESEARCH.md
@.planning/research/PITFALLS.md

Кодовая база фаз 1–4 создаётся параллельно с планированием; точные внутренности компонентов неизвестны. Каждая задача начинается с чтения актуального кода по globs из `read_first`, затем применяет контракт из 05-UI-SPEC.md. Решения CONTEXT по reveal (параметры, состав обёрток, reduce) заблокированы; мобильные пороги (`y: 16`, `amount: 0.15`, `margin: -6%`) взяты из 05-UI-SPEC.md «Умолчания».

<interfaces>
<!-- Контракт, который создаёт задача 1. Задачи 2–3 и все последующие планы используют его без исследования кода. -->

Из src/components/layout/reveal.constants.ts:
```ts
export const REVEAL_DURATION = 0.7;                 // секунды
export const REVEAL_EASE = [0.22, 1, 0.36, 1] as const;
export const REVEAL_STAGGER = 0.08;
export const REVEAL_DELAY_CHILDREN = 0.05;
export const REVEAL_SHIFT_DESKTOP = 24;             // px, от 768px
export const REVEAL_SHIFT_MOBILE = 16;              // px, ниже 768px
export const HERO_FADE_DURATION = 0.6;
export const HERO_FADE_DELAYS = [0, 0.08, 0.16] as const;
```

Из src/components/layout/Reveal.tsx:
```ts
type RevealProps = { delay?: number; as?: React.ElementType; className?: string; children: React.ReactNode };
export function Reveal(props: RevealProps): JSX.Element;
type RevealGroupProps = { as?: React.ElementType; className?: string; children: React.ReactNode };
export function RevealGroup(props: RevealGroupProps): JSX.Element;
type RevealItemProps = { as?: React.ElementType; className?: string; children: React.ReactNode };
export function RevealItem(props: RevealItemProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Контракт Reveal: константы, компоненты, тесты на reduced motion</name>
  <files>src/components/layout/reveal.constants.ts, src/components/layout/Reveal.tsx, src/components/layout/Reveal.test.tsx, src/components/layout/Reveal.motion.test.tsx</files>
  <read_first>
    - package.json (убедиться, что `motion` уже в dependencies; фаза 5 пакетов не устанавливает)
    - src/test/setup.ts (как замоканы `matchMedia` и `IntersectionObserver`; как переопределить мок в отдельном тесте)
    - src/components/layout/*.tsx (стиль примитивов фазы 1: один экспорт на файл, пропы `as`/`className`, именование)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, разделы «Motion Contract» → «Reveal», «RevealGroup и RevealItem», «Взаимодействие reveal со стеклом», «Reduced Motion Policy» → «JS»
    - .planning/phases/05-polish-and-release/05-RESEARCH.md, раздел «motion v13 (проверено)»
  </read_first>
  <behavior>
    - Reveal.test.tsx (мок `window.matchMedia` возвращает `matches: true` для любого запроса, содержащего `prefers-reduced-motion`, установлен до первого рендера в файле): `render(<Reveal><p>Текст</p></Reveal>)` показывает «Текст» через `getByText`, корневой элемент обёртки не имеет inline `opacity: 0` (`element.style.opacity !== "0"`) и не имеет атрибута `style` со словом `transform`
    - Reveal.test.tsx: `render(<RevealGroup><RevealItem>A</RevealItem><RevealItem>B</RevealItem><RevealItem>C</RevealItem></RevealGroup>)` при reduce показывает все три текста, ни у одного узла нет inline `opacity: 0`
    - Reveal.test.tsx: `<Reveal as="section" className="x">` рендерит тег `section` с классом `x`
    - Reveal.motion.test.tsx (мок `matchMedia` возвращает `matches: false`, файл отдельный, потому что `useReducedMotion` кэширует результат первого `matchMedia` на уровне модуля motion): `render(<Reveal>...</Reveal>)` даёт корневой элемент с inline `opacity: 0` (motion выставляет `initial` как inline-стиль до срабатывания IntersectionObserver, мок IO в jsdom событий не шлёт)
    - Reveal.motion.test.tsx: `RevealGroup` с тремя `RevealItem` рендерит трёх детей в DOM (текст доступен через `getByText`), при этом у каждого `RevealItem` inline `opacity: 0`
  </behavior>
  <action>
    Сначала написать оба тестовых файла и прогнать `npx vitest run src/components/layout` (RED: падают на отсутствии модуля). Затем реализовать.

    `reveal.constants.ts`: экспортировать восемь констант из блока `<interfaces>` выше. Никаких других значений длительности и кривой в JSX проекта появляться не должно: все места берут их отсюда.

    `Reveal.tsx`: импорт `{ motion, useReducedMotion } from "motion/react"`. Единственный источник флага reduce в React: `useReducedMotion()` (решение 05-UI-SPEC; `MotionConfig` не используем, чтобы не плодить второй источник правды). Ширину читать один раз при монтировании через `useState(() => window.matchMedia("(max-width: 767px)").matches)` с защитой `typeof window !== "undefined"`; на ресайз не реагировать (reveal играет один раз). Вычислять `shift = isMobile ? REVEAL_SHIFT_MOBILE : REVEAL_SHIFT_DESKTOP` и `viewport = isMobile ? { once: true, amount: 0.15, margin: "0px 0px -6% 0px" } : { once: true, amount: 0.2, margin: "0px 0px -10% 0px" }`.

    `Reveal`: при `reduce === true` вернуть `<As className={className}>{children}</As>` без `style`, без `initial`. Иначе `motion.create(as ?? "div")` (мемоизировать через `useMemo` по `as`) с `initial={{ opacity: 0, y: shift }}`, `whileInView={{ opacity: 1, y: 0 }}`, `viewport`, `transition={{ duration: REVEAL_DURATION, ease: REVEAL_EASE, delay: delay ?? 0 }}`. Анимировать только `opacity` и `y`: никаких `scale`, `filter`, `boxShadow` (см. «Взаимодействие reveal со стеклом»). Конечное `y: 0` оставляет `transform: none` на узле, motion делает это сам; `will-change` вручную не ставить.

    `RevealGroup`: те же правила reduce (обычный элемент). Иначе `motion.create(as)` с `variants={{ hidden: {}, visible: { transition: { staggerChildren: REVEAL_STAGGER, delayChildren: REVEAL_DELAY_CHILDREN } } }}`, `initial="hidden"`, `whileInView="visible"`, тот же `viewport`. `RevealItem`: при reduce обычный элемент; иначе `motion.create(as)` с `variants={{ hidden: { opacity: 0, y: shift }, visible: { opacity: 1, y: 0, transition: { duration: REVEAL_DURATION, ease: REVEAL_EASE } } }}` без `initial`/`whileInView` (наследует состояние от группы). UI-SPEC записывает сдвиг элемента как `y: "var(--reveal-shift)"`; в коде используем то же число `shift` из `matchMedia`, чтобы компонент не зависел от порядка появления CSS-токена (его добавляет параллельный план 05-02); значения совпадают: 24/16. Максимум 6 детей на группу: задокументировать в JSDoc над `RevealGroup`.

    Тесты: в `Reveal.test.tsx` переопределить `window.matchMedia` в начале файла (до первого `render`) функцией, возвращающей объект `{ matches: query.includes("prefers-reduced-motion"), media: query, onchange: null, addEventListener() {}, removeEventListener() {}, addListener() {}, removeListener() {}, dispatchEvent: () => false }`; в `Reveal.motion.test.tsx` тот же объект с `matches: false`. Оба файла не шлют событий IntersectionObserver, поэтому `whileInView` не срабатывает, и inline `opacity: 0` остаётся детерминированным.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Reveal.test.tsx src/components/layout/Reveal.motion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/layout/reveal.constants.ts && test -f src/components/layout/Reveal.tsx && test -f src/components/layout/Reveal.test.tsx && test -f src/components/layout/Reveal.motion.test.tsx` завершается 0
    - `grep -cE "^export const (REVEAL_DURATION = 0.7|REVEAL_STAGGER = 0.08|REVEAL_DELAY_CHILDREN = 0.05|REVEAL_SHIFT_DESKTOP = 24|REVEAL_SHIFT_MOBILE = 16|HERO_FADE_DURATION = 0.6)" src/components/layout/reveal.constants.ts` выводит 6
    - `grep -c "0.22, 1, 0.36, 1" src/components/layout/reveal.constants.ts` выводит не меньше 1
    - `grep -cE "^export function (Reveal|RevealGroup|RevealItem)\b" src/components/layout/Reveal.tsx` выводит 3
    - `grep -c "useReducedMotion" src/components/layout/Reveal.tsx` выводит не меньше 1; `grep -c 'from "motion/react"' src/components/layout/Reveal.tsx` выводит 1
    - `grep -c "once: true" src/components/layout/Reveal.tsx` не меньше 1; `grep -c "amount: 0.2" src/components/layout/Reveal.tsx` не меньше 1; `grep -c "amount: 0.15" src/components/layout/Reveal.tsx` не меньше 1; `grep -c '"0px 0px -10% 0px"' src/components/layout/Reveal.tsx` не меньше 1; `grep -c '"0px 0px -6% 0px"' src/components/layout/Reveal.tsx` не меньше 1
    - `grep -c "staggerChildren: REVEAL_STAGGER" src/components/layout/Reveal.tsx` выводит 1; `grep -c "delayChildren: REVEAL_DELAY_CHILDREN" src/components/layout/Reveal.tsx` выводит 1
    - `grep -cE "scale|filter|boxShadow|willChange|will-change" src/components/layout/Reveal.tsx` выводит 0
    - `grep -cE "MotionConfig" src/components/layout/Reveal.tsx src/main.tsx src/App.tsx` для каждого файла выводит 0
    - `npx vitest run src/components/layout/Reveal.test.tsx src/components/layout/Reveal.motion.test.tsx` завершается 0, в выводе не меньше 5 passed
  </acceptance_criteria>
  <done>Три компонента и восемь констант экспортированы, пять тестов зелёные, ни одной анимации кроме opacity/y в Reveal.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Обёртки reveal в map, light-form, about, involve и fade текста hero</name>
  <files>src/components/layout/Section.tsx, src/components/hero/Hero.tsx, src/components/map/MapSection.tsx, src/components/form/LightForm.tsx, src/components/about/About.tsx, src/components/involve/Involve.tsx</files>
  <read_first>
    - src/components/layout/Section.tsx (рендерит ли примитив заголовочный блок eyebrow + title сам по пропам)
    - src/components/hero/Hero.tsx (где eyebrow, H1, подзаголовок, CTA)
    - src/components/map/MapSection.tsx (заголовочный блок, контейнер карты, пара счётчиков `Counters`)
    - src/components/form/LightForm.tsx (заголовочный блок, `GlassCard` формы)
    - src/components/about/About.tsx (заголовочный блок, `VideoEmbed`, три `StepCard`)
    - src/components/involve/Involve.tsx (заголовочный блок, три карточки триптиха)
    - src/components/layout/Reveal.tsx и reveal.constants.ts (созданы задачей 1)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, таблицы «Что оборачивается» и «Что не оборачивается»
  </read_first>
  <action>
    Применить таблицу «Что оборачивается» из 05-UI-SPEC.md, не меняя API секций и не трогая тексты:

    `#map` (MapSection.tsx): `Reveal` вокруг заголовочного блока (eyebrow + H2), `Reveal delay={0.1}` вокруг контейнера карты (обёртка снаружи контейнера с `ResizeObserver`, чтобы не менять его размеры), `RevealGroup` вокруг пары счётчиков, каждая карточка счётчика в `RevealItem`. Если счётчики стоят абсолютом поверх карты, `RevealGroup` получает тот же позиционирующий класс (проп `className`), а карточки остаются детьми.
    `#light-form` (LightForm.tsx): `Reveal` вокруг заголовочного блока, `Reveal delay={0.1}` вокруг `GlassCard` формы. Тост `role="status"` не оборачивать.
    `#about` (About.tsx): `Reveal` вокруг заголовочного блока, `Reveal delay={0.1}` вокруг видео-фасада, `RevealGroup` вокруг сетки из трёх `StepCard` с каждой карточкой в `RevealItem` (группа получает классы сетки через `className`, чтобы раскладка не менялась).
    `#involve` (Involve.tsx): `Reveal` вокруг заголовочного блока, `RevealGroup` вокруг триптиха, каждая карточка в `RevealItem`. Если триптих имеет общую рамку без зазоров (одна `GlassCard` со швами), `RevealGroup as` берёт тег контейнера рамки, а `RevealItem` оборачивает каждую карточку внутри без добавления зазоров.

    Если заголовочный блок рендерится примитивом `Section` по пропам `eyebrow`/`title`, обернуть этот блок в `Reveal` внутри `Section.tsx` один раз и добавить проп `reveal?: boolean` (по умолчанию `true`); в этом случае секции ниже не дублируют обёртку заголовка. Hero, если он использует `Section`, передаёт `reveal={false}`.

    Hero.tsx: `Reveal` не использовать. Обернуть eyebrow, H1 и подзаголовок в три `motion.div` (только `opacity`, без `y`, чтобы `background-clip: text` градиентного H1 не попадал в transform-контекст) с `initial={{ opacity: 0 }}`, `animate={{ opacity: 1 }}`, `transition={{ duration: HERO_FADE_DURATION, ease: REVEAL_EASE, delay: HERO_FADE_DELAYS[i] }}`. При `useReducedMotion() === true` рендерить обычные `div` без motion. CTA, `Starfield` и `GlobeCanvas` не трогать.

    После правок прогнать `npm test`: тесты фаз 2–3 на эти секции должны остаться зелёными (обёртки при моке `matchMedia` с `matches: false` рендерят детей с inline `opacity: 0`, но текст в DOM остаётся; если существующий тест проверяет `toBeVisible()` на обёрнутом блоке, переключить мок `matchMedia` этого теста на reduce либо проверять `toBeInTheDocument()`).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<Reveal" src/components/hero/Hero.tsx` выводит 0; `grep -c "HERO_FADE_DURATION" src/components/hero/Hero.tsx` выводит не меньше 1; `grep -c "useReducedMotion" src/components/hero/Hero.tsx` не меньше 1
    - `grep -c "<RevealGroup" src/components/map/MapSection.tsx` выводит не меньше 1; `grep -c "<RevealItem" src/components/map/MapSection.tsx` не меньше 1 (или не меньше 2, если карточки перечислены явно); `grep -c "delay={0.1}" src/components/map/MapSection.tsx` не меньше 1
    - `grep -c "delay={0.1}" src/components/form/LightForm.tsx` выводит не меньше 1
    - `grep -c "<RevealGroup" src/components/about/About.tsx` не меньше 1; `grep -c "<RevealItem" src/components/about/About.tsx` не меньше 1; `grep -c "delay={0.1}" src/components/about/About.tsx` не меньше 1
    - `grep -c "<RevealGroup" src/components/involve/Involve.tsx` не меньше 1; `grep -c "<RevealItem" src/components/involve/Involve.tsx` не меньше 1
    - Для каждого из четырёх файлов секций (`MapSection.tsx`, `LightForm.tsx`, `About.tsx`, `Involve.tsx`) либо `grep -c "<Reveal" <file>` не меньше 1, либо `grep -c "<Reveal" src/components/layout/Section.tsx` не меньше 1 и файл использует `<Section` с пропом `title`
    - `grep -rn "duration: 0\.[0-9]" src/components --include=*.tsx | grep -v "reveal.constants" | grep -vE "Reveal\.tsx|test" | wc -l` выводит 0 (длительности только из констант)
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Четыре секции и hero получили обёртки строго по таблице UI-SPEC, все существующие тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Обёртки reveal в news, resources, quote и сборка</name>
  <files>src/components/news/News.tsx, src/components/resources/Resources.tsx, src/components/quote/Quote.tsx</files>
  <read_first>
    - src/components/news/News.tsx и src/components/news/*.tsx (заголовочный блок, сетка карточек, пагинация, как рендерится страница из `paginate`)
    - src/components/resources/Resources.tsx и src/components/resources/*.tsx (центральный текстовый блок, три карточки-кнопки, `ResourcePanel`)
    - src/components/quote/Quote.tsx (блок цитаты с подписью)
    - src/components/layout/Reveal.tsx (контракт из задачи 1)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, таблицы «Что оборачивается», «RevealGroup и RevealItem» (правило про смену страницы пагинации)
  </read_first>
  <action>
    `#news` (News.tsx): `Reveal` вокруг заголовочного блока, `RevealGroup` вокруг сетки карточек (группа получает классы сетки через `className`; каждая карточка первой страницы в `RevealItem`), `Reveal delay={0.1}` вокруг пагинации. Смена страницы reveal не перезапускает: `once: true` уже отработал у группы; карточки второй страницы появляются мгновенно. Для этого `RevealGroup` монтируется один раз (ключ группы не зависит от номера страницы), а `RevealItem` получают стабильный `key` по `item.id`; при смене страницы новые `RevealItem` монтируются внутри группы, которая уже в состоянии `visible`, и получают `visible` без анимации ожидания.
    `#resources` (Resources.tsx): `Reveal` вокруг центрального текстового блока, `RevealGroup` вокруг трёх карточек-кнопок, каждая в `RevealItem` (если раскладка 12-колоночная с явными `grid-column`/`grid-row` на карточках, эти классы переносятся на `RevealItem` через `className`, чтобы карточка осталась прямым grid-ребёнком). `ResourcePanel` не оборачивать: раскрытие уже анимировано фазой 4.
    `#quote` (Quote.tsx): один `Reveal` вокруг блока цитаты вместе с подписью (`figure` целиком, `as="figure"` если корневой элемент блока сам `figure`, иначе `div`-обёртка снаружи).

    Header, оверлей меню и footer не трогать (владелец 05-02). Затем `npm test` и `npm run build`.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<RevealGroup" src/components/news/News.tsx` не меньше 1; `grep -c "<RevealItem" src/components/news/News.tsx` не меньше 1; `grep -c "delay={0.1}" src/components/news/News.tsx` не меньше 1
    - `grep -c "<RevealGroup" src/components/resources/Resources.tsx` не меньше 1; `grep -c "<RevealItem" src/components/resources/Resources.tsx` не меньше 1
    - `grep -c "<Reveal" src/components/quote/Quote.tsx` не меньше 1; `grep -c "<RevealGroup" src/components/quote/Quote.tsx` выводит 0
    - `grep -c "<Reveal" src/components/layout/Header.tsx src/components/layout/Footer.tsx` для каждого файла выводит 0
    - `grep -rl "<Reveal" src/components | wc -l` выводит не меньше 7 (Section.tsx засчитывается вместо секций, использующих его заголовок)
    - `npm test` завершается 0; `npm run build` завершается 0
  </acceptance_criteria>
  <done>Все семь секций из таблицы обёрнуты, hero не обёрнут, тесты и сборка зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер посетителя → DOM | Reveal скрывает контент до IntersectionObserver; при отказе JS или reduce контент обязан быть виден |
| Проект → npm-пакет `motion` | Уже установлен фазой 1; фаза 5 новых пакетов не ставит |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | Reveal (opacity 0 без срабатывания IO) | mitigate | `once: true` + `useReducedMotion` fallback без motion; тест reduce подтверждает видимость; smoke 05-07 проверяет `opacity: 1` после прокрутки |
| T-05-02 | Tampering | Hero fade поверх `background-clip: text` | mitigate | Только `opacity`, без `y`/transform на обёртке H1 |
| T-05-SC | Tampering | npm installs | mitigate | В этом плане `npm install` не выполняется; `git diff -- package.json` пуст по секциям dependencies. Появление новой зависимости останавливает план |
</threat_model>

<verification>
- `npx vitest run src/components/layout` зелёный: 5 тестов Reveal
- `npm test` и `npm run build` завершаются 0
- `grep -rl "<Reveal" src/components | wc -l` не меньше 7, `grep -c "<Reveal" src/components/hero/Hero.tsx` равен 0
- Нет длительностей, объявленных вне `reveal.constants.ts`
</verification>

<success_criteria>
- Reveal/RevealGroup/RevealItem реализованы по таблицам 05-UI-SPEC.md, параметры совпадают дословно (0.7s, [0.22, 1, 0.36, 1], once, amount 0.2/0.15, margin -10%/-6%, stagger 0.08, delayChildren 0.05)
- При reduce компоненты рендерят обычные элементы, тест это доказывает
- Семь секций обёрнуты по таблице, hero получил fade 0.6s без transform
- Тесты и сборка зелёные
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-01-SUMMARY.md` when done
</output>
