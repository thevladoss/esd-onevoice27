---
phase: 15-lights-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/map/lightsCanvas.ts
  - src/components/map/lightsCanvas.test.ts
autonomous: true
requirements: [LIGHT-02, LIGHT-04, LIGHT-05]
tags: [map, canvas, lights, math, tdd]

must_haves:
  truths:
    - "lightsCanvas.ts экспортирует константы спецификации: период 2600 мс, 5 корзин, радиус ореола 7–12px, alpha .30–.60, спрайт ореола 12px c alpha .9 в центре, ядро 2,2px с белой обводкой .9px alpha .5, цвета rgb(158 67 154) и rgb(84 164 172), порог кадра 33 мс, кольцо 900 мс 6→20,4px alpha .5→0 по cubic-bezier(.16, 1, .3, 1), reduce 9px alpha .22, dpr не выше 2"
    - "breath(t, n) = (1 + sin(2π·t/2600 − 2π·n/5)) / 2, haloRadius(s) = 7 + 5·s, haloAlpha(s) = .30 + .30·s; корзина огонька bucketOf(index) = index % 5"
    - "drawFrame рисует кадр: clearRect, по корзинам globalAlpha и ореол-спрайт радиусом 7 + 5·s в точке transform.apply([x, y]), ядра с alpha 1, кольца новых огоньков поверх; под reduce ореол 9px alpha .22 и без колец; точки вне холста пропускаются; размер спрайта от масштаба не зависит"
    - "frameDue ложно раньше 33 мс; shouldAnimate ложно вне экрана, в скрытой вкладке и при reduce"
    - "makeSprites создаёт четыре offscreen-спрайта под dpr и возвращает null, если у спрайта нет 2d-контекста"
  artifacts:
    - path: "src/components/map/lightsCanvas.ts"
      provides: "Константы, дыхание корзин, кольцо нового огонька, планировщик 30 fps, спрайты, drawFrame"
      exports: ["BREATH_PERIOD_MS", "LIGHT_BUCKETS", "HALO_RADIUS_MIN", "HALO_RADIUS_MAX", "HALO_ALPHA_MIN", "HALO_ALPHA_MAX", "LIGHT_COLORS", "breath", "haloRadius", "haloAlpha", "splitBuckets", "ringState", "frameDue", "shouldAnimate", "clampDpr", "summarize", "makeSprites", "drawFrame"]
      min_lines: 180
    - path: "src/components/map/lightsCanvas.test.ts"
      provides: "Unit-тесты констант и математики без canvas; drawFrame и makeSprites на мок-контексте"
      min_lines: 200
  key_links:
    - from: "src/components/map/lightsCanvas.ts drawFrame"
      to: "PointTransform.apply (ZoomTransform из d3-zoom)"
      via: "экранная позиция огонька transform.apply([x, y]); ширина drawImage равна 2·r и от k не зависит"
      pattern: "transform\\.apply\\("
    - from: "src/components/map/lightsCanvas.ts breath"
      to: "BREATH_PERIOD_MS и LIGHT_BUCKETS"
      via: "Math.sin(2π·t/BREATH_PERIOD_MS − 2π·n/LIGHT_BUCKETS)"
      pattern: "Math\\.sin\\("
    - from: "src/components/map/lightsCanvas.ts ringState"
      to: "cubicBezier(.16, 1, .3, 1)"
      via: "радиус 6 + 14,4·ease(p), alpha .5·(1 − ease(p)), p = elapsed / 900"
      pattern: "RING_EASE"
---

<objective>
Собрать чистый модуль `lightsCanvas.ts`: константы спецификации LIGHT-02/04/05, математику дыхания по пяти корзинам, кольцо нового огонька, планировщик кадров 30 fps, флаги паузы, спрайты ореола и ядра под dpr и функцию `drawFrame`, которая рисует один кадр огоньков на переданный 2d-контекст. Модуль не знает о React и DOM: canvas для спрайтов приходит через фабрику, контекст кадра приходит параметром, поэтому вся математика проверяется unit-тестами без браузера.

Purpose: 942 огонька в SVG (1884 круга, пять корзин с анимацией opacity) держат 30 fps на телефоне при CPU×4 (`docs/research/v1.2/measurements.md`); canvas со спрайтами рисует тот же кадр десятками `drawImage` без 1884 узлов DOM и возвращает дыхание радиуса 7→12px, снятое fallback'ом MAP-06 в v1.1.
Output: `src/components/map/lightsCanvas.ts` с контрактом для плана 15-02 и `src/components/map/lightsCanvas.test.ts`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@docs/research/v1.2/measurements.md
@src/data/lights.ts
@src/components/hero/globe.ts
@src/components/hero/GlobeCanvas.test.tsx
@src/lib/easing.ts

<ownership>
Фаза 15 идёт параллельно с фазами 14 и 16 в отдельном worktree от одного `main`. Редактировать ТОЛЬКО файлы из `files_modified`. Запрещено трогать `src/styles/global.css`, `src/state/lights.tsx`, `src/test/setup.ts`, `src/components/map/Counters.tsx`, `CountryChips.tsx`, `MapBand.tsx`, `MapSection.tsx`, `src/components/form/*`, `src/components/hero/*` (файлы hero читаем как образец, не правим: фаза 14 их удаляет), `package.json`, `package-lock.json`, `vite.config.ts`. Комментарии в коде на русском, идентификаторы на английском. Коммитов исполнитель не делает. Команды в задачах относительные: cwd уже равен корню worktree.
</ownership>

<interfaces>
<!-- Существующие контракты. Извлечены из кода, исследовать заново не нужно. -->

From src/data/lights.ts:
```ts
export type LightType = "person" | "group";
export interface Light { id: string; type: LightType; countryId: number; lon: number; lat: number; isNew?: boolean }
export function generateLights(seed?: number, people?: number, groups?: number): Light[]; // 694 person + 248 group = 942, детерминированно
```

From d3-zoom (структурно достаточно `apply`):
```ts
class ZoomTransform { k: number; x: number; y: number; apply(point: [number, number]): [number, number] /* [x·k + tx, y·k + ty] */ }
```

From src/components/hero/globe.ts (образец флагов паузы, файл не править):
```ts
export type GlobeMotionFlags = { inView: boolean; hidden: boolean; reducedMotion: boolean };
export function shouldAnimate(flags: GlobeMotionFlags): boolean { return flags.inView && !flags.hidden && !flags.reducedMotion; }
```

From src/components/hero/GlobeCanvas.test.tsx (образец мок-контекста для тестов без canvas): функция `mockContext()` возвращает объект с `vi.fn()` для `clearRect`, `beginPath`, `arc`, `fill`, `stroke`, `setTransform`, `createRadialGradient` (возвращает `{ addColorStop: vi.fn() }`) и полями `globalAlpha`, `fillStyle`, `strokeStyle`, `lineWidth`; в этом плане мок расширяется методами `drawImage`, `fillRect`, `scale`.

Контрольные значения для тестов (посчитаны заранее, точность toBeCloseTo с 3 знаками):
- breath(0, 0) = 0.5; breath(650, 0) = 1; breath(1300, 0) = 0.5; breath(1950, 0) = 0; breath(0, 1) = 0.0245; breath(520, 1) = 0.5; breath(2600, 3) = 0.7939.
- cubicBezier(.16, 1, .3, 1): ease(0) = 0, ease(0.1) = 0.494, ease(0.25) = 0.826, ease(0.5) = 0.972, ease(0.75) = 0.998, ease(1) = 1.
- ringState(450): radius = 6 + 14.4·ease(0.5) = 19.994, alpha = 0.5·(1 − ease(0.5)) = 0.014.
- 942 = 5 × 188 + 2: splitBuckets даёт длины [189, 189, 188, 188, 188].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Константы и чистая математика lightsCanvas.ts с тестами (дыхание, кольцо, планировщик, флаги, сводка)</name>
  <files>src/components/map/lightsCanvas.ts, src/components/map/lightsCanvas.test.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, раздел 2 (LIGHT-02, LIGHT-04, LIGHT-05: все числа зафиксированы там)
    - .planning/ROADMAP.md, Phase 15, Success Criteria 2 и 4
    - src/components/hero/globe.ts строки 39–41 и 96–108 (FRAME_MS, шаг по времени кадра, shouldAnimate: образец стиля констант и флагов)
    - src/lib/easing.ts (стиль чистых функций; для кольца нужен настоящий решатель кривой Безье, а не аппроксимация степенью)
    - src/data/lights.ts строки 1–20 (тип Light, isNew)
  </read_first>
  <behavior>
    - Константы равны числам спецификации: BREATH_PERIOD_MS 2600, LIGHT_BUCKETS 5, HALO_RADIUS_MIN 7, HALO_RADIUS_MAX 12, HALO_ALPHA_MIN 0.3, HALO_ALPHA_MAX 0.6, SPRITE_HALO_RADIUS 12, SPRITE_HALO_ALPHA 0.9, CORE_RADIUS 2.2, CORE_STROKE_WIDTH 0.9, CORE_STROKE_ALPHA 0.5, FRAME_MS 1000/30, MIN_FRAME_GAP_MS 33, RING_MS 900, RING_RADIUS_FROM 6, RING_RADIUS_TO 20.4, RING_ALPHA_FROM 0.5, RING_EASE [0.16, 1, 0.3, 1], REDUCED_HALO_RADIUS 9, REDUCED_HALO_ALPHA 0.22, MAX_DPR 2; LIGHT_COLORS.person "rgb(158 67 154)", LIGHT_COLORS.group "rgb(84 164 172)"; colorWithAlpha("person", 0.9) = "rgba(158, 67, 154, 0.9)"
    - breath даёт контрольные значения из блока interfaces; breath(t + 2600, n) = breath(t, n); breath(t, 1) = breath(t − 520, 0) (сдвиг корзины равен пятой части периода)
    - haloRadius(0) = 7, haloRadius(1) = 12, haloRadius(0.5) = 9.5; haloAlpha(0) = 0.3, haloAlpha(1) = 0.6
    - bucketOf(0…6) = [0, 1, 2, 3, 4, 0, 1]; splitBuckets на 942 элементах даёт длины [189, 189, 188, 188, 188], порядок внутри корзины сохраняется, сумма 942
    - ringEase = cubicBezier(...RING_EASE): контрольные точки из interfaces с точностью 0.005, монотонно не убывает на сетке шагом 0.05, ease(x < 0) = 0, ease(x > 1) = 1
    - ringState(0) = { radius: 6, alpha: 0.5, done: false }; ringState(450) = { radius ≈ 19.994, alpha ≈ 0.014, done: false }; ringState(900) = { radius: 20.4, alpha: 0, done: true }; ringState(1200) тоже done с радиусом 20.4 и alpha 0
    - frameDue(16.7) false, frameDue(32.9) false, frameDue(33) true, frameDue(50) true
    - shouldAnimate: true только при inView && !hidden && !reducedMotion (все восемь комбинаций)
    - clampDpr(undefined) = 1, clampDpr(0) = 1, clampDpr(1.5) = 1.5, clampDpr(2) = 2, clampDpr(3) = 2
    - summarize на 942 точках из generateLights (x, y любые) = { count: 942, people: 694, groups: 248, fresh: 0 }; после добавления точки с isNew: true и type "person" = { 943, 695, 248, 1 }
    - isOnCanvas(−13, 10, 100, 100, 12) false, isOnCanvas(−11, 10, 100, 100, 12) true, isOnCanvas(50, 113, 100, 100, 12) false, isOnCanvas(NaN, 10, 100, 100, 12) false
  </behavior>
  <action>
    Реализует LIGHT-02 (формула дыхания, радиусы, alpha, цвета), LIGHT-04 (порог 33 мс, флаги паузы, значения reduce) и LIGHT-05 (кольцо) в виде чистых функций. Сначала написать тесты из `<behavior>` в `src/components/map/lightsCanvas.test.ts` (describe «lightsCanvas: константы», «дыхание корзин», «кольцо нового огонька», «планировщик и флаги», «сводка огоньков»), убедиться, что они красные (модуля нет), затем создать `src/components/map/lightsCanvas.ts`.

    1. Константы, все `export const`, с однострочными комментариями на русском, откуда число (LIGHT-02/04/05): `BREATH_PERIOD_MS = 2600`, `LIGHT_BUCKETS = 5`, `HALO_RADIUS_MIN = 7`, `HALO_RADIUS_MAX = 12`, `HALO_ALPHA_MIN = 0.3`, `HALO_ALPHA_MAX = 0.6`, `SPRITE_HALO_RADIUS = 12`, `SPRITE_HALO_ALPHA = 0.9`, `CORE_RADIUS = 2.2`, `CORE_STROKE_WIDTH = 0.9`, `CORE_STROKE_ALPHA = 0.5`, `CORE_SPRITE_RADIUS = 3.1` (CORE_RADIUS + CORE_STROKE_WIDTH: половина стороны спрайта ядра с запасом под обводку и сглаживание), `FRAME_MS = 1000 / 30`, `MIN_FRAME_GAP_MS = 33` (кадр раньше 33 мс пропускается), `RING_MS = 900`, `RING_RADIUS_FROM = 6`, `RING_RADIUS_TO = 20.4`, `RING_ALPHA_FROM = 0.5`, `RING_EASE: readonly [number, number, number, number] = [0.16, 1, 0.3, 1]`, `REDUCED_HALO_RADIUS = 9`, `REDUCED_HALO_ALPHA = 0.22`, `MAX_DPR = 2`, `LIGHT_RGB: Record<LightType, readonly [number, number, number]> = { person: [158, 67, 154], group: [84, 164, 172] }`, `LIGHT_COLORS: Record<LightType, string> = { person: "rgb(158 67 154)", group: "rgb(84 164 172)" }` (строки спецификации, их читают тесты `App.seams.test.tsx` и зонд). Функция `colorWithAlpha(type: LightType, alpha: number): string` возвращает `rgba(r, g, b, a)` через запятую: такой синтаксис понимают все движки canvas, включая старые Safari, а `rgb(r g b / a)` в `fillStyle` там мог бы не разобраться.

    2. Типы: `LightPoint { light: Light; x: number; y: number }` (тот же элемент, что `points` в `EsdMap.tsx`), `PointTransform { apply(point: [number, number]): [number, number] }` (ZoomTransform подходит структурно, импорт d3-zoom модулю не нужен), `MotionFlags { inView: boolean; hidden: boolean; reducedMotion: boolean }`, `RingState { radius: number; alpha: number; done: boolean }`, `LightsSummary { count: number; people: number; groups: number; fresh: number }`. Импорт `Light`/`LightType` только через `import type` из `../../data/lights`.

    3. Функции: `breath(nowMs, bucket)` по формуле `(1 + Math.sin((2 * Math.PI * nowMs) / BREATH_PERIOD_MS - (2 * Math.PI * bucket) / LIGHT_BUCKETS)) / 2`; `haloRadius(s) = HALO_RADIUS_MIN + (HALO_RADIUS_MAX - HALO_RADIUS_MIN) * s`; `haloAlpha(s) = HALO_ALPHA_MIN + (HALO_ALPHA_MAX - HALO_ALPHA_MIN) * s`; `bucketOf(index) = index % LIGHT_BUCKETS`; `splitBuckets<T>(points: readonly T[]): T[][]` раскладывает по `bucketOf` в `LIGHT_BUCKETS` массивов (та же логика, что `buckets` в `EsdMap.tsx` сейчас; после плана 15-02 она живёт только здесь); `cubicBezier(x1, y1, x2, y2)` возвращает функцию x → y по алгоритму CSS-кривой: решение x(t) = x методом Ньютона (8 итераций, при нулевой производной остановка), затем y(t); значения вне [0, 1] зажимаются в 0 и 1; `ringEase = cubicBezier(...RING_EASE)`; `ringState(elapsedMs)`: `p = min(1, max(0, elapsedMs / RING_MS))`, `e = ringEase(p)`, `radius = RING_RADIUS_FROM + (RING_RADIUS_TO - RING_RADIUS_FROM) * e`, `alpha = RING_ALPHA_FROM * (1 - e)`, `done = elapsedMs >= RING_MS`; `frameDue(elapsedMs) = elapsedMs >= MIN_FRAME_GAP_MS`; `shouldAnimate(flags)` как в `globe.ts`; `clampDpr(ratio)`: нечисло, NaN и значения ≤ 0 дают 1, иначе `Math.min(ratio, MAX_DPR)`; `summarize(points)` считает `count` (длина), `people`/`groups` по `light.type`, `fresh` по `light.isNew === true` за один проход; `isOnCanvas(x, y, width, height, margin)`: `x >= -margin && x <= width + margin && y >= -margin && y <= height + margin` (NaN даёт false сам по себе).

    4. Заголовочный комментарий модуля: почему canvas (числа из measurements.md: SVG держал 30 fps при CPU×4 на 390×844), что модуль чистый и что рисование живёт в `drawFrame` (Task 2). В тестах генерацию `generateLights()` вызывать один раз на файл (rejection sampling дорогой), точки строить без проекции: `x = 10 + (i % 40) * 25`, `y = 10 + Math.floor(i / 40) * 25`.

    Запрещено: любые импорты React и DOM-API в `lightsCanvas.ts` (кроме типов `HTMLCanvasElement`/`CanvasRenderingContext2D` в Task 2), `Math.random`, чтение `window`/`document` на уровне модуля.
  </action>
  <acceptance_criteria>
    - `src/components/map/lightsCanvas.ts` существует и содержит строки `export const BREATH_PERIOD_MS = 2600`, `export const LIGHT_BUCKETS = 5`, `export const HALO_RADIUS_MIN = 7`, `export const HALO_RADIUS_MAX = 12`, `export const HALO_ALPHA_MIN = 0.3`, `export const HALO_ALPHA_MAX = 0.6`, `export const MIN_FRAME_GAP_MS = 33`, `export const RING_MS = 900`, `export const RING_RADIUS_TO = 20.4`, `export const REDUCED_HALO_RADIUS = 9`, `export const REDUCED_HALO_ALPHA = 0.22`, `export const MAX_DPR = 2`, `"rgb(158 67 154)"`, `"rgb(84 164 172)"`, `[0.16, 1, 0.3, 1]`, `Math.sin(`
    - `grep -c "from \"react\"\|from 'react'\|document\.\|window\." src/components/map/lightsCanvas.ts` даёт 0 (Task 2 добавит единственное `document.createElement` внутри фабрики по умолчанию; до Task 2 обращений к DOM нет)
    - `src/components/map/lightsCanvas.test.ts` содержит describe-блоки «константы», «дыхание корзин», «кольцо нового огонька», «планировщик и флаги», «сводка огоньков» и проверки `toBeCloseTo(0.0245, 3)`, `toBeCloseTo(0.7939, 3)`, `toBeCloseTo(0.972, 2)`, `toEqual([189, 189, 188, 188, 188])`
    - `npx vitest run src/components/map/lightsCanvas.test.ts` код 0, все тесты passed
    - `npx tsc -b` код 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map/lightsCanvas.test.ts && npx tsc -b</automated>
  </verify>
  <done>Модуль экспортирует все константы и чистые функции контракта; тесты математики зелёные; tsc чистый.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Спрайты и drawFrame с тестами на мок-контексте</name>
  <files>src/components/map/lightsCanvas.ts, src/components/map/lightsCanvas.test.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, LIGHT-02 (порядок отрисовки кадра), LIGHT-03 (transform.apply, размер спрайта не зависит от масштаба), LIGHT-04 (статичный кадр reduce), LIGHT-05 (кольцо: 1px, currentColor огонька, одна прокрутка)
    - src/components/hero/GlobeCanvas.test.tsx строки 5–23 (мок-контекст; расширить `drawImage`, `fillRect`, `scale`, `createRadialGradient`)
    - src/components/hero/GlobeCanvas.tsx строки 41–52 (dpr, `setTransform(dpr, 0, 0, dpr, 0, 0)`: тот же приём для спрайтов через `scale(dpr, dpr)`)
  </read_first>
  <behavior>
    - makeSprites(2, factory): фабрика вызвана 4 раза; у спрайтов ореола width = height = ceil(24·2) = 48, у ядра ceil(6.2·2) = 13; контекст каждого спрайта получил scale(2, 2); ореол: createRadialGradient(12, 12, 0, 12, 12, 12), addColorStop(0, "rgba(158, 67, 154, 0.9)") и addColorStop(1, "rgba(158, 67, 154, 0)") для person (для group 84, 164, 172), fillRect(0, 0, 24, 24); ядро: arc(3.1, 3.1, 2.2, 0, 2π), fillStyle "rgba(158, 67, 154, 1)", lineWidth 0.9, strokeStyle "rgba(255, 255, 255, 0.5)", вызовы fill и stroke; результат: dpr 2, halo.person.radius 12, halo.group.radius 12, core.person.radius 3.1, core.group.radius 3.1
    - makeSprites с фабрикой, чей canvas даёт getContext → null, возвращает null и не бросает
    - drawFrame на 10 точках (5 person, 5 group, буферы через splitBuckets), identity-трансформе, now = 650, без колец, холст 400×300: первый вызов clearRect(0, 0, 400, 300); 10 вызовов drawImage ореола, для точек корзины 0 при globalAlpha 0.6 с аргументами (halo[type].canvas, x − 12, y − 12, 24, 24); для корзины 1 globalAlpha ≈ 0.4964 и ширина ≈ 20.545 (s = 0.6545); затем 10 вызовов drawImage ядра при globalAlpha 1 с аргументами (core[type].canvas, x − 3.1, y − 3.1, 6.2, 6.2); в конце globalAlpha 1; ringsActive false
    - drawFrame с трансформом apply([x, y]) → [2x + 10, 2y + 20]: ореол корзины 0 рисуется в (2x + 10 − 12, 2y + 20 − 12) шириной 24 (размер не зависит от k), ядро шириной 6.2
    - drawFrame с кольцом { point, startedAt: 650 } при now 650: после ядер beginPath, arc(x, y, 6, 0, 2π), stroke при globalAlpha 0.5, strokeStyle "rgba(158, 67, 154, 1)" для person, lineWidth 1; ringsActive true; при now 1550 (elapsed 900) arc не вызывается, ringsActive false; при now 1100 arc с радиусом ringState(450).radius
    - drawFrame с reduced: true (и тем же кольцом): все ореолы шириной 18 при globalAlpha 0.22, ядра как обычно, arc не вызывается, ringsActive false
    - точка с координатами (−50, −50) при identity не рисуется ни ореолом, ни ядром (число drawImage на 2 меньше); точка, которая после трансформа уходит за холст, тоже пропускается
    - позиции считаются через transform.apply: apply замокан vi.fn и вызван хотя бы по разу на каждую нарисованную точку
  </behavior>
  <action>
    Реализует отрисовку LIGHT-02, LIGHT-03 (координаты), LIGHT-04 (reduce-кадр) и LIGHT-05 (кольцо). Сначала дописать тесты из `<behavior>` (describe «спрайты» и «drawFrame») с мок-контекстом по образцу `GlobeCanvas.test.tsx` (в мок добавить `drawImage`, `fillRect`, `scale`; `drawImage` реализовать через `vi.fn` с `mockImplementation`, который пушит в массив снимок `{ alpha: ctx.globalAlpha, args }`, потому что `globalAlpha` меняется между вызовами и по одним `mock.calls` его не восстановить). Фабрика canvas в тестах: `() => ({ width: 0, height: 0, getContext: () => ctxForSprite }) as unknown as HTMLCanvasElement`, для каждого вызова свой мок, чтобы сверять вызовы по спрайтам. Затем реализовать.

    1. Типы: `Sprite { canvas: HTMLCanvasElement; radius: number }` (radius в CSS-пикселях: половина стороны рисунка), `LightSprites { dpr: number; halo: Record<LightType, Sprite>; core: Record<LightType, Sprite> }`, `CanvasFactory = () => HTMLCanvasElement`, `Ring { point: LightPoint; startedAt: number }`, `FrameInput { points: readonly LightPoint[]; buckets: ReadonlyArray<readonly LightPoint[]>; transform: PointTransform; now: number; rings: readonly Ring[]; reduced: boolean; width: number; height: number }`.

    2. `makeSprites(dpr: number, createCanvas: CanvasFactory = () => document.createElement("canvas")): LightSprites | null`. Внутренняя `paintSprite(radius, paint)`: `side = Math.ceil(radius * 2 * dpr)`, `canvas = createCanvas()`, `canvas.width = canvas.height = side`, `ctx = canvas.getContext("2d")`, при null вернуть null, иначе `ctx.scale(dpr, dpr)` и `paint(ctx)`; вернуть `{ canvas, radius }`. Ореол типа: `radius = SPRITE_HALO_RADIUS`, градиент `createRadialGradient(12, 12, 0, 12, 12, 12)` со стопами `0 → colorWithAlpha(type, SPRITE_HALO_ALPHA)` и `1 → colorWithAlpha(type, 0)`, `fillStyle = gradient`, `fillRect(0, 0, 24, 24)`. Ядро типа: `radius = CORE_SPRITE_RADIUS`, `beginPath`, `arc(3.1, 3.1, CORE_RADIUS, 0, 2π)`, `fillStyle = colorWithAlpha(type, 1)`, `fill`, `lineWidth = CORE_STROKE_WIDTH`, `strokeStyle = "rgba(255, 255, 255, 0.5)"` (через общий хелпер `rgba(255, 255, 255, CORE_STROKE_ALPHA)`), `stroke`. Если любой из четырёх спрайтов вернул null, вся функция возвращает null. Комментарий: спрайты перерисовываются только при смене размера или dpr (LIGHT-02), это решает вызывающая сторона.

    3. `drawFrame(ctx, sprites, input): { ringsActive: boolean }`. Порядок: `ctx.clearRect(0, 0, width, height)`; проход по корзинам `n`: при `reduced` `r = REDUCED_HALO_RADIUS`, `ctx.globalAlpha = REDUCED_HALO_ALPHA`, иначе `s = breath(now, n)`, `r = haloRadius(s)`, `ctx.globalAlpha = haloAlpha(s)`; для каждой точки корзины `[sx, sy] = transform.apply([x, y])`, при `!isOnCanvas(sx, sy, width, height, r)` пропуск, иначе `ctx.drawImage(sprites.halo[type].canvas, sx - r, sy - r, r * 2, r * 2)`; затем `ctx.globalAlpha = 1` и проход по `points`: `d = sprites.core[type].radius`, тот же apply и отсев, `ctx.drawImage(sprites.core[type].canvas, sx - d, sy - d, d * 2, d * 2)`; затем кольца, только если `!reduced`: для каждого `ring` `state = ringState(now - ring.startedAt)`, при `state.done` пропуск, иначе `ringsActive = true`, `[sx, sy] = transform.apply(...)`, `ctx.globalAlpha = state.alpha`, `ctx.strokeStyle = colorWithAlpha(type, 1)`, `ctx.lineWidth = 1`, `beginPath`, `arc(sx, sy, state.radius, 0, 2π)`, `stroke`; в конце `ctx.globalAlpha = 1`. Дискреция по производительности: экранные позиции можно посчитать один раз за кадр в переиспользуемый `Float64Array` и читать в обоих проходах, но каждая позиция обязана идти через `transform.apply` (ключевая связь LIGHT-03). Размер `drawImage` зависит только от `r`/`d`, не от `transform.k` (LIGHT-03).

    4. Обновить заголовочный комментарий модуля: перечислить, что делает кадр, и что под reduce рисуется один статичный кадр (ореол 9px alpha .22, ядра обычные, без колец), а планировщик и наблюдатели живут в компоненте (план 15-02).
  </action>
  <acceptance_criteria>
    - `lightsCanvas.ts` содержит `export function makeSprites(`, `export function drawFrame(`, `createRadialGradient(`, `clearRect(0, 0, `, `transform.apply([`, `isOnCanvas(`, `ringState(`, `REDUCED_HALO_RADIUS` и `REDUCED_HALO_ALPHA` внутри `drawFrame`, `document.createElement("canvas")` ровно один раз (фабрика по умолчанию)
    - `lightsCanvas.test.ts` содержит describe «спрайты» и «drawFrame», строки `"rgba(158, 67, 154, 0.9)"`, `"rgba(255, 255, 255, 0.5)"`, `toBe(24)` или `24, 24`, проверку ширины 18 под reduce, проверку `ringsActive`
    - `npx vitest run src/components/map/lightsCanvas.test.ts` код 0, все тесты passed
    - `npx tsc -b` код 0; `npm run lint` код 0
    - `git status --porcelain` показывает только `src/components/map/lightsCanvas.ts` и `src/components/map/lightsCanvas.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map/lightsCanvas.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>Спрайты и кадр воспроизводят числа спецификации на мок-контексте; кольцо рисуется одну прокрутку и гасится под reduce; отсев за холстом работает; vitest, tsc, lint зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл GitHub Pages | входа данных нет; модуль рисует замоканные огоньки из `src/data/lights.ts` |
| модуль → canvas 2d | контекст приходит параметром; при null модуль возвращает null и ничего не рисует |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Denial of service | `drawFrame` (1884 `drawImage` на кадр) | mitigate | спрайты вместо градиентов на каждый кадр, отсев точек за холстом, порог 33 мс в планировщике; бюджет меряет план 15-03 |
| T-15-02 | Tampering | константы спецификации | mitigate | тесты сверяют числа с LIGHT-02/04/05; `App.seams.test.tsx` (план 15-02) читает те же экспорты |
| T-15-SC | Tampering | npm-установки | accept | зависимостей не добавляется, `package.json` неизменен; `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `npx vitest run src/components/map/lightsCanvas.test.ts` зелёный: константы, дыхание, кольцо, планировщик, сводка, спрайты, drawFrame
- `npx tsc -b`, `npm run lint` чистые
- `git status --porcelain` не показывает файлов вне `files_modified`
</verification>

<success_criteria>
- Модуль `lightsCanvas.ts` экспортирует контракт из `<interfaces>` плана 15-02: константы, `breath`, `haloRadius`, `haloAlpha`, `bucketOf`, `splitBuckets`, `cubicBezier`, `ringEase`, `ringState`, `frameDue`, `shouldAnimate`, `clampDpr`, `summarize`, `isOnCanvas`, `colorWithAlpha`, `makeSprites`, `drawFrame` и типы `LightPoint`, `PointTransform`, `MotionFlags`, `Ring`, `FrameInput`, `LightSprites`
- Числа совпадают со спецификацией: 2600 мс, 5 корзин, 7–12px, .30–.60, спрайт 12px/.9, ядро 2,2px/.9px/.5, кольцо 900 мс 6→20,4 .5→0 по (.16, 1, .3, 1), reduce 9px/.22, порог 33 мс, dpr ≤ 2
- Тесты не используют настоящий canvas: мок-контекст и фабрика
</success_criteria>

<output>
Create `.planning/phases/15-lights-canvas/15-01-SUMMARY.md` when done (перечислить экспорты модуля и число тестов; отклонений от спецификации быть не должно, если есть, назвать число и причину)
</output>
