---
phase: 14-hero-video-particles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/heroParticles.ts
  - src/components/hero/heroParticles.test.ts
  - src/components/hero/HeroParticles.tsx
  - src/components/hero/HeroParticles.test.tsx
autonomous: true
requirements: [GLOBE-04, GLOBE-05, GLOBE-07]
tags: [hero, canvas, particles, motion, vitest]

must_haves:
  truths:
    - "Компонент HeroParticles рендерит <canvas class=\"hero__particles\" data-anim=\"stars\" aria-hidden=\"true\"> и рисует порт docs/research/v1.2/orig-hero-motion.js один к одному: шаг 30 fps (1000/30), elapsed ≤ 40 мс, dpr ≤ 1,75, статичное поле на offscreen-canvas с seededRandom(270927) и dpr ≤ 1,25, живые частицы, три туманности, падающие звёзды каждые 4,2–9,2 с, globalCompositeOperation \"screen\", лучи при flare > .34"
    - "Счёт звёзд min(max, max(140, round(w·h/3600))) при max 220 (<768) / 340 / 520 (≥1280); счёт частиц min(max, max(48, round(w·h/12000))) при max 70 / 100 / 140; доля «справа» 0,62 у статичного поля и 0,58 у частиц только на десктопе"
    - "Палитра литералами и только ей: light 248 247 251, signal 227 175 210, unity 184 192 230, horizon 170 217 220; никаких getComputedStyle и CSS-переменных"
    - "Вне экрана (IntersectionObserver на секции с rootMargin \"100px\"), в скрытой вкладке (visibilitychange) и при prefers-reduced-motion цикл остановлен и нарисован один статичный кадр: туманности на фазе, без падающих звёзд; ResizeObserver на canvas переразмечает поле"
    - "В jsdom без 2d-контекста компонент не падает и не вызывает requestAnimationFrame; heroParticles.test.ts и HeroParticles.test.tsx зелёные, motionPolicy.test.ts проходит без правок"
  artifacts:
    - path: "src/components/hero/heroParticles.ts"
      provides: "Чистый порт скрипта частиц: константы, seededRandom, starCount, particleCount, pickColor, shouldDrawFrame, frameElapsed, shouldAnimate, createScene, populateScene, drawScene"
      exports: ["BRAND_COLORS", "PALETTE", "FRAME_INTERVAL_MS", "MAX_ELAPSED_MS", "MAX_PIXEL_RATIO", "STATIC_MAX_PIXEL_RATIO", "STATIC_SEED", "VIEWPORT_ROOT_MARGIN", "seededRandom", "randomBetween", "starCount", "particleCount", "pickColor", "shouldDrawFrame", "frameElapsed", "shouldAnimate", "createParticle", "createNebulae", "createShootingStar", "createScene", "populateScene", "renderStaticField", "drawScene"]
      min_lines: 200
    - path: "src/components/hero/HeroParticles.tsx"
      provides: "Canvas-слой частиц hero с rAF-циклом на 30 fps и паузами по видимости, вкладке и reduce"
      exports: ["HeroParticles"]
      contains: "data-anim=\"stars\""
      min_lines: 90
    - path: "src/components/hero/heroParticles.test.ts"
      provides: "Тесты чистых функций: счёт по ширине, воспроизводимость seededRandom, цвет из палитры, пропуск кадра раньше 33 мс, статичный кадр без падающих звёзд, лучи при flare"
      min_lines: 90
    - path: "src/components/hero/HeroParticles.test.tsx"
      provides: "Тесты компонента: без 2d-контекста нет rAF, при reduce один кадр, цикл 30 fps и снятие при размонтировании, остановка при скрытой вкладке, наблюдение секции с rootMargin 100px"
      min_lines: 70
  key_links:
    - from: "src/components/hero/HeroParticles.tsx"
      to: "src/components/hero/heroParticles.ts"
      via: "импорт createScene/populateScene/drawScene/shouldDrawFrame/frameElapsed/shouldAnimate"
      pattern: "from \"\\./heroParticles\""
    - from: "src/components/hero/HeroParticles.tsx"
      to: "src/lib/useReducedMotion.ts"
      via: "window.matchMedia(REDUCED_MOTION_QUERY) и слушатель change"
      pattern: "REDUCED_MOTION_QUERY"
    - from: "src/components/hero/HeroParticles.tsx"
      to: "IntersectionObserver на <section class=\"hero\">"
      via: "canvas.closest(\"section\") с { rootMargin: VIEWPORT_ROOT_MARGIN }"
      pattern: "rootMargin: VIEWPORT_ROOT_MARGIN"
    - from: "src/components/hero/heroParticles.ts"
      to: "docs/research/v1.2/orig-hero-motion.js"
      via: "те же формулы, константы и порядок отрисовки; палитра литералами"
      pattern: "270927"
---

<objective>
Портировать скрипт частиц оригинала `docs/research/v1.2/orig-hero-motion.js` в пару модулей `heroParticles.ts` (чистые функции и сцена, без обращения к DOM на уровне модуля) и `HeroParticles.tsx` (canvas, наблюдатели, rAF-цикл на 30 fps), покрыть их тестами. Компонент в `Hero.tsx` в этом плане не подключается: это делает план 14-02 вместе с видео и удалением `Starfield`/`GlobeCanvas`.

Purpose: замер `docs/research/v1.2/measurements.md` показал 30 fps на мобильной эмуляции из-за canvas-глобуса на 2200 точек и dpr до 2; у оригинала глобус живёт в видео, а поверх него лёгкий canvas на 30 fps с паузами вне экрана. Пользователь просил «точно такой же» hero, поэтому порт один к одному, а не «по мотивам».
Output: `src/components/hero/heroParticles.ts`, `heroParticles.test.ts`, `HeroParticles.tsx`, `HeroParticles.test.tsx`; все тесты `src/components/hero` и `motionPolicy.test.ts` зелёные, `tsc -b` и lint чистые.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@docs/research/v1.2/orig-hero-motion.js
@docs/research/v1.2/measurements.md
@src/components/hero/GlobeCanvas.tsx
@src/components/hero/GlobeCanvas.test.tsx
@src/lib/useReducedMotion.ts
@src/test/setup.ts
@src/test/intersection.ts
@src/styles/motionPolicy.test.ts

<ownership>
Фаза 14 идёт в отдельном git worktree параллельно с фазами 15 (карта) и 16 (мобильная адаптация) от одного `main`. Оркестратор укажет путь worktree; все команды ниже относительные, без `cd`. Коммитов у исполнителя нет: оркестратор коммитит слиянием. Создавать и править ТОЛЬКО четыре файла из `files_modified`. `Hero.tsx`, `hero.css`, `Hero.test.tsx`, `GlobeCanvas*`, `globe*`, `Starfield.tsx` в этом плане не трогать (их правит и удаляет план 14-02). Запрещено трогать `src/styles/global.css`, `src/styles/motionPolicy.test.ts`, `src/test/setup.ts`, `src/App.tsx`, `src/components/map/*`, `src/components/layout/*`, `src/lib/useReducedMotion.ts`, `package.json`, `package-lock.json`, `vite.config.ts`. Новых npm-пакетов не ставить. Комментарии в коде на русском, идентификаторы на английском.
</ownership>

<interfaces>
<!-- Контракт нового модуля. План 14-02 и компонент HeroParticles.tsx собираются против этих сигнатур; менять имена нельзя. -->

Модуль `src/components/hero/heroParticles.ts` (создаётся в Task 1):
```ts
export type RGB = readonly [number, number, number];
export type Random = () => number;

/** Палитра оригинала литералами: light, signal, unity, horizon. */
export const BRAND_COLORS: { light: RGB; signal: RGB; unity: RGB; horizon: RGB };
/** [light, signal, unity, horizon] в этом порядке; pickColor берёт индекс floor(random() * 4). */
export const PALETTE: readonly RGB[];

export const FRAME_INTERVAL_MS: number;        // 1000 / 30
export const MAX_ELAPSED_MS: number;           // 40
export const MAX_PIXEL_RATIO: number;          // 1.75
export const STATIC_MAX_PIXEL_RATIO: number;   // 1.25
export const STATIC_SEED: number;              // 270927
export const TABLET_MIN_WIDTH: number;         // 768
export const DESKTOP_MIN_WIDTH: number;        // 1280
export const STAR_MAX: { phone: 220; tablet: 340; desktop: 520 };
export const PARTICLE_MAX: { phone: 70; tablet: 100; desktop: 140 };
export const STATIC_RIGHT_SHARE: number;       // 0.62
export const PARTICLE_RIGHT_SHARE: number;     // 0.58
export const SHOOTING_STAR_GAP_MS: readonly [number, number];        // [4200, 9200]
export const FIRST_SHOOTING_STAR_GAP_MS: readonly [number, number];  // [1600, 4800]
export const FLARE_RAY_THRESHOLD: number;      // 0.34
export const VIEWPORT_ROOT_MARGIN: string;     // "100px"

export function seededRandom(seed: number): Random;                // LCG 1664525 / 1013904223, >>> 0, / 4294967296
export function randomBetween(min: number, max: number, random?: Random): number;
export function isDesktop(width: number): boolean;                 // width >= 1280
export function starCount(width: number, height: number): number;
export function particleCount(width: number, height: number): number;
export function pickColor(random?: Random): RGB;
/** false, если предыдущий кадр был меньше FRAME_INTERVAL_MS назад; при lastTime === 0 всегда true. */
export function shouldDrawFrame(lastTime: number, time: number): boolean;
/** 0 при lastTime === 0, иначе min(time - lastTime, MAX_ELAPSED_MS). */
export function frameElapsed(lastTime: number, time: number): number;
export function shouldAnimate(flags: { visible: boolean; hidden: boolean; reduce: boolean }): boolean;

export type Particle = { x; y; radius; alpha; phase; twinkle; flare: boolean; flarePhase; flareSpeed; depthTravel: boolean; depthPhase; depthSpeed; driftX; driftY; color: RGB };
export type Nebula = { x; y; radius; phase; speed; color: RGB };
export type ShootingStar = { x; y; velocityX; velocityY; length; born; lifetime };
export type Scene = {
  width: number; height: number; pixelRatio: number;
  particles: Particle[]; nebulae: Nebula[]; shootingStars: ShootingStar[];
  nextShootingStar: number;
  staticCanvas: HTMLCanvasElement | null;
  staticContext: CanvasRenderingContext2D | null;
};

export function createParticle(width: number, height: number, preferRight: boolean, random?: Random): Particle;
export function createNebulae(): Nebula[];
export function createShootingStar(width: number, height: number, time: number, random?: Random): ShootingStar;
/** staticContext = staticCanvas?.getContext("2d", { alpha: true }) ?? null; без DOM-обращений сверх этого. */
export function createScene(staticCanvas: HTMLCanvasElement | null): Scene;
/** Пишет размеры и dpr в сцену, создаёт частицы и туманности, вызывает renderStaticField. */
export function populateScene(scene: Scene, width: number, height: number, pixelRatio: number): void;
export function renderStaticField(scene: Scene): void;
/** staticFrame = true: туманности на фазе без дрейфа, падающие звёзды сброшены (reduce, вне экрана, скрытая вкладка). */
export function drawScene(ctx: CanvasRenderingContext2D, scene: Scene, time: number, elapsed: number, staticFrame: boolean, random?: Random): void;
```

Компонент `src/components/hero/HeroParticles.tsx` (создаётся в Task 2):
```tsx
export function HeroParticles(): JSX.Element
// <canvas ref className="hero__particles" data-anim="stars" aria-hidden="true" />
// План 14-02 вставит <HeroParticles /> в <section class="hero"> между .hero__video и .hero__content
// и добавит правило .hero__particles в hero.css. В этом плане CSS не пишется.
```

Из `src/lib/useReducedMotion.ts` (не править):
```ts
export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
export function prefersReducedMotion(): boolean;
export function usePrefersReducedMotion(): boolean;
```

Что мокает `src/test/setup.ts` (не править): `IntersectionObserver` → `IntersectionObserverMock` (конструктор принимает только callback, событие шлёт `enterViewport()` из `src/test/intersection.ts`), `ResizeObserver` → пустой мок, `window.matchMedia` → `vi.fn` с `matches: false` и пустыми `addEventListener`/`removeEventListener`, `HTMLCanvasElement.prototype.getContext` → всегда `null`. Тесты с контекстом подменяют его через `vi.spyOn(HTMLCanvasElement.prototype, "getContext").mockReturnValue(ctx)` (образец в `GlobeCanvas.test.tsx`: `mockContext`, `mockCanvasSize`, `mockReducedMotion`, `afterEach(vi.restoreAllMocks)`).

Соответствие исходнику `orig-hero-motion.js` → модуль:
`seededRandom` → `seededRandom`; `renderStaticField` → `renderStaticField(scene)`; `createParticle(preferRight)` → `createParticle(width, height, preferRight, random)`; `populate` → `populateScene` (частицы, `createNebulae`, статичное поле); `drawNebula` → внутренняя `drawNebulae(ctx, scene, time, staticFrame)`; `createShootingStar`/`drawShootingStars` → одноимённые с явными `width`/`height`/`scene`; `draw(time, elapsed)` → `drawScene`; `animate`/`updateAnimation`/`resize` и наблюдатели → `HeroParticles.tsx`. `resolveBrandColors` не портируется: палитра задана литералами (спека GLOBE-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистый модуль heroParticles.ts и тесты heroParticles.test.ts</name>
  <files>src/components/hero/heroParticles.ts, src/components/hero/heroParticles.test.ts</files>
  <read_first>
    - docs/research/v1.2/orig-hero-motion.js целиком (источник порта: формулы, константы, порядок вызовов)
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, раздел 1, пункты GLOBE-04, GLOBE-05, GLOBE-07
    - src/components/hero/globe.test.ts (образец мок-контекста `createMockContext` и стиля тестов чистого модуля; сам файл не править, план 14-02 его удалит)
    - блок `<interfaces>` этого плана (контракт экспортов)
  </read_first>
  <behavior>
    - seededRandom(270927) дважды даёт одинаковые первые пять значений, каждое в [0, 1); seededRandom(1) и seededRandom(2) дают разные первые значения
    - starCount: (390, 844) → 140; (1024, 700) → 199; (1440, 600) → 240; (1920, 1080) → 520
    - particleCount: (390, 844) → 48; (1024, 700) → 60; (1440, 600) → 72; (1920, 1080) → 140
    - PALETTE ровно из четырёх литералов [248,247,251], [227,175,210], [184,192,230], [170,217,220]; pickColor(() => 0) → light, pickColor(() => 0.99) → horizon; pickColor() без аргумента возвращает элемент PALETTE
    - shouldDrawFrame(0, 16) → true; shouldDrawFrame(1000, 1030) → false; shouldDrawFrame(1000, 1034) → true; frameElapsed(0, 500) → 0; frameElapsed(1000, 1020) → 20; frameElapsed(1000, 1500) → 40
    - shouldAnimate: только { visible: true, hidden: false, reduce: false } → true, три остальных однофлаговых варианта → false
    - createParticle(1440, 600, true, seededRandom(7)): x в [0.44·1440, 0.98·1440], y в [0.30·600, 0.92·600], radius в [0.45, 1.65], alpha в [0.18, 0.74], color входит в PALETTE; createParticle(390, 844, false, …): x в [0, 390], y в [0, 844]
    - createNebulae() → три облака с color signal, unity, horizon и x/y/radius 0.25/0.46/0.62, 0.66/0.31/0.54, 0.82/0.68/0.48
    - populateScene(scene с фейковым staticCanvas, 1440, 600, 1.75): staticCanvas.width === 1800 и height === 750 (dpr обрезан до 1.25), staticContext.arc вызван starCount(1440, 600) = 240 раз, scene.particles.length === 72, scene.nebulae.length === 3
    - drawScene(ctx, scene, 1000, 0, false) после populateScene(scene с staticCanvas null, 1440, 600, 1): clearRect(0, 0, 1440, 600) один раз, createRadialGradient вызван не меньше 3 раз (туманности), drawImage не вызван (нет статичного контекста), при каждом fill значение ctx.globalCompositeOperation равно "screen", после кадра вызваны save и restore по разу
    - drawScene(ctx, scene, 1000, 16, true) при scene.shootingStars из одной звезды и scene.nextShootingStar = 0: после вызова scene.shootingStars пуст и createLinearGradient не вызван (статичный кадр без падающих звёзд); тот же вызов с staticFrame false и nextShootingStar = 0 создаёт звезду (length 1) и createLinearGradient вызван один раз, nextShootingStar в [1000 + 4200, 1000 + 9200]
    - drawScene с одной частицей { flare: true, flarePhase: Math.PI / 2, flareSpeed: 0, radius: 1, depthTravel: false, … } рисует лучи: moveTo вызван ≥ 2 раз и lineTo ≥ 2 раз, stroke ≥ 1; с flare: false — moveTo не вызван
  </behavior>
  <action>
    Реализует GLOBE-04 (порт один к одному) и чистую часть GLOBE-05/GLOBE-07.

    1. Создать `src/components/hero/heroParticles.ts` строго по контракту `<interfaces>`. Заголовочный комментарий: чистый порт `docs/research/v1.2/orig-hero-motion.js`, DOM на уровне модуля не трогается, всё состояние в объекте `Scene`, чтобы тесты и компонент делили один код. Палитра литералами `BRAND_COLORS` (light `[248, 247, 251]`, signal `[227, 175, 210]`, unity `[184, 192, 230]`, horizon `[170, 217, 220]`), `PALETTE` в порядке light, signal, unity, horizon; функцию `resolveBrandColors` оригинала не переносить (спека GLOBE-04: палитра литералами, как и в остальных CSS hero, см. комментарий в начале `hero.css`).

    2. Перенести формулы без изменений чисел: `seededRandom` (LCG `value * 1664525 + 1013904223` с `>>> 0`, деление на `4294967296`), `randomBetween(min, max, random = Math.random)`, `starCount` (`min(max, max(140, round(w·h/3600)))`, max 220 при `width < 768`, 520 при `width >= 1280`, иначе 340), `particleCount` (`min(max, max(48, round(w·h/12000)))`, max 70 / 140 / 100 по тем же порогам), `createParticle` (диапазоны `preferRight` 0.44–0.98 по x и 0.30–0.92 по y, radius 0.45–1.65, alpha 0.18–0.74, twinkle 0.00045–0.00125, flare с вероятностью 0.14, flareSpeed 0.00016–0.00034, depthTravel 0.34, depthSpeed 0.00008–0.00020, driftX −0.0025…0.005, driftY −0.009…−0.0025; все `Math.random()` оригинала заменить на параметр `random`, по умолчанию `Math.random`), `createNebulae` (три литерала оригинала с цветами signal, unity, horizon), `createShootingStar` (angle 0.42–0.68, speed 0.42–0.62, x от −0.08·w до 0.72·w, y от 0.04·h до 0.38·h, length 90–170, lifetime 680–980), `renderStaticField` (ratio `min(pixelRatio, 1.25)`, `seededRandom(STATIC_SEED)`, доля «справа» `index >= count * 0.62` только на десктопе, радиус 0.28–1.0, alpha 0.16–0.54, `setTransform(ratio, …)`, `clearRect`, `arc` + `fill` на каждую звезду), `drawNebulae` (стопы 0.085 / 0.050 / 0.018 / 0, дрейф `phase + time * speed`, при `staticFrame` только `phase`, амплитуды 0.055 по x и 0.045 по y, радиус `max(w, h) * radius`), `drawShootingStars` (при `staticFrame` очистить `scene.shootingStars` и выйти; спавн при `time >= scene.nextShootingStar` с новым `nextShootingStar = time + randomBetween(4200, 9200)`; стопы горизонта 0, unity `opacity * 0.38` на 0.72, light `opacity` на 1; `lineWidth 1.35`), тело `draw` → `drawScene` (`clearRect`, `save`, `globalCompositeOperation = "screen"`, туманности, `drawImage(staticCanvas, 0, 0, staticCanvas.width, staticCanvas.height, 0, 0, width, height)` только при `staticContext`, частицы с дрейфом при `elapsed`, обёртка по краям ±4, pulse `0.64 + sin(…) * 0.36`, flare `pow(max(0, sin(…)), 14)`, depth `0.5 + sin(…) * 0.5`, `drawRadius = radius * (1 + (1 - depth) * 3.2)`, alpha `min(1, (alpha * pulse + flare * 0.86) * (0.28 + depth * 0.72))`, ореол радиальным градиентом `drawRadius * 5` при `radius > 1.15 || depth < 0.72`, иначе круг; лучи при `flare > FLARE_RAY_THRESHOLD`: `ray = radius * (4 + flare * 5)`, `lineWidth 0.7`, alpha `flare * 0.58`, два отрезка через `moveTo`/`lineTo`; затем `drawShootingStars`, `restore`).

    3. Логика кадра вынесена в две чистые функции для компонента и тестов: `shouldDrawFrame(lastTime, time)` (false, если `lastTime` ненулевой и `time - lastTime < FRAME_INTERVAL_MS`), `frameElapsed(lastTime, time)` (0 при нулевом `lastTime`, иначе `min(time - lastTime, MAX_ELAPSED_MS)`), плюс `shouldAnimate({ visible, hidden, reduce })` = `visible && !hidden && !reduce`. Ноль как «цикл только что запущен» повторяет оригинал (`lastTime = 0`).

    4. `createScene(staticCanvas)` берёт `staticCanvas?.getContext("2d", { alpha: true }) ?? null` и заводит пустые массивы, `nextShootingStar: 0`; `populateScene(scene, width, height, pixelRatio)` пишет размеры, `particles = Array.from({ length: particleCount }, (_, i) => createParticle(w, h, desktop && i >= count * 0.58))`, `nebulae = createNebulae()`, затем `renderStaticField(scene)`. Типы `Particle`, `Nebula`, `ShootingStar`, `Scene`, `RGB`, `Random` экспортировать. Строгий TypeScript (`noUnusedLocals`, `verbatimModuleSyntax`): типы импортировать через `import type`.

    5. Создать `src/components/hero/heroParticles.test.ts` с кейсами из `<behavior>`. Мок-контекст по образцу `createMockContext` из `globe.test.ts`, расширенный методами `save`, `restore`, `drawImage`, `fillRect`, `moveTo`, `lineTo`, `createLinearGradient` (возвращает `{ addColorStop: vi.fn() }`) и полями `globalCompositeOperation`, `globalAlpha`, `fillStyle`, `strokeStyle`, `lineWidth`. Проверку «screen при каждом fill» делать через `ctx.fill.mockImplementation(() => composites.push(ctx.globalCompositeOperation))`, как в `globe.test.ts`. Фейковый статичный canvas для `populateScene`: объект `{ width: 0, height: 0, getContext: () => staticCtx }`, приведённый к `HTMLCanvasElement` через `unknown`. Для теста лучей собрать сцену вручную: `populateScene(scene, 200, 200, 1)` и затем заменить `scene.particles` на одну частицу с заданными полями (color из PALETTE, x 100, y 100, alpha 0.5, phase 0, twinkle 0, depthPhase 0, depthSpeed 0, driftX 0, driftY 0).
  </action>
  <acceptance_criteria>
    - `src/components/hero/heroParticles.ts` существует, экспортирует все имена из блока `<interfaces>`; в файле нет `document.`, `window.`, `getComputedStyle`, `requestAnimationFrame` (grep по файлу пуст, обращение к canvas только через параметры и `scene.staticCanvas`)
    - В файле есть литералы `270927`, `1664525`, `1013904223`, `4294967296`, `1000 / 30`, `1.75`, `1.25`, `3600`, `12000`, `0.62`, `0.58`, `4200`, `9200`, `0.34`, `"screen"`, `"100px"`, и четыре тройки палитры `248, 247, 251`, `227, 175, 210`, `184, 192, 230`, `170, 217, 220`
    - В файле нет `Math.random()` вне значений по умолчанию параметров `random` (grep `Math.random()` даёт только строки с `random: Random = Math.random` или `= Math.random`)
    - `src/components/hero/heroParticles.test.ts` содержит не меньше десяти `it(` и покрывает каждую строку `<behavior>`
    - `npx vitest run src/components/hero/heroParticles.test.ts` зелёный; `npx tsc -b` без ошибок
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero/heroParticles.test.ts && npx tsc -b</automated>
  </verify>
  <done>Чистый модуль порта с теми же числами, что в `orig-hero-motion.js`, палитрой литералами и состоянием в `Scene`; тесты счёта, воспроизводимости, цвета, шага 30 fps, статичного кадра и лучей зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Компонент HeroParticles.tsx с rAF-циклом и наблюдателями, тесты HeroParticles.test.tsx</name>
  <files>src/components/hero/HeroParticles.tsx, src/components/hero/HeroParticles.test.tsx</files>
  <read_first>
    - docs/research/v1.2/orig-hero-motion.js, функции `resize`, `animate`, `updateAnimation` и блок подписок в конце `initHeroParticles`
    - src/components/hero/GlobeCanvas.tsx (образец useEffect с ResizeObserver, IntersectionObserver, visibilitychange, matchMedia и cleanup; стрелки вместо function ради сужения типов `canvas`/`ctx`)
    - src/components/hero/GlobeCanvas.test.tsx (хелперы `mockContext`, `mockCanvasSize`, `mockReducedMotion`, сбор колбэков rAF в массив, `afterEach(vi.restoreAllMocks)`)
    - src/lib/useReducedMotion.ts (`REDUCED_MOTION_QUERY`)
    - src/test/setup.ts и src/test/intersection.ts (что уже замокано и как устроен `IntersectionObserverMock`)
    - src/components/hero/heroParticles.ts (создан в Task 1)
  </read_first>
  <behavior>
    - render(<HeroParticles />) в jsdom (getContext → null) не бросает; в DOM есть `canvas.hero__particles[data-anim="stars"][aria-hidden="true"]`; `window.requestAnimationFrame` не вызван
    - С мок-контекстом, размером 1200×600 и reduce = true: `requestAnimationFrame` не вызван, `ctx.clearRect` вызван (один статичный кадр), `ctx.createRadialGradient` вызван не меньше 3 раз
    - С мок-контекстом и reduce = false: `requestAnimationFrame` вызван после монтирования; вызов первого колбэка с time 1000 рисует кадр (число `clearRect` растёт), колбэк с 1010 кадр пропускает (число `clearRect` не меняется, rAF запрошен снова), колбэк с 1040 рисует; `unmount()` вызывает `cancelAnimationFrame` с последним выданным id
    - С мок-контекстом и reduce = false: после `Object.defineProperty(document, "hidden", { value: true, configurable: true })` и `document.dispatchEvent(new Event("visibilitychange"))` вызван `cancelAnimationFrame`, нарисован статичный кадр (`clearRect` вырос на один), новый rAF не запрошен; после возврата `hidden` в false и повторного события rAF запрошен снова
    - IntersectionObserver создаётся с `{ rootMargin: "100px" }` и наблюдает ближайший `section` (в тесте canvas обёрнут в `<section>`), при отсутствии секции наблюдает сам canvas
  </behavior>
  <action>
    Реализует GLOBE-04 (canvas с `data-anim="stars"`) и GLOBE-05 (паузы), а также часть GLOBE-07 (тест «без 2d-контекста не падает и не вызывает rAF»).

    1. Создать `src/components/hero/HeroParticles.tsx`: именованный экспорт `HeroParticles`, только он (правило `react-refresh/only-export-components`); импорты `useEffect`, `useRef` из `react`, `REDUCED_MOTION_QUERY` из `../../lib/useReducedMotion`, функции и константы из `./heroParticles` (`createScene`, `populateScene`, `drawScene`, `shouldDrawFrame`, `frameElapsed`, `shouldAnimate`, `randomBetween`, `MAX_PIXEL_RATIO`, `FIRST_SHOOTING_STAR_GAP_MS`, `VIEWPORT_ROOT_MARGIN`). Рендер: `<canvas ref={canvasRef} className="hero__particles" data-anim="stars" aria-hidden="true" />`. Комментарий над JSX: значение `stars` из закрытого реестра `motionPolicy.test.ts` переезжает со слоёв `.starfield__layer` на этот canvas; цикл живёт в rAF, а не в CSS, поэтому атрибут объявляет слой декоративным, а остановку при reduce делает сам компонент.

    2. В `useEffect(() => { … }, [])`: взять `canvas`, `ctx = canvas.getContext("2d", { alpha: true })`; при `null` выйти до создания сцены и подписок (jsdom и старые браузеры: ни rAF, ни наблюдателей, hero остаётся с видео и фоном). Далее `scene = createScene(document.createElement("canvas"))`, `motionQuery = window.matchMedia(REDUCED_MOTION_QUERY)`, локальное состояние `visible = true`, `hidden = document.hidden`, `reduce = motionQuery.matches`, `frameId = 0`, `lastTime = 0`. Стрелочные функции (как в `GlobeCanvas.tsx`, ради сужения `canvas`/`ctx`):
       - `running = () => shouldAnimate({ visible, hidden, reduce })`.
       - `resize`: `bounds = canvas.getBoundingClientRect()`, `nextWidth = max(1, round(bounds.width))`, `nextHeight` аналогично, `nextRatio = min(window.devicePixelRatio || 1, MAX_PIXEL_RATIO)`; если всё совпало со `scene` — выход; иначе `canvas.width/height = round(size * ratio)`, `ctx.setTransform(ratio, 0, 0, ratio, 0, 0)`, `populateScene(scene, nextWidth, nextHeight, nextRatio)`, при `scene.nextShootingStar === 0` поставить `performance.now() + randomBetween(FIRST_SHOOTING_STAR_GAP_MS[0], FIRST_SHOOTING_STAR_GAP_MS[1])`, затем `drawScene(ctx, scene, performance.now(), 0, !running())`.
       - `animate(time)`: `frameId = 0`; если `!shouldDrawFrame(lastTime, time)` — `frameId = requestAnimationFrame(animate)` и выход; иначе `elapsed = frameElapsed(lastTime, time)`, `lastTime = time`, `drawScene(ctx, scene, time, elapsed, false)`, и при `running()` снова `frameId = requestAnimationFrame(animate)`.
       - `updateAnimation`: снять текущий `frameId` через `cancelAnimationFrame`, обнулить `lastTime` (пауза рвёт отсчёт, иначе первый кадр после возврата прыгнет на всю паузу); при `running()` запросить `animate`, иначе `drawScene(ctx, scene, performance.now(), 0, true)` (статичный кадр: туманности на фазе, падающие звёзды сброшены).
       - `handleVisibility`: `hidden = document.hidden; updateAnimation()`; `handleMotionChange(event)`: `reduce = event.matches; updateAnimation()`.
       Подписки: `ResizeObserver` на `canvas` (при отсутствии — `window.addEventListener("resize", resize, { passive: true })`); `IntersectionObserver` на `canvas.closest("section") ?? canvas` с `{ rootMargin: VIEWPORT_ROOT_MARGIN }`, колбэк берёт `entries[entries.length - 1].isIntersecting` в `visible` и зовёт `updateAnimation` (при отсутствии `IntersectionObserver` считать `visible = true`); `document.addEventListener("visibilitychange", handleVisibility)`; `motionQuery.addEventListener?.("change", handleMotionChange)` (в jsdom-моке метод есть, но в старых Safari его нет — вызов необязательный, как в `useReducedMotion.ts`). Порядок запуска: `resize()`, затем `updateAnimation()`. Cleanup снимает rAF, оба наблюдателя, оба слушателя и resize.

    3. Комментарии в коде на русском объясняют: зачем 30 fps и потолок 40 мс (оригинал), почему `visible` стартует с `true` (первый кадр до ответа наблюдателя, как у оригинала), почему секция, а не canvas, под `IntersectionObserver` (спека GLOBE-05, `rootMargin 100px` даёт запас на прокрутке).

    4. Создать `src/components/hero/HeroParticles.test.tsx` с кейсами из `<behavior>`. Хелперы `mockContext` (с `save`, `restore`, `drawImage`, `fillRect`, `moveTo`, `lineTo`, `createLinearGradient`, `createRadialGradient`, `setTransform`, `clearRect`, `beginPath`, `arc`, `fill`, `stroke` и полями стиля), `mockCanvasSize`, `mockReducedMotion` перенести из `GlobeCanvas.test.tsx` (файл уходит в плане 14-02, поэтому копировать, а не импортировать). rAF мокать сбором колбэков в массив с возвратом растущего id; `cancelAnimationFrame` — `vi.spyOn(...).mockImplementation(() => {})`. Для проверки `rootMargin` временно заменить `globalThis.IntersectionObserver` подклассом `IntersectionObserverMock` из `src/test/intersection.ts`, который складывает `options` и `target` в массивы, и вернуть исходный класс в `finally` (не `vi.unstubAllGlobals`: он снял бы и мок из `setup.ts`). Для `document.hidden` использовать `Object.defineProperty(document, "hidden", { value: true, configurable: true })` и вернуть `false` в конце теста. `afterEach(() => vi.restoreAllMocks())`.
  </action>
  <acceptance_criteria>
    - `src/components/hero/HeroParticles.tsx` содержит `export function HeroParticles`, `className="hero__particles"`, `data-anim="stars"`, `aria-hidden="true"`, `getContext("2d", { alpha: true })`, `rootMargin: VIEWPORT_ROOT_MARGIN`, `closest("section")`, `"visibilitychange"`, `REDUCED_MOTION_QUERY`, `ResizeObserver`, `cancelAnimationFrame`; импортов из `./globe`, `./GlobeCanvas`, `./Starfield` нет
    - В `HeroParticles.tsx` нет `useState` и нет чтения `canvasRef.current` вне эффекта (правила `react-hooks/refs` и `set-state-in-effect` в eslint-plugin-react-hooks 7)
    - `src/components/hero/HeroParticles.test.tsx` содержит не меньше пяти `it(`, среди них тест с `expect(raf).not.toHaveBeenCalled()` без мок-контекста
    - `npx vitest run src/components/hero src/styles/motionPolicy.test.ts` зелёный (в реестре `used` появляется `stars` из нового файла, старые значения на месте)
    - `npx tsc -b` и `npm run lint` без ошибок и предупреждений
    - `git status --porcelain` показывает только четыре файла из `files_modified` (все со статусом `??`)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero src/styles/motionPolicy.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>`HeroParticles` рендерит canvas `data-anim="stars"`, ведёт цикл на 30 fps через чистые функции модуля, останавливается вне экрана, в скрытой вкладке и при reduce с одним статичным кадром, в jsdom не падает и не зовёт rAF; все тесты hero и политика движения зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл GitHub Pages | входа данных нет, модуль рисует по локальным константам и `Math.random` |
| canvas-цикл → CPU/GPU устройства посетителя | единственный ресурс, который фаза может исчерпать |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of service | rAF-цикл `HeroParticles.tsx` | mitigate | шаг 30 fps (`shouldDrawFrame`), `elapsed` ≤ 40 мс, dpr ≤ 1,75, остановка по `IntersectionObserver` (`rootMargin: "100px"`), `visibilitychange` и reduce; тесты на пропуск кадра и снятие rAF |
| T-14-02 | Denial of service | offscreen-canvas статичного поля | mitigate | dpr ≤ 1,25 и потолок 520 звёзд; перерисовка только при смене размера или dpr (`resize` сравнивает со `scene`) |
| T-14-03 | Tampering | палитра и константы | accept | литералы в модуле, `getComputedStyle` не читается; тест проверяет четыре тройки палитры |
| T-14-SC | Tampering | npm-установки | mitigate | новых пакетов в фазе нет; `package.json` и `package-lock.json` неизменны (проверка `git status`); любой `npm install` в этом плане — стоп и вопрос оркестратору |
</threat_model>

<verification>
- `npx vitest run src/components/hero src/styles/motionPolicy.test.ts` зелёный: новые `heroParticles.test.ts` и `HeroParticles.test.tsx` плюс старые тесты hero (они ещё живы до плана 14-02)
- `npx tsc -b` и `npm run lint` чистые
- `grep -c "270927" src/components/hero/heroParticles.ts` ≥ 1; `grep -E "document\.|window\.|getComputedStyle|requestAnimationFrame" src/components/hero/heroParticles.ts | grep -v '^\s*//\|^\s*\*' | wc -l` даёт 0
- `git status --porcelain` показывает ровно четыре новых файла
</verification>

<success_criteria>
- `heroParticles.ts` повторяет `orig-hero-motion.js` числом в число: 30 fps, 40 мс, dpr 1,75 и 1,25, seed 270927, счёт 3600/12000 с потолками 220/340/520 и 70/100/140, доли 0,62 и 0,58, три туманности, падающие звёзды 4,2–9,2 с, `screen`, лучи при flare > .34; палитра литералами
- `HeroParticles.tsx` рендерит `canvas.hero__particles[data-anim="stars"]`, крутит цикл только `visible && !hidden && !reduce`, иначе рисует один статичный кадр; без 2d-контекста не падает и не зовёт rAF
- Тесты обоих модулей зелёные, `motionPolicy.test.ts` без правок проходит, `tsc -b` и lint чистые
</success_criteria>

<output>
Create `.planning/phases/14-hero-video-particles/14-01-SUMMARY.md` when done (с перечнем экспортов `heroParticles.ts`, списком тестов и выводом команд проверки)
</output>
