---
phase: 14-hero-video-particles
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/components/hero/Hero.tsx
  - src/components/hero/hero.css
  - src/components/hero/Hero.test.tsx
  - src/components/hero/GlobeCanvas.tsx
  - src/components/hero/GlobeCanvas.test.tsx
  - src/components/hero/globe.ts
  - src/components/hero/globe.test.ts
  - src/components/hero/Starfield.tsx
  - scripts/check-dist.mjs
autonomous: true
requirements: [GLOBE-01, GLOBE-02, GLOBE-03, GLOBE-05, GLOBE-06, GLOBE-07, GLOBE-08]
tags: [hero, video, css, vitest, check-dist]

must_haves:
  truths:
    - "На первом экране <video data-anim=\"globe\" aria-hidden=\"true\" tabindex=\"-1\"> с autoplay, loop, playsinline, preload=\"auto\", disablepictureinpicture, disableremoteplayback и двумя источниками `${import.meta.env.BASE_URL}hero-globe.webm` (video/webm) и `hero-globe.mp4` (video/mp4); свойство muted продублировано через ref до play(); видео воспроизводится (!paused, currentTime растёт)"
    - "Стили видео по трём брейкпоинтам: до 640px object-fit: cover; object-position: center center; filter: saturate(1.18) contrast(1.28) brightness(0.96); от 640px object-position: 72% center; transform-origin: 72% 46%; от 1280px width: auto; max-width: min(100%, 1920px); aspect-ratio: 16 / 9; margin-left: auto; object-fit: contain; object-position: right top; mix-blend-mode: screen и двойная mask-image с mask-composite: intersect плюс -webkit-варианты; обёртка .hero__video absolute inset 0, z 0, flex-колонка align-items: flex-end"
    - "Высота hero как у оригинала: min-height: 100svh, от 768px max(600px, 65svh), от 1024px max(600px, 64vh); .hero__content, кегли, градиент H1 и .hero::after не изменились"
    - "При prefers-reduced-motion видео стоит на первом кадре (pause() после loadeddata, currentTime 0), при снятии reduce снова play(); при navigator.connection.saveData === true у <video> нет <source>, посетитель видит фон #070210 и частицы"
    - "В src/ нет GlobeCanvas.tsx, GlobeCanvas.test.tsx, globe.ts, globe.test.ts, Starfield.tsx, правил .starfield__*, .globe-canvas и keyframes star-drift*; <HeroParticles /> стоит между .hero__video и .hero__content; motionPolicy.test.ts проходит без правок"
    - "Hero.test.tsx проверяет видео, источники под BASE_URL, атрибуты, muted через ref, saveData, паузу при reduce и текст hero.css; node scripts/check-dist.mjs после npm run build подтверждает hero-globe.webm и hero-globe.mp4 в dist/ и ссылки на них в JS-бандле"
  artifacts:
    - path: "src/components/hero/Hero.tsx"
      provides: "Обёртка .hero__video с <video> и двумя <source>, ref для muted/pause/play, ветка saveData, <HeroParticles />"
      exports: ["Hero"]
      contains: "<HeroParticles />"
      min_lines: 110
    - path: "src/components/hero/hero.css"
      provides: "Высота секции по трём брейкпоинтам, .hero__video, .hero__video > video на трёх брейкпоинтах с масками, .hero__particles; без .starfield__*, .globe-canvas и star-drift*"
      contains: "-webkit-mask-composite: source-in"
      min_lines: 150
    - path: "src/components/hero/Hero.test.tsx"
      provides: "Тесты видео, источников, атрибутов, muted, saveData, reduce-паузы, canvas частиц и текста hero.css"
      min_lines: 150
    - path: "scripts/check-dist.mjs"
      provides: "Проверка hero-globe.webm и hero-globe.mp4 в dist/ (размер ≥ 1 МБ) и ссылок на них в JS-бандле"
      contains: "hero-globe.webm"
    - path: "public/hero-globe.webm"
      provides: "Видео оригинала, webm 1 914 635 Б, не меняется"
    - path: "public/hero-globe.mp4"
      provides: "Видео оригинала, mp4 2 856 265 Б, не меняется"
  key_links:
    - from: "src/components/hero/Hero.tsx"
      to: "src/components/hero/HeroParticles.tsx"
      via: "<HeroParticles /> вместо <Starfield /> и <GlobeCanvas />"
      pattern: "<HeroParticles />"
    - from: "src/components/hero/Hero.tsx"
      to: "public/hero-globe.webm, public/hero-globe.mp4"
      via: "<source src={`${import.meta.env.BASE_URL}hero-globe.webm`}> и mp4"
      pattern: "import\\.meta\\.env\\.BASE_URL\\}hero-globe\\.(webm|mp4)"
    - from: "src/components/hero/Hero.tsx"
      to: "src/lib/useReducedMotion.ts"
      via: "usePrefersReducedMotion() управляет pause()/play() видео"
      pattern: "usePrefersReducedMotion\\(\\)"
    - from: "src/components/hero/hero.css"
      to: "<video> внутри .hero__video"
      via: "селектор .hero__video > video на трёх брейкпоинтах"
      pattern: "\\.hero__video > video"
    - from: "scripts/check-dist.mjs"
      to: "dist/hero-globe.webm, dist/hero-globe.mp4, dist/assets/*.js"
      via: "checkHeroVideo: existsSync + statSync + поиск имени в бандле"
      pattern: "checkHeroVideo"
---

<objective>
Заменить canvas-глобус и CSS-звёздное поле на видео оригинала со стилями трёх брейкпоинтов и высотой секции оригинала, подключить `<HeroParticles />` из плана 14-01, добавить экономию трафика (`saveData`) и паузу видео при reduce, переписать `Hero.test.tsx`, удалить `GlobeCanvas`/`globe`/`Starfield` с тестами и расширить `scripts/check-dist.mjs` проверкой видеофайлов в `dist/`.

Purpose: пользователь просил «точно такой же» глобус, как на onevoice27.org; у оригинала это видеофайл (GPU-декодер) с масками и `mix-blend-mode: screen`, а не 2200 точек на canvas. Замеры `measurements.md`: hero на 390×844 при CPU×4 даёт 30 fps против 117 у оригинала.
Output: обновлённые `Hero.tsx`, `hero.css`, `Hero.test.tsx`, `scripts/check-dist.mjs`; удалённые `GlobeCanvas.tsx`, `GlobeCanvas.test.tsx`, `globe.ts`, `globe.test.ts`, `Starfield.tsx`; зелёный полный гейт `npx tsc -b && npm test && npm run lint && npm run build && node scripts/check-dist.mjs`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-hero-video-particles/14-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@docs/research/v1.2/measurements.md
@src/components/hero/Hero.tsx
@src/components/hero/hero.css
@src/components/hero/Hero.test.tsx
@src/components/hero/HeroParticles.tsx
@src/lib/useReducedMotion.ts
@src/test/setup.ts
@src/styles/motionPolicy.test.ts
@scripts/check-dist.mjs

<ownership>
Фаза 14 идёт в отдельном git worktree параллельно с фазами 15 и 16 от одного `main`. Оркестратор укажет путь worktree; все команды относительные, без `cd`. Коммитов у исполнителя нет: оркестратор коммитит слиянием, поэтому удаление файлов делать обычным `rm`, а не `git rm`. Править ТОЛЬКО файлы из `files_modified`. Запрещено трогать `src/styles/global.css` (единственный блок reduce), `src/styles/motionPolicy.test.ts`, `src/test/setup.ts`, `src/App.tsx`, `src/components/map/*`, `src/components/layout/*`, `src/lib/useReducedMotion.ts` (только вызывать), `public/hero-globe.webm`, `public/hero-globe.mp4` (уже на месте), `package.json`, `package-lock.json`, `vite.config.ts`. `HeroParticles.tsx` и `heroParticles.ts` из плана 14-01 не редактировать: если контракт не сходится, вернуть замечание оркестратору. Никаких `@media (prefers-reduced-motion)` в `hero.css`. Цвета литералами. Комментарии в коде на русском.
</ownership>

<interfaces>
<!-- Существующие контракты. Извлечены из кода, исследовать заново не нужно. -->

Из плана 14-01, `src/components/hero/HeroParticles.tsx` (не править, только вызывать):
```tsx
export function HeroParticles(): JSX.Element
// <canvas className="hero__particles" data-anim="stars" aria-hidden="true" />
// IntersectionObserver берёт canvas.closest("section"): <HeroParticles /> обязан быть потомком <section class="hero">
```

Из `src/lib/useReducedMotion.ts` (не править):
```ts
export const REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)";
export function prefersReducedMotion(): boolean;
export function usePrefersReducedMotion(): boolean; // useSyncExternalStore поверх window.matchMedia
```

Текущий `src/components/hero/Hero.tsx` (что меняется):
```tsx
import { Starfield } from "./Starfield";      // удалить
import { GlobeCanvas } from "./GlobeCanvas";  // удалить
export function Hero() {
  const reduce = usePrefersReducedMotion();   // остаётся, теперь ещё управляет видео
  …
  return (
    <section id="hero" className="hero" aria-labelledby="hero-title">
      <Starfield />                            // → <div className="hero__video">…<video/>…</div>
      <GlobeCanvas />                          // → <HeroParticles />
      <div className="hero__content">…</div>  // без изменений
    </section>
  );
}
```

Текущий `src/components/hero/hero.css` (структура): комментарий о палитре литералами → `.hero` (`min-height: clamp(600px, 92vh, 820px)` → заменить) → блок «Звёздное поле» `.starfield`, `.starfield__layer`, `--a`, `--b`, `.starfield__glow`, `--signal`, `--unity-a`, `--unity-b`, `@keyframes star-drift`, `star-drift-b` (удалить целиком) → «Глобус» `.globe-canvas` (удалить) → `.hero::after` (z-index 3, не трогать) → `.hero__content` (z-index 5), `.hero__title`, `.hero__subtitle`, `.hero__cta`, `.hero__title h1`, `@media (min-width: 1024px) .hero__title h1`, `@media (forced-colors: active)` (не трогать).

Слои по z внутри `.hero` (`isolation: isolate`): `.hero__video` 0 → `.hero__particles` 1 → `.hero::after` 3 → `.hero__content` 5.

Из `scripts/check-dist.mjs` (паттерны для новой проверки):
```js
import { existsSync, readFileSync, readdirSync, statSync } from "node:fs";
function jsAssets(distDir): string[]            // имена .js в dist/assets, отсортированы
function checkSectionIds(distDir)               // склеивает бандл: jsAssets(distDir).map(read).join("\n")
// каждая проверка возвращает { name, ok, detail } и кладётся в массив results в run()
const QUOTES = ['"', "'", "`"];
```

Поведение jsdom, важное для тестов: `HTMLMediaElement.prototype.play` и `pause` печатают «Not implemented» в консоль и ничего не возвращают (`play()` даёт `undefined`, не Promise); `readyState` всегда 0; `currentTime` можно присваивать; `muted` — обычное свойство. `import.meta.env.BASE_URL` в vitest берётся из `vite.config.ts` (`base: "/esd-onevoice27/"`), в тестах сравнивать со значением `import.meta.env.BASE_URL`, не с литералом. React 19: `disablePictureInPicture` и `disableRemotePlayback` известны react-dom и попадают в DOM как `disablepictureinpicture`/`disableremoteplayback`; `muted` React пишет свойством, а не атрибутом, отсюда дубль через ref.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Видео оригинала в Hero.tsx и hero.css, высота секции, <HeroParticles />, удаление GlobeCanvas/globe/Starfield</name>
  <files>src/components/hero/Hero.tsx, src/components/hero/hero.css, src/components/hero/GlobeCanvas.tsx, src/components/hero/GlobeCanvas.test.tsx, src/components/hero/globe.ts, src/components/hero/globe.test.ts, src/components/hero/Starfield.tsx</files>
  <read_first>
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, раздел 1, пункты GLOBE-01, GLOBE-02, GLOBE-03, GLOBE-05 (видео), GLOBE-06, GLOBE-08 (все значения CSS и атрибуты)
    - .planning/ROADMAP.md, Phase 14, Success Criteria 1, 3, 4 и список «Не трогать»
    - src/components/hero/Hero.tsx целиком (структура секции, `usePrefersReducedMotion`, `fadeIn`)
    - src/components/hero/hero.css целиком (что удалить, что оставить нетронутым)
    - src/components/hero/HeroParticles.tsx (контракт из плана 14-01: имя экспорта, `closest("section")`)
    - docs/research/v1.2/measurements.md, раздел «Hero оригинала» (1067×600 на 1440×900, cover по центру на 390×844)
  </read_first>
  <action>
    Реализует GLOBE-01, GLOBE-02, GLOBE-03, GLOBE-06, GLOBE-08 и видео-часть GLOBE-05.

    1. `src/components/hero/hero.css`:
       - В блоке `.hero` заменить `min-height: clamp(600px, 92vh, 820px)` на `min-height: 100svh`. Остальные свойства блока (`position: relative; isolation: isolate; overflow: hidden; display: flex; align-items: flex-end; background: #070210; color: #f8f7fb`) не трогать. Сразу после блока добавить `@media (min-width: 768px) { .hero { min-height: max(600px, 65svh); } }` и `@media (min-width: 1024px) { .hero { min-height: max(600px, 64vh); } }` с комментарием: высота секции оригинала, на 1440×900 это 600px; текстовая колонка, кегли и `.hero::after` не меняются (GLOBE-03).
       - Удалить целиком блок «Звёздное поле» (`.starfield`, `.starfield__layer`, `.starfield__layer--a`, `.starfield__layer--b`, `.starfield__glow`, `.starfield__glow--signal`, `.starfield__glow--unity-a`, `.starfield__glow--unity-b`, `@keyframes star-drift`, `@keyframes star-drift-b`) и блок «Глобус» `.globe-canvas` с их комментариями.
       - На их место поставить блок «Видео глобуса» (комментарий: файл оригинала, декодирует GPU; стили с onevoice27.org по трём брейкпоинтам): `.hero__video { position: absolute; inset: 0; z-index: 0; overflow: hidden; pointer-events: none; display: flex; flex-direction: column; align-items: flex-end; justify-content: flex-start; }` и базовое правило `.hero__video > video { display: block; width: 100%; height: 100%; max-width: none; object-fit: cover; object-position: center center; transform: scale(1); transform-origin: center center; filter: saturate(1.18) contrast(1.28) brightness(0.96); }`. Затем `@media (min-width: 40rem) { .hero__video > video { object-position: 72% center; transform-origin: 72% 46%; } }` и `@media (min-width: 80rem) { .hero__video > video { width: auto; max-width: min(100%, 1920px); aspect-ratio: 16 / 9; margin-left: auto; object-fit: contain; object-position: right top; mix-blend-mode: screen; -webkit-mask-image: linear-gradient(to right, transparent 0%, rgb(0 0 0 / .35) 4%, black 14%), linear-gradient(black 0%, black 76%, transparent 100%); -webkit-mask-composite: source-in; mask-image: linear-gradient(to right, transparent 0%, rgb(0 0 0 / .35) 4%, black 14%), linear-gradient(black 0%, black 76%, transparent 100%); mask-composite: intersect; } }`. Комментарий у десктопного блока: при `height: 100%` (600px) и `aspect-ratio: 16 / 9` видео получает 1067px ширины и прижимается вправо вверх, как у оригинала; `-webkit-` дубли для Safari.
       - После видео блок «Частицы»: `.hero__particles { position: absolute; inset: 0; z-index: 1; width: 100%; height: 100%; opacity: .72; mix-blend-mode: screen; pointer-events: none; }` с комментарием: canvas порта `orig-hero-motion.js`, значения opacity и blend с оригинала (GLOBE-04, компонент из плана 14-01).
       - Блоки `.hero::after`, `.hero__content`, `.hero__title`, `.hero__subtitle`, `.hero__cta`, `.hero__title h1`, медиаблок `.hero__title h1` и `forced-colors` оставить символ в символ. В файле не должно появиться `prefers-reduced-motion`.

    2. `src/components/hero/Hero.tsx`:
       - Импорты: удалить `Starfield` и `GlobeCanvas`; добавить `useEffect`, `useRef` из `react` (в одну строку с существующим `import type { MouseEvent }` или отдельно, по стилю файла) и `HeroParticles` из `./HeroParticles`.
       - Добавить модульную функцию `prefersSaveData(): boolean` над компонентом (не экспортировать: правило `react-refresh/only-export-components`): при `typeof navigator === "undefined"` вернуть false; иначе прочитать `(navigator as Navigator & { connection?: { saveData?: boolean } }).connection?.saveData === true`. Комментарий: `NetworkInformation` нет в lib.dom, поэтому локальное сужение типа; при экономии трафика источники не подключаются, остаётся фон `#070210` и частицы (GLOBE-08, принятое отклонение от оригинала).
       - В компоненте: `const reduce = usePrefersReducedMotion();` остаётся; добавить `const videoRef = useRef<HTMLVideoElement>(null);` и `const saveData = prefersSaveData();` (чтение в рендере допустимо: значение стабильно на время сессии; если `react-hooks/purity` в lint возразит, перенести в ленивый инициализатор `useState(prefersSaveData)`, других вариантов не выбирать).
       - `useEffect` с зависимостью `[reduce]`: взять `video = videoRef.current`, при `null` выйти; `video.muted = true` (React 19 пишет `muted` свойством, а без него iOS не стартует автовоспроизведение; дубль до `play()` по GLOBE-01). Ветка `reduce`: локальная `freeze = () => { video.pause(); video.currentTime = 0; }`; если `video.readyState >= HTMLMediaElement.HAVE_CURRENT_DATA` — вызвать `freeze()` сразу, иначе `video.addEventListener("loadeddata", freeze, { once: true })` и вернуть cleanup, снимающий слушатель. Ветка без reduce: `const attempt = video.play();` и, если `attempt` определён и у него есть `catch`, `attempt.catch(() => {})` (политика автовоспроизведения может отклонить промис; jsdom возвращает `undefined`). Комментарий: при снятии reduce эффект перезапускается и зовёт `play()` (GLOBE-05).
       - JSX секции: первым ребёнком `<div className="hero__video">` с `<video ref={videoRef} autoPlay muted loop playsInline preload={saveData ? "none" : "auto"} disablePictureInPicture disableRemotePlayback tabIndex={-1} aria-hidden="true" data-anim="globe">`; внутри, только если `!saveData`, два `<source>`: `src={\`${import.meta.env.BASE_URL}hero-globe.webm\`} type="video/webm"` и затем `src={\`${import.meta.env.BASE_URL}hero-globe.mp4\`} type="video/mp4"` (webm первым: он вдвое легче). Вторым ребёнком `<HeroParticles />` (его `IntersectionObserver` ищет `closest("section")`, поэтому он остаётся прямым потомком секции). Третьим `.hero__content` без изменений. Комментарий над видео: `data-anim="globe"` переезжает с canvas на видео, значение реестра `motionPolicy.test.ts` сохраняется; постера нет, как у оригинала, первые кадры закрывает фон секции.
       - В комментариях не упоминать удалённые имена `Starfield`, `GlobeCanvas`, `starfield`: grep-гейты ниже ищут их по всему `src`.

    3. Удалить файлы `src/components/hero/GlobeCanvas.tsx`, `GlobeCanvas.test.tsx`, `globe.ts`, `globe.test.ts`, `Starfield.tsx` командой `rm` (без `git rm`: коммитит оркестратор). После удаления убедиться, что `grep -rn "GlobeCanvas\|Starfield\|from \"./globe\"" src` пуст.

    4. Прогнать `npx tsc -b`, `npx vitest run src/styles/motionPolicy.test.ts` (значения `stars` из `HeroParticles.tsx` и `globe` из `Hero.tsx` в реестре) и `npm run lint`. `Hero.test.tsx` в этой задаче ещё красный по двум старым кейсам (`.starfield`, `canvas.globe-canvas`); его переписывает Task 2.
  </action>
  <acceptance_criteria>
    - Файлы `src/components/hero/GlobeCanvas.tsx`, `GlobeCanvas.test.tsx`, `globe.ts`, `globe.test.ts`, `Starfield.tsx` отсутствуют; `ls src/components/hero` показывает `Hero.tsx`, `hero.css`, `Hero.test.tsx`, `HeroParticles.tsx`, `HeroParticles.test.tsx`, `heroParticles.ts`, `heroParticles.test.ts` и ничего больше
    - `grep -rEn "Starfield|GlobeCanvas|starfield|star-drift|globe-canvas" src --include='*.ts' --include='*.tsx' --include='*.css' | grep -v '^\s*//' | wc -l` даёт 0
    - `src/components/hero/Hero.tsx` содержит `<HeroParticles />`, `className="hero__video"`, `ref={videoRef}`, `data-anim="globe"`, `disablePictureInPicture`, `disableRemotePlayback`, `tabIndex={-1}`, `playsInline`, `preload=`, `video.muted = true`, `"loadeddata"`, `.pause()`, `.play()`, `saveData`, `import.meta.env.BASE_URL}hero-globe.webm`, `import.meta.env.BASE_URL}hero-globe.mp4`, `type="video/webm"`, `type="video/mp4"`, `usePrefersReducedMotion()`
    - `src/components/hero/hero.css` содержит `min-height: 100svh`, `min-height: max(600px, 65svh)`, `min-height: max(600px, 64vh)`, `.hero__video {`, `.hero__video > video {`, `filter: saturate(1.18) contrast(1.28) brightness(0.96)`, `@media (min-width: 40rem)`, `object-position: 72% center`, `transform-origin: 72% 46%`, `@media (min-width: 80rem)`, `max-width: min(100%, 1920px)`, `aspect-ratio: 16 / 9`, `object-fit: contain`, `object-position: right top`, `mix-blend-mode: screen`, `-webkit-mask-image:`, `-webkit-mask-composite: source-in`, `mask-composite: intersect`, `.hero__particles {`, `opacity: .72`; не содержит `clamp(600px, 92vh, 820px)`, `starfield`, `star-drift`, `globe-canvas`, `prefers-reduced-motion`
    - Блоки `.hero::after`, `.hero__content`, `.hero__title h1` в `hero.css` совпадают с версией из `main` (`git diff main -- src/components/hero/hero.css` не показывает изменений в этих блоках)
    - `npx tsc -b` без ошибок; `npx vitest run src/styles/motionPolicy.test.ts` зелёный; `npm run lint` чистый
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npm run lint && npx vitest run src/styles/motionPolicy.test.ts src/components/hero/HeroParticles.test.tsx</automated>
  </verify>
  <done>Hero рендерит `.hero__video > video` с двумя источниками под `BASE_URL`, `<HeroParticles />` и прежнюю текстовую колонку; CSS видео по трём брейкпоинтам и высота секции как у оригинала; canvas-глобус и звёздное поле удалены из кода и стилей; tsc, lint и политика движения зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Переписать Hero.test.tsx под видео, saveData, reduce-паузу, canvas частиц и текст hero.css</name>
  <files>src/components/hero/Hero.test.tsx</files>
  <read_first>
    - src/components/hero/Hero.test.tsx целиком (десять существующих кейсов; два про `.starfield` и `canvas.globe-canvas` заменяются, остальные остаются)
    - src/components/hero/Hero.tsx после Task 1
    - src/components/hero/HeroParticles.test.tsx (хелпер `mockReducedMotion` через `vi.spyOn(window, "matchMedia")`, `afterEach(vi.restoreAllMocks)`)
    - src/styles/motionPolicy.test.ts, строки 1–30 (чтение CSS с диска через `readFileSync(resolve(process.cwd(), …))`: vitest с `css: false` отдаёт CSS-модули пустой строкой)
    - .planning/ROADMAP.md, Phase 14, Success Criteria 5
  </read_first>
  <behavior>
    - Видео: `document.querySelector("#hero .hero__video > video")` не null; атрибуты `data-anim="globe"`, `aria-hidden="true"`, `tabindex="-1"`, `preload="auto"`, наличие `autoplay`, `loop`, `playsinline`, `disablepictureinpicture`, `disableremoteplayback`; свойство `muted === true`
    - Источники: ровно два `<source>` внутри видео; первый `src === \`${import.meta.env.BASE_URL}hero-globe.webm\`` и `type="video/webm"`, второй `…hero-globe.mp4` и `type="video/mp4"`; каждый `src` начинается с `/` и заканчивается именем файла
    - Порядок слоёв: у `section#hero` первый ребёнок `.hero__video`, второй `canvas.hero__particles[data-anim="stars"][aria-hidden="true"]`, третий `.hero__content`
    - Без reduce: `HTMLMediaElement.prototype.play` вызван один раз после монтирования, `pause` не вызван
    - При reduce (`matchMedia` с `matches: true` для запроса reduce): `play` не вызван; после `fireEvent(video, new Event("loadeddata"))` вызван `pause` и `video.currentTime === 0`
    - При `navigator.connection = { saveData: true }`: видео есть, `<source>` внутри нет, `preload="none"`, canvas частиц на месте; после теста свойство `connection` удалено и обычный рендер снова даёт два источника
    - Текст `src/components/hero/hero.css`: содержит `min-height: 100svh`, `min-height: max(600px, 65svh)`, `min-height: max(600px, 64vh)`, `filter: saturate(1.18) contrast(1.28) brightness(0.96)`, `object-position: 72% center`, `transform-origin: 72% 46%`, `@media (min-width: 40rem)`, `@media (min-width: 80rem)`, `max-width: min(100%, 1920px)`, `aspect-ratio: 16 / 9`, `object-fit: contain`, `object-position: right top`, `mix-blend-mode: screen`, `-webkit-mask-image:`, `-webkit-mask-composite: source-in`, `mask-composite: intersect`, `.hero__particles {`, `opacity: .72`, `background: #070210`; не содержит `starfield`, `star-drift`, `globe-canvas`, `clamp(600px, 92vh, 820px)`, `prefers-reduced-motion`
    - Существующие кейсы (секция, aria-labelledby, H1, подзаголовок, CTA, луч, тексты из heroCopy, скролл по клику) проходят без изменений
  </behavior>
  <action>
    Реализует тестовую часть GLOBE-07 для видео (и закрепляет GLOBE-01, 02, 03, 05, 06, 08 текстовыми и DOM-проверками).

    1. В `src/components/hero/Hero.test.tsx` добавить импорты `fireEvent` из `@testing-library/react`, `readFileSync` из `node:fs`, `resolve` из `node:path`. Ввести `beforeEach`, который глушит jsdom-заглушки медиа: `vi.spyOn(HTMLMediaElement.prototype, "play").mockImplementation(() => Promise.resolve())` и `vi.spyOn(HTMLMediaElement.prototype, "pause").mockImplementation(() => {})` (иначе каждый рендер печатает «Not implemented: HTMLMediaElement.prototype.play»); `afterEach(() => vi.restoreAllMocks())`. Существующий кейс с `scrollTo` уже восстанавливает свой шпион вручную, его не трогать.

    2. Удалить кейсы «кладёт в секцию декоративное звёздное поле» и «кладёт в секцию canvas глобуса». Добавить кейсы из `<behavior>`: «кладёт на первый экран видео глобуса с атрибутами автовоспроизведения», «подключает webm и mp4 из public под BASE_URL», «складывает слои: видео, частицы, текст», «без reduce запускает видео и дублирует muted через ref», «при reduce держит видео на первом кадре после loadeddata», «при saveData не подключает источники», «hero.css: высота секции, видео по трём брейкпоинтам, без звёздного поля». Хелпер `mockReducedMotion(reduce)` скопировать из `HeroParticles.test.tsx` (`vi.spyOn(window, "matchMedia").mockImplementation((query) => ({ matches: reduce && query.includes("reduce"), media: query, onchange: null, addEventListener: vi.fn(), removeEventListener: vi.fn(), addListener: vi.fn(), removeListener: vi.fn(), dispatchEvent: vi.fn() }) as unknown as MediaQueryList)`). Для `saveData`: `Object.defineProperty(navigator, "connection", { value: { saveData: true }, configurable: true })` перед рендером и `delete (navigator as { connection?: unknown }).connection` в `finally`. Пути к файлам: `resolve(process.cwd(), "src/components/hero/hero.css")`, читать один раз на уровне `describe`.

    3. Утверждения через `toHaveAttribute` (для булевых атрибутов без значения), `toBe` для свойств `muted` и `currentTime`, `toHaveBeenCalledTimes(1)` для `play`. Порядок детей секции проверять через `section.children[0..2]` и `classList.contains`/`tagName`. Названия тестов на русском в стиле файла.
  </action>
  <acceptance_criteria>
    - `src/components/hero/Hero.test.tsx` содержит `hero-globe.webm`, `hero-globe.mp4`, `import.meta.env.BASE_URL`, `"loadeddata"`, `saveData`, `hero__particles`, `data-anim`, `readFileSync`, `mask-composite: intersect`, `min-height: 100svh`; не содержит `starfield` и `globe-canvas` вне строк `expect(css).not.toContain(...)`
    - В файле не меньше пятнадцати `it(`
    - `npx vitest run src/components/hero` зелёный без строк «Not implemented» в выводе
    - `npx tsc -b` и `npm run lint` чистые
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero src/App.test.tsx src/styles/motionPolicy.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>`Hero.test.tsx` закрепляет видео с двумя источниками под `BASE_URL`, атрибуты автовоспроизведения и `data-anim`, дубль `muted`, паузу при reduce, ветку `saveData`, порядок слоёв и текст `hero.css`; все тесты hero, App и политика движения зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: check-dist.mjs проверяет hero-globe.webm/.mp4 в dist и ссылки в бандле; полный гейт фазы</name>
  <files>scripts/check-dist.mjs</files>
  <read_first>
    - scripts/check-dist.mjs целиком (форма результата `{ name, ok, detail }`, `jsAssets`, `checkSectionIds`, массив `results` в `run`)
    - .planning/ROADMAP.md, Phase 14, Success Criteria 5 (гейт) и требование SHIP-01 в REQUIREMENTS.md (тот же гейт после слияния)
  </read_first>
  <action>
    Реализует гейт-часть GLOBE-07.

    1. В `scripts/check-dist.mjs` добавить константы `HERO_VIDEO = ["hero-globe.webm", "hero-globe.mp4"]` и `MIN_VIDEO_BYTES = 1024 * 1024` (оба файла в `public/` весят 1,9 и 2,9 МБ; порог ловит усечённую копию). Вынести склейку бандла из `checkSectionIds` в хелпер `bundleText(distDir)` (`jsAssets(distDir).map(read).join("\n")`) и использовать его в обеих проверках.

    2. Добавить `checkHeroVideo(distDir)`: для каждого имени из `HERO_VIDEO` проверить `existsSync(join(distDir, name))` (Vite копирует `public/` в корень `dist/`), размер `statSync(...).size >= MIN_VIDEO_BYTES` и вхождение имени в `bundleText(distDir)` (ссылка из `Hero.tsx` через `import.meta.env.BASE_URL` после сборки становится строкой с `hero-globe.webm`/`.mp4`). Результат: `name: "видео глобуса hero-globe.webm/.mp4 в dist и ссылки в бандле"`, `ok` при отсутствии проблем, `detail` с размерами в КБ через существующий `kb()` при успехе или списком проблем (`нет файла`, `меньше 1 МБ`, `нет ссылки в JS`) через `; `. Вставить вызов в `results` сразу после `checkSectionIds(distDir)`. Комментарий на русском над функцией.

    3. Прогнать полный гейт фазы: `npx tsc -b && npx vitest run && npm run lint && npm run build && node scripts/check-dist.mjs`. В выводе `check-dist` должна быть строка `OK видео глобуса …` и итог `OK: 12 проверок`. Проверить размер главного JS-чанка в выводе `checkJsSize`: он должен уменьшиться относительно `main` (ушли 2200 точек и математика сферы); записать числа в SUMMARY.

    4. Визуальный smoke (без блокирующего чекпоинта, формальная приёмка в фазе 17 по SHIP-03): если в сессии доступен Playwright MCP, поднять `npx vite preview --port 4173 --strictPort`, открыть `http://localhost:4173/esd-onevoice27/` на 1440×900 и 390×844, снять `video.paused`, `video.currentTime` через 1 с, `getBoundingClientRect()` видео (ожидание 1067×600 справа вверху на 1440, полный экран на 390) и `getComputedStyle(video).objectFit` (`contain` и `cover`), записать в SUMMARY. Если MCP недоступен, зафиксировать в SUMMARY, что визуальная проверка отложена до фазы 17.
  </action>
  <acceptance_criteria>
    - `scripts/check-dist.mjs` содержит `HERO_VIDEO`, `MIN_VIDEO_BYTES`, `function bundleText`, `function checkHeroVideo`, `checkHeroVideo(distDir)` внутри `results`; `checkSectionIds` использует `bundleText`
    - `npm run build && node scripts/check-dist.mjs` печатает `OK видео глобуса` и завершает `OK: 12 проверок` с кодом 0
    - `ls dist/hero-globe.webm dist/hero-globe.mp4` показывает оба файла; `grep -l "hero-globe.webm" dist/assets/*.js` находит хотя бы один чанк
    - `npx vitest run` (весь набор) зелёный; `npx tsc -b` и `npm run lint` чистые
    - `git status --porcelain` показывает только файлы из `files_modified` обоих планов фазы (изменённые `Hero.tsx`, `hero.css`, `Hero.test.tsx`, `scripts/check-dist.mjs`, удалённые пять файлов hero, новые четыре файла плана 14-01); `package.json`, `package-lock.json`, `public/*` без изменений
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run && npm run lint && npm run build && node scripts/check-dist.mjs</automated>
  </verify>
  <done>`check-dist` подтверждает оба видеофайла в `dist/` и ссылки на них в JS; полный гейт фазы зелёный; в SUMMARY размеры чанков и результат smoke (или отметка о переносе в фазу 17).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл GitHub Pages | видео и JS с того же origin; входа данных нет |
| `navigator.connection.saveData` → рендер | чтение подсказки браузера, наружу ничего не уходит |
| `npm run build` → `dist/` → `check-dist` | локальная сборка, скрипт только читает файлы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | источники `<video>` | mitigate | только `import.meta.env.BASE_URL` + имя файла, никаких внешних хостов; `checkHosts` в `check-dist` держит белый список, `checkHeroVideo` сверяет файлы в `dist/` |
| T-14-05 | Denial of service | автозагрузка видео 1,9–2,9 МБ на мобильной сети | mitigate | `saveData` отключает источники (GLOBE-08); `preload="auto"` в остальных случаях, как у оригинала (принятое отклонение записано в спеке) |
| T-14-06 | Information disclosure | `navigator.connection` | accept | значение читается локально и не логируется |
| T-14-07 | Elevation of privilege | `<video>` в дереве доступности | mitigate | `aria-hidden="true"`, `tabIndex={-1}`, `disablePictureInPicture`, `disableRemotePlayback`: слой декоративный, в фокус и в PiP не попадает |
| T-14-SC | Tampering | npm-установки | mitigate | новых пакетов нет; `package.json`/`package-lock.json` неизменны (гейт `git status`); любой `npm install` — стоп и вопрос оркестратору |
</threat_model>

<verification>
- `npx vitest run src/components/hero`, `npx vitest run src/styles/motionPolicy.test.ts`, `npx vitest run` (весь набор) зелёные
- `npx tsc -b`, `npm run lint` чистые
- `npm run build && node scripts/check-dist.mjs` печатает `OK видео глобуса …` и `OK: 12 проверок`
- `ls src/components/hero` без `GlobeCanvas*`, `globe*`, `Starfield.tsx`; grep по `src` на `Starfield|GlobeCanvas|starfield|star-drift|globe-canvas` пуст
- `git diff main -- src/components/hero/hero.css` не трогает `.hero::after`, `.hero__content`, `.hero__title h1`
- Smoke на preview (при наличии Playwright MCP): на 1440×900 видео 1067×600 справа вверху с `object-fit: contain`, на 390×844 на весь экран с `cover`, `!paused` и `currentTime` растёт; результат в SUMMARY
</verification>

<success_criteria>
- `<video data-anim="globe">` с `autoPlay muted loop playsInline preload="auto" disablePictureInPicture disableRemotePlayback tabIndex={-1} aria-hidden`, два источника под `BASE_URL`, `muted` через ref, `pause()` после `loadeddata` при reduce и `play()` при снятии reduce; при `saveData` источников нет
- `hero.css`: `min-height: 100svh` / `max(600px, 65svh)` от 768px / `max(600px, 64vh)` от 1024px; видео `cover` + filter до 640px, `72% center` и `72% 46%` от 640px, `contain` 16:9 справа вверху с `screen` и двойной маской (`mask-composite: intersect`, `-webkit-mask-composite: source-in`) от 1280px; `.hero__particles` z 1, `opacity: .72`, `screen`
- `<HeroParticles />` между видео и текстом; `GlobeCanvas`, `globe`, `Starfield`, `.starfield__*`, `star-drift*`, `.globe-canvas` удалены; `motionPolicy.test.ts` без правок зелёный
- `Hero.test.tsx` покрывает видео, источники, атрибуты, `muted`, reduce, `saveData`, слои и текст CSS; `check-dist` подтверждает `hero-globe.webm`/`.mp4` в `dist/` и ссылки в бандле; полный гейт зелёный
</success_criteria>

<output>
Create `.planning/phases/14-hero-video-particles/14-02-SUMMARY.md` when done (с выводом `check-dist`, размерами JS-чанков до и после, списком удалённых файлов и результатом smoke на preview или пометкой о переносе в фазу 17)
</output>
