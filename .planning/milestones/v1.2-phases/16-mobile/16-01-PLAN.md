---
phase: 16-mobile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Footer.css
  - src/components/layout/Footer.test.tsx
  - src/components/form/ConsentCheckbox.tsx
  - src/components/form/light-form.css
  - src/components/form/LightForm.test.tsx
  - src/components/news/NewsCard.tsx
  - src/components/news/News.tsx
  - src/components/news/NewsCard.test.tsx
  - src/components/news/News.test.tsx
  - src/components/layout/Header.css
  - src/components/layout/Header.test.tsx
autonomous: true
requirements: [MOB-01, MOB-02, MOB-03, MOB-04]
tags: [mobile, tap-target, footer, form, news, header, css]

must_haves:
  truths:
    - "Каждая ссылка .site-footer__links a описана как display: inline-flex; align-items: center; min-height: 44px; padding-inline: 8px; на 390×844 её бокс не ниже 44px, текст центрирован в боксе"
    - "Список .site-footer__links ul с gap: 0: шаг столбца 44px вместо 30.4px, боксы соседних ссылок не перекрываются, касание по краю бокса не уходит в соседнюю ссылку; отрицательных margin у ссылок нет"
    - "Label согласия .lf-check имеет min-height: 44px; display: flex; align-items: center; gap: 12px, чекбокс .lf-checkbox 20×20 с margin: 0; клик по тексту согласия переключает чекбокс, ошибка «Нужно согласие на обработку данных» показывается как раньше"
    - "Каждый <img> карточки новостей несёт width=\"480\" height=\"360\" decoding=\"async\"; первая карточка первой страницы получает loading=\"eager\" и fetchpriority=\"high\", остальные (и все карточки второй страницы) loading=\"lazy\" без fetchpriority; классы object-cover/object-center, coverZoom и coverPosition не изменились"
    - "Подпись «МИССИЯ ДЛЯ ВСЕХ» в шапке имеет font-size: .625rem и letter-spacing: .16em на любой ширине экрана; строки .5625rem в Header.css нет; подпись вордмарка футера clamp(.625rem, 1.1vw, .8125rem) не изменилась"
    - "Файлы из списка «Не трогать» (Footer.tsx, Header.tsx, Wordmark.tsx, LightForm.tsx, FormField.tsx, news.css, global.css, map/*, hero/*, App.seams.test.tsx) не изменены; блок .lf-section в light-form.css без background"
  artifacts:
    - path: "src/components/layout/Footer.css"
      provides: "Цель касания 44px у ссылок футера, список с gap: 0 и шагом столбца 44px"
      contains: "min-height: 44px"
    - path: "src/components/layout/Footer.test.tsx"
      provides: "Текстовая проверка блоков .site-footer__links a и ul (gap: 0, без margin-block) и неизменной подписи вордмарка футера"
      contains: "gap: 0"
    - path: "src/components/form/light-form.css"
      provides: "Label согласия 44px, чекбокс 20×20 без margin"
      contains: "gap: 12px"
    - path: "src/components/form/ConsentCheckbox.tsx"
      provides: "Label с htmlFor, оборачивающая input и текст: вся label и есть область нажатия"
      contains: "htmlFor={id}"
    - path: "src/components/form/LightForm.test.tsx"
      provides: "Тест переключения согласия кликом по тексту label и контракт правил .lf-check/.lf-checkbox"
      contains: "lf-check-text"
    - path: "src/components/news/NewsCard.tsx"
      provides: "Проп priority, атрибуты width/height/decoding, eager+fetchPriority у приоритетной карточки"
      contains: "fetchPriority"
    - path: "src/components/news/News.tsx"
      provides: "Передача priority первой карточке первой страницы"
      contains: "priority={result.page === 1 && index === 0}"
    - path: "src/components/news/NewsCard.test.tsx"
      provides: "Тесты атрибутов обложки по умолчанию и с priority"
      contains: "fetchpriority"
    - path: "src/components/news/News.test.tsx"
      provides: "Тест приоритета первой карточки первой страницы и отсутствия приоритета на второй"
      contains: "fetchpriority"
    - path: "src/components/layout/Header.css"
      provides: "Подпись логотипа .625rem на узком экране"
      contains: "font-size: .625rem"
    - path: "src/components/layout/Header.test.tsx"
      provides: "Контракт единственного правила .site-header .wordmark__tagline"
      contains: "wordmark__tagline"
  key_links:
    - from: "src/components/news/News.tsx"
      to: "src/components/news/NewsCard.tsx"
      via: "проп priority у первой карточки первой страницы"
      pattern: "priority=\\{result\\.page === 1 && index === 0\\}"
    - from: "src/components/news/NewsCard.tsx"
      to: "<img> обложки"
      via: "loading, fetchPriority, width, height, decoding из пропа priority"
      pattern: "fetchPriority=\\{priority \\? \"high\" : undefined\\}"
    - from: "src/components/form/ConsentCheckbox.tsx"
      to: "input.lf-checkbox"
      via: "label.lf-check с htmlFor={id}, input вложен в label"
      pattern: "<label className=\"lf-check\" htmlFor=\\{id\\}>"
    - from: "src/components/form/LightForm.test.tsx"
      to: "src/components/form/light-form.css"
      via: "readFileSync (vitest css: false)"
      pattern: "light-form\\.css"
    - from: "src/components/layout/Footer.test.tsx"
      to: "src/components/layout/Footer.css"
      via: "readFileSync (vitest css: false)"
      pattern: "Footer\\.css"
    - from: "src/components/layout/Header.test.tsx"
      to: "src/components/layout/Header.css"
      via: "HEADER_CSS, regex по правилу .site-header .wordmark__tagline"
      pattern: "wordmark__tagline"
---

<objective>
Закрыть четыре находки мобильного аудита 390×844 (docs/research/v1.2/measurements.md): ссылки футера высотой 22px и чекбокс согласия 18×18 становятся целями касания не ниже 44px, превью новостей получают `width`/`height` и приоритет загрузки у первой карточки, подпись логотипа в шапке растёт с 9px до 10px. Разметка остаётся как в v1.1; столбец ссылок футера получает шаг 44px вместо 30.4px (боксы 44px стоят встык, без перекрытия), ошибка согласия и кроп обложек не меняются.

Purpose: раздел 3 (MOB) спецификации `docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md` и требования MOB-01…MOB-04. Аудит SHIP-03 в фазе 17 проверяет, что на странице нет целей касания ниже 44px (кроме визуально скрытых radio).
Output: обновлённые `Footer.css`, `light-form.css`, `ConsentCheckbox.tsx`, `NewsCard.tsx`, `News.tsx`, `Header.css` и тесты рядом; `npx vitest run src/components/layout src/components/form src/components/news src/App.seams.test.tsx`, `npx tsc -b`, `npm run lint` зелёные.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@docs/research/v1.2/measurements.md

@src/components/layout/Footer.css
@src/components/layout/Footer.test.tsx
@src/components/layout/Header.css
@src/components/layout/Header.test.tsx
@src/components/form/ConsentCheckbox.tsx
@src/components/form/light-form.css
@src/components/form/LightForm.test.tsx
@src/components/news/NewsCard.tsx
@src/components/news/News.tsx
@src/components/news/NewsCard.test.tsx
@src/components/news/News.test.tsx

**Условия работы исполнителя.** Работа идёт в отдельном git worktree, путь даёт оркестратор: команды пишутся относительно корня worktree, без `cd` и абсолютных путей. Коммитов исполнитель не делает. Файлы из списка «Не трогать» в ROADMAP (`Footer.tsx`, `Header.tsx`, `Wordmark.tsx`, `LightForm.tsx`, `FormField.tsx`, `news.css`, `src/styles/global.css`, `src/components/map/*`, `src/components/hero/*`, `src/App.seams.test.tsx`) не редактируются. В `light-form.css` блоки `.lf-section {` и `.lf-section::before` не получают `background`: это читает `App.seams.test.tsx`.

**Тесты.** Vitest 4 (`globals: true`, `environment: jsdom`, `css: false`, setup `src/test/setup.ts`), Testing Library. CSS в jsdom не вычисляется, поэтому значения правил проверяются по тексту файла через `readFileSync(resolve(process.cwd(), "..."))`, как уже делают `Footer.test.tsx`, `Header.test.tsx`, `LightForm.test.tsx`, `NewsCard.test.tsx`. `@testing-library/user-event` 14 установлен, но для клика по label достаточно `fireEvent.click` из `@testing-library/react` (jsdom выполняет activation behavior label: клик по потомку label кликает связанный input).

<interfaces>
<!-- Контракты, которые исполнитель использует без разведки. -->

Текущие правила Footer.css (строки 109–128), меняются в Task 1:
```css
/* Ссылки столбиком по центру: интервал держит gap, а не padding у ссылки. */
.site-footer__links ul {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  margin: 0;
  padding: 0;
  list-style: none;
}

.site-footer__links a {
  display: inline-block;
  font-size: .875rem;
  font-weight: 700;
  line-height: 1.6;
  color: rgb(248 247 251 / .92);
  text-decoration: none;
  transition: color var(--dur-ui) var(--ease-ui);
}
```
Подпись вордмарка футера (строки 85–87, не меняется): `.site-footer .wordmark--footer .wordmark__tagline { font-size: clamp(.625rem, 1.1vw, .8125rem); }`.
Разметка `Footer.tsx` (не меняется): `nav.site-footer__links > ul > li > a[target=_blank]`, внутри `a` текст ссылки и `span.sr-only` (Tailwind-утилита, position: absolute, в потоке не участвует).

Текущие правила light-form.css (строки 343–357), меняются в Task 2:
```css
/* Согласие */
.lf-check {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  cursor: pointer;
}

.lf-checkbox {
  flex: 0 0 18px;
  width: 18px;
  height: 18px;
  margin: 2px 0 0;
  accent-color: rgb(170 217 220);
  cursor: pointer;
}
```
Правила `.lf-checkbox[aria-invalid="true"]`, `.lf-checkbox:focus-visible`, `.lf-check-text` (`font: 650 14px/1.4 var(--font-body)`), `.lf-error` не меняются.

Разметка `ConsentCheckbox.tsx` (не меняется, только JSDoc):
```tsx
export function ConsentCheckbox({ id, checked, error, onChange, onBlur }: {
  id: string; checked: boolean; error?: string;
  onChange: (next: boolean) => void; onBlur: () => void;
}) // рендерит div.lf-consent > label.lf-check[htmlFor=id] > (input.lf-checkbox#id[name=consent] + span.lf-check-text) и p.lf-error при ошибке
```
Хелперы `LightForm.test.tsx`: `renderForm()` (LightsProvider + LightForm), `control(name)` ищет `[name="consent"]`, `clickSubmit()`, `errorFor(name)`; ошибка согласия: «Нужно согласие на обработку данных».

Новости:
```ts
// src/data/news.ts
export type NewsItem = { id: string; title: string; date: string; cover: string; href: string; source: string; coverZoom?: number; coverPosition?: string };
// 9 записей; news[0].id === "day-of-prayer" (coverZoom 1.45, coverPosition "50% 65%"), news[1].id === "kaminsky" (без style)

// src/lib/paginate.ts
export function paginate<T>(items: T[], page: number, perPage = 6): { items: T[]; page: number; totalPages: number };

// src/components/news/NewsCard.tsx (текущая сигнатура)
export function NewsCard({ item }: { item: NewsItem }): JSX.Element;
export function formatNewsDate(iso: string): string;
```
`News.tsx` строки 63–67 сейчас: `result.items.map((item) => (<RevealItem as="li" key={item.id} className="min-w-0"><NewsCard item={item} /></RevealItem>))`; `PER_PAGE = 6`, на странице 1 шесть карточек, на странице 2 три. Кнопки пагинации: `screen.getByRole("button", { name: "Страница 2" })`.
Текущий `<img>` в `NewsCard.tsx` (строки 59–76): `src={item.cover} alt="" loading="lazy" decoding="async" onError=... className="news-card__image h-full w-full max-w-full object-cover object-center ..." style={coverZoom/coverPosition ? {...} : undefined}`.
`@types/react` 19.2: `ImgHTMLAttributes.fetchPriority?: "high" | "low" | "auto"`; React 19 рендерит его в DOM как `fetchpriority`.

Шапка, Header.css строки 129–132 и 187–189 (меняются в Task 4):
```css
.site-header .wordmark__tagline {
  font-size: .5625rem;
  letter-spacing: .16em;
}
/* внутри @media (width >= theme(--breakpoint-desktop)) — брейкпоинт 768px из tokens.css */
  .site-header .wordmark__tagline {
    font-size: .625rem;
  }
```
`Header.test.tsx`: константа `HEADER_CSS` (readFileSync), describe «Header: контракт стилей пилюли» с проверками `toMatch(/\.site-header\.is-menu-open \{[^}]*transition: none;/)` и подобными.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ссылки футера как цели касания 44px с шагом столбца 44px (MOB-01)</name>
  <files>src/components/layout/Footer.css, src/components/layout/Footer.test.tsx</files>
  <read_first>
    - src/components/layout/Footer.css строки 109–133 (правила `.site-footer__links ul`, `.site-footer__links a`, hover/focus)
    - src/components/layout/Footer.test.tsx: константа `FOOTER_CSS`; тест «ставит ссылки столбиком и убирает линию над юридическим текстом» (строки 130–155), в его массиве ожидаемых строк на строке 144 стоит `"gap: 8px"`
    - src/components/layout/Footer.tsx строки 18–28 (только чтение: `li > a` с текстом и `span.sr-only`)
  </read_first>
  <action>
    Расчёт для комментария над правилом. В v1.1 строка ссылки `font-size: .875rem` × `line-height: 1.6` = 22.4px, интервал списка `gap: 8px`, шаг столбца 30.4px. Бокс ссылки растёт до 44px, и удержать шаг 30.4px можно только перекрытием соседних боксов на 13.6px (отрицательный `gap` CSS отбрасывает, отрицательный `margin-block` даёт то же перекрытие), а перекрытие уводит касание по краю бокса в соседнюю ссылку. Поэтому по MOB-01 шаг столбца растёт до 44px: список получает `gap: 0`, боксы 44px стоят встык, текст центрирован в боксе.

    В `.site-footer__links ul` заменить `gap: 8px` на `gap: 0`. В `.site-footer__links a` заменить `display: inline-block` на `display: inline-flex; align-items: center; min-height: 44px; padding-inline: 8px;`; никакого `margin-block` и других отрицательных отступов. `font-size`, `font-weight`, `line-height: 1.6`, цвет, `text-decoration`, `transition`, правила `:hover`/`:focus-visible` оставить. Комментарий над `.site-footer__links ul` («интервал держит gap…») переписать: шаг столбца 44px вместо 30.4px, потому что бокс 44px при прежнем шаге перекрывал бы соседа; интервал между боксами нулевой, а визуальный просвет между строками даёт центрирование текста в боксе. Никаких `prefers-reduced-motion`, `border-top`, `grid` в файле не появляется.

    В `Footer.test.tsx` в тесте «ставит ссылки столбиком и убирает линию над юридическим текстом» заменить элемент массива `"gap: 8px"` (строка 144) на `"gap: 0;"`; остальные элементы массива и проверки этого теста не трогать. Добавить тест «даёт ссылкам цель касания 44px без перекрытия боксов»: вырезать блок `FOOTER_CSS.match(/\.site-footer__links a \{([^}]*)\}/)` и проверить, что тело содержит `display: inline-flex`, `align-items: center`, `min-height: 44px`, `padding-inline: 8px`, `font-size: .875rem`, `line-height: 1.6` и не содержит `inline-block`, `margin-block`, `margin-top`, `margin-bottom`; вырезать блок `FOOTER_CSS.match(/\.site-footer__links ul \{([^}]*)\}/)` и проверить, что он содержит `gap: 0;` и `flex-direction: column`, а `FOOTER_CSS` целиком не содержит `gap: 8px`. В том же тесте зафиксировать подпись вордмарка футера для MOB-04: `FOOTER_CSS` содержит `clamp(.625rem, 1.1vw, .8125rem)`. Другие существующие тесты не редактировать.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Footer.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Блок `.site-footer__links a {` в Footer.css содержит `display: inline-flex`, `align-items: center`, `min-height: 44px`, `padding-inline: 8px`, `line-height: 1.6`, `font-size: .875rem`; в нём нет `inline-block`, `margin-block`, `margin-top`, `margin-bottom`
    - Блок `.site-footer__links ul {` содержит `gap: 0;` и `flex-direction: column`; строки `gap: 8px` в файле нет; `border-top` и `prefers-reduced-motion` отсутствуют
    - Правило `.site-footer .wordmark--footer .wordmark__tagline` не изменено
    - `Footer.test.tsx`: элемент `"gap: 8px"` на строке 144 заменён на `"gap: 0;"`, добавлен новый тест, все тесты файла зелёные; `Footer.tsx` не изменён
  </acceptance_criteria>
  <done>Ссылки футера описаны как inline-flex 44px, список с gap 0 и шагом 44px без перекрытия боксов, тест Footer зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Label согласия 44px и чекбокс 20×20 с нажатием по тексту (MOB-02)</name>
  <files>src/components/form/light-form.css, src/components/form/ConsentCheckbox.tsx, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - src/components/form/light-form.css строки 342–375 (блок «Согласие»: `.lf-check`, `.lf-checkbox`, `[aria-invalid]`, `:focus-visible`, `.lf-check-text`)
    - src/components/form/ConsentCheckbox.tsx (весь файл, 51 строка)
    - src/components/form/LightForm.test.tsx строки 27–75 (хелперы `renderForm`, `control`, `errorFor`), 247–254 (тест «даёт согласию нативный видимый чекбокс»), 698–764 (describe «light-form.css: значения оригинала»)
    - src/App.seams.test.tsx строки 74–75 (только чтение: блок `.lf-section {` без `background`)
  </read_first>
  <behavior>
    - Клик по `span.lf-check-text` внутри label переводит `input[name="consent"]` в checked; второй клик снимает
    - После пустой отправки ошибка «Нужно согласие на обработку данных» связана с чекбоксом через `aria-describedby` (существующие тесты валидации остаются зелёными)
    - Блок `.lf-check {` содержит `min-height: 44px`, `display: flex`, `align-items: center`, `gap: 12px`, `cursor: pointer` и не содержит `flex-start`
    - Блок `.lf-checkbox {` содержит `flex: 0 0 20px`, `width: 20px`, `height: 20px`, `margin: 0;`, `accent-color: rgb(170 217 220)` и не содержит `18px`
  </behavior>
  <action>
    В `light-form.css` заменить `.lf-check` на `min-height: 44px; display: flex; align-items: center; gap: 12px; cursor: pointer;` (порядок объявлений как в MOB-02) и `.lf-checkbox` на `flex: 0 0 20px; width: 20px; height: 20px; margin: 0; accent-color: rgb(170 217 220); cursor: pointer;`. Комментарий «Согласие» дополнить одной строкой: label целиком является целью касания не ниже 44px, чекбокс центрируется по тексту. Правила `[aria-invalid="true"]`, `:focus-visible`, `.lf-check-text`, `.lf-error` и всё остальное в файле не трогать; в `.lf-section {` и рядом `background` не добавлять.

    В `ConsentCheckbox.tsx` разметка уже даёт нажатие по всей label: `label.lf-check` с `htmlFor={id}` оборачивает `input` и `span.lf-check-text`. Разметку не менять; в JSDoc компонента добавить фразу о том, что label служит целью касания 44px (`light-form.css`), и клик по тексту переключает чекбокс.

    В `LightForm.test.tsx` внутри describe «LightForm: структура секции» после теста «даёт согласию нативный видимый чекбокс» добавить тест «переключает согласие по нажатию на текст label»: `renderForm()`, взять `document.querySelector<HTMLElement>(".lf-check-text")` (бросить Error, если null), убедиться, что `control("consent")` не checked, `fireEvent.click(text)` → `expect(control("consent")).toBeChecked()`, повторный `fireEvent.click(text)` → `not.toBeChecked()`; дополнительно проверить, что `text.closest("label")` имеет класс `lf-check` и атрибут `for`, равный `control("consent").id`. Внутри describe «light-form.css: значения оригинала» добавить тест «даёт согласию цель касания 44px»: вырезать `CSS.match(/\.lf-check \{([^}]*)\}/)` и `CSS.match(/\.lf-checkbox \{([^}]*)\}/)` и проверить значения из блока `<behavior>` (наличие и отсутствие). Остальные кейсы файла не редактировать; кейсы валидации согласия («на пустой отправке показывает шесть ошибок…», «уводит фокус на согласие…») должны остаться зелёными без правок.
  </action>
  <verify>
    <automated>npx vitest run src/components/form/LightForm.test.tsx src/App.seams.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `light-form.css`: блок `.lf-check {` содержит `min-height: 44px`, `display: flex`, `align-items: center`, `gap: 12px`; блок `.lf-checkbox {` содержит `width: 20px`, `height: 20px`, `margin: 0;`; в этих блоках нет `flex-start`, `18px`, `2px 0 0`
    - Блок `.lf-section {` без `background`, `.lf-section::before` в файле нет; `App.seams.test.tsx` зелёный без правок
    - `LightForm.test.tsx` содержит два новых теста, все тесты файла зелёные, включая кейсы с текстом «Нужно согласие на обработку данных»
    - `ConsentCheckbox.tsx`: изменился только JSDoc, `LightForm.tsx` и `FormField.tsx` не изменены
  </acceptance_criteria>
  <done>Label согласия 44px с чекбоксом 20×20 по центру, клик по тексту переключает чекбокс, тесты формы и стыков зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Атрибуты размеров у превью новостей и приоритет первой карточки (MOB-03)</name>
  <files>src/components/news/NewsCard.tsx, src/components/news/News.tsx, src/components/news/NewsCard.test.tsx, src/components/news/News.test.tsx</files>
  <read_first>
    - src/components/news/NewsCard.tsx строки 31–92 (компонент и `<img>`)
    - src/components/news/News.tsx строки 14–18 и 58–68 (`PER_PAGE`, `paginate`, `result.items.map`)
    - src/components/news/NewsCard.test.tsx строки 31–79 (хелпер `renderCard`, тесты кропа и `object-cover`)
    - src/components/news/News.test.tsx строки 39–64 (переключение на «Страница 2») и 136–151 (тест с `loading="lazy"` у всех обложек)
    - src/data/news.ts строки 1–15 (тип `NewsItem`, только чтение)
  </read_first>
  <behavior>
    - `render(<NewsCard item={news[1]} />)`: `img` имеет `width="480"`, `height="360"`, `decoding="async"`, `loading="lazy"`, атрибута `fetchpriority` нет, атрибута `style` нет
    - `render(<NewsCard item={news[0]} priority />)`: `img` имеет `loading="eager"`, `fetchpriority="high"`, те же `width`/`height`/`decoding`, `style.transform === "scale(1.45)"` и `style.objectPosition === "50% 65%"` (кроп не изменился)
    - `render(<News />)`: у первой из шести `article` обложка `eager` + `fetchpriority="high"`, у остальных пяти `lazy` без `fetchpriority`; у всех `width="480"`, `height="360"`, `decoding="async"`, `alt=""`
    - После клика по кнопке «Страница 2» все три обложки `lazy` без `fetchpriority`
  </behavior>
  <action>
    В `NewsCard.tsx` расширить пропсы до `{ item: NewsItem; priority?: boolean }` со значением по умолчанию `priority = false`, JSDoc на русском: приоритет получает первая карточка первой страницы, чтобы браузер запросил её обложку до остальных (LCP-кандидат ленты). У `<img>` задать `width={480}`, `height={360}` (размер `hqdefault.jpg`, даёт браузеру пропорцию до загрузки; итоговый размер по-прежнему задают классы `h-full w-full` и кадр `aspect-video`), `decoding="async"` оставить, `loading={priority ? "eager" : "lazy"}`, `fetchPriority={priority ? "high" : undefined}` (React 19 и `@types/react` 19.2 принимают проп `fetchPriority`, в DOM он становится `fetchpriority`). `className`, `style` с `coverZoom`/`coverPosition`, `onError`, `alt=""` и ветку упавшей обложки не трогать. `news.css` не менять.

    В `News.tsx` в `result.items.map` добавить индекс и передать `priority={result.page === 1 && index === 0}`; больше ничего в файле не менять. Обоснование в комментарии рядом: приоритет только на первой странице, потому что при переходе по пагинации обложки уже ниже первого экрана.

    В `NewsCard.test.tsx` добавить describe «NewsCard: атрибуты обложки» с двумя тестами из `<behavior>` (по умолчанию и с `priority`), используя `renderCard` и `container.querySelector("img")`; проверки `fetchpriority` писать в нижнем регистре (`toHaveAttribute("fetchpriority", "high")`, `not.toHaveAttribute("fetchpriority")`). В `News.test.tsx` в тесте «держит обложку декоративной…» убрать строку `expect(image).toHaveAttribute("loading", "lazy")` и добавить новый тест «грузит обложку первой карточки первой страницы с приоритетом»: цикл по `screen.getAllByRole("article")` с индексом, ожидания из `<behavior>`, затем `fireEvent.click(screen.getByRole("button", { name: "Страница 2" }))` и проверка трёх `article` на `lazy` без `fetchpriority`. Остальные тесты файлов не редактировать.
  </action>
  <verify>
    <automated>npx vitest run src/components/news</automated>
  </verify>
  <acceptance_criteria>
    - `NewsCard.tsx` экспортирует `NewsCard({ item, priority = false })`; `<img>` содержит `width={480}`, `height={360}`, `decoding="async"`, `loading={priority ? "eager" : "lazy"}`, `fetchPriority={priority ? "high" : undefined}`; `style` с `coverZoom`/`coverPosition` и классы `object-cover object-center` без изменений
    - `News.tsx` содержит `priority={result.page === 1 && index === 0}`, других изменений нет; `news.css` не изменён
    - `NewsCard.test.tsx` и `News.test.tsx` содержат новые тесты, `npx vitest run src/components/news` зелёный
    - `npx tsc -b` без ошибок (проп `fetchPriority` типизирован)
  </acceptance_criteria>
  <done>Все обложки несут width/height/decoding, первая карточка первой страницы грузится eager с fetchpriority high, тесты новостей зелёные.</done>
</task>

<task type="auto">
  <name>Task 4: Подпись логотипа в шапке .625rem на узком экране (MOB-04)</name>
  <files>src/components/layout/Header.css, src/components/layout/Header.test.tsx</files>
  <read_first>
    - src/components/layout/Header.css строки 120–132 (`.site-header .wordmark`, `.wordmark__title`, `.wordmark__tagline`) и 146–190 (медиаблок `@media (width >= theme(--breakpoint-desktop))` с переопределением подписи)
    - src/components/layout/Header.test.tsx строки 1–30 (константа `HEADER_CSS`) и 697–725 (describe «Header: контракт стилей пилюли»)
    - src/components/layout/Wordmark.tsx (только чтение: `span.wordmark__tagline` с `copy.shell.tagline`)
  </read_first>
  <action>
    В базовом правиле `.site-header .wordmark__tagline` заменить `font-size: .5625rem` на `font-size: .625rem`, `letter-spacing: .16em` оставить. Переопределение `.site-header .wordmark__tagline { font-size: .625rem; }` внутри медиаблока `@media (width >= theme(--breakpoint-desktop))` удалить: базовое значение теперь совпадает с ним, и десктоп получает те же .625rem через каскад; правило `.site-header .wordmark__title` в том же медиаблоке и всё остальное в файле не трогать. В комментарий у базового правила добавить причину: на 390px подпись 9px читалась хуже 12px-порога аудита, десктоп уже был на 10px. `Wordmark.tsx`, `Header.tsx`, `Footer.css` не менять: вордмарк футера держит `clamp(.625rem, 1.1vw, .8125rem)` (это фиксирует тест Task 1).

    В `Header.test.tsx` в describe «Header: контракт стилей пилюли» добавить тест «держит подпись логотипа на .625rem с прежним трекингом на любой ширине»: `const rules = HEADER_CSS.match(/\.site-header \.wordmark__tagline \{[^}]*\}/g) ?? []`, `expect(rules).toHaveLength(1)`, `expect(rules[0]).toContain("font-size: .625rem;")`, `expect(rules[0]).toContain("letter-spacing: .16em;")`, `expect(HEADER_CSS).not.toContain(".5625rem")`. Существующие тесты не редактировать.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Header.test.tsx src/components/layout/Wordmark.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - В `Header.css` ровно одно правило `.site-header .wordmark__tagline {` с `font-size: .625rem;` и `letter-spacing: .16em;`; строки `.5625rem` в файле нет
    - Правило `.site-header .wordmark__title` в медиаблоке 768px и остальные правила файла не изменены
    - `Header.test.tsx` содержит новый тест, `Header.test.tsx` и `Wordmark.test.tsx` зелёные без правок `Wordmark.tsx`/`Header.tsx`
  </acceptance_criteria>
  <done>Подпись «МИССИЯ ДЛЯ ВСЕХ» в шапке .625rem на всех ширинах с трекингом .16em, тесты шапки зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| страница → img.youtube.com | обложки новостей грузятся с внешнего хоста; первая теперь запрашивается с приоритетом |
| ввод посетителя → состояние формы | клик по label переключает чекбокс согласия; данные формы никуда не отправляются (без action/method) |
| статические тексты `copy.ts` / `news.ts` → DOM | адреса ссылок футера и обложек захардкожены, пользовательского ввода в них нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information disclosure | `NewsCard.tsx`, `<img fetchPriority="high" loading="eager">` | accept | тот же хост `img.youtube.com`, что и раньше; меняется только порядок запросов, Referer-политика и `alt=""` без изменений |
| T-16-02 | Tampering | `News.tsx`, вычисление `priority` из `result.page` и индекса | accept | `paginate` зажимает страницу в [1, totalPages]; приоритет получает ровно одна карточка, что проверяет `News.test.tsx` |
| T-16-03 | Elevation of privilege | `ConsentCheckbox.tsx`, label 44px переключает согласие | mitigate | валидация согласия в `LightForm.tsx` не меняется: пустое согласие даёт ошибку и фокус на чекбоксе (существующие тесты); увеличенная label не добавляет обработчиков, только нативную связь `htmlFor` |
| T-16-04 | Denial of service | `Footer.css`, соседство боксов ссылок 44px | mitigate | `gap: 0` без отрицательных `margin`: боксы стоят встык и не перекрываются, касание по краю остаётся в своей ссылке; тест Footer запрещает `margin-block`/`margin-top`/`margin-bottom` в блоке ссылки и `gap: 8px` в файле |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | фаза не устанавливает пакеты; `package.json` и `package-lock.json` не меняются (проверка через `git status --short`) |
</threat_model>

<verification>
- `npx vitest run src/components/layout src/components/form src/components/news src/App.seams.test.tsx` зелёный
- `npx tsc -b` и `npm run lint` без ошибок
- `git status --short` показывает только одиннадцать файлов из `files_modified`; `Footer.tsx`, `Header.tsx`, `Wordmark.tsx`, `LightForm.tsx`, `FormField.tsx`, `news.css`, `src/styles/global.css`, `src/components/map/*`, `src/components/hero/*`, `src/App.seams.test.tsx`, `package.json`, `package-lock.json` не тронуты
- Визуальная приёмка на 390×844 (аудит целей касания, шаг столбца ссылок футера, кегль подписи) выполняется фазой 17 (SHIP-03), в этой фазе не требуется
</verification>

<success_criteria>
1. `.site-footer__links a` описана как `display: inline-flex; align-items: center; min-height: 44px; padding-inline: 8px`, список с `gap: 0`: шаг столбца 44px вместо 30.4px, боксы соседних ссылок не перекрываются, текст центрирован в боксе; тест Footer фиксирует оба блока (MOB-01)
2. `.lf-check` с `min-height: 44px; display: flex; align-items: center; gap: 12px`, `.lf-checkbox` 20×20 с `margin: 0`; клик по тексту согласия переключает чекбокс (тест зелёный), ошибка «Нужно согласие на обработку данных» показывается как раньше, `.lf-section` без `background` (MOB-02)
3. Все `<img>` карточек новостей несут `width="480" height="360" decoding="async"`; первая карточка первой страницы `loading="eager"` и `fetchpriority="high"`, остальные и вся вторая страница `loading="lazy"`; `object-cover`, `coverZoom`, `coverPosition`, `news.css` не изменились (MOB-03)
4. Подпись «МИССИЯ ДЛЯ ВСЕХ» в шапке `font-size: .625rem` с `letter-spacing: .16em` на любой ширине, `.5625rem` в Header.css нет; вордмарк футера `clamp(.625rem, 1.1vw, .8125rem)` без изменений (MOB-04)
5. `npx vitest run src/components/layout src/components/form src/components/news src/App.seams.test.tsx`, `npx tsc -b`, `npm run lint` зелёные; файлы «Не трогать» не изменены
</success_criteria>

<output>
Создать `.planning/phases/16-mobile/16-01-SUMMARY.md` по завершении.
</output>
