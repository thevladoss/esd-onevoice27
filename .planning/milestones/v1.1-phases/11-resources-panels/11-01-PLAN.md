---
phase: 11-resources-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/resourceFiles.ts
  - src/data/resourceFiles.test.ts
  - src/data/materials.ts
  - src/data/copy.resources.ts
  - src/components/resources/VideoGrid.test.tsx
autonomous: true
requirements: [RES-05]
must_haves:
  truths:
    - "musicFiles содержит три файла оригинала с адресами из RES-05: Sheet Music PDF, Theme Song (no vocals) ZIP, Music Lyrics MOV; все с action download"
    - "materialGroups содержит пять групп в порядке esd, en, es, pt, fr; esd раскрыта по умолчанию и собрана из четырёх позиций ЕАД (DOCX + три WEB); en состоит из шести файлов оригинала и папки SharePoint «Materials (English)»; es, pt, fr по пять файлов"
    - "videoFiles содержит одну карточку «Video backgrounds» ZIP с адресом SharePoint из RES-05"
    - "resourcesCopy отдаёт заголовки панелей «Музыка» / «Материалы» / «Видео», описания из RES-05, подписи «Назад», «Скачать», «Открыть» и названия пяти групп"
    - "Акценты карточек в copy.resources.ts записаны литералами rgb(143 157 214), rgb(123 194 199), rgb(210 142 190)"
    - "Все id файлов уникальны, все href начинаются с https://, у каждого файла type из закрытого списка"
  artifacts:
    - path: src/data/resourceFiles.ts
      provides: "типы ResourceFile, ResourceGroup, ResourceFileType, ResourceFileAction и наборы musicFiles, materialGroups, videoFiles"
      exports: [ResourceFile, ResourceGroup, ResourceFileType, ResourceFileAction, musicFiles, materialGroups, videoFiles]
      contains: "PIl1787819788226.pdf"
    - path: src/data/resourceFiles.test.ts
      provides: "тест на состав групп, уникальность id, адреса и строки панелей"
      min_lines: 60
    - path: src/data/materials.ts
      provides: "esdMaterials (четыре позиции ЕАД) и englishFolder (папка SharePoint)"
      exports: [esdMaterials, englishFolder, materials, MaterialItem, MaterialKind]
    - path: src/data/copy.resources.ts
      provides: "ResourceGroupId, resourcesCopy.panels, resourcesCopy.groups, resourcesCopy.panel.back/download/open"
      contains: "Официальная песня движения и материалы к ней"
  key_links:
    - from: src/data/resourceFiles.ts
      to: src/data/materials.ts
      via: "группа esd строится из esdMaterials, папка SharePoint берётся из englishFolder"
      pattern: "from \"./materials\""
    - from: src/data/resourceFiles.ts
      to: src/data/copy.resources.ts
      via: "названия групп берутся из resourcesCopy.groups"
      pattern: "resourcesCopy\\.groups"
---

<objective>
Дать панелям ресурсов типизированные данные и тексты: новый модуль `src/data/resourceFiles.ts` со всеми адресами файлов оригинала и ЕАД из RES-05, разделение `materials.ts` на группу ЕАД и папку SharePoint, тексты панелей, кнопок и групп в `copy.resources.ts`, акценты карточек литералами. Тест на состав и ссылки живёт рядом с данными.

Purpose: план 11-03 наполняет полноэкранные панели из этих наборов без единой строки-адреса в компонентах; план 11-02 берёт акценты карточек из copy. Оба плана собираются против контрактов, зафиксированных здесь.
Output: `src/data/resourceFiles.ts`, `src/data/resourceFiles.test.ts`, правки `src/data/materials.ts`, `src/data/copy.resources.ts`, перенос data-тестов из `VideoGrid.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resources-panels/11-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/data/copy.resources.ts
@src/data/materials.ts
@src/data/videos.ts
@src/components/resources/VideoGrid.test.tsx

<ownership>
Фаза 11 идёт параллельно с фазами 7–10 и 12 в отдельном worktree. Этот план правит только пять файлов из `files_modified`. Не трогать: `src/styles/global.css`, `src/components/layout/primitives.css`, `src/components/layout/Header.css`, `src/components/about/VideoEmbed.tsx`, `src/components/about/*`, `src/components/news/*`, любые файлы других секций. Новые файлы создавать только в `src/data/`.
</ownership>

<interfaces>
<!-- Текущие контракты, против которых пишется план. Использовать напрямую, кодовую базу не исследовать. -->

Из src/data/materials.ts (сейчас):
```typescript
export type MaterialKind = "document" | "image" | "book" | "phone" | "folder";
export type MaterialItem = { id: string; title: string; caption: string; href: string; kind: MaterialKind };
export const materials: readonly MaterialItem[]; // 5 позиций: project-description (.docx), banners, desire-of-ages, wallpapers, english (SharePoint)
```

Из src/data/copy.resources.ts (сейчас):
```typescript
export type ResourceKey = "music" | "materials" | "video";
export const resourceKeys: readonly ResourceKey[];
export const resourcesCopy: {
  eyebrow: string; title: string; body: string;
  cards: Record<ResourceKey, { label: string; title: string; description: string; cta: string; accent: string }>;
  panel: { close: string; closeLabel: string };
  music: { emptyTitle: string; emptyBody: string };
};
```

Потребители, которые сейчас читают эти модули и должны продолжать компилироваться после плана: `src/components/resources/ResourcePanel.tsx` (`resourcesCopy.panel.close`, `panel.closeLabel`), `MusicPlaceholder.tsx` (`resourcesCopy.music.emptyTitle/emptyBody`), `MaterialsList.tsx` (`materials`), `Resources.test.tsx` (`materials`, ожидает 5 ссылок). Их убирает план 11-03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Тексты панелей, акценты литералами и разделение materials.ts</name>
  <files>src/data/copy.resources.ts, src/data/materials.ts</files>
  <read_first>
    - src/data/copy.resources.ts (полностью)
    - src/data/materials.ts (полностью)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел 5: RES-02 (акценты) и RES-05 (описания панелей)
    - .planning/phases/11-resources-panels/11-CONTEXT.md, раздел «Данные»
  </read_first>
  <action>
    **copy.resources.ts.** Добавить экспорт типа `ResourceGroupId = "esd" | "en" | "es" | "pt" | "fr"`. Расширить тип и объект `resourcesCopy`:
    - `cards.*.accent` заменить на литералы по RES-02: music `rgb(143 157 214)`, materials `rgb(123 194 199)`, video `rgb(210 142 190)`. Комментарий у поля: цвет уходит в custom property `--accent` карточки, токены проекта сдвинуты относительно палитры оригинала, поэтому литерал.
    - `panel`: добавить `back: "Назад"`, `download: "Скачать"`, `open: "Открыть"`. Поля `close` и `closeLabel` оставить с комментарием «читает старый ResourcePanel, снимает план 11-03».
    - Новое поле `panels: Record<ResourceKey, { title: string; description: string }>`: music `{ title: "Музыка", description: "Официальная песня движения и материалы к ней. Пока это материалы глобального проекта на английском: версия дивизиона готовится." }`, materials `{ title: "Материалы", description: "Материалы дивизиона на русском и ресурсы глобального проекта на других языках, готовые к скачиванию." }`, video `{ title: "Видео", description: "16 роликов дивизиона и видеофоны глобального проекта." }`. Тексты дословно из RES-05.
    - Новое поле `groups: Record<ResourceGroupId, string>`: esd «Материалы ЕАД (на русском)», en «English resources», es «Spanish resources», pt «Portuguese resources», fr «French resources».
    - Блок `music: { emptyTitle, emptyBody }` оставить с комментарием «читает MusicPlaceholder, снимает план 11-03».

    **materials.ts.** Разделить массив: `export const esdMaterials: readonly MaterialItem[]` с четырьмя позициями ЕАД (project-description, banners, desire-of-ages, wallpapers, содержимое без изменений) и `export const englishFolder: MaterialItem` с текущей позицией `english` (SharePoint `https://gcsda.sharepoint.com/:f:/s/digitalmediateam/EtNk5tgXbZRMiJ7Nk1nOlRsBhAq-Xx8Oc7vfWiJzI86OJA?e=sSsYtg`). Экспорт `materials` оставить как `[...esdMaterials, englishFolder]` с комментарием «читают MaterialsList и Resources.test, снимает план 11-03». Типы `MaterialKind` и `MaterialItem` не менять. Все комментарии на русском.
  </action>
  <verify>
    <automated>npx tsc -b && grep -c "rgb(143 157 214)" src/data/copy.resources.ts | grep -q 1 && grep -q "englishFolder" src/data/materials.ts</automated>
  </verify>
  <acceptance_criteria>
    - `resourcesCopy.cards.music.accent === "rgb(143 157 214)"`, `.materials.accent === "rgb(123 194 199)"`, `.video.accent === "rgb(210 142 190)"`; строк `var(--color-` в `copy.resources.ts` нет
    - `resourcesCopy.panel.back === "Назад"`, `.download === "Скачать"`, `.open === "Открыть"`; `panel.close`/`panel.closeLabel` на месте
    - `resourcesCopy.panels.music.title === "Музыка"`, `.materials.title === "Материалы"`, `.video.title === "Видео"`; описания совпадают с RES-05 посимвольно
    - `resourcesCopy.groups` содержит пять ключей `esd, en, es, pt, fr` с названиями из решения CONTEXT
    - `esdMaterials.length === 4`, ни одного `kind: "folder"` внутри; `englishFolder.kind === "folder"`, href содержит `sharepoint.com`; `materials.length === 5`
    - `npx tsc -b` без ошибок (старые потребители компилируются)
  </acceptance_criteria>
  <done>Тексты панелей, кнопок и групп читаются из `resourcesCopy`, акценты литералами, `materials.ts` отдаёт `esdMaterials` и `englishFolder`, проект компилируется.</done>
</task>

<task type="auto">
  <name>Task 2: Модуль resourceFiles.ts с наборами файлов по RES-05</name>
  <files>src/data/resourceFiles.ts</files>
  <read_first>
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел 5, RES-05 (все адреса)
    - .planning/phases/11-resources-panels/11-CONTEXT.md, раздел «Данные» (типы)
    - src/data/materials.ts (после Task 1: esdMaterials, englishFolder)
    - src/data/copy.resources.ts (после Task 1: resourcesCopy.groups, ResourceGroupId)
  </read_first>
  <action>
    Создать `src/data/resourceFiles.ts`. Типы ровно по CONTEXT: `ResourceFileType = "pdf" | "zip" | "mov" | "pptx" | "png" | "mp4" | "docx" | "web"`, `ResourceFileAction = "download" | "open"`, `ResourceFile = { id: string; name: string; type: ResourceFileType; href: string; action: ResourceFileAction }`, `ResourceGroup = { id: ResourceGroupId; title: string; files: readonly ResourceFile[]; open?: boolean }`. Модульная константа `HOPE = "https://hope-documents.fra1.digitaloceanspaces.com/67054013a60919c92d92c959/"`; адреса в наборах ниже записаны как `HOPE + "<имя файла>"`, SharePoint-адреса целиком литералами. Названия файлов на языке оригинала, как в спецификации.

    `musicFiles` (все `action: "download"`):
    1. id `music-sheet`, name «OneVoice27 Song — Sheet Music», type `pdf`, `PIl1787819788226.pdf`
    2. id `music-no-vocals`, name «One Voice Theme Song (no vocals)», type `zip`, `vVx1787819861503.zip`
    3. id `music-lyrics-video`, name «Video Music 2 — Music Lyrics», type `mov`, href `https://gcsda.sharepoint.com/sites/digitalmediateam/Public/OneVoice%202027/Music/Video%20Music%202%20-%20OneVoice27%20-%20Music%20Lyrics.mov&download=1`

    `materialGroups` (порядок esd, en, es, pt, fr; `title` из `resourcesCopy.groups[id]`):
    - `esd`, `open: true`: функция `materialToFile(item: MaterialItem): ResourceFile` над `esdMaterials`: id `esd-${item.id}`, name `item.title`; если `item.href` заканчивается на `.docx`, то `type: "docx"`, `action: "download"`, иначе `type: "web"`, `action: "open"`. Итог: описание проекта DOCX со «Скачать», три страницы ЕАД с бейджем WEB и «Открыть» (так в спецификации; формулировка ROADMAP «4 позиции с бейджем WEB» уступает спецификации по правилу CONTEXT).
    - `en`: `en-template` «OneVoice27 template» pptx `S4z1787584266485.pptx`; `en-presentation` «OneVoice27 Presentation» pdf `R4n1787584270370.pdf`; `en-brochure` «OneVoice27 Brochure» pdf `pGK1787584274551.pdf`; `en-logo` «ENG logo» png `omL1787584642273.png`; `en-logo-adventist` «ENG logo Adventist» png `wiS1787584642347.png`; `en-files` «OneVoice27 files» zip `uq31788340270047.zip`; последним `en-sharepoint` «Materials (English)» type `web`, action `open`, href `englishFolder.href`. Остальные `action: "download"`.
    - `es` (все download): `es-logotype-1` «Logotype 1» png `FDj1787819159965.png`; `es-logotype-2` «Logotype 2» png `IVv1787819160044.png`; `es-folleto` «UnaVoz27 Folleto» pdf `Ofg1787819160083.pdf`; `es-plantilla` «UnaVoz27 Plantilla» pptx `sg61787819160553.pptx`; `es-presentacion` «UnaVoz27 Presentación» pdf `aPl1787819163536.pdf`.
    - `pt` (все download): `pt-logo-1` «Logo 1» png `xO01787818991806.png`; `pt-logo-2` «Logo 2» png `jON1787818991847.png`; `pt-apresentacao` «UmaVoz27 Apresentação» pdf `Ta71787818991954.pdf`; `pt-folheto` «UmaVoz27 Folheto» pdf `HKu1787818996618.pdf`; `pt-plantilha` «UmaVoz27 Plantilha» pptx `vdv1787818996782.pptx`.
    - `fr` (все download): `fr-logo-1` «Logo 1» png `AaN1787818396083.png`; `fr-logo-2` «Logo 2» png `NNA1787818396147.png`; `fr-brochure` «UneVoix27 Brochure» pdf `KtA1787818396181.pdf`; `fr-modele` «UneVoix27 Modèle» pptx `Wy91787818396696.pptx`; `fr-presentation` «UneVoix27 Présentation» pdf `0iv1787818401338.pdf`.

    `videoFiles`: один файл `video-backgrounds` «Video backgrounds», type `zip`, action `download`, href `https://gcsda.sharepoint.com/sites/digitalmediateam/Public/Forms/AllItems.aspx?viewid=9de9ca49%2D7ff1%2D4e2c%2Da9c7%2D62b9d5ad1201&ga=1&id=%2Fsites%2Fdigitalmediateam%2FPublic%2FOneVoice%202027%2FVideo%2DBackgrounds%2Ezip&parent=%2Fsites%2Fdigitalmediateam%2FPublic%2FOneVoice%202027`.

    Все наборы `readonly` и `as const` там, где это не мешает типу `ResourceFile`. Комментарии на русском: откуда взяты адреса (RES-05, оригинал onevoice27.org) и почему группа esd строится из `esdMaterials`.
  </action>
  <verify>
    <automated>npx tsc -b && grep -c "1787" src/data/resourceFiles.ts | awk '$1>=23{exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - Файл экспортирует `ResourceFile`, `ResourceGroup`, `ResourceFileType`, `ResourceFileAction`, `musicFiles`, `materialGroups`, `videoFiles`
    - `musicFiles.length === 3`, типы `pdf`, `zip`, `mov` в этом порядке; href первого равен `https://hope-documents.fra1.digitaloceanspaces.com/67054013a60919c92d92c959/PIl1787819788226.pdf`
    - `materialGroups.map(g => g.id)` равен `["esd", "en", "es", "pt", "fr"]`; только `esd` имеет `open: true`
    - Длины `files`: esd 4, en 7, es 5, pt 5, fr 5; в esd ровно один `docx`/`download` и три `web`/`open`; последний файл en имеет `type: "web"`, `action: "open"`, href `englishFolder.href`
    - `videoFiles.length === 1`, type `zip`, href содержит `Video%2DBackgrounds%2Ezip`
    - Все id по всем наборам уникальны; все href начинаются с `https://`
    - В файле нет импортов из `src/components/*`
  </acceptance_criteria>
  <done>`resourceFiles.ts` содержит все адреса RES-05 дословно, типы совпадают с CONTEXT, группы собраны в нужном порядке.</done>
</task>

<task type="auto">
  <name>Task 3: Тест данных ресурсов и перенос data-describe из VideoGrid.test.tsx</name>
  <files>src/data/resourceFiles.test.ts, src/components/resources/VideoGrid.test.tsx</files>
  <read_first>
    - src/components/resources/VideoGrid.test.tsx (describe «данные ресурсов» переезжает)
    - src/data/about.test.ts (стиль data-тестов проекта)
    - src/data/resourceFiles.ts, src/data/materials.ts, src/data/copy.resources.ts (после Task 1–2)
  </read_first>
  <action>
    Создать `src/data/resourceFiles.test.ts` (vitest globals, без React). Сценарии:
    1. «musicFiles: три файла оригинала»: длина 3, типы `["pdf","zip","mov"]`, все `action === "download"`, полные href первых двух равны `HOPE`-адресам из RES-05 (строки целиком), третий содержит `Music%20Lyrics.mov`.
    2. «materialGroups: пять групп в порядке оригинала»: id `["esd","en","es","pt","fr"]`, `title` каждой равен `resourcesCopy.groups[id]`, `open === true` только у esd.
    3. «группа ЕАД собрана из esdMaterials»: 4 файла, href совпадают с `esdMaterials.map(m => m.href)` в том же порядке, первый `docx`/`download`, остальные `web`/`open`.
    4. «English заканчивается папкой SharePoint»: длина 7, последний файл `web`/`open` с href `englishFolder.href`, среди типов есть `pptx`, `pdf`, `png`, `zip`.
    5. «Spanish, Portuguese, French по пять файлов»: длины 5, у каждой группы два `png`, два `pdf`, один `pptx`.
    6. «videoFiles: Video backgrounds ZIP»: длина 1, type `zip`, href содержит `Video%2DBackgrounds%2Ezip`.
    7. «id уникальны, адреса абсолютные, типы из списка»: собрать все файлы из трёх наборов, `new Set(ids).size === total`, каждый href `startsWith("https://")`, каждый type входит в `["pdf","zip","mov","pptx","png","mp4","docx","web"]`.
    8. Перенесённые из VideoGrid.test.tsx: «videos содержит 16 уникальных роликов ЕАД» (без изменений), «materials разделён на ЕАД и папку SharePoint» (переписать: `esdMaterials.length === 4`, среди href есть `.docx`, `/materials/banners`, `/materials/desire-of-ages`, `/materials/wallpapers`, ни одного `sharepoint.com`; `englishFolder.href` содержит `sharepoint.com`, `kind === "folder"`; `materials.length === 5`), «resourcesCopy отдаёт строки карточек и панелей» (`cards.materials.cta === "Открыть материалы"`, `cards.music.label === "МУЗЫКА"`, `panels.music.title === "Музыка"`, `panel.back === "Назад"`, `panel.download === "Скачать"`, `panel.open === "Открыть"`, `cards.music.accent === "rgb(143 157 214)"`). Проверку `music.emptyTitle` не переносить.

    В `VideoGrid.test.tsx` удалить describe «данные ресурсов» и неиспользуемые импорты `materials`, `resourcesCopy`; describe «VideoGrid» оставить без изменений.
  </action>
  <verify>
    <automated>npx vitest run src/data/resourceFiles.test.ts src/components/resources/VideoGrid.test.tsx && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/data/resourceFiles.test.ts` зелёный, не меньше 10 сценариев
    - `npx vitest run src/components/resources/VideoGrid.test.tsx` зелёный, в файле нет строки `emptyTitle` и импорта `materials`
    - `npx tsc -b` и `npm run lint` без ошибок
    - `npx vitest run src/components/resources/Resources.test.tsx` тоже зелёный: экспорт `materials` сохранён, старая панель по-прежнему показывает 5 ссылок
  </acceptance_criteria>
  <done>Данные ресурсов покрыты тестом рядом с модулем, VideoGrid.test.tsx проверяет только компонент, весь затронутый набор зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Данные → внешние хосты | href ведут на hope-documents.fra1.digitaloceanspaces.com, gcsda.sharepoint.com, esd.onevoice27.org; проект только ссылается, ничего не загружает |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | src/data/resourceFiles.ts (адреса) | mitigate | адреса захардкожены литералами из спецификации, тест сверяет полные строки для музыки и `startsWith("https://")` для всех; пользовательского ввода в href нет |
| T-11-02 | Spoofing | ссылки на SharePoint / DigitalOcean Spaces | accept | домены принадлежат глобальному проекту и ГК; рендер в 11-03 ставит `target="_blank" rel="noopener noreferrer"` (тест motionPolicy это проверяет) |
| T-11-SC | Tampering | npm-установки | accept | план не устанавливает пакетов; при появлении install-задачи нужен Package Legitimacy Audit из RESEARCH |
</threat_model>

<verification>
- `npx vitest run src/data/resourceFiles.test.ts src/components/resources/VideoGrid.test.tsx src/components/resources/Resources.test.tsx` зелёный
- `npx tsc -b` без ошибок, `npm run lint` без ошибок
- `git status` показывает изменения только в пяти файлах плана
</verification>

<success_criteria>
- Все адреса RES-05 присутствуют в `resourceFiles.ts` дословно, состав групп и порядок соответствуют оригиналу
- `copy.resources.ts` отдаёт заголовки, описания, кнопки и названия групп; акценты литералами
- `materials.ts` разделён на `esdMaterials` и `englishFolder`, старые потребители компилируются
- Тесты данных зелёные, `tsc -b` и `lint` чистые
</success_criteria>

<output>
Create `.planning/phases/11-resources-panels/11-01-SUMMARY.md` when done
</output>
