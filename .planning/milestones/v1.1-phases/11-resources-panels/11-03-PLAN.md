---
phase: 11-resources-panels
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - src/components/resources/ResourcePanel.tsx
  - src/components/resources/FileCard.tsx
  - src/components/resources/FileGroup.tsx
  - src/components/resources/ResourcePanel.test.tsx
  - src/components/resources/Resources.tsx
  - src/components/resources/Resources.test.tsx
  - src/components/resources/resources.css
  - src/components/resources/MusicPlaceholder.tsx
  - src/components/resources/MaterialsList.tsx
  - src/data/copy.resources.ts
  - src/data/materials.ts
autonomous: true
requirements: [RES-03, RES-04, RES-06]
must_haves:
  truths:
    - "Клик по карточке открывает полноэкранную панель поверх шапки: контейнер fixed inset 0 z-index 10000 порталом в body, слои rgb(132 53 127) и rgb(59 77 161) въезжают справа за 620ms со сдвигом 90ms, панель проявляется с задержкой 180ms"
    - "Пока панель открыта, страница под ней не прокручивается (класс resources-panel-locked на html и body), фокус стоит на кнопке «Назад», Tab не выходит за пределы панели"
    - "Escape и «Назад» закрывают панель обратной анимацией (панель гаснет за 180ms, слои уезжают с задержкой 180ms) и возвращают фокус на карточку; #resources-materials и клик по a[href=\"#resources-materials\"] открывают материалы"
    - "При prefers-reduced-motion панель открывается и закрывается без промежуточных фаз; JS-состояние переключается по transitionend или fallback-таймеру"
    - "Внутри панели: кнопка «Назад», заголовок, описание, сетка файлов repeat(auto-fill, minmax(min(100%, 17.5rem), 1fr)), карточки файлов с бейджем типа и кнопкой «Скачать»/«Открыть», языковые группы details"
    - "«Музыка» показывает три файла оригинала; «Материалы» показывает раскрытую группу ЕАД (DOCX + три WEB) и группы English/Spanish/Portuguese/French; «Видео» показывает 16 роликов и карточку «Video backgrounds» ZIP; файлов MusicPlaceholder.tsx и MaterialsList.tsx в коде нет"
    - "При открытой панели секция красится в rgb(22 29 61) / rgb(25 47 54) / rgb(50 16 47) через is-*-active, атмосфера и частицы на месте"
  artifacts:
    - path: src/components/resources/ResourcePanel.tsx
      provides: "полноэкранный хост панелей: портал, фазы closed/opening/open/closing, scroll lock, фокус, Escape, ловушка Tab"
      exports: [ResourcePanel]
      contains: "createPortal"
    - path: src/components/resources/FileCard.tsx
      provides: "карточка файла: название, бейдж типа, ссылка «Скачать»/«Открыть»"
      exports: [FileCard]
    - path: src/components/resources/FileGroup.tsx
      provides: "языковая группа details/summary с сеткой файлов"
      exports: [FileGroup]
    - path: src/components/resources/resources.css
      provides: "правила .resources-panels*, .resources-panel*, .resources-file*, .resources-group*, html/body.resources-panel-locked"
      contains: "z-index: 10000"
    - path: src/components/resources/ResourcePanel.test.tsx
      provides: "тесты хоста: содержимое трёх панелей, фазы, scroll lock, фокус, Escape, Tab, reduced motion, transitionend"
      min_lines: 150
  key_links:
    - from: src/components/resources/Resources.tsx
      to: src/components/resources/ResourcePanel.tsx
      via: "<ResourcePanel active={active} onClose={close} />"
      pattern: "<ResourcePanel active="
    - from: src/components/resources/ResourcePanel.tsx
      to: src/data/resourceFiles.ts
      via: "musicFiles, materialGroups, videoFiles"
      pattern: "from \"../../data/resourceFiles\""
    - from: src/components/resources/ResourcePanel.tsx
      to: document.body
      via: "createPortal + классы resources-panel-locked на documentElement и body"
      pattern: "resources-panel-locked"
    - from: src/components/resources/ResourcePanel.tsx
      to: src/components/about/VideoEmbed.tsx
      via: "VideoGrid с size=\"compact\" (VideoEmbed не править)"
      pattern: "<VideoGrid"
---

<objective>
Заменить раскрывающуюся под сеткой панель на полноэкранные панели оригинала: fixed-контейнер порталом в `document.body` с двумя цветными слоями и шторным переходом, панель с кнопкой «Назад», заголовком, описанием, сеткой карточек файлов и языковыми группами `<details>`, наполнение из `resourceFiles.ts` и `VideoGrid`. Scroll lock, фокус, Escape, ловушка Tab, deep link и reduced motion по RES-03. Старые `MusicPlaceholder` и `MaterialsList` удаляются вместе с легаси-строками.

Purpose: пункт 5 правок пользователя («при клике: если у ЕАД нет аналога, вставить английские материалы оригинала»). Закрывает RES-03, RES-04, RES-06 и завершает RES-05 (удаление заглушки).
Output: новый `ResourcePanel.tsx` (хост), `FileCard.tsx`, `FileGroup.tsx`, `ResourcePanel.test.tsx`; правки `Resources.tsx`, `Resources.test.tsx`, `resources.css`, `copy.resources.ts`, `materials.ts`; удаление `MusicPlaceholder.tsx`, `MaterialsList.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resources-panels/11-CONTEXT.md
@.planning/phases/11-resources-panels/11-01-SUMMARY.md
@.planning/phases/11-resources-panels/11-02-SUMMARY.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@docs/research/v1.1/orig-rules.css
@docs/research/v1.1/dom-ov-resources-panels.html
@docs/research/v1.1/orig-panel-music.jpeg
@docs/research/v1.1/orig-panel-materials.jpeg
@src/components/resources/Resources.tsx
@src/components/resources/ResourcePanel.tsx
@src/components/resources/VideoGrid.tsx
@src/components/resources/resources.css
@src/components/resources/Resources.test.tsx
@src/data/resourceFiles.ts
@src/data/copy.resources.ts
@src/lib/useReducedMotion.ts
@src/styles/motionPolicy.test.ts

<ownership>
Фаза 11 идёт параллельно с фазами 7–10 и 12 в отдельном worktree. Этот план правит только файлы из `files_modified`; новые компоненты создаются в `src/components/resources/`. Не трогать: `src/styles/global.css`, `src/components/layout/primitives.css`, `src/components/layout/Header.css` (шапка z-index 40, бургер 42: панель z 10000 порталом в body оказывается выше без правок шапки), `src/components/about/VideoEmbed.tsx` (вызывать через `VideoGrid` с текущим API `size="compact"`), `src/components/about/*`, `src/components/news/*`, `src/components/involve/*`, `src/lib/*` (`scrollLock.ts` не использовать и не править: он фиксирует body и возвращает прокрутку на исходную позицию, что ломает deep link с прокруткой к секции; блокировка идёт классом на html/body с правилом в `resources.css`).

Reduced motion: в `resources.css` не добавлять `@media (prefers-reduced-motion: reduce)` (тест `src/styles/motionPolicy.test.ts` допускает единственный блок в `global.css`, он гасит любые переходы через `transition-duration: 0.01ms !important`). Атрибуты `data-anim="particles"` и `data-anim="atmosphere"` сохранить, новых значений `data-anim` не заводить, на слои панели `data-anim` не вешать. Нигде не писать `outline: none` и его варианты; `target="_blank"` и `rel="noopener noreferrer"` держать в одной строке JSX (оба требования проверяет motionPolicy).

Цвета в CSS: литералы `rgb(r g b / a)` из спецификации. Палитра для бейджей, которые спецификация задаёт именем семейства: pptx (unity) рамка `rgb(143 157 214 / .66)`, цвет `rgb(184 192 230)`, фон `rgb(48 63 131 / .2)`; png (horizon) рамка `rgb(123 194 199 / .66)`, цвет `rgb(231 245 245)`, фон `rgb(51 94 104 / .2)`; zip рамка `rgb(219 215 232 / .4)`, цвет `rgb(248 247 251 / .88)`.
</ownership>

<interfaces>
<!-- Контракты из планов 11-01 и 11-02 и существующих модулей. Использовать напрямую. -->

Из src/data/resourceFiles.ts (план 11-01):
```typescript
export type ResourceFileType = "pdf" | "zip" | "mov" | "pptx" | "png" | "mp4" | "docx" | "web";
export type ResourceFileAction = "download" | "open";
export type ResourceFile = { id: string; name: string; type: ResourceFileType; href: string; action: ResourceFileAction };
export type ResourceGroup = { id: ResourceGroupId; title: string; files: readonly ResourceFile[]; open?: boolean };
export const musicFiles: readonly ResourceFile[];        // 3
export const materialGroups: readonly ResourceGroup[];   // esd(open, 4), en(7), es(5), pt(5), fr(5)
export const videoFiles: readonly ResourceFile[];        // 1: Video backgrounds zip
```

Из src/data/copy.resources.ts (план 11-01):
```typescript
export type ResourceKey = "music" | "materials" | "video";
export type ResourceGroupId = "esd" | "en" | "es" | "pt" | "fr";
resourcesCopy.panel: { back: "Назад"; download: "Скачать"; open: "Открыть"; close: string; closeLabel: string }; // close/closeLabel удаляет этот план
resourcesCopy.panels: Record<ResourceKey, { title: string; description: string }>;
resourcesCopy.groups: Record<ResourceGroupId, string>;
resourcesCopy.music: { emptyTitle: string; emptyBody: string }; // удаляет этот план
```

Из src/data/materials.ts (план 11-01): `esdMaterials`, `englishFolder`, легаси `materials` (удаляет этот план).

Из src/components/resources/VideoGrid.tsx (не менять):
```typescript
export function VideoGrid({ items = videos, className = "grid-cols-2 md:grid-cols-3 lg:grid-cols-4" }: { items?: readonly VideoItem[]; className?: string });
// рендерит <ul class="grid gap-4 …"> из 16 <li> с VideoEmbed size="compact"; фасад = <button> с именем «Смотреть видео: …»
```

Из src/lib/useReducedMotion.ts (не менять):
```typescript
export function prefersReducedMotion(): boolean;     // синхронно, matchMedia
export function usePrefersReducedMotion(): boolean;  // useSyncExternalStore
```

Из src/components/resources/Resources.tsx (план 11-02): состояние `active: ResourceKey | null`, `cardRefs`, `toggle(kind)`, `close()` (setActive(null) + фокус на карточку-триггер), эффект deep link (`MATERIALS_HASH`, `hashchange`, делегированный клик), класс секции `is-${active}-active`, атмосфера `data-kind={active ?? "none"}`. Карточка при `isOpen` ставит `aria-controls="resources-panel"`.

Тесты: `src/test/setup.ts` мокает `window.matchMedia` (matches: false), `scrollIntoView`, `scrollTo`; jest-dom подключён; vitest globals включены.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Компоненты FileCard и FileGroup, CSS панелей, слоёв и scroll lock</name>
  <files>src/components/resources/FileCard.tsx, src/components/resources/FileGroup.tsx, src/components/resources/resources.css</files>
  <read_first>
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел 5: RES-03 (контейнер, слои, тайминги, фон панели), RES-04 (кнопка «Назад», заголовок, описание, сетка и карточка файла, бейдж, кнопка «Скачать», группы)
    - docs/research/v1.1/orig-rules.css: блоки `#ov-resources-panels*`, `.ov-resources-panel*`, `.ov-resources-file*`, `.ov-resources-language-group*`, `html.ov-resources-panel-locked`
    - docs/research/v1.1/dom-ov-resources-panels.html (порядок узлов: header → copy → list; file-item → meta (name, type) → action)
    - src/components/resources/resources.css (после 11-02; блоки `.resources-panel-wrap*` и старый `.resources-panel` удалить)
    - src/data/resourceFiles.ts, src/data/copy.resources.ts
  </read_first>
  <action>
    **FileCard.tsx.** `export function FileCard({ file }: { file: ResourceFile })` рендерит `<li className="resources-file">`: блок `<div className="resources-file__meta">` с `<p className="resources-file__name" id={`resources-file-${file.id}`}>{file.name}</p>` и `<span className="resources-file__type" data-file-type={file.type}>{file.type.toUpperCase()}</span>`; затем `<a className="resources-file__action" data-action={file.action} href={file.href} target="_blank" rel="noopener noreferrer" aria-label={`${label}: ${file.name}`}>{label}</a>`, где `label` равен `resourcesCopy.panel.download` для `action === "download"` и `resourcesCopy.panel.open` для `"open"`. Стрелку «↓» / «→» рисует CSS через `::after`, в разметке её нет.

    **FileGroup.tsx.** `export function FileGroup({ group }: { group: ResourceGroup })` рендерит `<details className="resources-group" id={`resources-group-${group.id}`} open={group.open}>`: `<summary className="resources-group__summary"><span className="resources-group__title">{group.title}</span><svg className="resources-group__chevron" aria-hidden="true" width="20" height="20" viewBox="0 0 20 20">` со штрихом-шевроном «›» `</svg></summary>` и `<div className="resources-group__inner"><ul className="resources-files">{group.files.map(file => <FileCard key={file.id} file={file} />)}</ul></div>`.

    **resources.css.** Удалить блоки `.resources-panel-wrap`, `.resources-panel-wrap[data-open="true"]` и старый `.resources-panel { background: … var(--glass-surface) }`. Добавить:

    Контейнер `.resources-panels`: `position: fixed; inset: 0; z-index: 10000; width: 100vw; height: 100dvh; visibility: hidden; pointer-events: none`. Слои `.resources-panels::before, .resources-panels::after { content: ""; position: absolute; inset: 0; transform: translate3d(100%, 0, 0); transition: transform 620ms cubic-bezier(.22, 1, .36, 1) }`; `::before { z-index: 0; background: rgb(132 53 127) }`; `::after { z-index: 1; background: rgb(59 77 161); transition-delay: 90ms }`. Состояния: `.resources-panels.is-opening, .is-open, .is-closing { visibility: visible; pointer-events: auto }`; `.resources-panels.is-open::before, .resources-panels.is-open::after { transform: translate3d(0, 0, 0) }`; `.resources-panels.is-closing::before, .resources-panels.is-closing::after { transition-delay: 180ms }`.

    Панель `.resources-panel`: `position: absolute; z-index: 2; inset: 0; width: 100%; height: 100%; min-height: 100svh; overflow: hidden auto; overscroll-behavior: contain; color: rgb(248 247 251 / .96); background: radial-gradient(circle at 15% 12%, rgb(79 93 175 / .2), transparent 38%), radial-gradient(circle at 86% 84%, rgb(57 114 126 / .16), transparent 42%), rgb(18 12 52 / .985); opacity: 0; transform: translate3d(8%, 0, 0); transition: opacity 620ms ease, transform 620ms cubic-bezier(.22, 1, .36, 1); transition-delay: 180ms`. `.resources-panels.is-open .resources-panel { opacity: 1; transform: translate3d(0, 0, 0) }`; `.resources-panels.is-closing .resources-panel { opacity: 0; transform: translate3d(4%, 0, 0); transition-duration: 180ms; transition-delay: 0ms }`.

    Внутренняя колонка `.resources-panel__inner`: `position: relative; z-index: 1; display: flex; flex-direction: column; gap: 56px; width: min(100%, 72rem); min-height: 100%; margin-inline: auto; padding: max(clamp(20px, 5vw, 56px), env(safe-area-inset-top)) clamp(20px, 5vw, 56px) max(clamp(20px, 5vw, 56px), env(safe-area-inset-bottom))`. Шапка `.resources-panel__header { display: flex; flex-direction: column; align-items: flex-start; gap: clamp(24px, 4vw, 44px) }`, `.resources-panel__copy { display: flex; flex-direction: column; gap: 10px }`. Кнопка `.resources-panel__back`: `display: inline-flex; align-items: center; gap: 8px; min-height: 40px; padding: 8px 16px; border: 1px solid rgb(170 217 220 / .22); border-radius: 52px; background: rgb(49 41 77 / .52); color: rgb(248 247 251 / .96); font-family: var(--font-body); font-size: 16px; font-weight: 700; text-transform: uppercase; letter-spacing: .08em; transition: border-color 420ms ease, background-color 420ms ease`; `:hover, :focus-visible { border-color: rgb(170 217 220); background: rgb(59 77 161 / .55) }`. Заголовок `.resources-panel__title { margin: 0; font-family: var(--font-display); font-size: clamp(1.25rem, 6vw, 1.35rem); font-weight: 800; letter-spacing: -0.03em; line-height: 1.05 }`; описание `.resources-panel__description { margin: 0; max-width: 42rem; color: rgb(239 237 245 / .72); font-family: var(--font-body); line-height: 1.55 }`. Список панели `.resources-panel__list { display: flex; flex-direction: column; gap: 16px; width: 100% }` (в панели «Видео» между сеткой роликов и карточкой файла gap 24px через `.resources-panel__list--video { gap: 24px }`).

    Сетка файлов `.resources-files { display: grid; grid-template-columns: repeat(auto-fill, minmax(min(100%, 17.5rem), 1fr)); align-items: stretch; gap: 16px; width: 100%; margin: 0; padding: 0; list-style: none }`. Карточка `.resources-file { display: flex; flex-direction: column; gap: 18px; min-width: 0; max-width: 23rem; height: 100%; padding: 18px; border: 1px solid rgb(239 237 245 / .16); border-radius: 14px; background: rgb(49 41 77 / .42) }`, в `@media (min-width: 48rem)` `padding: 22px 24px`. Мета `.resources-file__meta { display: flex; flex-wrap: wrap; align-items: flex-start; justify-content: space-between; gap: 8px; min-width: 0; min-height: 3.3125rem; align-content: flex-start }`. Название `.resources-file__name { margin: 0; overflow-wrap: anywhere; font-family: var(--font-display); font-weight: 700 }`. Бейдж `.resources-file__type { flex: 0 0 auto; padding: 3px 7px; border: 1px solid rgb(239 237 245 / .28); border-radius: 6px; background: rgb(239 237 245 / .08); color: rgb(248 247 251 / .9); font-family: ui-monospace, SFMono-Regular, Consolas, monospace; font-size: .72rem; font-weight: 600; line-height: 1.2; text-transform: uppercase }` и варианты `[data-file-type="pdf"] { border-color: rgb(210 142 190 / .66); color: rgb(240 211 231); background: rgb(108 44 104 / .2) }`, `[data-file-type="pptx"]`, `[data-file-type="png"]`, `[data-file-type="zip"]` с цветами из блока ownership. Кнопка `.resources-file__action { position: relative; display: inline-flex; align-items: center; margin-top: auto; min-height: 38px; padding: 9px 14px; border: 1px solid transparent; border-radius: 16px; background: transparent; color: rgb(123 194 199); font-family: ui-monospace, SFMono-Regular, Consolas, monospace; font-size: .78rem; font-weight: 700; letter-spacing: .1em; text-transform: uppercase; text-decoration: none; transition: border-color 420ms ease, color 420ms ease, transform 420ms cubic-bezier(.22, 1, .36, 1) }`; `::after { content: "↓"; margin-left: 10px; color: rgb(231 245 245); opacity: 0; transform: translateY(-7px) scale(.9); transition: opacity 420ms ease, transform 420ms cubic-bezier(.22, 1, .36, 1) }`; `[data-action="open"]::after { content: "→" }`; `:hover, :focus-visible { transform: translateY(-1px) scale(1.02); border-color: rgb(231 245 245 / .3); color: rgb(231 245 245) }`; `:hover::after, :focus-visible::after { opacity: 1; transform: translateY(0) scale(1.1) }`.

    Группы `.resources-group { width: 100%; border-bottom: 1px solid rgb(239 237 245 / .16) }`; `.resources-group__summary { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 18px 0; list-style: none; cursor: pointer }` и `::-webkit-details-marker { display: none }`; `.resources-group__title { font-family: var(--font-body); font-size: 1.125rem; font-weight: 600 }`, в `@media (min-width: 64rem)` `font-size: 1.25rem`; `.resources-group__chevron { flex: 0 0 20px; transition: transform 420ms cubic-bezier(.22, 1, .36, 1) }`, `.resources-group[open] > .resources-group__summary .resources-group__chevron { transform: rotate(90deg) }`; `.resources-group__inner { padding: 0 0 18px }`.

    Scroll lock: `html.resources-panel-locked, body.resources-panel-locked { overflow: hidden; overscroll-behavior: none }`.

    Комментарии на русском: почему портал и z 10000 (шапка z 40 в корневом контексте, секция `isolate`), почему нет reduce-блока (глобальный `transition-duration: 0.01ms`), почему lock классом, а не `scrollLock.ts`.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q "z-index: 10000" src/components/resources/resources.css && grep -q "repeat(auto-fill, minmax(min(100%, 17.5rem), 1fr))" src/components/resources/resources.css && grep -q "resources-panel-locked" src/components/resources/resources.css && ! grep -q "prefers-reduced-motion" src/components/resources/resources.css && ! grep -q "resources-panel-wrap" src/components/resources/resources.css</automated>
  </verify>
  <acceptance_criteria>
    - `FileCard.tsx` экспортирует `FileCard`; ссылка несёт `data-action`, `href`, `target="_blank" rel="noopener noreferrer"` в одной строке, `aria-label` вида «Скачать: <name>» / «Открыть: <name>»; бейдж `data-file-type` с текстом типа в верхнем регистре
    - `FileGroup.tsx` экспортирует `FileGroup`; `details#resources-group-<id>` с `open` только когда `group.open`; внутри `ul.resources-files` с `FileCard` на каждый файл
    - `resources.css` содержит: `z-index: 10000`, `translate3d(100%, 0, 0)`, `rgb(132 53 127)`, `rgb(59 77 161)`, `620ms cubic-bezier(.22, 1, .36, 1)`, `transition-delay: 90ms`, `transition-delay: 180ms`, `rgb(18 12 52 / .985)`, `translate3d(8%, 0, 0)`, `translate3d(4%, 0, 0)`, `transition-duration: 180ms`, `repeat(auto-fill, minmax(min(100%, 17.5rem), 1fr))`, `rgb(49 41 77 / .42)`, `border-radius: 14px`, `padding: 22px 24px`, `font-size: .72rem`, `font-size: .78rem`, `border-radius: 52px`, `rgb(170 217 220 / .22)`, `rgb(59 77 161 / .55)`, `content: "↓"`, `content: "→"`, `rotate(90deg)`, `html.resources-panel-locked, body.resources-panel-locked`
    - В `resources.css` нет `resources-panel-wrap`, нет `prefers-reduced-motion`, нет `var(--color-`, нет `outline: none`; блоки `.resources`, `.resources-atmosphere*`, `.resources-particles`, `.resources-grid`, `.resources-cell*`, `.resource-card*` из 11-02 сохранены
    - `npx tsc -b` без ошибок (новые компоненты пока никем не импортируются)
  </acceptance_criteria>
  <done>Карточка файла и языковая группа готовы, CSS полноэкранных панелей и scroll lock описан по RES-03/RES-04.</done>
</task>

<task type="auto">
  <name>Task 2: Хост ResourcePanel с порталом, фазами, scroll lock, фокусом и Escape; подключение в Resources.tsx</name>
  <files>src/components/resources/ResourcePanel.tsx, src/components/resources/Resources.tsx</files>
  <read_first>
    - src/components/resources/ResourcePanel.tsx (текущая панель, переписывается целиком)
    - src/components/resources/Resources.tsx (после 11-02: состояние active, close, эффект deep link, обёртка .resources-panel-wrap)
    - src/lib/useReducedMotion.ts (prefersReducedMotion, usePrefersReducedMotion)
    - .planning/phases/11-resources-panels/11-CONTEXT.md, раздел «Панели»
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, RES-03 (тайминги открытия и закрытия), RES-05 (что показывает каждая панель)
    - eslint.config.js (react-hooks recommended v7: setState синхронно в теле эффекта запрещён правилом set-state-in-effect; обновления делать в обработчиках, rAF- и timer-колбэках или по шаблону «производное состояние от пропа» из документации React)
  </read_first>
  <action>
    **ResourcePanel.tsx** переписать как хост: `export function ResourcePanel({ active, onClose }: { active: ResourceKey | null; onClose: () => void })`. Тип фаз `PanelPhase = "closed" | "opening" | "open" | "closing"`. Состояние `{ tracked: ResourceKey | null; kind: ResourceKey | null; phase: PanelPhase }`, где `tracked` хранит последнее увиденное значение `active`, `kind` то, что отрисовано (держится во время закрытия). Reduced motion через `usePrefersReducedMotion()`.

    Переходы при смене `active` (шаблон производного состояния: сравнение `tracked !== active` в рендере с последующим `setState`, без setState в теле `useEffect`):
    - `active` не null и `phase` `closed` или `closing`: `kind = active`, `phase = reduced ? "open" : "opening"`.
    - `active` не null и `phase` `opening`/`open`, `active !== kind`: `kind = active`, фаза не меняется (смена содержимого по `hashchange` при открытой панели).
    - `active` null и `phase` `opening`/`open`: `phase = reduced ? "closed" : "closing"`, `kind` сохранить.

    Эффекты (все обновления состояния только из колбэков):
    - `phase === "opening"`: `requestAnimationFrame` колбэк читает `containerRef.current?.offsetWidth` (принудительный расчёт стилей с классом `is-opening`, иначе браузер схлопнет оба класса в один кадр и перехода не будет) и ставит `phase = "open"`; cleanup через `cancelAnimationFrame`.
    - `phase === "closing"`: слушатель `transitionend` на контейнере с фильтром `event.target === containerRef.current && event.pseudoElement === "::after" && event.propertyName === "transform"` и fallback-таймер `setTimeout(finish, 900)` (180ms гашения панели + 620ms слоёв + запас; при reduced motion глобальное правило даёт 0.01ms, и таймер тоже срабатывает); `finish` ставит `phase = "closed"`, `kind = null`; cleanup снимает слушатель и таймер.
    - `phase !== "closed"`: добавить класс `resources-panel-locked` на `document.documentElement` и `document.body`, в cleanup снять.
    - Фокус: при переходе из `closed` в `opening`/`open` (отслеживать предыдущую фазу через `useRef`) вызвать `backRef.current?.focus({ preventScroll: true })`.
    - Escape: при `phase` `opening`/`open` слушатель `keydown` на `document`; `event.key === "Escape"` → `event.preventDefault()`, `onClose()`. Во время `closing` слушателя нет.
    - Ловушка Tab: `onKeyDown` на диалоге; при `Tab` собрать фокусируемые узлы диалога селектором `a[href], button:not([disabled]), summary, [tabindex]:not([tabindex="-1"])`, отфильтровав узлы внутри закрытых `details` (оставить только их `summary`); Shift+Tab на первом → фокус на последний, Tab на последнем → на первый, активный элемент вне диалога → на первый; в этих случаях `preventDefault`.

    Рендер: при `phase === "closed"` вернуть `null`; иначе `createPortal(…, document.body)` с `<div ref={containerRef} className={`resources-panels is-${phase}`}>` и внутри `<div id="resources-panel" role="dialog" aria-modal="true" aria-labelledby="resources-panel-title" tabIndex={-1} data-kind={kind} className="resources-panel" onKeyDown={trapTab}>` → `<div className="resources-panel__inner">` → шапка `.resources-panel__header` (`<button type="button" ref={backRef} className="resources-panel__back" onClick={onClose}>` с inline-svg стрелкой «←» `aria-hidden="true"` и текстом `resourcesCopy.panel.back`; блок `.resources-panel__copy` с `<h2 id="resources-panel-title" className="resources-panel__title">{resourcesCopy.panels[kind].title}</h2>` и `<p className="resources-panel__description">{resourcesCopy.panels[kind].description}</p>`) → список `.resources-panel__list`:
    - `music`: `<ul className="resources-files">` из `musicFiles.map(file => <FileCard …/>)`.
    - `materials`: `materialGroups.map(group => <FileGroup key={group.id} group={group} />)`.
    - `video`: `<VideoGrid className="grid-cols-2 md:grid-cols-3 lg:grid-cols-4" />` и под ним `<ul className="resources-files">` из `videoFiles`; список получает модификатор `resources-panel__list--video`.
    Импорты: `createPortal` из `react-dom`, `FileCard`, `FileGroup`, `VideoGrid`, `musicFiles/materialGroups/videoFiles`, `resourcesCopy`, `usePrefersReducedMotion`. Импортов `MusicPlaceholder` и `MaterialsList` нет. Комментарии на русском: зачем портал, зачем принудительный расчёт стилей перед `is-open`, зачем fallback-таймер, почему lock классом.

    **Resources.tsx.** Убрать `panelRef` и эффект `panelRef.current?.focus(...)` (фокус ставит хост). Блок `<div data-open=… className="resources-panel-wrap mt-8">…</div>` заменить на `<ResourcePanel active={active} onClose={close} />` сразу после `</div>` контейнера `.resources-content`, внутри `<section>`. `close()` оставить: `setActive(null)` и фокус на `cardRefs.current[trigger]`. `toggle`, эффект хэша и делегированного клика, `is-${active}-active`, атмосфера с `data-kind`, частицы не менять. Если правило `react-hooks/set-state-in-render` пометит производное состояние в хосте, перенести обновление в `useEffect` с вызовом `setState` внутри `queueMicrotask`; директивы `eslint-disable` не добавлять.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && grep -q "createPortal" src/components/resources/ResourcePanel.tsx && grep -q '<ResourcePanel active=' src/components/resources/Resources.tsx && ! grep -qE "MusicPlaceholder|MaterialsList" src/components/resources/ResourcePanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ResourcePanel.tsx` рендерит `null` при `active === null` после завершения закрытия и портал в `document.body` иначе; контейнер несёт ровно один класс состояния из `is-opening` / `is-open` / `is-closing`
    - Диалог: `id="resources-panel"`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby="resources-panel-title"`, `tabIndex={-1}`, `data-kind`
    - Кнопка «Назад» первая в диалоге и получает фокус при открытии; клик по ней вызывает `onClose`
    - `keydown` Escape на `document` при открытой панели вызывает `onClose` один раз; в фазе `closing` не вызывает
    - Tab на последнем фокусируемом узле переводит фокус на «Назад», Shift+Tab на «Назад» переводит на последний узел
    - Классы `resources-panel-locked` на `html` и `body` присутствуют в фазах `opening`/`open`/`closing` и сняты в `closed`
    - `opening` → `open` по `requestAnimationFrame`; `closing` → `closed` по `transitionend` (`pseudoElement === "::after"`, `propertyName === "transform"`) или таймеру 900ms; при reduced motion открытие сразу `open`, закрытие сразу `closed`
    - `Resources.tsx` не содержит `resources-panel-wrap`, `panelRef`, `onKeyDown`; содержит `<ResourcePanel active={active} onClose={close} />`; эффект deep link дословно сохранён
    - `npx tsc -b` и `npm run lint` без ошибок и без `eslint-disable`
  </acceptance_criteria>
  <done>Полноэкранный хост панелей работает с фазами, scroll lock, фокусом, Escape и ловушкой Tab; секция подключает его порталом.</done>
</task>

<task type="auto">
  <name>Task 3: Удаление легаси, тесты ResourcePanel.test.tsx и обновление Resources.test.tsx</name>
  <files>src/components/resources/ResourcePanel.test.tsx, src/components/resources/Resources.test.tsx, src/components/resources/MusicPlaceholder.tsx, src/components/resources/MaterialsList.tsx, src/data/copy.resources.ts, src/data/materials.ts</files>
  <read_first>
    - src/components/resources/Resources.test.tsx (хелперы panelRegion/queryPanelRegion, сценарии панели, deep link, Involve)
    - src/components/resources/ResourcePanel.tsx (после Task 2)
    - src/components/layout/Reveal.test.tsx, строки 1–25 (мок matchMedia с matches для prefers-reduced-motion)
    - src/data/copy.resources.ts, src/data/materials.ts (легаси-поля с комментариями «снимает план 11-03»)
  </read_first>
  <action>
    **Удаление легаси.** Удалить файлы `MusicPlaceholder.tsx` и `MaterialsList.tsx` (`git rm`). В `copy.resources.ts` убрать блок `music: { emptyTitle, emptyBody }` из типа и объекта, из `panel` убрать `close` и `closeLabel`. В `materials.ts` убрать экспорт `materials` (остаются `MaterialKind`, `MaterialItem`, `esdMaterials`, `englishFolder`). Проверить `grep -rnE "MusicPlaceholder|MaterialsList|emptyTitle|closeLabel" src`: совпадений быть не должно.

    **ResourcePanel.test.tsx** (новый). Хелперы: `container()` = `document.querySelector(".resources-panels")`, `dialog()` = `screen.getByRole("dialog")`, `useFakeTimers` с явным списком `toFake: ["setTimeout", "clearTimeout", "requestAnimationFrame", "cancelAnimationFrame"]`, `afterEach` восстанавливает реальные таймеры и снимает классы с `html`/`body`. Сценарии:
    1. «музыка: три файла оригинала, портал в body, фокус на «Назад»»: `render(<ResourcePanel active="music" onClose={vi.fn()} />)`; `dialog()` внутри `document.body` и вне `container` RTL; `aria-modal="true"`, `aria-labelledby` указывает на заголовок «Музыка»; описание из `resourcesCopy.panels.music.description`; ссылки: `getAllByRole("link")` длиной 3, `href` равны `musicFiles.map(f => f.href)`, у каждой `target="_blank"` и `rel="noopener noreferrer"`, имена `Скачать: <name>`; бейджи `[data-file-type]` с текстами `PDF`, `ZIP`, `MOV`; `screen.getByRole("button", { name: "Назад" })` имеет фокус.
    2. «фазы открытия: is-opening → is-open по кадру»: сразу после рендера `container()` имеет класс `is-opening`; после `act(() => vi.advanceTimersByTime(20))` класс `is-open`; `html` и `body` имеют `resources-panel-locked`.
    3. «материалы: раскрытая группа ЕАД и четыре языковые группы»: `document.querySelectorAll("details.resources-group")` длиной 5 с id `resources-group-esd|en|es|pt|fr`; только `#resources-group-esd` имеет атрибут `open`; внутри esd 4 `li.resources-file`: первый бейдж `DOCX` и ссылка «Скачать: …», остальные бейдж `WEB` и ссылка «Открыть: …»; в en 7 ссылок, последняя с href `englishFolder.href`; в es, pt, fr по 5 ссылок; заголовки групп из `resourcesCopy.groups`. Счётчики брать через `querySelectorAll("a")` внутри группы, не через доступность (закрытые `details` в jsdom не скрывают содержимое).
    4. «видео: 16 роликов и Video backgrounds»: `getAllByRole("button", { name: /^Смотреть видео: / })` длиной 16; одна ссылка `Скачать: Video backgrounds` с бейджем `ZIP` и href `videoFiles[0].href`; `iframe` в DOM нет.
    5. «закрытие: is-closing, затем размонтирование по таймеру»: `rerender(<ResourcePanel active={null} onClose={fn} />)` после открытия → `container()` имеет `is-closing`, диалог ещё в DOM; после `act(() => vi.advanceTimersByTime(900))` `container()` null, классы `resources-panel-locked` сняты с `html` и `body`.
    6. «закрытие по transitionend раньше таймера»: в фазе `closing` отправить `fireEvent.transitionEnd(container(), { propertyName: "transform", pseudoElement: "::after" })` (если jsdom теряет поля, собрать `Object.assign(new Event("transitionend", { bubbles: true }), { propertyName: "transform", pseudoElement: "::after" })` и `fireEvent(container(), event)`); контейнер исчезает без продвижения таймеров.
    7. «Escape вызывает onClose один раз и молчит во время закрытия»: `fireEvent.keyDown(document, { key: "Escape" })` → `onClose` вызван 1 раз; `rerender` с `active={null}` и повторный Escape → всё ещё 1.
    8. «Tab не выходит за пределы панели»: для `active="music"` последний фокусируемый узел = последняя ссылка; `last.focus()`, `fireEvent.keyDown(dialog(), { key: "Tab" })` → фокус на «Назад»; `fireEvent.keyDown(dialog(), { key: "Tab", shiftKey: true })` → фокус на последней ссылке.
    9. «reduced motion: без промежуточных фаз»: мок `window.matchMedia` с `matches: query.includes("prefers-reduced-motion")`; после рендера сразу `is-open` без продвижения таймеров; после `rerender` с `active={null}` контейнер сразу null.
    10. «клик по «Назад» вызывает onClose».

    **Resources.test.tsx.** Хелперы `panelRegion`/`queryPanelRegion` заменить на `panelDialog()` = `screen.getByRole("dialog")` и `queryPanelDialog()`; добавить `panelsContainer()` = `document.querySelector(".resources-panels")`. Обновить сценарии:
    - «клик по «Материалы» раскрывает панель…»: диалог с `aria-labelledby` на заголовок «Материалы», внутри `#resources-group-esd[open]` с 4 ссылками (href равны `esdMaterials.map(m => m.href)`), фокус на кнопке «Назад»; проверку «5 ссылок» убрать.
    - «повторный клик по активной карточке закрывает панель»: после второго клика `aria-expanded="false"`, контейнер `is-closing`; с fake timers после 900ms `queryPanelDialog()` null.
    - «клик по «Видео» показывает 16 фасадов и переключает aria-expanded»: второй клик по «Материалы» при открытом «Видео» меняет `data-kind` диалога на `materials` и показывает группы; фасадов нет.
    - «клик по «Музыка» показывает честную заглушку» заменить на «клик по «Музыка» показывает три файла оригинала»: 3 ссылки с href `musicFiles`, текст описания `resourcesCopy.panels.music.description`, текста «Песня ещё в работе» нет.
    - «показывает кнопку «Свернуть панель»» → кнопка «Назад».
    - «держит id панели на элементе с role=region» → `role="dialog"` с `id="resources-panel"`, `aria-controls` раскрытой карточки указывает на него.
    - «Esc внутри панели закрывает…»: `fireEvent.keyDown(document, { key: "Escape" })`; `aria-expanded="false"`, фокус на карточке-триггере, контейнер `is-closing`.
    - «кнопка «Свернуть панель» закрывает…» → «Назад»; фокус на карточке.
    - Deep link (два сценария): диалог с `data-kind="materials"`, `aria-expanded="true"` у карточки; ссылки считать внутри `#resources-group-esd`.
    - Сценарий с `Involve`: после `user.click(link)` диалог есть; после клика «Назад» контейнер `is-closing`; повторный `user.click(link)` → диалог снова с `data-kind="materials"` и контейнер без `is-closing` (реальные таймеры, удаление не ждать).
    - «снимает слушателя хэша при размонтировании»: `document.getElementById("resources-panel")` null после unmount.
    - Новый сценарий «блокирует прокрутку документа, пока панель открыта»: после клика по карточке `document.documentElement` и `document.body` имеют `resources-panel-locked`; после «Назад» и 900ms fake timers класс снят.
    - Сценарии про секцию (`is-video-active`, атмосфера `data-kind`, частицы, заголовки, сетка из 11-02) не менять. Импорт `materials` заменить на `esdMaterials`; добавить импорты `musicFiles`, `resourcesCopy`.
  </action>
  <verify>
    <automated>npx vitest run src/components/resources src/data src/styles/motionPolicy.test.ts src/components/involve && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Файлов `src/components/resources/MusicPlaceholder.tsx` и `MaterialsList.tsx` нет; `grep -rnE "MusicPlaceholder|MaterialsList|emptyTitle|closeLabel" src` пуст; экспорта `materials` в `materials.ts` нет
    - `npx vitest run src/components/resources/ResourcePanel.test.tsx` зелёный, не меньше 10 сценариев из списка выше
    - `npx vitest run src/components/resources/Resources.test.tsx` зелёный; сценарии Escape, «Назад», deep link, Involve, scroll lock, `is-*-active`, атмосфера, частицы присутствуют
    - `npx vitest run src/styles/motionPolicy.test.ts` зелёный (reduce-блок только в global.css, реестр data-anim без новых значений, `target="_blank"` всегда с `rel="noopener noreferrer"` в той же строке, `outline: none` нигде нет)
    - `npx vitest run src/data src/components/involve` зелёный
    - `npx tsc -b`, `npm run lint` без ошибок; финально `npx vitest run` целиком зелёный
  </acceptance_criteria>
  <done>Легаси удалено, хост и содержимое трёх панелей покрыты тестами, весь набор тестов проекта зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Адресная строка → состояние панели | `window.location.hash` сравнивается с константой `#resources-materials`; в разметку и селекторы значение хэша не попадает |
| Панель → внешние хосты файлов | ссылки на hope-documents, SharePoint, esd.onevoice27.org открываются в новой вкладке |
| Модальный диалог → остальной документ | фокус и прокрутка удерживаются внутри панели |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | deep link `#resources-materials` в Resources.tsx | mitigate | сравнение с константой модуля, селектор делегированного клика собран из той же константы; логика сохранена без изменений |
| T-11-06 | Spoofing | ссылки FileCard на внешние хосты | mitigate | `target="_blank" rel="noopener noreferrer"` в одной строке JSX, тест motionPolicy падает при отсутствии rel |
| T-11-07 | Denial of service | scroll lock классом на html/body | mitigate | класс снимается в cleanup эффекта при любом размонтировании и по завершении фазы `closing`; fallback-таймер 900ms гарантирует выход из `closing` без `transitionend` |
| T-11-08 | Elevation of privilege | ловушка Tab и Escape на document | accept | слушатели живут только в фазах `opening`/`open` и снимаются в cleanup; сторонних обработчиков Escape в проекте нет |
| T-11-SC | Tampering | npm-установки | accept | план не устанавливает пакетов (`react-dom` уже в зависимостях) |
</threat_model>

<verification>
- `npx vitest run` целиком зелёный, `npx tsc -b` и `npm run lint` без ошибок
- `npx vite build` собирается; `npx vite preview` на 1440×900: клик по карточке «Материалы» → слои въезжают справа (фиолетовый, затем синий), панель проявляется, шапка под панелью не видна, страница не прокручивается, фокус на «Назад», Escape закрывает обратной анимацией и возвращает фокус на карточку; `http://localhost:4173/esd-onevoice27/#resources-materials` открывает материалы; при `emulateMedia({ reducedMotion: "reduce" })` панель появляется без движения
- Секция при открытой панели: класс `is-materials-active`, фон `rgb(25 47 54)`; атмосфера `data-kind="materials"`
</verification>

<success_criteria>
- RES-03: fixed-контейнер z 10000 порталом в body, слои и тайминги оригинала, scroll lock, фокус на «Назад», Escape, возврат фокуса, deep link, reduced motion без промежуточных фаз
- RES-04: кнопка «Назад», заголовок, описание, сетка файлов, карточки с бейджами и кнопками, группы `<details>`
- RES-05 (завершение): три панели наполнены из `resourceFiles.ts` и `VideoGrid`; `MusicPlaceholder` и `MaterialsList` удалены
- RES-06: секция красится в цвет карточки, атмосфера и частицы сохранены
- Все тесты проекта зелёные, `tsc -b` и `lint` чистые
</success_criteria>

<output>
Create `.planning/phases/11-resources-panels/11-03-SUMMARY.md` when done
</output>
