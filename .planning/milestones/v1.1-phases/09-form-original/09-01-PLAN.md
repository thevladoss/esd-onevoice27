---
phase: 09-form-original
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/copy.form.ts
  - src/lib/validation.ts
  - src/lib/validation.test.ts
  - src/components/form/LightTypeChoice.tsx
  - src/components/form/LightForm.tsx
  - src/components/form/FormField.tsx
  - src/components/form/ConsentCheckbox.tsx
  - src/components/form/light-form.css
  - src/components/form/LightForm.test.tsx
  - src/components/form/LightForm.failure.test.tsx
autonomous: true
requirements: [FORM-01, FORM-02]
tags: [form, validation, a11y, copy]

must_haves:
  truths:
    - "При загрузке ни одна карточка типа света не выбрана; отправка без типа показывает «Выберите тип света» у группы и ведёт фокус на группу (radiogroup «Тип света»)"
    - "Клик по «Групповой маяк» показывает обязательное поле «Название организации» с placeholder «Например, община в Твери»; пустое поле после отправки даёт «Укажите название организации» и получает фокус первым после типа"
    - "Клик по «Личный свет» после группы убирает поле организации вместе с его значением и ошибкой; повторный выбор группы показывает пустое поле"
    - "Подписи формы совпадают со спецификацией FORM-02: «Тип света», «Личный свет», «Групповой маяк» с описаниями, «Название организации», «Имя», «Фамилия», «Страна» с опцией «Выберите страну», «Город» с placeholder «Например, Алматы», «Электронная почта» с placeholder «name@example.com», согласие «Даю согласие на использование моих данных только для получения новостной рассылки.», кнопка «Зажечь свой свет →»"
    - "Обязательные поля (организация, имя, фамилия, страна, почта, согласие) помечены звёздочкой-иконкой с title=\"Обязательно\" и aria-hidden, рядом sr-only «обязательно»; у поля «Город» звёздочки нет"
    - "Успешная отправка по-прежнему добавляет огонёк через addLight и показывает тост; отказ addLight по-прежнему показывает ошибку без тоста"
  artifacts:
    - path: "src/lib/validation.ts"
      provides: "LightFormValues с type: LightType | \"\" и orgName; порядок фокуса LIGHT_FORM_FIELD_ORDER из восьми полей"
      contains: "orgName"
    - path: "src/data/copy.form.ts"
      provides: "Все подписи FORM-02, тексты обязательности и ошибки type/orgName"
      contains: "Название организации"
    - path: "src/components/form/LightTypeChoice.tsx"
      provides: "Группа radio с id от родителя, role=radiogroup, ошибкой через aria-describedby"
      contains: "radiogroup"
    - path: "src/components/form/FormField.tsx"
      provides: "Проп required и компонент RequiredMark"
      exports: ["FormField", "RequiredMark"]
    - path: "src/components/form/LightForm.test.tsx"
      provides: "Тесты обоих состояний формы и звёздочек"
      contains: "Укажите название организации"
    - path: "src/lib/validation.test.ts"
      provides: "Тесты пустого типа, orgName и порядка фокуса"
      contains: "Выберите тип света"
  key_links:
    - from: "src/components/form/LightForm.tsx"
      to: "src/lib/validation.ts"
      via: "LIGHT_FORM_FIELD_ORDER.find для первого невалидного поля"
      pattern: "LIGHT_FORM_FIELD_ORDER\\.find"
    - from: "src/components/form/LightForm.tsx"
      to: "src/components/form/LightTypeChoice.tsx"
      via: "id группы от родителя, тот же id ищет focusInForm"
      pattern: "id=\\{fieldId\\(\"type\"\\)\\}"
    - from: "src/components/form/LightForm.tsx"
      to: "src/state/lights.tsx"
      via: "addLight с toLightType после сужения типа"
      pattern: "addLight\\(\\{ type: toLightType"
    - from: "src/lib/validation.ts"
      to: "src/data/copy.form.ts"
      via: "тексты ошибок только из formCopy.errors"
      pattern: "formCopy\\.errors\\.(type|orgName)"
---

<objective>
Перевести модель формы «Зажгите свой свет» на состояния оригинала: тип света не выбран при старте, для группового маяка появляется обязательное поле «Название организации», порядок фокуса при ошибке начинается с группы типа. Заменить все подписи на тексты спецификации FORM-02 и пометить обязательные поля звёздочкой с title="Обязательно" и sr-only «обязательно».

Purpose: FORM-01 и FORM-02 задают поведение и слова формы; без них стили плана 09-02 красили бы форму с неверными полями.
Output: обновлённые `validation.ts`, `copy.form.ts`, `LightTypeChoice.tsx`, `LightForm.tsx`, `FormField.tsx`, `ConsentCheckbox.tsx`, одно правило `.lf-required` в `light-form.css`, зелёные тесты на оба состояния.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-form-original/09-CONTEXT.md
@.planning/phases/09-form-original/09-PATTERNS.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/lib/validation.ts
@src/data/copy.form.ts
@src/components/form/LightForm.tsx
@src/components/form/LightTypeChoice.tsx
@src/components/form/FormField.tsx
@src/components/form/ConsentCheckbox.tsx

## Правило владения файлами (фаза 9 идёт параллельно с 7, 8, 10, 11, 12)

Редактировать только файлы из `files_modified` этого плана. Не трогать `src/App.tsx`, `src/components/map/map.css`, `src/components/layout/primitives.css`, `src/components/layout/Button.tsx`, `src/components/layout/GlassCard.tsx`, `src/components/layout/GradientTitle.tsx`, `src/styles/global.css`, `src/components/layout/Section.tsx`, `src/components/layout/Eyebrow.tsx`, `src/components/layout/Reveal.tsx`, `src/state/lights.tsx`, `src/components/form/SuccessToast.tsx`. Новых файлов план не создаёт. Цвета в CSS литералами `rgb(r g b / a)`. Локальный `@media (prefers-reduced-motion: reduce)` в `light-form.css` запрещён: `src/styles/motionPolicy.test.ts` требует единственный блок в global.css. Новых значений `data-anim` не заводить.

<interfaces>
<!-- Контракты, которые план меняет или на которые опирается. Из кода, без разведки. -->

Сейчас в src/lib/validation.ts:
```typescript
export type LightType = "individual" | "group";
export interface LightFormValues { type: LightType; firstName: string; lastName: string; countryId: string; city: string; email: string; consent: boolean; }
export type LightFormField = Exclude<keyof LightFormValues, "type">;
export type LightFormErrors = Partial<Record<LightFormField, string>>;
export const LIGHT_FORM_FIELD_ORDER: readonly LightFormField[];
export const initialLightFormValues: LightFormValues;
export function toLightType(type: LightType): MapLightType;   // "individual" → "person", "group" → "group"
export function validateLightForm(values: LightFormValues): LightFormErrors;
```

Целевые контракты после задачи 1:
```typescript
export type LightType = "individual" | "group";
export type LightTypeValue = LightType | "";                    // "" = тип не выбран
export interface LightFormValues { type: LightTypeValue; orgName: string; firstName: string; lastName: string; countryId: string; city: string; email: string; consent: boolean; }
export type LightFormField = keyof LightFormValues;             // "type" теперь внутри
export const LIGHT_FORM_FIELD_ORDER: readonly LightFormField[]; // type, orgName, firstName, lastName, countryId, city, email, consent
export function toLightType(type: LightType): MapLightType;      // сигнатура не меняется
```

Из src/state/lights.tsx (не менять):
```typescript
export interface AddLightInput { type: "person" | "group"; countryId: number; }
addLight: (input: AddLightInput) => boolean;
```

Из src/components/form/FormField.tsx (контракт render-prop, расширяется в задаче 3):
```typescript
export interface FormControlProps { id: string; className: string; "aria-invalid": true | undefined; "aria-describedby": string | undefined; }
```

Целевой контракт LightTypeChoice после задачи 2:
```typescript
export function LightTypeChoice(props: { id: string; value: LightTypeValue; error?: string; onChange: (next: LightType) => void }): JSX.Element;
// fieldset: id={id}, role="radiogroup", tabIndex={-1}, aria-labelledby={`${id}-legend`},
//   aria-invalid={error ? true : undefined}, aria-describedby={error ? `${id}-error` : undefined}
// legend: id={`${id}-legend`}, className="sr-only"; радио: id `${id}-individual`, `${id}-group`, name="lightType"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Тексты FORM-02 и модель валидации с пустым типом и orgName</name>
  <files>src/data/copy.form.ts, src/lib/validation.ts, src/lib/validation.test.ts</files>
  <read_first>
    - src/data/copy.form.ts (весь файл: плоский `as const`-объект, из `errors` читает validation.ts)
    - src/lib/validation.ts (весь файл: типы, LIGHT_FORM_FIELD_ORDER, initialLightFormValues, validateLightForm, toLightType)
    - src/lib/validation.test.ts (весь файл: каркас `valid` + точечные `{ ...valid, поле }`, литералы сообщений в тесте намеренно)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md строки 93-118 (порядок полей, звёздочки обязательности, тексты FORM-02)
    - .planning/phases/09-form-original/09-CONTEXT.md раздел «Тексты (copy.form.ts)» и «Структура и состояния»
  </read_first>
  <action>
    Сначала copy.form.ts, потом validation.ts (он импортирует `formCopy.errors`).

    **copy.form.ts** (per D: тексты FORM-02 дословно). Оставить `eyebrow`, `title`, `lead`, `submit` «Зажечь свой свет →», `submitting`, `success`, `failure` без изменений. Заменить: `typeLegend: "Тип света"`; `types.individual = { title: "Личный свет", text: "Я лично посвящаю себя молитве, изучению Библии и миссии там, где живу." }`; `types.group = { title: "Групповой маяк", text: "Церковь, школа, малая группа, семья или коллектив, которые посвящают себя вместе." }`. Поля: добавить `fields.orgName = { label: "Название организации", placeholder: "Например, община в Твери" }`; `fields.firstName = { label: "Имя" }` и `fields.lastName = { label: "Фамилия" }` без ключа `placeholder` (в оригинале у имени и фамилии его нет; отсутствие ключа заставит убрать проп в LightForm через tsc); `fields.country = { label: "Страна", placeholder: "Выберите страну" }`; `fields.city = { label: "Город", placeholder: "Например, Алматы" }`; `fields.email = { label: "Электронная почта", placeholder: "name@example.com" }`. `consent: "Даю согласие на использование моих данных только для получения новостной рассылки."`. Добавить `required: { title: "Обязательно", hint: "обязательно" }` (title для иконки, hint для sr-only). В `errors` добавить `type: "Выберите тип света"` и `orgName: "Укажите название организации"`, удалить `city` (см. ниже), остальные строки ошибок не менять.

    **Город необязателен.** В спецификации (раздел 3) обязательные поля помечены звёздочкой: «Страна* | Город», «Адрес» оригинала тоже без звёздочки. FORM-02 требует, чтобы звёздочка стояла у каждого обязательного поля, поэтому у города либо звёздочка, либо нет проверки; спецификация выбирает второе. Убрать из `validateLightForm` проверку `city` и строку `errors.city` из copy. `city` остаётся в `LightFormValues` и в порядке фокуса.

    **validation.ts.** Добавить `export type LightTypeValue = LightType | ""`. В `LightFormValues`: `type: LightTypeValue`, новое поле `orgName: string` сразу после `type`. `LightFormField = keyof LightFormValues` (без `Exclude`). `LIGHT_FORM_FIELD_ORDER` = `["type", "orgName", "firstName", "lastName", "countryId", "city", "email", "consent"]` (порядок фокуса из CONTEXT). `initialLightFormValues`: `type: ""`, `orgName: ""`. В `validateLightForm` первой проверкой `if (values.type === "") errors.type = formCopy.errors.type;` затем `if (values.type === "group" && values.orgName.trim().length < MIN_TEXT_LENGTH) errors.orgName = formCopy.errors.orgName;` с комментарием, что при личном свете организация не проверяется, иначе личный свет не отправить. Сигнатуру `toLightType(type: LightType)` не расширять: до неё доходит только выбранный тип, сужение делает LightForm.

    **validation.test.ts.** Обновить `valid` (добавить `orgName: "Община Твери"`), тест пустой формы: ключи `["consent", "countryId", "email", "firstName", "lastName", "type"]`, `errors.type === "Выберите тип света"`, `errors.city` undefined. Убрать ожидание «Укажите город» и проверку `city: "М"`; добавить `expect(validateLightForm({ ...valid, city: "" }).city).toBeUndefined()`. Новые тесты: «требует название организации только для группы» (`{ ...valid, type: "group", orgName: "" }` → «Укажите название организации», `orgName: " О "` → та же ошибка, `orgName: "Община"` → undefined, `{ ...valid, type: "individual", orgName: "" }` → undefined); «пустой тип даёт ошибку и не трогает остальные поля» (`{ ...valid, type: "" }` → только ключ `type`); «порядок фокуса начинается с типа и организации» (`LIGHT_FORM_FIELD_ORDER` toEqual массив из восьми полей); «стартовое состояние без типа» (`initialLightFormValues.type === ""`, `orgName === ""`). Тексты сообщений в тестах литералами, как в файле.
  </action>
  <verify>
    <automated>npx vitest run src/lib/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Название организации\|Например, община в Твери\|Личный свет\|Электронная почта\|name@example.com\|Например, Алматы\|Выберите тип света\|Укажите название организации" src/data/copy.form.ts` даёт 8
    - `grep -c "Укажите город\|Индивидуальный свет" src/data/copy.form.ts` даёт 0
    - `grep -n 'placeholder' src/data/copy.form.ts` не содержит строк для firstName и lastName
    - `src/lib/validation.ts` содержит `export type LightTypeValue = LightType | ""`, `orgName: string`, `LightFormField = keyof LightFormValues`, массив порядка ровно из `"type", "orgName", "firstName", "lastName", "countryId", "city", "email", "consent"`
    - `grep -c "errors.city" src/lib/validation.ts` даёт 0
    - `npx vitest run src/lib/validation.test.ts` зелёный; тест-файл содержит строки «Выберите тип света» и «Укажите название организации»
    - `npx tsc -b` на этом шаге падает только в `src/components/form/LightForm.tsx` (тип `LightTypeValue` и placeholder имени/фамилии); задача 2 закрывает эти ошибки
  </acceptance_criteria>
  <done>Тексты FORM-02 в copy.form.ts, модель с `type: ""` и `orgName` в validation.ts, тесты валидации зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Состояния формы: группа типа с id от родителя, условное поле orgName, тесты обоих состояний</name>
  <files>src/components/form/LightTypeChoice.tsx, src/components/form/LightForm.tsx, src/components/form/LightForm.test.tsx, src/components/form/LightForm.failure.test.tsx</files>
  <read_first>
    - src/components/form/LightForm.tsx (весь файл: fieldId/useId, focusInForm, updateField, revalidateField, handleSubmit, сброс после успеха, вызов LightTypeChoice строки 163-166 и ConsentCheckbox строки 267-273)
    - src/components/form/LightTypeChoice.tsx (весь файл: OPTIONS из copy, скрытое radio `sr-only` + label с htmlFor)
    - src/components/form/ConsentCheckbox.tsx строки 7-35 (аналог: id и error приходят сверху, `errorId = id + "-error"`)
    - src/components/form/FormField.tsx (контракт render-prop и aria-связка)
    - src/components/form/LightForm.test.tsx (весь файл: хелперы control/errorFor/errorNodes, fillValidForm, fillIndividual, зашитые числа 18 id, 8 контролов, «шесть ошибок»)
    - src/components/form/LightForm.failure.test.tsx (мок useLights, fillValidForm без выбора типа)
    - .planning/phases/09-form-original/09-PATTERNS.md разделы «LightForm.tsx», «LightTypeChoice.tsx», «LightForm.test.tsx» (ограничение с useId внутри LightTypeChoice и смена типа мимо updateField)
  </read_first>
  <action>
    **LightTypeChoice.tsx** (per D-структура: fieldset типа с sr-only legend, ошибка «Выберите тип света» через aria-describedby на группу). Убрать внутренний `useId`. Пропсы: `id: string`, `value: LightTypeValue`, `error?: string`, `onChange: (next: LightType) => void`. `errorId = `${id}-error``, `legendId = `${id}-legend``. `<fieldset className="lf-types" id={id} role="radiogroup" tabIndex={-1} aria-labelledby={legendId} aria-invalid={error ? true : undefined} aria-describedby={error ? errorId : undefined}>`. Роль radiogroup нужна потому, что на роли group атрибут aria-invalid не разрешён; tabIndex -1 делает группу целью `focusInForm`, и скринридер читает legend с текстом ошибки. `<legend id={legendId} className="sr-only">{formCopy.typeLegend}</legend>`. Радио: `id={`${id}-${option.value}`}`, `htmlFor` тот же, `name="lightType"`, `checked={value === option.value}`; при `value === ""` ни одно не выбрано. Порядок вложенных span оставить как сейчас (title, text, dot): разметку карточки перестраивает план 09-02. Ошибку рисовать последним потомком fieldset: `{error ? <p className="lf-error" id={errorId}>{error}</p> : null}`.

    **LightForm.tsx.** Импортировать `LightTypeValue`/`LightType` по мере надобности. Вызов: `<LightTypeChoice id={fieldId("type")} value={values.type} error={errors.type} onChange={(next) => updateField("type", next)} />` (per замечание PATTERNS: смена типа через updateField, чтобы гасла ошибка типа и сбрасывался `failed`). Расширить `updateField`: если `field === "type"` и `value !== "group"`, в `next` дополнительно `orgName: ""` (переключение на личный свет стирает значение организации); после `fresh = validateLightForm(next)` из `prev` убирать ошибку `field`, если `fresh` её не даёт, и дополнительно ошибку `orgName`, если `field === "type"` и `fresh.orgName` отсутствует. Новых ошибок при наборе по-прежнему не зажигать. Поле организации рендерить условно только при `values.type === "group"` первым внутри `.lf-grid` (перед именем): `<FormField id={fieldId("orgName")} label={formCopy.fields.orgName.label} error={errors.orgName} className="lf-span">` с `input type="text" name="orgName" autoComplete="organization" placeholder={formCopy.fields.orgName.placeholder}` и теми же `onChange`/`onBlur`, что у других полей. Условный рендер, а не `hidden`: при личном свете поля нет в DOM, фокус и aria не ссылаются на скрытое. Убрать `placeholder` у имени и фамилии (в copy ключа больше нет). В `handleSubmit` после проверки `firstInvalid` сузить тип: `const lightType = values.type; if (lightType === "") return;` с комментарием, что валидация уже отсекла пустой тип и проверка нужна только TypeScript; внутри таймера `toLightType(lightType)`. Сброс после успеха оставить `{ ...initialLightFormValues, type: prev.type }` (организация очищается вместе с остальным, тип остаётся).

    **LightForm.test.tsx.** Обновить под новые тексты и состояния:
    - структура: `screen.getByRole("radiogroup", { name: "Тип света" })` существует; радио «Личный свет» и «Групповой маяк» оба `not.toBeChecked()`; описания карточек по новым текстам;
    - «две формы»: ожидаемое число id 22 (одиннадцать на форму: группа типа, её legend, две радио-карточки, пять полей, согласие, кнопка); подписи имени искать `screen.getAllByLabelText(/^Имя/)`;
    - «связывает контрол с подписью»: 8 контролов при личном свете; после клика «Групповой маяк» 9, и у `control("orgName")` есть `label[for]` с текстом, начинающимся на «Название организации»;
    - пустая отправка: шесть ошибок в порядке «Выберите тип света», «Введите имя», «Введите фамилию», «Выберите страну», «Введите корректный email», «Нужно согласие на обработку данных»; `[aria-invalid="true"]` шесть узлов (группа плюс пять), у каждого `aria-describedby === id + "-error"`; `document.activeElement` равен `screen.getByRole("radiogroup")`;
    - «выбор типа гасит его ошибку»: после пустой отправки клик «Личный свет» убирает «Выберите тип света», остаются пять; `control` для orgName отсутствует (`document.querySelector('[name="orgName"]')` null);
    - «групповой маяк требует организацию»: клик «Групповой маяк» показывает `control("orgName")` с placeholder «Например, община в Твери»; пустая отправка даёт шесть ошибок начиная с «Укажите название организации» и фокус на `control("orgName")`;
    - «возврат к личному свету убирает поле, значение и ошибку»: группа, ввести «Община», отправить (ошибки других полей), клик «Личный свет»: `[name="orgName"]` null, узла с текстом «Укажите название организации» нет; снова «Групповой маяк»: `control("orgName").value === ""`;
    - blur-тест: числа 6 → 5 остаются те же по составу с типом вместо города;
    - `fillValidForm`: после клика «Групповой маяк» заполнить `orgName` «Община Твери»; в успехе дополнительно `control("orgName").value === ""` после сброса и группа по-прежнему выбрана;
    - `fillIndividual`: первым действием клик по радио «Личный свет»;
    - тест «Человек» с Counters: числа 694 → 695 без изменений.

    **LightForm.failure.test.tsx.** В `fillValidForm` первым действием `fireEvent.click(screen.getByRole("radio", { name: /Личный свет/ }))`; остальное без изменений.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/form/LightForm.test.tsx src/components/form/LightForm.failure.test.tsx src/lib/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="radiogroup"' src/components/form/LightTypeChoice.tsx` даёт 1; файл содержит `tabIndex={-1}`, `aria-labelledby={legendId}`, `className="sr-only"` на legend и не содержит `useId`
    - `grep -c 'id={fieldId("type")}' src/components/form/LightForm.tsx` даёт 1; `grep -c 'updateField("type"' src/components/form/LightForm.tsx` даёт 1; `grep -c 'name="orgName"' src/components/form/LightForm.tsx` даёт 1; `grep -c 'GlassCard' src/components/form/LightForm.tsx` даёт 2 (импорт и обёртка остаются до плана 09-02)
    - `grep -c 'values.type === "group"' src/components/form/LightForm.tsx` не меньше 1
    - `npx tsc -b` без ошибок
    - `npx vitest run src/components/form/LightForm.test.tsx src/components/form/LightForm.failure.test.tsx src/lib/validation.test.ts` зелёный; в LightForm.test.tsx есть тесты с текстами «Выберите тип света», «Укажите название организации», «Например, община в Твери», проверка `toHaveLength(22)` и `getByRole("radiogroup"`
  </acceptance_criteria>
  <done>Форма стартует без типа, показывает и валидирует поле организации только для группы, фокус при ошибке идёт на группу через id от родителя; компонентные тесты обоих состояний зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Звёздочка обязательности с title «Обязательно» и sr-only «обязательно»</name>
  <files>src/components/form/FormField.tsx, src/components/form/ConsentCheckbox.tsx, src/components/form/LightForm.tsx, src/components/form/light-form.css, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - src/components/form/FormField.tsx (весь файл: label с htmlFor, render-prop FormControlProps)
    - src/components/form/ConsentCheckbox.tsx строки 20-53 (label `.lf-check` с `.lf-check-text`)
    - src/components/map/Counters.tsx строки 33-38 и src/components/layout/Footer.tsx строка 25 (пара «значок aria-hidden / текст sr-only»)
    - docs/research/v1.1/dom-ov-light-form-container.html (оригинал: `<span class="ms-1 inline-block shrink-0 h-3 w-3 text-danger-500" title="Required">` со svg внутри label после текста подписи)
    - src/components/form/light-form.css строки 1-34 (локальные токены секции; добавить `.lf-required` рядом с `.lf-label`)
  </read_first>
  <action>
    **FormField.tsx.** Добавить в `FormControlProps` поле `"aria-required": true | undefined` и проп `required?: boolean` у FormField; в render-prop передавать `"aria-required": required ? true : undefined`. Экспортировать `export function RequiredMark()` из этого же файла (именованный экспорт компонента, как везде в проекте): возвращает фрагмент из `<span className="lf-required" title={formCopy.required.title} aria-hidden="true">` с inline svg (viewBox "0 0 12 12", `focusable="false"`, один `path` с `d="M6 1v10M1.67 3.5l8.66 5M1.67 8.5l8.66-5"`, `fill="none"`, `stroke="currentColor"`, `strokeWidth="2"`, `strokeLinecap="round"`; это шестилучевая звёздочка без внешних ассетов), затем `{" "}` и `<span className="sr-only">{formCopy.required.hint}</span>`. Пробел перед sr-only обязателен: без него доступное имя поля склеится в «Имяобязательно». В label: `{label}{required ? <RequiredMark /> : null}`. Импорт `formCopy` из `../../data/copy.form`.

    **ConsentCheckbox.tsx.** Импортировать `RequiredMark` из `./FormField`; внутри `.lf-check-text` после текста согласия поставить `<RequiredMark />`; на input добавить `aria-required={true}`. Скрытый чекбокс и `.lf-check-box` не трогать: их меняет план 09-02.

    **LightForm.tsx.** Передать `required` в FormField для orgName, firstName, lastName, countryId, email. Для city не передавать (город необязателен, см. задачу 1).

    **light-form.css.** Рядом с `.lf-label` добавить `.lf-required { display: inline-block; width: 12px; height: 12px; margin-inline-start: 4px; vertical-align: -1px; color: rgb(252 165 165); }` и `.lf-required svg { display: block; width: 100%; height: 100%; }`. Больше в CSS ничего не менять: остальное перепишет план 09-02.

    **LightForm.test.tsx.** Новый describe «обязательные поля»: для `firstName`, `lastName`, `countryId`, `email` (личный свет) и `orgName` (после клика «Групповой маяк») найти `label[for=control.id]`, внутри `.lf-required` с `title === "Обязательно"` и `aria-hidden === "true"` и svg, а также `.sr-only` с текстом «обязательно»; у контрола `aria-required === "true"`; у подписи `city` нет `.lf-required` и у контрола нет `aria-required`; у согласия внутри `label[for=control("consent").id]` есть `.lf-required` и `aria-required`. Доступное имя: `screen.getByRole("textbox", { name: "Имя обязательно" })` и `screen.getByRole("combobox", { name: "Страна обязательно" })` находятся. Тест из задачи 2 с `getAllByLabelText(/^Имя/)` остаётся рабочим.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'title={formCopy.required.title}' src/components/form/FormField.tsx` даёт 1; `grep -c 'aria-hidden="true"' src/components/form/FormField.tsx` даёт 1; `grep -c 'export function RequiredMark' src/components/form/FormField.tsx` даёт 1; `grep -c '"aria-required"' src/components/form/FormField.tsx` не меньше 1
    - `grep -c '<RequiredMark />' src/components/form/ConsentCheckbox.tsx` даёт 1; `grep -c 'aria-required' src/components/form/ConsentCheckbox.tsx` даёт 1
    - `grep -c 'required$\|required ' src/components/form/LightForm.tsx` даёт 5 (orgName, firstName, lastName, countryId, email); у поля city `required` нет
    - `grep -c 'rgb(252 165 165)' src/components/form/light-form.css` не меньше 1; `grep -c 'prefers-reduced-motion' src/components/form/light-form.css` даёт 0
    - `npx tsc -b`, `npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts`, `npm run lint` без ошибок
    - тест-файл содержит `name: "Имя обязательно"` и проверку `title` `"Обязательно"`
  </acceptance_criteria>
  <done>Обязательные поля и согласие помечены звёздочкой rgb(252 165 165) с title «Обязательно» и sr-only «обязательно», город без пометки; полный набор тестов формы, валидации и политики движения зелёный, lint чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ввод посетителя → состояние формы | Текст полей и значение select попадают в `validateLightForm` и дальше в `addLight`; сети нет, данные не покидают браузер |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `validateLightForm` / `countryId` | mitigate | членство страны в `ESD_COUNTRIES` проверяется до `addLight` (существующая проверка сохраняется), пустой `type` отсекается до `toLightType` |
| T-09-02 | Information disclosure | значения формы (имя, почта, организация) | accept | форма без action/method и без сети; на карту уходит только тип и страна, поля стираются после успеха |
| T-09-03 | Denial of service | `focusInForm` при отсутствующем `orgName` | mitigate | поле рендерится ровно при `type === "group"`, а ошибка `orgName` возможна только при `type === "group"`, поэтому цель фокуса всегда в DOM |
| T-09-SC | Tampering | npm installs | accept | план не ставит пакетов; `package.json` не меняется |
</threat_model>

<verification>
- `npx tsc -b` без ошибок.
- `npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts` зелёный.
- `npm run lint` чист.
- Быстрая ручная проверка в `npm run dev`: при загрузке карточки типа не выбраны; отправка пустой формы подсвечивает группу и показывает «Выберите тип света»; клик по «Групповой маяк» добавляет «Название организации» со звёздочкой; клик «Личный свет» убирает поле.
</verification>

<success_criteria>
- FORM-01: тип не выбран при старте, ошибки «Выберите тип света» и «Укажите название организации» работают, поле организации живёт только при группе, тесты покрывают оба состояния.
- FORM-02: все подписи по спецификации, обязательные поля помечены звёздочкой `rgb(252 165 165)` с `title="Обязательно"` и sr-only «обязательно», у города пометки нет.
- Логика `addLight` и тост не изменились: успех добавляет огонёк и показывает тост, отказ показывает ошибку без тоста.
</success_criteria>

<output>
Create `.planning/phases/09-form-original/09-01-SUMMARY.md` when done
</output>
