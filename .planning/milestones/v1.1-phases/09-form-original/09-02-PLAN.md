---
phase: 09-form-original
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/components/form/LightForm.tsx
  - src/components/form/LightTypeChoice.tsx
  - src/components/form/ConsentCheckbox.tsx
  - src/components/form/light-form.css
  - src/components/form/LightForm.test.tsx
autonomous: true
requirements: [FORM-01, FORM-03, FORM-04, FORM-05]
tags: [form, css, layout, a11y]

must_haves:
  truths:
    - "Форма стоит прямо на секции: внутри #light-form нет .glass-card, form.lf-form лежит в обёртке Reveal, колонка шапки и формы 42rem по центру"
    - "Карточка типа: поверхность rgb(33 26 62 / .42), маячок ::before 40×40 из radial-gradient с var(--beacon)/var(--halo), строка «название + точка-радио 16px»; выбранная карточка с рамкой rgb(123 194 199 / .72) и свечением 0 0 24px var(--halo); ховер и focus-within поднимают на 2px"
    - "Поля и select: min-height 54px, radius 16px, фон rgb(33 26 62 / .58), рамка rgb(239 237 245 / .18); фокус: рамка rgb(170 217 220), фон rgb(49 41 77 / .7), тень 0 0 0 3px rgb(123 194 199 / .12); select со своей SVG-стрелкой"
    - "Чекбокс нативный 18×18 с accent-color rgb(170 217 220); кнопка size=\"form\" во всю ширину с margin-top 8px, ширину и высоту даёт global.css"
    - "Шапка: eyebrow rgb(170 217 220), лид 18px/1.65 rgb(219 215 232), отступы секции 64px сверху и снизу, форма через 48px после лида; у секции нет собственного background и ::before"
    - "id light-form, живой регион и тост успеха сохранены; отправка валидной формы добавляет огонёк"
  artifacts:
    - path: "src/components/form/light-form.css"
      provides: "Стили карточек, полей, чекбокса, кнопки и шапки по FORM-03/04/05 без фона и орба секции"
      contains: "0 0 24px var(--halo)"
      min_lines: 200
    - path: "src/components/form/LightForm.tsx"
      provides: "Форма без GlassCard, классы сетки lf-col-3 / lf-span"
      contains: "lf-col-3"
    - path: "src/components/form/LightTypeChoice.tsx"
      provides: "Строка «название + точка» .lf-type-row внутри карточки"
      contains: "lf-type-row"
    - path: "src/components/form/ConsentCheckbox.tsx"
      provides: "Видимый нативный чекбокс .lf-checkbox"
      contains: "lf-checkbox"
    - path: "src/components/form/LightForm.test.tsx"
      provides: "Тесты разметки без карточки и инварианты значений light-form.css"
      contains: "light-form.css"
  key_links:
    - from: "src/components/form/LightForm.tsx"
      to: "src/components/form/light-form.css"
      via: "import первой строкой"
      pattern: "import \"\\./light-form\\.css\""
    - from: "src/components/form/light-form.css (.lf-type::before)"
      to: ".lf-type[data-type=…] (--beacon, --halo)"
      via: "radial-gradient через переменные карточки"
      pattern: "radial-gradient\\(circle, var\\(--beacon\\) 0 2px, transparent 3px\\)"
    - from: "src/components/form/LightForm.tsx (Button size=\"form\")"
      to: "src/styles/global.css (.btn[data-beam][data-size=\"form\"])"
      via: "data-size=\"form\" даёт ширину 100% и min-height 54px, light-form.css добавляет только margin-top"
      pattern: "size=\"form\""
    - from: "src/components/form/light-form.css (.lf-section > div)"
      to: "src/components/layout/Section.tsx (контейнер py-16 md:py-24, mt-6)"
      via: "неслоёное правило перебивает утилиты Tailwind из @layer utilities"
      pattern: "\\.lf-section > div \\{"
---

<objective>
Снять с формы стеклянную карточку и перенести на неё стили оригинала: карточки типа с маячком и точкой-радио, поля 54px с radius 16px, нативный чекбокс, кнопка во всю ширину с отступом 8px, шапка в колонке 42rem с отступами секции 64px. Секция теряет собственный фон и орб: подложку даёт лента `.map-band` фазы 8 после слияния.

Purpose: FORM-03, FORM-04 и FORM-05 закрывают визуальное расхождение формы с onevoice27.org, названное пользователем после приёмки v1.0.
Output: `LightForm.tsx` без GlassCard, новая разметка карточки в `LightTypeChoice.tsx`, нативный чекбокс в `ConsentCheckbox.tsx`, переписанный `light-form.css`, тесты разметки и инварианты значений CSS.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-form-original/09-CONTEXT.md
@.planning/phases/09-form-original/09-PATTERNS.md
@.planning/phases/09-form-original/09-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@docs/research/v1.1/orig-rules.css
@docs/research/v1.1/orig-form.jpeg
@docs/research/v1.1/orig-form-group.jpeg
@src/components/form/light-form.css
@src/components/form/LightForm.tsx
@src/components/form/LightTypeChoice.tsx
@src/components/form/ConsentCheckbox.tsx

## Правило владения файлами (фаза 9 идёт параллельно с 7, 8, 10, 11, 12)

Редактировать только файлы из `files_modified` этого плана. Не трогать `src/App.tsx`, `src/components/map/map.css`, `src/components/layout/primitives.css`, `src/components/layout/Button.tsx`, `src/components/layout/GlassCard.tsx`, `src/components/layout/GradientTitle.tsx`, `src/styles/global.css`, `src/components/layout/Section.tsx`, `src/components/layout/Eyebrow.tsx`, `src/components/layout/Reveal.tsx`, `src/components/form/FormField.tsx`, `src/components/form/SuccessToast.tsx`. Отступы контейнера `Section` перебиваются правилом в своём CSS, а не правкой `Section.tsx`. Цвета литералами `rgb(r g b / a)` из спецификации, токены `--color-midnight-*` проекта сдвинуты относительно палитры оригинала. Локальный `@media (prefers-reduced-motion: reduce)` в `light-form.css` запрещён (`src/styles/motionPolicy.test.ts`); переходы 220-240ms гасит глобальное `transition-duration: 0.01ms`. Новых значений `data-anim` не заводить. Вызов `GradientTitle variant="section"` не менять: плоский белый заголовок приходит из фазы 7.

<interfaces>
<!-- Что уже есть после плана 09-01 и на что опирается разметка. -->

Из src/components/form/LightTypeChoice.tsx (после 09-01):
```typescript
export function LightTypeChoice(props: { id: string; value: LightTypeValue; error?: string; onChange: (next: LightType) => void }): JSX.Element;
// fieldset.lf-types[role=radiogroup][tabindex=-1] > legend.sr-only + label.lf-type[data-type] × 2 + p.lf-error?
```

Из src/components/form/FormField.tsx (после 09-01, не менять):
```typescript
export function FormField(props: { id: string; label: string; error?: string; required?: boolean; className?: string; children: (control: FormControlProps) => ReactNode }): JSX.Element;
export function RequiredMark(): JSX.Element;
// обёртка: div.lf-field(.className) > label.lf-label + контрол.lf-control + p.lf-error?
```

Из src/components/layout/Button.tsx (не менять):
```typescript
<Button as="button" type="submit" variant="primary" size="form" className="lf-submit" …/>
// рендерит button.btn.btn--primary[data-beam][data-anim="beam"][data-size="form"] > span.btn__label
```

Из src/styles/global.css строки 269-274 (не менять, только опираться):
```css
.btn[data-beam][data-size="form"] { width: 100%; min-height: 54px; padding: 14px 28px; }
```

Из src/components/layout/Section.tsx (не менять): секция рендерит `section#light-form.lf-section > div.mx-auto.max-w-[72rem].px-4.py-16.md:px-8.md:py-24 > div.mt-6 > children`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Разметка без GlassCard: карточка с точкой-радио, сетка 6 колонок, нативный чекбокс</name>
  <files>src/components/form/LightForm.tsx, src/components/form/LightTypeChoice.tsx, src/components/form/ConsentCheckbox.tsx, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - src/components/form/LightForm.tsx (после 09-01: import GlassCard, `<Reveal delay={0.1}><GlassCard className="lf-card"><form …>`, классы FormField `lf-span`)
    - src/components/form/LightTypeChoice.tsx (после 09-01: label.lf-type с input.sr-only и тремя span)
    - src/components/form/ConsentCheckbox.tsx (input.sr-only и span.lf-check-box со svg-галочкой)
    - docs/research/v1.1/dom-ov-light-form-container.html (оригинал: label карточки содержит `span.flex.items-start.justify-between` с названием и кружком `size-4 rounded-full border-2`, ниже описание; сетка `md:grid-cols-6`, имя и фамилия `md:col-span-3`, остальное `md:col-span-6`; чекбокс нативный `h-4 w-4`)
    - docs/research/v1.1/orig-form.jpeg (вид карточек, полей, чекбокса и кнопки)
    - src/components/form/LightForm.test.tsx describe «структура секции» (хелперы control/countrySelect, тест «две формы» с числом 22)
  </read_first>
  <action>
    **LightForm.tsx** (per D-структура: без GlassCard, колонка 42rem, сетка 6 колонок). Удалить импорт `GlassCard` и обёртку `<GlassCard className="lf-card">`: `<Reveal delay={0.1}>` оборачивает `<form className="lf-form" …>` напрямую. Классы сетки в FormField: `orgName` и `email` с `className="lf-span"`, `firstName`, `lastName`, `countryId`, `city` с `className="lf-col-3"` (на ≥768px имя | фамилия и страна | город по три колонки, на <768px всё в одну). Порядок внутри `.lf-grid`: orgName (условно), firstName, lastName, countryId, city, email. Ничего в состоянии и обработчиках не менять.

    **LightTypeChoice.tsx** (per FORM-03: строка «название + точка-радио», маячок через `::before`). Внутри label после `input.sr-only`: `<span className="lf-type-row"><span className="lf-type-title">{option.title}</span><span className="lf-type-dot" aria-hidden="true" /></span>` затем `<span className="lf-type-text">{option.text}</span>`. Маячок разметки не требует: его рисует `.lf-type::before` в CSS. Атрибуты fieldset, legend, радио и ошибка из плана 09-01 без изменений.

    **ConsentCheckbox.tsx** (per FORM-04: нативный чекбокс 18×18). У input заменить `className="sr-only"` на `className="lf-checkbox"`; удалить `<span className="lf-check-box">` вместе с svg-галочкой. Порядок: input, затем `<span className="lf-check-text">` с текстом согласия и `<RequiredMark />`. Ошибка, `name="consent"`, `aria-*` и обработчики без изменений.

    **LightForm.test.tsx.** В describe «структура секции» добавить тесты: «стоит на секции без стеклянной карточки»: `document.querySelector("#light-form .glass-card")` null, `document.querySelector("#light-form form")` существует и `form.parentElement.className` не содержит `glass`; «карточка типа несёт строку с названием и точкой»: `document.querySelectorAll(".lf-type .lf-type-row")` длиной 2, в каждой `.lf-type-title` и `.lf-type-dot[aria-hidden="true"]`, у каждой карточки атрибут `data-type` individual/group; «чекбокс согласия нативный и видимый»: `control("consent").classList.contains("lf-checkbox")` true, `sr-only` false, `document.querySelector(".lf-check-box")` null; «поля стоят в сетке 6 колонок»: `control("firstName").closest(".lf-field")` и `control("lastName")`, `countrySelect()`, `control("city")` имеют класс `lf-col-3`, `control("email").closest(".lf-field")` имеет `lf-span`; после клика «Групповой маяк» `control("orgName").closest(".lf-field")` имеет `lf-span` и стоит в DOM раньше поля имени (`compareDocumentPosition` даёт `DOCUMENT_POSITION_FOLLOWING`). Остальные тесты без изменений: число id 22, контролов 8/9.
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/form/LightForm.test.tsx src/components/form/LightForm.failure.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GlassCard" src/components/form/LightForm.tsx` даёт 0; `grep -c 'className="lf-col-3"' src/components/form/LightForm.tsx` даёт 4; `grep -c 'className="lf-span"' src/components/form/LightForm.tsx` даёт 2
    - `grep -c 'className="lf-type-row"' src/components/form/LightTypeChoice.tsx` даёт 1; `grep -c 'className="lf-type-dot" aria-hidden="true"' src/components/form/LightTypeChoice.tsx` даёт 1
    - `grep -c 'className="lf-checkbox"' src/components/form/ConsentCheckbox.tsx` даёт 1; `grep -c 'sr-only\|lf-check-box\|<svg' src/components/form/ConsentCheckbox.tsx` даёт 0
    - `npx tsc -b` без ошибок; `npx vitest run src/components/form/LightForm.test.tsx src/components/form/LightForm.failure.test.tsx` зелёный
    - в тест-файле есть проверки `#light-form .glass-card`, `.lf-type-row`, `lf-checkbox`, `lf-col-3`, `lf-span`
  </acceptance_criteria>
  <done>Форма без GlassCard, карточка типа со строкой «название + точка», нативный чекбокс, поля с классами сетки; тесты разметки зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: light-form.css по FORM-03/04/05 и инварианты значений</name>
  <files>src/components/form/light-form.css, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - src/components/form/light-form.css (весь файл: токены на `.lf-section`, `::before`-орб, `.lf-card`, `.lf-types`, `.lf-type*`, `.lf-grid`, `.lf-field`, `.lf-control`, `select.lf-control` со стрелкой, `.lf-check*`, `.lf-submit*`, `.lf-toast*`, `.lf-required` из 09-01)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md строки 93-138 (значения FORM-03, FORM-04, FORM-05 дословно)
    - docs/research/v1.1/orig-rules.css строки 71-117 (правила оригинала `#ov-light-form-container …`: карточки, `::before`, точка, поля, фокус, чекбокс, кнопка, min-height 190px на широком экране)
    - src/styles/global.css строки 269-274 (кнопка size=form) и 298-372 (глобальный reduced motion; в light-form.css такого блока быть не должно)
    - src/styles/motionPolicy.test.ts строки 1-38 (чтение CSS с диска через `readFileSync(resolve(process.cwd(), …))`: тот же приём для инвариантов формы)
    - src/components/involve/involve.css строки 1-22 (конвенция локальных токенов на корне секции)
  </read_first>
  <action>
    Переписать `light-form.css` целиком, сохранив блок `.lf-toast*` и правило `.lf-required` без изменений. Значения ниже из спецификации; ничего не «улучшать».

    **Секция (FORM-05).** `.lf-section` оставляет `position: relative; isolation: isolate; overflow-x: clip` и локальные токены; удалить `background` и весь `.lf-section::before` (подложку и орб даёт лента `.map-band` фазы 8; до слияния секция прозрачна на фоне страницы). Токены на `.lf-section`: `--field-height: 54px; --field-radius: 16px; --field-surface: rgb(33 26 62 / .58); --field-surface-focus: rgb(49 41 77 / .7); --field-border: rgb(239 237 245 / .18); --field-border-focus: rgb(170 217 220); --field-focus-ring: 0 0 0 3px rgb(123 194 199 / .12); --field-copy: rgb(248 247 251 / .94); --field-placeholder: rgb(219 215 232 / .56); --field-error: rgb(252 165 165); --option-surface: rgb(33 26 62 / .42); --option-surface-active: rgb(51 94 104 / .16); --option-border: rgb(239 237 245 / .16); --option-border-hover: rgb(123 194 199 / .46); --option-border-active: rgb(123 194 199 / .72); --focus-ring: rgb(170 217 220); --form-width: 42rem`. Отступы контейнера `Section` (его классы `py-16 md:py-24` и `mt-6` нам не принадлежат): `.lf-section > div { padding-block: 64px; }` и `.lf-section > div > div { margin-top: 0; }` с комментарием, что неслоёное правило перебивает утилиты Tailwind из `@layer utilities`, поэтому специфичность не нужна. Шапка: `.lf-head { max-width: var(--form-width); margin-inline: auto; margin-bottom: 48px; text-align: center; }`, `.lf-head .eyebrow { color: rgb(170 217 220); }`, `.lf-lead { margin-inline: auto; color: rgb(219 215 232); font: 400 18px/1.65 var(--font-body); }`. Удалить `.lf-card` и его media-блок, удалить `.lf-legend` (legend теперь sr-only).

    **Форма и сетка (FORM-05, спецификация «сетка 6 колонок gap 16px»).** `.lf-form { display: flex; flex-direction: column; gap: 16px; width: 100%; max-width: var(--form-width); margin-inline: auto; }`; `.lf-fields { display: contents; margin: 0; padding: 0; border: 0; min-inline-size: 0; }` как сейчас. `.lf-grid { display: grid; gap: 16px; }`; в `@media (min-width: 768px)`: `.lf-grid { grid-template-columns: repeat(6, minmax(0, 1fr)); }`, `.lf-col-3 { grid-column: span 3; }`, `.lf-span { grid-column: 1 / -1; }`. `.lf-field { display: flex; flex-direction: column; min-width: 0; }`: резервная третья строка под ошибку убирается, потому что спецификация задаёт ритм строк 16px как у оригинала, а резерв 26px растягивал форму на четыре строки; ошибка появляется под контролом и сдвигает следующее поле, как в обычной форме.

    **Карточки типа (FORM-03).** `.lf-types { display: grid; gap: 12px; margin: 0; padding: 0; border: 0; min-inline-size: 0; }`, в `@media (min-width: 768px)` две колонки `repeat(2, minmax(0, 1fr))`; `.lf-types:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 4px; }` (группа получает фокус при ошибке типа); `.lf-types > .lf-error { grid-column: 1 / -1; margin-top: 0; }`. `.lf-type { position: relative; isolation: isolate; display: flex; flex-direction: column; align-items: stretch; justify-content: flex-start; gap: 10px; min-width: 0; min-height: 150px; padding: 24px; overflow: hidden; border: 1px solid var(--option-border); border-radius: 16px; background: var(--option-surface); box-shadow: inset 0 1px 0 rgb(248 247 251 / .04), 0 16px 38px rgb(18 12 52 / .16); color: rgb(248 247 251 / .96); cursor: pointer; transition: border-color 240ms, background-color 240ms, box-shadow 240ms, transform 240ms; }`; в `@media (min-width: 768px)` `.lf-type { min-height: 190px; }`. Переменные маячка: `.lf-type[data-type="individual"] { --beacon: rgb(170 217 220); --halo: rgb(84 164 172 / .32); }`, `.lf-type[data-type="group"] { --beacon: rgb(227 175 210); --halo: rgb(187 108 174 / .3); }`. Маячок: `.lf-type::before { content: ""; width: 40px; height: 40px; flex: 0 0 40px; background: radial-gradient(circle, var(--beacon) 0 2px, transparent 3px), radial-gradient(circle, var(--halo) 0, transparent 68%); }`. Ховер и фокус внутри: `@media (hover: hover) { .lf-type:hover { border-color: var(--option-border-hover); transform: translateY(-2px); } }` и отдельно `.lf-type:focus-within { border-color: var(--option-border-hover); transform: translateY(-2px); }`. Выбранная: `.lf-type:has(input:checked) { border-color: var(--option-border-active); background: radial-gradient(circle at 10% 12%, var(--halo), transparent 42%), var(--option-surface-active); box-shadow: inset 0 1px 0 rgb(248 247 251 / .08), 0 18px 42px rgb(18 12 52 / .28), 0 0 24px var(--halo); }`. Кольцо клавиатуры оставить: `.lf-type:has(input:focus-visible) { outline: 2px solid var(--focus-ring); outline-offset: 3px; }`. Строка: `.lf-type-row { display: flex; align-items: flex-start; justify-content: space-between; gap: 12px; }`; `.lf-type-title { font: 700 14px/1.4 var(--font-body); }`; `.lf-type-text { font: 400 14px/20px var(--font-body); opacity: .8; }`. Точка-радио: `.lf-type-dot { flex: 0 0 16px; width: 16px; height: 16px; margin-top: 4px; border: 2px solid rgb(239 237 245 / .42); border-radius: 50%; background: rgb(18 12 52 / .52); box-shadow: inset 0 0 0 3px rgb(18 12 52); transition: border-color 220ms, background-color 220ms, box-shadow 220ms; }`; `.lf-type:has(input:checked) .lf-type-dot { border-color: var(--beacon); background: var(--beacon); box-shadow: inset 0 0 0 3px rgb(18 12 52), 0 0 10px var(--beacon); }`. Старые правила `.lf-type-dot` с `opacity` и цветами `--color-signal-400`/`--color-horizon-400` удалить.

    **Подписи и поля (FORM-04).** `.lf-label { display: block; margin-bottom: 8px; color: rgb(248 247 251 / .96); font: 650 14px/1.4 var(--font-body); }`. `.lf-control { width: 100%; min-width: 0; min-height: var(--field-height); padding: 14px 16px; border: 1px solid var(--field-border); border-radius: var(--field-radius); background-color: var(--field-surface); box-shadow: inset 0 1px 0 rgb(255 255 255 / .024); color: var(--field-copy); font: 500 16px/1.4 var(--font-body); transition: border-color 220ms, background-color 220ms, box-shadow 220ms; }` (`background-color`, а не `background`: иначе стрелка select сотрётся, комментарий сохранить); `.lf-control::placeholder { color: var(--field-placeholder); opacity: 1; }`; правило `.lf-control:hover` удалить (у полей оригинала ховера нет); `.lf-control:focus { border-color: var(--field-border-focus); background-color: var(--field-surface-focus); box-shadow: var(--field-focus-ring); outline: none; }` (индикатор фокуса задают рамка и кольцо, как в оригинале); `.lf-control[aria-invalid="true"] { border-color: var(--field-error); }`. `select.lf-control` со своей inline-SVG-стрелкой, `appearance: none`, `padding-right: 44px`, позиция `right 16px center`, размер 16px без изменений; `select.lf-control option { background: rgb(18 12 52); color: rgb(248 247 251); }`. `.lf-error { margin-top: 8px; color: var(--field-error); font: 400 13px/1.35 var(--font-body); }`.

    **Согласие (FORM-04).** `.lf-check { display: flex; align-items: flex-start; gap: 8px; cursor: pointer; }`; `.lf-checkbox { flex: 0 0 18px; width: 18px; height: 18px; margin: 2px 0 0; accent-color: rgb(170 217 220); cursor: pointer; }`; `.lf-checkbox:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 2px; }`; `.lf-checkbox[aria-invalid="true"] { outline: 2px solid var(--field-error); outline-offset: 2px; }` (перед правилом focus-visible, чтобы фокус перекрывал); `.lf-check-text { color: rgb(248 247 251 / .96); font: 650 14px/1.4 var(--font-body); }`. Все правила `.lf-check-box*` и `.lf-check:has(...)` удалить.

    **Кнопка (FORM-04).** `.lf-submit { margin-top: 8px; }` и `.lf-submit[aria-busy="true"] { opacity: .72; cursor: progress; }`; ширину, высоту и отступы даёт `.btn[data-beam][data-size="form"]` в global.css, дублировать нельзя. `.lf-submit-error { margin: 0; }` (расстояние даёт gap формы).

    **Тесты-инварианты.** В `LightForm.test.tsx` добавить describe «light-form.css: значения оригинала» по образцу `motionPolicy.test.ts`: `const CSS = readFileSync(resolve(process.cwd(), "src/components/form/light-form.css"), "utf8")` (импорты `node:fs`, `node:path`). Проверки: CSS не содержит `prefers-reduced-motion`, `.lf-section::before`, `.lf-card`, `.lf-check-box`, `.lf-legend`, `data-anim`, `--color-signal-400`, `--color-horizon-400`; блок `.lf-section {` (regex `/\.lf-section \{([^}]*)\}/`) не содержит `background`; CSS содержит каждую из строк: `--field-height: 54px`, `--field-radius: 16px`, `rgb(33 26 62 / .58)`, `rgb(239 237 245 / .18)`, `rgb(49 41 77 / .7)`, `0 0 0 3px rgb(123 194 199 / .12)`, `--field-border-focus: rgb(170 217 220)`, `rgb(33 26 62 / .42)`, `rgb(123 194 199 / .72)`, `0 0 24px var(--halo)`, `translateY(-2px)`, `width: 40px`, `radial-gradient(circle, var(--beacon) 0 2px, transparent 3px)`, `--beacon: rgb(170 217 220)`, `--beacon: rgb(227 175 210)`, `0 0 10px var(--beacon)`, `accent-color: rgb(170 217 220)`, `--form-width: 42rem`, `padding-block: 64px`, `margin-bottom: 48px`, `18px/1.65`, `rgb(219 215 232)`, `repeat(6, minmax(0, 1fr))`, `grid-column: span 3`, `rgb(252 165 165)`, `min-height: 190px`; блок `.lf-submit {` содержит `margin-top: 8px` и не содержит `width`; `CSS.match(/@media \(min-width: 768px\)/g)` длиной не меньше 3 (типы, карточка 190px, сетка).
  </action>
  <verify>
    <automated>npx tsc -b && npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts && npm run lint && npm run build</automated>
    <human-check>Для фазы 13 (QA-03), не блокирует план: `npm run dev`, 1440×900 и 390×844. Карточки типа с маячком слева сверху и кружком справа от названия; выбранная светится бирюзой (личный) или розовым (группа); поля 54px с radius 16; чекбокс нативный бирюзовый; кнопка на всю ширину через 8px; секция без фиолетового орба (до слияния с фазой 8 фон страницы); на 390 всё в одну колонку.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "prefers-reduced-motion\|\.lf-section::before\|\.lf-card\|\.lf-check-box\|\.lf-legend\|data-anim" src/components/form/light-form.css` даёт 0
    - `grep -c "0 0 24px var(--halo)\|radial-gradient(circle, var(--beacon) 0 2px, transparent 3px)\|accent-color: rgb(170 217 220)\|padding-block: 64px\|--form-width: 42rem\|18px/1.65\|rgb(219 215 232)\|repeat(6, minmax(0, 1fr))\|min-height: 190px" src/components/form/light-form.css` даёт 9
    - `grep -c "rgb(33 26 62 / .58)\|rgb(239 237 245 / .18)\|rgb(33 26 62 / .42)\|rgb(123 194 199 / .72)\|0 0 0 3px rgb(123 194 199 / .12)\|rgb(252 165 165)" src/components/form/light-form.css` не меньше 6
    - `awk '/^\.lf-submit \{/,/\}/' src/components/form/light-form.css` содержит `margin-top: 8px` и не содержит `width`
    - `awk '/^\.lf-section \{/,/\}/' src/components/form/light-form.css` не содержит `background`
    - `grep -c "\.lf-toast" src/components/form/light-form.css` не меньше 5 (блок тоста сохранён); `grep -c "\.lf-required" src/components/form/light-form.css` равен 2
    - `npx tsc -b`, `npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts`, `npm run lint`, `npm run build` без ошибок
    - `src/components/form/LightForm.test.tsx` содержит `readFileSync` и строку `light-form.css`
  </acceptance_criteria>
  <done>light-form.css несёт значения FORM-03/04/05 без фона и орба секции, инварианты значений и политика движения зелёные, билд проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ввод посетителя → состояние формы | Не меняется в этом плане: валидация и addLight из 09-01; план трогает разметку и CSS |
| CSS формы → чужие компоненты | Правило `.lf-section > div` перебивает отступы контейнера `Section`; область действия ограничена `#light-form` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `select.lf-control` inline SVG стрелка | accept | data-URL из репозитория без внешних адресов, как в v1.0 |
| T-09-05 | Denial of service | `.lf-section > div` переопределение отступов | mitigate | селектор ограничен `.lf-section`, тест структуры подтверждает, что секция и форма рендерятся; чужие секции не затронуты |
| T-09-06 | Information disclosure | `a11y`: скрытие фокуса `outline: none` на полях | mitigate | видимый индикатор фокуса задают рамка `rgb(170 217 220)` и кольцо 3px, группа типа и чекбокс держат `:focus-visible` outline |
| T-09-SC | Tampering | npm installs | accept | план не ставит пакетов |
</threat_model>

<verification>
- `npx tsc -b` без ошибок.
- `npx vitest run src/components/form src/lib/validation.test.ts src/styles/motionPolicy.test.ts` зелёный.
- `npm run lint` чист, `npm run build` собирает dist.
- Визуальная сверка с `docs/research/v1.1/orig-form.jpeg` и `orig-form-group.jpeg` откладывается на фазу 13 (QA-03, Playwright 1440×900 и 390×844).
</verification>

<success_criteria>
- FORM-03: карточки типа с поверхностью `rgb(33 26 62 / .42)`, маячком `::before` 40×40, точкой-радио 16px, выбранным состоянием `rgb(123 194 199 / .72)` + `0 0 24px var(--halo)`, ховером `translateY(-2px)`.
- FORM-04: поля 54px, radius 16px, фон `rgb(33 26 62 / .58)`, рамка `rgb(239 237 245 / .18)`, фокус `rgb(170 217 220)` + `0 0 0 3px rgb(123 194 199 / .12)`; чекбокс 18px с `accent-color`; кнопка `size="form"` во всю ширину с `margin-top: 8px`.
- FORM-05: eyebrow `rgb(170 217 220)`, лид 18px/1.65 `rgb(219 215 232)`, колонка 42rem, отступы секции 64px, форма через 48px после лида; `#light-form` и тост сохранены; секция без собственного фона и `::before`.
</success_criteria>

<output>
Create `.planning/phases/09-form-original/09-02-SUMMARY.md` when done
</output>
