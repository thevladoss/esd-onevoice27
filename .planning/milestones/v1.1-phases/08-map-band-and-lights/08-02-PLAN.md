---
phase: 08-map-band-and-lights
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/map/EsdMap.tsx
  - src/components/map/EsdMap.test.tsx
  - src/components/map/map.css
  - .planning/phases/08-map-band-and-lights/qa/map-probe.mjs
autonomous: true
requirements: [MAP-05, MAP-06, MAP-07]
tags: [map, svg, css-animation, performance, playwright]

must_haves:
  truths:
    - "Огоньки дышат свечением в пяти фазовых корзинах (index % 5): ядро 2.2px с обводкой #fff .9px opacity .5, ореол радиусом 6→12px и opacity группы .30→.60 с периодом 2.6s, цвета rgb(158 67 154) и rgb(84 164 172)"
    - "Анимации light-pulse в CSS нет, константы PULSE_EVERY нет; кольцо light-arrive у нового огонька после отправки формы остаётся"
    - "Chrome 1440×900 держит ≥ 50 fps с 942 огоньками (медиана трёх rAF-замеров по 2 с), иначе в keyframes остаётся только дыхание opacity, и это записано в SUMMARY"
    - "Без поддержки @property дышит только opacity групп (keyframes держат оба свойства, незарегистрированное игнорируется по решению CONTEXT)"
    - "При prefers-reduced-motion: reduce корзины без анимации (глобальное правило [data-anim]), ореолы статичны радиусом 6px"
    - "Счётчики карты выглядят как до фазы: --counter-accent закреплён литералами rgb(210 142 190) и rgb(123 194 199)"
  artifacts:
    - path: "src/components/map/EsdMap.tsx"
      provides: "defs с двумя radialGradient, пять <g class=\"light-bucket\" data-bucket=\"n\" data-anim=\"pulse\"> с ореолами, группа .light-cores с ядрами и кольцом нового огонька"
      exports: ["EsdMap", "LIGHT_CORE_RADIUS", "LIGHT_HALO_RADIUS", "LIGHT_BUCKETS"]
      contains: "light-bucket"
    - path: "src/components/map/map.css"
      provides: "@property --halo-k, @keyframes light-breathe, .light-bucket с задержками, .light-halo с r через --halo-k, .light-core с обводкой, цвета огоньков, закреплённые акценты счётчиков"
      contains: "@property --halo-k"
    - path: "src/components/map/EsdMap.test.tsx"
      provides: "Тесты корзин, ореолов, ядра, кольца нового огонька и текстовые проверки map.css"
      min_lines: 300
  key_links:
    - from: "src/components/map/EsdMap.tsx"
      to: "map.css .light-bucket"
      via: "className=\"light-bucket\" + data-bucket + data-anim=\"pulse\""
      pattern: "data-anim=\"pulse\""
    - from: "src/components/map/EsdMap.tsx .light-halo"
      to: "<radialGradient id=\"light-halo-person|group\">"
      via: "атрибут fill=url(#light-halo-<type>)"
      pattern: "url\\(#light-halo-"
    - from: "map.css .light-halo"
      to: "@property --halo-k на .light-bucket"
      via: "r: calc(var(--light-halo-r, 6px) * var(--halo-k, 1) / var(--zoom-k, 1))"
      pattern: "var\\(--halo-k, 1\\)"
    - from: "src/styles/global.css [data-anim] (не править)"
      to: ".light-bucket[data-anim=\"pulse\"]"
      via: "под reduce глобальный блок гасит animation и даёт статичный ореол"
      pattern: "data-anim=\"pulse\""
---

<objective>
Переделать огоньки карты под слои Mapbox оригинала: ядро с белой обводкой, ореол радиальным градиентом, «дыхание» радиуса и прозрачности на пяти фазовых корзинах через `@property --halo-k`, без старой пульсации `light-pulse`; замерить fps и при нехватке бюджета оставить только дыхание opacity.

Purpose: у оригинала (`awe-pin-lights-core` + пять слоёв `awe-pin-lights-glow-0…4`) поле огоньков дышит, а у нас каждый двадцать четвёртый огонёк мигал масштабом 1.8 и пропадал вместе с reveal (жалоба пользователя «огоньки пропадают и мигают»).
Output: `EsdMap.tsx` с корзинами и градиентами, блок огоньков в `map.css`, обновлённый `EsdMap.test.tsx`, режим `lights` в зонде, результаты замера fps и проверки reduced motion в SUMMARY.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-map-band-and-lights/08-CONTEXT.md
@.planning/phases/08-map-band-and-lights/08-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/components/map/EsdMap.tsx
@src/components/map/EsdMap.test.tsx
@src/components/map/map.css
@src/components/map/Counters.tsx
@src/styles/motionPolicy.test.ts
@src/App.test.tsx
@docs/research/v1.1/orig-map-dots.png

<ownership>
Фаза 8 идёт параллельно с фазами 7, 9–12. Редактировать ТОЛЬКО файлы из `files_modified`. Запрещено трогать `src/components/form/*` (фаза 9), `src/styles/global.css`, `src/components/layout/primitives.css` (фаза 7), `src/styles/motionPolicy.test.ts`, `src/App.test.tsx` (в этом плане не нужен), `src/components/map/Counters.tsx` (GLASS-05), `package.json`. Цвета литералами `rgb(r g b / a)`. Комментарии в коде на русском.

Поправка координатора к CONTEXT (обязательна): `src/styles/motionPolicy.test.ts` требует, чтобы `@media (prefers-reduced-motion: reduce)` был только в `global.css`, и держит закрытый реестр `data-anim` (`stars, globe, beam, pulse, new-light, particles, atmosphere, wave, halo`), причём литералы `data-anim="pulse"` и `data-anim="new-light"` обязаны остаться в коде. Поэтому в `map.css` медиазапроса reduced motion нет; пять групп корзин несут `data-anim="pulse"`, и глобальный блок гасит их анимацию (`[data-anim] { animation: none !important }`) и даёт статичное состояние (`[data-anim="pulse"] circle { transform: none; opacity: .9 }`, `[data-anim="pulse"] .light-halo { opacity: .22 }`). Статичная opacity ореола под reduce получается .22, а не .45 из спецификации: отклонение принято координатором, зафиксировать его в SUMMARY.
</ownership>

<interfaces>
<!-- Существующие контракты. Извлечены из кода, исследовать заново не нужно. -->

From src/data/lights.ts:
```ts
export type LightType = "person" | "group";
export interface Light { id: string; type: LightType; countryId: number; lon: number; lat: number; isNew?: boolean }
export function generateLights(seed?: number, people?: number, groups?: number): Light[]  // 694 person + 248 group = 942
```

From src/components/map/EsdMap.tsx (текущее):
```ts
export interface EsdMapProps { lights: readonly Light[]; selectedCountryId?: number | null; onUserZoomAway?: () => void; onError?: (hasError: boolean) => void; size?: { width: number; height: number }; flightKey?: number }
export const LIGHT_CORE_RADIUS = 2.2;
export const LIGHT_HALO_RADIUS = 6;
export const PULSE_EVERY = 24;          // удалить
// points: { light, x, y }[] в порядке lights (useMemo по projection и lights)
// coreRadius = LIGHT_CORE_RADIUS / transform.k; haloRadius = LIGHT_HALO_RADIUS / transform.k
// <g className="map-viewport" style={{ "--zoom-k", "--light-core-r": "2.2px", "--light-halo-r": "6px" }}>
//   <g className="map-countries">…</g>
//   <g className="map-lights" aria-hidden="true"> … per-light <g className="light light--person [pulse] [is-new]" data-anim="pulse"?> <circle .light-halo/> <circle .light-core/> [<circle .light-ring data-anim="new-light"/>] </g> … </g>
```

From src/styles/global.css (не править; правила, которые сработают на корзинах под reduce):
```css
@media (prefers-reduced-motion: reduce) {
  [data-anim], [data-anim]::before, [data-anim]::after { animation: none !important; }
  [data-anim="pulse"] circle { transform: none; opacity: .9; }
  [data-anim="pulse"] .light-halo { opacity: .22; }
  [data-anim="new-light"] { display: none; }
}
```

From src/components/map/map.css (текущие правила огоньков, строки 30–97, заменяются): `.light--person { color: var(--light-person) }`, `.light--group`, `.light-core { fill: currentColor; opacity: .9; r: calc(var(--light-core-r, 2.2px) / var(--zoom-k, 1)) }`, `.light-halo { fill: currentColor; opacity: .22; r: calc(var(--light-halo-r, 6px) / var(--zoom-k, 1)) }`, `.light-ring`, `.light.pulse`, `@keyframes light-pulse`, `.light.is-new .light-ring`, `@keyframes light-arrive`. Цвета сейчас `--light-person: #d28ebe; --light-group: #7bc2c7` на `.map-section`; `.counter--people { --counter-accent: var(--light-person) }`, `.counter--groups { --counter-accent: var(--light-group) }` (счётчики красятся этими же переменными, поэтому акценты закрепляются литералами).

Зонд из плана 01: `.planning/phases/08-map-band-and-lights/qa/map-probe.mjs <band|fps> [--url] [--width] [--height] [--runs] [--reduced]`, env `PW_ROOT`, `PW_CHANNEL`, `PW_HEADLESS`; способ запуска и порт preview описаны в 08-01-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Корзины, градиентные ореолы и ядро с обводкой в EsdMap.tsx; дыхание через @property в map.css</name>
  <files>src/components/map/EsdMap.tsx, src/components/map/map.css</files>
  <read_first>
    - .planning/phases/08-map-band-and-lights/08-CONTEXT.md, раздел «Огоньки» (все значения зафиксированы) и поправка координатора в `<ownership>` этого плана
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, MAP-05, MAP-06, MAP-07
    - src/components/map/EsdMap.tsx строки 36–40 (константы), 120–130 (`points`), 237–240 (радиусы), 301–336 (разметка огоньков)
    - src/components/map/map.css строки 30–97 (правила огоньков), 243–251 (`.map-section` с `--light-person`/`--light-group`), 166–178 (`.counter--people`/`.counter--groups`)
    - src/styles/global.css строки 298–330 и 344–348 (что глобальный блок делает с `[data-anim="pulse"]` и `[data-anim="new-light"]`; файл не править)
    - docs/research/v1.1/orig-map-dots.png (яркое ядро с белой кромкой, мягкое свечение вокруг)
  </read_first>
  <action>
    Реализует D «Огоньки» из CONTEXT (MAP-05, MAP-06, MAP-07) с поправкой координатора по reduced motion.

    1. `src/components/map/EsdMap.tsx`:
       - Удалить `PULSE_EVERY` и комментарий над ней. Добавить `export const LIGHT_BUCKETS = 5;` с комментарием: пять фазовых корзин, как пять glow-слоёв Mapbox у оригинала; корзина огонька равна `index % LIGHT_BUCKETS`, где index это позиция в `points`.
       - После `points` добавить `useMemo` `buckets`: массив из `LIGHT_BUCKETS` массивов `{ light, x, y }`, заполненный проходом по `points` (`buckets[index % LIGHT_BUCKETS].push(point)`), зависимость `[points]`.
       - В `<svg>` перед `<g className="map-viewport">` добавить `<defs>` с двумя градиентами: `<radialGradient id="light-halo-person" className="light-halo-def light-halo-def--person">` и `<radialGradient id="light-halo-group" className="light-halo-def light-halo-def--group">`; в каждом два `<stop>`: `offset="0"` с `stopColor="currentColor"` и `stopOpacity="0.9"`, `offset="1"` с `stopColor="currentColor"` и `stopOpacity="0"`. Цвет задаёт CSS через `color` на элементе градиента (аналог `circle-blur: 1` оригинала: цвет в центре, прозрачность на краю). Id литеральные, как в CONTEXT: карта на странице одна; тесты, рендерящие две карты в одном документе, проверяют структуру, а не отрисовку.
       - Разметку `.map-lights` заменить на два слоя. Первый: пять групп `<g key={n} className="light-bucket" data-bucket={n} data-anim="pulse">` (литерал `data-anim="pulse"` обязателен: реестр motionPolicy), внутри каждой ореолы её корзины: `<circle key={light.id} className="light-halo" cx={x} cy={y} r={haloRadius} fill={`url(#light-halo-${light.type})`} />`. Второй: `<g className="light-cores">`, внутри для каждого `point` группа `<g key={light.id} className={"light light--" + light.type + (light.isNew ? " is-new" : "")}>` с `<circle className="light-core" cx cy r={coreRadius} />` и, если `light.isNew`, `<circle className="light-ring" data-anim="new-light" cx cy r={haloRadius} />`. Класс `pulse`, атрибут `data-anim` на группе огонька и `style={{ animationDelay }}` удалить. Комментарий над слоями: ореолы лежат в корзинах, потому что дышит группа целиком (opacity и `--halo-k`), а ядра живут отдельным слоем и не тускнеют, как слой `awe-pin-lights-core` у оригинала.
       - Атрибут `r` у ореолов и ядер остаётся запасным для движков без геометрических свойств CSS; радиусы по-прежнему делятся на `transform.k`.

    2. `src/components/map/map.css` (блок огоньков, строки 30–97, переписать целиком):
       - В самом начале файла (до первого правила) объявить `@property --halo-k { syntax: "<number>"; inherits: true; initial-value: 1; }` с комментарием: множитель радиуса ореола, анимируется на пяти корзинах и наследуется кругами; без регистрации браузер меняет свойство только дискретно, поэтому keyframes держат и opacity (дыхание прозрачности остаётся в любом случае, решение CONTEXT).
       - `.light--person { color: var(--light-person); }`, `.light--group { color: var(--light-group); }` оставить; добавить `.light-halo-def--person { color: var(--light-person); }` и `.light-halo-def--group { color: var(--light-group); }`.
       - `.light-core { fill: currentColor; stroke: #fff; stroke-width: .9px; stroke-opacity: .5; vector-effect: non-scaling-stroke; r: calc(var(--light-core-r, 2.2px) / var(--zoom-k, 1)); }` (opacity .9 убрать: ядро полной яркости; `non-scaling-stroke` держит обводку .9px на любом зуме, иначе на k = 8 она стала бы 7px).
       - `.light-halo { r: calc(var(--light-halo-r, 6px) * var(--halo-k, 1) / var(--zoom-k, 1)); }` без `fill` и без `opacity` (заливку даёт атрибут `fill=url(#…)`, любое CSS-правило `fill` перебило бы его; прозрачностью управляет группа).
       - `.light-bucket { animation: light-breathe 2.6s ease-in-out infinite; }` и четыре правила задержек: `.light-bucket[data-bucket="1"] { animation-delay: calc(-2.6s * 1 / 5); }`, то же для 2, 3, 4 (корзина 0 без задержки). Комментарий: отрицательная задержка сдвигает фазу на n/5 периода, поле дышит, а не мигает синхронно.
       - `@keyframes light-breathe { 0%, 100% { --halo-k: 1; opacity: .3; } 50% { --halo-k: 2; opacity: .6; } }` (радиус 6px → 12px, opacity группы .30 → .60).
       - Удалить `.light.pulse` и `@keyframes light-pulse`. Оставить `.light-ring`, `.light.is-new .light-ring` и `@keyframes light-arrive` без изменений.
       - В блоке `.map-section` заменить `--light-person: #d28ebe` на `--light-person: rgb(158 67 154)` и `--light-group: #7bc2c7` на `--light-group: rgb(84 164 172)`.
       - В `.counter--people` заменить `--counter-accent: var(--light-person)` на `--counter-accent: rgb(210 142 190)`, в `.counter--groups` `var(--light-group)` на `rgb(123 194 199)` (те же цвета, что были: `#d28ebe` и `#7bc2c7`; счётчики по GLASS-05 выглядят как до фазы, `Counters.tsx` не меняется). Комментарий: акценты счётчиков отвязаны от цвета огоньков, у оригинала это signal-400 и horizon-400.
       - Никакого `@media (prefers-reduced-motion: reduce)` в map.css. Под reduce глобальный блок делает: `animation: none !important` на корзинах (`--halo-k` остаётся initial-value 1, радиус 6px, opacity группы 1), `.light-halo` получает opacity .22 (статичный ореол .22 вместо .45 спецификации, отклонение принято координатором), кольцо `new-light` скрыто. Записать этот расклад комментарием у `.light-bucket`.
       - Комментарий в шапке блока огоньков обновить: свечение градиентом вместо тени (сотни точек с filter не тянет ни один браузер), корзины вместо пульсации каждого 24-го огонька.

    Запрещено: `global.css`, `Counters.tsx`, `form/*`. Не менять поведение `useMapZoom`, `handleFrame`, `--zoom-k`.
  </action>
  <acceptance_criteria>
    - `grep -rn "PULSE_EVERY\|light-pulse\|\.pulse\b\|\" pulse\"" src --include='*.ts' --include='*.tsx' --include='*.css' | grep -v '\.test\.'` пуст
    - `grep -c 'data-anim="pulse"' src/components/map/EsdMap.tsx` равен 1; `grep -c 'data-anim="new-light"' src/components/map/EsdMap.tsx` равен 1
    - `EsdMap.tsx` содержит `export const LIGHT_BUCKETS = 5`, `className="light-bucket"`, `data-bucket={`, `className="light-cores"`, `id="light-halo-person"`, `id="light-halo-group"`, `stopColor="currentColor"`, `stopOpacity="0.9"`, `` fill={`url(#light-halo-${light.type})`} ``
    - `map.css` содержит ровно один `@property --halo-k {` с `syntax: "<number>";`, `inherits: true;`, `initial-value: 1;`
    - `map.css` содержит `@keyframes light-breathe {`, внутри `--halo-k: 1;`, `opacity: .3;`, `--halo-k: 2;`, `opacity: .6;`
    - `map.css` содержит `.light-bucket {` с `animation: light-breathe 2.6s ease-in-out infinite;`; `grep -c "animation-delay: calc(-2.6s \* " src/components/map/map.css` равен 4
    - Блок `.light-halo {` содержит `r: calc(var(--light-halo-r, 6px) * var(--halo-k, 1) / var(--zoom-k, 1));` и не содержит `fill:` и `opacity:`
    - Блок `.light-core {` содержит `stroke: #fff;`, `stroke-width: .9px;`, `stroke-opacity: .5;`, `vector-effect: non-scaling-stroke;` и не содержит `opacity: .9`
    - `map.css` содержит `--light-person: rgb(158 67 154);`, `--light-group: rgb(84 164 172);`, `--counter-accent: rgb(210 142 190);`, `--counter-accent: rgb(123 194 199);` и не содержит `#d28ebe`, `#7bc2c7`, `var(--light-person)` внутри `.counter--people`, `var(--light-group)` внутри `.counter--groups`
    - `grep -c "prefers-reduced-motion" src/components/map/map.css` равен 0
    - `@keyframes light-arrive` и `.light.is-new .light-ring` на месте
    - `npx tsc -b` код 0; `git status --porcelain` показывает только файлы из `files_modified`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run src/styles/motionPolicy.test.ts src/App.test.tsx</automated>
  </verify>
  <done>Огоньки рендерятся в пяти корзинах с градиентными ореолами и отдельным слоем ядер; дыхание описано через `@property --halo-k` и `light-breathe`; старой пульсации нет; цвета и акценты счётчиков зафиксированы; motionPolicy и App зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Тесты корзин и текстовые проверки map.css в EsdMap.test.tsx</name>
  <files>src/components/map/EsdMap.test.tsx</files>
  <read_first>
    - src/components/map/EsdMap.test.tsx (хелперы `count`, `AddLightHarness`, тесты «рисует 694 огонька…», «пульсирует не больше сорока…», «помечает пульсирующие огоньки…», «делит радиусы огоньков на масштаб», «на кадре жеста двигает только вьюпорт»)
    - src/styles/motionPolicy.test.ts строки 1–15 (паттерн `readFileSync(resolve(process.cwd(), …))` для текстовых проверок CSS)
    - src/components/map/MapBand.test.tsx из плана 01 (тот же паттерн блока `describe("map.css: …")`)
  </read_first>
  <action>
    1. Импортировать `LIGHT_BUCKETS` из `./EsdMap` (вместе с `LIGHT_CORE_RADIUS`, `LIGHT_HALO_RADIUS`); `readFileSync` из `node:fs`, `resolve` из `node:path`.
    2. Заменить тест «пульсирует не больше сорока огоньков и всегда пульсирует новым» на «раскладывает ореолы по пяти фазовым корзинам»: `count(".light-bucket")` равен `LIGHT_BUCKETS` (5); атрибуты `data-bucket` корзин равны `["0","1","2","3","4"]`; у каждой корзины от 188 до 189 `.light-halo` (942 = 5 × 188 + 2), сумма 942; у каждой корзины `data-anim="pulse"`; `count(".light-cores .light-core")` 942; `count(".light.pulse")` 0; после клика «зажечь» через `AddLightHarness`: `count(".light-halo")` 943, `count(".light.is-new")` 1, у нового огонька есть `.light-ring[data-anim="new-light"]`, а корзина с `data-bucket="2"` (942 % 5) получила 189 ореолов.
    3. Заменить тест «помечает пульсирующие огоньки и кольцо нового атрибутами реестра движения» на «держит data-anim только на корзинах и кольце нового огонька»: `count('[data-anim="pulse"]')` равен 5 и все они `g.light-bucket`; `count('circle[data-anim="pulse"]')` 0; `count('.light[data-anim]')` 0; после добавления `count('.light.is-new circle[data-anim="new-light"]')` 1.
    4. Добавить тест «красит ореол радиальным градиентом типа, ядро оставляет на currentColor»: в `svg` есть `radialGradient#light-halo-person` и `#light-halo-group`, у каждого два `stop` с `stop-opacity` `0.9` и `0`, `stop-color="currentColor"`; все `.light-halo` внутри `.light-bucket`; у ореолов огоньков людей `fill="url(#light-halo-person)"` ровно 694 штуки, у групповых `url(#light-halo-group)` 248; у `.light-core` атрибута `fill` нет (цвет через `.light--person`/`.light--group` на родительской группе: `count(".light--person")` 694, `count(".light--group")` 248).
    5. Тесты «рисует 694 огонька людей…», «делит радиусы огоньков на масштаб» (`.light-halo` берётся через `querySelector`, r = 6 / k), «на кадре жеста…» и «обходится без filter на огоньках» оставить: они должны пройти без правок (проверить, что `[filter]` по-прежнему 0: градиент не фильтр).
    6. Добавить блок `describe("map.css: дыхание огоньков")` с текстом `src/components/map/map.css`:
       a) ровно одно `@property --halo-k {` (regex с флагом g даёт длину 1), внутри `syntax: "<number>";`, `inherits: true;`, `initial-value: 1;`;
       b) `@keyframes light-breathe {` содержит `--halo-k: 2;` и `opacity: .6;` в кадре 50% и `--halo-k: 1;`, `opacity: .3;` в кадрах 0%/100% (regex `/@keyframes light-breathe \{[\s\S]*?--halo-k: 1;\s*opacity: \.3;[\s\S]*?--halo-k: 2;\s*opacity: \.6;/`);
       c) `.light-bucket {` содержит `animation: light-breathe 2.6s ease-in-out infinite;`; текст содержит `animation-delay: calc(-2.6s * 3 / 5);` и ровно 4 вхождения `animation-delay: calc(-2.6s * `;
       d) `.light-halo {` содержит `var(--halo-k, 1)`, не содержит `fill:`;
       e) `.light-core {` содержит `stroke: #fff;`, `stroke-width: .9px;`, `stroke-opacity: .5;`, `vector-effect: non-scaling-stroke;`;
       f) текст не содержит `light-pulse`, содержит `@keyframes light-arrive`;
       g) текст содержит `--light-person: rgb(158 67 154);`, `--light-group: rgb(84 164 172);`, `--counter-accent: rgb(210 142 190);`, `--counter-accent: rgb(123 194 199);`;
       h) текст не содержит `prefers-reduced-motion` (гашение под reduce отдано `global.css` через `data-anim="pulse"`).
       Если по итогам Task 3 сработал fallback (без `--halo-k` в keyframes), пункт b) переписать на проверку отсутствия `--halo-k` внутри `@keyframes light-breathe` при сохранении `opacity: .3;`/`.6;` и `@property`; в тесте оставить комментарий со ссылкой на замер в SUMMARY.
    7. Прогнать `npx vitest run src/components/map/EsdMap.test.tsx src/components/map/MapSection.test.tsx src/components/map/MapBand.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts`, `npx tsc -b`, `npm run lint`.
  </action>
  <acceptance_criteria>
    - В `EsdMap.test.tsx` нет строк `PULSE_EVERY`, `".light.pulse"` в позитивных проверках (допустимо только `expect(count(".light.pulse")).toBe(0)`); есть `LIGHT_BUCKETS`, `data-bucket`, `light-halo-person`, `light-halo-group`, `readFileSync`, `@property --halo-k`, `light-breathe`
    - `npx vitest run src/components/map/EsdMap.test.tsx src/components/map/MapSection.test.tsx src/components/map/MapBand.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts` код 0, все тесты passed, без `console.error` и предупреждений `act`
    - `npx tsc -b` код 0; `npm run lint` код 0
    - `npx vitest run` (весь набор) код 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map/EsdMap.test.tsx src/components/map/MapSection.test.tsx src/components/map/MapBand.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>Тесты покрывают пять корзин, градиентные ореолы, ядро с обводкой, кольцо нового огонька и текст CSS дыхания; весь набор vitest, tsc и lint зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Замер fps и проверка корзин в браузере (режим lights зонда), fallback при < 50 fps, запись в SUMMARY</name>
  <files>.planning/phases/08-map-band-and-lights/qa/map-probe.mjs, src/components/map/map.css, src/components/map/EsdMap.test.tsx</files>
  <read_first>
    - .planning/phases/08-map-band-and-lights/08-01-SUMMARY.md, раздел «Проверки MAP-03 и базовый fps» (команда запуска, резолв playwright, порт, базовый fps до смены огоньков)
    - .planning/phases/08-map-band-and-lights/qa/map-probe.mjs (структура режимов `band` и `fps`, парсинг аргументов, `--reduced`)
    - .planning/phases/08-map-band-and-lights/08-CONTEXT.md, «Замер fps» и «Reduced motion»
    - docs/qa/SMOKE.md строка 41 и замечание 2 в «Итог» (fps в Playwright-Chrome падал до 7–11 при открытой WebGL-вкладке и параллельных `npm test`/`npm run build`)
  </read_first>
  <action>
    Реализует MAP-06 (бюджет ≥ 50 fps, fallback на opacity) и MAP-07 (первый кадр, reduced motion).

    1. Добавить в `map-probe.mjs` режим `lights` (тот же CLI и контекст, что у `band`/`fps`):
       a) goto, `bringToFront`, `.map-shell.scrollIntoView({ block: "center" })`, ждать 1000 мс;
       b) структура: `count(".light-bucket")` 5, значения `data-bucket` 0…4, у всех `data-anim="pulse"`; `count(".light-halo")` 942 и у каждого атрибут `fill` вида `url(#light-halo-person|group)`; `count(".light-cores .light-core")` 942; `count(".light.pulse")` 0; `document.querySelectorAll("radialGradient").length` 2;
       c) вычисленные стили без `--reduced`: у `.light-bucket[data-bucket="0"]` `animationName === "light-breathe"`, `animationDuration === "2.6s"`; у `[data-bucket="3"]` `animationDelay === "-1.56s"`; у первого `.light-halo` `getComputedStyle(...).r` и у корзины `opacity` снять дважды с паузой 650 мс: оба значения меняются (дыхание радиуса живое, значит `@property` зарегистрирован); `typeof CSS.registerProperty === "function"` → `supportsProperty: true`; `getComputedStyle(.light-core).strokeWidth === "0.9px"`, `strokeOpacity === "0.5"`, `stroke === "rgb(255, 255, 255)"`; у `.light-core` в группе `.light--person` `fill === "rgb(158, 67, 154)"`, в `.light--group` `fill === "rgb(84, 164, 172)"`; у `.counter__value` в `.counter--people` `color === "rgb(210, 142, 190)"`, в `.counter--groups` `rgb(123, 194, 199)`;
       d) с `--reduced` (`emulateMedia({ reducedMotion: "reduce" })` до `goto`): у всех пяти корзин `animationName === "none"`, `opacity === "1"`; у первого `.light-halo` `opacity === "0.22"` и `r === "6px"`; два замера `r` с паузой 650 мс равны; `.light-core` 942;
       e) первый кадр (MAP-07): сразу после `goto` с `waitUntil: "domcontentloaded"` и одним `requestAnimationFrame` `count(".light-core") === 942` и `count(".light-bucket") === 5` (карта и огоньки в первом кадре, без плейсхолдеров); в JSON `firstFrame: true`;
       f) PASS, если все проверки соответствующего режима выполнены; вывод JSON + `PASS`/`FAIL`.
    2. `npm run build`, `node scripts/check-dist.mjs`, поднять preview (порт как в 08-01-SUMMARY), и прогнать при закрытых WebGL-вкладках и без параллельных `npm test`/`npm run build`:
       - `node …/map-probe.mjs lights --width 1440 --height 900`
       - `node …/map-probe.mjs lights --width 1440 --height 900 --reduced`
       - `node …/map-probe.mjs fps --width 1440 --height 900 --runs 3` (медиана трёх rAF-замеров по 2 с, окно Chrome на переднем плане)
       - `node …/map-probe.mjs band --width 1440 --height 900` (MAP-03 не сломался сменой огоньков)
    3. Решение по MAP-06: если медиана fps ≥ 50, дыхание радиуса остаётся. Если медиана < 50: в `map.css` внутри `@keyframes light-breathe` удалить обе строки `--halo-k: …;` (объявление `@property --halo-k` и `r: calc(… var(--halo-k, 1) …)` остаются: радиус держится 6px, дышит только opacity .3 → .6), обновить комментарий у `@property` (замер такой-то, радиус отключён), обновить пункт b) блока `map.css: дыхание огоньков` в `EsdMap.test.tsx` (см. Task 2, п. 6) и проверку `r` в режиме `lights` зонда (радиус постоянный, opacity меняется), затем повторить `npm run build`, `fps` и `lights`. Выбор фиксируется числами, а не оценкой на глаз.
    4. Записать в SUMMARY плана раздел «Замер fps и reduced motion»: канал браузера, headless или окно, вьюпорт, три значения fps и медиану, `maxGapMs`, базовый fps из 08-01-SUMMARY для сравнения, результат `lights` (обычный и `--reduced`) полным JSON, результат `band`, вердикт «дыхание радиуса оставлено» или «применён fallback на opacity (fps до/после)», а также принятое отклонение: статичный ореол под reduce имеет opacity .22 из глобального правила `[data-anim="pulse"] .light-halo`, а не .45 из спецификации (поправка координатора, `motionPolicy.test.ts` запрещает медиазапрос в map.css). Остановить preview.
  </action>
  <acceptance_criteria>
    - `map-probe.mjs` содержит режим `"lights"`, строки `light-breathe`, `"2.6s"`, `"-1.56s"`, `registerProperty`, `reducedMotion`, `"0.22"`, `"6px"`, `strokeWidth`
    - `node …/map-probe.mjs lights --width 1440 --height 900` печатает `PASS`, код 0; в JSON `buckets: 5`, `halos: 942`, `cores: 942`, `supportsProperty: true`, два значения `r` различаются (или, при применённом fallback, равны при различающейся `opacity`), `firstFrame: true`
    - `node …/map-probe.mjs lights --width 1440 --height 900 --reduced` печатает `PASS`, код 0; в JSON `animationName` всех корзин `none`, `haloOpacity: "0.22"`, `haloR: "6px"`
    - `node …/map-probe.mjs fps --width 1440 --height 900 --runs 3` печатает три значения и медиану; медиана ≥ 50 при финальной конфигурации CSS, строка `PASS`, код 0
    - `node …/map-probe.mjs band --width 1440 --height 900` по-прежнему `PASS`
    - Если применён fallback: в `@keyframes light-breathe` нет `--halo-k`, `@property --halo-k` и `var(--halo-k, 1)` на месте, тест `EsdMap.test.tsx` обновлён и зелёный, SUMMARY содержит fps до и после
    - SUMMARY плана содержит раздел «Замер fps и reduced motion» с числами, командами, вердиктом по MAP-06 и зафиксированным отклонением .22 вместо .45
    - `npx vitest run` (весь набор), `npx tsc -b`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` завершаются кодом 0; `git status --porcelain` показывает только файлы из `files_modified` планов 01 и 02
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/check-dist.mjs && (npx vite preview --port 4173 --strictPort & sleep 2; node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs lights --url http://localhost:4173/esd-onevoice27/ --width 1440 --height 900 && node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs fps --url http://localhost:4173/esd-onevoice27/ --width 1440 --height 900 --runs 3; code=$?; kill %1; exit $code)</automated>
  </verify>
  <done>В браузере пять корзин дышат радиусом и прозрачностью (или только прозрачностью после задокументированного fallback), медиана fps ≥ 50 на 1440×900, под reduce анимации нет и ореолы статичны, карта с огоньками в первом кадре; числа и команды записаны в SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл | входа данных нет; 942 анимированных SVG-круга нагружают только CPU/GPU клиента |
| зонд Playwright → localhost preview | локальный процесс |
| исполнитель → реестр npm (только при отсутствии `playwright` в кэше npx) | установка вне репозитория |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of service | `.light-bucket` (5 анимаций, 942 круга с пересчётом `r` через `--halo-k`) | mitigate | порог 50 fps по rAF-замеру, fallback на opacity без пересчёта геометрии; анимация на 5 группах, а не на 942 элементах |
| T-08-05 | Denial of service | `prefers-reduced-motion` | mitigate | `data-anim="pulse"` на корзинах: глобальный блок гасит анимацию, проверено режимом `lights --reduced` |
| T-08-06 | Tampering | результат замера fps | mitigate | сырые числа трёх прогонов и медиана в SUMMARY, команда воспроизводима фазой 13 |
| T-08-SC | Tampering | npm-установки | mitigate | зависимостей в репозиторий не добавляется; `playwright` из кэша npx MCP или scratch-установка `playwright@1.63.0` с npmjs.com (Microsoft), `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `npx vitest run` (весь набор), `npx tsc -b`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` зелёные
- Зонд `lights` PASS в обычном режиме (MAP-05: 5 корзин, градиентные ореолы, ядро с обводкой, `data-anim` только на корзинах и кольце; MAP-07: первый кадр) и с `--reduced` (MAP-07: анимации нет, ореол статичен)
- Зонд `fps` PASS: медиана ≥ 50 (MAP-06), при fallback числа до и после в SUMMARY
- Зонд `band` PASS: MAP-03 не сломан
- `git status` без файлов вне `files_modified`
</verification>

<success_criteria>
- Ядро 2.2px `currentColor` с обводкой `#fff` .9px opacity .5 и `non-scaling-stroke`; ореол `<circle fill="url(#light-halo-<type>)">` с `r: calc(var(--light-halo-r, 6px) * var(--halo-k, 1) / var(--zoom-k, 1))`; пять `<g class="light-bucket" data-bucket="n" data-anim="pulse">` с `light-breathe 2.6s ease-in-out infinite` и задержками `calc(-2.6s * n / 5)`; `@property --halo-k` в `map.css`
- Цвета `rgb(158 67 154)` / `rgb(84 164 172)`; акценты счётчиков закреплены `rgb(210 142 190)` / `rgb(123 194 199)`
- `light-pulse` и `PULSE_EVERY` удалены, `light-arrive` и `data-anim="new-light"` сохранены
- Медиана fps ≥ 50 на 1440×900 либо задокументированный fallback на opacity; reduced motion гасит дыхание через глобальный блок
- Тесты, tsc, lint, build, check-dist зелёные; SUMMARY с числами
</success_criteria>

<output>
Create `.planning/phases/08-map-band-and-lights/08-02-SUMMARY.md` when done (с разделом «Замер fps и reduced motion»: команды, JSON, вердикт MAP-06, отклонение .22 вместо .45)
</output>
