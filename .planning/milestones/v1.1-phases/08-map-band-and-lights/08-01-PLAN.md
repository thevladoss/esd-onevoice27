---
phase: 08-map-band-and-lights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/map/MapBand.tsx
  - src/components/map/MapBand.test.tsx
  - src/components/map/MapSection.tsx
  - src/components/map/MapSection.test.tsx
  - src/components/map/map.css
  - .planning/phases/08-map-band-and-lights/qa/map-probe.mjs
autonomous: true
requirements: [MAP-01, MAP-02, MAP-03, MAP-04]
tags: [map, css, layout, playwright]

must_haves:
  truths:
    - "Под картой и формой одна лента .map-band с подложкой rgb(18 12 52) и clip-path polygon(0 var(--map-wedge), 100% 0, 100% 100%, 0 100%); секции #map и #light-form внутри неё прозрачные"
    - "Между низом карты и формой нет второй линии: выборка пикселей по x = 200 и x = 1240 на отрезке «низ карты + 120px» на 1440×900 не содержит перепада яркости > 6, кроме линии скоса"
    - "Нижний орб центрирован на нижнем правом крае карты (translate(38%, -50%)) и светит в область формы без обрезки; на <768px он полоса во всю ширину"
    - "Карта и огоньки видны в первом кадре: вокруг .map-container нет Reveal, заголовок и счётчики по-прежнему появляются каскадом"
    - "Горизонтальной прокрутки нет: scrollWidth <= innerWidth + 1 на 1440 и 390"
  artifacts:
    - path: "src/components/map/MapBand.tsx"
      provides: "Лента: <div class=\"map-band\"> с <MapSection /> и <LightForm /> внутри"
      exports: ["MapBand"]
      min_lines: 10
    - path: "src/components/map/map.css"
      provides: "Блоки .map-band, .map-band::before, .map-band .lf-section, .map-band .lf-section::before, орб .map-orb--bottom с top: 100%"
      contains: ".map-band::before"
    - path: "src/components/map/MapBand.test.tsx"
      provides: "Структура ленты и текстовые проверки map.css"
      min_lines: 40
    - path: ".planning/phases/08-map-band-and-lights/qa/map-probe.mjs"
      provides: "Playwright-зонд: режимы band (MAP-03, scrollWidth, первый кадр) и fps (rAF за 2 с)"
      min_lines: 120
  key_links:
    - from: "src/App.tsx"
      to: "src/components/map/MapBand.tsx"
      via: "<MapBand /> вместо пары <MapSection /><LightForm />"
      pattern: "<MapBand />"
    - from: "src/components/map/MapBand.tsx"
      to: "src/components/form/LightForm.tsx"
      via: "вызов <LightForm /> внутри .map-band (без правок формы)"
      pattern: "<LightForm />"
    - from: "src/components/map/map.css"
      to: ".lf-section (light-form.css фазы 9)"
      via: "правила .map-band .lf-section { background: transparent } и ::before { content: none }"
      pattern: "\\.map-band \\.lf-section"
    - from: "src/components/map/MapSection.tsx"
      to: ".map-shell > .map-container"
      via: "контейнер карты прямой ребёнок .map-shell, без Reveal"
      pattern: "className=\"map-container\""
---

<objective>
Собрать карту и форму на одной скошенной ленте `.map-band`, убрать вторую линию среза между ними, центрировать нижний орб на нижнем крае карты, снять reveal с контейнера карты и построить Playwright-зонд, который доказывает MAP-03 пиксельной выборкой и даёт базовый замер fps до смены огоньков.

Purpose: у оригинала карта и форма живут в одном `#ov-map` с единой подложкой; у нас две секции с двумя непрозрачными подложками, и орб карты режется прямой линией на границе с формой (см. `docs/research/v1.1/prod-map-bottom-before.jpeg` против `orig-map-bottom.jpeg`). Reveal вокруг карты гасил огоньки при прокрутке.
Output: `MapBand.tsx` + тест, обновлённые `App.tsx`, `MapSection.tsx` + тест, блок ленты и орбов в `map.css`, зонд `qa/map-probe.mjs`, результаты прогонов в SUMMARY.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-map-band-and-lights/08-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/App.tsx
@src/App.test.tsx
@src/components/map/MapSection.tsx
@src/components/map/MapSection.test.tsx
@src/components/map/map.css
@src/components/layout/Reveal.tsx
@src/components/form/light-form.css
@src/styles/motionPolicy.test.ts
@docs/qa/SMOKE.md

<ownership>
Фаза 8 идёт параллельно с фазами 7, 9–12 в отдельном worktree от одного `main`. Редактировать ТОЛЬКО файлы из `files_modified` этого плана. Запрещено трогать `src/components/form/*` (фаза 9), `src/styles/global.css` и `src/components/layout/primitives.css` (фаза 7), `src/styles/motionPolicy.test.ts`, `package.json`, `package-lock.json`, `vite.config.ts`. Правила для чужих селекторов (`.lf-section`) живут только в `map.css`. Цвета в CSS писать литералами `rgb(r g b / a)`. Комментарии в коде на русском.
</ownership>

<interfaces>
<!-- Существующие контракты, которые план использует. Извлечены из кода, исследовать заново не нужно. -->

From src/components/map/MapSection.tsx:
```tsx
export function MapSection(): JSX.Element
// рендерит <section id="map" className="map-section" aria-labelledby="map-title">
//   <div className="map-section__skew" aria-hidden="true" />   <- удалить
//   <span className="map-orb map-orb--top" aria-hidden="true" />
//   <span className="map-orb map-orb--bottom" aria-hidden="true" />
//   <div className="map-section__inner"> <Reveal className="map-section__header">…</Reveal> <CountryChips … /> </div>
//   <div className="map-stage"> <div className="map-stage__panel"><Counters /></div>
//     <div className="map-shell"> <Reveal delay={0.1}> <div className="map-container"><EsdMap … /></div> </Reveal> </div>   <- Reveal убрать
//     <ZoomHint /> </div>
```

From src/components/form/LightForm.tsx (не править, только вызывать):
```tsx
export function LightForm(): JSX.Element
// рендерит <Section id="light-form" titleId="form-title" className="lf-section"> → <section id="light-form" class="lf-section" aria-labelledby="form-title">
```

From src/components/layout/Reveal.tsx:
```tsx
export function Reveal<T extends RevealTag = "div">(props: { as?: T; className?: string; delay?: number; children: ReactNode; ref?: Ref<…> } & AriaAttributes)
// без reduce рендерит motion-элемент с initial={{ opacity: 0, y: shift }} и whileInView
```

From src/state/lights.tsx:
```tsx
export function LightsProvider(props: { initialLights?: Light[]; children: ReactNode }): JSX.Element
export function useLights(): LightsValue
```

Существующие правила map.css, которые остаются как есть: `.map-section::before` (три ореола у верхней кромки, clip-path по скосу), `.map-orb`, `.map-orb--top`, `.map-section__inner` (padding с `var(--map-wedge)`), `.map-shell` (полигон `0 var(--map-wedge), 100% 0, 100% calc(100% - var(--map-wedge)), 0 100%`), `.map-shell::before`, `.map-container`, `.map-hint`.

Текущий `.lf-section` в light-form.css (фаза 9, не править): `background: rgb(18 12 52)`, `overflow-x: clip`, `isolation: isolate`, `position: relative`; `.lf-section::before` рисует свой орб `translate(38%, -50%)`. Именно эта непрозрачная подложка и режет орб карты.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Лента .map-band: MapBand.tsx, App.tsx, MapSection без подложки и reveal, CSS ленты и орбов</name>
  <files>src/components/map/MapBand.tsx, src/App.tsx, src/components/map/MapSection.tsx, src/components/map/map.css</files>
  <read_first>
    - .planning/phases/08-map-band-and-lights/08-CONTEXT.md (раздел «Лента»: все значения зафиксированы)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел 2, пункты MAP-01, MAP-02, MAP-03, MAP-04
    - src/components/map/map.css (блоки `.map-section`, `.map-section__skew`, `.map-orb--bottom` и его мобильный медиаблок, строки 243–335)
    - src/components/map/MapSection.tsx (строки 36–75: подложка, орбы, Reveal вокруг .map-container)
    - src/App.tsx (импорты MapSection и LightForm, строки 5–6 и 22–23)
    - src/components/form/light-form.css строки 4–50 (что именно гасят правила `.map-band .lf-section`; файл не править)
    - docs/research/v1.1/orig-rules.css строки 29–38 (`#ov-map`, `.ov-map-skew-mask--bottom::after`: якорь `top: 50%; right: 0; transform: translate(38%, -50%)`, мобильный вариант `translateY(-50%)`)
  </read_first>
  <action>
    Реализует D «Лента» из CONTEXT (MAP-01, MAP-02, MAP-04).

    1. Создать `src/components/map/MapBand.tsx`: именованный экспорт `MapBand`, рендерит `<div className="map-band"><MapSection /><LightForm /></div>`; импорты `./MapSection`, `../form/LightForm` (форму только вызываем), `./map.css`. Комментарий над компонентом: лента даёт одну скошенную подложку на две секции, как `#ov-map` у оригинала. Никаких aria-атрибутов и ролей на обёртке: секции `#map` и `#light-form` остаются `<section>` с прежними `aria-labelledby`, тесты App ищут их по id.

    2. В `src/App.tsx` заменить пару `<MapSection />` и `<LightForm />` на `<MapBand />`; импорт `MapBand` из `./components/map/MapBand`; импорты `MapSection` и `LightForm` удалить (иначе lint no-unused-vars). Порядок секций в `main` не меняется: Hero → MapBand → About → …

    3. В `src/components/map/MapSection.tsx`: удалить `<div className="map-section__skew" aria-hidden="true" />` и комментарий над ним; оба `<span className="map-orb …">` оставить на месте (они дети `#map`, тест «держит оба ореола» ждёт `#map > .map-orb` × 2). Внутри `.map-shell` снять обёртку `<Reveal delay={0.1}>`: `<div className="map-container">` становится единственным прямым ребёнком `.map-shell` (MAP-04, у оригинала карта видна сразу). Импорт `Reveal` оставить: он держит `.map-section__header`. Комментарий над `.map-shell` переписать: обёртки появления нет, карта и огоньки видны в первом кадре, каскад остаётся у заголовка и счётчиков.

    4. В `src/components/map/map.css`:
       - Перед блоком `.map-section` добавить блок ленты: `.map-band { --map-wedge: clamp(32px, 3.2vw, 52px); position: relative; isolation: isolate; overflow-x: clip; margin-top: calc(0px - var(--map-wedge) - 1px); }` с комментарием: горизонталь клипует лента (орбы уезжают за края на 38% и раздвигали бы scrollWidth), вертикаль открыта; лента заходит под hero на глубину скоса.
       - `.map-band::before { content: ""; position: absolute; inset: 0; z-index: -1; background: rgb(18 12 52); clip-path: polygon(0 var(--map-wedge), 100% 0, 100% 100%, 0 100%); pointer-events: none; }`.
       - `.map-band .lf-section { background: transparent; }` и `.map-band .lf-section::before { content: none; }` с комментарием: подложку и орб даёт лента; фаза 9 убирает те же правила из `light-form.css`, после слияния строки становятся no-op.
       - В блоке `.map-section` удалить `--map-wedge` (переехала на ленту и наследуется), `margin-top: calc(0px - var(--map-wedge) - 1px)` и `overflow-x: clip` вместе с их комментариями; оставить `--map-atmosphere`, `--map-transition`, `--light-person`, `--light-group`, `position: relative`, `isolation: isolate`, `color: #f8f7fb`. Обновить вводный комментарий: секция стоит на ленте и своей подложки не имеет.
       - Удалить блок `.map-section__skew` целиком.
       - `.map-orb--bottom`: заменить `bottom: 0` на `top: 100%`, `transform: translate(38%, 50%)` на `transform: translate(38%, -50%)`; `right: 0`, `width: clamp(520px, 72vw, 1100px)` и градиент `radial-gradient(circle, rgb(132 53 127 / .15) 0, rgb(39 52 102 / .22) 30%, rgb(22 29 61 / .34) 48%, transparent 70%)` оставить. Пояснить в комментарии: верхняя кромка орба на нижнем крае секции (секция заканчивается низом карты), сдвиг на −50% ставит центр ровно на кромку, и половина орба светит в форму.
       - В медиаблоке `@media (max-width: 767px)` для `.map-orb--bottom` заменить `transform: translateY(50%)` на `transform: translateY(-50%)`; `width: 100%`, `height: min(clamp(520px, 72vw, 1100px), 135vw)`, `aspect-ratio: auto`, `border-radius: 0` и градиент `circle at 112% 50%` оставить.
       - z-index (дискреция): орбы остаются `z-index: -1` внутри изолированного контекста `.map-section`, то есть выше `.map-band::before` (он `z-index: -1` в контексте ленты, ниже всех её детей) и ниже `.map-section::before` (0), `.map-section__inner` (1) и `.map-stage`. Контент формы (`.lf-section` идёт в DOM позже) рисуется поверх орба, его прозрачный фон орб не закрывает. Записать это соображение комментарием у `.map-orb`.
       - `.map-shell`, `.map-shell::before`, `.map-section::before`, `.map-hint` не трогать.

    Ничего из `src/components/form/*`, `global.css`, `primitives.css` не редактировать. Никаких `@media (prefers-reduced-motion)` в map.css (единственный блок живёт в global.css, тест motionPolicy это проверяет).
  </action>
  <acceptance_criteria>
    - `src/components/map/MapBand.tsx` существует, содержит строки `export function MapBand`, `className="map-band"`, `<MapSection />`, `<LightForm />`, `import "./map.css"`
    - `grep -n "MapBand\|MapSection\|LightForm" src/App.tsx` даёт ровно две строки: импорт `MapBand` и `<MapBand />`; строк с `MapSection` и `LightForm` нет
    - `grep -rn "map-section__skew" src --include='*.tsx' --include='*.css' | grep -v '\.test\.tsx'` пуст
    - В `src/components/map/MapSection.tsx` нет строки `<Reveal delay={0.1}>`; строка `<div className="map-container">` идёт сразу после строки с `className="map-shell"` (между ними только комментарий); `import { Reveal }` присутствует и используется для `.map-section__header`
    - В `map.css` блок `.map-band {` содержит `--map-wedge: clamp(32px, 3.2vw, 52px);`, `position: relative;`, `isolation: isolate;`, `overflow-x: clip;`, `margin-top: calc(0px - var(--map-wedge) - 1px);`
    - В `map.css` блок `.map-band::before {` содержит `background: rgb(18 12 52);` и `clip-path: polygon(0 var(--map-wedge), 100% 0, 100% 100%, 0 100%);` и `z-index: -1;`
    - В `map.css` есть `.map-band .lf-section {` с `background: transparent;` и `.map-band .lf-section::before {` с `content: none;`
    - Блок `.map-section {` (текст между `.map-section {` и первой `}`) не содержит `margin-top`, `overflow-x`, `--map-wedge`
    - Блок `.map-orb--bottom {` содержит `top: 100%;`, `right: 0;`, `transform: translate(38%, -50%);` и не содержит `bottom: 0`; мобильный блок содержит `transform: translateY(-50%);` и не содержит `translateY(50%)`
    - `grep -c "prefers-reduced-motion" src/components/map/map.css` даёт 0
    - `git status --porcelain` показывает изменения только в файлах из `files_modified` плана
    - `npx tsc -b` завершается без ошибок
  </acceptance_criteria>
  <verify>
    <automated>npx tsc -b && npx vitest run src/App.test.tsx src/styles/motionPolicy.test.ts</automated>
  </verify>
  <done>Приложение рендерит `main > div.map-band > section#map + section#light-form`; CSS ленты, подложки и орбов соответствует CONTEXT; секция карты без собственной подложки и без Reveal вокруг контейнера; tsc и тесты App зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Тесты ленты: MapBand.test.tsx и обновление MapSection.test.tsx</name>
  <files>src/components/map/MapBand.test.tsx, src/components/map/MapSection.test.tsx</files>
  <read_first>
    - src/components/map/MapSection.test.tsx (хелпер `renderSection`, тесты «держит скошенную подложку внутри секции» и «держит оба ореола границ», заглушки `stubSize`/`stubReducedMotion`)
    - src/styles/motionPolicy.test.ts строки 1–15 (паттерн чтения CSS с диска через `readFileSync(resolve(process.cwd(), …))`: vitest настроен с `css: false`, вычисленных стилей в jsdom нет, проверяем текст файла)
    - src/App.test.tsx (хелпер `renderApp` под `LightsProvider`, проверки секций по id)
  </read_first>
  <action>
    1. Создать `src/components/map/MapBand.test.tsx`. Рендер через `render(<LightsProvider><MapBand /></LightsProvider>)` (карта, счётчики и форма читают контекст огоньков). Тесты:
       - «оборачивает карту и форму одной лентой»: `container.querySelector(".map-band")` не null, `tagName === "DIV"`, у обёртки ровно один атрибут (`class`), `band.children` длиной 2, `children[0]` это `section#map` с классом `map-section`, `children[1]` это `section#light-form` с классом `lf-section`; порядок именно такой.
       - «не рисует собственную подложку секции карты»: `document.querySelector(".map-section__skew")` null; `#map > .map-orb` по-прежнему 2 узла с `aria-hidden="true"`.
       - «показывает контейнер карты без обёртки появления»: `.map-shell` содержит ровно одного ребёнка, это `.map-container`, у него нет атрибута `style` (motion-обёртка ставила inline opacity 0); при этом `.map-section__header` существует и содержит `h2#map-title` (каскад заголовка остался).
       - Блок `describe("map.css: лента")` с текстом файла через `readFileSync(resolve(process.cwd(), "src/components/map/map.css"), "utf8")`:
         a) блок `.map-band {` содержит `isolation: isolate;`, `overflow-x: clip;`, `margin-top: calc(0px - var(--map-wedge) - 1px);`, `--map-wedge: clamp(32px, 3.2vw, 52px);`;
         b) блок `.map-band::before {` содержит `background: rgb(18 12 52);` и `clip-path: polygon(0 var(--map-wedge), 100% 0, 100% 100%, 0 100%);`;
         c) есть `.map-band .lf-section {` с `background: transparent;` и `.map-band .lf-section::before {` с `content: none;`;
         d) текст не содержит `.map-section__skew`; блок `.map-section {` (regex `/\.map-section \{([^}]*)\}/`) не содержит `margin-top`, `overflow-x`, `--map-wedge`;
         e) блок `.map-orb--bottom {` содержит `top: 100%;` и `transform: translate(38%, -50%);`; текст содержит `transform: translateY(-50%);` и не содержит `translateY(50%)`;
         f) блок `.map-shell {` содержит `100% calc(100% - var(--map-wedge))` (полигон MAP-03 на месте);
         g) текст не содержит `prefers-reduced-motion` (политика живёт только в global.css).
    2. В `src/components/map/MapSection.test.tsx` заменить тест «держит скошенную подложку внутри секции» на «не несёт собственной подложки: скос рисует лента» (`#map .map-section__skew` длиной 0). Добавить тест «рисует контейнер карты прямым ребёнком .map-shell без inline-стилей» (`shell.children` длиной 1, `shell.firstElementChild` имеет класс `map-container`, `getAttribute("style")` равен null). Остальные тесты не менять.
    3. Прогнать `npx vitest run src/components/map/MapBand.test.tsx src/components/map/MapSection.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts`, затем `npx tsc -b` и `npm run lint`. Предупреждений `act` и ошибок консоли в выводе быть не должно (тест App «рендерит страницу молча» роняет прогон при любом console.error).
  </action>
  <acceptance_criteria>
    - Файл `src/components/map/MapBand.test.tsx` существует, содержит `readFileSync`, `".map-band"`, `"map-section__skew"`, `"prefers-reduced-motion"`, `translate(38%, -50%)`
    - В `MapSection.test.tsx` нет строки `expect(skew).toHaveLength(1)`; есть проверка `.map-section__skew` длиной 0 и проверка `.map-shell` с единственным ребёнком `.map-container` без атрибута `style`
    - `npx vitest run src/components/map/MapBand.test.tsx src/components/map/MapSection.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts` завершается с кодом 0, все тесты passed, в выводе нет `Warning: An update` и `console.error`
    - `npx tsc -b` код 0; `npm run lint` код 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map/MapBand.test.tsx src/components/map/MapSection.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>Новый и обновлённый тесты зелёные вместе с App и motionPolicy; tsc и lint чистые.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright-зонд map-probe.mjs (режимы band и fps) и прогон на preview 1440×900 и 390×844</name>
  <files>.planning/phases/08-map-band-and-lights/qa/map-probe.mjs</files>
  <read_first>
    - docs/qa/SMOKE.md строки 15–27 и 41 (как поднимали preview, вьюпорты 1440×900 и 390×844, DPR 1, замер fps через rAF, «FPS мерить при закрытых WebGL-вкладках»)
    - .planning/STATE.md, решение «[Phase 05]: … FPS в Playwright-Chrome мерить при закрытых WebGL-вкладках»
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, MAP-03 (формула проверки) и MAP-06 (метод замера fps)
    - src/components/map/map.css: `.map-shell` (полигон: левая точка `0 100%`, правая `100% calc(100% - var(--map-wedge))`, значит y линии скоса на абсциссе x равен bottom − wedge · x / width), `.map-hint` (на ≥768 подсказка стоит выше скоса на 16px + wedge и в окно выборки не попадает)
  </read_first>
  <action>
    Реализует проверку MAP-03 из CONTEXT («скриптом Playwright по пиксельным выборкам; описать в SUMMARY как воспроизводимую команду для фазы 13») и базовый замер fps для MAP-06 (план 02 сравнит «до» и «после»).

    1. Написать ESM-скрипт `.planning/phases/08-map-band-and-lights/qa/map-probe.mjs` (каталог `qa/` создать). Зависимости в репозиторий не добавлять: пакет `playwright` резолвить через `createRequire` в таком порядке: `process.env.PW_ROOT` (каталог, где лежит `node_modules/playwright`), затем первый найденный `~/.npm/_npx/*/node_modules/playwright` (кэш Playwright MCP, сейчас `~/.npm/_npx/9833c18b2d85bc59/node_modules/playwright` 1.63.0-alpha с chromium-1234 в `~/Library/Caches/ms-playwright`), затем обычный `import("playwright")`. Если ничего не найдено, напечатать подсказку `mkdir -p <scratch>/pw && cd <scratch>/pw && npm init -y && npm i playwright@1.63.0 && npx playwright install chromium`, затем `PW_ROOT=<scratch>/pw node …` и выйти с кодом 2. Пакет `playwright` на npmjs.com принадлежит Microsoft (мейнтейнеры microsoft1es, dgozman-ms), версия 1.63.0 проверена 2026-09-06.
       CLI: `node map-probe.mjs <band|fps> [--url http://localhost:4173/esd-onevoice27/] [--width 1440] [--height 900] [--runs 3] [--reduced]`; env `PW_CHANNEL` (`chrome` по умолчанию, при ошибке запуска повтор с встроенным chromium), `PW_HEADLESS` (`0` по умолчанию: fps нужен GPU в окне на переднем плане). Контекст с `viewport: {width, height}`, `deviceScaleFactor: 1`, `reducedMotion: "reduce"` при `--reduced`. Вывод: одна JSON-строка результатов плюс итоговая строка `PASS`/`FAIL`, код выхода 0/1.
       Режим `band`:
       a) `page.goto(url, { waitUntil: "load" })`, `page.bringToFront()`, дождаться одного кадра (`requestAnimationFrame`) и без прокрутки снять `firstFrame`: `getComputedStyle(.map-container).opacity === "1"`, у `.map-container` нет атрибута `style`, `document.querySelectorAll(".light-core").length === 942`, у `.map-shell` computed `clipPath` содержит `calc(100% - ` (полигон MAP-03 на месте), у `.map-band` computed `clipPath` через `getComputedStyle(band, "::before").clipPath` начинается с `polygon(`;
       b) прокрутка: `shellBottomPage = .map-shell.getBoundingClientRect().bottom + scrollY`; `window.scrollTo(0, shellBottomPage - 450)`; ждать 1500 мс (скрытие шапки 420 мс и reveal заголовка формы 700 мс должны закончиться);
       c) `wedge = clamp(32, 0.032 * width, 52)` (custom property `--map-wedge` из getComputedStyle возвращается строкой clamp, поэтому считается в скрипте; на 1440 это 46.08px); `rect = .map-shell.getBoundingClientRect()` (координаты вьюпорта после прокрутки); для каждого x из списка (на ширине ≥ 768: `[200, 1240]`; на < 768: `[20, width - 20]`, информативно) `ySkew = rect.bottom - wedge * x / rect.width`;
       d) `page.screenshot({ type: "png" })` вьюпорта (без clip и fullPage: координаты совпадают с getBoundingClientRect при DPR 1); декодировать без внешних библиотек: открыть `context.newPage()`, `setContent('<canvas id="c"></canvas>')`, в `evaluate` создать `Image` из `data:image/png;base64,…`, `await img.decode()`, `drawImage` на canvas, `getImageData(x, y0, 1, rows)` для каждого x, где `y0 = floor(ySkew - 6)`, `rows = 127` (до `ySkew + 120`);
       e) яркость строки `L = 0.2126 R + 0.7152 G + 0.0722 B`; перепад `|L[i+1] − L[i]|`; строки с перепадом > 6 вне окна `[ySkew − 3, ySkew + 3]` записать в `jumps` с y и значением; критерий MAP-03 выполнен, если на ширине 1440 для обеих абсцисс `jumps` пуст; в JSON вывести `maxJumpOutsideSkew`, `skewJump` (перепад на самой линии, ожидается > 6, иначе скос не виден), `jumps`;
       f) `scrollWidth`: после `window.scrollTo(0, document.body.scrollHeight)` проверить `document.documentElement.scrollWidth <= innerWidth + 1`; в JSON `scrollWidth`, `innerWidth`;
       g) PASS на 1440×900 = `firstFrame` все true, `jumps` пусты для x = 200 и x = 1240, `skewJump > 6` хотя бы на одной абсциссе, scrollWidth в норме; на 390×844 PASS = `firstFrame`, scrollWidth в норме, выборки по x печатаются как информация.
       Режим `fps`:
       a) goto, bringToFront, `document.querySelector(".map-shell").scrollIntoView({ block: "center" })`, ждать 2500 мс (reveal, счёт счётчиков, скрытие шапки);
       b) `--runs` раз (по умолчанию 3, пауза 300 мс между) выполнить в `page.evaluate` счётчик `requestAnimationFrame` за 2000 мс: вернуть `fps = frames / (elapsed / 1000)` и `maxGapMs` (максимальный интервал между кадрами); напечатать все прогоны, `median`, `maxGapMs`; PASS при `median >= 50`;
       c) в шапке вывода напечатать канал браузера (`chrome`/`chromium`), headless или нет, вьюпорт и `--reduced`.
    2. Собрать и поднять preview: `npm run build` (без предупреждений о размере чанков), `node scripts/check-dist.mjs`, `npx vite preview --port 4173 --strictPort` в фоне (если порт занят, `--port 4174` и тот же порт в `--url`). Прогнать:
       - `node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs band --width 1440 --height 900`
       - `node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs band --width 390 --height 844`
       - `node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs fps --width 1440 --height 900 --runs 3` (базовый замер до смены огоньков; во время замера не запускать `npm test`/`npm run build`, закрыть вкладки с WebGL)
       Все три прогона должны дать PASS. Если `band` на 1440 падает по `jumps`, снять скриншот области (`page.screenshot` с `clip` вокруг `ySkew ± 130`) и разобраться, откуда перепад (типичные причины: орб всё ещё клипуется `.lf-section`, у секции формы остался непрозрачный фон, не переехал `top: 100%`); чинить только в файлах фазы.
    3. Записать в SUMMARY плана (раздел «Проверки MAP-03 и базовый fps»): точные команды (включая способ резолва `playwright` и порт), JSON-вывод обоих `band`-прогонов (`jumps`, `maxJumpOutsideSkew`, `skewJump`, `scrollWidth`, `firstFrame`) и `fps`-прогона (три значения, медиана, `maxGapMs`, канал браузера, headless или окно). Эти же команды фаза 13 использует для таблицы SMOKE.md по MAP-03 и MAP-06. Остановить preview после прогонов.
  </action>
  <acceptance_criteria>
    - Файл `.planning/phases/08-map-band-and-lights/qa/map-probe.mjs` существует, содержит строки `createRequire`, `PW_ROOT`, `deviceScaleFactor: 1`, `"band"`, `"fps"`, `0.2126`, `requestAnimationFrame`, `scrollWidth`, `> 6` (порог перепада) и `50` (порог fps); внешних зависимостей, кроме `playwright` и модулей `node:*`, не импортирует
    - `git status --porcelain` не показывает `package.json`, `package-lock.json`, `node_modules` и файлы вне `files_modified`
    - `node …/map-probe.mjs band --width 1440 --height 900` печатает JSON с `"jumps":[]` для x=200 и x=1240, `"skewJump"` > 6 хотя бы для одной абсциссы, `"firstFrame"` со всеми true, `scrollWidth <= innerWidth + 1`, завершается строкой `PASS` и кодом 0
    - `node …/map-probe.mjs band --width 390 --height 844` печатает `firstFrame` все true, `scrollWidth <= innerWidth + 1`, строку `PASS`, код 0
    - `node …/map-probe.mjs fps --width 1440 --height 900 --runs 3` печатает три значения fps и медиану ≥ 50, строку `PASS`, код 0
    - SUMMARY плана содержит раздел с командами запуска (включая `PW_ROOT` или путь кэша npx, порт preview) и полными JSON-результатами трёх прогонов
    - `npm run build` завершился без предупреждений о размере чанков; `node scripts/check-dist.mjs` печатает только PASS-строки
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/check-dist.mjs && (npx vite preview --port 4173 --strictPort & sleep 2; node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs band --url http://localhost:4173/esd-onevoice27/ --width 1440 --height 900; code=$?; kill %1; exit $code)</automated>
  </verify>
  <done>Зонд воспроизводимо подтверждает MAP-03 на 1440×900 (нет перепадов > 6 вне линии скоса по x = 200 и x = 1240), отсутствие горизонтальной прокрутки на 1440 и 390, карту в первом кадре, и даёт базовый fps; результаты и команды записаны в SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл GitHub Pages | входа данных нет, фаза меняет только разметку и CSS |
| зонд Playwright → localhost preview | локальный процесс, внешняя сеть не нужна (шрифты Google и превью YouTube грузит сама страница, как раньше) |
| исполнитель → реестр npm (только если `playwright` не найден в кэше npx) | установка вне репозитория, в scratch-каталог |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of service | `.map-band` + орбы (перерисовка при прокрутке) | accept | статические слои без анимации; проверка scrollWidth и fps зондом |
| T-08-02 | Tampering | результаты зонда в SUMMARY | mitigate | зонд печатает сырые JSON-числа и команду запуска; фаза 13 повторяет прогон |
| T-08-03 | Information disclosure | зонд | accept | скриншоты только вьюпорта localhost, в репозиторий не пишутся |
| T-08-SC | Tampering | npm-установки | mitigate | в репозиторий зависимости не добавляются (`package.json` неизменен); `playwright` берётся из кэша npx MCP или ставится в scratch-каталог только с npmjs.com (`playwright@1.63.0`, мейнтейнеры Microsoft, проверено 2026-09-06); `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `npx tsc -b`, `npm run lint`, `npx vitest run src/components/map src/App.test.tsx src/styles/motionPolicy.test.ts` зелёные
- `npm run build && node scripts/check-dist.mjs` без ошибок и предупреждений
- Зонд `band` PASS на 1440×900 (MAP-01, MAP-03: jumps пусты по x = 200 и 1240; MAP-04: firstFrame) и на 390×844 (MAP-02: scrollWidth в норме при орбе-полосе)
- Зонд `fps` PASS (медиана ≥ 50, базовый замер до плана 02)
- `git status` не показывает файлов вне `files_modified`
</verification>

<success_criteria>
- Под картой и формой одна лента `.map-band` с подложкой `rgb(18 12 52)` и полигоном `0 var(--map-wedge), 100% 0, 100% 100%, 0 100%`; `.map-section` без `.map-section__skew`, `margin-top` и `overflow-x`; `.lf-section` внутри ленты прозрачна через правила в `map.css`
- Нижний орб `top: 100%; right: 0; transform: translate(38%, -50%)`, на <768px полоса с `translateY(-50%)`
- `.map-container` прямой ребёнок `.map-shell`, без Reveal; заголовок и счётчики с каскадом
- Тесты `MapBand.test.tsx`, `MapSection.test.tsx`, `App.test.tsx`, `motionPolicy.test.ts` зелёные
- Зонд `map-probe.mjs` в `.planning/phases/08-map-band-and-lights/qa/`, результаты трёх прогонов в SUMMARY
</success_criteria>

<output>
Create `.planning/phases/08-map-band-and-lights/08-01-SUMMARY.md` when done (с разделом «Проверки MAP-03 и базовый fps»: команды, JSON-результаты, порт preview, способ резолва playwright)
</output>
