---
phase: 13-integration-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.seams.test.tsx
  - .planning/phases/13-integration-qa/qa/seams.evaluate.js
  - .planning/phases/13-integration-qa/qa/results/preview-band-1440.json
  - .planning/phases/13-integration-qa/qa/results/preview-band-390.json
  - .planning/phases/13-integration-qa/qa/results/preview-lights-1440.json
  - .planning/phases/13-integration-qa/qa/results/preview-lights-reduced-1440.json
  - .planning/phases/13-integration-qa/qa/results/preview-fps-1440.json
  - .planning/phases/13-integration-qa/qa/results/preview-seams-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-hashes.txt
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [QA-01, QA-02]
tags: [integration, vitest, gate, deploy, github-pages, playwright, seams]

must_haves:
  truths:
    - "После слияния фаз 7–12 полный `npx vitest run` зелёный, и в наборе есть тесты из списка QA-01: состояния формы и orgName (LightForm.test.tsx), панель ресурсов с фокусом, Escape, scroll lock и deep link (ResourcePanel.test.tsx, Resources.test.tsx), пять корзин огоньков без pulse (EsdMap.test.tsx), карточка новости 16:9 (NewsCard.test.tsx), порядок узлов футера (Footer.test.tsx), плюс тесты стыков фаз (App.seams.test.tsx)"
    - "Гейт `npx tsc -b` → `npm test` → `npm run lint` → `npm run build` → `node scripts/check-dist.mjs` проходит с кодом 0 на каждом шаге, check-dist печатает `OK: 11 проверок`"
    - "Стыки без дефектов: `main > .map-band > section#map + section#light-form`, секция формы прозрачна и без `::before`; `.resource-card` несёт `glass glass-resource` и в браузере получает `backdrop-filter: blur(14px) saturate(1.25)`; `#form-title` плоский `rgb(239, 237, 245)`, `#about-title` с градиентом 104deg; превью в панели «Видео» 16:9 с `object-fit: cover`; deep link `#resources-materials` открывает панель материалов"
    - "Зонд фазы 8 на preview 4173: `band` 1440 и 390 PASS с пустыми `jumps`, `lights` PASS (5 корзин, 942 ореола, `.light.pulse` 0), `lights --reduced` PASS (ореол opacity .22, r 6px), `fps` медиана ≥ 50"
    - "Прогон «Deploy to GitHub Pages» зелёный на HEAD ветки main; `index.html` и три ассета прода побайтно (sha256) равны локальному `dist`"
    - "REQUIREMENTS.md MAP-07 уточнён: при reduce ореол статичен с opacity .22 по глобальной политике вместо .45 спецификации"
  artifacts:
    - path: "src/App.seams.test.tsx"
      provides: "Тесты стыков 8+9, 7+11, 7+9, 10+11 и скоса карты на рендере всего приложения"
      min_lines: 60
      contains: "map-band"
    - path: ".planning/phases/13-integration-qa/qa/seams.evaluate.js"
      provides: "Функция для browser_evaluate: снимок стыков (computed styles, корзины, keyframes) на preview и проде"
      contains: "backdropFilter"
    - path: ".planning/phases/13-integration-qa/qa/results/prod-hashes.txt"
      provides: "Таблица сверки sha256 живых файлов Pages и локального dist на коммите деплоя"
      contains: "sha256"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MAP-07 с уточнением .22"
      contains: ".22"
  key_links:
    - from: "src/App.seams.test.tsx"
      to: "src/App.tsx"
      via: "render(<App />) под LightsProvider, как в App.test.tsx"
      pattern: "\\.map-band"
    - from: "src/App.seams.test.tsx"
      to: "src/components/resources/ResourcePanel.tsx"
      via: "клик по button[data-kind=\"video\"] и портал #resources-panel в document.body"
      pattern: "data-kind=\"video\""
    - from: ".planning/phases/13-integration-qa/qa/results/prod-hashes.txt"
      to: "dist/index.html и dist/assets/*"
      via: "curl живого файла | shasum -a 256 против shasum локального"
      pattern: "sha256"
    - from: ".planning/phases/08-map-band-and-lights/qa/map-probe.mjs"
      to: "http://localhost:4173/esd-onevoice27/"
      via: "режимы band, lights, fps с флагом --url"
      pattern: "map-probe"
---

<objective>
Убедиться, что шесть слитых фаз работают вместе: проверить стыки 8+9, 7+11, 7+9, 10+11 и скос карты, закрепить их тестом на рендере всего приложения, сверить инвентарь тестов QA-01 и дописать недостающие, прогнать полный гейт и зонд фазы 8 на preview, уточнить MAP-07 в требованиях, задеплоить `main` на GitHub Pages и доказать побайтное равенство прода и локального `dist`.

Purpose: QA-01 и QA-02 из спецификации v1.1 (раздел 7). Без этого плана приёмка Playwright (план 13-02) сравнивала бы с оригиналом сборку, которая не прошла гейт и не подтверждена на проде.
Output: `src/App.seams.test.tsx`, `qa/seams.evaluate.js`, JSON-результаты зонда и снимка стыков на preview в `qa/results/`, таблица хэшей `qa/results/prod-hashes.txt`, правка MAP-07 в `.planning/REQUIREMENTS.md`, зелёный прогон workflow на HEAD.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integration-qa/13-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@.planning/phases/08-map-band-and-lights/08-01-SUMMARY.md
@.planning/phases/08-map-band-and-lights/08-02-SUMMARY.md
@.planning/phases/11-resources-panels/11-03-SUMMARY.md
@.planning/phases/09-form-original/09-02-SUMMARY.md
@docs/qa/SMOKE.md
@src/App.tsx
@src/App.test.tsx
@src/components/map/MapBand.test.tsx
@src/components/resources/ResourcePanel.test.tsx
@src/styles/global.css
@src/components/form/light-form.css
@src/components/map/map.css
@src/components/about/video-embed.css
@scripts/check-dist.mjs
@.github/workflows/deploy.yml

<ownership>
Фаза 13 работает в основном каталоге на ветке `main`, параллельных соседей нет, worktree фаз 7–12 удалены. Правки исходников допустимы только для дефектов стыков (минимальные, в файле-владельце правила, без новых фич). Не трогать `STATE.md` и `ROADMAP.md` (их обновляет оркестратор) и `.planning/HANDOFF.json` (состояние оркестратора, в коммиты не включать). Цвета в CSS литералами `rgb(r g b / a)`. Локальный `@media (prefers-reduced-motion: reduce)` только в `global.css`, реестр `data-anim` закрыт (`motionPolicy.test.ts`), эти инварианты по D «Гейт и деплой» не обходить. Комментарии и тексты на русском, идентификаторы на английском.
</ownership>

<interfaces>
<!-- Контракты слитых фаз. Использовать напрямую, повторно не исследовать. -->

Разметка после фазы 8 (src/App.tsx, src/components/map/MapBand.tsx):
```
main#main > section#hero + div.map-band > (section#map.map-section + section#light-form.lf-section) + section#about + section#involve + section#news + section#resources + section#quote
.map-band::before  { background: rgb(18 12 52); clip-path: polygon(0 var(--map-wedge), 100% 0, 100% 100%, 0 100%) }
.map-shell         { clip-path: polygon(0 var(--map-wedge), 100% 0, 100% calc(100% - var(--map-wedge)), 0 100%) }
.map-band .lf-section { background: transparent }  .map-band .lf-section::before { content: none }   /* после фазы 9 no-op */
svg .map-lights > g.light-bucket[data-bucket="0..4"][data-anim="pulse"] > circle.light-halo[fill="url(#light-halo-person|group)"]; g.light-cores > g.light.light--person|group > circle.light-core
map.css: @property --halo-k; @keyframes light-breathe; .light-bucket { animation: light-breathe 2.6s ease-in-out infinite }; keyframes light-pulse удалены
```

Форма после фазы 9 (src/components/form/*): `section#light-form.lf-section` без `.glass-card`; `fieldset.lf-types > label.lf-type[data-type="individual"|"group"] > input[name="lightType"]`; поля `input[name="orgName"]` (только при group), `firstName`, `lastName`, `select[name="countryId"]`, `city`, `email`, `input[name="consent"].lf-checkbox`; `h2#form-title.gradient-title.gradient-title--section`.

Заголовки после фазы 7 (src/components/layout/GradientTitle.tsx): `variant: "hero" | "section" | "section-gradient"` → класс `gradient-title gradient-title--<variant>`; единственный `section-gradient` в `src/components/about/About.tsx` (`h2#about-title`); `.gradient-title--section { color: rgb(239 237 245) }` без `background-image`.

Стекло после фазы 7 (src/styles/global.css): `@utility glass { … backdrop-filter: blur(14px) saturate(112%) }`, `@utility glass-resource { background: linear-gradient(180deg, rgb(255 255 255 / .075), transparent 34%), linear-gradient(145deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62)); backdrop-filter: blur(14px) saturate(125%) }`; карточка ресурсов `div.resource-card.glass.glass-resource[data-kind]` (фаза 11-02) с `button[data-kind][aria-expanded]` внутри.

Панель ресурсов после 11-03 (src/components/resources/ResourcePanel.tsx): портал в `document.body`: `div.resources-panels.is-opening|is-open|is-closing > div#resources-panel[role="dialog"][data-kind="music"|"materials"|"video"] > .resources-panel__inner > .resources-panel__header > button.resources-panel__back`; при открытой панели классы `resources-panel-locked` на `html` и `body`; `.resources-panels { position: fixed; inset: 0; z-index: 10000 }`; панель «Видео» рендерит `VideoGrid` (16 × `div.ve.ve--compact > .ve-frame > button.ve-trigger > img.ve-poster`) и одну карточку `li.resources-file`; deep link: `MATERIALS_HASH = "#resources-materials"` в `Resources.tsx`.

VideoEmbed (src/components/about/video-embed.css): `.ve { aspect-ratio: 16 / 9 }`, `.ve-poster { object-fit: cover; object-position: center }`, `.ve--compact` не переопределяет пропорцию.

Тесты: `src/test/setup.ts` мокает `matchMedia` (matches false), `IntersectionObserver`, `ResizeObserver`, `scrollIntoView`, `scrollTo`; vitest `globals: true`, `css: false` (значения CSS проверяются текстом файла через `readFileSync(resolve(process.cwd(), "<путь>"), "utf8")`); `App.test.tsx` рендерит `<App />` под `LightsProvider` хелпером `renderApp`; `ResourcePanel.test.tsx` использует `vi.useFakeTimers({ toFake: ["setTimeout", "clearTimeout", "requestAnimationFrame", "cancelAnimationFrame"] })`.

Зонд фазы 8: `node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs <band|fps|lights> [--url http://localhost:4173/esd-onevoice27/] [--width 1440] [--height 900] [--runs 3] [--reduced]`; env `PW_ROOT` (каталог с `node_modules/playwright`, иначе кэш `~/.npm/_npx/*/node_modules/playwright`), `PW_CHANNEL` (`chrome`), `PW_HEADLESS` (`0`). Печатает одну JSON-строку и `PASS`/`FAIL`, код 0/1. Точная команда и путь кэша записаны в 08-01-SUMMARY.md.

Деплой: `.github/workflows/deploy.yml` на push в `main`: lint → test → build → check:dist → upload → deploy; прод `https://thevladoss.github.io/esd-onevoice27/`; приём деплоя по решению фазы 5: код 200, пустой diff списка ассетов, совпадение sha256 живых файлов и локального `dist`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проверка стыков после слияния, инвентарь тестов QA-01, тесты стыков App.seams.test.tsx</name>
  <files>src/App.seams.test.tsx</files>
  <read_first>
    - .planning/phases/13-integration-qa/13-CONTEXT.md, раздел «Стыки, которые надо проверить первыми» (D-стыки) и «Гейт и деплой»
    - .planning/phases/08-map-band-and-lights/08-01-SUMMARY.md и 08-02-SUMMARY.md (что реально слито: лента, орб, корзины, вердикт MAP-06, отклонение .22)
    - .planning/phases/11-resources-panels/11-03-SUMMARY.md (хост панелей, scroll lock классом, deep link, удалённые MusicPlaceholder и MaterialsList)
    - src/App.test.tsx строки 1–35 (хелпер `renderApp` под `LightsProvider`, проверка «рендерит страницу молча»)
    - src/components/resources/ResourcePanel.test.tsx (fake timers с явным `toFake`, cleanup классов `resources-panel-locked`)
    - src/components/map/MapBand.test.tsx (какие правила `map.css` уже закреплены, чтобы не дублировать)
    - src/components/form/LightForm.test.tsx строки 120–130 и 285–360 (тесты состояний и orgName уже есть)
    - src/components/news/NewsCard.test.tsx строки 36–60, src/components/layout/Footer.test.tsx строки 76–102, src/components/map/EsdMap.test.tsx (тест корзин из 08-02)
  </read_first>
  <action>
    Реализует D-стыки и первую половину QA-01 (наличие тестов из списка).

    **Предусловия.** `git branch --show-current` даёт `main`; `git worktree list` показывает одну строку; `git log --oneline -60` содержит merge-коммиты `agent-08` (или `agent-08-01`/`agent-08-02`) и `agent-11-03`; файлы `08-02-SUMMARY.md` и `11-03-SUMMARY.md` существуют; `git status --porcelain` пуст, не считая `.planning/HANDOFF.json` оркестратора. Если фаза 8 или план 11-03 не слиты, остановиться и вернуть блокер оркестратору: стыки проверять не на чем.

    **Статические проверки стыков** (каждая команда с ожидаемым выводом в acceptance_criteria):
    - 8+9 (форма прозрачна внутри ленты): в `light-form.css` нет `.lf-section::before`, блок `.lf-section {` без `background`; `map.css` содержит оба правила `.map-band .lf-section` (после фазы 9 они no-op). Решение по дискреции CONTEXT: правила оставить, потому что `MapBand.test.tsx` закрепляет их текстом, а удаление принесло бы правку теста фазы 8 без пользы; записать решение в SUMMARY. `App.tsx` содержит `<MapBand />` и не содержит `<LightForm />`.
    - 7+11 (стекло ресурсов): `ResourceCard.tsx` вешает `glass glass-resource`; блок `@utility glass-resource {` в `global.css` содержит `blur(14px) saturate(125%)`; блок `.resource-card {` в `resources.css` без `background` и `backdrop-filter`.
    - 7+9 (заголовки): `LightForm.tsx` вызывает `variant="section"` один раз; `variant="section-gradient"` встречается в исходниках только в `About.tsx`.
    - 10+11 (видео в панели, deep link): `ResourcePanel.tsx` рендерит `<VideoGrid`; `video-embed.css` содержит `aspect-ratio: 16 / 9` и `object-position: center`; `Resources.tsx` содержит `resources-materials` не меньше двух раз (константа и селектор делегированного клика); файлов `MusicPlaceholder.tsx` и `MaterialsList.tsx` нет.
    - 8 (скос и огоньки): блок `.map-shell {` содержит `calc(100% - var(--map-wedge))`; блок `.map-band::before {` содержит `rgb(18 12 52)`; `map.css` без `light-pulse`, с одним `@property --halo-k`.
    Любой провал здесь означает дефект стыка: править минимально в файле-владельце правила (например, остаток `.lf-section::before` в `light-form.css`, отсутствующий класс на карточке), фиксировать как deviation в SUMMARY. Новых фич не добавлять.

    **Инвентарь тестов QA-01.** Проверить командами из acceptance_criteria, что каждый пункт списка уже покрыт: состояния формы и `orgName` (`LightForm.test.tsx`: «групповой маяк требует название организации», «возврат к личному свету убирает поле организации»), панель ресурсов (`ResourcePanel.test.tsx`: фокус на «Назад», Escape, `resources-panel-locked`; `Resources.test.tsx`: `#resources-materials`, «блокирует прокрутку документа»), корзины (`EsdMap.test.tsx`: `LIGHT_BUCKETS`, `data-bucket`, `count(".light.pulse")` равен 0), карточка 16:9 (`NewsCard.test.tsx`: `aspect-video`), футер (`Footer.test.tsx`: «логотип → подпись → ссылки → юридический текст»). Если какой-то пункт отсутствует, дописать его в тот же тестовый файл по спецификации соответствующего плана: панель по списку сценариев 1–10 из `11-03-PLAN.md` (Task 3), корзины по пунктам 2–4 из `08-02-PLAN.md` (Task 2); форма, карточка и футер уже покрыты в `main` и дописи не требуют.

    **Новый файл `src/App.seams.test.tsx`.** Импорты как в `App.test.tsx` (`render`, `screen`, `fireEvent`, `App`, `LightsProvider`), плюс `readFileSync` из `node:fs` и `resolve` из `node:path`; локальный хелпер `renderApp` по образцу `App.test.tsx`. Один `describe("стыки фаз v1.1")` с пятью тестами:
    1. «форма стоит внутри ленты карты на прозрачном фоне (8 + 9)»: `document.querySelector("main > .map-band")` не null; `band.children` длиной 2, `children[0]` это `section#map` с классом `map-section`, `children[1]` это `section#light-form` с классом `lf-section`; `document.querySelector("#light-form .glass-card")` null; текст `light-form.css` не содержит `.lf-section::before`, блок `.lf-section {` (regex `/\.lf-section \{([^}]*)\}/`) не содержит `background`; текст `map.css` содержит `.map-band .lf-section {`.
    2. «карточки ресурсов носят утилиту стекла ресурсов (7 + 11)»: `document.querySelectorAll("#resources .resource-card")` длиной 3, у каждой классы `glass` и `glass-resource`; текст `global.css` содержит `@utility glass-resource {`, а тело этого блока содержит `blur(14px) saturate(125%)` и `rgb(255 255 255 / .075)`.
    3. «заголовок формы плоский, заголовок About градиентный (7 + 9)»: `#form-title`, `#map-title`, `#involve-title` имеют `gradient-title--section` и не имеют `gradient-title--section-gradient`; `#about-title` имеет `gradient-title--section-gradient`; текст `primitives.css` в блоке `.gradient-title--section {` не содержит `background-image`, а блок `.gradient-title--section-gradient {` содержит `104deg`.
    4. «панель «Видео» показывает превью 16:9 (10 + 11)»: `vi.useFakeTimers` с тем же `toFake`, что в `ResourcePanel.test.tsx`; клик по `button[data-kind="video"]`; `document.body.querySelector(".resources-panels #resources-panel[data-kind=\"video\"]")` не null; внутри `.ve` ровно 16 и `.ve-poster` ровно 16; секция `#resources` имеет класс `is-video-active`; текст `video-embed.css` в блоке `.ve {` содержит `aspect-ratio: 16 / 9`, в блоке `.ve-poster {` содержит `object-fit: cover` и `object-position: center`; в `afterEach` вернуть реальные таймеры и снять `resources-panel-locked` с `html` и `body`.
    5. «скос карты и дыхание огоньков на месте (8)»: `document.querySelectorAll(".map-lights .light-bucket")` длиной 5 с `data-bucket` `["0","1","2","3","4"]`, `document.querySelectorAll(".light.pulse")` длиной 0; текст `map.css` в блоке `.map-shell {` содержит `100% calc(100% - var(--map-wedge))`, в блоке `.map-band::before {` содержит `rgb(18 12 52)`, не содержит `light-pulse`, содержит `@keyframes light-breathe`.
    Хелпер `block(css, head)` объявить локально (первое вхождение `head`, текст до ближайшей `}`), как в тестах фазы 7. Тест «рендерит страницу молча» из `App.test.tsx` роняет прогон при любом `console.error`: новый файл тоже не должен их порождать (проверить вывод).

    Прогнать `npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts` и `npx eslint src/App.seams.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts && npx eslint src/App.seams.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` → `main`; `git worktree list | wc -l` → 1; `ls .planning/phases/08-map-band-and-lights/08-02-SUMMARY.md .planning/phases/11-resources-panels/11-03-SUMMARY.md` без ошибок
    - 8+9: `grep -c "\.lf-section::before" src/components/form/light-form.css` → 0; `awk '/^\.lf-section \{/,/\}/' src/components/form/light-form.css | grep -c "background"` → 0; `grep -c "\.map-band \.lf-section" src/components/map/map.css` → 2; `grep -c "<MapBand />" src/App.tsx` → 1; `grep -c "<LightForm />" src/App.tsx` → 0
    - 7+11: `grep -c "glass glass-resource" src/components/resources/ResourceCard.tsx` → 1; `awk '/^@utility glass-resource \{/,/\}/' src/styles/global.css | grep -c "blur(14px) saturate(125%)"` → 1; `awk '/^\.resource-card \{/,/\}/' src/components/resources/resources.css | grep -c "background\|backdrop-filter"` → 0
    - 7+9: `grep -c 'variant="section"' src/components/form/LightForm.tsx` → 1; `grep -rl 'variant="section-gradient"' src --include='*.tsx' | grep -v '\.test\.' | wc -l` → 1 (About.tsx)
    - 10+11: `grep -c "<VideoGrid" src/components/resources/ResourcePanel.tsx` → 1; `grep -c "aspect-ratio: 16 / 9" src/components/about/video-embed.css` ≥ 1; `grep -c "object-position: center" src/components/about/video-embed.css` ≥ 1; `grep -c "resources-materials" src/components/resources/Resources.tsx` ≥ 2; `ls src/components/resources/MusicPlaceholder.tsx src/components/resources/MaterialsList.tsx 2>&1 | grep -c "No such file"` → 2
    - 8: `awk '/^\.map-shell \{/,/\}/' src/components/map/map.css | grep -c "calc(100% - var(--map-wedge))"` → 1; `awk '/^\.map-band::before \{/,/\}/' src/components/map/map.css | grep -c "rgb(18 12 52)"` → 1; `grep -c "light-pulse" src/components/map/map.css` → 0; `grep -c "@property --halo-k" src/components/map/map.css` → 1
    - Инвентарь QA-01: `grep -c "название организации" src/components/form/LightForm.test.tsx` ≥ 2; `grep -c "resources-panel-locked" src/components/resources/ResourcePanel.test.tsx` ≥ 1 и `grep -c "Escape" src/components/resources/ResourcePanel.test.tsx` ≥ 1; `grep -c "resources-materials" src/components/resources/Resources.test.tsx` ≥ 2; `grep -c "LIGHT_BUCKETS\|data-bucket" src/components/map/EsdMap.test.tsx` ≥ 2 и `grep -c '\.light\.pulse' src/components/map/EsdMap.test.tsx` ≥ 1; `grep -c "aspect-video" src/components/news/NewsCard.test.tsx` ≥ 1; `grep -c "логотип → подпись → ссылки" src/components/layout/Footer.test.tsx` → 1
    - `src/App.seams.test.tsx` существует, `grep -c "^  it(" src/App.seams.test.tsx` → 5, файл содержит строки `map-band`, `glass-resource`, `gradient-title--section-gradient`, `data-kind="video"`, `light-bucket`, `readFileSync`
    - `npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts` → код 0, все тесты passed, в выводе нет `console.error` и `act(`
    - `npx eslint src/App.seams.test.tsx` → код 0
    - `git status --porcelain` показывает только `src/App.seams.test.tsx` (и правки стыков, если были, каждая записана в SUMMARY как deviation) плюс `.planning/HANDOFF.json`
  </acceptance_criteria>
  <done>Все пять стыков подтверждены командами и закреплены тестом на рендере приложения; список тестов QA-01 присутствует в наборе; целевые прогоны vitest и eslint зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Полный гейт, зонд фазы 8 и снимок стыков на preview, уточнение MAP-07, коммиты</name>
  <files>.planning/phases/13-integration-qa/qa/seams.evaluate.js, .planning/phases/13-integration-qa/qa/results/preview-band-1440.json, .planning/phases/13-integration-qa/qa/results/preview-band-390.json, .planning/phases/13-integration-qa/qa/results/preview-lights-1440.json, .planning/phases/13-integration-qa/qa/results/preview-lights-reduced-1440.json, .planning/phases/13-integration-qa/qa/results/preview-fps-1440.json, .planning/phases/13-integration-qa/qa/results/preview-seams-1440.json, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/13-integration-qa/13-CONTEXT.md, разделы «Гейт и деплой» и «Playwright-приёмка» (среда: FPS при закрытых WebGL-вкладках, `browser_evaluate` с `filename` пишет JSON-экранированный файл в корень репозитория)
    - .planning/phases/08-map-band-and-lights/08-01-SUMMARY.md, раздел с командами запуска зонда (резолв `playwright`, `PW_ROOT`, порт preview) и 08-02-SUMMARY.md, раздел «Замер fps и reduced motion» (числа для сравнения)
    - docs/qa/SMOKE.md строки 20–30 (как поднимался preview в v1.0) и замечание 2 в разделе «Итог» (fps 7–11 при открытой WebGL-вкладке и параллельных `npm test`/`npm run build`)
    - .planning/REQUIREMENTS.md строка с MAP-07 и таблица Traceability
    - .planning/phases/08-map-band-and-lights/qa/map-probe.mjs (ключи JSON режимов `band`, `lights`, `fps`)
  </read_first>
  <action>
    Реализует QA-02 (гейт) и D «Гейт и деплой» до пуша, а также запись стыков на preview для таблицы плана 13-02.

    **Гейт строго в порядке из CONTEXT**, каждый шаг ждать до конца и фиксировать вывод для SUMMARY: `npx tsc -b` (код 0, вывод пуст); `npm test` (полный `vitest run`: записать число файлов и тестов, ожидается не меньше 48 файлов, 0 failed, 0 skipped, в выводе нет `Warning: An update` и `console.error`); `npm run lint` (код 0, без предупреждений); `npm run build` (код 0, без предупреждений о размере чанков; записать имена `dist/assets/index-*.js`, `index-*.css`, `vendor-map-*.js`); `node scripts/check-dist.mjs` (последняя строка `OK: 11 проверок`). Дополнительные инварианты D: `@media (prefers-reduced-motion: reduce)` встречается ровно один раз и только в `global.css`; реестр `data-anim` закрыт (это же проверяет `motionPolicy.test.ts` в составе `npm test`). Провал любого шага гейта чинить в файле-владельце и повторять гейт с начала; никаких `--force`, `eslint-disable`, `skip`.

    **Preview и зонд фазы 8.** Поднять `npx vite preview --port 4173 --strictPort` в фоне (порт свободен по CONTEXT), дождаться `curl -s -o /dev/null -w '%{http_code}' http://localhost:4173/esd-onevoice27/` → `200`. Закрыть вкладки с WebGL (esd-map.vercel.app, onevoice27.org с Mapbox), не запускать параллельно `npm test`/`npm run build`. Прогнать зонд с `--url http://localhost:4173/esd-onevoice27/` и сохранить JSON-строку каждого прогона в `qa/results/` (каталог создать): `band --width 1440 --height 900` → `preview-band-1440.json`; `band --width 390 --height 844` → `preview-band-390.json`; `lights --width 1440 --height 900` → `preview-lights-1440.json`; `lights --width 1440 --height 900 --reduced` → `preview-lights-reduced-1440.json`; `fps --width 1440 --height 900 --runs 3` → `preview-fps-1440.json`. Ожидания: `band` PASS с `jumps: []` для x = 200 и x = 1240 и `skewJump` > 6 на 1440, PASS на 390, `scrollWidth <= innerWidth + 1`; `lights` PASS с `buckets: 5`, `halos: 942`, `cores: 942`, `firstFrame: true`; `lights --reduced` PASS с opacity ореола `0.22` и r `6px`; `fps` медиана ≥ 50 (сравнить с числами 08-02-SUMMARY). Провал `band` означает дефект стыка 8+9 (типовые причины перечислены в 08-01-PLAN Task 3): править и повторять с гейта.

    **Снимок стыков на preview.** Записать `qa/seams.evaluate.js`: тело одной стрелочной функции без аргументов для `browser_evaluate`, возвращающей объект с ключами `band` (`!!document.querySelector("main > .map-band")`, массив id детей ленты, `getComputedStyle("#light-form").backgroundColor`, `getComputedStyle(lightForm, "::before").content`), `bandBefore` (`backgroundColor` и `clipPath` псевдоэлемента `::before` у `.map-band`), `shellClip` (`clipPath` у `.map-shell`), `resourceCard` (`backdropFilter`, `backgroundImage`, `borderTopColor`, `borderRadius` первой `.resource-card`), `titles` (для `#about-title`, `#form-title`, `#map-title`, `#involve-title`: `backgroundImage`, `color`, `webkitTextFillColor`), `buckets` (число `.light-bucket`, список `data-bucket`, `animationName` и `animationDuration` корзины 0), `pulse` (число `.light.pulse`, список имён `CSSKeyframesRule` из `document.styleSheets`, флаг наличия `light-breathe` и отсутствия `light-pulse`), `videoPanel` (заполняется вторым вызовом после клика: `zIndex` и `position` у `.resources-panels`, число `.resources-panel .ve`, отношение `width/height` первого `.ve`, `objectFit` и `objectPosition` первого `.ve-poster`), `deepLink` (после навигации на `#resources-materials`: `data-kind` у `#resources-panel`, `aria-expanded` у `button[data-kind="materials"]`, число `details.resources-group` и наличие `open` у `#resources-group-esd`). Через Playwright MCP на 1440×900 (`browser_resize`), открыть preview, выполнить функцию (ключи без панели), затем кликнуть `button[data-kind="video"]`, выполнить снова, нажать Escape, перейти на `http://localhost:4173/esd-onevoice27/#resources-materials`, выполнить третий раз; объединённый результат сохранить как `qa/results/preview-seams-1440.json`. Если использовался `browser_evaluate` с `filename`, файл появится в корне репозитория JSON-экранированным: перенести в `qa/results/`, привести к обычному JSON, корень репозитория оставить чистым.
    Ожидаемые значения: `band.childIds` равен `["map","light-form"]`, `backgroundColor` формы `rgba(0, 0, 0, 0)`, `::before` формы `content` равен `none`; `bandBefore.backgroundColor` `rgb(18, 12, 52)`, `clipPath` начинается с `polygon(`; `shellClip` содержит `calc(100% - `; `resourceCard.backdropFilter` равен `blur(14px) saturate(1.25)`, `backgroundImage` содержит `rgba(255, 255, 255, 0.075)` и `34%`; `#about-title.backgroundImage` содержит `104deg`, остальные заголовки `backgroundImage` `none` и `color` `rgb(239, 237, 245)`; `buckets.count` 5, `animationName` `light-breathe`, `animationDuration` `2.6s`; `pulse.count` 0, `light-pulse` отсутствует в keyframes; `videoPanel.zIndex` `10000`, `position` `fixed`, `.ve` 16, отношение сторон в пределах 1.767…1.789, `objectFit` `cover`, `objectPosition` `50% 50%`; `deepLink.kind` `materials`, `aria-expanded` `true`, групп 5, `esdOpen` true. Расхождение здесь тоже дефект стыка: править, повторять гейт.

    Остановить preview. Убедиться, что в корне репозитория не осталось файлов Playwright (`git status --porcelain | grep -v "^ M .planning/HANDOFF.json"` показывает только ожидаемые пути).

    **REQUIREMENTS.md.** Заменить строку MAP-07 на: «- [ ] **MAP-07**: Карта и огоньки рисуются в первом кадре после монтирования; `prefers-reduced-motion: reduce` выключает дыхание: ореол статичен радиусом 6px с opacity .22 из глобальной политики `[data-anim="pulse"] .light-halo` в `global.css` (спецификация называла .45; отклонение принято координатором фазы 8, `motionPolicy.test.ts` запрещает второй reduce-блок)». Чекбокс и таблицу Traceability не менять (их закрывает оркестратор).

    **Коммиты** (по образцу истории, сообщения на русском, стадировать файлы поимённо, `.planning/HANDOFF.json` не включать): `test(13-01): тесты стыков фаз 7–12 и снимок стыков на preview` для `src/App.seams.test.tsx`, `qa/seams.evaluate.js`, `qa/results/preview-*.json` и правок стыков из Task 1, если были; `docs(13-01): MAP-07 уточнён, статичный ореол .22 при reduce` для `.planning/REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && node scripts/check-dist.mjs && node -e "const fs=require('fs');const d='.planning/phases/13-integration-qa/qa/results/';const j=n=>JSON.parse(fs.readFileSync(d+n,'utf8'));const b=j('preview-band-1440.json');const l=j('preview-lights-1440.json');const f=j('preview-fps-1440.json');const s=j('preview-seams-1440.json');if(!(l.buckets===5&&l.halos===942&&s.resourceCard.backdropFilter==='blur(14px) saturate(1.25)'&&s.videoPanel.zIndex==='10000'&&Number(f.median)>=50))process.exit(1);console.log('seams+probe OK', JSON.stringify({band:b.jumps??b, fps:f.median}))" && grep -c "opacity .22" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` код 0; `npm test` код 0, не меньше 48 файлов, 0 failed, 0 skipped, без `act(` и `console.error` в выводе; `npm run lint` код 0; `npm run build` код 0 без предупреждений о размере чанков; `node scripts/check-dist.mjs` печатает `OK: 11 проверок`
    - `grep -c "@media (prefers-reduced-motion: reduce)" src/styles/global.css` → 1; `grep -rl "prefers-reduced-motion" src --include='*.css' | wc -l` → 1
    - Файлы `qa/results/preview-band-1440.json`, `preview-band-390.json`, `preview-lights-1440.json`, `preview-lights-reduced-1440.json`, `preview-fps-1440.json` существуют и парсятся; в `preview-band-1440.json` `jumps` пусты для обеих абсцисс и `skewJump` > 6; в `preview-lights-1440.json` `buckets` 5, `halos` 942, `cores` 942, `firstFrame` true; в `preview-lights-reduced-1440.json` opacity ореола `"0.22"` и r `"6px"`; в `preview-fps-1440.json` медиана ≥ 50 (три значения записаны)
    - `qa/seams.evaluate.js` существует, содержит строки `backdropFilter`, `CSSKeyframesRule`, `light-breathe`, `resources-panels`, `resources-group-esd`
    - `qa/results/preview-seams-1440.json`: `band.childIds` `["map","light-form"]`, фон формы `rgba(0, 0, 0, 0)`, `bandBefore.backgroundColor` `rgb(18, 12, 52)`, `resourceCard.backdropFilter` `blur(14px) saturate(1.25)`, заголовок About с `104deg`, `#form-title` `backgroundImage` `none` и `color` `rgb(239, 237, 245)`, `buckets.count` 5 с `animationName` `light-breathe`, `pulse.count` 0 и `light-pulse` не в списке keyframes, `videoPanel`: `zIndex` `10000`, `.ve` 16, отношение сторон 1.767…1.789, `objectFit` `cover`; `deepLink.kind` `materials`, `esdOpen` true
    - Порт 4173 после задачи свободен (`lsof -nP -iTCP:4173 -sTCP:LISTEN | wc -l` → 0); в корне репозитория нет файлов `*.json`/`*.jpeg` от Playwright
    - `grep -c "opacity .22" .planning/REQUIREMENTS.md` → 1; `grep -c "^- \[ \] \*\*MAP-07\*\*" .planning/REQUIREMENTS.md` → 1
    - `git log --oneline -3` содержит коммиты `test(13-01)` и `docs(13-01)`; `git status --porcelain | grep -v HANDOFF.json` пуст
  </acceptance_criteria>
  <done>Гейт пройден целиком, зонд фазы 8 и снимок стыков на preview дали ожидаемые числа и сохранены в `qa/results/`, MAP-07 уточнён, всё закоммичено в `main`.</done>
</task>

<task type="auto">
  <name>Task 3: Push в main, ожидание Deploy to GitHub Pages, побайтная сверка прода с dist</name>
  <files>.planning/phases/13-integration-qa/qa/results/prod-hashes.txt</files>
  <read_first>
    - docs/qa/SMOKE.md, раздел «Прод» (формат таблицы сверки v1.0: путь, код ответа, sha256) и запись STATE.md о том, что `timeout` для обёртки `gh run watch` на macOS недоступен и ожидание делается циклом `gh run view --json status,conclusion`
    - README.md, раздел «Деплой и живой сайт» (команды `gh run list`, `gh run watch`, `curl -sI`)
    - .github/workflows/deploy.yml (джоба build гоняет lint, test, build, check:dist; concurrency `pages` без отмены)
  </read_first>
  <action>
    Реализует вторую половину QA-02 и D «Гейт и деплой» (push → workflow → сверка хэшей как в v1.0).

    1. `git push origin main`; сохранить `HEAD=$(git rev-parse HEAD)`.
    2. Найти прогон: `gh run list --workflow deploy.yml --limit 1 --json databaseId,headSha,status,url`. Если `headSha` ещё не равен `HEAD`, подождать 10 с и повторить (до 6 раз). Дождаться завершения: `gh run watch <databaseId> --exit-status`; если команда зависает или недоступна, цикл `gh run view <databaseId> --json status,conclusion --jq '.status + " " + .conclusion'` с паузой 30 с, не дольше 15 мин. Ожидание: `completed success`. При провале джобы `build` прочитать лог `gh run view <id> --log-failed`, починить причину, повторить гейт Task 2 с начала и push.
    3. Живой сайт: `LIVE=https://thevladoss.github.io/esd-onevoice27/`; `curl -sI "$LIVE" | head -1` → `HTTP/2 200`. Скачать `index.html` с заголовком `Cache-Control: no-cache` во временный каталог вне репозитория. Список ассетов: `grep -o '/esd-onevoice27/assets/[^"]*' dist/index.html | sort` против того же grep по живому HTML: `diff` пуст. Для `index.html` и каждого ассета (ожидаются три: `index-*.js`, `index-*.css`, `vendor-map-*.js`) сравнить `curl -s -H 'Cache-Control: no-cache' "https://thevladoss.github.io<путь>" | shasum -a 256` с `shasum -a 256 dist<путь без /esd-onevoice27>`; код ответа каждого файла 200. При расхождении (кэш Pages после свежего деплоя) подождать 60 с и повторить, до 5 раз; устойчивое расхождение означает, что прод отдаёт не этот коммит: остановиться и вернуть блокер (план 13-02 без этого не запускать).
    4. Записать `qa/results/prod-hashes.txt`: шапка с `HEAD`, id и url прогона, время; таблица «путь | код | sha256 живого (первые 12) | sha256 dist (первые 12) | равен» на четыре строки; строка с итогом «4 из 4 равны». Этот файл план 13-02 переносит в `docs/qa/SMOKE.md`.
    5. Закоммитировать `docs(13-01): сверка хэшей прода после деплоя` (только `prod-hashes.txt`); повторный push в этой задаче не делать: docs-коммиты пушит план 13-02 вместе с приёмкой, сборка от них не меняется, и хэши он перепроверит.
  </action>
  <verify>
    <automated>RUN_ID=$(gh run list --workflow deploy.yml --limit 1 --json databaseId --jq '.[0].databaseId'); test "$(gh run view "$RUN_ID" --json conclusion --jq .conclusion)" = success && test "$(curl -s -o /dev/null -w '%{http_code}' https://thevladoss.github.io/esd-onevoice27/)" = 200 && test "$(grep -c '| да' .planning/phases/13-integration-qa/qa/results/prod-hashes.txt)" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow deploy.yml --limit 1 --json headSha,conclusion --jq '.[0].headSha + " " + .[0].conclusion'` → `<sha коммита test/docs(13-01)> success`, где sha совпадает с `git rev-parse HEAD~1` (HEAD после коммита prod-hashes) или с HEAD на момент push
    - `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` → `HTTP/2 200`
    - diff списков `/esd-onevoice27/assets/*` из живого `index.html` и `dist/index.html` пуст; ассетов 3
    - sha256 живых `index.html`, `index-*.js`, `index-*.css`, `vendor-map-*.js` равны локальным `dist` (4 из 4), коды ответа 200
    - `qa/results/prod-hashes.txt` существует, содержит `sha256`, четыре строки таблицы с `| да` и строку «4 из 4»
    - `git log --oneline -1` → `docs(13-01): сверка хэшей прода после деплоя`; `git status --porcelain | grep -v HANDOFF.json` пуст
  </acceptance_criteria>
  <done>Прод отдаёт сборку коммита из `main`, подтверждённую зелёным workflow и побайтным равенством четырёх файлов; таблица хэшей сохранена для SMOKE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Исполнитель → GitHub (push, Actions, Pages) | публикация сборки; права workflow разложены по джобам (build без OIDC) |
| Локальный preview 4173 ← зонд и Playwright MCP | локальный процесс, только чтение страницы |
| Репозиторий ← артефакты Playwright (файлы в корне) | случайный коммит служебных файлов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | прод GitHub Pages | mitigate | приём деплоя только по sha256 четырёх файлов против локального `dist` на том же коммите, а не по коду 200 |
| T-13-02 | Repudiation | результаты гейта и зонда | mitigate | JSON зонда и снимок стыков сохраняются в `qa/results/`, числа переносятся в SUMMARY и SMOKE |
| T-13-03 | Information disclosure | файлы Playwright в корне репозитория | mitigate | перенос в `qa/results/`, проверка `git status` перед коммитом, стадирование поимённо |
| T-13-04 | Denial of service | замер fps под нагрузкой | mitigate | WebGL-вкладки закрыты, `npm test`/`npm run build` не идут параллельно, три прогона и медиана |
| T-13-SC | Tampering | npm-установки | accept | пакеты не добавляются; `playwright` берётся из кэша npx MCP или scratch-каталога по 08-01-SUMMARY, `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- Все проверки стыков из Task 1 дали ожидаемые числа, `src/App.seams.test.tsx` зелёный вместе с `App.test.tsx` и `motionPolicy.test.ts`
- Гейт `npx tsc -b`, `npm test`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` пройден без правок обходного характера
- Зонд фазы 8 на preview: band 1440/390 PASS, lights PASS (обычный и reduced), fps медиана ≥ 50; снимок стыков совпал с ожиданиями
- Workflow зелёный на HEAD, прод побайтно равен `dist` (4 из 4), `prod-hashes.txt` записан
- `git status` чист (кроме HANDOFF.json), в корне репозитория нет артефактов Playwright
</verification>

<success_criteria>
- QA-01: полный набор тестов зелёный; тесты состояний формы и `orgName`, панели ресурсов (фокус, Escape, scroll lock, deep link), пяти корзин без `pulse`, карточки 16:9, порядка узлов футера присутствуют; добавлен тест стыков
- QA-02: гейт из пяти команд пройден, деплой зелёный, прод равен `dist` по sha256
- Стыки 8+9, 7+11, 7+9, 10+11 и скос карты подтверждены статически, тестом и на preview в браузере
- MAP-07 в REQUIREMENTS.md уточнён значением .22
</success_criteria>

<output>
Create `.planning/phases/13-integration-qa/13-01-SUMMARY.md` when done (с числами гейта: файлы и тесты vitest, имена ассетов, JSON зонда и снимка стыков, id прогона workflow, таблица sha256, решение по правилам `.map-band .lf-section`, список правок стыков, если были)
</output>
