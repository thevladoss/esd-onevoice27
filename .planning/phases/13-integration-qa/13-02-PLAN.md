---
phase: 13-integration-qa
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .planning/phases/13-integration-qa/qa/v11-measure.js
  - .planning/phases/13-integration-qa/qa/pixel-probe.mjs
  - .planning/phases/13-integration-qa/qa/results/orig-1440.json
  - .planning/phases/13-integration-qa/qa/results/orig-390.json
  - .planning/phases/13-integration-qa/qa/results/prod-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-390.json
  - .planning/phases/13-integration-qa/qa/results/prod-band-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-band-390.json
  - .planning/phases/13-integration-qa/qa/results/prod-lights-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-fps-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-pixels-1440.json
  - .planning/phases/13-integration-qa/qa/results/prod-interactive-1440.json
  - .planning/phases/13-integration-qa/qa/results/orig-interactive-1440.json
  - docs/qa/SMOKE.md
  - docs/qa/v11-desktop.jpeg
  - docs/qa/v11-mobile.jpeg
  - docs/qa/v11-full.jpeg
  - docs/qa/v11-form-group.jpeg
  - docs/qa/v11-panel-materials.jpeg
  - docs/qa/v11-map-bottom.jpeg
  - docs/qa/v11-footer.jpeg
  - README.md
autonomous: true
requirements: [QA-03]
tags: [playwright, acceptance, smoke, screenshots, docs, github-pages]

must_haves:
  truths:
    - "docs/qa/SMOKE.md содержит раздел «Фаза 13 / v1.1» с таблицами «оригинал / прод» на 1440×900 и 390×844 по пунктам GLASS, MAP, FORM, MEDIA, RES, FOOT, где в ячейках стоят измеренные значения, а не «да»"
    - "GLASS: computed background-image, border, box-shadow, backdrop-filter карточек About (`.ab-step`), ресурсов (`.resource-card`) и триптиха (`.inv-card`, `.inv-triptych`) на проде совпадают со строками оригинала (`.ov-about-step-card`, `.ov-resources-card`, `.ov-involve-item`, `.ov-involve-triptych`); `#about-title` с градиентом 104deg, заголовки карты, формы, involve, новостей и ресурсов `rgb(239, 237, 245)` без background-image"
    - "MAP: clip-path `.map-shell` это полигон из четырёх вершин со скосом 46px на 1440 и 32px на 390, как `#ov-map-element`; зонд band на проде без перепада яркости > 6 вне линии скоса по x = 200 и x = 1240; fps медиана трёх rAF-замеров за 2 с ≥ 50; пять корзин data-bucket 0–4; keyframes light-pulse нет; цвет полотна карты замерен на обоих сайтах"
    - "FORM: список видимых полей в состояниях individual и group совпадает с оригиналом по составу и порядку с учётом замены адреса на страну и город; computed стили карточек типа (rgba(33, 26, 62, 0.42), рамка rgba(239, 237, 245, 0.16), radius 16px, min-height 190px на 1440) и полей (min-height 54px, radius 16px, rgba(33, 26, 62, 0.58)) равны спецификации"
    - "MEDIA: отношение сторон `.news-card__cover` 1.778 ± 0.01 у всех карточек первой страницы, `object-fit: cover`; средняя яркость 6px-полос у верхнего и нижнего края обложки > 12; превью в панели «Видео» 16:9 с cover"
    - "RES: прямоугольники `.resources-copy`, `.resources-cell--music`, `--materials`, `--video` на ширине сетки 1152 в пределах ±8px от 528×523 / 320×296 / 272×336 / 368×256; панель: z-index 10000, position fixed, переходы слоёв 0.62s с задержкой 0.09s и 0.18s, scroll lock на html и body, фокус на «Назад», Escape закрывает и возвращает фокус на карточку, deep link открывает материалы"
    - "FOOT: `.site-footer__inner` flex-колонка по центру шириной 1152 на 1440 и 358 на 390, четыре узла в порядке логотип → подпись → ссылки → юридический текст, центр каждого узла в ±2px от центра вьюпорта; у оригинала та же колонка и порядок"
    - "Семь скриншотов v11-*.jpeg лежат в docs/qa/, README упоминает раздел v1.1, документы закоммичены и запушены, повторный прогон workflow зелёный, хэши ассетов не изменились"
  artifacts:
    - path: ".planning/phases/13-integration-qa/qa/v11-measure.js"
      provides: "Одна функция для browser_evaluate, снимающая GLASS/MAP/FORM/MEDIA/RES/FOOT на любом из двух сайтов по таблице селекторов"
      min_lines: 120
      contains: "ov-resources-card-music"
    - path: ".planning/phases/13-integration-qa/qa/pixel-probe.mjs"
      provides: "Node-зонд на playwright: яркость 6px-полос обложки новости и цвет полотна карты по скриншоту вьюпорта"
      min_lines: 80
      contains: "0.2126"
    - path: "docs/qa/SMOKE.md"
      provides: "Раздел «Фаза 13 / v1.1» с таблицами по шести пунктам, принятыми отклонениями, скриншотами и вердиктом"
      contains: "Фаза 13 / v1.1"
    - path: "docs/qa/v11-full.jpeg"
      provides: "Полная страница прода на 1440 после приёмки v1.1"
    - path: "README.md"
      provides: "Упоминание раздела v1.1 в «Браузерный smoke» и панелей в структуре"
      contains: "v11-"
  key_links:
    - from: ".planning/phases/13-integration-qa/qa/v11-measure.js"
      to: "прод https://thevladoss.github.io/esd-onevoice27/ и оригинал https://onevoice27.org/"
      via: "параметр site: \"prod\" | \"orig\" выбирает столбец таблицы селекторов"
      pattern: "site === \"orig\""
    - from: "docs/qa/SMOKE.md"
      to: ".planning/phases/13-integration-qa/qa/results/*.json"
      via: "значения ячеек таблиц переписаны из JSON без округления сверх двух знаков"
      pattern: "qa/results"
    - from: "docs/qa/SMOKE.md"
      to: ".planning/phases/13-integration-qa/qa/results/prod-hashes.txt"
      via: "таблица sha256 из плана 13-01"
      pattern: "sha256"
---

<objective>
Сравнить прод с оригиналом onevoice27.org через Playwright на 1440×900 и 390×844 по шести пунктам спецификации v1.1 (GLASS, MAP, FORM, MEDIA, RES, FOOT) с точными проверками и порогами из CONTEXT, снять семь скриншотов, записать раздел «Фаза 13 / v1.1» в `docs/qa/SMOKE.md` с таблицами «оригинал / прод» и списком принятых отклонений, обновить README, закоммитить и запушить документы.

Purpose: QA-03, критерий 3 фазы 13 в ROADMAP. Фазы 7–12 отложили сверку computed-стилей, пиксельные и визуальные проверки именно сюда.
Output: `qa/v11-measure.js`, `qa/pixel-probe.mjs`, JSON замеров в `qa/results/`, `docs/qa/v11-*.jpeg` (7 файлов), раздел v1.1 в `docs/qa/SMOKE.md`, правка `README.md`, коммит docs и зелёный повторный прогон workflow.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-integration-qa/13-CONTEXT.md
@.planning/phases/13-integration-qa/13-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@docs/qa/SMOKE.md
@docs/research/v1.1/orig-rules.css
@docs/research/v1.1/orig-vars.txt
@docs/research/v1.1/dom-ov-light-form-container.html
@docs/research/v1.1/dom-ov-main-site-footer.html
@.planning/phases/08-map-band-and-lights/qa/map-probe.mjs
@.planning/phases/13-integration-qa/qa/seams.evaluate.js
@.planning/phases/09-form-original/09-02-SUMMARY.md
@.planning/phases/10-media-previews/10-01-SUMMARY.md
@README.md

<ownership>
Работа в основном каталоге на `main` после плана 13-01. Исходники не правятся, кроме дефекта приёмки (тогда: правка в файле-владельце, полный гейт из 13-01 Task 2, push, ожидание workflow, повторная сверка хэшей, и только затем продолжение замеров). `STATE.md`, `ROADMAP.md`, `.planning/HANDOFF.json` не трогать. Тексты на русском, идентификаторы и селекторы на английском. Скриншоты JPEG качества 80, без PNG в `docs/qa/`.

Среда по CONTEXT: оригинал за Vercel challenge, браузер Playwright MCP проходит после долгой навигации (`waitUntil: "commit"`, таймаут 120 с); `page.request` cookie не наследует, поэтому оригинал читать только через страницу; в `browser_run_code_unsafe` нет `URL` и `location`, хост брать regex из `page.url()`; `browser_evaluate` с `filename` сохраняет JSON-экранированный результат в корень репозитория, после использования файлы перенести в `qa/results/` и корень оставить чистым. FPS мерить при закрытых WebGL-вкладках (оригинал с Mapbox закрыть до замера) и без параллельных `npm test`/`npm run build`.
</ownership>

<interfaces>
<!-- Таблица селекторов для одного скрипта на оба сайта. Оригинал: docs/research/v1.1/orig-rules.css и dom-*.html. Прод: исходники после слияния фаз 7–12. -->

| Пункт | Прод (наш) | Оригинал |
|---|---|---|
| GLASS карточка About | `#about .ab-step` (первая, 3 шт.) | `#ov-about .ov-about-step-card` (первая) |
| GLASS карточка ресурсов | `#resources .resource-card` (первая, 3 шт.) | `#ov-resources .ov-resources-card` (первая) |
| GLASS рамка триптиха | `#involve .inv-triptych` | `#ov-involve .ov-involve-triptych` |
| GLASS карточка триптиха | `#involve .inv-card` (первая, 3 шт.) | `#ov-involve .ov-involve-item` (первая) |
| GLASS заголовок About | `#about-title` | `#ov-about .ov-about-title` (запасной: первый `h2` в `#ov-about`) |
| GLASS плоские заголовки | `#map-title`, `#form-title`, `#involve-title`, `#news-title` → `closest("h2")`, `#resources-title` → `closest("h2")` | первый `h2` внутри `#ov-map-copy`, `#ov-light-form-container`, `#ov-involve`, `#ov-news`, `#ov-resources-copy` |
| MAP оболочка карты | `.map-shell` | `#ov-map-element` |
| MAP подложка ленты | `.map-band` (`::before`) | `#ov-map` (`::before`) |
| MAP полотно (пиксели) | точки внутри `.map-container` | точки внутри `#ov-map-element .awe-map` |
| FORM корень | `#light-form form` | `#ov-light-form-container form` |
| FORM карточки типа | `.lf-type[data-type="individual"]`, `.lf-type[data-type="group"]` | `label[for="LIGHT_TYPE-individual"]`, `label[for="LIGHT_TYPE-group"]` |
| FORM текстовое поле | `#light-form input[name="firstName"]` | `#FIRSTNAME` |
| FORM подпись поля | `label[for="<id firstName>"]` | `label[for="FIRSTNAME"]` |
| FORM кнопка | `#light-form button[type="submit"]` | `#ov-light-form-container button[type="submit"]` |
| MEDIA кадр и обложка | `.news-card__cover`, `.news-card__image` | `#ov-news-feed article > div:first-child`, `#ov-news-feed article img` |
| RES текстовый блок | `.resources-copy` | `#ov-resources-copy` |
| RES карточки | `.resources-cell--music`, `.resources-cell--materials`, `.resources-cell--video` (и `.resource-card` внутри) | `#ov-resources-card-music`, `#ov-resources-card-materials`, `#ov-resources-card-video` |
| RES сетка | `.resources-grid` | `#ov-resources-content` |
| RES контейнер панелей | `.resources-panels` | `#ov-resources-panels` |
| RES панель | `#resources-panel` | `#ov-resources-panels .ov-resources-panel` (видимая) |
| RES кнопка «Назад» | `.resources-panel__back` | `#ov-resources-panels .ov-resources-back-action` |
| RES триггер материалов | `.resource-card button[data-kind="materials"]` | `#ov-resources-card-materials .ov-resources-panel-trigger` |
| FOOT колонка | `.site-footer__inner` | `#ov-main-footer-content > footer > div` |

Ожидаемые computed-значения (сериализация Chrome) для прода по спецификации: карточка About `border` `1px solid rgba(239, 237, 245, 0.18)`, `borderRadius` `16px`, `boxShadow` `rgba(255, 255, 255, 0.035) 0px 1px 0px 0px inset, rgba(3, 2, 18, 0.24) 0px 20px 46px 0px`, `backdropFilter` `blur(14px) saturate(1.12)`, `backgroundImage` с `145deg`, `rgba(255, 255, 255, 0.045)`, `30%`, `rgba(49, 41, 77, 0.44)`, `rgba(18, 12, 52, 0.62)`; карточка ресурсов `backdropFilter` `blur(14px) saturate(1.25)`, `backgroundImage` с `rgba(255, 255, 255, 0.075)` и `34%`; карточка триптиха `backgroundColor` `rgba(33, 26, 62, 0.48)`, `border` `1px solid rgba(239, 237, 245, 0.15)`, `borderRadius` `14px`; рамка триптиха `backdropFilter` `none`, `borderRadius` `18px`, `boxShadow` с `rgba(2, 2, 12, 0.58) 0px 34px 76px` и `rgba(59, 77, 161, 0.22) 0px 0px 54px`; `#about-title` `backgroundImage` `linear-gradient(104deg, rgb(227, 175, 210) 2%, rgb(143, 157, 214) 52%, rgb(123, 194, 199))`, `webkitTextFillColor` `rgba(0, 0, 0, 0)`; плоские заголовки `backgroundImage` `none`, `color` `rgb(239, 237, 245)`. Карта: `--map-wedge: clamp(32px, 3.2vw, 52px)` даёт 46.08px на 1440 и 32px на 390; у оригинала `--ov-map-wedge-depth` 46px на 1440 (спецификация MAP-03). Ресурсы: `gridTemplateColumns` `320px 528px 272px` на 1440 (1152 − 2·16 = 1120 = 320 + 528 + 272). Панели оригинала: `--ov-resources-panel-z-index: 10000`, `--ov-resources-panel-duration: 620ms`, `--ov-resources-panel-stagger: 90ms`, `--ov-resources-panel-content-exit-duration: 180ms`. Футер: логотип `clamp(190px, 26vw, 300px)` → 300 на 1440 и 190 на 390; ширина колонки `min(100% - 32px, 1152px)` → 1152 и 358.

Поля формы: прод individual: `lightType` ×2 (radio), `firstName`, `lastName`, `countryId` (select), `city`, `email`, `consent` (checkbox); group: те же плюс `orgName` перед `firstName`. Оригинал individual: `LIGHT_TYPE` ×2, `FIRSTNAME`, `LASTNAME`, адрес (combobox `input[role="combobox"]`, id `base-ui-*`), `email`, `terms`; group: плюс поле организации во всю ширину (имя контрола снять по факту, спецификация называет его «Название организации*»). Скрытый `input[aria-hidden="true"]` combobox исключается.

Зонд фазы 8: `node .planning/phases/08-map-band-and-lights/qa/map-probe.mjs <band|fps|lights> --url https://thevladoss.github.io/esd-onevoice27/ --width 1440 --height 900 [--runs 3] [--reduced]`; резолв `playwright` через `PW_ROOT` или кэш `~/.npm/_npx/*/node_modules/playwright`; декодирование PNG без зависимостей: вторая страница с `<canvas>`, `Image` из data-URL, `drawImage`, `getImageData`; яркость `L = 0.2126 R + 0.7152 G + 0.0722 B`.

Формат SMOKE v1.0 (docs/qa/SMOKE.md): шапка-таблица «Поле | Значение» (дата, коммит, исполнитель, инструмент, прогон workflow), таблицы проверок с колонками критерия и результатов, блок скриншотов «Файл | Вьюпорт | Снят», «Итог» с вердиктом и замечаниями; разделы фаз 6 и 5 идут ниже основного чеклиста.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Скрипт замеров v11-measure.js, пиксельный зонд, снятие GLASS/MAP/FORM/MEDIA/RES/FOOT с оригинала и прода на двух вьюпортах</name>
  <files>.planning/phases/13-integration-qa/qa/v11-measure.js, .planning/phases/13-integration-qa/qa/pixel-probe.mjs, .planning/phases/13-integration-qa/qa/results/orig-1440.json, .planning/phases/13-integration-qa/qa/results/orig-390.json, .planning/phases/13-integration-qa/qa/results/prod-1440.json, .planning/phases/13-integration-qa/qa/results/prod-390.json, .planning/phases/13-integration-qa/qa/results/prod-band-1440.json, .planning/phases/13-integration-qa/qa/results/prod-band-390.json, .planning/phases/13-integration-qa/qa/results/prod-lights-1440.json, .planning/phases/13-integration-qa/qa/results/prod-fps-1440.json, .planning/phases/13-integration-qa/qa/results/prod-pixels-1440.json</files>
  <read_first>
    - .planning/phases/13-integration-qa/13-CONTEXT.md, раздел «Playwright-приёмка (QA-03)»: точные проверки и пороги по каждому пункту
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, разделы 1–6 (значения, с которыми сверяются computed-стили) и 7 QA-03
    - .planning/phases/13-integration-qa/qa/seams.evaluate.js (образец функции для browser_evaluate из плана 13-01, переиспользовать хелперы чтения стилей)
    - .planning/phases/08-map-band-and-lights/qa/map-probe.mjs (резолв playwright, запуск браузера, декодирование PNG через canvas, формула яркости, парсинг аргументов: скопировать в pixel-probe.mjs)
    - docs/research/v1.1/orig-rules.css строки 29–47 (карта оригинала), 296–328 (ресурсы), 329–337 (панели), 392–396 (футер); docs/research/v1.1/orig-vars.txt строки 156–160 и 196–199
    - docs/qa/SMOKE.md, замечания 2 и 4 раздела «Итог» (fps при WebGL-вкладках; счётчики в неактивной вкладке стоят на нуле)
  </read_first>
  <action>
    Реализует QA-03 (замеры) по D «Playwright-приёмка».

    **`qa/v11-measure.js`.** Тело одной функции с параметром `site` (`"prod"` или `"orig"`); внутри объект `S` с селекторами из таблицы `<interfaces>` в двух столбцах и хелперы: `cs(el, props, pseudo)` возвращает объект выбранных свойств `getComputedStyle`; `rect(el)` возвращает округлённые до 0.01 `x, y, width, height`; `ratio(el)` = `width / height`; `visibleControls(root)` собирает `input:not([type="hidden"]), select, textarea` с `getClientRects().length > 0` и без `aria-hidden="true"`, отдавая `{ name, id, type, required }` в порядке DOM. Возвращает объект:
    - `meta`: `site`, `href`, `innerWidth`, `innerHeight`, `devicePixelRatio`, `scrollWidth`, `documentHeight`, `userAgent`.
    - `glass`: для карточки About, карточки ресурсов, карточки и рамки триптиха свойства `backgroundImage`, `backgroundColor`, `borderTopWidth`, `borderTopStyle`, `borderTopColor`, `borderRadius`, `boxShadow`, `backdropFilter`; для заголовков `backgroundImage`, `color`, `webkitTextFillColor`, `fontWeight`, `fontSize`.
    - `map`: `clipPath` у оболочки карты, `clipPath` и `backgroundColor` псевдоэлемента `::before` подложки, `rect` оболочки, число `.light-bucket` и список `data-bucket` (только прод), `animationName`/`animationDuration` корзины 0 (прод), имена `CSSKeyframesRule` из доступных `document.styleSheets` (правила чужих origin пропускать через try/catch), число `.light.pulse` (прод), три точки для пиксельного зонда: `x = rect.left + rect.width * 0.5`, `y = rect.top + rect.height * 0.12`; `x = rect.left + rect.width * 0.08`, `y = rect.top + rect.height * 0.5`; `x = rect.left + rect.width * 0.92`, `y = rect.top + rect.height * 0.85` (округлить, записать в `map.samplePoints` вместе с текущим `scrollY`, чтобы зонд снимал те же места).
    - `form`: `fields.initial` (список контролов до выбора типа), затем клик по карточке individual → `fields.individual`, клик по карточке group → `fields.group`; `typeCard` (свойства `backgroundColor`, `borderTopColor`, `borderRadius`, `minHeight`, `padding`, `boxShadow` карточки group до и после выбора: `typeCardIdle`, `typeCardChecked`), `field` (`minHeight`, `borderRadius`, `backgroundColor`, `borderTopColor`, `padding`, `fontSize`, `fontWeight`, `color`), `label` (`fontWeight`, `fontSize`, `color`), `submit` (`minHeight`, `rect.width` против `rect.width` формы), `glassWrapper` (число `.glass-card` внутри корня формы, прод) и `formWidth` (`rect.width` корня формы).
    - `media`: для каждой карточки первой страницы (прод: до 6 `.news-card__cover`; оригинал: до 6 `#ov-news-feed article > div:first-child`) `ratio`, `rect`; у картинки `objectFit`, `objectPosition`, `naturalWidth`, `naturalHeight`, `currentSrc`; `coverRects` первой и четвёртой карточки с текущим `scrollY` для пиксельного зонда.
    - `res`: `rect` четырёх блоков и вложенной `.resource-card` (прод), `gridTemplateColumns`, `columnGap`, `rowGap`, `justifyContent`, `alignItems`, `rect.width` сетки; для текстового блока `borderTopStyle`, `borderTopColor`, `backgroundColor`, `borderRadius`, `padding`; отклонения `dw`, `dh` от эталонов 320×296 (music), 528×523 (copy), 272×336 (materials), 368×256 (video) в пикселях.
    - `foot`: у колонки `display`, `flexDirection`, `alignItems`, `textAlign`, `gap`, `rect.width`; список видимых прямых детей (`className` или `tagName`, `rect`, `centerDx = |cx − innerWidth / 2|`); у логотипа (`.wordmark--footer` на проде, `div:first-child` у оригинала) `rect.width`, `rect.height`, `filter`; у ссылок прода `flexDirection` списка `ul` и число `li`; `borderTopWidth` юридического текста.
    Функция ничего не кликает, кроме карточек типа формы; все клики панелей идут в Task 2.

    **Сессии Playwright MCP.** Порядок: сначала прод (WebGL-вкладок нет), затем оригинал. Прод 1440×900: `browser_resize`, открыть `https://thevladoss.github.io/esd-onevoice27/`, дождаться `#quote`, прокрутить до низа и обратно (reveal-обёртки открыты, огоньки видны), выполнить функцию с `site: "prod"` → `qa/results/prod-1440.json`. То же на 390×844 → `prod-390.json`. Оригинал: навигация на `https://onevoice27.org/` через `browser_run_code_unsafe` с `page.goto(url, { waitUntil: "commit", timeout: 120000 })` и ожиданием `#ov-about` до 120 с (Vercel challenge проходит на странице, `page.request` не использовать); при таймауте повторить навигацию ещё дважды; на 1440×900 и 390×844 выполнить функцию с `site: "orig"` → `orig-1440.json`, `orig-390.json`. Прокрутку до низа перед снятием делать и на оригинале (ленивые картинки новостей). Файлы из корня репозитория перенести в `qa/results/`, JSON-экранирование снять.

    **Зонд фазы 8 на проде** (закрыть вкладку оригинала, никаких `npm test`/`npm run build`): `band --width 1440 --height 900` → `prod-band-1440.json`; `band --width 390 --height 844` → `prod-band-390.json`; `lights --width 1440 --height 900` → `prod-lights-1440.json`; `fps --width 1440 --height 900 --runs 3` → `prod-fps-1440.json`. Ожидания как в 13-01 Task 2: `jumps` пустые, `skewJump` > 6, `buckets` 5, `halos` 942, медиана fps ≥ 50. Информативно снять fps оригинала тем же режимом (`--url https://onevoice27.org/`), если зонд проходит challenge; если нет, записать «не измерялся: challenge».

    **`qa/pixel-probe.mjs`.** ESM-скрипт на `playwright` с тем же резолвом и запуском браузера, что у `map-probe.mjs` (скопировать блоки `createRequire`/`PW_ROOT`/`PW_CHANNEL`/`PW_HEADLESS`, контекст `deviceScaleFactor: 1`). CLI: `node pixel-probe.mjs --url <url> --width 1440 --height 900 [--points] [--cover <css-селектор>] [--cover-index 0]`. Режим `points`: `goto` с `waitUntil: "load"`, прокрутить к `.map-shell` (`scrollIntoView({ block: "center" })`), подождать 600 мс, снять `page.screenshot({ type: "png" })` вьюпорта, декодировать на второй странице через canvas и для трёх точек (те же доли 0.5/0.12, 0.08/0.5, 0.92/0.85 от `getBoundingClientRect` оболочки после прокрутки, чтобы не зависеть от `scrollY`) напечатать `r, g, b, L`. Режим `cover`: найти N-й элемент по селектору, `scrollIntoView({ block: "center" })`, дождаться `img.complete` внутри (или 2 с), снять скриншот, взять `rect` элемента, посчитать среднюю яркость `L` полос `rows [top, top + 6)` и `[bottom − 6, bottom)` по всей ширине кадра, а также среднюю яркость центральной трети; напечатать JSON `{ selector, index, rect, topBand, bottomBand, center, pass: topBand > 12 && bottomBand > 12 }` и `PASS`/`FAIL`. Внешних зависимостей, кроме `playwright` и `node:*`, нет.
    Прогоны на проде 1440×900: `--cover .news-card__cover --cover-index 0`, `--cover-index 3` (карточка второго ряда), затем `--points` для полотна карты; всё в `prod-pixels-1440.json` (массив результатов). Ожидание: `topBand` и `bottomBand` > 12 у обеих карточек (чёрные полосы hqdefault дали бы около 0–8, отрезанные полосы дают яркость кадра); полотно карты: записать `r, g, b` трёх точек; ожидание CONTEXT для тёмного полотна `rgb(5 4 15)`: если наши точки отличаются от него по `L` больше чем на 6, замерить те же доли на оригинале (при прохождении challenge зондом; иначе через `browser_take_screenshot` вьюпорта карты оригинала и отчёт по пикселю в `browser_run_code_unsafe` с canvas) и внести расхождение в принятые отклонения SMOKE. Дефектом стыка это не считается: цвет полотна не входит в MAP-01…07.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/13-integration-qa/qa/results/';const j=n=>JSON.parse(fs.readFileSync(d+n,'utf8'));const p=j('prod-1440.json'),o=j('orig-1440.json'),f=j('prod-fps-1440.json'),l=j('prod-lights-1440.json'),px=j('prod-pixels-1440.json');const r=p.res;const ok=[Math.abs(r.music.dw)<=8,Math.abs(r.music.dh)<=8,Math.abs(r.copy.dw)<=8,Math.abs(r.copy.dh)<=8,Math.abs(r.materials.dw)<=8,Math.abs(r.materials.dh)<=8,Math.abs(r.video.dw)<=8,Math.abs(r.video.dh)<=8,Number(f.median)>=50,l.buckets===5,p.media.cards.every(c=>Math.abs(c.ratio-16/9)<=0.01),px.filter(x=>x.topBand!==undefined).every(x=>x.topBand>12&&x.bottomBand>12),p.glass.resourceCard.backdropFilter==='blur(14px) saturate(1.25)',o.glass.aboutCard.backdropFilter===p.glass.aboutCard.backdropFilter];if(ok.some(x=>!x)){console.error(ok);process.exit(1)}console.log('measure OK')" && grep -c "0.2126" .planning/phases/13-integration-qa/qa/pixel-probe.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `qa/v11-measure.js` существует (≥ 120 строк), содержит `site === "orig"`, селекторы `ov-about-step-card`, `ov-resources-card-music`, `ov-involve-item`, `LIGHT_TYPE-group`, `ov-news-feed`, `ov-main-footer-content`, `.resources-cell--video`, `.site-footer__inner`, `CSSKeyframesRule`, `samplePoints`
    - Четыре JSON `orig-1440.json`, `orig-390.json`, `prod-1440.json`, `prod-390.json` парсятся, `meta.site` и `meta.innerWidth` соответствуют имени файла; `meta.scrollWidth <= meta.innerWidth + 1` у прода на обоих вьюпортах
    - GLASS (prod-1440): `aboutCard.backdropFilter` `blur(14px) saturate(1.12)`, `aboutCard.borderTopColor` `rgba(239, 237, 245, 0.18)`, `aboutCard.borderRadius` `16px`, `aboutCard.boxShadow` содержит `rgba(3, 2, 18, 0.24) 0px 20px 46px`; `resourceCard.backdropFilter` `blur(14px) saturate(1.25)`, `resourceCard.backgroundImage` содержит `rgba(255, 255, 255, 0.075)`; `triptychItem.backgroundColor` `rgba(33, 26, 62, 0.48)`, `triptychItem.borderRadius` `14px`; `triptychFrame.backdropFilter` `none`; `titles.about.backgroundImage` содержит `104deg`; у `titles.map|form|involve|news|resources` `backgroundImage` `none` и `color` `rgb(239, 237, 245)`; те же свойства у `orig-1440.json` совпадают строка в строку для карточки About, карточки ресурсов и карточки триптиха (расхождения только в сериализации `transparent`/`rgba(0, 0, 0, 0)` и порядке слоёв записываются в SMOKE как замечание, а не как провал)
    - MAP (prod-1440): `map.shellClipPath` начинается с `polygon(` и содержит четыре вершины, вторая координата первой вершины 46.08px (±1), на 390 32px (±1); `orig-1440.json` `map.shellClipPath` четыре вершины со скосом 46px (±1); `map.buckets` `["0","1","2","3","4"]`, `map.pulseCount` 0, `map.keyframes` содержит `light-breathe` и не содержит `light-pulse`
    - `prod-band-1440.json`: `jumps` пусты для x = 200 и x = 1240, `skewJump` > 6; `prod-band-390.json` PASS; `prod-lights-1440.json`: `buckets` 5, `halos` 942, `cores` 942; `prod-fps-1440.json`: три значения и медиана ≥ 50
    - FORM (prod-1440): `form.fields.initial` без `orgName`, `form.fields.group` содержит `orgName` перед `firstName`, `form.fields.individual` без `orgName`; `form.glassWrapper` 0; `form.typeCardIdle.backgroundColor` `rgba(33, 26, 62, 0.42)`, `borderTopColor` `rgba(239, 237, 245, 0.16)`, `borderRadius` `16px`, `minHeight` `190px` (на 390 `150px`); `form.typeCardChecked.borderTopColor` `rgba(123, 194, 199, 0.72)`; `form.field.minHeight` `54px`, `borderRadius` `16px`, `backgroundColor` `rgba(33, 26, 62, 0.58)`, `borderTopColor` `rgba(239, 237, 245, 0.18)`; `form.submit.minHeight` ≥ 54px и ширина равна ширине формы (±1); `form.formWidth` 672 на 1440 (42rem); `orig-1440.json`: `form.fields.group` длиннее `form.fields.individual` ровно на одно поле
    - MEDIA (prod-1440 и prod-390): у всех `media.cards` `ratio` в пределах 1.767…1.789, `objectFit` `cover`, `objectPosition` `50% 50%`, `naturalWidth` 480 и `naturalHeight` 360; `orig-1440.json` `media.cards` `ratio` в тех же пределах; `prod-pixels-1440.json`: у двух записей `cover` `topBand` > 12 и `bottomBand` > 12, `pass` true; три точки полотна карты с `r, g, b, L`
    - RES (prod-1440): `res.gridTemplateColumns` `320px 528px 272px`, `res.gridWidth` 1152 (±1); `|dw| ≤ 8` и `|dh| ≤ 8` у `music` (320×296), `copy` (528×523), `materials` (272×336), `video` (368×256); `orig-1440.json` те же четыре блока в ±8 от тех же эталонов; prod-390: `copy` первым по `rect.y`, карточки шириной 328 (±8) и высотой 248 (±8)
    - FOOT (prod-1440 и prod-390): `foot.display` `flex`, `flexDirection` `column`, `alignItems` `center`, `textAlign` `center`; `foot.width` 1152 (±1) на 1440 и 358 (±1) на 390; четыре видимых ребёнка в порядке `wordmark--footer`, `site-footer__caption`, `site-footer__links`, `site-footer__legal`, у каждого `centerDx ≤ 2`; логотип 300 (±1) на 1440 и 190 (±1) на 390, `filter` содержит `drop-shadow`; `legalBorderTop` `0px`; `orig-1440.json`: `flexDirection` `column`, `alignItems` `center`, ширина 1152 (±1), видимые дети: логотип → подпись → юридический текст
    - `qa/pixel-probe.mjs` существует (≥ 80 строк), содержит `createRequire`, `PW_ROOT`, `deviceScaleFactor: 1`, `getImageData`, `0.2126`, `topBand`, `bottomBand`, `--points`; `node .planning/phases/13-integration-qa/qa/pixel-probe.mjs --url https://thevladoss.github.io/esd-onevoice27/ --width 1440 --height 900 --cover .news-card__cover --cover-index 0` печатает `PASS`
    - В корне репозитория нет файлов Playwright; `git status --porcelain | grep -v HANDOFF.json` показывает только `qa/v11-measure.js`, `qa/pixel-probe.mjs`, `qa/results/*.json`
  </acceptance_criteria>
  <done>Замеры шести пунктов сняты с обоих сайтов на двух вьюпортах одной функцией, зонды band/lights/fps и пиксельные проверки прода дали числа в пределах порогов, всё сохранено в `qa/results/`.</done>
</task>

<task type="auto">
  <name>Task 2: Интерактивные сценарии (панель, deep link, форма group, видео 16:9) на проде и оригинале, семь скриншотов</name>
  <files>.planning/phases/13-integration-qa/qa/results/prod-interactive-1440.json, .planning/phases/13-integration-qa/qa/results/orig-interactive-1440.json, docs/qa/v11-desktop.jpeg, docs/qa/v11-mobile.jpeg, docs/qa/v11-full.jpeg, docs/qa/v11-form-group.jpeg, docs/qa/v11-panel-materials.jpeg, docs/qa/v11-map-bottom.jpeg, docs/qa/v11-footer.jpeg</files>
  <read_first>
    - .planning/phases/13-integration-qa/13-CONTEXT.md: RES (z-index 10000, тайминги, scroll lock, фокус, Escape), список скриншотов и требование JPEG качества 80
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, RES-03 (тайминги 620ms / 90ms / 180ms, фокус на «Назад», Escape, возврат фокуса, deep link), MEDIA-03
    - .planning/phases/11-resources-panels/11-03-SUMMARY.md (классы фаз `is-opening`/`is-open`/`is-closing`, fallback-таймер 900 мс, `resources-panel-locked`)
    - docs/qa/SMOKE.md, раздел «Скриншоты» и замечание 4 (полную страницу снимать из активной вкладки, счётчики досчитаны)
    - docs/research/v1.1/orig-rules.css строки 329–337 (`#ov-resources-panels` и `.ov-resources-panel`), `html.ov-resources-panel-locked`
  </read_first>
  <action>
    Реализует QA-03 (интерактивные проверки RES, FORM, MEDIA-03) и список скриншотов из D.

    **Прод, 1440×900, `prod-interactive-1440.json`.** Открыть прод, прокрутить к `#resources`. Сценарий «панель материалов» (ключ `panelOpen`): кликнуть `.resource-card button[data-kind="materials"]`; через 100 мс снять `className` контейнера `.resources-panels` (ожидание `is-opening` или `is-open`), `position` `fixed`, `zIndex` `10000`; у псевдоэлементов: `::before` `transitionDuration` (`beforeDuration`, ожидание `0.62s`) и `transitionTimingFunction` `cubic-bezier(0.22, 1, 0.36, 1)`, `::after` `transitionDelay` (`afterDelay`, `0.09s`); у `#resources-panel` `transitionDelay` (`panelDelay`, `0.18s`); через 900 мс `is-open`, `opacity` панели `1`, `transform` `none` или `matrix(1, 0, 0, 1, 0, 0)`; `documentElement` и `body` с классом `resources-panel-locked` и `overflow` `hidden` (`htmlLocked`, `bodyLocked`); `document.activeElement` с классом `resources-panel__back` (`activeIsBack`); `document.elementFromPoint(720, 40)` лежит внутри `.resources-panels` (`coversHeader`); `window.scrollY` не изменился после открытия. Затем `browser_press_key` Escape (ключ `panelEscape`): через 50 мс класс `is-closing`; через 1000 мс `document.querySelector(".resources-panels")` null (`containerGone`), классы lock сняты, `overflow` не `hidden`, `document.activeElement` это `button[data-kind="materials"]` с `aria-expanded="false"` (`focusOnCard`). Сценарий «Назад» (`panelBack`): открыть снова, кликнуть `.resources-panel__back`, те же проверки закрытия. Сценарий «deep link» (`deepLink`): перейти на `https://thevladoss.github.io/esd-onevoice27/#resources-materials` полной навигацией, через 1000 мс: `#resources-panel[data-kind="materials"]` есть (`kind`), `button[data-kind="materials"]` с `aria-expanded="true"`, `details.resources-group` 5, `#resources-group-esd[open]` с 4 ссылками `a.resources-file__action`; закрыть Escape. Сценарий «видео 16:9» (`videoPanel`): клик `button[data-kind="video"]`, через 900 мс: `.resources-panel .ve` 16 (`count`), у каждого `ratio` 1.767…1.789, `.ve-poster` `objectFit` `cover`, `objectPosition` `50% 50%`, `naturalWidth` 480 / `naturalHeight` 360 у загрузившихся, `li.resources-file` 1; закрыть Escape. Сценарий «reduced motion» (`reducedMotion`): `emulateMedia({ reducedMotion: "reduce" })` через `browser_run_code_unsafe`, перезагрузка, клик по «Материалы»: класс `is-open` уже в первом снятии через 20 мс, `transitionDuration` слоёв `0.01ms`; вернуть `reducedMotion: null`. Сценарий «форма group» (`formGroup`): прокрутить к `#light-form`, клик по `.lf-type[data-type="group"]`: `input[name="orgName"]` виден (`orgVisible`), стоит в DOM раньше `firstName`, у его `.lf-field` класс `lf-span`, подпись начинается с «Название организации» и содержит `.lf-required[title="Обязательно"]`; клик «Зажечь свой свет» с пустыми полями: ошибка «Укажите название организации» рядом с полем; затем выбрать individual: `orgName` исчез.

    **Оригинал, 1440×900, `orig-interactive-1440.json`** (навигация по протоколу Task 1). Панель: клик `#ov-resources-card-materials .ov-resources-panel-trigger`; контейнер `#ov-resources-panels`: `position`, `zIndex` (ожидание `10000`), `::before` `transitionDuration` (`0.62s`), `::after` `transitionDelay` (`0.09s`), панель `transitionDelay` (`0.18s`), класс `is-open` через 900 мс, `html` с классом `ov-resources-panel-locked` и `overflow hidden`, `document.activeElement` (ожидание `.ov-resources-back-action`); Escape → `is-closing`, затем контейнер `visibility hidden`, фокус вернулся на триггер (записать фактическое). Deep link оригинала не проверяется (другой механизм). Форма group: клик `label[for="LIGHT_TYPE-group"]` → список видимых контролов, имя поля организации, его положение относительно `#FIRSTNAME`. Видео: у оригинала панель видео с фото-превью; снять число превью и `ratio` первых трёх, если панель открывается по `#ov-resources-card-video .ov-resources-panel-trigger`.

    **Скриншоты прода** (JPEG качества 80; если `browser_take_screenshot` не даёт задать качество, снимать через `browser_run_code_unsafe` вызовом `page.screenshot({ path, type: "jpeg", quality: 80, ... })`; файлы из корня репозитория или каталога MCP перенести в `docs/qa/`):
    - `v11-desktop.jpeg`: 1440×900, первый экран после загрузки;
    - `v11-mobile.jpeg`: 390×844, первый экран;
    - `v11-full.jpeg`: 1440, `fullPage: true`, из активной вкладки после прокрутки до низа и обратно (reveal открыты, счётчики досчитаны до 694 / 248);
    - `v11-form-group.jpeg`: 1440, `clip` по `getBoundingClientRect` секции `#light-form` после выбора «Групповой маяк» (поле организации видно);
    - `v11-panel-materials.jpeg`: 1440×900 вьюпорт с открытой панелью материалов в фазе `is-open`;
    - `v11-map-bottom.jpeg`: 1440, `clip` во всю ширину от `bottom` прямоугольника `.map-shell` − 260px до + 260px (стык карты и формы, орб, отсутствие второй линии);
    - `v11-footer.jpeg`: 1440, `clip` по прямоугольнику `footer.site-footer`.
    Проверить размер каждого файла: больше 8 КБ и меньше 900 КБ; `file` показывает JPEG. Сравнить глазами `v11-map-bottom.jpeg` с `docs/research/v1.1/orig-map-bottom.jpeg`, `v11-form-group.jpeg` с `orig-form-group.jpeg`, `v11-panel-materials.jpeg` с `orig-panel-materials.jpeg`, `v11-footer.jpeg` с `orig-footer.jpeg` и записать наблюдения (одна строка на пару) для раздела SMOKE. Корень репозитория после задачи без служебных файлов.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/13-integration-qa/qa/results/';const p=JSON.parse(fs.readFileSync(d+'prod-interactive-1440.json','utf8'));const ok=[p.panelOpen.zIndex==='10000',p.panelOpen.position==='fixed',p.panelOpen.beforeDuration==='0.62s',p.panelOpen.afterDelay==='0.09s',p.panelOpen.panelDelay==='0.18s',p.panelOpen.htmlLocked===true,p.panelOpen.activeIsBack===true,p.panelEscape.containerGone===true,p.panelEscape.focusOnCard===true,p.deepLink.kind==='materials',p.videoPanel.count===16,p.formGroup.orgVisible===true];if(ok.some(x=>!x)){console.error(ok);process.exit(1)};for(const f of ['v11-desktop','v11-mobile','v11-full','v11-form-group','v11-panel-materials','v11-map-bottom','v11-footer']){const s=fs.statSync('docs/qa/'+f+'.jpeg').size;if(s<8192||s>921600){console.error(f,s);process.exit(1)}}console.log('interactive+screens OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `prod-interactive-1440.json`: `panelOpen.zIndex` `10000`, `position` `fixed`, `beforeDuration` `0.62s`, `beforeTiming` `cubic-bezier(0.22, 1, 0.36, 1)`, `afterDelay` `0.09s`, `panelDelay` `0.18s`, `htmlLocked` и `bodyLocked` true, `activeIsBack` true, `coversHeader` true, `scrollYBefore === scrollYAfter`; `panelEscape.closingSeen` true, `containerGone` true, `lockCleared` true, `focusOnCard` true; `panelBack` те же четыре флага; `deepLink.kind` `materials`, `deepLink.expanded` `true`, `deepLink.groups` 5, `deepLink.esdOpen` true, `deepLink.esdLinks` 4; `videoPanel.count` 16, все `ratio` 1.767…1.789, `objectFit` `cover`, `fileCards` 1; `reducedMotion.firstClass` содержит `is-open` и не содержит `is-opening`, `layerDuration` `0.01ms`; `formGroup.orgVisible` true, `orgBeforeFirstName` true, `orgSpan` true, `requiredMark` true, `emptyError` «Укажите название организации», `orgGoneAfterIndividual` true
    - `orig-interactive-1440.json`: `panelOpen.zIndex` `10000`, `position` `fixed`, `beforeDuration` `0.62s`, `afterDelay` `0.09s`, `panelDelay` `0.18s`, `htmlLocked` true; `activeElement` записан (ожидание `.ov-resources-back-action`); `panelEscape.closingSeen` true; `formGroup.fields` длиннее `formGroup.fieldsIndividual` на одно поле, имя поля организации записано
    - Семь файлов `docs/qa/v11-desktop.jpeg`, `v11-mobile.jpeg`, `v11-full.jpeg`, `v11-form-group.jpeg`, `v11-panel-materials.jpeg`, `v11-map-bottom.jpeg`, `v11-footer.jpeg` существуют, `file` определяет каждый как JPEG, размер каждого 8 КБ…900 КБ; `v11-full.jpeg` выше 8000px (полная страница), `v11-mobile.jpeg` шириной 390
    - `ls docs/qa/*.png 2>/dev/null | wc -l` → 0; в корне репозитория нет `*.jpeg`, `*.png`, `*.json` от Playwright (`git status --porcelain | grep -v HANDOFF.json` показывает только файлы из `files_modified` планов 13-02)
  </acceptance_criteria>
  <done>Панель, deep link, видео 16:9, reduced motion и форма в состоянии group проверены на проде числами и флагами, оригинал снят для тех же строк таблицы, семь скриншотов лежат в docs/qa/.</done>
</task>

<task type="auto">
  <name>Task 3: Раздел «Фаза 13 / v1.1» в docs/qa/SMOKE.md, правка README, коммит docs, push и повторная проверка прода</name>
  <files>docs/qa/SMOKE.md, README.md</files>
  <read_first>
    - docs/qa/SMOKE.md целиком (формат шапки, таблиц, блоков «Скриншоты», «Прод», «Итог», разделов фаз 5 и 6; новый раздел встаёт после раздела «Фаза 6» в конце файла)
    - .planning/phases/13-integration-qa/13-CONTEXT.md, «Известные принятые отклонения» и «Документы»
    - .planning/phases/13-integration-qa/13-01-SUMMARY.md (числа гейта, id прогона, таблица хэшей `qa/results/prod-hashes.txt`, решение по `.map-band .lf-section`, правки стыков)
    - .planning/phases/09-form-original/09-02-SUMMARY.md, «Assumption Drift» (обводка focus-visible поверх рамки поля); .planning/phases/10-media-previews/10-01-SUMMARY.md (панель `inset: auto 4px 4px`); .planning/phases/08-map-band-and-lights/08-02-SUMMARY.md (вердикт MAP-06, .22)
    - README.md строки 55–60 («Браузерный smoke») и 82–100 («Структура»)
    - qa/results/*.json из Task 1 и Task 2 (источник чисел для ячеек)
  </read_first>
  <action>
    Реализует QA-03 (таблица «оригинал / прод» и скриншоты в docs) и D «Документы».

    **`docs/qa/SMOKE.md`.** Добавить в конец раздел `## Фаза 13 / v1.1: приёмка шести правок по оригиналу` (заголовок содержит строку `Фаза 13 / v1.1`). Структура:
    1. Шапка-таблица «Поле | Значение»: дата, коммит прода (sha из `prod-hashes.txt`) и имена ассетов, прогон workflow со ссылкой `https://github.com/thevladoss/esd-onevoice27/actions/runs/<id>`, инструмент (Playwright MCP, Chrome, вьюпорты 1440×900 и 390×844, DPR 1; зонд `map-probe.mjs` фазы 8 и `pixel-probe.mjs` фазы 13 через `playwright` из кэша npx), исполнитель.
    2. `### Гейт и деплой (QA-01, QA-02)`: таблица «Шаг | Результат» на семь строк: `npx tsc -b`, `npm test` (файлы/тесты), `npm run lint`, `npm run build` (ассеты), `node scripts/check-dist.mjs` (`OK: 11 проверок`), workflow (id, success), побайтная сверка (таблица из `prod-hashes.txt`, 4 из 4). Ниже список тестов QA-01 с файлами: `LightForm.test.tsx` (состояния и `orgName`), `ResourcePanel.test.tsx` и `Resources.test.tsx` (фокус, Escape, scroll lock, deep link), `EsdMap.test.tsx` (пять корзин без `pulse`), `NewsCard.test.tsx` (16:9), `Footer.test.tsx` (порядок узлов), `App.seams.test.tsx` (стыки).
    3. `### Стыки фаз`: таблица «Стык | Проверка | Preview | Прод» на пять строк (8+9, 7+11, 7+9, 10+11, скос и огоньки) со значениями из `preview-seams-1440.json` и `prod-1440.json`/`prod-interactive-1440.json`.
    4. Шесть подразделов `### GLASS`, `### MAP`, `### FORM`, `### MEDIA`, `### RES`, `### FOOT`, каждый таблица «Проверка | Ожидание | Оригинал 1440 | Прод 1440 | Прод 390 | Статус». Строки: GLASS: карточка About (background-image, border, box-shadow, backdrop-filter), карточка ресурсов (те же четыре), карточка триптиха (background-color, border, radius), рамка триптиха (backdrop-filter, box-shadow), заголовок About (background-image), пять плоских заголовков (color, background-image). MAP: полигон `clip-path` (четыре вершины, глубина скоса), вторая линия по MAP-03 (`jumps` по x = 200 и 1240, `skewJump`), fps (три значения, медиана, порог 50; fps оригинала или «не измерялся: challenge»), корзины (`data-bucket` 0–4, `animationName`), отсутствие `light-pulse` в keyframes, reduced motion (из `preview-lights-reduced-1440.json`: opacity .22, r 6px), полотно карты (три точки `r, g, b`, ожидание CONTEXT `rgb(5 4 15)`, фактическое значение обоих сайтов), scrollWidth. FORM: поля в состоянии individual (список), поля в состоянии group (список, положение поля организации), карточка типа в покое и выбранная (background, border, radius, min-height), поле (min-height, radius, background, border), кнопка (min-height, ширина), отсутствие стеклянной обёртки, ошибка пустого `orgName`. MEDIA: отношение сторон карточек (все шесть), `object-fit`/`object-position`, яркость полос (`topBand`, `bottomBand` двух карточек, порог 12), превью в панели «Видео» (16 штук, ratio, cover). RES: прямоугольники четырёх блоков с `dw`/`dh` (порог ±8), `grid-template-columns`, колонка на 390 (порядок и размеры карточек), панель (z-index 10000, position fixed, 0.62s / 0.09s / 0.18s, scroll lock html и body, фокус на «Назад», Escape, «Назад», возврат фокуса, deep link, reduced motion). FOOT: колонка (display, direction, align, ширина 1152 / 358), порядок узлов, центровка (`centerDx` каждого узла, порог 2), логотип (300 / 190, drop-shadow), ссылки столбиком, юридический текст без линии. В столбце «Статус» только «да» или «отклонение №N» со ссылкой на список ниже; пустых ячеек не оставлять. Значения оригинала на 390 приводить строкой «390: …» внутри ячейки оригинала там, где они снимались (`orig-390.json`).
    5. `### Принятые отклонения`: нумерованный список: (1) статичный ореол под reduced motion opacity .22 по глобальной политике вместо .45 спецификации; (2) город необязателен, адрес оригинала (геокодер) заменён на выбор страны и город; (3) панель заголовка карточки новости `inset: auto 4px 4px` (совпадает с CSS оригинала, спецификация обобщала); (4) Onest вместо Figtree; (5) обводка `:focus-visible` проекта поверх рамки поля формы (политика фокуса, `motionPolicy.test.ts`); (6) футер прода несёт три ссылки в `nav`, у оригинала оба `nav` пусты; (7) цвет полотна карты, если замер разошёлся с `rgb(5 4 15)` больше чем на 6 по яркости; плюс любые расхождения, найденные в Task 1–2 (с числами). Ничего из этого списка не чинить: это запись, не задача.
    6. `### Скриншоты`: таблица «Файл | Вьюпорт | Что видно» на семь строк `docs/qa/v11-*.jpeg` и строка сравнения с `docs/research/v1.1/orig-*.jpeg` из Task 2.
    7. `### Итог`: таблица «Поле | Значение»: вердикт («Принято» или «Принято с замечаниями» с перечислением), замечания к методике (challenge Vercel, fps при закрытых WebGL-вкладках, экранирование `browser_evaluate`), отложенное на v2 (из REQUIREMENTS: Lighthouse, OG-превью и прочее без изменений).
    Существующие разделы файла не менять. Заголовок файла и вводный абзац дополнить одной фразой: чеклист v1.1 ниже, в разделе «Фаза 13 / v1.1».

    **`README.md`.** В разделе «Браузерный smoke» добавить абзац: раздел «Фаза 13 / v1.1» в `docs/qa/SMOKE.md` сравнивает прод с оригиналом onevoice27.org по шести правкам (стекло, лента карты и огоньки, форма, превью, ресурсы и панели, футер) на 1440×900 и 390×844, скриншоты `docs/qa/v11-*.jpeg`; скрипты замеров лежат в `.planning/phases/13-integration-qa/qa/` и запускаются против прода. В разделе «Структура» уточнить строки: `src/components/map/` → «карта дивизиона, лента `MapBand` с формой и огоньки», `src/components/resources/` → «музыка, материалы, видео и полноэкранные панели». Остальной README не трогать; проверить, что упомянутое число проверок `check:dist` (11) и адреса не изменились.

    **Коммит и push.** Стадировать поимённо: `docs/qa/SMOKE.md`, семь `docs/qa/v11-*.jpeg`, `README.md`, `.planning/phases/13-integration-qa/qa/v11-measure.js`, `qa/pixel-probe.mjs`, `qa/results/*.json` (без `.planning/HANDOFF.json`). Коммит `docs(13): приёмка v1.1 на проде, SMOKE, скриншоты и README`. `git push origin main`. Дождаться прогона workflow по протоколу 13-01 Task 3 (`gh run watch <id> --exit-status` или цикл `gh run view`), ожидание `success`. Повторно снять sha256 живых `index.html` и трёх ассетов и сравнить с `qa/results/prod-hashes.txt`: имена и хэши ассетов не изменились (сборка не зависит от docs), `index.html` тоже равен. Если хэш отличается, разобраться (например, изменился `package-lock` или исходники в Task 1–2 по дефекту приёмки) и обновить таблицу в SMOKE. `git status --porcelain | grep -v HANDOFF.json` пуст.
  </action>
  <verify>
    <automated>grep -c "Фаза 13 / v1.1" docs/qa/SMOKE.md && for h in "### GLASS" "### MAP" "### FORM" "### MEDIA" "### RES" "### FOOT" "### Принятые отклонения" "### Скриншоты"; do grep -q "^$h" docs/qa/SMOKE.md || exit 1; done && grep -c "v11-" README.md && test "$(git status --porcelain | grep -v HANDOFF.json | wc -l)" -eq 0 && test "$(gh run view "$(gh run list --workflow deploy.yml --limit 1 --json databaseId --jq '.[0].databaseId')" --json conclusion --jq .conclusion)" = success</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Фаза 13 / v1.1" docs/qa/SMOKE.md` → 1; подразделы `### Гейт и деплой`, `### Стыки фаз`, `### GLASS`, `### MAP`, `### FORM`, `### MEDIA`, `### RES`, `### FOOT`, `### Принятые отклонения`, `### Скриншоты`, `### Итог` присутствуют по одному разу
    - В таблицах GLASS…FOOT нет пустых ячеек результата (`grep -c "| *|" docs/qa/SMOKE.md` в новом разделе → 0); в разделе есть строки со значениями `blur(14px) saturate(1.25)`, `10000`, `0.62s`, `320px 528px 272px`, `1152`, порогами `± 8`, `> 12`, `≥ 50`, `> 6`; `grep -c "sha256" docs/qa/SMOKE.md` вырос на 1 и больше относительно версии до задачи
    - `### Принятые отклонения` содержит не меньше шести пунктов, среди них `.22`, `Onest`, `inset: auto 4px 4px`, `focus-visible`
    - `### Скриншоты` перечисляет все семь файлов `v11-desktop.jpeg`, `v11-mobile.jpeg`, `v11-full.jpeg`, `v11-form-group.jpeg`, `v11-panel-materials.jpeg`, `v11-map-bottom.jpeg`, `v11-footer.jpeg`
    - Существующие разделы SMOKE.md выше нового не изменились по содержанию (`git diff HEAD~1 -- docs/qa/SMOKE.md` содержит только добавления и одну фразу во введении)
    - README: `grep -c "v11-" README.md` ≥ 1; `grep -c "Фаза 13 / v1.1" README.md` → 1; `grep -c "MapBand" README.md` → 1; `grep -c "OK: 11 проверок" README.md` → 1 (число проверок не сбито)
    - `git log --oneline -1` → `docs(13): приёмка v1.1 на проде, SMOKE, скриншоты и README`; `git status --porcelain | grep -v HANDOFF.json` пуст; `git log origin/main --oneline -1` совпадает с локальным HEAD
    - Последний прогон `deploy.yml` на HEAD завершился `success`; sha256 живых `index.html` и трёх ассетов равны строкам `qa/results/prod-hashes.txt` (4 из 4)
  </acceptance_criteria>
  <done>SMOKE.md содержит полную таблицу приёмки v1.1 с числами обоих сайтов, отклонениями и скриншотами; README указывает на неё; документы в `main` и на GitHub, прод после docs-пуша прежний.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер Playwright MCP → onevoice27.org (Vercel challenge, Mapbox WebGL) | чужой сайт читается только через страницу; данные не отправляются, форма оригинала не сабмитится |
| Браузер → прод GitHub Pages | статический сайт, только чтение и клики по панелям и форме без отправки |
| Репозиторий ← артефакты Playwright (JSON, скриншоты в корне) | случайный коммит служебных файлов или экранированного JSON |
| Исполнитель → GitHub (push docs, workflow) | публикация документов и повторный деплой той же сборки |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Information disclosure | скриншоты и JSON с оригинала | accept | публичные страницы без персональных данных; форма оригинала заполняется только выбором типа, поля не сабмитятся |
| T-13-06 | Tampering | числа в таблицах SMOKE | mitigate | все значения переписываются из `qa/results/*.json`, файлы коммитятся рядом с SMOKE, проверочные `node -e` в verify читают те же JSON |
| T-13-07 | Denial of service | fps-замер при открытом Mapbox оригинала | mitigate | зонд fps запускается до сессии с оригиналом и при закрытых WebGL-вкладках; три прогона, медиана |
| T-13-08 | Repudiation | приём прода после docs-пуша | mitigate | повторная сверка sha256 четырёх файлов с `prod-hashes.txt` |
| T-13-09 | Tampering | файлы Playwright в корне репозитория | mitigate | перенос в `qa/results/` и `docs/qa/`, стадирование поимённо, `git status` пуст перед коммитом |
| T-13-SC | Tampering | npm-установки | accept | пакеты не добавляются; `playwright` из кэша npx MCP или scratch-каталога по 08-01-SUMMARY, `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `qa/results/prod-1440.json`, `prod-390.json`, `orig-1440.json`, `orig-390.json`, `prod-interactive-1440.json`, `orig-interactive-1440.json`, `prod-pixels-1440.json`, `prod-band-*.json`, `prod-lights-1440.json`, `prod-fps-1440.json` существуют и проходят проверки `node -e` из verify задач 1 и 2
- Пороги: fps ≥ 50; `jumps` пусты вне скоса (перепад > 6 только на линии скоса); яркость полос > 12; блоки ресурсов в ±8px; полигон четырёх вершин со скосом 46px / 32px; z-index 10000; тайминги 0.62s / 0.09s / 0.18s; центры узлов футера в ±2px
- Семь JPEG в `docs/qa/`, PNG нет, корень репозитория чист
- SMOKE.md с разделом «Фаза 13 / v1.1», README обновлён, коммит docs в `origin/main`, workflow зелёный, хэши прода равны `prod-hashes.txt`
</verification>

<success_criteria>
- QA-03: таблица «оригинал / прод» на 1440×900 и 390×844 по GLASS, MAP, FORM, MEDIA, RES, FOOT в `docs/qa/SMOKE.md` с измеренными значениями и статусом каждой строки; скриншоты `docs/qa/v11-*.jpeg`
- Критерий 3 фазы 13 ROADMAP выполнен; критерий 4 (стыки) подтверждён на проде в таблице «Стыки фаз»
- Принятые отклонения записаны списком с номерами, на них ссылаются статусы строк
- Документы в `main` и на GitHub, прод после пуша побайтно прежний
</success_criteria>

<output>
Create `.planning/phases/13-integration-qa/13-02-SUMMARY.md` when done (вердикт приёмки, список отклонений с числами, id повторного прогона workflow, замечания к методике Playwright MCP с оригиналом)
</output>
