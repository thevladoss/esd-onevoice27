---
phase: 01-scaffold-and-deploy
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/Section.tsx
  - src/components/layout/Eyebrow.tsx
  - src/components/layout/GradientTitle.tsx
  - src/components/layout/Button.tsx
  - src/components/layout/GlassCard.tsx
  - src/components/layout/primitives.css
  - src/components/layout/primitives.test.tsx
  - src/components/placeholders.test.tsx
  - src/components/hero/Hero.tsx
  - src/components/map/MapSection.tsx
  - src/components/form/LightForm.tsx
  - src/components/about/About.tsx
  - src/components/involve/Involve.tsx
  - src/components/news/News.tsx
  - src/components/resources/Resources.tsx
  - src/components/quote/Quote.tsx
autonomous: true
requirements: [SHELL-05]

must_haves:
  truths:
    - "Посетитель видит восемь секций с надзаголовком, градиентным заголовком и одной строкой о том, что появится, оформленных шрифтами Onest/Noto Sans и палитрой оригинала"
    - "В секции #hero стоит единственный h1 «Вместе, единым голосом» и кнопка-пилюля «Зажечь свой свет», ведущая на #light-form"
    - "Примитивы Section, Eyebrow, GradientTitle, Button, GlassCard существуют, покрыты тестами и готовы к использованию фазами 2–4"
  artifacts:
    - path: "src/components/layout/Section.tsx"
      provides: "секция с id, eyebrow, h2-заголовком и контентной шириной 72rem"
      exports: ["Section"]
    - path: "src/components/layout/GradientTitle.tsx"
      provides: "h1/h2 с градиентом section или hero через background-clip: text"
      exports: ["GradientTitle"]
    - path: "src/components/layout/Button.tsx"
      provides: "пилюля primary/ghost как a или button"
      exports: ["Button"]
    - path: "src/components/layout/GlassCard.tsx"
      provides: "стеклянная карточка с border rgba(184,192,230,.22), radius 16px, тенью оригинала"
      exports: ["GlassCard"]
    - path: "src/components/layout/primitives.css"
      provides: "классы .gradient-title*, .btn*, .glass-card*"
      contains: ".btn--primary"
    - path: "src/components/hero/Hero.tsx"
      provides: "заглушка hero с h1 и CTA"
      contains: "#light-form"
  key_links:
    - from: "src/components/hero/Hero.tsx"
      to: "src/data/copy.ts"
      via: "copy.sections.hero и copy.cta.lightYourLight"
      pattern: "copy\\.cta\\.lightYourLight"
    - from: "src/components/layout/Section.tsx"
      to: "src/components/layout/GradientTitle.tsx"
      via: "рендер title как h2 variant section"
      pattern: "GradientTitle"
    - from: "src/components/layout/primitives.css"
      to: "src/styles/global.css"
      via: "var(--gradient-title), var(--gradient-action), var(--glass-border)"
      pattern: "var\\(--gradient-action\\)"
---

## Phase Goal

**Как** посетитель, **я хочу** открыть опубликованный на GitHub Pages лендинг с рабочим header, footer и визуальным языком оригинала, **чтобы** с первого экрана узнавать «Единый голос 27» и переходить к разделам по меню, пока каждый пуш в main сам прогоняет тесты, билд и деплой.

<objective>
Собрать дизайн-примитивы (`Section`, `Eyebrow`, `GradientTitle`, `Button`, `GlassCard`) по контракту UI-SPEC и заменить восемь пустых секций-заглушек на осмысленные заглушки с eyebrow, заголовком и строкой о будущем содержимом; в hero поставить единственный h1 и primary-кнопку «Зажечь свой свет».

Purpose: без этих примитивов фазы 2–4 будут изобретать оформление заново; заглушки дают якорям меню реальные цели и делают страницу читаемой уже после первого деплоя.

Output: пять примитивов с общим `primitives.css`, восемь секций-заглушек, тесты `primitives.test.tsx` и `placeholders.test.tsx`, зелёные `npm test`, `npm run build`, `npm run lint`.

Ограничение параллельности: план выполняется одновременно с 01-03 (Header) и 01-04 (Footer). Не редактировать `src/App.tsx`, `src/main.tsx`, `index.html`, `src/styles/*.css`, `src/data/copy.ts`, `src/test/setup.ts`, `package.json`, `Header.tsx`, `Footer.tsx`, `Wordmark.tsx`, `SkipLink.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-and-deploy/01-CONTEXT.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md
@.planning/phases/01-scaffold-and-deploy/01-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Готовые контракты из плана 01-01. Использовать напрямую, не исследовать и не менять. -->

```ts
// src/data/copy.ts
export const sectionIds = ["hero", "map", "light-form", "about", "involve", "news", "resources", "quote"] as const;
export type SectionId = (typeof sectionIds)[number];
export const copy = {
  cta: { lightYourLight: { label: "Зажечь свой свет", href: "#light-form" } },
  sections: {
    hero: { eyebrow: string; title: string; body: string },
    map, lightForm, about, involve, news, resources, quote  // тот же shape
  },
  // shell, footer — см. 01-01-PLAN.md
} as const;
```

```css
/* Доступно из src/styles/tokens.css (@theme → утилиты Tailwind v4): */
/* цвета: bg-/text-midnight-950|900|800, unity-950|700|500, signal-600|400|300, horizon-600|400|200, paper */
/* шрифты: font-display (Onest), font-body (Noto Sans) */
/* размеры: text-xs, text-sm, text-base, text-section, text-wordmark, text-tagline */
/* радиусы: rounded-card, rounded-header, rounded-header-compact, rounded-pill */
/* тени: shadow-card, shadow-header, shadow-header-mobile, shadow-button; кривые: ease-ui, ease-header */

/* Доступно из src/styles/global.css (:root): */
/* --glass-border, --glass-surface, --gradient-brand, --gradient-title, --gradient-action, --dur-ui, --dur-header */
/* утилиты: glass, text-gradient-brand, focus-ring; глобальный :focus-visible уже задан */
```

```ts
// Контракты, которые СОЗДАЁТ этот план (фазы 2–4 опираются на них):
export function Section(props: { id: SectionId; eyebrow?: string; title?: string; children?: ReactNode; className?: string }): ReactElement;
export function Eyebrow(props: { children: ReactNode; className?: string }): ReactElement;           // <p class="eyebrow">
export function GradientTitle(props: { as: "h1" | "h2"; variant: "hero" | "section"; children: ReactNode; className?: string }): ReactElement; // class="gradient-title gradient-title--{variant}"
type ButtonOwnProps = { variant?: "primary" | "ghost"; className?: string; children: ReactNode };
export function Button(props: ButtonOwnProps & ({ as: "a" } & ComponentPropsWithoutRef<"a">) | ButtonOwnProps & ({ as?: "button" } & ComponentPropsWithoutRef<"button">)): ReactElement; // class="btn btn--{variant}"
export function GlassCard(props: { children: ReactNode; className?: string; interactive?: boolean; as?: "div" | "article" | "li" }): ReactElement; // class="glass-card glass [glass-card--interactive]"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Примитивы Section, Eyebrow, GradientTitle, Button, GlassCard с тестами</name>
  <files>src/components/layout/Section.tsx, src/components/layout/Eyebrow.tsx, src/components/layout/GradientTitle.tsx, src/components/layout/Button.tsx, src/components/layout/GlassCard.tsx, src/components/layout/primitives.css, src/components/layout/primitives.test.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md (строки 171–210 типографика и правило насыщенности 900; 407–456 Section, Eyebrow, GradientTitle, Button, GlassCard; 227–239 где разрешён акцент)
    - .planning/phases/01-scaffold-and-deploy/01-CONTEXT.md (раздел «Дизайн-система», абзац «Примитивы в src/components/layout/»)
    - src/styles/tokens.css и src/styles/global.css (какие утилиты и переменные уже существуют; ничего не дублировать)
    - src/data/copy.ts (тип SectionId)
    - docs/research/orig-custom-styles.css (найти `grep -n 'ov-hero-title-gradient' docs/research/orig-custom-styles.css`: стопы градиента H1 для variant="hero"; строки 1329–1339 градиент заголовка секции; 1355–1400 стеклянная карточка; 2839–2900 кнопка hero)
    - .planning/phases/01-scaffold-and-deploy/01-01-SUMMARY.md (что фактически создано планом 01-01)
  </read_first>
  <behavior>
    - Section: `render(<Section id="about" eyebrow="Глобальное влияние" title="Что такое Единый голос 27?">Тело</Section>)` даёт `section#about`, внутри `p.eyebrow` с текстом eyebrow, `h2` с текстом title и текст «Тело»; без `eyebrow`/`title` в секции нет `h2` и нет `p.eyebrow`
    - GradientTitle: `as="h1" variant="hero"` рендерит `H1` с классами `gradient-title gradient-title--hero`; `as="h2" variant="section"` рендерит `H2` с классом `gradient-title--section`
    - Button: `as="a" href="#light-form"` рендерит ссылку с `href="#light-form"` и классами `btn btn--primary`; без `as` рендерит `button` с `type="button"`; `variant="ghost"` даёт класс `btn--ghost`; `disabled` пробрасывается на `button`
    - GlassCard: рендерит детей внутри элемента с классами `glass-card glass`; `interactive` добавляет `glass-card--interactive`; `as="article"` меняет тег
    - Eyebrow: рендерит `p.eyebrow` с текстом
  </behavior>
  <action>
Сначала написать `primitives.test.tsx` (названия тестов на русском) по блоку `<behavior>`, убедиться, что он падает, затем реализовать примитивы. Один именованный экспорт на файл, типы только через `import type`. Стили с псевдоэлементами и градиентами живут в `src/components/layout/primitives.css` (импортируется из `GradientTitle.tsx`, `Button.tsx`, `GlassCard.tsx`; Vite дедуплицирует); раскладка и отступы через утилиты Tailwind.

1. `Section`: `section` с `id` и `className`, внутренняя обёртка `mx-auto max-w-[72rem] px-4 py-16 md:px-8 md:py-24`. При `eyebrow` рендерит `Eyebrow`, при `title` рендерит `GradientTitle as="h2" variant="section"` с `mt-2` (8px после eyebrow); контент после заголовка с `mt-6` (24px). `scroll-margin-top` не задавать: прокрутку считает Header.

2. `Eyebrow`: `p` с классом `eyebrow` и утилитами `font-body text-xs font-bold uppercase tracking-[0.1em] leading-[1.4] text-horizon-200`.

3. `GradientTitle`: тег из `as`, классы `gradient-title gradient-title--${variant}` плюс `font-display`. В `primitives.css`: `.gradient-title` общее: `-webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; box-decoration-break: clone; -webkit-box-decoration-break: clone; text-wrap: balance; padding: 0.08em 0 0.12em; margin: -0.08em 0 -0.12em; font-weight: 900`. `.gradient-title--section`: `background-image: var(--gradient-title); font-size: var(--text-section)` (в токенах `clamp(1.875rem, 4vw, 3rem)`), `letter-spacing: -0.035em; line-height: 1.05`. `.gradient-title--hero`: `background-image` со стопами оригинала из `--ov-hero-title-gradient` (скопировать из orig-custom-styles.css дословно), `font-size: clamp(2.75rem, 8vw, 4.5rem); letter-spacing: -0.055em; line-height: 0.94`. Насыщенность 900 разрешена именно здесь (вордмарк, H1 hero, H2 секций) и запрещена в кнопках, меню и заголовках карточек.

4. `Button`: полиморфный компонент: `as="a"` рендерит `a` с пробросом атрибутов ссылки, иначе `button` с `type="button"` по умолчанию (переопределяемым). Классы `btn btn--${variant ?? "primary"}` плюс `className`. В `primitives.css`: `.btn` общее: `display: inline-flex; align-items: center; justify-content: center; gap: 8px; border-radius: var(--radius-pill); font-family: var(--font-body); font-size: var(--text-sm); font-weight: 700; line-height: 1.2; letter-spacing: 0.08em; text-transform: uppercase; text-decoration: none; text-align: center; cursor: pointer; transition: transform var(--dur-ui) var(--ease-ui), box-shadow var(--dur-ui) var(--ease-ui), border-color var(--dur-ui) var(--ease-ui), color var(--dur-ui) var(--ease-ui)`. `.btn--primary`: `background: var(--gradient-action); color: var(--color-paper); padding: 16px 40px; min-height: 52px; box-shadow: var(--shadow-button), inset 0 0 0 1px rgb(255 255 255 / .12)`; `:hover` → `transform: translateY(-2px)` и тень `0 16px 40px rgb(59 77 161 / .42), inset 0 0 0 1px rgb(255 255 255 / .16)`; `:active` → `transform: translateY(0)`. `.btn--ghost`: `background: transparent; border: 1.5px solid var(--glass-border); color: rgb(248 247 251 / .9); padding: 16px 32px; min-height: 48px`; `:hover` → `border-color: rgb(170 217 220 / .58); color: var(--color-horizon-200)`. `.btn:disabled, .btn[aria-disabled="true"]`: `opacity: .5; cursor: not-allowed; transform: none`. Луч по границе не делать: его добавит фаза 2 через `.btn--primary::before` в этом же `primitives.css` (зафиксировать в SUMMARY).

5. `GlassCard`: тег из `as` (по умолчанию `div`), классы `glass-card glass p-6 md:p-8` плюс `glass-card--interactive` при `interactive` и `className`. В `primitives.css`: `.glass-card { position: relative; overflow: hidden; isolation: isolate }`, `.glass-card::before { content: ""; position: absolute; inset: 0; border-radius: inherit; pointer-events: none; box-shadow: inset 1px 1px 0 rgb(255 255 255 / .06), inset -1px -1px 0 rgb(7 2 16 / .35); opacity: .58 }`, `.glass-card--interactive { transition: border-color var(--dur-ui) var(--ease-ui), box-shadow var(--dur-ui) var(--ease-ui) }`, `.glass-card--interactive:hover { border-color: rgb(184 192 230 / .34); box-shadow: 0 36px 80px rgb(18 12 52 / .7), 0 12px 28px rgb(18 12 52 / .48), inset 0 1px 0 rgb(248 247 251 / .1) }`. Фон, рамка `1px solid var(--glass-border)`, радиус 16px, тень и `backdrop-filter: blur(14px) saturate(112%)` приходят из утилиты `glass` в `global.css`, не дублировать.

6. `npm test`, `npm run build`, `npm run lint` зелёные.
  </action>
  <verify>
    <automated>npm test -- src/components/layout/primitives.test.tsx && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Существуют `src/components/layout/Section.tsx`, `Eyebrow.tsx`, `GradientTitle.tsx`, `Button.tsx`, `GlassCard.tsx`, `primitives.css`, `primitives.test.tsx`; каждый `.tsx` кроме теста содержит ровно одну строку `export function`
    - `primitives.css` содержит `.gradient-title--section`, `.gradient-title--hero`, `background-image: var(--gradient-title)`, `font-size: var(--text-section)`, `letter-spacing: -0.035em`, `line-height: 1.05`, `font-weight: 900`, `clamp(2.75rem, 8vw, 4.5rem)`, `.btn--primary`, `.btn--ghost`, `background: var(--gradient-action)`, `border-radius: var(--radius-pill)`, `padding: 16px 40px`, `min-height: 52px`, `var(--shadow-button)`, `translateY(-2px)`, `.glass-card::before`, `opacity: .58`
    - `grep -c 'font-weight: 900' src/components/layout/primitives.css` ≥ 1 и `grep -c '900' src/components/layout/Button.tsx` = 0
    - `grep -c 'backdrop-filter' src/components/layout/primitives.css` = 0 (стекло берётся из утилиты `glass`)
    - `grep -c 'type="button"\|type: "button"\|type ?? "button"' src/components/layout/Button.tsx` ≥ 1
    - `npm test -- src/components/layout/primitives.test.tsx` проходит: не меньше 8 тестов, код 0
    - `npm run build` код 0; `npm run lint` код 0
  </acceptance_criteria>
  <done>Пять примитивов рендерятся с классами-хуками и стилями из UI-SPEC, тесты зелёные, фазы 2–4 могут импортировать их из `src/components/layout/`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Восемь секций-заглушек с копирайтом, h1 и CTA в hero</name>
  <files>src/components/hero/Hero.tsx, src/components/map/MapSection.tsx, src/components/form/LightForm.tsx, src/components/about/About.tsx, src/components/involve/Involve.tsx, src/components/news/News.tsx, src/components/resources/Resources.tsx, src/components/quote/Quote.tsx, src/components/placeholders.test.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md (строки 490–513 Primary CTA и заглушки секций; 542–557 Accessibility: один h1 на страницу; 254–262 Layout: заглушка min-height 40vh)
    - src/data/copy.ts (ключи `copy.sections.*` и `copy.cta.lightYourLight`)
    - src/components/layout/Section.tsx, GradientTitle.tsx, Button.tsx (созданы в Task 1)
    - src/components/hero/Hero.tsx и остальные заглушки (текущее состояние: пустые section с id)
    - src/App.tsx (порядок секций; файл не редактировать)
  </read_first>
  <behavior>
    - Для каждой из восьми секций: рендер даёт `section#<id>`, внутри `p.eyebrow` с `copy.sections.<key>.eyebrow`, заголовок с `copy.sections.<key>.title` и абзац с `copy.sections.<key>.body`
    - `Hero`: заголовок имеет уровень 1 (`getByRole("heading", { level: 1, name: "Вместе, единым голосом" })`), остальные семь секций дают заголовок уровня 2 и ни одного h1
    - `Hero`: ссылка с именем «Зажечь свой свет» имеет `href="#light-form"` и классы `btn btn--primary`
    - Ни в одной заглушке нет текста «скоро», «coming soon», «TODO», «lorem»
  </behavior>
  <action>
Сначала `src/components/placeholders.test.tsx` (названия на русском) по `<behavior>`: импортировать восемь компонентов, для каждой пары (компонент, ключ copy) проверить id, eyebrow, title, body; отдельно h1 и CTA у `Hero`. Убедиться, что тест падает на заглушках, затем реализовать.

1. Семь секций (`MapSection` → `map`, `LightForm` → `lightForm` с id `light-form`, `About`, `Involve`, `News`, `Resources`, `Quote`): `Section` с `id`, `eyebrow={copy.sections.<key>.eyebrow}`, `title={copy.sections.<key>.title}`, `className="min-h-[40vh]"`, внутри один `p` с `copy.sections.<key>.body` и классами `max-w-[54ch] font-body text-base leading-[1.6] text-paper/72`. Никаких дополнительных элементов, фазы 2–4 заменят тело.

2. `Hero`: `Section id="hero" eyebrow={copy.sections.hero.eyebrow} className="min-h-[40vh]"` без `title`; внутри `GradientTitle as="h1" variant="section"` с `copy.sections.hero.title` (фаза 2 переключит на `variant="hero"`), абзац с `copy.sections.hero.body` (те же классы, что выше) и `Button as="a" variant="primary" href={copy.cta.lightYourLight.href}` с текстом `copy.cta.lightYourLight.label`, обёрнутый в `div` с `mt-8`. Это единственный h1 на странице.

3. Все тексты только из `copy.ts`; строковые литералы с русским текстом в этих восьми файлах запрещены.

4. `npm test` (включая `App.test.tsx`, `primitives.test.tsx`, `placeholders.test.tsx`), `npm run build`, `npm run lint` зелёные.
  </action>
  <verify>
    <automated>npm test && npm run build && npm run lint && grep -q 'copy.cta.lightYourLight' src/components/hero/Hero.tsx && grep -q 'as="h1"' src/components/hero/Hero.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Каждый из восьми файлов секций импортирует `Section` из `../layout/Section` и `copy` из `../../data/copy`; `grep -c 'min-h-\[40vh\]'` = 1 в каждом
    - `grep -c 'as="h1"' src/components/hero/Hero.tsx` = 1; `grep -c 'as="h1"' src/components/{map,form,about,involve,news,resources,quote}/*.tsx` даёт 0 в каждом файле
    - `grep -c 'Button' src/components/hero/Hero.tsx` ≥ 2 (импорт и использование); `grep -c 'copy.cta.lightYourLight.href' src/components/hero/Hero.tsx` = 1
    - `grep -rEc '[А-Яа-яЁё]' src/components/{hero,map,form,about,involve,news,resources,quote}/*.tsx` даёт 0 для каждого файла компонента (русский текст только в copy.ts; файлы тестов исключены)
    - `npm test` выводит 4 файла тестов passed (App, Wordmark, primitives, placeholders), код 0
    - `npm run build` код 0; `npm run lint` код 0
  </acceptance_criteria>
  <done>Все восемь якорей меню ведут к секциям с eyebrow, заголовком и строкой о содержимом; hero держит единственный h1 и CTA «Зажечь свой свет» → `#light-form`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy.ts → JSX | Статические строки из репозитория рендерятся React как текст, не как HTML |
| CTA `#light-form` | Внутренний якорь, без внешних переходов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering (XSS) | Section, Eyebrow, GradientTitle, заглушки | mitigate | Текст рендерится через JSX-интерполяцию `{copy...}`; `dangerouslySetInnerHTML` в этих файлах запрещён (проверка `grep -rc dangerouslySetInnerHTML src/components` = 0) |
| T-01-06 | Spoofing | Button as="a" | accept | В фазе 1 единственная ссылка ведёт на внутренний `#light-form`; внешние ссылки появятся в Footer (план 01-04) с `rel="noopener noreferrer"` |
| T-01-SC | Tampering | npm installs | mitigate | План не устанавливает пакетов; аудит зависимостей выполнен в 01-01 |
</threat_model>

<verification>
- `npm test` зелёный с четырьмя файлами тестов, `npm run build` и `npm run lint` без ошибок.
- `grep -rc dangerouslySetInnerHTML src/components` = 0.
- В собранном CSS (`dist/assets/*.css`) присутствуют `gradient-title--section`, `btn--primary`, `glass-card` (классы используются в исходниках, Tailwind их не режет, обычный CSS проходит как есть).
</verification>

<success_criteria>
- Пять примитивов с классами-хуками `.gradient-title*`, `.btn*`, `.glass-card*` и тестами.
- Восемь секций-заглушек с копирайтом из `copy.ts`, один h1, CTA на `#light-form`.
- Ни одного изменения в файлах, принадлежащих планам 01-01, 01-03, 01-04.
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-and-deploy/01-02-SUMMARY.md` when done. Зафиксировать: где живут стили Button (`primitives.css`, фаза 2 добавляет луч в `.btn--primary::before`), что hero использует `GradientTitle as="h1" variant="section"` до фазы 2, список экспортов примитивов с сигнатурами.
</output>
