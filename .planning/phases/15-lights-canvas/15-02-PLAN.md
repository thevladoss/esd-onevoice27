---
phase: 15-lights-canvas
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/components/map/LightsCanvas.tsx
  - src/components/map/LightsCanvas.test.tsx
  - src/components/map/EsdMap.tsx
  - src/components/map/EsdMap.test.tsx
  - src/components/map/map.css
  - src/App.seams.test.tsx
  - src/styles/motionPolicy.test.ts
autonomous: true
requirements: [LIGHT-01, LIGHT-03, LIGHT-04, LIGHT-05, LIGHT-06]
tags: [map, canvas, react, lights, tests]

must_haves:
  truths:
    - "Внутри .esd-map поверх SVG лежит <canvas class=\"map-lights-canvas\" data-anim=\"pulse\" aria-hidden=\"true\"> (absolute inset 0, pointer-events none, dpr ≤ 2); в SVG остались только страны: нет .map-lights, корзин, .light-core, .light-ring и <defs> с градиентами; в map.css нет @property --halo-k, правил .light-*, keyframes light-breathe и light-arrive"
    - "На кадре жеста и полёта handleFrame в EsdMap.tsx вызывает draw(transform) немедленно: огоньки рисуются в той же синхронной фазе, что и атрибут transform у .map-viewport; позиция transform.apply([x, y]), размер спрайта от масштаба не зависит"
    - "Цикл дыхания идёт на 30 fps (кадр раньше 33 мс пропускается), стоит вне экрана (IntersectionObserver threshold 0), в скрытой вкладке и при prefers-reduced-motion; под reduce один статичный кадр: ореол 9px alpha .22, ядра обычные, без колец; новый огонёк получает кольцо одну прокрутку, и на это время цикл идёт на полной частоте rAF"
    - "В jsdom EsdMap рендерит canvas с data-light-count=\"942\", data-people=\"694\", data-groups=\"248\", data-new=\"0\"; после addLight data-light-count=\"943\", data-new=\"1\"; rAF не вызывается, потому что getContext даёт null"
    - "EsdMap.test.tsx и App.seams.test.tsx читают атрибуты canvas и константы lightsCanvas.ts вместо .light-core/.light-bucket и текста map.css; motionPolicy.test.ts держит new-light в реестре, но не в списке обязательных; проверки light-form.css в App.seams.test.tsx не тронуты"
  artifacts:
    - path: "src/components/map/LightsCanvas.tsx"
      provides: "Компонент canvas огоньков: наблюдатели, планировщик, кольца, атрибуты, императивный draw(transform)"
      exports: ["LightsCanvas", "LightsCanvasHandle", "LightsCanvasProps"]
      contains: "data-anim=\"pulse\""
      min_lines: 150
    - path: "src/components/map/LightsCanvas.test.tsx"
      provides: "Тесты jsdom без контекста, первого кадра, порога 33 мс, reduce, draw(transform), кольца, visibilitychange и размонтирования"
      min_lines: 180
    - path: "src/components/map/EsdMap.tsx"
      provides: "SVG только со странами, <LightsCanvas> поверх, handleFrame → draw(transform)"
      contains: "<LightsCanvas"
    - path: "src/components/map/map.css"
      provides: "Блок .map-lights-canvas; без @property --halo-k, .light-*, light-breathe, light-arrive"
      contains: ".map-lights-canvas {"
    - path: "src/components/map/EsdMap.test.tsx"
      provides: "Тесты атрибутов canvas, отсутствия SVG-огоньков, кадра жеста с мок-контекстом, текста map.css без правил огоньков"
      min_lines: 400
  key_links:
    - from: "src/components/map/EsdMap.tsx"
      to: "src/components/map/LightsCanvas.tsx"
      via: "<LightsCanvas ref={lightsRef} points={points} transform={transform} width={width} height={height} /> сразу после </svg>"
      pattern: "<LightsCanvas"
    - from: "src/components/map/EsdMap.tsx handleFrame (onFrame из useMapZoom)"
      to: "LightsCanvasHandle.draw"
      via: "lightsRef.current?.draw(next) в том же вызове, что setAttribute(\"transform\")"
      pattern: "lightsRef\\.current\\?\\.draw\\("
    - from: "src/components/map/LightsCanvas.tsx"
      to: "src/components/map/lightsCanvas.ts drawFrame"
      via: "render(now) → drawFrame(ctx, sprites, { points, buckets, transform, now, rings, reduced, width, height })"
      pattern: "drawFrame\\("
    - from: "src/App.seams.test.tsx"
      to: "canvas.map-lights-canvas data-* и константы lightsCanvas.ts"
      via: "getAttribute(\"data-light-count\") и импорт BREATH_PERIOD_MS, LIGHT_BUCKETS, HALO_RADIUS_*, HALO_ALPHA_*"
      pattern: "data-light-count"
    - from: "src/styles/global.css (не править)"
      to: "canvas[data-anim=\"pulse\"]"
      via: "правила [data-anim=\"pulse\"] circle и .light-halo в блоке reduce остаются no-op; паузу под reduce делает сам компонент через matchMedia"
      pattern: "data-anim=\"pulse\""
---

<objective>
Перевести огоньки карты с SVG на canvas: компонент `LightsCanvas.tsx` поверх SVG с наблюдателями (размер и dpr, видимость, скрытая вкладка, reduce), планировщиком 30 fps, кольцами новых огоньков и data-атрибутами; встроить его в `EsdMap.tsx` так, чтобы кадр жеста рисовал огоньки немедленно; вычистить SVG-огоньки и их CSS; перевести `EsdMap.test.tsx`, `App.seams.test.tsx` и `motionPolicy.test.ts` на новые проверки.

Purpose: 1884 круга SVG и пять анимаций opacity держали карту и форму на 30 fps при CPU×4; canvas со спрайтами рисует тот же кадр без узлов DOM, а число узлов SVG на странице падает ниже 1300 (LIGHT-01, LIGHT-07).
Output: `LightsCanvas.tsx` + тест, обновлённые `EsdMap.tsx`, `EsdMap.test.tsx`, `map.css`, `App.seams.test.tsx`, `motionPolicy.test.ts`; зелёные `npm test`, `npx tsc -b`, `npm run lint`, `npm run build`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lights-canvas/15-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@src/components/map/lightsCanvas.ts
@src/components/map/EsdMap.tsx
@src/components/map/EsdMap.test.tsx
@src/components/map/useMapZoom.ts
@src/components/map/map.css
@src/App.seams.test.tsx
@src/styles/motionPolicy.test.ts
@src/lib/useReducedMotion.ts
@src/test/setup.ts
@src/test/intersection.ts
@src/components/hero/GlobeCanvas.tsx
@src/components/hero/GlobeCanvas.test.tsx
@src/components/layout/Reveal.tsx

<ownership>
Фаза 15 идёт параллельно с фазами 14 и 16 в отдельном worktree. Редактировать ТОЛЬКО файлы из `files_modified`. Запрещено трогать `src/styles/global.css` (правила `[data-anim="pulse"] circle`, `[data-anim="pulse"] .light-halo`, `[data-anim="new-light"]` в блоке reduce остаются no-op), `src/state/lights.tsx` (`isNew` уже есть), `src/test/setup.ts` (`getContext` там уже отдаёт null), `src/components/map/Counters.tsx`, `CountryChips.tsx`, `MapBand.tsx`, `MapSection.tsx`, `useMapZoom.ts` (`onFrame` уже есть, править не нужно), `src/components/form/*`, `src/components/hero/*` (только образец), `src/App.test.tsx` (словарь data-anim там уже содержит pulse), `package.json`. В `App.seams.test.tsx` менять только тест «скос карты и дыхание огоньков на месте (8)»: проверки `light-form.css` (тест про `.lf-section`) не трогать, их CSS правит фаза 16. В `motionPolicy.test.ts` менять только список обязательных значений. Никаких `@media (prefers-reduced-motion)` в `map.css`. Комментарии в коде на русском. Коммитов исполнитель не делает; команды относительные.
</ownership>

<interfaces>
<!-- Контракты. Извлечены из кода и плана 15-01, исследовать заново не нужно. -->

From src/components/map/lightsCanvas.ts (план 15-01):
```ts
export const BREATH_PERIOD_MS = 2600; export const LIGHT_BUCKETS = 5;
export const HALO_RADIUS_MIN = 7; export const HALO_RADIUS_MAX = 12;
export const HALO_ALPHA_MIN = 0.3; export const HALO_ALPHA_MAX = 0.6;
export const CORE_SPRITE_RADIUS = 3.1; export const MIN_FRAME_GAP_MS = 33; export const RING_MS = 900;
export const REDUCED_HALO_RADIUS = 9; export const REDUCED_HALO_ALPHA = 0.22; export const MAX_DPR = 2;
export const LIGHT_COLORS: Record<LightType, string>; // "rgb(158 67 154)", "rgb(84 164 172)"
export interface LightPoint { light: Light; x: number; y: number }
export interface PointTransform { apply(point: [number, number]): [number, number] }
export interface MotionFlags { inView: boolean; hidden: boolean; reducedMotion: boolean }
export interface Ring { point: LightPoint; startedAt: number }
export interface LightSprites { dpr: number; halo: Record<LightType, Sprite>; core: Record<LightType, Sprite> }
export interface FrameInput { points: readonly LightPoint[]; buckets: ReadonlyArray<readonly LightPoint[]>; transform: PointTransform; now: number; rings: readonly Ring[]; reduced: boolean; width: number; height: number }
export function splitBuckets<T>(points: readonly T[]): T[][];
export function summarize(points: readonly LightPoint[]): { count: number; people: number; groups: number; fresh: number };
export function ringState(elapsedMs: number): { radius: number; alpha: number; done: boolean };
export function frameDue(elapsedMs: number): boolean;
export function shouldAnimate(flags: MotionFlags): boolean;
export function clampDpr(ratio: number | undefined): number;
export function makeSprites(dpr: number, createCanvas?: () => HTMLCanvasElement): LightSprites | null;
export function drawFrame(ctx: CanvasRenderingContext2D, sprites: LightSprites, input: FrameInput): { ringsActive: boolean };
```

From src/components/map/EsdMap.tsx (текущее, строки указаны по файлу до правок):
```ts
// 36–42: export const LIGHT_CORE_RADIUS = 2.2; LIGHT_HALO_RADIUS = 6; LIGHT_BUCKETS = 5  → удалить (импортирует только EsdMap.test.tsx)
// 60–62: containerRef (div.esd-map), svgRef, viewportRef (g.map-viewport)
// 91–93: resolved = size ?? measured; width, height  → те же числа идут в viewBox и в LightsCanvas
// 124–133: points = useMemo(() => placed: { light, x, y }[] по projection([lon, lat]) с отсевом не-чисел, [projection, lights])
// 137–146: buckets = useMemo(...)  → удалить (splitBuckets живёт в модуле, корзины считает LightsCanvas)
// 183–190: handleFrame = useCallback((next) => { viewportRef.current.setAttribute("transform", next.toString()); node.style.setProperty("--zoom-k", …) }, [])
// 192–198: const { transform, dragging, zoomTo } = useMapZoom(svgRef, { width, height, enabled, onUserZoom: handleUserZoom, onFrame: handleFrame })
// 254–256: coreRadius/haloRadius = LIGHT_*/transform.k  → удалить
// 288–302: <defs> с radialGradient  → удалить
// 303–316: <g ref={viewportRef} className="map-viewport" transform={transform.toString()} style={{ "--zoom-k", "--light-core-r", "--light-halo-r" }}>  → style убрать целиком
// 338–382: <g className="map-lights" aria-hidden="true"> корзины + .light-cores  → удалить
// 386–388: <p id={descId} className="sr-only">  → остаётся
```

From src/components/map/useMapZoom.ts (не править): `onFrame?: (transform: ZoomTransform) => void` вызывается в обработчике `zoom` d3 на каждом кадре жеста и полёта, синхронно, до `setTransform` в конце жеста; состояние `transform` обновляется в конце жеста и в конце полёта.

From src/lib/useReducedMotion.ts: `REDUCED_MOTION_QUERY = "(prefers-reduced-motion: reduce)"`, `prefersReducedMotion(): boolean` (синхронно, для rAF-циклов), `usePrefersReducedMotion()`.

From src/test/setup.ts (не править): `IntersectionObserver` и `ResizeObserver` замоканы; `window.matchMedia` даёт `matches: false`; `HTMLCanvasElement.prototype.getContext` возвращает null.

From src/test/intersection.ts: `enterViewport()` вызывает колбэки всех наблюдателей с `isIntersecting: true` для наблюдаемых узлов.

From src/components/layout/Reveal.tsx (образец ref как проп в React 19): `props: { …; ref?: Ref<…> }`, без forwardRef.

From src/components/hero/GlobeCanvas.tsx (образец, не править): mount-эффект с `getContext("2d")`, ранний выход при null; `ResizeObserver` на canvas; `IntersectionObserver` с `{ threshold: 0 }`; `visibilitychange`; `matchMedia(REDUCED_MOTION_QUERY).addEventListener("change")`; `sync()` запускает или снимает rAF; `lastAt = null` при паузе.

From src/App.seams.test.tsx строки 144–176 (тест карты, переписать): мок `HTMLDivElement.prototype.getBoundingClientRect` для `.esd-map` → `DOMRect(0, 0, 1200, 700)`, затем `renderApp()`; хелперы `block(css, head)` и `flat(css)` в файле уже есть.

From src/styles/motionPolicy.test.ts строки 104–109: тест «проставлен на всех декоративных слоях оболочки, hero и карты» с массивом `["stars", "globe", "beam", "pulse", "new-light", "wave", "halo"]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Компонент LightsCanvas.tsx (наблюдатели, планировщик, кольца, атрибуты, draw) и LightsCanvas.test.tsx</name>
  <files>src/components/map/LightsCanvas.tsx, src/components/map/LightsCanvas.test.tsx</files>
  <read_first>
    - .planning/phases/15-lights-canvas/15-01-SUMMARY.md и src/components/map/lightsCanvas.ts (точные сигнатуры)
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, LIGHT-01, LIGHT-03 (последний абзац: состояние transform из useMapZoom остаётся источником при рендере React), LIGHT-04, LIGHT-05, LIGHT-06
    - src/components/hero/GlobeCanvas.tsx (структура mount-эффекта и sync; не копировать вращение)
    - src/components/hero/GlobeCanvas.test.tsx (мок контекста, `vi.spyOn(HTMLCanvasElement.prototype, "getContext")`, `mockReducedMotion`, перехват rAF)
    - src/test/intersection.ts (enterViewport) и src/test/setup.ts (что уже замокано)
    - src/components/layout/Reveal.tsx строки 45–60 (ref как проп)
  </read_first>
  <behavior>
    - В jsdom (getContext → null): render(<LightsCanvas points={942 точки} transform={zoomIdentity} width={1200} height={700} />) даёт canvas.map-lights-canvas с data-anim="pulse", aria-hidden="true", data-light-count="942", data-people="694", data-groups="248", data-new="0"; requestAnimationFrame не вызывается; rerender с 943 точками (одна isNew person) даёт data-light-count="943", data-people="695", data-new="1"; unmount не бросает
    - С мок-контекстом: сразу после render canvas.width = 1200, canvas.height = 700, ctx.setTransform(1, 0, 0, 1, 0, 0) (jsdom devicePixelRatio 1), drawImage вызван не меньше 1884 раз (первый кадр рисуется без ожидания), rAF не вызван; после act(() => enterViewport()) rAF вызван один раз
    - Порог 33 мс: перехваченные колбэки rAF; cb(1000) рисует (clearRect +1), cb(1016) не рисует, cb(1034) рисует
    - Под prefers-reduced-motion: первый кадр рисует все ореолы шириной 18 (REDUCED_HALO_RADIUS·2) при globalAlpha 0.22 и ядра шириной 6.2 при alpha 1; ctx.arc не вызывается; после enterViewport rAF не вызван
    - ref.current.draw(new ZoomTransform(2, 10, 20)) синхронно рисует кадр: для первой точки ядро в (2·x + 10 − 3.1, 2·y + 20 − 3.1) шириной 6.2; ширина ореола в пределах 14…24 при любом k
    - Кольцо: performance.now замокан на 5000; rerender с новой isNew-точкой даёт arc(x, y, 6, 0, 2π) при globalAlpha 0.5 и strokeStyle "rgba(158, 67, 154, 1)"; после enterViewport колбэк rAF с now 5010 рисует кадр (полная частота, хотя прошло меньше 33 мс); при now 5950 arc уже не вызывается; повторный кадр через 10 мс снова пропускается (порог вернулся)
    - visibilitychange с document.hidden = true снимает rAF (cancelAnimationFrame с id последнего кадра); возврат hidden = false запускает снова; unmount снимает rAF и вызывает disconnect у наблюдателей
  </behavior>
  <action>
    Реализует LIGHT-01 (canvas в разметке), LIGHT-03 (императивный draw), LIGHT-04 (30 fps, паузы, reduce), LIGHT-05 (кольцо, полная частота), LIGHT-06 (атрибуты, jsdom). Сначала тесты из `<behavior>` в `src/components/map/LightsCanvas.test.tsx`, затем компонент.

    Тестовая обвязка: `mockContext()` продублировать из `GlobeCanvas.test.tsx` (не импортировать из hero: фаза 14 удаляет тот файл), добавив `drawImage`, `fillRect`, `scale`, `createRadialGradient`; `drawImage`, `arc` и `stroke` реализовать через `mockImplementation`, которая пушит снимок `{ alpha: ctx.globalAlpha, strokeStyle: ctx.strokeStyle, args }` в массив (globalAlpha меняется между вызовами). `vi.spyOn(HTMLCanvasElement.prototype, "getContext").mockReturnValue(ctx)`: один и тот же мок получат и спрайты, и основной canvas, для проверок этого достаточно (спрайты рисуют `fillRect`/`arc`/`fill`, кадр рисует `drawImage`). `generateLights()` один раз на файл; точки строить сеткой без проекции (`x = 10 + (i % 40) * 25`, `y = 10 + Math.floor(i / 40) * 25`, все внутри 1200×700). Новую точку добавлять как `{ light: { id: "n942", type: "person", countryId: 643, lon: 0, lat: 0, isNew: true }, x: 300, y: 200 }`. `mockReducedMotion(true)` как в `GlobeCanvas.test.tsx`. Для `document.hidden` использовать `Object.defineProperty(document, "hidden", { value: true, configurable: true })` и восстанавливать в `afterEach` вместе с `vi.restoreAllMocks()`. Все вызовы `enterViewport()`, `draw()` и события оборачивать в `act`.

    Компонент `src/components/map/LightsCanvas.tsx`:

    1. Экспорты: `interface LightsCanvasHandle { draw(transform: ZoomTransform): void }`, `interface LightsCanvasProps { points: readonly LightPoint[]; transform: ZoomTransform; width: number; height: number; ref?: Ref<LightsCanvasHandle> }`, `function LightsCanvas(props)`. Ничего кроме компонента и типов не экспортировать (правило react-refresh). `ZoomTransform` импортировать как тип из `d3-zoom`; из модуля импортировать `splitBuckets`, `summarize`, `drawFrame`, `makeSprites`, `clampDpr`, `frameDue`, `ringState`, `shouldAnimate` и типы; из `../../lib/useReducedMotion` `REDUCED_MOTION_QUERY` и `prefersReducedMotion`.

    2. Рендер: `buckets = useMemo(() => splitBuckets(points), [points])`, `summary = useMemo(() => summarize(points), [points])`; разметка `<canvas ref={canvasRef} className="map-lights-canvas" data-anim="pulse" aria-hidden="true" data-light-count={summary.count} data-people={summary.people} data-groups={summary.groups} data-new={summary.fresh} />`. Атрибуты объявлены декларативно, поэтому обновляются при каждом изменении `points` и без 2d-контекста (LIGHT-06). Литерал `data-anim="pulse"` обязателен: закрытый реестр `motionPolicy.test.ts`. Комментарий над canvas: глобальный блок reduce гасит только CSS-анимации, а цикл rAF компонент останавливает сам по `matchMedia`.

    3. Сцена и движок. `sceneRef = useRef<Scene>({ points, buckets, transform, width, height })` (тип `Scene` внутренний); `engineRef = useRef<Engine | null>(null)`. `useLayoutEffect(() => { sceneRef.current = { points, buckets, transform, width, height }; engineRef.current?.onScene(); }, [points, buckets, transform, width, height])`: layout-эффект, а не запись в ref во время рендера (правило `react-hooks/refs` в eslint-plugin-react-hooks 7 запрещает трогать ref в рендере). Mount-эффект `useEffect(() => { … }, [])`: взять canvas из `canvasRef`, `ctx = canvas.getContext("2d")`, при null выйти без подписок и без rAF (jsdom, LIGHT-06); иначе `engineRef.current = createEngine(canvas, ctx, sceneRef)`, в cleanup `engine.dispose()` и обнуление ref. `useImperativeHandle(ref, () => ({ draw: (next) => engineRef.current?.draw(next) }), [])`.

    4. `createEngine(canvas, ctx, sceneRef)` (функция уровня модуля, не экспортируется) держит в замыкании: `sprites: LightSprites | null`, `spritesDpr = 0`, `bitmapWidth = 0`, `bitmapHeight = 0`; флаги `inView = typeof IntersectionObserver === "undefined"` (без наблюдателя считаем видимым; с наблюдателем ждём его первого отчёта, браузер шлёт его сразу после observe), `hidden = document.hidden`, `reduced = prefersReducedMotion()`; `frameId: number | null = null`, `lastAt: number | null = null`, `ringsActive = false`; `currentTransform = sceneRef.current.transform`, `lastPropTransform = currentTransform`; `seen = new Set<string>()`, `rings: { id: string; startedAt: number }[] = []`, `pointById = new Map<string, LightPoint>()`. Методы:
       - `resize(): boolean`: `dpr = clampDpr(window.devicePixelRatio)`, `{ width, height }` из сцены; при ширине или высоте < 1 вернуть false; `bw = Math.round(width * dpr)`, `bh = Math.round(height * dpr)`; если `bw`, `bh` и `dpr` не изменились, вернуть false; иначе `canvas.width = bw`, `canvas.height = bh`, `ctx.setTransform(dpr, 0, 0, dpr, 0, 0)` (смена размера сбрасывает состояние контекста, поэтому трансформ ставится после), при смене dpr `sprites = makeSprites(dpr)` и `spritesDpr = dpr` (LIGHT-02: спрайты только при смене размера или dpr), запомнить размеры, вернуть true. CSS-размер canvas задаёт `map.css` (`inset: 0`), логический размер равен `viewBox` SVG, поэтому координаты `transform.apply` совпадают с координатами стран.
       - `render(now)`: при отсутствии `sprites` или размера выйти; собрать `resolved: Ring[]` из `rings` через `pointById` (кольца, чья точка пропала, отбросить); `{ ringsActive: active } = drawFrame(ctx, sprites, { points, buckets, transform: currentTransform, now, rings: resolved, reduced, width, height })`; `ringsActive = active`; `lastAt = now`; удалить из `rings` записи с `ringState(now - startedAt).done`.
       - `tick(now)`: `frameId = null`; если `!shouldAnimate({ inView, hidden, reducedMotion: reduced })` выйти; если `!ringsActive && lastAt !== null && !frameDue(now - lastAt)`, только перепланировать (`frameId = requestAnimationFrame(tick)`) и выйти (LIGHT-04: 30 fps; LIGHT-05: пока идёт кольцо, полная частота); иначе `render(now)` и перепланировать.
       - `sync()`: если `shouldAnimate(...)`, при `frameId === null` запустить `tick` через rAF; иначе снять rAF (`cancelAnimationFrame`, `frameId = null`), `lastAt = null` (после паузы первый кадр рисуется сразу), и если `reduced`, нарисовать один статичный кадр `render(performance.now())` (drawFrame сам даёт ореол 9px alpha .22 и без колец).
       - `onScene()`: если `sceneRef.current.transform !== lastPropTransform`, то `currentTransform = lastPropTransform = sceneRef.current.transform` (новое состояние React после жеста или полёта; та же идентичность значит промежуточный рендер посреди жеста, и кадр жеста не откатывается); `resize()`; пересобрать `pointById`; для каждой точки с `light.isNew === true` и id не в `seen`: `seen.add(id)`, `rings.push({ id, startedAt: performance.now() })` (одна прокрутка от момента появления в `lights`, LIGHT-05); `render(performance.now())` (новый огонёк, новый размер и новое состояние видны сразу, под reduce это обновлённый статичный кадр); `sync()`.
       - `draw(next)`: `currentTransform = next`; `render(performance.now())` без проверки порога (LIGHT-03: кадр жеста немедленно; `lastAt = now` внутри render не даёт циклу нарисовать второй кадр в ту же миллисекунду).
       - Подписки при создании: `ResizeObserver` на canvas (при наличии) → `if (resize()) render(performance.now())`; `IntersectionObserver((entries) => { inView = entries[entries.length - 1].isIntersecting; sync(); }, { threshold: 0 })` на canvas (canvas занимает тот же прямоугольник, что контейнер `.esd-map`); `document.addEventListener("visibilitychange", () => { hidden = document.hidden; sync(); })`; `matchMedia(REDUCED_MOTION_QUERY)` `change` → `reduced = event.matches; sync();` (`addEventListener?.` через optional chaining: мок jsdom может его не иметь); смена dpr при переносе окна между экранами (дискреция): слушатель `change` на `matchMedia(`(resolution: ${dpr}dppx)`)`, который перевешивается на новый dpr и вызывает `resize()` + `render()`. После подписок: `resize()`, `render(performance.now())` (первый кадр без ожидания, как у SVG раньше), `sync()`.
       - `dispose()`: снять rAF, `disconnect` у обоих наблюдателей, снять слушатели `visibilitychange`, reduce и dpr.

    5. Комментарии на русском у ключевых мест: почему `inView` стартует с false при наличии наблюдателя, почему кадр жеста идёт мимо порога, почему промежуточный рендер не откатывает трансформ, почему атрибуты декларативные.

    Запрещено: чтение `ref.current` в теле рендера; `setState` внутри движка; `data-anim` с любым значением, кроме `pulse`; хуки `motion/react`.
  </action>
  <acceptance_criteria>
    - `src/components/map/LightsCanvas.tsx` содержит `export interface LightsCanvasHandle`, `export function LightsCanvas`, `className="map-lights-canvas"`, `data-anim="pulse"`, `aria-hidden="true"`, `data-light-count=`, `data-people=`, `data-groups=`, `data-new=`, `useImperativeHandle(`, `getContext("2d")`, `{ threshold: 0 }`, `"visibilitychange"`, `REDUCED_MOTION_QUERY`, `frameDue(`, `drawFrame(`, `makeSprites(`, `clampDpr(`, `setTransform(dpr, 0, 0, dpr, 0, 0)`
    - `grep -c 'data-anim="' src/components/map/LightsCanvas.tsx` равен 1
    - `grep -c "export " src/components/map/LightsCanvas.tsx` равен 3 (два интерфейса и компонент)
    - `src/components/map/LightsCanvas.test.tsx` содержит `enterViewport`, `requestAnimationFrame`, `cancelAnimationFrame`, `"visibilitychange"`, `data-light-count`, `ZoomTransform(2, 10, 20)`, проверку ширины 18 под reduce и `toHaveBeenCalledTimes`
    - `npx vitest run src/components/map/LightsCanvas.test.tsx src/components/map/lightsCanvas.test.ts` код 0, без `console.error` и предупреждений `act` в выводе
    - `npx tsc -b` код 0; `npm run lint` код 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map/LightsCanvas.test.tsx src/components/map/lightsCanvas.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <done>Компонент ставит атрибуты без контекста и не трогает rAF; с контекстом рисует первый кадр сразу, дышит на 30 fps после появления в экране, стоит под reduce и в скрытой вкладке, рисует кадр жеста по `draw(transform)` и кольцо нового огонька одну прокрутку; тесты, tsc и lint зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: EsdMap.tsx без SVG-огоньков и с LightsCanvas, map.css без правил огоньков, перевод EsdMap.test.tsx</name>
  <files>src/components/map/EsdMap.tsx, src/components/map/map.css, src/components/map/EsdMap.test.tsx</files>
  <read_first>
    - src/components/map/EsdMap.tsx целиком (строки из `<interfaces>` показывают, что удалять)
    - src/components/map/map.css строки 1–143 (блок `@property` и блок огоньков удаляются), 330–341 (`.map-section` с `--light-person`/`--light-group`)
    - src/components/map/EsdMap.test.tsx целиком (какие тесты остаются, какие меняются)
    - src/lib/geo.ts: сигнатура `makeProjection(width, height)` (нужна тесту кадра жеста)
    - src/components/map/LightsCanvas.tsx из Task 1
  </read_first>
  <action>
    Реализует LIGHT-01 (SVG только со странами, canvas в `.esd-map`, CSS), LIGHT-03 (`handleFrame` → `draw(transform)`), LIGHT-06 (перевод `EsdMap.test.tsx`).

    1. `src/components/map/EsdMap.tsx`:
       - Удалить `LIGHT_CORE_RADIUS`, `LIGHT_HALO_RADIUS`, `LIGHT_BUCKETS` с комментариями (их импортировал только тест), `buckets`, `coreRadius`/`haloRadius` с комментарием, импорт `CSSProperties`. Импортировать `type { LightPoint }` из `./lightsCanvas`, `LightsCanvas` и `type { LightsCanvasHandle }` из `./LightsCanvas`. Массив `placed` в `points` типизировать как `LightPoint[]`.
       - Добавить `lightsRef = useRef<LightsCanvasHandle>(null)`.
       - `handleFrame`: оставить `setAttribute("transform", next.toString())` на вьюпорте, убрать `--zoom-k`, добавить `lightsRef.current?.draw(next)` в том же вызове; комментарий: огоньки рисуются в том же кадре жеста и полёта, что и страны, поэтому не отстают (LIGHT-03); состояние `transform` из `useMapZoom` остаётся источником для рендера React. Если `viewportRef.current` null, всё равно вызвать `draw` (canvas не зависит от вьюпорта).
       - В `<svg>` удалить `<defs>` с градиентами и комментарий над ними; у `<g ref={viewportRef} className="map-viewport" transform={…}>` убрать проп `style` и комментарий про переменные; удалить `<g className="map-lights" …>` целиком с комментарием. Внутри вьюпорта остаётся только `<g className="map-countries">`.
       - Сразу после `</svg>` (внутри фрагмента, до `<p id={descId} className="sr-only">`) добавить `<LightsCanvas ref={lightsRef} points={points} transform={transform} width={width} height={height} />` с комментарием: canvas лежит поверх SVG по `map.css`, размер логической сетки равен `viewBox`, поэтому `transform.apply` даёт те же экранные координаты, что и трансформ группы стран.
       - Ветка `hasError` и пустое состояние `.map-empty` не меняются.

    2. `src/components/map/map.css`:
       - Удалить строки 1–11 (`@property --halo-k` и комментарий).
       - Удалить блок огоньков со строки 42 (комментарий «Огоньки: свечение…») по строку 143 включительно (`.light--person`, `.light--group`, `.light-halo-def--*`, `.light-core`, `.light-halo`, `.light-ring`, `.light-bucket` с четырьмя задержками, `@keyframes light-breathe`, `.light.is-new .light-ring`, `@keyframes light-arrive`).
       - После `.esd-map__svg` добавить блок с комментарием на русском (огоньки рисует canvas поверх SVG: 942 огонька без узлов DOM, дыхание и кольцо считает `lightsCanvas.ts`; сам слой кликов не ловит, жесты уходят в SVG под ним): `.map-lights-canvas { position: absolute; inset: 0; display: block; width: 100%; height: 100%; pointer-events: none; }`.
       - В `.map-section` удалить `--light-person: rgb(158 67 154);`, `--light-group: rgb(84 164 172);` и комментарий «Семантические алиасы…» (цвета живут в `LIGHT_COLORS` модуля; счётчики уже на литералах). Акценты счётчиков `rgb(210 142 190)` и `rgb(123 194 199)` не трогать.
       - Никакого `prefers-reduced-motion` в файле. Проверить, что `grep -n "light-" src/components/map/map.css` показывает только `.map-lights-canvas` и, возможно, комментарий; строк `.light-`, `light-breathe`, `light-arrive`, `--halo-k`, `--zoom-k` нет.

    3. `src/components/map/EsdMap.test.tsx`:
       - Импорты: убрать `LIGHT_BUCKETS, LIGHT_CORE_RADIUS, LIGHT_HALO_RADIUS` (оставить `EsdMap`); добавить `import { act } from "@testing-library/react"` (или из `react`), `import { makeProjection } from "../../lib/geo"`, `import { CORE_SPRITE_RADIUS, HALO_RADIUS_MAX, HALO_RADIUS_MIN } from "./lightsCanvas"`. `readFileSync`/`resolve` остаются для блока CSS.
       - Хелперы: удалить `haloPerBucket`; добавить `mockContext()` (та же обвязка, что в `LightsCanvas.test.tsx`, продублировать), `contexts = new Map<HTMLCanvasElement, ReturnType<typeof mockContext>>()` и `mockCanvasContexts()`, который делает `vi.spyOn(HTMLCanvasElement.prototype, "getContext").mockImplementation(function (this: HTMLCanvasElement) { const ctx = contexts.get(this) ?? mockContext(); contexts.set(this, ctx); return ctx; })` (каждому canvas свой контекст: два рендера карты в одном документе); `lightXs(container)` переписать: взять `contexts.get(container.querySelector("canvas.map-lights-canvas"))`, из снимков `drawImage` отобрать вызовы ядер (ширина `args[3]` равна `CORE_SPRITE_RADIUS * 2`) последнего кадра и вернуть `args[1] + args[3] / 2`; `canvasOf(container)` возвращает `container.querySelector("canvas.map-lights-canvas")`. `afterEach(() => { vi.restoreAllMocks(); contexts.clear(); })`.
       - Тест «рисует 694 огонька людей и 248 групповых с гало» заменить на «рисует огоньки на canvas поверх SVG и считает их в атрибутах»: `canvas = canvasOf(container)` не null, `canvas.parentElement` имеет класс `esd-map`, `canvas.previousElementSibling?.tagName.toLowerCase()` равен `svg`, атрибуты `data-anim="pulse"`, `aria-hidden="true"`, `data-light-count="942"`, `data-people="694"`, `data-groups="248"`, `data-new="0"`; `count("circle")` 0, `count(".map-lights")` 0, `count("defs")` 0, `count("radialGradient")` 0, `count(".light-core")` 0, `count(".light-bucket")` 0.
       - Тест «раскладывает ореолы по пяти фазовым корзинам» заменить на «после зажигания нового огонька обновляет атрибуты и не трогает rAF»: `raf = vi.spyOn(window, "requestAnimationFrame")`; рендер `AddLightHarness` в `LightsProvider`; клик «зажечь»; атрибуты `data-light-count="943"`, `data-people="695"`, `data-groups="248"`, `data-new="1"`; `raf` не вызван (jsdom без контекста); `count('[data-anim="new-light"]')` 0 (кольцо рисует canvas, узла в DOM нет).
       - Тест «держит data-anim только на корзинах и кольце нового огонька» заменить на «держит data-anim только на canvas огоньков»: `count('[data-anim="pulse"]')` 1 и это `canvas.map-lights-canvas`; внутри `.esd-map` ровно один элемент с `[data-anim]`; после клика «зажечь» по-прежнему 1.
       - Тест «красит ореол радиальным градиентом типа…» удалить (спрайты покрыты `lightsCanvas.test.ts`).
       - Тест «обходится без filter на огоньках» переименовать в «обходится без filter и без кругов SVG»: `[filter]` 0, `svg circle` 0, `svg path.country` 177.
       - В тесте «зовёт зажечь первый свет, когда огоньков нет» заменить `expect(count(".light-core")).toBe(0)` на проверку `canvasOf(container)` с `data-light-count="0"` (рендер через `const { container } = render(...)`).
       - Тест «делит радиусы огоньков на масштаб» заменить на «рисует огоньки в кадре жеста по новому трансформу, размер спрайта от масштаба не зависит»: `mockCanvasContexts()`; `vi.spyOn(window, "requestAnimationFrame").mockImplementation(() => 1)`; рендер `<EsdMap lights={lights} size={SIZE} />`; `ctx = contexts.get(canvasOf(container))`; `projection = makeProjection(SIZE.width, SIZE.height)`; `[x0, y0] = projection([lights[0].lon, lights[0].lat])`; сбросить снимки drawImage; `fireEvent.wheel(svg, { deltaY: -240, ctrlKey: true, clientX: 600, clientY: 350 })`; из атрибута `transform` вьюпорта распарсить `translate(tx,ty) scale(k)` (regex `/translate\(([-\d.e]+),([-\d.e]+)\) scale\(([-\d.e]+)\)/`), `k` равен 8; среди снимков после сброса есть вызов ядра с `args[1]` ≈ `k * x0 + tx − CORE_SPRITE_RADIUS` и `args[2]` ≈ `k * y0 + ty − CORE_SPRITE_RADIUS` (toBeCloseTo, 3 знака) и `args[3]` = `CORE_SPRITE_RADIUS * 2`; все ореолы имеют ширину между `HALO_RADIUS_MIN * 2` и `HALO_RADIUS_MAX * 2`; число снимков после сброса > 0 (кадр нарисован синхронно внутри события колеса, до любого rAF).
       - Тест «на кадре жеста двигает только вьюпорт, а огоньки не пересобирает» упростить: после колеса `viewport.getAttribute("transform")` содержит `scale(8)`, атрибута `style` у вьюпорта нет (`getAttribute("style")` null), `canvasOf(container)` тот же узел, что до жеста, `data-light-count` не изменился.
       - Тест «считает проекцию по фактической ширине контейнера»: включить `mockCanvasContexts()` и rAF-заглушку перед рендерами, `lightXs` по снимкам ядер; ожидания те же (span равны, центр сдвинут на 360).
       - Остальные тесты (роль изображения, 177 стран, описание, ошибки, подсветка страны, блок «зум карты» кроме двух переписанных, `constrainTransform`, `movedAway`, `zoomEventFilter`) не менять.
       - Блок `describe("map.css: дыхание огоньков")` заменить на `describe("map.css: canvas огоньков")`: а) блок `.map-lights-canvas {` содержит `position: absolute;`, `inset: 0;`, `pointer-events: none;`; б) текст не содержит `@property --halo-k`, `light-breathe`, `light-arrive`, `light-pulse`, `.light-core`, `.light-halo`, `.light-bucket`, `.light-ring`, `--light-person`, `--zoom-k`; в) содержит `--counter-accent: rgb(210 142 190);` и `--counter-accent: rgb(123 194 199);`; г) не содержит `prefers-reduced-motion`. Блок «map.css: полотно карты» оставить.
       - Прогнать `npx vitest run src/components/map`, `npx tsc -b`, `npm run lint`.
  </action>
  <acceptance_criteria>
    - `grep -n "LIGHT_CORE_RADIUS\|LIGHT_HALO_RADIUS\|LIGHT_BUCKETS\|radialGradient\|map-lights\"\|light-bucket\|light-core\|light-ring\|--zoom-k\|CSSProperties\|<defs>" src/components/map/EsdMap.tsx` пуст
    - `src/components/map/EsdMap.tsx` содержит `<LightsCanvas`, `ref={lightsRef}`, `lightsRef.current?.draw(next)`, `import { LightsCanvas`, `LightPoint`
    - `grep -c 'data-anim="' src/components/map/EsdMap.tsx` равен 0
    - `grep -n "@property\|\.light-\|light-breathe\|light-arrive\|--halo-k\|--zoom-k\|--light-person\|--light-group\|prefers-reduced-motion" src/components/map/map.css` пуст; `map.css` содержит блок `.map-lights-canvas {` с `position: absolute;`, `inset: 0;`, `pointer-events: none;`
    - `src/components/map/EsdMap.test.tsx` не содержит `haloPerBucket`, `.light-bucket"` и `.light-core"` в позитивных проверках (допустимы только `toBe(0)`), содержит `data-light-count`, `mockCanvasContexts`, `makeProjection`, `scale\\(` (парсинг трансформа), `CORE_SPRITE_RADIUS`, `map.css: canvas огоньков`
    - `npx vitest run src/components/map` код 0, все тесты passed, без `console.error` и предупреждений `act`
    - `npx tsc -b` код 0; `npm run lint` код 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/map && npx tsc -b && npm run lint</automated>
  </verify>
  <done>SVG рендерит только страны, canvas огоньков лежит поверх и получает кадр жеста синхронно; CSS без правил огоньков; тесты карты переведены на атрибуты и снимки контекста; всё зелёное.</done>
</task>

<task type="auto">
  <name>Task 3: App.seams.test.tsx (блок карты), motionPolicy.test.ts (new-light вне обязательных), полный гейт</name>
  <files>src/App.seams.test.tsx, src/styles/motionPolicy.test.ts</files>
  <read_first>
    - src/App.seams.test.tsx строки 144–176 (тест карты) и 56–78 (тест формы: не трогать)
    - src/styles/motionPolicy.test.ts строки 80–110 (реестр и список обязательных)
    - src/styles/global.css строки 342–374 (селекторы `[data-anim="pulse"] .light-halo` и `[data-anim="new-light"]` остаются; из-за них `new-light` по-прежнему попадает в `used`, но обязательным его держать нельзя: узла с этим атрибутом в DOM больше нет)
  </read_first>
  <action>
    Реализует LIGHT-05 (список обязательных `data-anim`) и LIGHT-06 (перевод `App.seams.test.tsx`).

    1. `src/App.seams.test.tsx`, только тест «скос карты и дыхание огоньков на месте (8)»: переименовать в «скос карты и canvas огоньков на месте (8 + 15)». Мок `getBoundingClientRect` для `.esd-map` (1200×700) и `renderApp()` оставить как есть. Заменить проверки корзин и `.light.pulse` на: `canvas = document.querySelector(".esd-map > canvas.map-lights-canvas")` не null, `data-anim="pulse"`, `aria-hidden="true"`, `data-light-count="942"`, `data-people="694"`, `data-groups="248"`, `data-new="0"`; `document.querySelectorAll(".map-lights, .light-bucket, .light-core, .light-ring, .esd-map defs, .esd-map circle")` длиной 0. Проверки `block(MAP_CSS, ".map-shell {")` и `.map-band::before` оставить; `expect(MAP_CSS).not.toContain("light-pulse")` оставить; `expect(MAP_CSS).toContain("@keyframes light-breathe")` заменить на `expect(MAP_CSS).not.toContain("light-breathe")`, `not.toContain("@property --halo-k")`, `not.toContain("light-arrive")` и `expect(block(MAP_CSS, ".map-lights-canvas {")).toContain("pointer-events: none")`. Добавить импорт `{ BREATH_PERIOD_MS, HALO_ALPHA_MAX, HALO_ALPHA_MIN, HALO_RADIUS_MAX, HALO_RADIUS_MIN, LIGHT_BUCKETS } from "./components/map/lightsCanvas"` и проверки `BREATH_PERIOD_MS === 2600`, `LIGHT_BUCKETS === 5`, `[HALO_RADIUS_MIN, HALO_RADIUS_MAX]` равно `[7, 12]`, `[HALO_ALPHA_MIN, HALO_ALPHA_MAX]` равно `[0.3, 0.6]` с комментарием: дыхание живёт в модуле canvas, текст CSS его больше не описывает. Тест «форма стоит внутри ленты карты на прозрачном фоне (8 + 9)» и строки про `LIGHT_FORM_CSS` не менять ни на символ.

    2. `src/styles/motionPolicy.test.ts`: в тесте «проставлен на всех декоративных слоях оболочки, hero и карты» убрать `"new-light"` из массива (остаётся `["stars", "globe", "beam", "pulse", "wave", "halo"]`), в `REGISTRY` значение `new-light` оставить. Заменить комментарий над циклом: `new-light` остаётся в реестре ради селектора `[data-anim="new-light"]` в блоке reduce global.css, но кольцо нового огонька с фазы 15 рисует canvas, узла с этим атрибутом в DOM нет, поэтому обязательным значение не считается; `pulse` держит canvas огоньков. Ничего больше в файле не менять (фаза 14 обязана пройти его без правок).

    3. Полный гейт в worktree: `npx vitest run` (весь набор), `npx tsc -b`, `npm run lint`, `npm run build` (без предупреждений о размере чанков), `node scripts/check-dist.mjs`. В выводе vitest не должно быть `console.error` (тест `App.test.tsx` «рендерит страницу молча» роняет прогон при любом) и предупреждений `act`. `git status --porcelain` показывает только файлы из `files_modified` планов 15-01 и 15-02.

    4. В SUMMARY плана записать: список удалённых из SVG узлов (оценка числа узлов SVG на странице: 177 стран плюс служебные, против 3109 до фазы), список удалённых правил CSS, изменения тестов (какие удалены, какие заменены), и напоминание для фазы 17: селекторы `[data-anim="pulse"] circle`, `[data-anim="pulse"] .light-halo`, `[data-anim="new-light"]` в `global.css` теперь no-op и остаются до общего решения.
  </action>
  <acceptance_criteria>
    - `src/App.seams.test.tsx` содержит `canvas.map-lights-canvas`, `data-light-count`, `BREATH_PERIOD_MS`, `LIGHT_BUCKETS`, `not.toContain("light-breathe")`; не содержит `.map-lights .light-bucket`, `@keyframes light-breathe`; строки 56–78 (тест формы) совпадают с исходными (`git diff src/App.seams.test.tsx` не затрагивает строки с `LIGHT_FORM_CSS` и `.lf-section`)
    - `src/styles/motionPolicy.test.ts`: `REGISTRY` содержит `"new-light"`; в массиве обязательных значений `"new-light"` нет, `"pulse"` есть; `git diff --stat src/styles/motionPolicy.test.ts` показывает изменения только в одном тесте (не больше 6 изменённых строк)
    - `npx vitest run` код 0, все файлы passed, без `console.error` и `Warning: An update`
    - `npx tsc -b`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` код 0; в выводе build нет предупреждения о чанках больше 500 КБ
    - `git status --porcelain` показывает только: `src/components/map/lightsCanvas.ts`, `lightsCanvas.test.ts`, `LightsCanvas.tsx`, `LightsCanvas.test.tsx`, `EsdMap.tsx`, `EsdMap.test.tsx`, `map.css`, `src/App.seams.test.tsx`, `src/styles/motionPolicy.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run && npx tsc -b && npm run lint && npm run build && node scripts/check-dist.mjs</automated>
  </verify>
  <done>Стыковой тест читает атрибуты canvas и константы модуля, политика движения не требует `new-light` в DOM, весь набор тестов, tsc, lint, build и check-dist зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| браузер посетителя → статический бандл GitHub Pages | входа данных нет; огоньки замоканы; canvas без внешних картинок, tainting невозможен |
| canvas поверх SVG → жесты карты | `pointer-events: none` на canvas, события колеса и указателя уходят в SVG под ним |
| наблюдатели браузера → цикл rAF | видимость, скрытая вкладка и reduce останавливают цикл; без 2d-контекста цикл не запускается |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Denial of service | цикл rAF `LightsCanvas` | mitigate | порог 33 мс, пауза по IntersectionObserver/visibilitychange/reduce, `cancelAnimationFrame` в cleanup; полная частота только на 900 мс кольца |
| T-15-04 | Denial of service | `resize()` при каждом ResizeObserver | mitigate | ранний выход при неизменных bitmap и dpr; спрайты только при смене dpr |
| T-15-05 | Tampering | рассинхрон огоньков и стран при жесте | mitigate | `draw(transform)` из `handleFrame` синхронно; тест кадра жеста сверяет позицию ядра с `k·x + tx` |
| T-15-06 | Information disclosure | `data-*` атрибуты canvas | accept | только счётчики замоканных огоньков, тот же текст уже в `.sr-only` |
| T-15-SC | Tampering | npm-установки | accept | зависимостей не добавляется, `package.json` неизменен; `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `npx vitest run` (весь набор), `npx tsc -b`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` зелёные
- В jsdom: canvas с атрибутами 942/694/248/0, после addLight 943/1, rAF не вызван (LIGHT-06)
- С мок-контекстом: первый кадр сразу, цикл после `enterViewport`, порог 33 мс, статичный кадр под reduce (18px, .22), кадр жеста по `draw(transform)` с позицией `k·x + tx`, кольцо 6px alpha .5 при появлении и его окончание через 900 мс (LIGHT-03/04/05)
- `EsdMap.tsx` без `defs`, `.map-lights`, `circle`; `map.css` без `@property --halo-k`, `.light-*`, `light-breathe`, `light-arrive` (LIGHT-01)
- `git status` без файлов вне `files_modified`
</verification>

<success_criteria>
- `<canvas class="map-lights-canvas" data-anim="pulse" aria-hidden="true">` внутри `.esd-map` после `<svg>`, `absolute inset 0`, `pointer-events: none`, dpr через `clampDpr` ≤ 2
- `handleFrame` → `lightsRef.current?.draw(next)`; `LightsCanvas` получает `points`, `transform`, `width`, `height`
- Цикл 30 fps с паузами и статичным кадром reduce; кольцо 900 мс на полной частоте
- `EsdMap.test.tsx`, `App.seams.test.tsx`, `motionPolicy.test.ts` переведены; проверки `light-form.css` не тронуты
- Полный гейт зелёный
</success_criteria>

<output>
Create `.planning/phases/15-lights-canvas/15-02-SUMMARY.md` when done (с разделами: что удалено из SVG и CSS, как устроен движок LightsCanvas, изменения тестов, заметка для фазы 17 о no-op селекторах в global.css)
</output>
