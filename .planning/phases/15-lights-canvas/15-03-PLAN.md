---
phase: 15-lights-canvas
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - .planning/phases/15-lights-canvas/qa/lights-probe.mjs
  - .planning/phases/15-lights-canvas/qa/results/lights-1440x900.json
  - .planning/phases/15-lights-canvas/qa/results/lights-1440x900-reduced.json
  - .planning/phases/15-lights-canvas/qa/results/fps-map-390x844-cpu4.json
  - .planning/phases/15-lights-canvas/qa/results/fps-form-390x844-cpu4.json
  - .planning/phases/15-lights-canvas/qa/results/fps-map-1440x900.json
  - .planning/phases/15-lights-canvas/qa/results/fps-form-1440x900.json
  - .planning/phases/15-lights-canvas/qa/results/fps-map-390x844-cpu4-dpr2.json
  - .planning/phases/15-lights-canvas/qa/results/lights-1440x900-map.png
  - .planning/phases/15-lights-canvas/qa/results/lights-1440x900-zoomed.png
  - src/components/map/lightsCanvas.ts
  - src/components/map/lightsCanvas.test.ts
autonomous: true
requirements: [LIGHT-07]
tags: [map, canvas, playwright, performance, fps]

must_haves:
  truths:
    - "На 390×844 при CPU×4 (CDP Emulation.setCPUThrottlingRate 4) сцена карты и сцена формы держат медиану ≥ 55 fps по трём rAF-замерам по 2 с; на 1440×900 без троттлинга обе ≥ 100"
    - "Число узлов SVG на странице < 1300; в DOM нет .map-lights, .light-bucket, .light-core, .light-ring, defs и circle внутри карты; canvas.map-lights-canvas несёт data-light-count=\"942\", data-people=\"694\", data-groups=\"248\", data-new=\"0\", computed position absolute и pointer-events none, bitmap равен размеру контейнера × min(devicePixelRatio, 2)"
    - "Без reduce кадр canvas меняется за 650 мс (дыхание живое); с reduce два снимка через 650 мс совпадают (один статичный кадр), при этом огоньки на холсте есть"
    - "После Ctrl+колесо трансформ вьюпорта получает scale(k > 1), кадр canvas меняется, скриншоты карты до и после зума лежат в qa/results/"
    - "Все JSON-результаты и команды запуска записаны в qa/results/ и SUMMARY; при недоборе бюджета на dpr 2 применён единственный разрешённый ход: MAX_DPR 1.5 (в пределах dpr ≤ 2), числа до и после записаны"
  artifacts:
    - path: ".planning/phases/15-lights-canvas/qa/lights-probe.mjs"
      provides: "Playwright-зонд: режимы lights и fps, флаги --width/--height/--cpu/--dpr/--section/--reduced/--runs/--url/--out, запись JSON и PNG в qa/results/"
      min_lines: 220
    - path: ".planning/phases/15-lights-canvas/qa/results/fps-map-390x844-cpu4.json"
      provides: "Замер карты на телефоне при CPU×4: три прогона, медиана, maxGapMs, floor 55"
      contains: "\"median\""
    - path: ".planning/phases/15-lights-canvas/qa/results/lights-1440x900.json"
      provides: "Структура DOM, число узлов SVG, атрибуты canvas, живое дыхание, зум"
      contains: "\"svgNodes\""
  key_links:
    - from: ".planning/phases/15-lights-canvas/qa/lights-probe.mjs"
      to: "playwright из кэша npx (~/.npm/_npx/*/node_modules/playwright) или PW_ROOT"
      via: "createRequire + import(pathToFileURL(entry)), тот же резолв, что в map-probe.mjs фазы 8"
      pattern: "createRequire"
    - from: ".planning/phases/15-lights-canvas/qa/lights-probe.mjs"
      to: "CDP Emulation.setCPUThrottlingRate"
      via: "context.newCDPSession(page).send(\"Emulation.setCPUThrottlingRate\", { rate })"
      pattern: "setCPUThrottlingRate"
    - from: ".planning/phases/15-lights-canvas/qa/lights-probe.mjs"
      to: "canvas.map-lights-canvas"
      via: "getContext(\"2d\").getImageData для снимков кадра и data-* для счётчиков"
      pattern: "getImageData"
---

<objective>
Построить Playwright-зонд `qa/lights-probe.mjs` (по образцу `map-probe.mjs` фазы 8, без добавления зависимостей в репозиторий), измерить fps карты и формы на 390×844 при CPU×4 и на 1440×900 без троттлинга, проверить структуру DOM (узлы SVG < 1300, атрибуты canvas, отсутствие SVG-огоньков), живое дыхание и статичный кадр под reduce, синхронизацию с зумом, и записать все результаты в `qa/results/`. Числа решают вопрос о dpr: если на dpr 2 бюджет не выдержан, `MAX_DPR` снижается до 1,5.

Purpose: LIGHT-07 задаёт бюджет ≥ 55 fps на телефоне при CPU×4 и ≥ 100 на десктопе; до фазы карта и форма давали 30 (`docs/research/v1.2/measurements.md`). Фаза 17 повторит эти же команды для сводной таблицы против оригинала.
Output: `qa/lights-probe.mjs`, семь JSON и два PNG в `qa/results/`, раздел «Замеры LIGHT-07» в SUMMARY.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lights-canvas/15-02-SUMMARY.md
@docs/research/v1.2/measurements.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@.planning/milestones/v1.1-phases/08-map-band-and-lights/qa/map-probe.mjs
@.planning/milestones/v1.1-phases/08-map-band-and-lights/08-02-SUMMARY.md
@src/components/map/lightsCanvas.ts
@src/components/map/LightsCanvas.tsx

<ownership>
Зонд и результаты живут в `.planning/phases/15-lights-canvas/qa/`. Исходники правятся только по правилу dpr из Task 2 (`MAX_DPR` в `lightsCanvas.ts` и его проверка в `lightsCanvas.test.ts`); ничего другого в `src/` не трогать. Зависимости в репозиторий не добавлять: `package.json` и `package-lock.json` неизменны, `playwright` берётся из кэша npx (`~/.npm/_npx/*/node_modules/playwright`, сейчас 1.63.0-alpha-2026-08-31 с chromium-1234 в `~/Library/Caches/ms-playwright`) или из `PW_ROOT`. Замеры fps делать при закрытых WebGL-вкладках и без параллельных `npm test`/`npm run build` (решение фазы 5). Preview поднимать на порту 4175 (`--strictPort`): фазы 14 и 16 могут держать свои preview на соседних портах. Коммитов исполнитель не делает; команды относительные.
</ownership>

<interfaces>
<!-- Существующие контракты. Извлечены из кода, исследовать заново не нужно. -->

From .planning/milestones/v1.1-phases/08-map-band-and-lights/qa/map-probe.mjs (файл в архиве, скопировать нужные функции, не импортировать):
```js
// строки 19–23: import { existsSync, readdirSync } from "node:fs"; createRequire; homedir; join; pathToFileURL
// строки 78–124: playwrightRoots() → [PW_ROOT, ~/.npm/_npx/*/ с node_modules/playwright]; loadPlaywright() → { api, from, version } | null; missingPlaywright() печатает команду scratch-установки и exit 2
// строки 130–158: launchBrowser(chromium) → { browser, channel, headless }; PW_CHANNEL (chrome по умолчанию), PW_HEADLESS=1 для headless; по умолчанию окно
// строки 321–377: runFps: goto, bringToFront, scrollIntoView({ block: "center" }), waitForTimeout(2500), --runs замеров rAF по 2000 мс → { fps, maxGapMs, frames }, медиана, PASS при median >= floor
// строки 539–560: newContext({ viewport, deviceScaleFactor: 1, reducedMotion }), вывод JSON одной строкой + PASS/FAIL, exit 0/1
```

From playwright (Chromium): `const cdp = await context.newCDPSession(page); await cdp.send("Emulation.setCPUThrottlingRate", { rate: 4 });` включает троттлинг для страницы; `await cdp.detach()` снимает сессию.

From src/components/map/LightsCanvas.tsx (план 15-02): `canvas.map-lights-canvas` с `data-light-count`, `data-people`, `data-groups`, `data-new`; `canvas.width = round(rect.width · min(devicePixelRatio, MAX_DPR))`; повторный `canvas.getContext("2d")` в браузере возвращает тот же контекст, `getImageData` доступен (внешних картинок нет, холст не tainted).

From src/components/map/lightsCanvas.ts: `MAX_DPR = 2`, `BREATH_PERIOD_MS = 2600` (четверть периода 650 мс: два снимка кадра с этим интервалом различаются), `REDUCED_HALO_RADIUS = 9`.

Ожидания страницы (из ROADMAP и measurements.md): 942 огонька; до фазы 3109 узлов SVG, из них 1884 круга; после фазы ожидается около 177 путей стран плюс служебные узлы (title, группы), то есть заметно меньше 1300.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Зонд qa/lights-probe.mjs: режимы lights и fps, CPU-троттлинг через CDP, запись результатов</name>
  <files>.planning/phases/15-lights-canvas/qa/lights-probe.mjs</files>
  <read_first>
    - .planning/milestones/v1.1-phases/08-map-band-and-lights/qa/map-probe.mjs (строки 1–160 и 321–377, 539–560: резолв playwright, запуск браузера, runFps, вывод)
    - .planning/milestones/v1.1-phases/08-map-band-and-lights/08-02-SUMMARY.md, раздел «Замер fps и reduced motion» (как поднимали preview, канал браузера, оговорки)
    - docs/research/v1.2/measurements.md (стенд: viewport 390×844, CPU×4 через CDP, dpr 1, fps = rAF за 2,5 с)
    - .planning/ROADMAP.md, Phase 15, Success Criteria 1, 4, 5
  </read_first>
  <action>
    Реализует инструмент проверки LIGHT-07 (бюджет fps, узлы SVG) и браузерную проверку LIGHT-01/04 (структура, дыхание, reduce).

    1. Создать каталоги `.planning/phases/15-lights-canvas/qa/` и `qa/results/`. Написать ESM-скрипт `lights-probe.mjs` с шапкой-комментарием (режимы, флаги, примеры команд). Резолв `playwright`, `launchBrowser`, `round` скопировать из `map-probe.mjs` фазы 8 (тот же порядок: `PW_ROOT`, кэш npx, `import("playwright")`; при отсутствии подсказка со scratch-установкой `playwright@1.63.0` и код 2). Импорты только из `node:*` и `playwright`.
       CLI: `node lights-probe.mjs <lights|fps> [--url http://localhost:4175/esd-onevoice27/] [--width 1440] [--height 900] [--dpr 1] [--cpu 1] [--section map|form] [--runs 3] [--reduced] [--out <dir>]`; `--out` по умолчанию `results/` рядом со скриптом (`fileURLToPath(new URL("./results/", import.meta.url))`), каталог создаётся `mkdirSync({ recursive: true })`. Контекст: `viewport`, `deviceScaleFactor: opts.dpr`, `reducedMotion: "reduce"` при `--reduced`; на ширине < 768 дополнительно `isMobile: true`, `hasTouch: true` (мобильная эмуляция, как в measurements.md). Имя файла результата: `${mode}[-${section}]-${width}x${height}[-cpu${cpu}][-dpr${dpr}][-reduced].json` (суффиксы `cpu` и `dpr` только при значениях ≠ 1). Вывод: одна JSON-строка результата, затем `PASS`/`FAIL`, коды 0/1/2. В шапке вывода печатать канал браузера, headless или окно, версию playwright, вьюпорт, cpu, dpr, reduced.

    2. Режим `fps`: `goto(url, { waitUntil: "load" })`, `bringToFront`, прокрутка цели `--section` в центр (`map` → `.map-shell`, `form` → `#light-form`, `scrollIntoView({ block: "center" })`), `waitForTimeout(2500)` (reveal, счётчики, шапка). Если `--cpu > 1`: `cdp = await context.newCDPSession(page)`, `cdp.send("Emulation.setCPUThrottlingRate", { rate: cpu })`, пауза 300 мс, замеры, затем `rate: 1` и `detach`. Замер как в `map-probe.mjs`: `--runs` раз по 2000 мс rAF → `fps`, `maxGapMs`, `frames`; медиана; `floor` = 55 при `cpu > 1`, иначе 100 (LIGHT-07). В JSON также `section`, `cpu`, `dpr`, `lightCount` (атрибут `data-light-count`), `canvasBitmap` (`{ width, height }` canvas). PASS при `median >= floor`.

    3. Режим `lights` (без `--cpu`): `goto`, `bringToFront`, один кадр rAF, затем проверки в `page.evaluate`:
       a) `structure`: `canvas = document.querySelector(".esd-map > canvas.map-lights-canvas")` есть; `dataset.anim === "pulse"`, `aria-hidden === "true"`; атрибуты `data-light-count === "942"`, `data-people === "694"`, `data-groups === "248"`, `data-new === "0"`; `getComputedStyle(canvas)`: `position === "absolute"`, `pointerEvents === "none"`, `top`, `left`, `right`, `bottom` равны `"0px"`; `rect = canvas.getBoundingClientRect()`, `expectedDpr = Math.min(devicePixelRatio, 2)`, `canvas.width === Math.round(rect.width * expectedDpr)` и то же по высоте; `canvas.previousElementSibling.tagName.toLowerCase() === "svg"`;
       b) `svg`: `svgNodes = document.querySelectorAll("svg, svg *").length` (< 1300), `mapCircles = document.querySelectorAll(".esd-map circle").length` (0), `document.querySelectorAll(".map-lights, .light-bucket, .light-core, .light-ring, .esd-map defs, .esd-map radialGradient").length` (0), `countries = document.querySelectorAll(".esd-map path.country").length` (177);
       c) `breath`: функция `frameSignature()` в странице: `ctx = canvas.getContext("2d")`, `data = ctx.getImageData(0, 0, canvas.width, canvas.height).data`, вернуть `{ lit: число пикселей с alpha > 0 (шаг по каждому 4-му байту), sum: сумма alpha }`; снять `before`, `waitForTimeout(650)`, снять `after`; без `--reduced` ожидается `before.lit > 0` и `before.sum !== after.sum` (дыхание живое); с `--reduced` ожидается `before.lit > 0` и `before.sum === after.sum` (один статичный кадр); прокрутить `.map-shell` в центр перед снимками, чтобы карта была в экране и цикл шёл;
       d) `zoom` (только без `--reduced`): скриншот карты `page.screenshot({ clip: прямоугольник .map-shell, path: <out>/lights-<w>x<h>-map.png })`; затем `page.mouse.move(cx, cy)` в центр `.esd-map`, `page.keyboard.down("Control")`, `page.mouse.wheel(0, -240)`, `page.keyboard.up("Control")`, `waitForTimeout(150)`; прочитать `transform` у `g.map-viewport` (`scale(k)`, ожидается `k > 1`), снять `frameSignature()` (ожидается `sum` отличается от `before` и `lit > 0`), скриншот `<out>/lights-<w>x<h>-zoomed.png`; в JSON `zoom: { k, litAfter, changed }`;
       e) PASS, если все проверки режима истинны; JSON с полями `structure`, `svgNodes`, `mapCircles`, `legacyNodes`, `countries`, `breath: { before, after, alive|static }`, `zoom`, `checks`, `failed` (список имён проваленных проверок).

    4. После каждого режима закрыть страницу; в `finally` закрыть контекст и браузер. Записать результат в `--out` (`writeFileSync(path, JSON.stringify(result, null, 2))`) до печати PASS/FAIL. Скрипт не должен трогать ничего вне `--out`.
  </action>
  <acceptance_criteria>
    - Файл `.planning/phases/15-lights-canvas/qa/lights-probe.mjs` существует, содержит `createRequire`, `PW_ROOT`, `newCDPSession`, `Emulation.setCPUThrottlingRate`, `getImageData`, `"map-lights-canvas"`, `"svg, svg *"`, `1300`, `55`, `100`, `requestAnimationFrame`, `mkdirSync`, `writeFileSync`, `keyboard.down("Control")`; импортов, кроме `node:*` и `playwright`, нет
    - `node .planning/phases/15-lights-canvas/qa/lights-probe.mjs` без аргументов печатает подсказку по режимам и завершается кодом 2
    - `node --check .planning/phases/15-lights-canvas/qa/lights-probe.mjs` код 0
    - `git status --porcelain` не показывает `package.json`, `package-lock.json`, `node_modules`
  </acceptance_criteria>
  <verify>
    <automated>node --check .planning/phases/15-lights-canvas/qa/lights-probe.mjs && (node .planning/phases/15-lights-canvas/qa/lights-probe.mjs >/dev/null 2>&1; test $? -eq 2)</automated>
  </verify>
  <done>Зонд разбирает флаги, резолвит playwright без установки в репозиторий, умеет троттлить CPU через CDP, проверяет структуру, дыхание, reduce и зум, меряет fps по секциям и пишет JSON/PNG в qa/results/.</done>
</task>

<task type="auto">
  <name>Task 2: Сборка, preview, матрица замеров LIGHT-07, правило dpr, запись результатов и SUMMARY</name>
  <files>.planning/phases/15-lights-canvas/qa/results/lights-1440x900.json, .planning/phases/15-lights-canvas/qa/results/lights-1440x900-reduced.json, .planning/phases/15-lights-canvas/qa/results/fps-map-390x844-cpu4.json, .planning/phases/15-lights-canvas/qa/results/fps-form-390x844-cpu4.json, .planning/phases/15-lights-canvas/qa/results/fps-map-1440x900.json, .planning/phases/15-lights-canvas/qa/results/fps-form-1440x900.json, .planning/phases/15-lights-canvas/qa/results/fps-map-390x844-cpu4-dpr2.json, .planning/phases/15-lights-canvas/qa/results/lights-1440x900-map.png, .planning/phases/15-lights-canvas/qa/results/lights-1440x900-zoomed.png, src/components/map/lightsCanvas.ts, src/components/map/lightsCanvas.test.ts</files>
  <read_first>
    - .planning/STATE.md, «Blockers/Concerns», риск фазы 15 (при недоборе первым делом снижать dpr canvas с 2 до 1,5, затем частоту цикла: второй ход в этом плане запрещён, см. п. 3)
    - .planning/milestones/v1.1-phases/08-map-band-and-lights/08-02-SUMMARY.md, раздел «Замер fps и reduced motion» (формат записи чисел и команд)
    - docs/research/v1.2/measurements.md (таблица «до»: карта 30, форма 30 при CPU×4; 66 и 69 на десктопе)
  </read_first>
  <action>
    Реализует LIGHT-07 числами.

    1. `npm run build` (без предупреждений о чанках), `node scripts/check-dist.mjs`, затем в фоне `npx vite preview --port 4175 --strictPort` (если порт занят, `--port 4176` и тот же порт в `--url`). Закрыть WebGL-вкладки, не запускать параллельно `npm test`/`npm run build`. Переменная `P=.planning/phases/15-lights-canvas/qa/lights-probe.mjs`, `U=http://localhost:4175/esd-onevoice27/`.

    2. Прогоны (каждый пишет JSON в `qa/results/`):
       - `node $P lights --url $U --width 1440 --height 900` → `lights-1440x900.json` + два PNG; ожидается PASS: `svgNodes < 1300`, `mapCircles 0`, `legacyNodes 0`, атрибуты 942/694/248/0, дыхание живое, `zoom.k > 1`.
       - `node $P lights --url $U --width 1440 --height 900 --reduced` → `lights-1440x900-reduced.json`; PASS: статичный кадр (`before.sum === after.sum`, `lit > 0`).
       - `node $P fps --url $U --width 390 --height 844 --cpu 4 --section map --runs 3` → `fps-map-390x844-cpu4.json`; floor 55.
       - `node $P fps --url $U --width 390 --height 844 --cpu 4 --section form --runs 3` → `fps-form-390x844-cpu4.json`; floor 55.
       - `node $P fps --url $U --width 1440 --height 900 --section map --runs 3` → `fps-map-1440x900.json`; floor 100.
       - `node $P fps --url $U --width 1440 --height 900 --section form --runs 3` → `fps-form-1440x900.json`; floor 100.
       - Информационный прогон на реальном dpr телефона: `node $P fps --url $U --width 390 --height 844 --cpu 4 --dpr 2 --section map --runs 3` → `fps-map-390x844-cpu4-dpr2.json` (canvas при dpr 2 рисует вчетверо больше пикселей; стенд measurements.md был на dpr 1, поэтому гейт LIGHT-07 держат прогоны с dpr 1, а этот прогон решает вопрос о `MAX_DPR`).

    3. Правило по числам (без оценки на глаз):
       - Если четыре гейтовых прогона PASS и информационный прогон на dpr 2 даёт медиану ≥ 55: конфигурация остаётся, `MAX_DPR = 2`.
       - Если гейтовые PASS, а прогон на dpr 2 даёт медиану < 55: в `src/components/map/lightsCanvas.ts` заменить `MAX_DPR = 2` на `MAX_DPR = 1.5` с комментарием (замер такой-то: медиана N на dpr 2, M на dpr 1,5; LIGHT-01 требует dpr ≤ 2, 1,5 внутри нормы), в `lightsCanvas.test.ts` обновить ожидание `MAX_DPR` и `clampDpr(2)` → 1.5, `clampDpr(1.5)` → 1.5; повторить `npm run build`, `npx vitest run src/components/map`, поднять preview заново и повторить прогон на dpr 2 и оба гейтовых прогона 390×844; оба набора чисел записать.
       - Если любой гейтовый прогон FAIL при dpr 1: не менять частоту цикла и не упрощать кадр (30 fps, дыхание радиуса, спрайты и кольцо зафиксированы LIGHT-02/04/05); сначала проверить стенд (окно Chrome на переднем плане, WebGL-вкладки закрыты, нет параллельных процессов) и повторить прогон; если недобор повторяется, записать числа, `maxGapMs` и условия в SUMMARY и вернуть оркестратору блокер «LIGHT-07 не выдержан: медиана N при пороге 55/100» с предложением вариантов (снижение `MAX_DPR`, проверка стоимости `getImageData` в самом зонде, замер на другой машине в фазе 17). Решение о любом отклонении от спецификации принимает не исполнитель.

    4. Остановить preview. Записать в SUMMARY плана раздел «Замеры LIGHT-07»: канал браузера, headless или окно, версия playwright и путь резолва, порт preview, точные команды, таблица «секция / вьюпорт / cpu / dpr / три значения / медиана / maxGapMs / порог / вердикт» по всем семи прогонам, числа «до» из measurements.md (30/30 при CPU×4, 66/69 на десктопе) рядом, `svgNodes` до (3109) и после, полный JSON `lights` обоих прогонов, решение по `MAX_DPR` с числами, имена PNG. Эти команды фаза 17 использует для сводной таблицы против оригинала.
  </action>
  <acceptance_criteria>
    - В `.planning/phases/15-lights-canvas/qa/results/` лежат `lights-1440x900.json`, `lights-1440x900-reduced.json`, `fps-map-390x844-cpu4.json`, `fps-form-390x844-cpu4.json`, `fps-map-1440x900.json`, `fps-form-1440x900.json`, `fps-map-390x844-cpu4-dpr2.json`, `lights-1440x900-map.png`, `lights-1440x900-zoomed.png`
    - `lights-1440x900.json`: `svgNodes < 1300`, `mapCircles: 0`, `legacyNodes: 0`, `countries: 177`, атрибуты `data-light-count "942"`, `data-people "694"`, `data-groups "248"`, `data-new "0"`, `breath.alive: true`, `zoom.k > 1`, `failed: []`; `lights-1440x900-reduced.json`: `breath.static: true`, `failed: []`
    - `fps-map-390x844-cpu4.json` и `fps-form-390x844-cpu4.json`: `median >= 55`, `cpu: 4`; `fps-map-1440x900.json` и `fps-form-1440x900.json`: `median >= 100`; каждый с тремя прогонами и `maxGapMs`
    - `fps-map-390x844-cpu4-dpr2.json` существует; если его медиана < 55, то `lightsCanvas.ts` содержит `MAX_DPR = 1.5`, тест обновлён, `npx vitest run src/components/map` зелёный и в `results/` есть повторные числа; иначе `MAX_DPR = 2` не тронут
    - SUMMARY плана содержит раздел «Замеры LIGHT-07» с таблицей семи прогонов, числами «до» из measurements.md, `svgNodes` до/после и решением по `MAX_DPR`
    - `npm run build` без предупреждений о чанках; `node scripts/check-dist.mjs` только PASS-строки; `git status --porcelain` показывает только файлы из `files_modified` планов 15-01, 15-02, 15-03
  </acceptance_criteria>
  <verify>
    <automated>npm run build && node scripts/check-dist.mjs && (npx vite preview --port 4175 --strictPort & sleep 2; node .planning/phases/15-lights-canvas/qa/lights-probe.mjs lights --url http://localhost:4175/esd-onevoice27/ --width 1440 --height 900 && node .planning/phases/15-lights-canvas/qa/lights-probe.mjs fps --url http://localhost:4175/esd-onevoice27/ --width 390 --height 844 --cpu 4 --section map --runs 3; code=$?; kill %1; exit $code)</automated>
  </verify>
  <done>Семь прогонов записаны в qa/results/, гейтовые медианы ≥ 55 (390×844, CPU×4) и ≥ 100 (1440×900) для карты и формы, узлов SVG < 1300, дыхание живое и статичное под reduce, зум синхронен по скриншотам; решение по dpr принято числами и записано.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| зонд Playwright → localhost preview | локальный процесс; внешняя сеть нужна только самой странице (Google Fonts, превью YouTube), как и раньше |
| зонд → canvas страницы (`getImageData`) | холст без внешних картинок, чтение пикселей разрешено; результат только в JSON и PNG внутри `qa/results/` |
| исполнитель → реестр npm (только если `playwright` не найден в кэше npx) | установка вне репозитория, в scratch-каталог |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Tampering | результаты fps | mitigate | сырые JSON с тремя прогонами, `maxGapMs`, каналом и условиями стенда; команды воспроизводимы фазой 17 |
| T-15-08 | Denial of service | `getImageData` в зонде на большом холсте | mitigate | снимки кадра только в режиме `lights`, не во время замера fps |
| T-15-09 | Information disclosure | скриншоты | accept | только область карты localhost; в репозиторий попадают в `qa/results/` фазы, без данных посетителей |
| T-15-SC | Tampering | npm-установки | mitigate | зависимостей в репозиторий не добавляется; `playwright` из кэша npx MCP или scratch-установка `playwright@1.63.0` с npmjs.com (Microsoft, проверено 2026-09-06); `[ASSUMED]`/`[SUS]`-пакетов нет |
</threat_model>

<verification>
- `node --check` зонда, режим без аргументов даёт код 2
- `lights` PASS на 1440×900 (структура, `svgNodes < 1300`, дыхание, зум, скриншоты) и с `--reduced` (статичный кадр)
- `fps` PASS: карта и форма ≥ 55 на 390×844 при CPU×4, ≥ 100 на 1440×900
- Информационный прогон на dpr 2 записан; правило `MAX_DPR` применено по числам
- `npm run build`, `node scripts/check-dist.mjs` чистые; `git status` без файлов вне `files_modified`
</verification>

<success_criteria>
- Зонд `qa/lights-probe.mjs` с режимами `lights` и `fps`, CPU-троттлингом через CDP и записью в `qa/results/`
- Семь JSON и два PNG в `qa/results/`, гейтовые медианы выше порогов LIGHT-07, узлов SVG < 1300
- SUMMARY с таблицей замеров, числами «до», решением по `MAX_DPR` и командами для фазы 17
</success_criteria>

<output>
Create `.planning/phases/15-lights-canvas/15-03-SUMMARY.md` when done (раздел «Замеры LIGHT-07»: команды, таблица семи прогонов, JSON режима lights, решение по MAX_DPR, имена PNG, оговорки стенда)
</output>
