---
phase: 07-glass-and-titles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/layout/primitives.css
  - src/components/about/about.css
  - src/components/involve/involve.css
  - src/components/layout/primitives.test.tsx
  - src/components/about/About.test.tsx
  - src/components/involve/Involve.test.tsx
autonomous: true
requirements: [GLASS-01, GLASS-02, GLASS-03, GLASS-04, GLASS-05]

must_haves:
  truths:
    - "Карточка шага About пропускает фон секции: поверхность `linear-gradient(145deg, rgb(255 255 255 / .045), transparent 30%), linear-gradient(180deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62))`, рамка `1px solid rgb(239 237 245 / .18)`, тень `inset 0 1px 0 rgb(255 255 255 / .035), 0 20px 46px rgb(3 2 18 / .24)`, `backdrop-filter: blur(14px) saturate(112%)`"
    - "При наведении на карточку шага рамка светлеет до `rgb(143 157 214 / .34)` за 420ms; акцентная линия `.ab-step::after` и разделитель `.ab-step-rule` на месте"
    - "В `global.css` есть утилита `glass-resource` с поверхностью `linear-gradient(180deg, rgb(255 255 255 / .075), transparent 34%), linear-gradient(145deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62))` и `blur(14px) saturate(125%)`; тест primitives проверяет её значения; класс на `.resource-card` вешает фаза 11"
    - "Рамка триптиха Involve без `backdrop-filter`, с тенью `0 34px 76px rgb(2 2 12 / .58), 0 0 54px rgb(59 77 161 / .22)`; карточки `.inv-card` с поверхностью `rgb(33 26 62 / .48)`, швом `1px solid rgb(239 237 245 / .15)`, радиусом 14px внутри рамки 18px; при наведении поверхность `rgb(49 41 77 / .54)`"
    - "Счётчики карты и шапка не изменились: `Counters.test.tsx` зелёный без правок, `map.css`, `Counters.tsx` и `Header.css` не ссылаются на `--glass-surface`, `--glass-border`, `--shadow-card` и класс `glass`"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Токены стекла оригинала и утилиты glass / glass-resource"
      contains: "@utility glass-resource"
    - path: "src/components/layout/primitives.css"
      provides: "Блик и ховер стеклянной карточки по спецификации"
      contains: "rgb(143 157 214 / .34)"
    - path: "src/components/about/about.css"
      provides: "Карточка шага без своего фона, ховер рамки и тени оригинала"
      contains: "rgb(143 157 214 / .34)"
    - path: "src/components/involve/involve.css"
      provides: "Рамка триптиха без размытия, карточки с поверхностью и швом оригинала"
      contains: "rgb(33 26 62 / .48)"
    - path: "src/components/layout/primitives.test.tsx"
      provides: "Текстовые проверки токенов, утилит, блика, ховера, независимости шапки и счётчиков"
      contains: "glass-resource"
    - path: "src/components/about/About.test.tsx"
      provides: "Проверка about.css: нет фона у .ab-step, ховер по спецификации, линия и разделитель на месте"
      contains: "readFileSync"
    - path: "src/components/involve/Involve.test.tsx"
      provides: "Проверка involve.css: рамка без blur, поверхность, шов, ховер карточки"
      contains: "readFileSync"
  key_links:
    - from: "src/components/about/StepCard.tsx"
      to: "src/styles/global.css (@utility glass)"
      via: "GlassCard className=\"ab-step\" даёт классы glass-card glass"
      pattern: "glass-card glass"
    - from: "src/components/about/about.css (.ab-step)"
      to: "src/styles/global.css (--glass-surface)"
      via: "правило .ab-step без background: поверхность приходит из утилиты"
      pattern: "\\.ab-step \\{[^}]*\\}"
    - from: "src/components/involve/involve.css (.inv-triptych)"
      to: "src/styles/global.css (@utility glass)"
      via: "переопределение backdrop-filter: none и box-shadow рамки поверх утилиты"
      pattern: "backdrop-filter: none"
    - from: "src/components/involve/InvolveCard.tsx"
      to: "src/components/involve/involve.css (.inv-card)"
      via: "article className=\"inv-card\""
      pattern: "inv-card"
---

<objective>
Привести стекло проекта к карточке оригинала: токены `--glass-surface`, `--glass-border`, `--shadow-card` и утилита `glass` в `global.css` становятся полупрозрачными, появляется утилита `glass-resource` для фазы 11, блик и ховер `.glass-card` берут значения спецификации, карточки шагов About получают ховер оригинала без своего фона, триптих Involve теряет размытие рамки и получает поверхность, шов и ховер карточек оригинала. Счётчики карты и шапка не меняются.

Purpose: пользователь после приёмки v1.0 назвал «мало глассморфизма»; причина в непрозрачном индиго `rgb(48 63 131 / .86)` и тяжёлой тени. Токены меняются в одном месте, всё стекло проекта получает поверхность оригинала автоматически.
Output: обновлённые `global.css`, `primitives.css` (только правила `.glass-card*`), `about.css`, `involve.css` и три тестовых файла с текстовыми проверками CSS.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-glass-and-titles/07-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/styles/global.css
@src/components/layout/primitives.css
@src/components/about/about.css
@src/components/involve/involve.css
@src/styles/motionPolicy.test.ts

<ownership>
Фаза 7 идёт параллельно с фазами 8–12 в отдельном worktree от общего `main`. Редактировать разрешено только файлы из `files_modified` этого плана. Запрещено трогать даже ради одной строки: `src/styles/tokens.css`, `src/components/map/map.css`, `src/components/form/light-form.css`, `src/components/resources/resources.css`, `src/components/news/*`, `src/components/map/MapSection.tsx`, `src/components/map/Counters.tsx`, `src/components/form/LightForm.tsx`, `src/components/resources/Resources.tsx`, `src/components/resources/ResourceCard.tsx`, `src/components/news/News.tsx`, `src/components/involve/Involve.tsx`, `src/components/involve/InvolveCard.tsx`, `src/components/about/StepCard.tsx`, `src/components/layout/GlassCard.tsx`, `src/components/layout/Header.css`, `src/components/about/video-embed.css`. Правило для чужого селектора кладётся в свой CSS-файл. Правила `.gradient-title*` в `primitives.css` в этом плане не меняются: их правит план 07-02.

Цвета в CSS писать литералами `rgb(r g b / a)` из спецификации: токены `--color-midnight-*` проекта сдвинуты относительно палитры оригинала, по имени их путать нельзя.

Новых `@media (prefers-reduced-motion: reduce)` блоков не заводить: `src/styles/motionPolicy.test.ts` требует единственный блок в `global.css`. План не добавляет ни одного `@keyframes`, только transition на hover, а его длительность общий блок уже гасит.

В `global.css` менять только: три токена стекла в первом `:root`, комментарий у `@utility glass`, новую `@utility glass-resource`. Всё остальное `motionPolicy.test.ts` сверяет буквально, и эти строки остаются байт в байт: блок `@media (prefers-reduced-motion: reduce)` целиком, `@property --ov-hero-beam`, все правила `.btn[data-beam]` (в том числе `animation: hero-beam 3s linear infinite;` ровно 2 раза и `background-size: 7px 7px` ровно 1 раз), `outline: 2px solid var(--color-horizon-200)` ровно 2 раза, правило `main:focus,\nmain:focus-visible { outline-color: transparent; }`, токены reveal (`--dur-reveal: 700ms`, `--ease-reveal`, `--stagger-reveal: 80ms`, `--reveal-shift: 24px` и блок `@media (max-width: 767px)` с `--reveal-shift: 16px`), `overflow-x: clip`. Реестр `data-anim` (stars, globe, beam, pulse, new-light, particles, atmosphere, wave, halo) не расширять: новых атрибутов `data-anim` фаза не заводит.
</ownership>

<interfaces>
<!-- Текущее состояние правил, которые план меняет. Взято из исходников, исследовать заново не нужно. -->

Из src/styles/global.css (`:root`):
```css
--glass-border: rgb(184 192 230 / .22);
--glass-surface: linear-gradient(145deg, rgb(48 63 131 / .86), rgb(18 12 52 / .76));
```

Из src/styles/tokens.css (`@theme`, файл вне владения фазы, только читать):
```css
--radius-card: 16px;
--shadow-card:
  0 30px 72px rgb(18 12 52 / .62),
  0 10px 24px rgb(18 12 52 / .42),
  inset 0 1px 0 rgb(248 247 251 / .08);
```

Из src/styles/global.css (`@utility glass`):
```css
background:
  linear-gradient(145deg, rgb(255 255 255 / .045), transparent 30%),
  var(--glass-surface);
border: 1px solid var(--glass-border);
border-radius: var(--radius-card);
box-shadow: var(--shadow-card);
-webkit-backdrop-filter: blur(14px) saturate(112%);
backdrop-filter: blur(14px) saturate(112%);
```

Из src/components/layout/primitives.css:
```css
.glass-card::before { ... box-shadow: inset 1px 1px 0 rgb(255 255 255 / .06), inset -1px -1px 0 rgb(7 2 16 / .35); opacity: .58; }
.glass-card--interactive { transition: border-color var(--dur-ui) var(--ease-ui), box-shadow var(--dur-ui) var(--ease-ui); }
.glass-card--interactive:hover { border-color: rgb(184 192 230 / .34); box-shadow: 0 36px 80px rgb(18 12 52 / .7), 0 12px 28px rgb(18 12 52 / .48), inset 0 1px 0 rgb(248 247 251 / .1); }
```

Из src/components/layout/GlassCard.tsx (не меняется):
```tsx
className={"glass-card glass p-6 md:p-8" + (interactive ? " glass-card--interactive" : "") + (className ? " " + className : "")}
```

Из src/components/about/StepCard.tsx (не меняется): `<GlassCard className="ab-step">`, внутри `.ab-step-num`, `.ab-step-title`, `.ab-step-list`, `<hr className="ab-step-rule" />`, `.ab-step-summary`.

Из src/components/involve/Involve.tsx (не меняется): `<GlassCard className="inv-triptych">` содержит три `<RevealItem className="inv-slot">`, в каждом `<article className="inv-card" data-card=...>` из InvolveCard.tsx.

Кто ещё читает токены (правки не нужны, эффект принят в CONTEXT.md): `.btn--ghost` в primitives.css (`var(--glass-border)`), `src/components/news/News.tsx` и `NewsCard.tsx` (`border-[var(--glass-border)]`), `src/components/resources/resources.css` строка `var(--glass-surface)` в `.resources-panel`, `src/components/about/video-embed.css` (`var(--shadow-card)`), `ResourceCard.tsx` и `ResourcePanel.tsx` (класс `glass`). `Header.css` и `map.css` токены стекла не используют.

Паттерн текстовых тестов CSS (vitest настроен с `css: false`, computed styles из файлов в jsdom недоступны): `readFileSync(resolve(process.cwd(), "src/..."), "utf8")` как в `src/styles/motionPolicy.test.ts` и `Header.test.tsx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Токены и утилиты стекла в global.css, блик и ховер .glass-card, тесты primitives (GLASS-01, GLASS-03, GLASS-05)</name>
  <files>src/styles/global.css, src/components/layout/primitives.css, src/components/layout/primitives.test.tsx</files>
  <read_first>
    - src/styles/global.css (блок `:root`, `@utility glass`, комментарий к блоку reduced motion)
    - src/styles/tokens.css (только чтение: где сейчас объявлен `--shadow-card`; файл не править)
    - src/components/layout/primitives.css (правила `.glass-card`, `.glass-card::before`, `.glass-card--interactive`)
    - src/components/layout/primitives.test.tsx (текущие describe-блоки, стиль тестов)
    - src/styles/motionPolicy.test.ts (паттерн чтения CSS с диска, функция `filesWithExt`)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел «1. GLASS», пункты GLASS-01, GLASS-03, GLASS-05
    - .planning/phases/07-glass-and-titles/07-CONTEXT.md, раздел «Стекло»
  </read_first>
  <action>
    Правки в `src/styles/global.css` (per GLASS-01, GLASS-03):

    1. В первом блоке `:root` заменить два токена и добавить третий, значения литералами из спецификации: `--glass-border: rgb(239 237 245 / .18);`, `--glass-surface: linear-gradient(180deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62));`, `--shadow-card: inset 0 1px 0 rgb(255 255 255 / .035), 0 20px 46px rgb(3 2 18 / .24);`. Над `--shadow-card` комментарий на русском: значение переопределяет `--shadow-card` из `@theme` в tokens.css; tokens.css вне списка файлов фазы 7, а этот `:root` стоит после импортов и вне слоёв Tailwind, поэтому в каскаде побеждает он; фаза 13 после слияния может перенести значение в tokens.css. Старые литералы `rgb(48 63 131 / .86)`, `rgb(18 12 52 / .76)`, `rgb(184 192 230 / .22)` из global.css исчезают.

    2. `@utility glass` по структуре не меняется (первый слой `linear-gradient(145deg, rgb(255 255 255 / .045), transparent 30%)`, затем `var(--glass-surface)`, `border: 1px solid var(--glass-border)`, `border-radius: var(--radius-card)`, `box-shadow: var(--shadow-card)`, обе формы `backdrop-filter: blur(14px) saturate(112%)`). Обновить только комментарий: поверхность снята с `#ov-about .ov-about-step-card` оригинала.

    3. Сразу после `@utility glass` добавить `@utility glass-resource` с телом: `background` из двух слоёв `linear-gradient(180deg, rgb(255 255 255 / .075), transparent 34%)` и `linear-gradient(145deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62))`; `border: 1px solid var(--glass-border)`; `border-radius: var(--radius-card)`; `box-shadow: var(--shadow-card)`; `-webkit-backdrop-filter: blur(14px) saturate(125%)` и `backdrop-filter: blur(14px) saturate(125%)`. Комментарий на русском: поверхность `#ov-resources .ov-resources-card` оригинала; класс на `.resource-card` вешает фаза 11; Tailwind добавляет утилиту в бандл после первого использования класса в исходниках, до слияния она живёт только в этом файле. `@source inline` не нужен.

    Правки в `src/components/layout/primitives.css` (per GLASS-01), только правила `.glass-card*`; правила `.gradient-title*` и `.btn*` не трогать:

    4. `.glass-card::before`: `box-shadow: inset 1px 1px 0 rgb(255 255 255 / .075), inset -1px -1px 0 rgb(3 2 18 / .30);`, `opacity: .58` остаётся.

    5. `.glass-card--interactive`: `transition: border-color 420ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 420ms cubic-bezier(0.22, 1, 0.36, 1);` (литералы вместо `var(--dur-ui)`: у примитива 240ms, у оригинала 420ms).

    6. `.glass-card--interactive:hover`: `border-color: rgb(143 157 214 / .34); box-shadow: inset 0 1px 0 rgb(255 255 255 / .055), 0 24px 52px rgb(3 2 18 / .30);`. Старые `rgb(184 192 230 / .34)` и `0 36px 80px rgb(18 12 52 / .7)` из файла исчезают.

    Побочные эффекты смены токенов (`.btn--ghost`, рамки новостей, `.resources-panel`, тень `video-embed.css`) приняты в CONTEXT.md: ничего не компенсировать и чужие файлы не править.

    Тесты в `src/components/layout/primitives.test.tsx` (test-as-you-go, в этом же заходе):

    7. Импортировать `readFileSync` из `node:fs` и `resolve` из `node:path`. Добавить модульные константы с текстом файлов: `GLOBAL_CSS` (`src/styles/global.css`), `PRIMITIVES_CSS` (`src/components/layout/primitives.css`), `HEADER_CSS` (`src/components/layout/Header.css`), `MAP_CSS` (`src/components/map/map.css`), `COUNTERS_TSX` (`src/components/map/Counters.tsx`); все через `resolve(process.cwd(), ...)`. Добавить два хелпера: `flat(css)` схлопывает любые пробельные последовательности в один пробел; `block(css, head)` возвращает тело первого правила, чей заголовок вместе с открывающей скобкой равен `head` (например `".glass-card::before {"`), то есть текст между этим `head` и ближайшей `}` в `flat(css)`; при отсутствии правила бросает ошибку с именем селектора. Комментарий над хелперами на русском: vitest настроен с `css: false`, поэтому значения проверяются по тексту исходника, как в motionPolicy.test.ts.

    8. Новый `describe("токены и утилиты стекла (GLASS-01, GLASS-03)")` с тестами: (а) `flat(GLOBAL_CSS)` содержит три строки токенов из пункта 1 дословно (с `;`); (б) `block(GLOBAL_CSS, "@utility glass {")` содержит `linear-gradient(145deg, rgb(255 255 255 / .045), transparent 30%), var(--glass-surface);`, `border: 1px solid var(--glass-border);`, `border-radius: var(--radius-card);`, `box-shadow: var(--shadow-card);`, `backdrop-filter: blur(14px) saturate(112%);`; (в) `block(GLOBAL_CSS, "@utility glass-resource {")` содержит `linear-gradient(180deg, rgb(255 255 255 / .075), transparent 34%), linear-gradient(145deg, rgb(49 41 77 / .44), rgb(18 12 52 / .62));`, `border: 1px solid var(--glass-border);`, `border-radius: var(--radius-card);`, `box-shadow: var(--shadow-card);`, `backdrop-filter: blur(14px) saturate(125%);`; (г) `GLOBAL_CSS` не содержит `rgb(48 63 131 / .86)` и `rgb(184 192 230 / .22)`; (д) `block(PRIMITIVES_CSS, ".glass-card::before {")` содержит `inset 1px 1px 0 rgb(255 255 255 / .075), inset -1px -1px 0 rgb(3 2 18 / .30);` и `opacity: .58;`; (е) `block(PRIMITIVES_CSS, ".glass-card--interactive:hover {")` содержит `border-color: rgb(143 157 214 / .34);` и `inset 0 1px 0 rgb(255 255 255 / .055), 0 24px 52px rgb(3 2 18 / .30);`, а `block(PRIMITIVES_CSS, ".glass-card--interactive {")` содержит `border-color 420ms cubic-bezier(0.22, 1, 0.36, 1)`.

    9. Новый `describe("стекло не трогает шапку и счётчики (GLASS-05)")`: (а) `HEADER_CSS` не содержит `var(--glass-`, `var(--shadow-card)` и `radius-card`; (б) `MAP_CSS` не содержит `var(--glass-`, `var(--shadow-card)`; (в) `COUNTERS_TSX` не содержит подстроки `glass`; (г) `block(MAP_CSS, ".counter {")` существует (правило на месте). Комментарий: файлы читаются, но не правятся; шапка держит собственные литералы стекла, счётчики совпадают с оригиналом ещё с v1.0.

    Существующие тесты `GlassCard` (классы `glass-card glass`, `glass-card--interactive`) оставить без изменений.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/primitives.test.tsx src/styles/motionPolicy.test.ts src/components/map/Counters.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rgb(48 63 131 / .86)" src/styles/global.css` выводит 0; `grep -c "rgb(184 192 230 / .22)" src/styles/global.css` выводит 0
    - `grep -c "@utility glass-resource" src/styles/global.css` выводит 1; `grep -c "blur(14px) saturate(125%)" src/styles/global.css` выводит 2 (обе формы backdrop-filter)
    - `grep -c -- "--shadow-card: inset 0 1px 0 rgb(255 255 255 / .035), 0 20px 46px rgb(3 2 18 / .24);" src/styles/global.css` выводит 1
    - `grep -c "prefers-reduced-motion" src/styles/global.css` выводит 1, как до правки; `grep -c "animation: hero-beam 3s linear infinite;" src/styles/global.css` выводит 2; `grep -c "background-size: 7px 7px" src/styles/global.css` выводит 1; `grep -c "outline: 2px solid var(--color-horizon-200)" src/styles/global.css` выводит 2; `grep -c "@property --ov-hero-beam" src/styles/global.css` выводит 1
    - `npx vitest run src/styles/motionPolicy.test.ts` проходит без правок этого теста
    - `grep -c "rgb(143 157 214 / .34)" src/components/layout/primitives.css` выводит 1; `grep -c "rgb(184 192 230 / .34)" src/components/layout/primitives.css` выводит 0
    - `grep -c "readFileSync" src/components/layout/primitives.test.tsx` выводит не меньше 1; в файле есть строки `glass-resource` и `GLASS-05`
    - `git status --porcelain` показывает изменёнными только `src/styles/global.css`, `src/components/layout/primitives.css`, `src/components/layout/primitives.test.tsx`; `src/styles/tokens.css` не изменён
    - Команда verify завершается с кодом 0: все тесты трёх файлов зелёные, `Counters.test.tsx` не правился
  </acceptance_criteria>
  <done>Токены и утилита `glass` в global.css совпадают со спецификацией GLASS-01, утилита `glass-resource` существует со значениями GLASS-03, блик и ховер `.glass-card` соответствуют спецификации, тесты primitives проверяют это по тексту CSS и подтверждают независимость шапки и счётчиков; motionPolicy и Counters зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Карточки шагов About и триптих Involve на стекле оригинала, тесты About и Involve (GLASS-02, GLASS-04)</name>
  <files>src/components/about/about.css, src/components/involve/involve.css, src/components/about/About.test.tsx, src/components/involve/Involve.test.tsx</files>
  <read_first>
    - src/components/about/about.css (правила `.ab-step`, `.ab-step::after`, `.ab-step-rule`, блок `@media (hover: hover) and (pointer: fine)`)
    - src/components/involve/involve.css (переменные `.inv-section`, `.inv-triptych`, `.inv-card`, `.inv-slot`, три медиа-блока и блок `@media (hover: hover)`)
    - src/components/about/About.test.tsx и src/components/involve/Involve.test.tsx (стиль существующих тестов)
    - src/components/layout/primitives.test.tsx после Task 1 (хелперы `flat` и `block`: повторить их локально в каждом тестовом файле, общий тестовый модуль не заводить, чтобы не создавать новых файлов вне списка)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, пункты GLASS-02 и GLASS-04
    - docs/research/v1.1/orig-rules.css, правила `#ov-involve .ov-involve-triptych`, `#ov-involve .ov-involve-item` (радиус `frame-radius - 4px`, свой border у каждой карточки)
    - .planning/phases/07-glass-and-titles/07-CONTEXT.md, раздел «Стекло»
  </read_first>
  <action>
    `src/components/about/about.css` (per GLASS-02): карточка шага берёт поверхность, рамку, тень и blur из утилиты `glass` и своего `background` не объявляет (сейчас его и нет: проверить и оставить так). Правки:

    1. `.ab-step`: заменить `transition: border-color var(--ui-transition);` на `transition: border-color 420ms cubic-bezier(0.22, 1, 0.36, 1), box-shadow 420ms cubic-bezier(0.22, 1, 0.36, 1);`. Комментарий над правилом обновить: стекло приходит из утилиты `glass` (фаза 7), карточка добавляет только линию, разделитель и ховер.
    2. Внутри существующего `@media (hover: hover) and (pointer: fine)` правило `.ab-step:hover`: `border-color: rgb(143 157 214 / .34); box-shadow: inset 0 1px 0 rgb(255 255 255 / .055), 0 24px 52px rgb(3 2 18 / .30);`. Старый литерал `rgb(184 192 230 / .34)` из файла исчезает.
    3. `.ab-step::after` (акцентная линия 96px от `left: 24px`), `.ab-step:hover::after`, `.ab-step-rule` и `.ab-step:hover .ab-step-rule` не меняются. `--ui-transition` секции остаётся для линии и разделителя.

    `src/components/involve/involve.css` (per GLASS-04), структура `.inv-triptych` / `.inv-slot` / `.inv-card` сохраняется:

    4. В `.inv-section`: `--triptych-radius: 18px;` (было 24px); удалить `--triptych-seam` и `--triptych-surface` (их заменяют литералы на `.inv-card`). `--triptych-media-saturation`, `--focus-ring`, `--ui-transition`, `--inv-top`, `--inv-bottom` остаются.
    5. `.inv-triptych` (базовое правило): `display: grid; padding: 4px; gap: 4px; border-radius: var(--triptych-radius); overflow: hidden; -webkit-backdrop-filter: none; backdrop-filter: none; box-shadow: 0 34px 76px rgb(2 2 12 / .58), 0 0 54px rgb(59 77 161 / .22);`. Поверхность и рамка приходят из утилиты `glass`, размытие рамке не нужно: под ней карточки со своей поверхностью. Комментарий на русском: рамка 18px, карточки 14px, 4px зазор между ними даёт кольцо рамки как в оригинале.
    6. `.inv-card` (базовое правило, остаётся до медиа-блоков): `position: relative; min-width: 0; overflow: hidden; border-radius: calc(var(--triptych-radius) - 4px); border: 1px solid rgb(239 237 245 / .15); background: rgb(33 26 62 / .48); transition: background-color var(--ui-transition), border-color var(--ui-transition);`. Радиус 14px получается из 18px минус 4px, как у `.ov-involve-item` оригинала.
    7. Шов теперь несёт рамка каждой карточки: удалить правило `.inv-slot + .inv-slot { border-top ... }` из блока 768–1023px и правило `.inv-slot + .inv-slot { border-left ...; border-top: 0 }` из блока ≥1024px. `.inv-slot { display: grid; min-width: 0 }` остаётся.
    8. Мобильный блок `@media (max-width: 767px)`: у `.inv-triptych` оставить `gap: 16px; padding: 0; background: none; border: 0; box-shadow: none; overflow: visible;` (строки `backdrop-filter: none` из него убрать, они теперь в базовом правиле); правило `.inv-card` внутри этого блока удалить целиком (поверхность, рамка и радиус уже в базовом правиле; `var(--glass-border)` в involve.css больше не встречается).
    9. В существующий блок `@media (hover: hover)` добавить `.inv-card:hover { background: rgb(49 41 77 / .54); }` рядом с правилами `.inv-card:hover .inv-art`.
    10. `.inv-triptych-wrap::before` (световое поле, `calc(var(--triptych-radius) + 18px)`) и `.inv-section::before/::after` не меняются.

    Тесты (в этом же заходе). В обоих файлах локально объявить `flat` и `block` как в primitives.test.tsx и константу с текстом CSS через `readFileSync(resolve(process.cwd(), "src/components/<секция>/<файл>.css"), "utf8")`.

    11. `src/components/about/About.test.tsx`, новый `describe("стекло карточек шагов (GLASS-02)")`: (а) три элемента `.ab-step` в разметке имеют классы `glass-card` и `glass` (поверхность из утилиты); (б) `block(ABOUT_CSS, ".ab-step {")` не содержит `background`; (в) `block(ABOUT_CSS, ".ab-step:hover {")` содержит `border-color: rgb(143 157 214 / .34);` и `0 24px 52px rgb(3 2 18 / .30)`; (г) `block(ABOUT_CSS, ".ab-step {")` содержит `border-color 420ms cubic-bezier(0.22, 1, 0.36, 1)`; (д) `flat(ABOUT_CSS)` содержит `.ab-step::after {` и `.ab-step-rule {`, а `ABOUT_CSS` не содержит `rgb(184 192 230 / .34)`.

    12. `src/components/involve/Involve.test.tsx`, новый `describe("стекло триптиха (GLASS-04)")`: (а) элемент `.inv-triptych` имеет классы `glass-card` и `glass`, внутри ровно три `article.inv-card`; (б) `block(INVOLVE_CSS, ".inv-triptych {")` содержит `backdrop-filter: none;`, `padding: 4px;` и `box-shadow: 0 34px 76px rgb(2 2 12 / .58), 0 0 54px rgb(59 77 161 / .22);`; (в) `block(INVOLVE_CSS, ".inv-section {")` содержит `--triptych-radius: 18px;`; (г) `block(INVOLVE_CSS, ".inv-card {")` содержит `background: rgb(33 26 62 / .48);`, `border: 1px solid rgb(239 237 245 / .15);` и `border-radius: calc(var(--triptych-radius) - 4px);`; (д) `block(INVOLVE_CSS, ".inv-card:hover {")` содержит `background: rgb(49 41 77 / .54);`; (е) `INVOLVE_CSS` не содержит `.inv-slot + .inv-slot`, `var(--glass-border)` и `--triptych-surface`.

    Существующие тесты обоих файлов не менять.
  </action>
  <verify>
    <automated>npx vitest run src/components/about/About.test.tsx src/components/involve/Involve.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "background" src/components/about/about.css` не изменился по сравнению с main для правила `.ab-step` (в теле `.ab-step {` нет строки `background`); `grep -c "rgb(184 192 230 / .34)" src/components/about/about.css` выводит 0; `grep -c "rgb(143 157 214 / .34)" src/components/about/about.css` выводит 1
    - `grep -c "\.ab-step::after" src/components/about/about.css` не меньше 1; `grep -c "\.ab-step-rule" src/components/about/about.css` не меньше 2
    - `grep -c "backdrop-filter: none" src/components/involve/involve.css` выводит 2 (обе формы, в базовом `.inv-triptych`); `grep -c "0 34px 76px rgb(2 2 12 / .58), 0 0 54px rgb(59 77 161 / .22)" src/components/involve/involve.css` выводит 1
    - `grep -c "rgb(33 26 62 / .48)" src/components/involve/involve.css` выводит 1; `grep -c "rgb(239 237 245 / .15)" src/components/involve/involve.css` выводит 1; `grep -c "rgb(49 41 77 / .54)" src/components/involve/involve.css` выводит 1; `grep -c -- "--triptych-radius: 18px" src/components/involve/involve.css` выводит 1
    - `grep -c "inv-slot + .inv-slot" src/components/involve/involve.css` выводит 0; `grep -c "var(--glass-border)" src/components/involve/involve.css` выводит 0; `grep -c "prefers-reduced-motion" src/components/about/about.css src/components/involve/involve.css` выводит 0 для обоих
    - В `About.test.tsx` и `Involve.test.tsx` есть строки `GLASS-02` и `GLASS-04` соответственно и `readFileSync`
    - `git status --porcelain` показывает изменёнными только четыре файла задачи (плюс три файла Task 1); `Involve.tsx`, `InvolveCard.tsx`, `StepCard.tsx`, `About.tsx` не изменены
    - Команда verify завершается с кодом 0
  </acceptance_criteria>
  <done>Карточки шагов About стоят на поверхности утилиты `glass` без своего фона, с ховером рамки `rgb(143 157 214 / .34)` за 420ms и с линией и разделителем на месте; рамка триптиха без размытия несёт тень оригинала, карточки `.inv-card` имеют поверхность `rgb(33 26 62 / .48)`, шов `1px solid rgb(239 237 245 / .15)`, радиус 14px и ховер `rgb(49 41 77 / .54)`; тесты About и Involve проверяют это по тексту CSS и разметке.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет новых | План меняет только статический CSS и тесты; пользовательский ввод, сеть и секреты не задействованы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Правки вне зоны владения фазы (tokens.css, map.css, resources.css, компоненты секций) ломают слияние фаз 7–12 | mitigate | Список запрещённых файлов в `<ownership>`; проверка `git status --porcelain` в критериях приёмки каждой задачи и в `<verification>` |
| T-07-02 | Denial of Service | `backdrop-filter` на трёх карточках About, панелях ресурсов и (до фазы 9) карточке формы | accept | Столько же слоёв blur, сколько в оригинале; рамка триптиха размытие теряет, слоёв становится меньше, чем в v1.0 |
| T-07-03 | Tampering | Тесты сверяют CSS по тексту: переформатирование (перенос строк, отступы) роняет проверки | mitigate | Хелпер `flat` схлопывает пробелы, проверяются тела правил через `block`, а не позиции строк |
| T-07-SC | Tampering | npm installs | mitigate | Новых пакетов нет, `npm ci`/`npm install` в фазе не выполняются; `package.json` и `package-lock.json` в `git status` не появляются |
</threat_model>

<verification>
Гейт плана (все команды с кодом 0):

1. `npx tsc -b`
2. `npx vitest run src/components/layout/primitives.test.tsx src/components/about/About.test.tsx src/components/involve/Involve.test.tsx src/styles/motionPolicy.test.ts src/components/map/Counters.test.tsx`
3. `npm run lint`
4. `git status --porcelain` перечисляет только семь файлов из `files_modified`; `git diff --name-only -- src/styles/tokens.css src/components/map src/components/form src/components/resources src/components/news src/components/layout/Header.css src/components/about/StepCard.tsx src/components/about/About.tsx src/components/involve/Involve.tsx src/components/involve/InvolveCard.tsx` пуст

Наблюдаемое поведение (сверка компьютед-стилей в браузере остаётся фазе 13 по QA-03; здесь по желанию исполнителя через Playwright MCP после `npx vite build && npx vite preview`): у `.ab-step` `getComputedStyle().backgroundImage` начинается с `linear-gradient(145deg, rgba(255, 255, 255, 0.045), rgba(0, 0, 0, 0) 30%), linear-gradient(180deg, rgba(49, 41, 77, 0.44), rgba(18, 12, 52, 0.62))`, `borderColor` равен `rgba(239, 237, 245, 0.18)`, `backdropFilter` равен `blur(14px) saturate(1.12)`; у `.inv-triptych` `backdropFilter` равен `none`; у `.inv-card` `backgroundColor` равен `rgba(33, 26, 62, 0.48)`; у `.counter` стили те же, что на main.
</verification>

<success_criteria>
- GLASS-01: `global.css` держит токены и утилиту `glass` со значениями спецификации; блик и ховер `.glass-card` в `primitives.css` совпадают со спецификацией; старые литералы индиго из обоих файлов удалены
- GLASS-02: `.ab-step` без собственного `background`, ховер рамки `rgb(143 157 214 / .34)` и тень `0 24px 52px rgb(3 2 18 / .30)` за 420ms; `.ab-step::after` и `.ab-step-rule` на месте
- GLASS-03: утилита `glass-resource` в `global.css` с поверхностью и `blur(14px) saturate(125%)` из спецификации; тест primitives проверяет её тело
- GLASS-04: `.inv-triptych` без `backdrop-filter` и с тенью рамки; `.inv-card` с поверхностью `rgb(33 26 62 / .48)`, швом `1px solid rgb(239 237 245 / .15)`, радиусом 14px и ховером `rgb(49 41 77 / .54)`
- GLASS-05: `Counters.test.tsx` зелёный без правок; `map.css`, `Counters.tsx`, `Header.css` не изменены и не ссылаются на токены стекла
- `npx tsc -b`, перечисленные тесты и `npm run lint` проходят; изменены только файлы из `files_modified`
</success_criteria>

<output>
Create `.planning/phases/07-glass-and-titles/07-01-SUMMARY.md` when done
</output>
