---
phase: 07-glass-and-titles
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/layout/primitives.css
  - src/components/layout/GradientTitle.tsx
  - src/styles/global.css
  - src/components/about/About.tsx
  - src/components/layout/primitives.test.tsx
  - src/components/about/About.test.tsx
  - src/components/involve/Involve.test.tsx
autonomous: true
requirements: [GLASS-06]

must_haves:
  truths:
    - "Заголовки карты, формы, участия, новостей и ресурсов плоские `rgb(239 237 245)`: класс `.gradient-title--section` без `background-clip` и без градиента, тот же кегль `var(--text-section)`, вес 900, `letter-spacing: -0.035em`"
    - "Заголовок About градиентный `linear-gradient(104deg, rgb(227 175 210) 2%, rgb(143 157 214) 52%, rgb(123 194 199))` через `variant=\"section-gradient\"`; только About его использует"
    - "Hero h1 остаётся градиентным: `.gradient-title--hero` сохраняет свой градиент и `background-clip: text`"
    - "Вызовы `GradientTitle` в MapSection, LightForm, Involve, News, Resources и Section не изменены: плоский вид приходит через класс, а не через правку чужих файлов"
  artifacts:
    - path: "src/components/layout/primitives.css"
      provides: "Плоский .gradient-title--section и новый .gradient-title--section-gradient"
      contains: ".gradient-title--section-gradient"
    - path: "src/components/layout/GradientTitle.tsx"
      provides: "Тип variant с тремя значениями hero | section | section-gradient"
      contains: "section-gradient"
    - path: "src/components/about/About.tsx"
      provides: "Единственный вызов variant=\"section-gradient\""
      contains: "variant=\"section-gradient\""
    - path: "src/styles/global.css"
      provides: "Токен --gradient-title удалён, градиент живёт литералом в primitives.css"
    - path: "src/components/layout/primitives.test.tsx"
      provides: "Тесты варианта section-gradient и текстовые проверки правил заголовков"
      contains: "section-gradient"
    - path: "src/components/about/About.test.tsx"
      provides: "Проверка, что h2 About несёт класс gradient-title--section-gradient"
      contains: "gradient-title--section-gradient"
    - path: "src/components/involve/Involve.test.tsx"
      provides: "Проверка, что h2 Involve плоский: класс gradient-title--section без section-gradient"
      contains: "gradient-title--section"
  key_links:
    - from: "src/components/layout/GradientTitle.tsx"
      to: "src/components/layout/primitives.css"
      via: "className gradient-title gradient-title--${variant}"
      pattern: "gradient-title--\\$\\{variant\\}"
    - from: "src/components/about/About.tsx"
      to: "src/components/layout/GradientTitle.tsx"
      via: "variant=\"section-gradient\""
      pattern: "variant=\"section-gradient\""
    - from: "src/components/layout/primitives.css (.gradient-title--hero, .gradient-title--section-gradient)"
      to: "background-clip: text"
      via: "общее правило клипа для двух градиентных вариантов"
      pattern: "\\.gradient-title--hero,\\s*\\.gradient-title--section-gradient \\{"
---

<objective>
Сделать заголовки секций плоскими белыми `rgb(239 237 245)`, оставив градиент только у About через новый вариант `section-gradient`, и не тронуть hero h1. Вызовы `GradientTitle` в чужих секциях остаются как есть: плоский вид приходит через класс `.gradient-title--section`.

Purpose: у оригинала градиентный только заголовок About, остальные h2 плоские; сейчас все секции градиентные (GLASS-06).
Output: обновлённые `primitives.css` (правила `.gradient-title*`), `GradientTitle.tsx`, `About.tsx`, `global.css` (удалён `--gradient-title`), тесты primitives, About и Involve.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-glass-and-titles/07-CONTEXT.md
@.planning/phases/07-glass-and-titles/07-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@src/components/layout/primitives.css
@src/components/layout/GradientTitle.tsx
@src/components/about/About.tsx

<ownership>
Фаза 7 идёт параллельно с фазами 8–12 в отдельном worktree от общего `main`. Редактировать разрешено только файлы из `files_modified` этого плана. Запрещено трогать даже ради одной строки: `src/components/map/MapSection.tsx`, `src/components/form/LightForm.tsx`, `src/components/involve/Involve.tsx`, `src/components/news/News.tsx`, `src/components/resources/Resources.tsx`, `src/components/hero/Hero.tsx`, `src/components/layout/Section.tsx` (изменение не требуется: он и так вызывает `variant="section"`), `src/styles/tokens.css`, `map.css`, `light-form.css`, `resources.css`, `news/*`, `about.css`, `involve.css`. В `primitives.css` менять только правила `.gradient-title*`; правила `.glass-card*` и `.btn*` уже приведены планом 07-01 и не трогаются. В `global.css` менять только строку токена `--gradient-title`. Остальное `src/styles/motionPolicy.test.ts` сверяет буквально и оно остаётся байт в байт: блок `@media (prefers-reduced-motion: reduce)`, `@property --ov-hero-beam`, правила `.btn[data-beam]` (`animation: hero-beam 3s linear infinite;` ровно 2 раза, `background-size: 7px 7px` ровно 1 раз), `outline: 2px solid var(--color-horizon-200)` ровно 2 раза, токены reveal, `overflow-x: clip`. Реестр `data-anim` не расширять. Новых `@media (prefers-reduced-motion: reduce)` блоков ни в одном файле.

Вызовы `<GradientTitle as="h2" variant="section">` в MapSection, LightForm, Involve, News, Resources и Section остаются без правок: они становятся плоскими через CSS-класс.
</ownership>

<interfaces>
<!-- Текущее состояние. Взято из исходников, исследовать заново не нужно. -->

Из src/components/layout/GradientTitle.tsx:
```tsx
export function GradientTitle({ as, variant, children, className, id }: {
  as: "h1" | "h2";
  variant: "hero" | "section";
  children: ReactNode;
  className?: string;
  id?: string;
}) // className = `gradient-title gradient-title--${variant} font-display` + (className ? " " + className : "")
```

Из src/components/layout/primitives.css (правила заголовков):
```css
.gradient-title { -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; -webkit-box-decoration-break: clone; box-decoration-break: clone; text-wrap: balance; font-weight: 900; padding: 0.08em 0 0.12em; margin: -0.08em 0 -0.12em; }
.gradient-title--section { background-image: var(--gradient-title); font-size: var(--text-section); letter-spacing: -0.035em; line-height: 1.05; }
.gradient-title--hero { background-image: linear-gradient(90deg, #d28ebe 0%, ... rgb(84 164 172 / 0.88) 100%); font-size: clamp(2.75rem, 8vw, 4.5rem); letter-spacing: -0.055em; line-height: 0.94; }
```

Из src/styles/global.css (`:root`): `--gradient-title: linear-gradient(104deg, #e3afd2 2%, #8f9dd6 52%, #7bc2c7 98%);` (единственный потребитель: `.gradient-title--section`).

Вызовы `GradientTitle` в проекте: Hero.tsx (`as="h1" variant="hero" id="hero-title"`), About.tsx (`as="h2" variant="section" className="mt-2" id="about-title"`), MapSection.tsx, LightForm.tsx, Involve.tsx, News.tsx, Resources.tsx, Section.tsx (все `variant="section"`).

Хелперы `flat` и `block` и константы `GLOBAL_CSS`, `PRIMITIVES_CSS` уже есть в primitives.test.tsx после плана 07-01; в About.test.tsx и Involve.test.tsx тоже есть локальные `flat`/`block`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Плоский section и градиентный section-gradient в primitives.css и GradientTitle.tsx, тесты primitives (GLASS-06)</name>
  <files>src/components/layout/primitives.css, src/components/layout/GradientTitle.tsx, src/styles/global.css, src/components/layout/primitives.test.tsx</files>
  <read_first>
    - src/components/layout/primitives.css (правила `.gradient-title`, `.gradient-title--section`, `.gradient-title--hero`)
    - src/components/layout/GradientTitle.tsx
    - src/styles/global.css (строка `--gradient-title` в `:root`)
    - src/components/layout/primitives.test.tsx (describe `GradientTitle`, хелперы `flat`/`block` из плана 07-01)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, пункт GLASS-06
    - .planning/phases/07-glass-and-titles/07-CONTEXT.md, раздел «Заголовки»
  </read_first>
  <action>
    `src/components/layout/primitives.css` (per GLASS-06), только правила заголовков:

    1. Базовое `.gradient-title` оставляет общее для всех вариантов: `text-wrap: balance; font-weight: 900; padding: 0.08em 0 0.12em; margin: -0.08em 0 -0.12em;`. Компенсация padding/margin остаётся у всех вариантов, включая плоский: она нейтральна по высоте, а её удаление сдвинуло бы плоские заголовки формы, карты и ресурсов относительно v1.0 (в каскаде она перекрывает `mt-2`). Из базового правила убрать `-webkit-background-clip`, `background-clip`, `-webkit-text-fill-color`, `-webkit-box-decoration-break`, `box-decoration-break`. Комментарий на русском: класс исторически называется gradient-title, но с фазы 7 градиент несут только варианты hero и section-gradient.

    2. Новое общее правило для двух градиентных вариантов, селектор ровно `.gradient-title--hero,\n.gradient-title--section-gradient {` с телом: `-webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent; -webkit-box-decoration-break: clone; box-decoration-break: clone;`.

    3. `.gradient-title--section` (плоский): `color: rgb(239 237 245); font-size: var(--text-section); letter-spacing: -0.035em; line-height: 1.05;`. Ни `background-image`, ни `background-clip` в правиле нет.

    4. Новое `.gradient-title--section-gradient` сразу после плоского: `background-image: linear-gradient(104deg, rgb(227 175 210) 2%, rgb(143 157 214) 52%, rgb(123 194 199)); font-size: var(--text-section); letter-spacing: -0.035em; line-height: 1.05;` (литерал из спецификации, тот же кегль и трекинг, что у плоского).

    5. `.gradient-title--hero` не меняется по значениям (градиент 90deg, `font-size: clamp(2.75rem, 8vw, 4.5rem)`, `letter-spacing: -0.055em`, `line-height: 0.94`); клип он теперь получает из правила пункта 2.

    `src/components/layout/GradientTitle.tsx`:

    6. Тип `variant` расширить до `"hero" | "section" | "section-gradient"`. JSDoc на русском над полем: `section` плоский белый заголовок секции, `section-gradient` градиентный заголовок About, `hero` градиент h1 первого экрана. Формирование className не менять: `gradient-title gradient-title--${variant} font-display`.

    `src/styles/global.css`:

    7. Удалить строку `--gradient-title: ...` из `:root` (единственный потребитель ушёл; градиент About живёт литералом в primitives.css). `--gradient-brand` и `--gradient-action` остаются.

    Тесты в `src/components/layout/primitives.test.tsx` (в этом же заходе):

    8. В describe `GradientTitle` добавить тест: `variant="section-gradient"` с `as="h2"` рендерит h2 с классами `gradient-title` и `gradient-title--section-gradient` и без класса `gradient-title--section`; существующие тесты hero и section остаются, к тесту section добавить `expect(heading).not.toHaveClass("gradient-title--section-gradient")`.

    9. Новый `describe("плоские и градиентные заголовки (GLASS-06)")`: (а) `block(PRIMITIVES_CSS, ".gradient-title--section {")` содержит `color: rgb(239 237 245);`, `font-size: var(--text-section);`, `letter-spacing: -0.035em;` и не содержит `background`; (б) `block(PRIMITIVES_CSS, ".gradient-title--section-gradient {")` содержит `background-image: linear-gradient(104deg, rgb(227 175 210) 2%, rgb(143 157 214) 52%, rgb(123 194 199));` и `font-size: var(--text-section);`; (в) `block(PRIMITIVES_CSS, ".gradient-title--hero, .gradient-title--section-gradient {")` содержит `background-clip: text;` и `-webkit-text-fill-color: transparent;`; (г) `block(PRIMITIVES_CSS, ".gradient-title {")` не содержит `background-clip` и `text-fill-color`, но содержит `font-weight: 900;`; (д) `block(PRIMITIVES_CSS, ".gradient-title--hero {")` содержит `font-size: clamp(2.75rem, 8vw, 4.5rem);`; (е) `GLOBAL_CSS` не содержит `--gradient-title:`, а `PRIMITIVES_CSS` не содержит `var(--gradient-title)`.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/primitives.test.tsx src/styles/motionPolicy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gradient-title--section-gradient" src/components/layout/primitives.css` выводит 2 (общее правило клипа и собственное правило)
    - `grep -c "color: rgb(239 237 245);" src/components/layout/primitives.css` выводит 1
    - `grep -c "linear-gradient(104deg, rgb(227 175 210) 2%, rgb(143 157 214) 52%, rgb(123 194 199))" src/components/layout/primitives.css` выводит 1
    - `grep -c "background-clip: text" src/components/layout/primitives.css` выводит 1 (только в общем правиле двух градиентных вариантов; `-webkit-background-clip` считается отдельно)
    - `grep -c "var(--gradient-title)" src/components/layout/primitives.css` выводит 0; `grep -c -- "--gradient-title:" src/styles/global.css` выводит 0; `grep -c -- "--gradient-brand:" src/styles/global.css` выводит 1
    - `grep -c '"hero" | "section" | "section-gradient"' src/components/layout/GradientTitle.tsx` выводит 1
    - `npx tsc -b` завершается с кодом 0 (существующие вызовы `variant="section"` и `variant="hero"` компилируются)
    - `npx vitest run src/styles/motionPolicy.test.ts` проходит без правок этого теста; `grep -c "prefers-reduced-motion" src/styles/global.css` выводит 1; `grep -c "animation: hero-beam 3s linear infinite;" src/styles/global.css` выводит 2
    - Команда verify завершается с кодом 0; в primitives.test.tsx есть строка `GLASS-06`
    - `git status --porcelain` показывает изменёнными только четыре файла задачи
  </acceptance_criteria>
  <done>`.gradient-title--section` плоский `rgb(239 237 245)` без клипа, `.gradient-title--section-gradient` несёт градиент спецификации, hero сохраняет свой градиент через общее правило клипа, `GradientTitle` принимает три варианта, токен `--gradient-title` удалён, тесты primitives проверяют классы и текст правил.</done>
</task>

<task type="auto">
  <name>Task 2: Градиентный заголовок About, плоские заголовки остальных секций, тесты About и Involve (GLASS-06)</name>
  <files>src/components/about/About.tsx, src/components/about/About.test.tsx, src/components/involve/Involve.test.tsx</files>
  <read_first>
    - src/components/about/About.tsx (вызов `GradientTitle` с `id="about-title"`)
    - src/components/about/About.test.tsx и src/components/involve/Involve.test.tsx (тесты `aria-labelledby`, стиль)
    - src/components/layout/GradientTitle.tsx после Task 1
    - .planning/phases/07-glass-and-titles/07-CONTEXT.md, раздел «Заголовки»
    - .planning/ROADMAP.md, фаза 7, критерий успеха 4
  </read_first>
  <action>
    1. `src/components/about/About.tsx`: в вызове `GradientTitle` заменить `variant="section"` на `variant="section-gradient"`; `as="h2"`, `className="mt-2"` и `id="about-title"` не менять. Другие правки в файле не нужны.

    2. `src/components/about/About.test.tsx`, новый тест в существующем describe «Секция About»: h2 с именем `aboutCopy.title` имеет классы `gradient-title` и `gradient-title--section-gradient` и не имеет класса `gradient-title--section`; комментарий: единственный градиентный заголовок секции по GLASS-06.

    3. `src/components/involve/Involve.test.tsx`, новый тест в существующем describe: h2 с именем `involveCopy.title` имеет класс `gradient-title--section` и не имеет `gradient-title--section-gradient`; комментарий: заголовок участия плоский, как у карты, формы, новостей и ресурсов; вызов в Involve.tsx не менялся.

    4. Проверить grep-ом, что `variant="section-gradient"` встречается ровно в одном файле проекта (About.tsx) и что `MapSection.tsx`, `LightForm.tsx`, `Involve.tsx`, `News.tsx`, `Resources.tsx`, `Section.tsx`, `Hero.tsx` не изменены (`git diff --name-only` по ним пуст).
  </action>
  <verify>
    <automated>npx vitest run src/components/about/About.test.tsx src/components/involve/Involve.test.tsx src/components/layout/primitives.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl 'variant="section-gradient"' src --include='*.tsx' | grep -v test` выводит ровно одну строку: `src/components/about/About.tsx`
    - `grep -c 'variant="section"' src/components/about/About.tsx` выводит 0
    - `grep -c 'variant="section"' src/components/map/MapSection.tsx src/components/form/LightForm.tsx src/components/involve/Involve.tsx src/components/news/News.tsx src/components/resources/Resources.tsx src/components/layout/Section.tsx` выводит 1 для каждого файла, файлы не изменены по `git diff --name-only`
    - `grep -c 'variant="hero"' src/components/hero/Hero.tsx` выводит 1, файл не изменён
    - В About.test.tsx есть строка `gradient-title--section-gradient`; в Involve.test.tsx есть строка `gradient-title--section`
    - Команда verify завершается с кодом 0
    - `git status --porcelain` показывает изменёнными только три файла задачи (плюс четыре файла Task 1)
  </acceptance_criteria>
  <done>About рендерит h2 с классом `gradient-title--section-gradient`, остальные секции продолжают вызывать `variant="section"` и получают плоский заголовок через CSS; тесты About и Involve фиксируют оба состояния.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Нет новых | Правка типа пропса, CSS-классов и статических правил; пользовательский ввод, сеть и секреты не задействованы |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | Правка вызовов `GradientTitle` в чужих секциях (MapSection, LightForm, Involve, News, Resources, Hero) создаёт конфликты слияния с фазами 8–12 | mitigate | Плоский вид приходит через класс `.gradient-title--section`; критерий приёмки Task 2 проверяет `git diff --name-only` по этим файлам |
| T-07-05 | Denial of Service | Расширение типа `variant` без обновления всех вызовов роняет `tsc -b` | mitigate | Расширение объединения типов обратно совместимо; `npx tsc -b` в критериях приёмки Task 1 |
| T-07-06 | Tampering | Удаление токена `--gradient-title` при скрытом потребителе в другой ветке | accept | В `main` единственный потребитель `.gradient-title--section`; при конфликте в фазе 13 токен восстанавливается одной строкой |
| T-07-SC | Tampering | npm installs | mitigate | Новых пакетов нет, `npm ci`/`npm install` не выполняются; `package.json` и `package-lock.json` в `git status` не появляются |
</threat_model>

<verification>
Гейт плана (все команды с кодом 0):

1. `npx tsc -b`
2. `npx vitest run src/components/layout src/components/about src/components/involve src/styles/motionPolicy.test.ts`
3. `npm run lint`
4. `git status --porcelain` вместе с файлами плана 07-01 перечисляет только файлы из `files_modified` обоих планов; `git diff --name-only -- src/components/map src/components/form src/components/news src/components/resources src/components/hero src/components/layout/Section.tsx src/styles/tokens.css` пуст

Наблюдаемое поведение (сверка в браузере остаётся фазе 13 по QA-03; по желанию через Playwright MCP после `npx vite build && npx vite preview`): у `#about-title` `getComputedStyle().backgroundImage` равен `linear-gradient(104deg, rgb(227, 175, 210) 2%, rgb(143, 157, 214) 52%, rgb(123, 194, 199))` и `webkitTextFillColor` равен `rgba(0, 0, 0, 0)`; у `#involve-title`, `#map-title`, `#form-title` и заголовков новостей и ресурсов `color` равен `rgb(239, 237, 245)`, `backgroundImage` равен `none`, `webkitTextFillColor` равен `rgb(239, 237, 245)`; у `#hero-title` `backgroundImage` начинается с `linear-gradient(90deg, rgb(210, 142, 190) 0%`.
</verification>

<success_criteria>
- GLASS-06: `.gradient-title--section` плоский `rgb(239 237 245)` без `background-clip`; `.gradient-title--section-gradient` с градиентом `linear-gradient(104deg, rgb(227 175 210) 2%, rgb(143 157 214) 52%, rgb(123 194 199))`; hero h1 градиентный без изменений
- `GradientTitle` принимает `variant: "hero" | "section" | "section-gradient"`; `variant="section-gradient"` только в About.tsx; остальные вызовы не менялись
- Токен `--gradient-title` удалён из global.css, потребителей нет
- Тесты primitives, About и Involve проверяют классы и текст правил; `npx tsc -b`, перечисленные тесты и `npm run lint` проходят; изменены только файлы из `files_modified`
</success_criteria>

<output>
Create `.planning/phases/07-glass-and-titles/07-02-SUMMARY.md` when done
</output>
