---
phase: 12-footer-column
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Wordmark.tsx
  - src/components/layout/Wordmark.test.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Footer.css
  - src/components/layout/Footer.test.tsx
  - src/data/copy.ts
autonomous: true
requirements: [FOOT-01, FOOT-02]
tags: [footer, layout, css, wordmark]

must_haves:
  truths:
    - "Футер выстроен одной колонкой по центру: логотип, под ним подпись, ниже ссылки столбиком, ниже юридический текст"
    - "Контейнер футера имеет ширину min(100% - 32px, 1152px), gap clamp(20px, 3vw, 34px) и padding-block clamp(72px, 10vw, 124px) сверх скоса"
    - "Логотип в футере занимает clamp(190px, 26vw, 300px) на clamp(72px, 10vw, 108px) со свечением drop-shadow(0 0 22px rgb(91 90 214 / .13)), а логотип в шапке выглядит как до фазы"
    - "Ссылки стоят столбиком по центру с gap 8px, 700 14px rgb(248 247 251 / .92), при наведении и фокусе rgb(170 217 220), открываются в новой вкладке с rel=\"noopener noreferrer\" и sr-only подсказкой"
    - "Над юридическим текстом нет разделительной линии; текст .75rem, line-height 1.7, rgb(239 237 245 / .66), text-wrap: balance, ширина min(100%, 680px)"
    - "Скос, волны (::after) и гало (.site-footer__halo) футера на месте, атрибуты data-anim не изменились"
  artifacts:
    - path: "src/components/layout/Wordmark.tsx"
      provides: "Опциональный проп size со значением по умолчанию, не меняющим шапку"
      contains: "wordmark--footer"
    - path: "src/components/layout/Wordmark.test.tsx"
      provides: "Тесты пропа size: по умолчанию класса нет, size=\"footer\" добавляет wordmark--footer"
      contains: "wordmark--footer"
    - path: "src/components/layout/Footer.tsx"
      provides: "Разметка одной колонки: Wordmark size=\"footer\" → p.site-footer__caption → nav.site-footer__links → p.site-footer__legal внутри .site-footer__inner"
      contains: "size=\"footer\""
    - path: "src/components/layout/Footer.css"
      provides: "Flex-колонка .site-footer__inner, правило .wordmark--footer, столбик ссылок, юридический текст без рамки"
      contains: "min(100% - 32px, 1152px)"
    - path: "src/components/layout/Footer.test.tsx"
      provides: "Тест порядка узлов, тест rel=\"noopener noreferrer\", текстовая проверка Footer.css"
      contains: "site-footer__inner"
  key_links:
    - from: "src/components/layout/Footer.tsx"
      to: "src/components/layout/Wordmark.tsx"
      via: "проп size=\"footer\""
      pattern: "<Wordmark size=\"footer\""
    - from: "src/components/layout/Wordmark.tsx"
      to: "src/components/layout/Footer.css"
      via: "класс wordmark--footer на корне, правило в Footer.css (global.css принадлежит фазе 7)"
      pattern: "\\.wordmark--footer"
    - from: "src/components/layout/Footer.tsx"
      to: "src/data/copy.ts"
      via: "copy.footer.caption, links, legal, linksLabel, newTabHint"
      pattern: "copy\\.footer\\."
    - from: "src/components/layout/Footer.test.tsx"
      to: "src/components/layout/Footer.css"
      via: "readFileSync (vitest css: false)"
      pattern: "Footer\\.css"
---

<objective>
Перестроить футер в одну колонку по центру, как в оригинале onevoice27.org: логотип, подпись, ссылки столбиком, юридический текст. Grid `site-footer__grid` и верхняя линия над юридическим текстом уходят; скос, волны и гало остаются. `Wordmark` получает опциональный проп размера, шапка при этом не меняется.

Purpose: пункт 6 правок пользователя и раздел 6 (FOOT) спецификации v1.1: футер оригинала выстроен колонкой, наш сейчас двухколоночный grid с разделителем.
Output: обновлённые `Footer.tsx`, `Footer.css`, `Wordmark.tsx`, тесты `Footer.test.tsx` и `Wordmark.test.tsx` зелёные, `npx tsc -b` и `npm run lint` без ошибок.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-footer-column/12-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@docs/research/v1.1/dom-ov-main-site-footer.html
@src/components/layout/Footer.tsx
@src/components/layout/Footer.css
@src/components/layout/Footer.test.tsx
@src/components/layout/Wordmark.tsx
@src/components/layout/Wordmark.test.tsx
@src/data/copy.ts

## Правило владения файлами (фаза 12 идёт параллельно с фазами 7–11)

Редактировать только файлы из `files_modified` выше. Запрещено трогать `Header.tsx`, `Header.css`, `global.css`, `tokens.css`, `primitives.css` и любые файлы других секций даже ради одной строки. Правило для чужого селектора (в этой фазе `.wordmark--footer`, базовые стили `.wordmark` лежат в `global.css`) кладётся в свой `Footer.css`. Новые файлы не создаются. Цвета в CSS пишутся литералами `rgb(r g b / a)` из спецификации, а не токенами `--color-*`.

## Ограничения из существующих тестов (нарушение роняет `npm test`)

- `src/styles/motionPolicy.test.ts` требует, чтобы `prefers-reduced-motion` встречался только в `global.css`. Фаза не добавляет анимаций, поэтому в `Footer.css` блок `@media (prefers-reduced-motion: reduce)` НЕ добавляется (пункт CONTEXT.md про единый блок в своём CSS здесь не применяется: новых анимаций нет).
- Тот же тест требует, чтобы `target="_blank"` и `rel="noopener noreferrer"` стояли в одной строке TSX.
- Тот же тест запрещает `outline: none` и его варианты во всех файлах.
- Реестр `data-anim` закрыт: `wave` на `<footer>` и `halo` на `.site-footer__halo` сохраняются без изменений.
- Vitest настроен с `css: false` (см. `vite.config.ts`): импорт CSS в тестах даёт пустую строку, вычисленные стили в jsdom недоступны. Проверка CSS делается чтением файла через `readFileSync(resolve(process.cwd(), "src/components/layout/Footer.css"), "utf8")`, как в `Header.test.tsx` (строки 9–14).

<interfaces>
<!-- Контракты, которые исполнитель использует без разведки. -->

From src/components/layout/Wordmark.tsx (текущее состояние):
```typescript
type WordmarkProps = {
  className?: string;
  tone?: "gradient" | "solid";   // "solid" снимает text-gradient-brand с названия; шапка вызывает <Wordmark tone="solid" />
};
export function Wordmark({ className, tone = "gradient" }: WordmarkProps): JSX.Element;
// Корень: <span class="wordmark {className}"> с детьми .wordmark__title и .wordmark__tagline
```

From src/data/copy.ts (блок footer, менять не требуется):
```typescript
copy.footer = {
  caption: "Официальный сайт Церкви христиан адвентистов седьмого дня",
  linksLabel: "Внешние ссылки",
  newTabHint: "(откроется в новой вкладке)",
  links: [{ label, href }, { label, href }],   // esd.adventist.org, onevoice27.org
  legal: "© 2026 Евро-Азиатский дивизион Церкви христиан-адвентистов седьмого дня",
};
```

Из src/styles/global.css и tokens.css (только чтение, доступны через `@reference "../../styles/global.css"` в Footer.css):
```css
.wordmark { display: inline-flex; flex-direction: column; gap: 8px; }
.wordmark__title { font-size: var(--text-wordmark); }   /* clamp(1.125rem, 2.2vw, 1.375rem) */
.wordmark__tagline { font-size: var(--text-tagline); letter-spacing: 0.2em; }
--footer-wedge: clamp(24px, 3vw, 44px);
--breakpoint-desktop: 768px;   /* использовать как theme(--breakpoint-desktop) */
--breakpoint-nav: 1024px;      /* theme(--breakpoint-nav) */
--dur-ui: 240ms; --ease-ui: cubic-bezier(0.22, 1, 0.36, 1);
```

Из docs/research/v1.1/orig-rules.css (правила оригинала, строки 391–396):
```css
#ov-main-footer-content > footer > div { display: flex; width: min(100% - 32px, 1152px); margin-inline: auto; flex-direction: column; align-items: center; justify-content: center; gap: clamp(20px, 3vw, 34px); text-align: center; }
... > div:first-child { display: grid; width: clamp(190px, 26vw, 300px); min-height: clamp(72px, 10vw, 108px); place-items: center; }  /* ::before с логотипом и drop-shadow(0 0 22px rgb(91 90 214 / .13)) */
... > div:nth-child(2) { width: min(100%, 680px); }  /* подпись: text-base → md:text-lg → lg:text-xl, leading-normal */
... > div:last-child { width: min(100%, 680px); font-size: 0.75rem; line-height: 1.7; text-wrap: balance; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Проп размера у Wordmark с тестами</name>
  <files>src/components/layout/Wordmark.tsx, src/components/layout/Wordmark.test.tsx</files>
  <read_first>
    - src/components/layout/Wordmark.tsx (весь файл, 20 строк)
    - src/components/layout/Wordmark.test.tsx (весь файл: четыре теста, паттерн `container.firstElementChild`)
    - src/components/layout/Header.tsx строка 95 (`<Wordmark tone="solid" />`, вызов не меняется)
    - .planning/phases/12-footer-column/12-CONTEXT.md, блок «Логотип»
  </read_first>
  <action>
    Добавить в `WordmarkProps` опциональный проп `size?: "default" | "footer"` со значением по умолчанию `"default"` (имя пропа и значения на усмотрение из CONTEXT.md, здесь зафиксированы для тестов). При `size === "footer"` корневой `span` получает дополнительный класс-модификатор `wordmark--footer`; при `"default"` набор классов корня не меняется, чтобы шапка (`Header.tsx` вызывает `<Wordmark tone="solid" />` без `size`) осталась байт в байт прежней в DOM. Порядок классов корня: `wordmark`, затем `wordmark--footer` (если есть), затем переданный `className`. Проп `tone` и его логика не меняются: футер оставляет градиент названия (`text-gradient-brand`).

    Стили модификатора в этом файле не описываются и в `global.css` не добавляются: CSS для `.wordmark--footer` (ширина `clamp(190px, 26vw, 300px)`, `min-height: clamp(72px, 10vw, 108px)`, `filter: drop-shadow(...)`) живёт в `Footer.css` (Task 2), потому что `global.css` принадлежит фазе 7. В JSDoc пропа на русском указать: размеры модификатора задаёт `Footer.css`, значение по умолчанию оставляет шапку без изменений.

    В `Wordmark.test.tsx` добавить тесты:
    1. «по умолчанию не вешает модификатор размера»: `render(<Wordmark />)`, корень имеет класс `wordmark` и не имеет `wordmark--footer`.
    2. «size="footer" добавляет класс wordmark--footer, сохраняя wordmark и градиент»: корень содержит оба класса, название по-прежнему с `text-gradient-brand`.
    3. «size="footer" совмещается с className и tone="solid"»: корень содержит `wordmark`, `wordmark--footer` и переданный класс; название без `text-gradient-brand`.
    Существующие четыре теста не редактировать.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/layout/Wordmark.test.tsx src/components/layout/Header.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Wordmark.tsx` экспортирует `Wordmark` с пропом `size?: "default" | "footer"`, по умолчанию `"default"`
    - `render(<Wordmark />)`: `container.firstElementChild.className === "wordmark"` (без модификатора и без лишних пробелов)
    - `render(<Wordmark size="footer" />)`: корень имеет классы `wordmark` и `wordmark--footer`, название имеет `text-gradient-brand`
    - `render(<Wordmark size="footer" tone="solid" className="x" />)`: корень имеет `wordmark`, `wordmark--footer`, `x`; название без `text-gradient-brand`
    - `Header.tsx` и `Header.css` не изменены (`git status` не показывает их); `npx vitest run src/components/layout/Header.test.tsx` зелёный
    - В `Wordmark.test.tsx` не меньше 7 тестов, все зелёные
  </acceptance_criteria>
  <done>Проп `size` работает, шапка без изменений, `Wordmark.test.tsx` и `Header.test.tsx` зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Футер одной колонкой по центру (разметка и CSS)</name>
  <files>src/components/layout/Footer.tsx, src/components/layout/Footer.css, src/data/copy.ts</files>
  <read_first>
    - src/components/layout/Footer.tsx (весь файл)
    - src/components/layout/Footer.css (весь файл: `.site-footer`, `::after`, `__halo` не трогать; `__inner`, `__grid`, `__brand`, `__caption`, `__links`, `__legal` перестраиваются)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел «6. FOOT» (строки 237–249): значения CSS, источник истины
    - docs/research/v1.1/orig-rules.css строки 391–396 (`#ov-main-footer-content > footer > div ...`)
    - docs/research/v1.1/dom-ov-main-site-footer.html (порядок блоков оригинала: логотип, подпись, nav, юр. текст)
    - docs/research/v1.1/orig-footer.jpeg (визуальный ориентир)
    - src/data/copy.ts строки 31–40 (блок `footer`)
    - src/styles/motionPolicy.test.ts строки 29–37 и 51–60 (ограничения на `prefers-reduced-motion` и `rel`)
  </read_first>
  <action>
    **Footer.tsx (FOOT-01, FOOT-02).** Разметку внутри `<footer className="site-footer" data-anim="wave">` привести к одной колонке. `.site-footer__halo` с `data-anim="halo"` и `aria-hidden="true"` остаётся первым узлом без изменений. У `.site-footer__inner` убрать Tailwind-классы `mx-auto max-w-[72rem] px-4 md:px-8` (ширину и центровку теперь даёт CSS). Внутри `.site-footer__inner` ровно четыре прямых ребёнка в этом порядке, без обёрток `site-footer__grid` и `site-footer__brand`:
    1. `<Wordmark size="footer" />` (тон по умолчанию, градиент названия остаётся; проп из Task 1);
    2. `<p className="site-footer__caption">{copy.footer.caption}</p>`;
    3. `<nav className="site-footer__links" aria-label={copy.footer.linksLabel}>` с `<ul>` и `<li>` по `copy.footer.links`, ссылка `<a href={link.href} target="_blank" rel="noopener noreferrer">` в одной строке (требование motionPolicy.test), внутри `{link.label}` и `<span className="sr-only">{copy.footer.newTabHint}</span>`;
    4. `<p className="site-footer__legal">{copy.footer.legal}</p>` (вместо `div > p`).
    Комментарии в коде на русском; существующие комментарии про волны и гало сохранить. Порядок узлов соответствует D-решению CONTEXT.md «логотип → подпись → ссылки → юридический текст».

    **Footer.css (FOOT-01).** Блоки `.site-footer`, `.site-footer::after`, `.site-footer__halo` и `@reference` не менять: скос через `clip-path`, `margin-top` с `--footer-wedge`, `padding-block: calc(var(--footer-wedge) + clamp(72px, 10vw, 124px)) clamp(72px, 10vw, 124px)` уже совпадают со спецификацией. Удалить правила `.site-footer__grid`, `.site-footer__brand` (если есть) и медиазапрос с `grid-template-columns: 1fr auto`. Переписать:
    - `.site-footer__inner`: `position: relative; z-index: 1; display: flex; flex-direction: column; align-items: center; justify-content: center; width: min(100% - 32px, 1152px); margin-inline: auto; gap: clamp(20px, 3vw, 34px); text-align: center;`.
    - Новое правило `.site-footer .wordmark--footer` (правило для чужого класса живёт здесь по правилу владения): `width: clamp(190px, 26vw, 300px); min-height: clamp(72px, 10vw, 108px); justify-content: center; align-items: center; filter: drop-shadow(0 0 22px rgb(91 90 214 / .13));`. Крупнее кегль под ширину логотипа оригинала: `.site-footer .wordmark--footer .wordmark__title { font-size: clamp(1.25rem, 3.2vw, 1.875rem); white-space: nowrap; }` и `.site-footer .wordmark--footer .wordmark__tagline { font-size: clamp(.625rem, 1.1vw, .8125rem); }` (на усмотрение: границы clamp можно сдвинуть, но название «Единый голос 27» не должно переноситься при ширине 190px на вьюпорте 390px и не должно выходить за 300px на 1440px; расчёт: ~15 символов × ~0.62em при Onest 900).
    - `.site-footer__caption`: `width: min(100%, 680px); margin: 0; font-size: 1rem; line-height: normal; color: rgb(248 247 251 / .92);` плюс `@media (width >= theme(--breakpoint-desktop)) { font-size: 1.125rem }` и `@media (width >= theme(--breakpoint-nav)) { font-size: 1.25rem }` (16px на мобильном, 18px от 768, 20px от 1024). Старые `margin-top: 16px`, `max-width: 42ch`, `--text-sm`, `line-height: 1.6`, цвет `.66` убрать.
    - `.site-footer__links ul` (FOOT-02): `display: flex; flex-direction: column; align-items: center; gap: 8px; margin: 0; padding: 0; list-style: none;`.
    - `.site-footer__links a`: `display: inline-block; font-size: .875rem; font-weight: 700; line-height: 1.6; color: rgb(248 247 251 / .92); text-decoration: none; transition: color var(--dur-ui) var(--ease-ui);` без `padding-block` (gap 8px задаёт интервал сам). Ховер и фокус: `.site-footer__links a:hover, .site-footer__links a:focus-visible { color: rgb(170 217 220); }` литералом, а не `var(--color-horizon-200)`.
    - `.site-footer__legal` (FOOT-02): `width: min(100%, 680px); margin: 0; font-size: .75rem; line-height: 1.7; color: rgb(239 237 245 / .66); text-wrap: balance;` без `border-top`, `margin-top`, `padding-top`.
    В файле не должно появиться `prefers-reduced-motion`, `outline: none`, токенов `--color-*` для цветов и селекторов `site-footer__grid`/`site-footer__brand`. Комментарии в CSS на русском, коротко: почему правило `.wordmark--footer` живёт здесь.

    **copy.ts.** Блок `footer` проверить по разметке оригинала (`dom-ov-main-site-footer.html`): подпись «Официальный сайт Церкви христиан адвентистов седьмого дня» и юридическая строка уже соответствуют, менять не требуется. Файл правится только если при сверке обнаружится расхождение текста, и только внутри блока `footer`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/layout/Footer.test.tsx src/styles/motionPolicy.test.ts && npx tsc -b</automated>
  </verify>
  <acceptance_criteria>
    - `Footer.tsx`: у `.site-footer__inner` ровно четыре прямых ребёнка в порядке `.wordmark` (с классом `wordmark--footer`), `p.site-footer__caption`, `nav.site-footer__links`, `p.site-footer__legal`; строки `site-footer__grid` и `site-footer__brand` в файле отсутствуют; `grep -c 'max-w-\[72rem\]' Footer.tsx` даёт 0
    - `Footer.tsx`: строка со ссылкой содержит одновременно `target="_blank"` и `rel="noopener noreferrer"`; `data-anim="wave"` на footer и `data-anim="halo"` на `.site-footer__halo` сохранены
    - `Footer.css` содержит: `width: min(100% - 32px, 1152px)`, `gap: clamp(20px, 3vw, 34px)`, `flex-direction: column`, `width: clamp(190px, 26vw, 300px)`, `min-height: clamp(72px, 10vw, 108px)`, `drop-shadow(0 0 22px rgb(91 90 214 / .13))`, `rgb(170 217 220)`, `rgb(239 237 245 / .66)`, `text-wrap: balance`, `min(100%, 680px)` не меньше двух раз, `font-size: .75rem` (или `0.75rem`) для `.site-footer__legal`
    - `Footer.css` не содержит: `site-footer__grid`, `site-footer__brand`, `grid-template-columns`, `border-top`, `prefers-reduced-motion`, `var(--color-horizon-200)`
    - `Footer.css`: блоки `.site-footer {`, `.site-footer::after {`, `.site-footer__halo {` совпадают с состоянием до задачи (`git diff` по этим блокам пустой)
    - Пять существующих тестов `Footer.test.tsx` зелёные без правок; `motionPolicy.test.ts` зелёный; `npx tsc -b` без ошибок
    - `git status` не показывает `Header.tsx`, `Header.css`, `global.css`, `tokens.css`, `primitives.css`
  </acceptance_criteria>
  <done>Футер рендерится одной колонкой с точными значениями спецификации, старые тесты футера и политика движения зелёные, типы собираются.</done>
</task>

<task type="auto">
  <name>Task 3: Тесты порядка узлов и CSS футера, гейт фазы</name>
  <files>src/components/layout/Footer.test.tsx</files>
  <read_first>
    - src/components/layout/Footer.test.tsx (весь файл: пять тестов, `within(footer)`, паттерн запроса `nav` по имени «Внешние ссылки»)
    - src/components/layout/Header.test.tsx строки 1–14 (чтение CSS с диска через `readFileSync` и `resolve(process.cwd(), ...)`)
    - src/components/layout/Footer.tsx и Footer.css после Task 2 (селекторы и значения, которые проверяются)
    - .planning/ROADMAP.md, Phase 12, Success Criteria 3 (тест порядка узлов и `rel`)
  </read_first>
  <action>
    Дополнить `Footer.test.tsx`, существующие пять тестов не менять (третий тест уже проверяет `target="_blank"`, `rel="noopener noreferrer"` и sr-only подсказку; он остаётся проверкой FOOT-02 по `rel`).

    Добавить константу `FOOTER_CSS = readFileSync(resolve(process.cwd(), "src/components/layout/Footer.css"), "utf8")` с комментарием, что vitest настроен с `css: false` (как в `Header.test.tsx`).

    Новые тесты (структура на усмотрение, названия на русском):
    1. «выстраивает колонку: логотип → подпись → ссылки → юридический текст» (FOOT-01): взять `inner = footer.querySelector(".site-footer__inner")`, найти узлы `inner.querySelector(".wordmark")`, подпись через `getByText(copy caption)` (родитель `p.site-footer__caption`), `nav` через `getByRole("navigation", { name: "Внешние ссылки" })`, юр. текст через `getByText("© 2026 ...")`. Утверждать, что каждый из четырёх узлов является прямым ребёнком `inner` и их индексы в `Array.from(inner.children)` равны `[0, 1, 2, 3]`; `inner.children.length === 4`; `footer.querySelector(".site-footer__grid")` и `.site-footer__brand` равны `null`.
    2. «увеличивает вордмарк в футере и оставляет градиент названия» (FOOT-01): `.wordmark` внутри footer имеет класс `wordmark--footer`, название «Единый голос 27» имеет `text-gradient-brand`.
    3. «описывает колонку и логотип значениями оригинала в Footer.css» (FOOT-01): `FOOTER_CSS` содержит `width: min(100% - 32px, 1152px)`, `gap: clamp(20px, 3vw, 34px)`, `flex-direction: column`, `clamp(190px, 26vw, 300px)`, `clamp(72px, 10vw, 108px)`, `drop-shadow(0 0 22px rgb(91 90 214 / .13))`, `clamp(72px, 10vw, 124px)`; `FOOTER_CSS.match(/min\(100%, 680px\)/g)` длиной не меньше 2; не содержит `site-footer__grid` и `grid-template-columns`.
    4. «ставит ссылки столбиком и убирает линию над юридическим текстом» (FOOT-02): `FOOTER_CSS` содержит `rgb(170 217 220)`, `rgb(248 247 251 / .92)`, `gap: 8px`, `text-wrap: balance`, `rgb(239 237 245 / .66)`; не содержит `border-top`; не содержит `prefers-reduced-motion` (политика в global.css). Дополнительно: `ul` внутри `nav` содержит ровно 2 `li`, юр. текст находится в `p.site-footer__legal` без предка `.site-footer__legal` типа `div`.

    После зелёных тестов прогнать гейт фазы: `npx tsc -b`, `npx vitest run src/components/layout/Footer.test.tsx src/components/layout/Wordmark.test.tsx src/components/layout/Header.test.tsx src/styles/motionPolicy.test.ts`, `npm run lint`. Затем один полный `npx vitest run` (386+ тестов, около минуты) как финальная проверка ветки. Всё, что упало вне файлов фазы, не чинить в чужих файлах: записать в SUMMARY как стык для фазы 13.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx tsc -b && npx vitest run src/components/layout/Footer.test.tsx src/components/layout/Wordmark.test.tsx src/components/layout/Header.test.tsx src/styles/motionPolicy.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `Footer.test.tsx` содержит не меньше 9 тестов (5 старых + 4 новых), все зелёные
    - Тест порядка утверждает индексы `[0, 1, 2, 3]` для `.wordmark`, `.site-footer__caption`, `.site-footer__links`, `.site-footer__legal` среди `inner.children` и `inner.children.length === 4`
    - Тест `rel` (существующий) по-прежнему проверяет `target="_blank"`, `rel="noopener noreferrer"` и `sr-only` подсказку у каждой ссылки в nav «Внешние ссылки»
    - CSS-тесты читают `Footer.css` через `readFileSync` и проверяют строки из спецификации, перечисленные выше
    - `npx tsc -b` exit 0; `npm run lint` exit 0; `npx vitest run` по четырём файлам exit 0; полный `npx vitest run` без падений в файлах фазы 12
    - `git status` показывает изменения только в шести файлах из `files_modified` (copy.ts при отсутствии правок не меняется)
  </acceptance_criteria>
  <done>Тесты футера подтверждают порядок узлов, `rel` и значения CSS; гейт `tsc`, `vitest`, `lint` зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| страница → внешние сайты | ссылки футера ведут на esd.adventist.org и onevoice27.org в новой вкладке |
| статические тексты `copy.ts` → DOM | тексты захардкожены, пользовательского ввода в футере нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `Footer.tsx`, внешние ссылки `target="_blank"` | mitigate | `rel="noopener noreferrer"` в той же строке; проверяется существующим тестом Footer и `motionPolicy.test.ts` (reverse tabnabbing) |
| T-12-02 | Information disclosure | `Referer` при переходе на внешние сайты | mitigate | `noreferrer` в `rel` (та же строка) |
| T-12-03 | Spoofing | `copy.footer.links` (href) | accept | тексты и адреса статичны в репозитории, без пользовательского ввода и без рантайм-подстановки |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | фаза не устанавливает пакеты; `package.json` и lock не меняются (проверка через `git status`) |
</threat_model>

<verification>
- `npx vitest run src/components/layout/Footer.test.tsx src/components/layout/Wordmark.test.tsx src/components/layout/Header.test.tsx src/styles/motionPolicy.test.ts` зелёный
- `npx tsc -b` и `npm run lint` без ошибок
- `git status`: изменены только `Wordmark.tsx`, `Wordmark.test.tsx`, `Footer.tsx`, `Footer.css`, `Footer.test.tsx` (и `copy.ts` только при правке текста); `Header.tsx`, `Header.css`, `global.css`, `tokens.css`, `primitives.css` не тронуты
- `grep -c 'prefers-reduced-motion' src/components/layout/Footer.css` даёт 0; `grep -c 'border-top' src/components/layout/Footer.css` даёт 0
- Визуальная приёмка на 1440×900 и 390×844 выполняется фазой 13 (Playwright по `docs/qa/SMOKE.md`), в этой фазе не требуется
</verification>

<success_criteria>
1. Футер выстроен колонкой по центру шириной `min(100% - 32px, 1152px)` с gap `clamp(20px, 3vw, 34px)` и padding-block `clamp(72px, 10vw, 124px)`: логотип `clamp(190px, 26vw, 300px)` × `clamp(72px, 10vw, 108px)` с `drop-shadow(0 0 22px rgb(91 90 214 / .13))`, под ним подпись `min(100%, 680px)`, ниже ссылки, ниже юридический текст `.75rem` `rgb(239 237 245 / .66)`; скос, волны и гало на месте (FOOT-01)
2. Ссылки стоят столбиком по центру с gap 8px, 700 14px `rgb(248 247 251 / .92)`, при наведении и фокусе `rgb(170 217 220)`; над юридическим текстом нет разделительной линии (FOOT-02)
3. Тест Footer подтверждает порядок узлов «логотип → подпись → ссылки → юридический текст» и `rel="noopener noreferrer"` у внешних ссылок; шапка с тем же `Wordmark` выглядит как раньше: `Header.test.tsx` зелёный без правок, `Header.tsx`/`Header.css` не изменены (FOOT-01)
4. `npx tsc -b`, `npx vitest run` по затронутым тестам и `npm run lint` без ошибок
</success_criteria>

<output>
Create `.planning/phases/12-footer-column/12-01-SUMMARY.md` when done
</output>
