---
phase: 02-hero-and-map
plan: 05
type: execute
wave: 3
depends_on: ["02-03"]
files_modified:
  - src/lib/useInViewOnce.ts
  - src/lib/useInViewOnce.test.tsx
  - src/lib/useCountUp.ts
  - src/lib/useCountUp.test.tsx
  - src/components/map/Counters.tsx
  - src/components/map/Counters.test.tsx
autonomous: true
requirements: [MAP-04]
must_haves:
  truths:
    - "Счётчики «ЧЕЛОВЕК» и «ГРУПП» анимируют счёт от 0 до значения за 1600ms с easeOutCubic при первом появлении в вьюпорте (threshold .4), один раз"
    - "При prefers-reduced-motion счётчики сразу показывают конечное значение без анимации"
    - "Скринридер получает конечное форматированное значение через aria-live, промежуточные кадры анимации ему не читаются"
    - "После добавления огонька (фаза 3) счётчик сразу показывает новое значение"
  artifacts:
    - path: src/lib/useCountUp.ts
      provides: "хук count-up с rAF, easing и reduced motion"
      exports: [useCountUp]
    - path: src/lib/useInViewOnce.ts
      provides: "однократное срабатывание IntersectionObserver"
      exports: [useInViewOnce]
    - path: src/components/map/Counters.tsx
      provides: "счётчики с count-up"
      contains: "useCountUp("
  key_links:
    - from: src/components/map/Counters.tsx
      to: src/lib/useCountUp.ts
      via: "useCountUp(counts.people, { active: inView, reduced })"
      pattern: "useCountUp\\("
    - from: src/components/map/Counters.tsx
      to: src/lib/useInViewOnce.ts
      via: "useInViewOnce(ref, 0.4)"
      pattern: "useInViewOnce\\("
    - from: src/lib/useCountUp.ts
      to: src/lib/easing.ts
      via: "easeOutCubic по умолчанию"
      pattern: "easeOutCubic"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** увидеть эффектный hero с вращающимся глобусом и кнопкой «Зажечь свой свет», а ниже живую карту ЕАД с огоньками, счётчиками и переключением стран, **so that** почувствовать масштаб движения по дивизиону и перейти к форме, чтобы зажечь свой свет.

Этот план доводит счётчики до контракта MAP-04: числа «оживают» при появлении в вьюпорте и остаются доступными.

<objective>
Добавить хуки `useInViewOnce` и `useCountUp`, подключить их к `Counters` из 02-03: анимация 1600ms easeOutCubic по IntersectionObserver с threshold .4, один раз; при reduced motion конечное значение сразу; aria-live читает только конечное значение.

Purpose: count-up превращает статистику в событие (FEATURES.md), а доступность требует не спамить скринридер промежуточными числами.
Output: два хука с тестами, обновлённые `Counters.tsx` и `Counters.test.tsx`, зелёные `npm test` и `npm run build`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/02-hero-and-map/02-UI-SPEC.md
@.planning/phases/02-hero-and-map/02-03-SUMMARY.md
@src/components/map/Counters.tsx
@src/components/map/Counters.test.tsx
@src/lib/easing.ts
@src/lib/useReducedMotion.ts
@src/lib/format.ts
@src/state/lights.tsx
@src/test/setup.ts

<interfaces>
<!-- Из 02-02 и 02-03 (уже в кодовой базе). -->

```typescript
// src/lib/easing.ts
export function easeOutCubic(t: number): number;
// src/lib/useReducedMotion.ts
export function usePrefersReducedMotion(): boolean;
// src/lib/format.ts
export function formatCount(n: number): string;
// src/state/lights.tsx
export function useLights(): { lights: Light[]; counts: { people: number; groups: number }; addLight: (input) => void };
export function LightsProvider(props: { children: ReactNode; initialLights?: Light[] }): JSX.Element;
// src/components/map/Counters.tsx (02-03): разметка .counters > article.counter--people / .counter--groups,
// p.counter__label с span.counter__dot и подписью, p.counter__value > span[aria-live="polite"] с formatCount(...)
```

Контракт этого плана:
```typescript
// src/lib/useInViewOnce.ts
import type { RefObject } from "react";
export function useInViewOnce<T extends Element>(ref: RefObject<T | null>, threshold?: number): boolean;
// threshold по умолчанию 0.4; без IntersectionObserver в окружении возвращает true сразу; после первого пересечения disconnect()

// src/lib/useCountUp.ts
export interface CountUpOptions {
  duration?: number;               // мс, по умолчанию 1600
  active?: boolean;                // старт анимации; по умолчанию true
  reduced?: boolean;               // prefers-reduced-motion; по умолчанию false
  easing?: (t: number) => number;  // по умолчанию easeOutCubic
}
export function useCountUp(target: number, opts?: CountUpOptions): number; // отображаемое целое
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: хуки useInViewOnce и useCountUp с тестами на фейковых таймерах</name>
  <files>src/lib/useInViewOnce.ts, src/lib/useInViewOnce.test.tsx, src/lib/useCountUp.ts, src/lib/useCountUp.test.tsx</files>
  <read_first>
    - src/lib/easing.ts (`easeOutCubic`)
    - src/test/setup.ts (мок IntersectionObserver фазы 1: вызывает ли `observe` коллбэк; в тестах хука подменять глобал собственным классом)
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md, строка «Счётчики» в Motion Contract (1600ms, easeOutCubic, threshold .4, один раз, reduced → конечное значение)
    - .planning/phases/02-hero-and-map/02-CONTEXT.md, пункт «Счётчики (MAP-04)»
  </read_first>
  <behavior>
    - Test 1 (useInViewOnce.test.tsx): если `window.IntersectionObserver` временно `undefined`, `renderHook` даёт `true` сразу
    - Test 2: с моком IO, который при `observe` синхронно вызывает коллбэк с `[{ isIntersecting: true }]`, результат `true` и `disconnect` вызван один раз
    - Test 3: с моком IO, который вызывает коллбэк с `isIntersecting: false`, результат остаётся `false`, `disconnect` не вызван; конструктор получил `{ threshold: 0.4 }`
    - Test 4 (useCountUp.test.tsx): `useCountUp(1150, { reduced: true })` даёт `1150` сразу, `requestAnimationFrame` не вызывался
    - Test 5: `useCountUp(1150, { active: false })` даёт `0`
    - Test 6: с `vi.useFakeTimers()` и rAF, замоканным как `setTimeout(() => cb(Date.now()), 16)`: `useCountUp(1150, { active: true })` после `advanceTimersByTime(400)` даёт значение строго между 0 и 1150; после ещё 1300ms даёт ровно 1150; значения по кадрам не убывают
    - Test 7: после завершения анимации `rerender` с `target = 1151` даёт `1151` без новых кадров (мгновенно)
    - Test 8: `unmount` во время анимации вызывает `cancelAnimationFrame`
  </behavior>
  <action>
    Сначала оба тестовых файла (красные), затем реализация.

    `src/lib/useInViewOnce.ts`: `const [inView, setInView] = useState(() => typeof IntersectionObserver === "undefined")`; `useEffect` по `[ref, threshold]`: если уже `inView` или нет `ref.current` или нет IO, выйти; `const io = new IntersectionObserver(entries => { if (entries.some(e => e.isIntersecting)) { setInView(true); io.disconnect(); } }, { threshold })`; `io.observe(ref.current)`; cleanup `io.disconnect()`.

    `src/lib/useCountUp.ts`: `const { duration = 1600, active = true, reduced = false, easing = easeOutCubic } = opts`. State `value`, refs `frameRef`, `doneRef`, `startRef`, `fromRef`. Логика: если `reduced`, вернуть `target` (без state и rAF). `useEffect` по `[active, target, reduced, duration]`: если `reduced`, выйти; если `!active`, `setValue(0)` и выйти; если `doneRef.current`, `setValue(target)` и выйти (после первой анимации новые значения показываются сразу; UI-SPEC: анимация при первом появлении, один раз); иначе запустить анимацию: `fromRef = 0`, `startRef = null`, `frameRef = window.requestAnimationFrame(step)`, где `step(now)`: если `startRef === null`, `startRef = now`; `t = Math.min(1, (now - startRef) / duration)`; `setValue(Math.round(fromRef + (target - fromRef) * easing(t)))`; если `t < 1`, `frameRef = window.requestAnimationFrame(step)`, иначе `doneRef.current = true`. Cleanup эффекта: `window.cancelAnimationFrame(frameRef)`. Время брать из аргумента коллбэка rAF (тест передаёт `Date.now()` фейковых таймеров), не из `performance.now()`.

    В тестах rAF мокать через `vi.spyOn(window, "requestAnimationFrame").mockImplementation(cb => window.setTimeout(() => cb(Date.now()), 16) as unknown as number)` и `vi.spyOn(window, "cancelAnimationFrame").mockImplementation(id => window.clearTimeout(id))`; `vi.useFakeTimers()` в `beforeEach`, `vi.useRealTimers()` и `vi.restoreAllMocks()` в `afterEach`; продвижение времени через `act(() => { vi.advanceTimersByTime(ms) })`.
  </action>
  <verify>
    <automated>npm test -- src/lib/useInViewOnce.test.tsx src/lib/useCountUp.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/useInViewOnce.ts` содержит `export function useInViewOnce`, `new IntersectionObserver(`, `{ threshold }`, `disconnect()`
    - `src/lib/useCountUp.ts` содержит `export function useCountUp(`, `easeOutCubic`, `duration = 1600`, `window.requestAnimationFrame(`, `window.cancelAnimationFrame(`, `Math.round(`
    - `npm test -- src/lib/useInViewOnce.test.tsx src/lib/useCountUp.test.tsx` завершается с кодом 0, суммарно не меньше 8 тестов
  </acceptance_criteria>
  <done>useInViewOnce срабатывает один раз с threshold .4, useCountUp анимирует 1600ms с easeOutCubic, мгновенен при reduced motion и после первого прохода; оба покрыты тестами на фейковых таймерах.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: count-up в Counters с доступным aria-live и полный прогон (MAP-04)</name>
  <files>src/components/map/Counters.tsx, src/components/map/Counters.test.tsx</files>
  <read_first>
    - src/components/map/Counters.tsx и Counters.test.tsx (результат 02-03: разметка, классы, 4 теста сохраняются)
    - src/lib/useCountUp.ts, src/lib/useInViewOnce.ts (результат Task 1), src/lib/useReducedMotion.ts
    - .planning/phases/02-hero-and-map/02-UI-SPEC.md: строка «Счётчик» в Accessibility Contract и «Счётчики» в Motion Contract
  </read_first>
  <behavior>
    - Test 1 (сохранить из 02-03, адаптировав): при reduced motion `matches: true` и `initialLights={generateLights(27, 1150, 12)}` видимое число (`span[aria-hidden="true"]` внутри `.counter--people .counter__value`) равно `"1\u202F150"`, у групп `"12"`, `requestAnimationFrame` не вызывался
    - Test 2: без reduced motion и с моком IO, который никогда не вызывает коллбэк (мок фазы 1), видимое число `"0"`, а `span[aria-live="polite"]` содержит `"1\u202F150"`
    - Test 3: без reduced motion, с моком IO, вызывающим коллбэк `isIntersecting: true` при `observe`, и фейковыми таймерами с rAF-моком из Task 1: после `advanceTimersByTime(1700)` видимое число `"1\u202F150"`
    - Test 4: подписи «ЧЕЛОВЕК» и «ГРУПП», классы `counter--people`/`counter--groups` и ровно два `aria-live="polite"` сохраняются
    - Test 5: после `act(() => addLight({ type: "group", countryId: 643 }))` из тестового потребителя внутри того же провайдера при reduced motion видимое число групп становится `"13"`
  </behavior>
  <action>
    Сначала обновить `Counters.test.tsx` (красный), затем реализация.

    `src/components/map/Counters.tsx`: `const rootRef = useRef<HTMLDivElement>(null)`; `const inView = useInViewOnce(rootRef, 0.4)`; `const reduced = usePrefersReducedMotion()`; `const people = useCountUp(counts.people, { active: inView, reduced })`; `const groups = useCountUp(counts.groups, { active: inView, reduced })`. `ref={rootRef}` на `.counters`. Значение: `<p className="counter__value"><span aria-hidden="true">{formatCount(people)}</span><span className="sr-only" aria-live="polite">{formatCount(counts.people)}</span></p>` (видимые кадры анимации скрыты от скринридера, aria-live получает только конечное форматированное число; UI-SPEC требует aria-live вокруг числа и текст вместо canvas, оба условия соблюдены). Классы карточек, подписи и точки-индикаторы не менять.

    Затем `npm test` (весь набор) и `npm run build`. Если билд упал из-за одновременной записи `dist/` планом 02-04 той же волны, повторить один раз.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/map/Counters.tsx` содержит `useCountUp(`, `useInViewOnce(`, `usePrefersReducedMotion(`, `aria-live="polite"`, `aria-hidden="true"`, `formatCount(`, `0.4`
    - `grep -c 'aria-live="polite"' src/components/map/Counters.tsx` выводит 2
    - `npm test` завершается с кодом 0; `Counters.test.tsx` содержит не меньше 5 тестов
    - `npm run build` завершается с кодом 0
  </acceptance_criteria>
  <done>Счётчики анимируют счёт при первом появлении, мгновенны при reduced motion, читаются скринридером как конечное значение; весь набор тестов и билд зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LightsProvider.counts` → `Counters` | Числа из памяти страницы, ввода пользователя нет |
| IntersectionObserver / rAF → хуки | Браузерные API, только тайминг анимации |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Denial of Service | rAF-цикл useCountUp | mitigate | Цикл конечный (`t` достигает 1 за `duration`), один активный кадр, `cancelAnimationFrame` в cleanup, при reduced motion rAF не запускается |
| T-02-16 | Information Disclosure | aria-live | accept | Агрегаты синтетических данных |
| T-02-17 | Tampering | `formatCount(target)` | accept | Вход это число из редьюсера, строка собирается без HTML |
| T-02-SC | Tampering | npm installs | accept | Новых пакетов нет |
</threat_model>

<verification>
- `npm test` зелёный, включая `useInViewOnce.test.tsx`, `useCountUp.test.tsx`, `Counters.test.tsx`.
- `npm run build` зелёный.
- Ручная проверка (не блокирующая): `npm run preview`, прокрутить к карте: числа бегут от 0 до 694 и 248 за ~1.6s один раз; при эмуляции reduced motion значения появляются сразу.
</verification>

<success_criteria>
- MAP-04: счётчики показывают значения из состояния огоньков с форматированием U+202F, анимируют счёт при первом появлении (IO threshold .4, 1600ms, easeOutCubic), при reduced motion сразу конечное значение; aria-live читает конечное значение.
- Фаза 3 может рендерить `Counters` рядом с формой внутри `LightsProvider` и видеть рост счётчика после `addLight`.
</success_criteria>

<output>
Создать `.planning/phases/02-hero-and-map/02-05-SUMMARY.md` после выполнения.
</output>
