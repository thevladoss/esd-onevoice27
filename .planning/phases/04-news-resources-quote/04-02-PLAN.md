---
phase: 04-news-resources-quote
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/copy.quote.ts
  - src/components/quote/Quote.tsx
  - src/components/quote/WorldSilhouette.tsx
  - src/components/quote/Quote.test.tsx
autonomous: true
requirements: [QUOTE-01]
tags: [quote, d3-geo, svg, vitest]

must_haves:
  truths:
    - "Посетитель видит в секции #quote два абзаца цитаты из «Евангелизма» на градиентном фоне с полупрозрачным силуэтом карты мира"
    - "Под цитатой стоит подпись источника «Эллен Уайт, «Евангелизм», стр. 122», над цитатой eyebrow «Слово на дорогу»"
    - "Силуэт карты декоративный: скринридер его не читает (aria-hidden), клавиатура на него не попадает"
  artifacts:
    - path: "src/data/copy.quote.ts"
      provides: "eyebrow, два абзаца и cite цитаты"
      exports: ["quoteCopy"]
    - path: "src/components/quote/WorldSilhouette.tsx"
      provides: "статичный SVG всех стран world-atlas одним path через geoNaturalEarth1 + geoPath"
      exports: ["WorldSilhouette"]
    - path: "src/components/quote/Quote.tsx"
      provides: "секция #quote: градиентный фон, пятна, силуэт, figure/blockquote/figcaption"
      min_lines: 40
  key_links:
    - from: "src/components/quote/WorldSilhouette.tsx"
      to: "src/lib/geo.ts"
      via: "импорт FeatureCollection всех стран (loadWorld или world)"
      pattern: "from \"\\.\\./\\.\\./lib/geo\""
    - from: "src/components/quote/WorldSilhouette.tsx"
      to: "d3-geo"
      via: "geoNaturalEarth1().fitSize + geoPath"
      pattern: "geoNaturalEarth1"
    - from: "src/components/quote/Quote.tsx"
      to: "src/data/copy.quote.ts"
      via: "quoteCopy.paragraphs.map"
      pattern: "quoteCopy\\.paragraphs"
---

## Phase Goal

**As a** посетитель лендинга ЕАД, **I want to** просматривать новости дивизиона, находить нужные материалы через раскрывающиеся панели ресурсов и читать цитату из «Евангелизма», **so that** узнать, чем живёт движение в дивизионе, и сразу забрать реальные материалы для своей церкви.

Этот план закрывает третью часть истории: цитата из «Евангелизма» с силуэтом карты.

<objective>
Заменить заглушку `Quote` из фазы 1 на секцию цитаты: градиентный фон `#120c34 → #211a3e`, мягкие пятна signal/horizon, статичный SVG-силуэт карты мира из `world-atlas` через `lib/geo.ts` фазы 2, `figure > blockquote` с двумя абзацами и `figcaption > cite`.

Purpose: QUOTE-01. Секция завершает информационный поток лендинга перед футером и переиспользует геоданные фазы 2 без дополнительного веса бандла.
Output: `src/data/copy.quote.ts`, `WorldSilhouette.tsx`, `Quote.tsx`, `Quote.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-resources-quote/04-CONTEXT.md
@.planning/phases/04-news-resources-quote/04-UI-SPEC.md
@.planning/phases/04-news-resources-quote/04-RESEARCH.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/02-hero-and-map/02-RESEARCH.md

<interfaces>
<!-- Контракты фазы 2 по 02-CONTEXT.md и 02-RESEARCH.md. Имя экспорта коллекции стран подтвердить чтением src/lib/geo.ts перед реализацией. -->

From src/lib/geo.ts (фаза 2):
```typescript
// Ожидается один из вариантов:
export function loadWorld(): FeatureCollection<Geometry, GeoJsonProperties>; // синхронно, feature(topology, objects.countries), 177 стран
// или
export const world: FeatureCollection<Geometry, GeoJsonProperties>;
export const esdFeatures: Feature[]; // 12 стран ЕАД, для цитаты НЕ нужны
```

From d3-geo (уже в зависимостях фазы 2, @types/d3-geo установлены):
```typescript
import { geoNaturalEarth1, geoPath } from "d3-geo";
// geoNaturalEarth1().fitSize([w, h], geojson) → GeoProjection
// geoPath(projection)(featureCollection) → string | null  (без canvas-контекста возвращает атрибут d)
```

From src/components/layout/Eyebrow.tsx (фаза 1):
```typescript
export function Eyebrow(props: { children: ReactNode; className?: string }): JSX.Element;
```

Утилиты фазы 1 в global.css: `.text-gradient-brand` (--gradient-brand 104deg #d28ebe → #3b4da1 52% → #7bc2c7, background-clip: text), `.glass`, `.focus-ring`. Токены: text-paper, text-horizon-200, font-display (Onest), bg-midnight-900/800.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Падающий тест секции цитаты (RED)</name>
  <files>src/components/quote/Quote.test.tsx</files>
  <read_first>
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «Quote» в Component Inventory, «Layout Contract → Цитата», «Copywriting Contract» ключи `quote.*`, «Contract Checks»)
    - .planning/phases/04-news-resources-quote/04-CONTEXT.md (раздел «Цитата (#quote)»)
    - src/components/quote/Quote.tsx (заглушка фазы 1: вид экспорта, `id="quote"`)
    - src/test/setup.ts (моки; ResizeObserver не понадобится, SVG статичный)
  </read_first>
  <action>
    Написать `Quote.test.tsx` до реализации; тест падает на импорте `./Quote`/`./WorldSilhouette` и `../../data/copy.quote`. Проверки:
    1. `render(<Quote />)`; `document.querySelector("section#quote")` не null.
    2. Внутри `figure` есть `blockquote` с ровно двумя `<p>`; первый содержит «Пусть каждый работник в винограднике Господа исследует, планирует, разрабатывает методы работы с людьми. Нам необходимо предпринимать нечто выходящее за рамки обычного порядка вещей.», второй содержит «Мы обязаны приковывать внимание людей. Нам следует быть чрезвычайно серьезными. Мы стоим на самом пороге времени бедствий и смут, которые трудно вообразить.» (сравнивать через `toHaveTextContent`).
    3. `figcaption` содержит элемент `cite` с текстом «Эллен Уайт, «Евангелизм», стр. 122».
    4. Есть текст «Слово на дорогу».
    5. `section.querySelector("svg")` имеет `aria-hidden="true"` и `focusable="false"`; внутри `path` с атрибутом `d`, длина которого > 1000 символов (177 стран одним path); `preserveAspectRatio="xMidYMid slice"`.
    6. Декоративная кавычка: элемент с `aria-hidden="true"` и текстом «“» присутствует.
    7. В секции нет `<h2>`: `section.querySelector("h2")` равен null (по UI-SPEC цитата идёт без заголовка, только eyebrow).

    Запустить `npx vitest run src/components/quote/Quote.test.tsx`, убедиться в RED. Закоммить: `test(04-02): add failing test for quote section`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/quote/Quote.test.tsx 2>&1 | grep -E "FAIL|failed|Failed to resolve" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/quote/Quote.test.tsx` содержит строки `section#quote`, `blockquote`, `cite`, `"Слово на дорогу"`, `aria-hidden`, `preserveAspectRatio`, `стр. 122`
    - `npx vitest run src/components/quote/Quote.test.tsx` завершается с ненулевым кодом, в выводе `Failed to resolve import` или `Cannot find module`
    - Коммит `test(04-02):` есть в `git log -1`
  </acceptance_criteria>
  <done>Тест закоммичен и падает из-за отсутствия реализации.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Секция цитаты с силуэтом карты мира (GREEN)</name>
  <files>src/data/copy.quote.ts, src/components/quote/WorldSilhouette.tsx, src/components/quote/Quote.tsx</files>
  <read_first>
    - src/lib/geo.ts (фаза 2): точное имя экспорта коллекции всех стран (`loadWorld()` или `world`), тип FeatureCollection; если экспортируются только страны ЕАД, добавить в план SUMMARY заметку и использовать `feature(topology, objects.countries)` из `topojson-client` с импортом `world-atlas/countries-110m.json` прямо в WorldSilhouette (зависимости и типы уже стоят с фазы 2)
    - src/lib/geo.test.ts (фаза 2): как тесты импортируют world-atlas в jsdom, чтобы `Quote.test.tsx` работал так же
    - src/components/quote/Quote.tsx (заглушка: вид экспорта сохранить)
    - src/components/layout/Eyebrow.tsx, src/styles/global.css (утилита `.text-gradient-brand`, токены)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «Layout Contract → Цитата», «Quote», «Typography» строка о цитате: Onest 700, 22px, line-height 1.35)
  </read_first>
  <behavior>
    - Quote рендерит section#quote, figure > blockquote с двумя p из quoteCopy.paragraphs, figcaption > cite с quoteCopy.cite, eyebrow quoteCopy.eyebrow
    - WorldSilhouette рендерит svg aria-hidden focusable="false" preserveAspectRatio="xMidYMid slice" с одним path, d непустой (> 1000 символов)
    - В секции нет h2
  </behavior>
  <action>
    1. `src/data/copy.quote.ts`: `export const quoteCopy = { eyebrow: "Слово на дорогу", paragraphs: ["Пусть каждый работник в винограднике Господа исследует, планирует, разрабатывает методы работы с людьми. Нам необходимо предпринимать нечто выходящее за рамки обычного порядка вещей.", "Мы обязаны приковывать внимание людей. Нам следует быть чрезвычайно серьезными. Мы стоим на самом пороге времени бедствий и смут, которые трудно вообразить."], cite: "Эллен Уайт, «Евангелизм», стр. 122" } as const` (D: CONTEXT «Цитата»). Отдельный файл вместо `copy.ts`: фаза идёт параллельно с фазой 3, `copy.ts` не редактируется. Заглушка фазы 1 держала в `copy.ts` eyebrow «Вдохновение» и заголовок «Слово, с которого всё начинается»; фаза 4 по своему CONTEXT заменяет их на eyebrow «Слово на дорогу» без H2, старые ключи в `copy.ts` остаются нетронутыми до фазы 5.

    2. `src/components/quote/WorldSilhouette.tsx`: named export `WorldSilhouette({ className }: { className?: string })`. На уровне модуля (один раз, не в рендере): `const W = 960; const H = 480; const world = loadWorld()` (или импорт `world`), `const projection = geoNaturalEarth1().fitSize([W, H], world)`, `const d = geoPath(projection)(world) ?? ""`. Один `path` на всю коллекцию дешевле 177 отдельных (RESEARCH «Силуэт карты»). Рендер: `<svg aria-hidden="true" focusable="false" viewBox={`0 0 ${W} ${H}`} preserveAspectRatio="xMidYMid slice" className={className}>` → `<path d={d} fill="rgb(248 247 251 / 0.05)" stroke="rgb(248 247 251 / 0.1)" strokeWidth={0.5} vectorEffect="non-scaling-stroke" />`. Фиксированный viewBox плюс `slice` заменяют `fitSize([w, h])` по реальным пикселям: ResizeObserver не нужен, SVG статичен. Никакого `pointer-events`: добавить `pointer-events-none` в className по умолчанию.

    3. `src/components/quote/Quote.tsx`: тот же вид экспорта, что у заглушки. `<section id="quote" className="relative isolate overflow-hidden bg-[linear-gradient(180deg,#120c34,#211a3e)]">`. Слои фона в порядке: `<WorldSilhouette className="pointer-events-none absolute inset-0 -z-10 h-full w-full [mask-image:radial-gradient(ellipse_at_center,black_55%,transparent_100%)]" />`, затем `<div aria-hidden="true" className="pointer-events-none absolute inset-0 -z-10 bg-[radial-gradient(circle_at_22%_30%,rgb(158_67_154/.18),transparent_42%),radial-gradient(circle_at_78%_68%,rgb(84_164_172/.16),transparent_44%)]" />`. Контейнер `mx-auto max-w-[72rem] px-4 md:px-8 py-16 md:py-24` (ритм `Section` фазы 1; собственный `<section>` потому, что градиент и силуэт нужны на всю ширину). Внутри `<figure className="relative z-[1] mx-auto max-w-3xl text-center">`: `<Eyebrow>{quoteCopy.eyebrow}</Eyebrow>`, `<span aria-hidden="true" className="text-gradient-brand mt-4 block font-display text-[96px] font-extrabold leading-none opacity-50">“</span>`, `<blockquote className="mt-4 flex flex-col gap-6 font-display text-[22px] font-bold leading-[1.35] text-paper">` с `quoteCopy.paragraphs.map((text, i) => <p key={i}>{text}</p>)`, `<figcaption className="mt-8"><cite className="block text-xs font-bold uppercase not-italic tracking-[0.08em] text-horizon-200">{quoteCopy.cite}</cite></figcaption>`. H2 в секции нет (UI-SPEC «Quote»); в навигации header ссылки на `#quote` тоже нет, поэтому порядок заголовков страницы не ломается.

    Прогнать `npm test` (включая `App.test.tsx`: рендер всего App теперь тянет world-atlas в jsdom, это уже происходит из-за карты фазы 2) и `npm run build`. Закоммить: `feat(04-02): quote section with world silhouette`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/quote/Quote.test.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/quote/Quote.test.tsx` завершается с кодом 0 (все 7 проверок)
    - `npm test` завершается с кодом 0; `npm run build` завершается с кодом 0
    - `grep -n "geoNaturalEarth1" src/components/quote/WorldSilhouette.tsx` и `grep -n "preserveAspectRatio=\"xMidYMid slice\"" src/components/quote/WorldSilhouette.tsx` находят строки
    - `grep -n "aria-hidden=\"true\"" src/components/quote/WorldSilhouette.tsx` находит строку; `grep -n "<h2" src/components/quote/Quote.tsx` ничего не находит
    - `grep -n "стр. 122" src/data/copy.quote.ts` находит строку; `grep -c "<p key" src/components/quote/Quote.tsx` ≥ 1 или `paragraphs.map` присутствует
    - `grep -n "id=\"quote\"" src/components/quote/Quote.tsx` находит строку
    - `git diff --name-only HEAD~2 -- src | grep -v -E "^src/(data/copy\.quote|components/quote/)"` пуст
    - `package.json` не изменён (`git diff --name-only HEAD~2 -- package.json` пуст)
  </acceptance_criteria>
  <done>Секция цитаты рендерит два абзаца, подпись источника и декоративный силуэт карты, тест зелёный, билд проходит.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Статические данные `world-atlas` JSON → d3-geo → SVG `d` | Геометрия из npm-пакета, установленного в фазе 2, попадает в атрибут `d` |
| Статический текст `copy.quote.ts` → React | Цитата из типизированного модуля |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering (XSS) | `WorldSilhouette` `<path d>` | mitigate | `d` формируется `geoPath` из чисел и попадает в атрибут через JSX (React экранирует); `dangerouslySetInnerHTML` не используется |
| T-04-06 | Denial of Service | Расчёт `geoPath` для 177 стран | accept | Считается один раз на уровне модуля (~108 КБ JSON уже в бандле фазы 2), без ResizeObserver и перерасчётов |
| T-04-07 | Information Disclosure | Секция цитаты | accept | Внешних запросов нет, персональных данных нет |
| T-04-SC | Tampering | npm installs | accept | Новых пакетов нет; `d3-geo`, `topojson-client`, `world-atlas` установлены фазой 2 |
</threat_model>

<verification>
- `npm test` и `npm run build` зелёные.
- Ручной smoke (`npm run dev`): в `#quote` видны два абзаца Onest 700 22px по центру, подпись horizon-200 uppercase, за текстом полупрозрачный контур континентов, края силуэта растворяются к границам секции; фон плавно переходит в клин футера.
</verification>

<success_criteria>
- QUOTE-01: цитата из «Евангелизма», стр. 122 (два абзаца из esd.onevoice27.org) на градиентном фоне с силуэтом карты и подписью источника.
- Силуэт `aria-hidden`, один `path`, без интерактива и без ResizeObserver.
- Ни один файл вне `files_modified` не изменён; новых зависимостей нет.
</success_criteria>

<output>
Create `.planning/phases/04-news-resources-quote/04-02-SUMMARY.md` when done
</output>
