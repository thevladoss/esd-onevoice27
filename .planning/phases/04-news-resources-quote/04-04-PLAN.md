---
phase: 04-news-resources-quote
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - src/components/resources/Resources.tsx
  - src/components/resources/ResourceCard.tsx
  - src/components/resources/ResourcePanel.tsx
  - src/components/resources/MaterialsList.tsx
  - src/components/resources/MusicPlaceholder.tsx
  - src/components/resources/resources.css
  - src/components/resources/Resources.test.tsx
autonomous: true
requirements: [RES-01, RES-02, RES-03, RES-04]
tags: [resources, panels, accessibility, css-grid, vitest]

must_haves:
  truths:
    - "Посетитель видит в #resources eyebrow «Ресурсы», H2 «Всё, что нужно для старта» и три асимметрично расставленные стеклянные карточки Музыка / Материалы / Видео с цветными точками-индикаторами и дрейфующими частицами на фоне"
    - "Клик по карточке «Материалы» раскрывает под сеткой панель с 5 ссылками на реальные страницы ЕАД в новой вкладке; клик по «Видео» показывает 16 фасадов; клик по «Музыка» показывает заглушку «Песня ещё в работе»"
    - "Повторный клик по активной карточке, кнопка «Свернуть» и Esc закрывают панель; фокус возвращается на карточку-триггер; состояние отражено в aria-expanded и aria-controls"
    - "Переход по адресу с #resources-materials открывает панель материалов сразу"
    - "При prefers-reduced-motion частицы неподвижны, панель раскрывается мгновенно, карточки не сдвигаются при hover"
  artifacts:
    - path: "src/components/resources/Resources.tsx"
      provides: "секция #resources: сетка 12 колонок, три ResourceCard, состояние активной панели, deep link, Esc"
      min_lines: 80
    - path: "src/components/resources/ResourceCard.tsx"
      provides: "button aria-expanded aria-controls, индикатор, заголовок, описание, триггер"
      exports: ["ResourceCard"]
    - path: "src/components/resources/ResourcePanel.tsx"
      provides: "region aria-labelledby tabIndex=-1 с шапкой, кнопкой Свернуть и содержимым по ключу"
      exports: ["ResourcePanel"]
    - path: "src/components/resources/MaterialsList.tsx"
      provides: "ul из 5 ссылок с иконками по kind"
      exports: ["MaterialsList"]
    - path: "src/components/resources/MusicPlaceholder.tsx"
      provides: "GlassCard с нотой, заголовком и текстом заглушки"
      exports: ["MusicPlaceholder"]
    - path: "src/components/resources/resources.css"
      provides: "@keyframes resources-particles, слои частиц, звёздный фон, анимация панели 0fr→1fr, линия триггера, reduced motion"
      contains: "@keyframes resources-particles"
  key_links:
    - from: "src/components/resources/ResourceCard.tsx"
      to: "src/components/resources/Resources.tsx"
      via: "aria-controls=\"resources-panel\" указывает на постоянную обёртку панели"
      pattern: "aria-controls=\"resources-panel\""
    - from: "src/components/resources/ResourcePanel.tsx"
      to: "src/components/resources/VideoGrid.tsx"
      via: "ветка video рендерит VideoGrid"
      pattern: "<VideoGrid"
    - from: "src/components/resources/MaterialsList.tsx"
      to: "src/data/materials.ts"
      via: "materials.map → a[target=_blank]"
      pattern: "from \"\\.\\./\\.\\./data/materials\""
    - from: "src/components/resources/Resources.tsx"
      to: "src/components/resources/resources.css"
      via: "import \"./resources.css\""
      pattern: "import \"\\./resources\\.css\""
    - from: "src/components/resources/Resources.tsx"
      to: "window.location.hash"
      via: "проверка === \"#resources-materials\" при монтировании"
      pattern: "#resources-materials"
---

## Phase Goal

**As a** посетитель лендинга ЕАД, **I want to** просматривать новости дивизиона, находить нужные материалы через раскрывающиеся панели ресурсов и читать цитату из «Евангелизма», **so that** узнать, чем живёт движение в дивизионе, и сразу забрать реальные материалы для своей церкви.

Этот план закрывает вторую часть истории: карточки ресурсов и раскрывающиеся панели.

<objective>
Заменить заглушку `Resources` из фазы 1 на секцию ресурсов: звёздный фон с дрейфующими частицами, асимметричная сетка из трёх карточек-кнопок (Музыка / Материалы / Видео) вокруг центрального текстового блока, панель под сеткой с тремя ветками (5 материалов со ссылками ЕАД, 16 видео-фасадов из плана 04-03, заглушка музыки), переключение через `useState`, закрытие повторным кликом, кнопкой и Esc с возвратом фокуса, deep link `#resources-materials`.

Purpose: RES-01, RES-02, RES-03, RES-04. Самая интерактивная секция информационного потока; component-тесты панелей входят в QA-02.
Output: `Resources.tsx`, `ResourceCard.tsx`, `ResourcePanel.tsx`, `MaterialsList.tsx`, `MusicPlaceholder.tsx`, `resources.css`, `Resources.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-news-resources-quote/04-CONTEXT.md
@.planning/phases/04-news-resources-quote/04-UI-SPEC.md
@.planning/phases/04-news-resources-quote/04-RESEARCH.md
@.planning/phases/04-news-resources-quote/04-03-SUMMARY.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Контракты плана 04-03 (уже реализованы в волне 1). Использовать напрямую. -->

From src/data/copy.resources.ts:
```typescript
export type ResourceKey = "music" | "materials" | "video";
export const resourceKeys: readonly ResourceKey[];
export const resourcesCopy: {
  eyebrow: string; title: string; body: string;
  cards: Record<ResourceKey, { label: string; title: string; description: string; cta: string; accent: string }>;
  panel: { close: string; closeLabel: string };
  music: { emptyTitle: string; emptyBody: string };
  video: { watchLabel: (title: string) => string };
};
```

From src/data/materials.ts:
```typescript
export type MaterialKind = "document" | "image" | "book" | "phone" | "folder";
export type MaterialItem = { id: string; title: string; caption: string; href: string; kind: MaterialKind };
export const materials: readonly MaterialItem[]; // 5
```

From src/components/resources/VideoGrid.tsx:
```typescript
export function VideoGrid(props: { items?: readonly VideoItem[] }): JSX.Element; // 16 фасадов по умолчанию
```

Фаза 1 (подтвердить чтением файлов): `Eyebrow`, `GradientTitle({ as, variant, children, className? })`, `GlassCard({ children, className? })`; утилиты `.glass` (фон, рамка, радиус, тень, backdrop-filter стеклянной карточки), `.focus-ring`; токены text-paper, text-horizon-200, bg-midnight-950, rounded-card, shadow-card, ease-header; `:root` переменные `--glass-border`, `--glass-surface`, `--radius-card`, `--shadow-card`.

React 19: `ref` передаётся как обычный проп функционального компонента, `forwardRef` не нужен. Reduced motion читать через `window.matchMedia("(prefers-reduced-motion: reduce)").matches` (мок есть в setup.ts фазы 1), без импорта `motion`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Падающий тест карточек и панелей ресурсов (RED)</name>
  <files>src/components/resources/Resources.test.tsx</files>
  <read_first>
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «ResourceCard», «ResourcePanel», «MaterialsList», «MusicPlaceholder», «Accessibility Contract», «Contract Checks»)
    - .planning/phases/04-news-resources-quote/04-CONTEXT.md (разделы «Ресурсы (#resources)» и «Integration Points» про `#resources-materials`)
    - src/components/resources/VideoFacade.test.tsx (план 04-03: стиль импортов и запросов)
    - src/components/resources/Resources.tsx (заглушка фазы 1: вид экспорта, `id="resources"`)
    - src/data/materials.ts (5 href для точного сравнения)
  </read_first>
  <action>
    `Resources.test.tsx` импортирует `Resources` (тем же видом, что заглушка), `materials` из `../../data/materials`. Падает до реализации. Happy path (Task 2 делает зелёными):
    1. `render(<Resources />)`: `document.querySelector("section#resources")` не null; есть заголовок уровня 2 «Всё, что нужно для старта» и текст «Ресурсы» (eyebrow); ровно три кнопки с `aria-controls="resources-panel"` и все с `aria-expanded="false"`; `screen.queryByRole("region")` равен null.
    2. `const materialsBtn = getByRole("button", { name: /Будьте готовы/ })`; клик → `aria-expanded="true"`; `getByRole("region")` есть, у него `aria-labelledby` указывает на элемент с текстом «Будьте готовы»; `within(region).getAllByRole("link")` даёт 5; `href` ссылок в порядке `materials.map(m => m.href)`; у каждой `target="_blank"` и `rel="noopener noreferrer"`; `document.activeElement === region`.
    3. Повторный клик по `materialsBtn` → `aria-expanded="false"`, `queryByRole("region")` null.
    4. Клик по `getByRole("button", { name: /Смотрите и делитесь/ })` → в region 16 кнопок `/^Смотреть видео: /`; у кнопки «Материалы» `aria-expanded="false"`, у «Видео» `"true"`; затем клик по кнопке «Материалы» переключает панель: 5 ссылок, 0 фасадов.
    5. Клик по `getByRole("button", { name: /Пойте вместе/ })` → в region тексты «Песня ещё в работе» и «Официальная песня «Единого голоса 27» скоро появится здесь. Следите за новостями дивизиона.», `within(region).queryAllByRole("link")` пуст.
    6. Внутри карточек-кнопок нет вложенных `<a>` или `<button>` (`button.querySelector("a, button")` null для всех трёх).
    7. Кнопка `getByRole("button", { name: "Свернуть панель" })` присутствует при открытой панели.

    Расширенные проверки (Task 3 делает зелёными; написать сейчас, выделить блоком `describe("панель: клавиатура и deep link")`):
    8. Открыть «Материалы», `fireEvent.keyDown(region, { key: "Escape" })` → панель закрыта, `document.activeElement === materialsBtn`.
    9. Открыть «Видео», клик по «Свернуть панель» → панель закрыта, `document.activeElement` равен кнопке «Смотрите и делитесь».
    10. Перед `render` выставить `window.location.hash = "#resources-materials"`; после `render(<Resources />)` region открыт с 5 ссылками и `aria-expanded="true"` у «Материалы»; в `afterEach` очистить хэш через `history.replaceState(null, "", window.location.pathname)`.
    11. `section.querySelectorAll("[data-particles] > span[aria-hidden='true']").length === 3` (три слоя частиц).

    Запустить, убедиться в RED, закоммить: `test(04-04): add failing tests for resources cards and panels`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/resources/Resources.test.tsx 2>&1 | grep -E "FAIL|failed|Failed to resolve" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл содержит строки `aria-controls="resources-panel"`, `aria-expanded`, `getByRole("region")`, `noopener noreferrer`, `Свернуть панель`, `key: "Escape"`, `#resources-materials`, `data-particles`, `Смотреть видео:`
    - Файл содержит минимум 11 `it(`/`test(` блоков
    - `npx vitest run src/components/resources/Resources.test.tsx` завершается с ненулевым кодом; причина `Failed to resolve import` / `Cannot find module` либо падения на отсутствующих элементах, если заглушка фазы 1 импортируется
    - Коммит `test(04-04):` в `git log -1`
  </acceptance_criteria>
  <done>Тест закоммичен и падает до реализации секции.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Карточки, переключение панелей, материалы и заглушка музыки (GREEN happy path)</name>
  <files>src/components/resources/Resources.tsx, src/components/resources/ResourceCard.tsx, src/components/resources/ResourcePanel.tsx, src/components/resources/MaterialsList.tsx, src/components/resources/MusicPlaceholder.tsx</files>
  <read_first>
    - src/components/resources/Resources.tsx (заглушка фазы 1: вид экспорта сохранить)
    - src/components/layout/GlassCard.tsx, src/components/layout/Eyebrow.tsx, src/components/layout/GradientTitle.tsx (реальные пропсы)
    - src/styles/global.css (утилита `.glass`, `.focus-ring`, переменные `:root`)
    - src/data/copy.resources.ts, src/data/materials.ts, src/components/resources/VideoGrid.tsx (план 04-03, использовать как есть)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «Layout Contract → Ресурсы», «ResourceCard», «ResourcePanel», «MaterialsList», «MusicPlaceholder», «Interaction States», «Отклонения от CONTEXT»)
    - .planning/phases/04-news-resources-quote/04-RESEARCH.md (раздел «Раскладка ресурсов»)
  </read_first>
  <behavior>
    - Три карточки-кнопки с aria-expanded/aria-controls; клик открывает панель нужной ветки и фокусирует её; повторный клик закрывает; клик по другой карточке переключает
    - Панель материалов: 5 ссылок target=_blank rel="noopener noreferrer" в порядке materials
    - Панель видео: VideoGrid с 16 фасадами; панель музыки: MusicPlaceholder без ссылок
    - Внутри карточки-кнопки нет вложенных интерактивных элементов
  </behavior>
  <action>
    Реализовать happy path тестов 1–7. Раскладка на Tailwind-утилитах; `resources.css` (частицы, анимация панели, линия триггера) добавит Task 3.

    1. `ResourceCard.tsx`: `ResourceCard({ kind, isOpen, onToggle, ref }: { kind: ResourceKey; isOpen: boolean; onToggle: () => void; ref?: Ref<HTMLButtonElement> })`. `const card = resourcesCopy.cards[kind]`. Рендер `<button type="button" ref={ref} data-kind={kind} aria-expanded={isOpen} aria-controls="resources-panel" onClick={onToggle} style={{ "--accent": card.accent } as CSSProperties} className="resource-card glass flex min-h-64 w-full flex-col justify-between p-6 text-left transition-[transform,border-color] duration-[420ms] ease-header hover:border-[var(--accent)] motion-safe:hover:-translate-y-1 focus-visible:outline-2 focus-visible:outline-[var(--accent)] focus-visible:outline-offset-4 aria-expanded:border-[var(--accent)]">` (D: UI-SPEC «ResourceCard»; `<button>` вместо ссылки-триггера внутри, потому что вложенный интерактив невалиден). Верхняя строка `flex items-center justify-between`: `<span className="text-xs font-bold uppercase tracking-[0.08em] text-paper/60">{card.label}</span>` и точка `<span aria-hidden="true" className="resource-card__dot h-3 w-3 rounded-full bg-[var(--accent)]" />`. Низ: `<h3 className="font-display text-[22px] font-extrabold leading-[1.15] tracking-[-0.03em] text-paper">{card.title}</h3>`, `<p className="mt-2 text-base leading-[1.5] text-paper/80">{card.description}</p>`, `<span className="resource-card__trigger mt-4 inline-block text-xs font-bold uppercase tracking-[0.08em] text-paper">{card.cta}</span>` (это `<span>`, не ссылка). Поверхность карточки поверх `.glass`: `bg-[linear-gradient(180deg,rgb(248_247_251/.06),transparent_34%),linear-gradient(145deg,rgb(48_63_131/.86),rgb(18_12_52/.76))] backdrop-blur-[18px] backdrop-saturate-125`.

    2. `MaterialsList.tsx`: `MaterialsList({ items = materials })` → `<ul className="flex flex-col gap-2">`, каждая `<li><a href={m.href} target="_blank" rel="noopener noreferrer" className="group flex items-center gap-4 rounded-xl border border-paper/10 bg-paper/5 p-4 transition-[transform,border-color,background-color] duration-[240ms] hover:border-[rgb(123_194_199/.4)] hover:bg-paper/[.06] motion-safe:hover:translate-x-1 focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-2">`: иконка 24×24 по `m.kind` (пять локальных компонентов inline SVG в этом же файле: документ, изображение, книга, телефон, папка; `stroke="currentColor" strokeWidth={1.5} fill="none" aria-hidden="true"`, цвет `text-horizon-200`), блок текста `<span className="flex flex-col"><span className="text-base font-bold text-paper">{m.title}</span><span className="text-xs font-bold uppercase tracking-[0.08em] text-paper/80">{m.caption}</span></span>`, стрелка `<span aria-hidden="true" className="ml-auto text-base text-paper/80">→</span>`. Формы иконок на усмотрение исполнителя (Claude's Discretion по CONTEXT), размер и штрих фиксированы.

    3. `MusicPlaceholder.tsx`: `<GlassCard className="flex flex-col items-center p-8 text-center">` (если `GlassCard` не принимает `className`, использовать `<div className="glass ...">`), внутри inline SVG ноты 32×32 `text-[#8f9dd6]` `aria-hidden="true"`, `<h4 className="mt-4 font-display text-[22px] font-extrabold leading-[1.15] text-paper">{resourcesCopy.music.emptyTitle}</h4>`, `<p className="mt-2 max-w-md text-base leading-[1.5] text-paper/80">{resourcesCopy.music.emptyBody}</p>`. Кнопок нет. `h4`, потому что `h3` уже несёт заголовок панели.

    4. `ResourcePanel.tsx`: `ResourcePanel({ kind, onClose, ref, onKeyDown }: { kind: ResourceKey; onClose: () => void; ref?: Ref<HTMLDivElement>; onKeyDown?: KeyboardEventHandler<HTMLDivElement> })`. Рендер `<div ref={ref} role="region" aria-labelledby="resources-panel-title" tabIndex={-1} onKeyDown={onKeyDown} className="glass relative p-6 md:p-8 bg-[radial-gradient(circle_at_15%_12%,rgb(79_93_175/.2),transparent_38%)] focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-2">`. Шапка `flex items-start justify-between gap-4`: слева `<p className="text-xs font-bold uppercase tracking-[0.08em] text-paper/60">{card.label}</p>` и `<h3 id="resources-panel-title" className="font-display text-[22px] font-extrabold leading-[1.15] tracking-[-0.03em] text-paper">{card.title}</h3>` плюс `<p className="mt-2 text-base text-paper/80">{card.description}</p>`; справа `<button type="button" onClick={onClose} aria-label={resourcesCopy.panel.closeLabel} className="flex min-h-11 min-w-11 items-center justify-center gap-2 rounded-lg text-paper/80 hover:bg-paper/[.06] hover:text-paper focus-visible:outline-2 focus-visible:outline-horizon-400 focus-visible:outline-offset-2">` с inline SVG «×» 20px `aria-hidden` и `<span className="hidden md:inline text-xs font-bold uppercase tracking-[0.08em]">{resourcesCopy.panel.close}</span>`. Тело `mt-6`: `kind === "materials"` → `<MaterialsList />`, `kind === "video"` → `<VideoGrid />`, `kind === "music"` → `<MusicPlaceholder />`. Стеклянная поверхность через утилиту `.glass`, а не компонент `GlassCard`: панели нужны `id`-независимые атрибуты `role`, `tabIndex`, `ref`, которые `GlassCard` фазы 1 не обязан пробрасывать.

    5. `Resources.tsx` (тот же вид экспорта, что у заглушки): `import "./resources.css"` (файл создаст Task 3; на этой задаче создать пустой `resources.css` с одним комментарием, чтобы импорт собирался). Состояние `const [active, setActive] = useState<ResourceKey | null>(null)`, `const cardRefs = useRef<Record<ResourceKey, HTMLButtonElement | null>>({ music: null, materials: null, video: null })`, `const panelRef = useRef<HTMLDivElement>(null)`. `toggle(kind)` → `setActive(prev => (prev === kind ? null : kind))`. `useEffect(() => { if (active) panelRef.current?.focus({ preventScroll: true }) }, [active])`. Разметка: `<section id="resources" className="resources relative isolate overflow-hidden bg-midnight-950">`, внутри слой фона `<div aria-hidden="true" data-particles className="pointer-events-none absolute -inset-6 -z-10"><span aria-hidden="true" className="resources-particles resources-particles--1" /><span aria-hidden="true" className="resources-particles resources-particles--2" /><span aria-hidden="true" className="resources-particles resources-particles--3" /></div>` (классы получат стили в Task 3), контейнер `mx-auto max-w-[72rem] px-4 md:px-8 py-16 md:py-24`. Сетка `<div className="flex flex-col gap-6 md:grid md:grid-cols-6 md:gap-6 lg:grid-cols-12 lg:gap-x-6 lg:gap-y-8">` с четырьмя детьми и порядком в DOM: текст, музыка, материалы, видео (естественный порядок мобильного стека и таб-порядок):
       - текстовый блок `<div className="order-first text-center rounded-card border border-dotted border-[rgb(84_164_172/.25)] bg-[rgb(84_164_172/.05)] p-8 md:col-span-6 md:row-start-1 lg:col-start-5 lg:col-end-10 lg:row-start-1 lg:row-end-3 lg:self-center lg:max-w-[528px] lg:mx-auto">`: `<Eyebrow>{resourcesCopy.eyebrow}</Eyebrow>`, `<GradientTitle as="h2" variant="section" className="mt-2">{resourcesCopy.title}</GradientTitle>`, `<p className="mt-4 text-base leading-[1.5] text-paper/80">{resourcesCopy.body}</p>`;
       - `<ResourceCard kind="music" .../>` в обёртке `<div className="mx-auto w-full max-w-[360px] md:max-w-none md:col-start-1 md:col-end-4 md:row-start-2 lg:col-start-1 lg:col-end-5 lg:row-start-1 lg:row-end-3 lg:max-w-[320px] lg:self-start">`;
       - «materials» в `<div className="mx-auto w-full max-w-[360px] md:max-w-none md:col-start-4 md:col-end-7 md:row-start-2 md:mt-6 lg:col-start-10 lg:col-end-13 lg:row-start-2 lg:row-end-4 lg:mt-0 lg:max-w-[272px] lg:self-end lg:ml-auto">` (D: UI-SPEC колонки 10–12, а не 9–12 из CONTEXT: иначе наложение на текстовый блок);
       - «video» в `<div className="mx-auto w-full max-w-[360px] md:max-w-none md:col-start-2 md:col-end-6 md:row-start-3 md:-mt-4 lg:col-start-4 lg:col-end-8 lg:row-start-3 lg:row-end-4 lg:-mt-8 lg:max-w-[344px]">`.
       Каждой карточке `ref={(el) => { cardRefs.current[kind] = el }}`, `isOpen={active === kind}`, `onToggle={() => toggle(kind)}`. Под сеткой постоянная обёртка `<div id="resources-panel" data-open={active !== null} className="resources-panel-wrap mt-8"><div className="min-h-0 overflow-hidden">{active && <ResourcePanel key={active} kind={active} onClose={() => toggle(active)} ref={panelRef} />}</div></div>`. `id="resources-panel"` стоит на постоянной обёртке, а не на region: так `aria-controls` карточек всегда указывает на существующий элемент, даже когда панель закрыта.

    Прогнать `npm test` и `npm run build`. Закоммить: `feat(04-04): resources cards with switchable materials, video and music panels`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/resources/Resources.test.tsx -t "^(?!.*панель: клавиатура)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Тесты 1–7 из `Resources.test.tsx` проходят: `npx vitest run src/components/resources/Resources.test.tsx` показывает ≥ 7 passed, падают только тесты из блока «панель: клавиатура и deep link»
    - `npm run build` завершается с кодом 0; `npm test` падает только на тестах блока «панель: клавиатура и deep link» файла Resources.test.tsx (остальные файлы зелёные)
    - `grep -c "aria-controls=\"resources-panel\"" src/components/resources/ResourceCard.tsx` = 1; `grep -n "id=\"resources-panel\"" src/components/resources/Resources.tsx` находит строку
    - `grep -n "role=\"region\"" src/components/resources/ResourcePanel.tsx`, `grep -n "tabIndex={-1}" src/components/resources/ResourcePanel.tsx`, `grep -n "aria-labelledby=\"resources-panel-title\"" src/components/resources/ResourcePanel.tsx` находят строки
    - `grep -n "<VideoGrid" src/components/resources/ResourcePanel.tsx` находит строку; `grep -n "<a \|<button" src/components/resources/ResourceCard.tsx | grep -v "^.*<button type=\"button\"" ` не находит вложенных интерактивов (в файле ровно один `<button`, ноль `<a `)
    - `grep -c "rel=\"noopener noreferrer\"" src/components/resources/MaterialsList.tsx` ≥ 1
    - `grep -n "import \"./resources.css\"" src/components/resources/Resources.tsx` находит строку; файл `src/components/resources/resources.css` существует
    - `grep -n "components/about" src/components/resources/*.tsx` ничего не находит; `src/App.tsx`, `src/data/copy.ts`, `src/styles/*.css` не изменены
  </acceptance_criteria>
  <done>Три карточки переключают панель материалов, видео и музыки; панель получает фокус при открытии; happy-path тесты зелёные, билд проходит.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Частицы, анимация панели, Esc, возврат фокуса, deep link и reduced motion</name>
  <files>src/components/resources/resources.css, src/components/resources/Resources.tsx, src/components/resources/ResourcePanel.tsx, src/components/resources/Resources.test.tsx</files>
  <read_first>
    - src/components/resources/Resources.tsx и ResourcePanel.tsx (результат Task 2)
    - src/styles/global.css (как фаза 1 оформляет `@keyframes` и блок `@media (prefers-reduced-motion: reduce)`, чтобы `resources.css` повторял стиль)
    - .planning/phases/04-news-resources-quote/04-UI-SPEC.md (разделы «Motion Contract», «ResourcePanel» строки про Esc и глубокую ссылку, «Color» про `--color-unity-200`)
    - docs/research/orig-custom-styles.css строки 2150–2240 (`#ov-resources::before`, `@keyframes ov-resources-particles`: шаги радиальных градиентов оригинала)
  </read_first>
  <behavior>
    - Esc внутри region закрывает панель и возвращает фокус на карточку-триггер
    - Кнопка «Свернуть панель» закрывает и возвращает фокус на карточку-триггер
    - location.hash === "#resources-materials" при монтировании открывает панель материалов, вызывает scrollIntoView секции, фокусирует панель
    - В секции три слоя частиц span[aria-hidden] внутри [data-particles]
  </behavior>
  <action>
    Тесты 8–11 уже написаны в Task 1. Довести их до зелёного и добавить CSS.

    1. `Resources.tsx`: `close()` → запоминает `const trigger = active`, `setActive(null)`, затем `trigger && cardRefs.current[trigger]?.focus()`. Передать `onClose={close}` в `ResourcePanel` и `onKeyDown={(e) => { if (e.key === "Escape") { e.stopPropagation(); close() } }}`. Deep link: `const sectionRef = useRef<HTMLElement>(null)`; `useEffect(() => { if (window.location.hash !== "#resources-materials") return; setActive("materials"); const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches; sectionRef.current?.scrollIntoView({ behavior: reduced ? "auto" : "smooth", block: "start" }) }, [])`. `scrollIntoView` здесь допустим (мок есть в setup.ts фазы 1; `html { scroll-padding-top: 96px }` из фазы 1 учитывает header). Строгое сравнение хэша с литералом, никакого разбора и вставки в DOM.

    2. `resources.css` (импортируется из `Resources.tsx`, стили только с префиксом `.resources`/`.resource-`, чтобы не задеть чужие секции):
       - `.resources { --color-unity-200: #8f9dd6; }` с комментарием «токен из 04-UI-SPEC; фаза 5 поднимет в tokens.css @theme» (tokens.css фазы 1 в этой фазе не редактируется).
       - Звёздный фон: `.resources::before { content: ""; position: absolute; inset: 0; z-index: -1; pointer-events: none; opacity: .28; background: radial-gradient(circle at 13px 29px, rgb(170 217 220 / .88) 0 .7px, transparent 1.35px) 0 0 / 97px 113px repeat, radial-gradient(circle at 79px 41px, rgb(143 157 214 / .78) 0 .9px, transparent 1.55px) 17px 31px / 139px 157px repeat; }` (статичный слой по UI-SPEC «Звёздный фон»).
       - Частицы: `.resources-particles { position: absolute; inset: 0; will-change: transform, opacity; animation: resources-particles var(--dur) ease-in-out infinite alternate; }`, три модификатора с `--dur: 18s / 22s / 26s` и своими фонами из повторяющихся `radial-gradient` точек 0.7–1.8px цветов `rgb(170 217 220 / .74)` (horizon-200), `rgb(143 157 214 / .62)` (unity-200), `rgb(210 142 190 / .68)` (signal-300) с шагами `97px 113px`, `139px 157px`, `179px 193px`, `257px 233px` (по одному-два градиента на слой). `@keyframes resources-particles { 0% { opacity: .2; transform: translate3d(0, 0, 0) } 48% { opacity: .6; transform: translate3d(-18px, -24px, 0) } 100% { opacity: .22; transform: translate3d(12px, -46px, 0) } }` (D: UI-SPEC «Motion Contract»).
       - Панель: `.resources-panel-wrap { display: grid; grid-template-rows: 0fr; transition: grid-template-rows 420ms cubic-bezier(0.32, 0.72, 0, 1); } .resources-panel-wrap[data-open="true"] { grid-template-rows: 1fr; }`. Внутренний блок уже имеет `min-h-0 overflow-hidden`. Открытие анимируется от 0fr к 1fr; при закрытии контент размонтируется, обёртка схлопывается без анимации (осознанное упрощение: контент нельзя анимировать после размонтирования, а держать «прошлую» панель ради 420ms не стоит).
       - Линия триггера: `.resource-card__trigger { position: relative; } .resource-card__trigger::after { content: ""; position: absolute; left: 0; right: 0; bottom: -4px; height: 1px; background: linear-gradient(90deg, var(--accent), transparent); transform: scaleX(.34); transform-origin: left; transition: transform 420ms cubic-bezier(0.32, 0.72, 0, 1); } .resource-card:hover .resource-card__trigger::after, .resource-card:focus-visible .resource-card__trigger::after { transform: scaleX(1); }`.
       - Ореол точки у открытой карточки: `.resource-card[aria-expanded="true"] .resource-card__dot { box-shadow: 0 0 0 6px color-mix(in oklab, var(--accent) 18%, transparent); }`.
       - `@media (prefers-reduced-motion: reduce) { .resources-particles { animation: none; opacity: .28; } .resources-panel-wrap { transition: none; } .resource-card, .resource-card__trigger::after { transition: none; } }` (сдвиг карточки при hover уже за `motion-safe:` в Tailwind).

    3. `ResourcePanel.tsx`: убедиться, что `onKeyDown` навешан на корневой `region` (Esc ловится при фокусе на панели и на любом её потомке, включая ссылки и фасады).

    Прогнать `npm test` (весь набор) и `npm run build`. Закоммить: `feat(04-04): resources particles, panel motion, escape/focus return and deep link`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/resources/Resources.test.tsx && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/resources/Resources.test.tsx` завершается с кодом 0 (все 11 проверок)
    - `npm test` и `npm run build` завершаются с кодом 0
    - `grep -c "@keyframes resources-particles" src/components/resources/resources.css` = 1; `grep -n "prefers-reduced-motion: reduce" src/components/resources/resources.css` находит строку; `grep -n "grid-template-rows: 1fr" src/components/resources/resources.css` находит строку
    - `grep -n "#resources-materials" src/components/resources/Resources.tsx` и `grep -n "\"Escape\"" src/components/resources/Resources.tsx` находят строки
    - `grep -n "\-\-color-unity-200: #8f9dd6" src/components/resources/resources.css` находит строку
    - `grep -E "^[^.@:/ ]|^\.[a-z]" src/components/resources/resources.css | grep -v -E "^\.(resources|resource-)" | grep -E "^\." ` пуст (все селекторы с префиксом `.resources` или `.resource-`)
    - `git diff --name-only HEAD~3 -- src | grep -v -E "^src/components/resources/"` пуст; `package.json`, `src/styles/tokens.css`, `src/styles/global.css` не изменены
  </acceptance_criteria>
  <done>Панель закрывается Esc и кнопкой с возвратом фокуса, deep link открывает материалы, частицы дрейфуют и замирают при reduced motion, все тесты фазы зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → `window.location.hash` → состояние React | Ненадёжный ввод из адресной строки управляет открытием панели |
| Браузер → внешние сайты (esd.onevoice27.org, DigitalOcean Spaces, SharePoint) | Ссылки материалов в новую вкладку |
| Браузер ← img.youtube.com / youtube-nocookie.com | Через `VideoGrid` плана 04-03 |
| Статические данные `materials.ts` / `copy.resources.ts` → React | Тексты и URL из типизированных модулей |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Tampering | Deep link `location.hash` | mitigate | Строгое сравнение `=== "#resources-materials"`, значение хэша никуда не вставляется, не парсится и не попадает в DOM |
| T-04-14 | Tampering | `MaterialsList` `<a target="_blank">` | mitigate | `rel="noopener noreferrer"` на всех пяти ссылках, проверяется тестом |
| T-04-15 | Spoofing | Ссылка на `.docx` с hope-documents.fra1.digitaloceanspaces.com | accept | Адрес взят с официального esd.onevoice27.org и проверен исследователем (HTTP 200); подпись «DOCX» предупреждает о загрузке файла |
| T-04-16 | Elevation of Privilege | Кнопки-карточки и панель | mitigate | Вложенных интерактивов внутри `<button>` нет (тест 6); `aria-controls` всегда указывает на существующий элемент |
| T-04-17 | Tampering (XSS) | Рендер копирайта и материалов | mitigate | Только JSX-интерполяция, `dangerouslySetInnerHTML` отсутствует |
| T-04-18 | Denial of Service | Три бесконечные CSS-анимации частиц | mitigate | Только `transform`/`opacity` на трёх слоях, `will-change`, выключение при `prefers-reduced-motion` |
| T-04-SC | Tampering | npm installs | accept | Новых пакетов нет; `color-mix` и `grid-template-rows` transition нативные |
</threat_model>

<verification>
- `npm test` зелёный (включая `App.test.tsx` фазы 1 и тесты планов 04-01…04-03), `npm run build` проходит.
- Ручной smoke (`npm run dev`, 1440px): карточки расставлены как в `docs/research/orig-vp-6300.jpeg` (музыка слева сверху, текст в центре, материалы справа ниже, видео внизу слева от центра); частицы дрейфуют; клик по «Материалы» плавно раскрывает панель с 5 строками; Tab по строкам, Esc возвращает фокус на карточку; при `prefers-reduced-motion: reduce` в DevTools частицы замирают, панель открывается мгновенно.
- 390px: стек текст → музыка → материалы → видео без горизонтального скролла; панель во всю ширину.
- Переход на `http://localhost:5173/esd-onevoice27/#resources-materials` открывает панель материалов и прокручивает к секции.
</verification>

<success_criteria>
- RES-01: eyebrow «Ресурсы», H2 «Всё, что нужно для старта», асимметричная сетка трёх карточек с цветными индикаторами (#8f9dd6 / #7bc2c7 / #d28ebe) и дрейфующими частицами.
- RES-02: панель «Материалы» с 5 позициями и реальными ссылками ЕАД (описание проекта .docx, баннеры, «Желание веков», заставка, английские материалы) в новой вкладке.
- RES-03: панель «Видео» с 16 фасадами (VideoGrid из 04-03), панель «Музыка» с честной заглушкой «Песня ещё в работе».
- RES-04: повторный клик, «Свернуть» и Esc закрывают панель с возвратом фокуса; `aria-expanded` и `aria-controls` на карточках, `role="region"` на панели.
- Component-тесты панелей (11 проверок) зелёные; ни один файл вне `files_modified` не изменён.
</success_criteria>

<output>
Create `.planning/phases/04-news-resources-quote/04-04-SUMMARY.md` when done
</output>
