---
phase: 17-integration-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/setup.ts
  - src/components/hero/HeroParticles.test.tsx
  - src/App.seams.test.tsx
  - .planning/phases/17-integration-qa/qa/prod-hashes.mjs
  - .planning/phases/17-integration-qa/qa/results/preview-hashes.txt
  - .planning/phases/17-integration-qa/qa/results/prod-hashes.txt
autonomous: false
requirements: [SHIP-01, SHIP-02]
tags: [integration, vitest, gate, deploy, github-pages, sha256, seams]

must_haves:
  truths:
    - "После слияния фаз 14–16 в main гейт зелёный: `npx tsc -b` код 0, `npm test` не меньше 522 тестов без failed и skipped, `npm run lint` код 0, `npm run build` без предупреждений о размере чанков, `node scripts/check-dist.mjs` печатает `OK: 12 проверок`"
    - "В `src/styles/global.css` ровно один блок `@media (prefers-reduced-motion: reduce)` и это единственный CSS-файл с `prefers-reduced-motion`; `motionPolicy.test.ts` зелёный: `stars` (canvas частиц) и `pulse` (canvas огоньков) в обязательных, `new-light` в реестре, но не в обязательных"
    - "`App.test.tsx` и `App.seams.test.tsx` не печатают «Not implemented: HTMLMediaElement» (заглушки play/pause в `src/test/setup.ts`); в `HeroParticles.test.tsx` нет упоминания удалённого `GlobeCanvas.test.tsx`"
    - "`App.seams.test.tsx` содержит блок «стыки фаз v1.2»: hero складывает `video[data-anim=\"globe\"]` и `canvas[data-anim=\"stars\"]`, карта несёт `canvas.map-lights-canvas[data-anim=\"pulse\"]` с `data-light-count=\"942\"`, число узлов SVG на рендере всего приложения меньше 1300, набор значений `data-anim` в DOM равен восьми значениям реестра без `new-light`, label согласия и ссылки футера описаны боксами 44px"
    - "Прогон Deploy to GitHub Pages на HEAD после push завершился `success`; sha256 живых `index.html`, `index-*.js`, `index-*.css`, `vendor-map-*.js`, `hero-globe.webm`, `hero-globe.mp4` равны локальному `dist/`, таблица лежит в `qa/results/prod-hashes.txt`"
  artifacts:
    - path: "src/test/setup.ts"
      provides: "Глобальные заглушки HTMLMediaElement.prototype.play и pause для jsdom"
      contains: "HTMLMediaElement.prototype.play"
    - path: "src/components/hero/HeroParticles.test.tsx"
      provides: "Тест компонента без ссылки на удалённый GlobeCanvas.test.tsx"
      min_lines: 200
    - path: "src/App.seams.test.tsx"
      provides: "Блок describe «стыки фаз v1.2» на четыре теста поверх блока v1.1"
      contains: "стыки фаз v1.2"
      min_lines: 230
    - path: ".planning/phases/17-integration-qa/qa/prod-hashes.mjs"
      provides: "Node-скрипт сверки sha256 живого сайта с dist: index.html, ассеты из index.html, hero-globe.webm и .mp4, повторы при кэше Pages, таблица в файл"
      contains: "hero-globe.webm"
      min_lines: 80
    - path: ".planning/phases/17-integration-qa/qa/results/prod-hashes.txt"
      provides: "Таблица сверки прода после деплоя: путь, код, sha256 живого и dist, равен"
      contains: "| да"
  key_links:
    - from: "src/test/setup.ts"
      to: "src/App.test.tsx, src/App.seams.test.tsx"
      via: "setupFiles vite.config.ts: заглушки стоят до рендера App с <video>"
      pattern: "HTMLMediaElement\\.prototype\\.(play|pause)"
    - from: "src/App.seams.test.tsx"
      to: "src/components/hero/Hero.tsx, src/components/hero/HeroParticles.tsx"
      via: "querySelector video[data-anim=\"globe\"] и canvas.hero__particles[data-anim=\"stars\"] внутри section#hero"
      pattern: "data-anim=\"stars\""
    - from: "src/App.seams.test.tsx"
      to: "src/components/map/LightsCanvas.tsx"
      via: "canvas.map-lights-canvas с data-light-count при подменённом getBoundingClientRect у .esd-map"
      pattern: "data-light-count"
    - from: ".planning/phases/17-integration-qa/qa/prod-hashes.mjs"
      to: "dist/index.html и https://thevladoss.github.io/esd-onevoice27/"
      via: "список путей берётся из dist/index.html плюс два файла видео, каждый качается с Cache-Control: no-cache и хэшируется createHash(\"sha256\")"
      pattern: "createHash\\(\"sha256\"\\)"
    - from: ".github/workflows/deploy.yml"
      to: "https://thevladoss.github.io/esd-onevoice27/"
      via: "push в main → lint, test, build, check:dist → deploy-pages"
      pattern: "deploy-pages"
---

<objective>
Подтвердить, что три ветки v1.2 (hero на видео и canvas-частицах, огоньки карты на canvas, мобильная адаптация) живут в `main` вместе без дефектов стыка: закрыть два хвоста плана 14-02, закрепить стыки тестом на рендере всего приложения, пройти полный гейт, дождаться деплоя и побайтно сверить прод с `dist/`.

Purpose: SHIP-01 и SHIP-02, критерии 1, 2 и 5 фазы 17 в ROADMAP. Без зелёного гейта и подтверждённого прода приёмка плана 17-02 меряла бы неизвестно какую сборку.
Output: заглушки медиа в `src/test/setup.ts`, актуальный комментарий в `HeroParticles.test.tsx`, блок «стыки фаз v1.2» в `App.seams.test.tsx`, скрипт `qa/prod-hashes.mjs`, таблицы `qa/results/preview-hashes.txt` и `qa/results/prod-hashes.txt`, зелёный прогон workflow на HEAD.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@.planning/phases/14-hero-video-particles/14-02-SUMMARY.md
@.planning/phases/15-lights-canvas/15-02-SUMMARY.md
@.planning/phases/16-mobile/16-01-SUMMARY.md
@.planning/milestones/v1.1-phases/13-integration-qa/13-01-SUMMARY.md
@src/App.seams.test.tsx
@src/test/setup.ts
@src/components/hero/Hero.tsx
@src/components/hero/Hero.test.tsx
@src/components/hero/HeroParticles.test.tsx
@src/styles/motionPolicy.test.ts
@scripts/check-dist.mjs
@.github/workflows/deploy.yml

<ownership>
Работа в основном каталоге на ветке `main` после того, как оркестратор слил ветки `agent-14`, `agent-15`, `agent-16` и снял worktree. Исполнитель ничего не коммитит и не пушит: коммиты, `git push origin main` и запуск деплоя делает оркестратор на чекпоинте Task 3. Команды относительные, без `cd`. Тексты и комментарии на русском, идентификаторы на английском.

Правки исходников допускаются только в трёх файлах Task 1 и при дефекте стыка (тогда: правка в файле-владельце, запись в SUMMARY как deviation, гейт с начала). `src/styles/global.css` не трогать: селекторы `[data-anim="pulse"] circle`, `[data-anim="pulse"] .light-halo`, `[data-anim="new-light"]` в блоке reduce остаются no-op по решению roadmap v1.2. `package.json`, `package-lock.json`, `.planning/HANDOFF.json`, `STATE.md`, `ROADMAP.md` не менять. `node_modules` в репозиторий не попадает.

Playwright в этом плане не нужен. Preview для проверки скрипта хэшей поднимать на порту 4173 (`--strictPort`; сейчас порт свободен), после проверки остановить.
</ownership>

<orchestrator>
Шаги оркестратора вокруг этого плана (исполнитель их не делает):
1. До запуска плана: слить `agent-15` в `main` (как `merge(14)`, `merge(16)`), удалить worktree `esd_cringe-wt/15`, убедиться, что посторонние файлы worktree (`src/components/map/dbg.test.tsx`, симлинк `node_modules`) в `main` не попали.
2. На чекпоинте Task 3: закоммитить правки Task 1 и артефакты Task 2 (`test(17-01): стыки v1.2, заглушки медиа в setup, скрипт сверки хэшей прода`), выполнить `git push origin main`, затем возобновить исполнителя ответом `pushed <sha>`.
3. После Task 4: закоммитить `qa/results/prod-hashes.txt` (`docs(17-01): сверка хэшей прода после деплоя v1.2`); push вместе с документами плана 17-02.
</orchestrator>

<interfaces>
<!-- Контракты слитых фаз, на которые опираются тесты стыков. Использовать как есть. -->

From src/components/hero/Hero.tsx (фаза 14): `<section id="hero" class="hero">` → дети по порядку `div.hero__video > video[autoplay muted loop playsinline preload="auto" disablepictureinpicture disableremoteplayback tabindex="-1" aria-hidden="true" data-anim="globe"]` с двумя `<source>` (`type="video/webm"` со `src` `${BASE_URL}hero-globe.webm`, затем `video/mp4`), затем `canvas.hero__particles[data-anim="stars"][aria-hidden="true"]` (HeroParticles), затем `div.hero__content`. В vitest `import.meta.env.BASE_URL` равен `/`. При `navigator.connection.saveData === true` источников нет и `preload="none"`.

From src/components/hero/hero.css: блок `.hero__particles {` содержит `opacity: .72` и `mix-blend-mode: screen`; блок `.hero__video {` содержит `z-index: 0`.

From src/components/map/LightsCanvas.tsx и EsdMap.tsx (фаза 15, план 15-02): внутри `div.esd-map` после `svg.esd-map__svg` лежит `<canvas class="map-lights-canvas" data-anim="pulse" aria-hidden="true" data-light-count="942" data-people="694" data-groups="248" data-new="0">`; в jsdom `getContext` даёт null, атрибуты выставляются, rAF не вызывается. EsdMap строит проекцию только при измеренном контейнере: тест подменяет `HTMLDivElement.prototype.getBoundingClientRect` для узлов с классом `esd-map` на `DOMRect(0, 0, 1200, 700)` (образец в тесте «скос карты и canvas огоньков на месте (8 + 15)» блока v1.1). В SVG карты после фазы 15 нет `circle`, `defs`, `.map-lights`, `.light-bucket`, `.light-core`, `.light-ring`.

From src/components/map/lightsCanvas.ts: `BREATH_PERIOD_MS = 2600`, `LIGHT_BUCKETS = 5`, `HALO_RADIUS_MIN = 7`, `HALO_RADIUS_MAX = 12`, `HALO_ALPHA_MIN = 0.3`, `HALO_ALPHA_MAX = 0.6`, `MIN_FRAME_GAP_MS = 33`, `MAX_DPR = 2` (план 15-03 мог снизить до 1.5, см. 15-03-SUMMARY).

From src/components/form/ConsentCheckbox.tsx и light-form.css (фаза 16): `label.lf-check[for=<id>]` содержит `input.lf-checkbox[type="checkbox"]` и `span.lf-check-text`; блок `.lf-check {` содержит `min-height: 44px`, блок `.lf-checkbox {` содержит `width: 20px`, `height: 20px`, `margin: 0`.

From src/components/layout/Footer.css (фаза 16): блок `.site-footer__links a {` содержит `display: inline-flex`, `min-height: 44px`, `padding-inline: 8px`; блок `.site-footer__links ul {` содержит `gap: 0`. В DOM две ссылки `.site-footer__links a`.

From src/styles/motionPolicy.test.ts: `REGISTRY` из девяти значений `stars, globe, beam, pulse, new-light, particles, atmosphere, wave, halo`; обязательные после фазы 15: `stars, globe, beam, pulse, wave, halo`. В DOM отрендеренного `<App />` ожидаются восемь значений: `stars` (canvas hero), `globe` (video), `beam` (кнопки с лучом), `pulse` (canvas карты), `particles` и `atmosphere` (секция ресурсов), `wave` и `halo` (футер); `new-light` узла в DOM не имеет (кольцо рисует canvas).

From src/test/setup.ts: `vi.stubGlobal` для `IntersectionObserver`/`ResizeObserver`, `window.matchMedia` через `vi.fn`, `HTMLCanvasElement.prototype.getContext` → null, `Element.prototype.scrollIntoView` и `window.scrollTo` через `vi.fn`, `beforeEach(resetProgrammaticScroll)`.

From src/components/hero/Hero.test.tsx: `beforeEach` ставит `vi.spyOn(HTMLMediaElement.prototype, "play").mockImplementation(() => Promise.resolve())` и такой же spy на `pause`, `afterEach` зовёт `vi.restoreAllMocks()`; тесты считают вызовы `play`/`pause` по этим spy. Глобальные заглушки из setup.ts они перекрывают на время теста и возвращают после.

From scripts/check-dist.mjs: `urlsOf(html)` собирает значения `src`/`href` регуляркой `/(?:src|href)="([^"]+)"/g`; `HERO_VIDEO = ["hero-globe.webm", "hero-globe.mp4"]`; после фазы 14 скрипт печатает `OK: 12 проверок`.

From .planning/STATE.md (решения): ожидание прогона Actions делается циклом `gh run view --json status,conclusion` с паузой, обёртка `timeout` для `gh run watch` на macOS недоступна; приём деплоя = код 200 + пустой diff списка ассетов + совпадение sha256 живых файлов и локального dist.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Стыки после слияния, два хвоста плана 14-02, блок «стыки фаз v1.2» в App.seams.test.tsx</name>
  <files>src/test/setup.ts, src/components/hero/HeroParticles.test.tsx, src/App.seams.test.tsx</files>
  <read_first>
    - .planning/phases/14-hero-video-particles/14-02-SUMMARY.md, раздел «Issues Encountered» (два хвоста: шум «Not implemented: HTMLMediaElement's play() method» из App.test/App.seams, устаревший комментарий в HeroParticles.test.tsx)
    - .planning/phases/15-lights-canvas/15-02-SUMMARY.md (как переписан тест карты в App.seams.test.tsx, атрибуты canvas, что удалено из SVG, напоминание про no-op селекторы global.css)
    - src/App.seams.test.tsx целиком (хелперы `read`, `flat`, `block`, `renderApp`, мок getBoundingClientRect в тесте карты)
    - src/components/hero/Hero.test.tsx строки 34–46 (заглушки play/pause через spyOn)
    - src/test/setup.ts целиком
  </read_first>
  <action>
    Реализует SHIP-01 (реестр и политика не нарушены после слияния) и критерий 5 фазы 17 (стыки без дефектов).

    **Проверка слияния (до правок).** `git branch --show-current` → `main`; `git worktree list | wc -l` → 1; `git status --porcelain | grep -v HANDOFF.json` пуст. Файлы фазы 15 на месте: `ls src/components/map/LightsCanvas.tsx src/components/map/lightsCanvas.ts src/components/map/LightsCanvas.test.tsx`. Посторонних файлов нет: `test ! -e src/components/map/dbg.test.tsx`, `git ls-files node_modules | wc -l` → 0, `ls src/components/hero/GlobeCanvas.tsx src/components/hero/globe.ts src/components/hero/Starfield.tsx 2>&1 | grep -c "No such file"` → 3. Реестр движения: `grep -c "@media (prefers-reduced-motion: reduce)" src/styles/global.css` → 1; `grep -rl "prefers-reduced-motion" src --include='*.css' | wc -l` → 1; `grep -rho 'data-anim="[a-z-]*"' src --include='*.tsx' | sort -u` даёт только значения реестра (девять допустимых, `new-light` может встречаться лишь в селекторах тестов); `grep -c "light-bucket\|\.light-core\|\.map-lights\b" src/components/map/EsdMap.tsx` → 0; `grep -c "@keyframes light-breathe\|@property --halo-k" src/components/map/map.css` → 0; в `src/styles/motionPolicy.test.ts` массив обязательных значений содержит `"pulse"` и `"stars"` и не содержит `"new-light"`, а `REGISTRY` содержит `"new-light"`. Стык 15 + 16 в `App.seams.test.tsx`: тест формы (8 + 9) со строками `LIGHT_FORM_CSS` и `.lf-section` не тронут, тест карты переписан на `canvas.map-lights-canvas` и константы `lightsCanvas.ts`. Любое расхождение здесь означает дефект слияния: записать в SUMMARY и вернуть оркестратору до правок (слияние чинит он).

    Прогнать `npx vitest run src/App.test.tsx src/App.seams.test.tsx src/styles/motionPolicy.test.ts src/components/hero src/components/map 2>&1 | tee /dev/stderr | grep -c "Not implemented"`: до правок ожидается число больше 0 (шум из двух файлов с рендером App); зафиксировать число в SUMMARY.

    **Хвост 1: `src/test/setup.ts`.** После строки с `HTMLCanvasElement.prototype.getContext` добавить две заглушки обычными функциями (не `vi.fn`, чтобы между тестами не копилось состояние мока): `HTMLMediaElement.prototype.play = () => Promise.resolve();` и `HTMLMediaElement.prototype.pause = () => {};` с комментарием на русском: jsdom не реализует медиа, без заглушек каждый рендер `App` с `<video>` печатает «Not implemented»; `Hero.test.tsx` ставит поверх свои spy через `vi.spyOn` и снимает их `restoreAllMocks`, поэтому подсчёт вызовов там не меняется. Ничего другого в файле не трогать (`getContext` → null обязателен для обоих canvas).

    **Хвост 2: `src/components/hero/HeroParticles.test.tsx`.** Заменить блок-комментарий над `mockContext` («Хелперы повторяют GlobeCanvas.test.tsx: тот файл удаляет план 14-02, поэтому они скопированы, а не импортированы») на актуальный: мок 2d-контекста, размера canvas и reduced motion объявлены локально, общего хелпера для canvas-тестов в проекте нет, `LightsCanvas.test.tsx` фазы 15 держит свой. Код тестов не менять. `grep -c "GlobeCanvas" src/components/hero/HeroParticles.test.tsx` → 0.

    **Блок «стыки фаз v1.2» в `src/App.seams.test.tsx`.** Существующий `describe("стыки фаз v1.1")` не менять ни на символ. Добавить чтения `const HERO_CSS = read("src/components/hero/hero.css")` и `const FOOTER_CSS = read("src/components/layout/Footer.css")` рядом с остальными; добавить хелпер `renderMeasuredApp()` (подмена `HTMLDivElement.prototype.getBoundingClientRect` для узлов с классом `esd-map` на `new DOMRect(0, 0, 1200, 700)` на время `renderApp()` с возвратом в `finally`, как в тесте карты блока v1.1) и новый `describe("стыки фаз v1.2")` с четырьмя тестами:
    1. «hero складывает видео оригинала и canvas частиц (14)»: `renderApp()`; `hero = document.querySelector("section#hero")` не null; `hero.children[0]` имеет класс `hero__video` и содержит `video[data-anim="globe"]` с атрибутами `autoplay`, `loop`, `playsinline`, `preload="auto"`, `aria-hidden="true"`, `tabindex="-1"`; `video.querySelectorAll("source")` длиной 2, `[0]` с `type="video/webm"` и `src`, оканчивающимся на `hero-globe.webm`, `[1]` с `type="video/mp4"` и `hero-globe.mp4`; `hero.children[1]` это `canvas` с классом `hero__particles`, `data-anim="stars"`, `aria-hidden="true"`; `hero.children[2]` имеет класс `hero__content`; `block(HERO_CSS, ".hero__particles {")` содержит `mix-blend-mode: screen` и `opacity: .72`; `block(HERO_CSS, ".hero__video {")` содержит `z-index: 0`.
    2. «карта рисует огоньки на canvas, а SVG держит только страны (15 + 8)»: `renderMeasuredApp()`; `canvas = document.querySelector(".esd-map > canvas.map-lights-canvas")` не null, `data-anim="pulse"`, `aria-hidden="true"`, `data-light-count="942"`, `data-people="694"`, `data-groups="248"`, `data-new="0"`; `canvas.previousElementSibling?.tagName.toLowerCase()` равен `svg`; `document.querySelectorAll(".esd-map svg circle, .esd-map svg defs, .map-lights, .light-bucket, .light-core, .light-ring")` длиной 0; `document.querySelectorAll(".esd-map svg path").length` не меньше 177 (страны отрендерены); `document.querySelectorAll("svg, svg *").length` меньше 1300 (структурная половина LIGHT-07; число записать в SUMMARY).
    3. «форма и футер держат цели касания 44px (16 + 9)»: `renderApp()`; `#light-form label.lf-check` содержит `input.lf-checkbox[type="checkbox"]` и `span.lf-check-text`; `block(LIGHT_FORM_CSS, ".lf-check {")` содержит `min-height: 44px`; `block(LIGHT_FORM_CSS, ".lf-checkbox {")` содержит `width: 20px` и `margin: 0`; `document.querySelectorAll(".site-footer__links a")` длиной 2; `block(FOOTER_CSS, ".site-footer__links a {")` содержит `min-height: 44px` и `inline-flex`; `block(FOOTER_CSS, ".site-footer__links ul {")` содержит `gap: 0`.
    4. «реестр data-anim в DOM после слияния (14 + 15)»: `renderMeasuredApp()`; `used = new Set(Array.from(document.querySelectorAll("[data-anim]")).map(n => n.getAttribute("data-anim")))`; `[...used].sort()` равен `["atmosphere", "beam", "globe", "halo", "particles", "pulse", "stars", "wave"]` (восемь значений, `new-light` отсутствует: кольцо рисует canvas); `document.querySelectorAll('[data-anim="stars"]')` длиной 1 с тегом `CANVAS`, `[data-anim="globe"]` длиной 1 с тегом `VIDEO`, `[data-anim="pulse"]` длиной 1 с тегом `CANVAS`. Перед написанием подтвердить командой `grep -rln 'data-anim="atmosphere"\|data-anim="particles"' src --include='*.tsx' | grep -v test`, что эти значения ставит компонент, который монтируется под `<App />` (секция ресурсов); если какое-то значение в DOM jsdom не появляется, разобраться почему и записать в SUMMARY, а не подгонять список.
    В `afterEach` файла уже есть возврат реальных таймеров и снятие `resources-panel-locked`, новых `afterEach` не нужно. Тест «рендерит страницу молча» из `App.test.tsx` роняет прогон при любом `console.error`: новый блок их порождать не должен.

    Прогнать `npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts src/components/hero src/components/map 2>&1 | tee /dev/stderr | grep -c "Not implemented"` → 0, все тесты passed; `npx eslint src/App.seams.test.tsx src/test/setup.ts src/components/hero/HeroParticles.test.tsx` → код 0.
  </action>
  <verify>
    <automated>npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts src/components/hero src/components/map 2>&1 | tee /dev/stderr | grep -c "Not implemented" | grep -qx 0 && npx eslint src/App.seams.test.tsx src/test/setup.ts src/components/hero/HeroParticles.test.tsx && grep -c "стыки фаз v1.2" src/App.seams.test.tsx | grep -qx 1 && grep -c "GlobeCanvas" src/components/hero/HeroParticles.test.tsx | grep -qx 0 && grep -c "HTMLMediaElement.prototype.play" src/test/setup.ts | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list | wc -l` → 1; `test ! -e src/components/map/dbg.test.tsx`; `git ls-files node_modules | wc -l` → 0
    - `grep -c "@media (prefers-reduced-motion: reduce)" src/styles/global.css` → 1; `grep -rl "prefers-reduced-motion" src --include='*.css' | wc -l` → 1
    - `grep -c "HTMLMediaElement.prototype.play" src/test/setup.ts` → 1; `grep -c "HTMLMediaElement.prototype.pause" src/test/setup.ts` → 1; `grep -c "vi.fn(() => Promise" src/test/setup.ts` → 0; строка `HTMLCanvasElement.prototype.getContext` на месте
    - `grep -c "GlobeCanvas" src/components/hero/HeroParticles.test.tsx` → 0; `grep -rc "GlobeCanvas\|Starfield" src | grep -v ":0$" | wc -l` → 0
    - `grep -c 'describe("стыки фаз v1.1"' src/App.seams.test.tsx` → 1; `grep -c 'describe("стыки фаз v1.2"' src/App.seams.test.tsx` → 1; `grep -c "^  it(" src/App.seams.test.tsx` → 9 (пять v1.1 плюс четыре v1.2); файл содержит строки `hero__particles`, `data-anim="stars"`, `data-light-count`, `svg, svg *`, `1300`, `lf-check`, `site-footer__links a`, `renderMeasuredApp`
    - `git diff --stat src/App.seams.test.tsx` не затрагивает строки существующих тестов блока v1.1 (в диффе только добавления: новые константы, хелпер и describe)
    - `npx vitest run src/App.seams.test.tsx src/App.test.tsx src/styles/motionPolicy.test.ts src/components/hero src/components/map` → код 0, 0 failed, 0 skipped; в выводе нет `Not implemented`, `act(`, `console.error`
    - `npx eslint src/App.seams.test.tsx src/test/setup.ts src/components/hero/HeroParticles.test.tsx` → код 0
    - В SUMMARY записаны: число строк «Not implemented» до правки, число узлов SVG на рендере App из теста 2, набор `data-anim` из теста 4
  </acceptance_criteria>
  <done>Слияние проверено командами, оба хвоста 14-02 закрыты, четыре теста стыков v1.2 зелёные рядом с пятью тестами v1.1, прогон без шума jsdom.</done>
</task>

<task type="auto">
  <name>Task 2: Полный гейт, скрипт сверки хэшей prod-hashes.mjs и его проверка на preview</name>
  <files>.planning/phases/17-integration-qa/qa/prod-hashes.mjs, .planning/phases/17-integration-qa/qa/results/preview-hashes.txt</files>
  <read_first>
    - .planning/milestones/v1.1-phases/13-integration-qa/13-01-SUMMARY.md, разделы «Гейт» и «Деплой и прод» (формат таблицы хэшей, ловушка с переменной `path` в zsh)
    - .planning/milestones/v1.1-phases/13-integration-qa/qa/results/prod-hashes.txt (формат таблицы, который план 17-02 переносит в SMOKE)
    - scripts/check-dist.mjs (функции `urlsOf`, `jsAssets`, константы `BASE`, `HERO_VIDEO`; стиль вывода `OK`/`FAIL`)
    - .planning/phases/14-hero-video-particles/14-02-SUMMARY.md, «Next Phase Readiness» (`OK: 12 проверок`, новая строка про видео)
  </read_first>
  <action>
    Реализует SHIP-01 (гейт) и первую половину SHIP-02 (инструмент сверки, проверенный до деплоя).

    **Гейт** по порядку, каждый шаг код 0: `npx tsc -b` (вывод пуст); `npm test` (`vitest run`: не меньше 522 тестов и 51 файла, 0 failed, 0 skipped, в выводе нет `act(`, `console.error`, `Not implemented`); `npm run lint` (без предупреждений); `npm run build` (без предупреждений о размере чанков; записать имена и размеры `index-*.js`, `index-*.css`, `vendor-map-*.js`, `index.html`); `node scripts/check-dist.mjs` (последняя строка `OK: 12 проверок`, среди строк `OK видео глобуса`). Числа каждого шага записать в SUMMARY в таблицу «Гейт» по образцу 13-01-SUMMARY: план 17-02 переносит её в SMOKE. Провал любого шага означает дефект слияния: причину найти, поправить в файле-владельце, записать как deviation, гейт с начала.

    **Скрипт `.planning/phases/17-integration-qa/qa/prod-hashes.mjs`** (создать каталоги `qa/` и `qa/results/`). ESM, импорты только `node:fs`, `node:path`, `node:crypto`, `node:process`; сеть через глобальный `fetch` (Node 25). Шапка-комментарий на русском с назначением и примерами запуска. CLI: `--base <url>` (по умолчанию `https://thevladoss.github.io/esd-onevoice27/`), `--dist <dir>` (по умолчанию `dist`), `--out <file>` (по умолчанию `results/prod-hashes.txt` рядом со скриптом через `fileURLToPath(new URL("./results/", import.meta.url))`), `--retries <n>` (по умолчанию 5), `--wait <sec>` (по умолчанию 60). Логика: прочитать `dist/index.html`; список путей = `index.html` плюс все значения `src`/`href` из него, начинающиеся с `/esd-onevoice27/` (регулярка как `urlsOf` в check-dist: ассеты JS/CSS, `favicon.svg`), плюс константа `EXTRA_FILES = ["hero-globe.webm", "hero-globe.mp4"]`; для каждого пути скачать `new URL(путь_без_префикса_base, base)` с заголовками `Cache-Control: no-cache` и `Pragma: no-cache`, взять `response.status` и sha256 тела (`createHash("sha256").update(Buffer.from(await response.arrayBuffer())).digest("hex")`), сравнить с sha256 локального файла `join(dist, путь_без_/esd-onevoice27/)`; дополнительно сравнить список ассетов `/esd-onevoice27/assets/*` из живого `index.html` со списком из `dist/index.html` (должны совпасть). Если хоть один файл не равен или отвечает не 200, подождать `--wait` секунд и повторить весь проход, до `--retries` раз (кэш Pages после свежего деплоя). Вывод в stdout и в `--out`: шапка (base, dist, время, `git rev-parse HEAD`, число попыток), Markdown-таблица «Путь | Код | sha256 живого (первые 12) | sha256 dist (первые 12) | Равен» и строка «N из N равны» или «M из N равны, расхождения: …». Код выхода 0 при полном совпадении, 1 при расхождении, 2 при ошибке аргументов или отсутствии `dist/index.html`. Переменные не называть `path` (в zsh это массив `PATH`; в Node не мешает, но соглашение из 13-01 держим).

    **Проверка скрипта на preview.** `npx vite preview --port 4173 --strictPort` в фоне; `curl -s -o /dev/null -w '%{http_code}' http://localhost:4173/esd-onevoice27/` → `200`; `node .planning/phases/17-integration-qa/qa/prod-hashes.mjs --base http://localhost:4173/esd-onevoice27/ --out .planning/phases/17-integration-qa/qa/results/preview-hashes.txt --retries 1` → код 0, все строки «да», в таблице есть `index.html`, `index-`, `vendor-map-`, `.css`, `hero-globe.webm`, `hero-globe.mp4`; ожидаемое число строк 7 (index.html, favicon.svg, три ассета, два видео; если `favicon.svg` в `index.html` не ссылается, строк 6). Остановить preview, `lsof -nP -iTCP:4173 -sTCP:LISTEN | wc -l` → 0. Затем прогон против прода до деплоя: `node .planning/phases/17-integration-qa/qa/prod-hashes.mjs --retries 1 --out /dev/null`; ожидается код 1 с расхождениями по `index.html` и ассетам (прод ещё на сборке v1.1) и равенством `hero-globe.webm`/`hero-globe.mp4`, если видео уже лежало в `public/` на сборке v1.1 (это подтверждает, что скрипт различает равные и неравные файлы). Результат записать в SUMMARY одной строкой.

    `npx eslint .planning/phases/17-integration-qa/qa/prod-hashes.mjs` → код 0 (конфиг eslint проекта проверяет `.mjs`; если файл вне scope линтера, достаточно `node --check`). `git status --porcelain | grep -v HANDOFF.json` показывает только три файла Task 1, скрипт и `preview-hashes.txt`.
  </action>
  <verify>
    <automated>npx tsc -b && npm run lint && npm run build 2>&1 | grep -qv "chunks are larger" && node scripts/check-dist.mjs | tail -1 | grep -q "OK: 12 проверок" && node --check .planning/phases/17-integration-qa/qa/prod-hashes.mjs && grep -c "| да" .planning/phases/17-integration-qa/qa/results/preview-hashes.txt | awk '{exit ($1 >= 6) ? 0 : 1}' && grep -q "hero-globe.mp4" .planning/phases/17-integration-qa/qa/results/preview-hashes.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc -b` код 0; `npm test` код 0, не меньше 522 тестов, 0 failed, 0 skipped, без `act(`, `console.error`, `Not implemented`; `npm run lint` код 0; `npm run build` код 0 без предупреждений о размере чанков; `node scripts/check-dist.mjs` печатает `OK: 12 проверок`
    - `grep -c "@media (prefers-reduced-motion: reduce)" src/styles/global.css` → 1 (гейт не сдвинул)
    - Файл `qa/prod-hashes.mjs` существует, содержит `createHash("sha256")`, `hero-globe.webm`, `hero-globe.mp4`, `Cache-Control`, `--retries`, `arrayBuffer`, `/esd-onevoice27/assets/`; импортов кроме `node:*` нет; `node --check` код 0
    - `qa/results/preview-hashes.txt`: не меньше 6 строк с `| да`, ни одной с `| нет`, итоговая строка «N из N равны»
    - Прогон против прода до деплоя завершился кодом 1 с расхождением по `index.html` (записано в SUMMARY)
    - Порт 4173 свободен после задачи; в корне репозитория нет новых файлов; `git status --porcelain | grep -v HANDOFF.json` показывает ровно пять путей: `src/test/setup.ts`, `src/components/hero/HeroParticles.test.tsx`, `src/App.seams.test.tsx`, `qa/prod-hashes.mjs`, `qa/results/preview-hashes.txt`
  </acceptance_criteria>
  <done>Гейт из пяти команд зелёный на слитом коде, скрипт сверки хэшей проверен на preview (все равны) и на старом проде (различия найдены), рабочее дерево готово к коммиту оркестратора.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Коммит и push в main (шаг оркестратора)</name>
  <action>
    Исполнитель не коммитит и не пушит. Оркестратор: стадировать поимённо `src/test/setup.ts`, `src/components/hero/HeroParticles.test.tsx`, `src/App.seams.test.tsx`, `.planning/phases/17-integration-qa/qa/prod-hashes.mjs`, `.planning/phases/17-integration-qa/qa/results/preview-hashes.txt` (без `.planning/HANDOFF.json` и без `dist/`), закоммитить `test(17-01): стыки v1.2, заглушки медиа в setup, скрипт сверки хэшей прода`, выполнить `git push origin main`. Push запускает `Deploy to GitHub Pages` (lint → test → build → check:dist → deploy). Затем возобновить исполнителя.
  </action>
  <verify>
    <automated>git fetch origin && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && test "$(git status --porcelain | grep -v HANDOFF.json | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <resume-signal>Ответ оркестратора вида `pushed <sha>`; исполнитель проверяет, что `git rev-parse HEAD` равен `origin/main` и рабочее дерево чистое, и переходит к Task 4</resume-signal>
  <done>Коммит `test(17-01)` лежит в `origin/main`, рабочее дерево чистое, прогон Deploy to GitHub Pages запущен на HEAD.</done>
</task>

<task type="auto">
  <name>Task 4: Ожидание Deploy to GitHub Pages и побайтная сверка прода с dist</name>
  <files>.planning/phases/17-integration-qa/qa/results/prod-hashes.txt</files>
  <read_first>
    - README.md, раздел «Деплой и живой сайт» (команды `gh run list`, `gh run watch`, `curl -sI`)
    - .planning/STATE.md, решение фазы 1 про ожидание прогона циклом `gh run view` (обёртка `timeout` на macOS недоступна)
    - .github/workflows/deploy.yml (concurrency `pages` без отмены: второй push встанет в очередь, а не отменит первый)
  </read_first>
  <action>
    Реализует вторую половину SHIP-02.

    1. Убедиться, что чекпоинт пройден: `git fetch origin`, `git rev-parse HEAD` равен `git rev-parse origin/main`, `git status --porcelain | grep -v HANDOFF.json` пуст. Если нет, задачу не начинать: вернуть оркестратору «17-01 Task 4 ждёт коммита и push» и остановиться.
    2. Найти прогон на HEAD: `HEAD=$(git rev-parse HEAD)`; до 12 раз с паузой 10 с: `RUN_ID=$(gh run list --workflow deploy.yml --limit 3 --json databaseId,headSha --jq ".[] | select(.headSha == \"$HEAD\") | .databaseId" | head -1)`, выйти из цикла, когда `RUN_ID` не пуст. Дождаться завершения циклом до 30 итераций с паузой 30 с: `gh run view "$RUN_ID" --json status,conclusion --jq '.status + " " + .conclusion'`, выход при `completed *`. Ожидание: `completed success`. При `failure` прочитать `gh run view "$RUN_ID" --log-failed`, найти причину (в CI Node 22 и `npm ci`; локально гейт был зелёный, поэтому вероятны только различия окружения), поправить, записать deviation и вернуть оркестратору на повторный коммит и push (Task 3 повторяется).
    3. Живой сайт: `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` → `HTTP/2 200`. Сверка: `node .planning/phases/17-integration-qa/qa/prod-hashes.mjs --out .planning/phases/17-integration-qa/qa/results/prod-hashes.txt` (повторы и пауза по умолчанию: 5 × 60 с на случай кэша Pages) → код 0, строка «N из N равны», в таблице `index.html`, `index-*.js`, `index-*.css`, `vendor-map-*.js`, `hero-globe.webm`, `hero-globe.mp4` (и `favicon.svg`, если он в `index.html`), у каждого код 200 и «да». Имена ассетов должны совпадать с записанными в SUMMARY после `npm run build` Task 2; если `dist/` на диске отсутствует или устарел (например, после `git pull`), выполнить `npm run build` заново перед сверкой: сборка детерминирована по коммиту. Устойчивое расхождение после пяти попыток означает, что прод отдаёт не этот коммит: остановиться и вернуть блокер, план 17-02 без этого не запускать.
    4. Дописать в начало `prod-hashes.txt` строки с `HEAD`, id и URL прогона (`https://github.com/thevladoss/esd-onevoice27/actions/runs/<RUN_ID>`) и временем, если скрипт их не печатает сам; файл переносится в `docs/qa/SMOKE.md` планом 17-02. Коммит этого файла делает оркестратор (см. `<orchestrator>`).
    5. В SUMMARY: id прогона, длительность, число попыток сверки, таблица хэшей целиком, имена ассетов сборки.
  </action>
  <verify>
    <automated>RUN_ID=$(gh run list --workflow deploy.yml --limit 3 --json databaseId,headSha --jq ".[] | select(.headSha == \"$(git rev-parse HEAD)\") | .databaseId" | head -1); test -n "$RUN_ID" && test "$(gh run view "$RUN_ID" --json conclusion --jq .conclusion)" = success && test "$(curl -s -o /dev/null -w '%{http_code}' https://thevladoss.github.io/esd-onevoice27/)" = 200 && grep -c "| да" .planning/phases/17-integration-qa/qa/results/prod-hashes.txt | awk '{exit ($1 >= 6) ? 0 : 1}' && grep -c "| нет" .planning/phases/17-integration-qa/qa/results/prod-hashes.txt | grep -qx 0 && grep -q "hero-globe.webm" .planning/phases/17-integration-qa/qa/results/prod-hashes.txt</automated>
  </verify>
  <acceptance_criteria>
    - `gh run list --workflow deploy.yml --limit 3 --json headSha,conclusion` содержит запись с `headSha` равным `git rev-parse HEAD` и `conclusion` `success`
    - `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` → `HTTP/2 200`
    - `qa/results/prod-hashes.txt` существует, содержит `HEAD`, `actions/runs/`, строки для `index.html`, `index-`, `.css`, `vendor-map-`, `hero-globe.webm`, `hero-globe.mp4`, у всех код 200 и `| да`; строк `| нет` нет; итоговая строка «N из N равны» с N не меньше 6
    - Список ассетов живого `index.html` равен списку `dist/index.html` (скрипт печатает «список ассетов совпал»)
    - `git status --porcelain | grep -v HANDOFF.json` показывает только `qa/results/prod-hashes.txt`
  </acceptance_criteria>
  <done>Прод отдаёт сборку HEAD из main: workflow зелёный, шесть или семь живых файлов побайтно равны `dist/`, включая оба файла видео; таблица сохранена для SMOKE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Рабочее дерево ← слияние трёх веток | чужие файлы worktree (`dbg.test.tsx`, симлинк `node_modules`) могут попасть в `main` |
| Тесты jsdom → прототипы DOM (`HTMLMediaElement`, `HTMLCanvasElement`) | глобальные заглушки в setup влияют на все тесты |
| Скрипт хэшей → GitHub Pages | чтение публичных файлов по HTTPS, ничего не отправляется |
| Оркестратор → GitHub (push, workflow с OIDC) | публикация сборки; секретов у исполнителя нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `src/test/setup.ts` | mitigate | заглушки play/pause обычными функциями без состояния; `Hero.test.tsx` кладёт поверх свои spy и снимает их; `getContext` → null не трогается |
| T-17-02 | Tampering | слияние веток | mitigate | Task 1 проверяет `worktree list`, отсутствие `dbg.test.tsx`, `git ls-files node_modules`, реестр `data-anim`, единственный блок reduce |
| T-17-03 | Repudiation | приём прода | mitigate | sha256 живых файлов против `dist/` тем же скриптом на preview (равны) и на старом проде (различия), таблица в `qa/results/` |
| T-17-04 | Denial of service | кэш Pages после деплоя | mitigate | до 5 повторов с паузой 60 с; заголовок `Cache-Control: no-cache` |
| T-17-05 | Spoofing | `gh run list` по последнему прогону | mitigate | прогон выбирается по `headSha == HEAD`, а не «последний» |
| T-17-06 | Elevation of privilege | джоба build с OIDC-токеном | accept | `permissions` в deploy.yml выданы только джобе deploy; workflow не меняется |
| T-17-SC | Tampering | npm/pip/cargo установки | accept | пакеты не добавляются; `package.json` и `package-lock.json` неизменны, скрипт хэшей на `node:*` |
</threat_model>

<verification>
- `npx vitest run` без `Not implemented`, `act(`, `console.error`; не меньше 522 тестов; девять `it(` в `App.seams.test.tsx`
- Гейт: `npx tsc -b`, `npm run lint`, `npm run build`, `node scripts/check-dist.mjs` (`OK: 12 проверок`) с кодом 0
- Один блок reduce в `global.css`; `motionPolicy.test.ts` зелёный без `new-light` в обязательных
- Прогон deploy.yml на HEAD `success`; `prod-hashes.txt` без строк `| нет`, с `hero-globe.webm` и `hero-globe.mp4`
</verification>

<success_criteria>
- SHIP-01: гейт зелёный на слитом коде, реестр и политика движения не нарушены, тест стыков v1.2 закрепляет hero, карту, форму и футер вместе
- SHIP-02: сборка HEAD опубликована на GitHub Pages, sha256 всех живых файлов (включая видео) равны `dist/`
- Критерий 5 фазы 17: два хвоста 14-02 закрыты, `App.seams.test.tsx` читает атрибуты canvas карты и CSS формы фазы 16, `Hero.test.tsx` и `motionPolicy.test.ts` зелёные вместе
</success_criteria>

<output>
Create `.planning/phases/17-integration-qa/17-01-SUMMARY.md` when done: таблица «Гейт» с числами каждого шага, число узлов SVG из теста стыков, набор `data-anim` в DOM, id прогона workflow, таблица хэшей прода, список дефектов слияния (ожидается пустой) и отклонений.
</output>
