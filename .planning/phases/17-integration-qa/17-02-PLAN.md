---
phase: 17-integration-qa
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - .planning/phases/17-integration-qa/qa/v12-measure.js
  - .planning/phases/17-integration-qa/qa/v12-run.mjs
  - .planning/phases/17-integration-qa/qa/results/prod-measure-1440.json
  - .planning/phases/17-integration-qa/qa/results/prod-measure-390.json
  - .planning/phases/17-integration-qa/qa/results/prod-measure-1440-reduced.json
  - .planning/phases/17-integration-qa/qa/results/orig-measure-1440.json
  - .planning/phases/17-integration-qa/qa/results/orig-measure-390.json
  - .planning/phases/17-integration-qa/qa/results/prod-fps-1440.json
  - .planning/phases/17-integration-qa/qa/results/prod-fps-390-cpu4.json
  - .planning/phases/17-integration-qa/qa/results/orig-fps-390-cpu4.json
  - docs/qa/v12-hero-1440.jpeg
  - docs/qa/v12-hero-390.jpeg
  - docs/qa/v12-map-1440.jpeg
  - docs/qa/v12-map-390.jpeg
  - docs/qa/SMOKE.md
  - README.md
autonomous: true
requirements: [SHIP-03, SHIP-04, LIGHT-07]
tags: [playwright, acceptance, fps, cdp, smoke, screenshots, docs, github-pages]

must_haves:
  truths:
    - "На проде 1440×900 и 390×844 видео `video[data-anim=\"globe\"]` воспроизводится: `paused === false`, `currentTime` за 1,5 с растёт не меньше чем на 1 с, `currentSrc` оканчивается на `hero-globe.webm`; на 1440×900 видео 1067×600 (±2px) прижато к правому верхнему углу hero с `object-fit: contain` и `mix-blend-mode: screen`, на 390×844 видео 390×844 с `object-fit: cover`; те же размеры и `object-fit` у оригинала (или значения из `docs/research/v1.2/measurements.md`, если challenge не пройден, с записью ограничения)"
    - "Canvas частиц `canvas[data-anim=\"stars\"]` на проде рисует 24–31 кадр в секунду на обоих вьюпортах (порог 33,3 мс между кадрами, как у скрипта оригинала), с `opacity: 0.72` и `mix-blend-mode: screen`"
    - "Таблица fps по секциям: на 390×844 при CPU×4 hero, карта и форма держат медиану ≥ 55 fps; на 1440×900 без троттлинга ≥ 100 fps; число узлов SVG на странице < 1300; на замере формы цикл огоньков стоит, когда карта ушла с экрана (`lightsDraws` 0 при `mapIntersects` false)"
    - "Аудит целей касания на 390×844 не находит видимых `a`, `button`, `input`, `select`, `textarea` и `label` с `input` ниже 44px ни по высоте, ни по ширине, кроме визуально скрытых узлов 1×1 (radio карточек типа, ссылка пропуска) и чекбокса 20×20 внутри label 44px"
    - "Скриншоты `docs/qa/v12-hero-1440.jpeg`, `v12-hero-390.jpeg`, `v12-map-1440.jpeg`, `v12-map-390.jpeg` (JPEG качества 70) сняты с прода; hero сравнён с `docs/research/v1.2/orig-hero-1440.jpeg` и `orig-hero-390.jpeg`"
    - "`docs/qa/SMOKE.md` содержит раздел «Фаза 17 / v1.2» с таблицами «оригинал / прод» по GLOBE, LIGHT и MOB, таблицей fps по секциям, принятыми отклонениями (видео на паузе при reduce, без источников при saveData, синусное дыхание вместо ease-in-out, плюс найденные) и ссылками на скриншоты; README указывает на раздел и скрипты"
  artifacts:
    - path: ".planning/phases/17-integration-qa/qa/v12-measure.js"
      provides: "Одна async-функция для page.evaluate: видео, canvas частиц, canvas огоньков, узлы SVG, цели касания, реестр data-anim на любом из двух сайтов"
      contains: "data-anim=\"globe\""
      min_lines: 150
    - path: ".planning/phases/17-integration-qa/qa/v12-run.mjs"
      provides: "Драйвер на playwright из кэша npx: режимы measure, fps (CPU×4 через CDP), shots; постоянный профиль и обход Vercel challenge для оригинала"
      contains: "Emulation.setCPUThrottlingRate"
      min_lines: 200
    - path: ".planning/phases/17-integration-qa/qa/results/prod-fps-390-cpu4.json"
      provides: "Медианы fps по секциям hero, map, light-form, about, quote при CPU×4 с draw-счётчиками обоих canvas"
      contains: "light-form"
    - path: ".planning/phases/17-integration-qa/qa/results/prod-measure-390.json"
      provides: "Видео cover 390×844, canvas частиц, узлы SVG, аудит целей касания, атрибуты обложек новостей"
      contains: "touch"
    - path: "docs/qa/v12-hero-390.jpeg"
      provides: "Первый экран прода на 390×844 с видео cover"
    - path: "docs/qa/SMOKE.md"
      provides: "Раздел «Фаза 17 / v1.2» с таблицами GLOBE, LIGHT, fps, MOB, отклонениями, скриншотами и итогом"
      contains: "Фаза 17 / v1.2"
    - path: "README.md"
      provides: "Абзац о разделе v1.2 и скриптах qa/, актуальное число проверок check:dist"
      contains: "v12-"
  key_links:
    - from: ".planning/phases/17-integration-qa/qa/v12-run.mjs"
      to: ".planning/phases/17-integration-qa/qa/v12-measure.js"
      via: "readFileSync текста функции и page.evaluate(`(${src})(site)`)"
      pattern: "v12-measure\\.js"
    - from: ".planning/phases/17-integration-qa/qa/v12-run.mjs"
      to: "CDP Emulation.setCPUThrottlingRate"
      via: "context.newCDPSession(page) перед замером fps на 390×844, rate 4, сброс в rate 1 в finally"
      pattern: "setCPUThrottlingRate"
    - from: ".planning/phases/17-integration-qa/qa/v12-measure.js"
      to: "canvas[data-anim=\"stars\"] и canvas.map-lights-canvas"
      via: "подмена ctx.clearRect счётчиком на 2000 мс для draw-частоты"
      pattern: "clearRect"
    - from: "docs/qa/SMOKE.md"
      to: ".planning/phases/17-integration-qa/qa/results/*.json"
      via: "все числа ячеек переписаны из JSON"
      pattern: "qa/results"
    - from: "docs/qa/SMOKE.md"
      to: "docs/research/v1.2/measurements.md"
      via: "колонка «Прод до v1.2» и fps оригинала при CPU×4 (116,8)"
      pattern: "measurements\\.md"
    - from: "docs/qa/SMOKE.md"
      to: ".planning/phases/17-integration-qa/qa/results/prod-hashes.txt"
      via: "таблица sha256 из плана 17-01"
      pattern: "sha256"
---

<objective>
Сравнить прод с оригиналом onevoice27.org через Playwright на 1440×900 и 390×844 по трём зонам v1.2: видео-глобус и canvas частиц (GLOBE), огоньки на canvas и бюджет fps при CPU×4 (LIGHT, LIGHT-07), цели касания и картинки (MOB); снять четыре скриншота; записать раздел «Фаза 17 / v1.2» в `docs/qa/SMOKE.md` и обновить README.

Purpose: SHIP-03 и SHIP-04, критерии 3, 4 и 5 фазы 17 в ROADMAP. Фазы 14–16 проверяли себя в jsdom и на preview; воспроизведение видео, частоту canvas, fps под троттлингом и цели касания на живом сайте можно подтвердить только здесь.
Output: `qa/v12-measure.js`, `qa/v12-run.mjs`, JSON замеров в `qa/results/`, `docs/qa/v12-*.jpeg` (4 файла), раздел v1.2 в `docs/qa/SMOKE.md`, правка `README.md`. Коммит, push, повторная сверка хэшей и закрытие milestone (аудит, архив, тег `v1.2`) за оркестратором.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-integration-qa/17-01-SUMMARY.md
@docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md
@docs/research/v1.2/measurements.md
@docs/qa/SMOKE.md
@.planning/milestones/v1.1-phases/13-integration-qa/qa/v11-run.mjs
@.planning/milestones/v1.1-phases/13-integration-qa/qa/v11-measure.js
@.planning/milestones/v1.1-phases/13-integration-qa/qa/v11-shots.mjs
@.planning/milestones/v1.1-phases/13-integration-qa/13-02-SUMMARY.md
@.planning/phases/15-lights-canvas/15-03-PLAN.md
@.planning/phases/15-lights-canvas/15-03-SUMMARY.md
@.planning/phases/14-hero-video-particles/14-VERIFICATION.md
@.planning/phases/16-mobile/16-01-SUMMARY.md
@src/components/hero/hero.css
@src/components/hero/heroParticles.ts
@src/components/map/lightsCanvas.ts
@README.md

<ownership>
Работа в основном каталоге на `main` после плана 17-01: прод отдаёт сборку HEAD (таблица `qa/results/prod-hashes.txt`). Исходники не правятся, кроме дефекта приёмки (см. Task 2, правило для целей касания): тогда правка в файле-владельце плюс тест, полный гейт из 17-01 Task 2, остановка с сообщением оркестратору «нужен коммит и push: <файлы>», после его ответа `pushed <sha>` ожидание прогона по протоколу 17-01 Task 4, повторная сверка `prod-hashes.mjs` и повтор затронутых замеров. Коммитов и push исполнитель не делает. `STATE.md`, `ROADMAP.md`, `.planning/HANDOFF.json`, `package.json`, `package-lock.json` не трогать. Команды относительные, без `cd`. Тексты на русском, идентификаторы и селекторы на английском.

Playwright в репозитории не установлен и не устанавливается: драйвер резолвит его из кэша npx (`~/.npm/_npx/*/node_modules/playwright`, сейчас 1.63.0-alpha-2026-08-31, Chrome через `channel: "chrome"`) или из `PW_ROOT`, как `v11-run.mjs`. Замеры fps делать в окне браузера (не headless), при закрытых вкладках с WebGL (оригинал с Mapbox закрывать до замера прода) и без параллельных `npm test`/`npm run build`; dpr 1. Постоянный профиль для оригинала лежит вне репозитория (`os.tmpdir()/esd-v12-profile`). Артефактов Playwright в корне репозитория не оставлять: JSON пишутся в `qa/results/`, JPEG в `docs/qa/`.

Оригинал стоит за Vercel Security Checkpoint: обычный `chromium.launch()` получает «Failed to verify your browser. Code 21». Проходит постоянный профиль с `--disable-blink-features=AutomationControlled` и без `--enable-automation`; навигация `waitUntil: "commit"`, затем ожидание `#ov-main-header` до 150 с, три попытки. Если challenge не пройден, скрипт пишет `{ "unavailable": "vercel challenge" }` с кодом 3, а значения оригинала берутся из `docs/research/v1.2/measurements.md` (видео 1067×600 справа вверху на 1440×900, cover во весь экран 390×844, hero 116,8 fps при CPU×4) с записью в SMOKE как принятое ограничение методики. Баннер cookie оригинала снимается кнопкой «Allow selected» до замеров.
</ownership>

<orchestrator>
Шаги оркестратора после выполнения плана (исполнитель их не делает):
1. Стадировать поимённо `docs/qa/SMOKE.md`, четыре `docs/qa/v12-*.jpeg`, `README.md`, `.planning/phases/17-integration-qa/qa/v12-measure.js`, `qa/v12-run.mjs`, `qa/results/*.json`, `qa/results/prod-hashes.txt` (из 17-01) и правки исходников, если Task 2 чинил дефект (тогда они уже запушены). Коммит `docs(17): приёмка v1.2 на проде, SMOKE, скриншоты, README и скрипты замеров`. `git push origin main`.
2. Дождаться прогона deploy.yml по протоколу 17-01 Task 4 и перепроверить хэши: `node .planning/phases/17-integration-qa/qa/prod-hashes.mjs --retries 2 --out /dev/null` → код 0, те же имена ассетов, что в `prod-hashes.txt` (Tailwind с v1.1 читает классы только из `src/`, документы бандл не меняют).
3. Верификация фазы 17, затем закрытие milestone: `.planning/milestones/v1.2-MILESTONE-AUDIT.md` (по образцу `v1.1-MILESTONE-AUDIT.md`: requirements 23/23, phases 4/4, integration, flows, tech_debt), архив `.planning/phases/14…17` в `.planning/milestones/v1.2-phases/`, `v1.2-REQUIREMENTS.md`, `v1.2-ROADMAP.md`, обновление `MILESTONES.md`, `PROJECT.md`, `STATE.md`, `REQUIREMENTS.md` (SHIP-01…04 и LIGHT-07 в Complete), тег `git tag v1.2` и `git push origin v1.2`.
</orchestrator>

<interfaces>
<!-- Селекторы и ожидаемые значения обеих сторон сравнения. Прод: исходники после слияния 14–16. Оригинал: docs/research/v1.2/measurements.md и опыт v11-measure.js. -->

| Пункт | Прод | Оригинал |
|---|---|---|
| Секция hero | `section#hero` | `video.closest("section")` или, если null, родитель родителя видео |
| Видео глобуса | `video[data-anim="globe"]` | первый `video` на странице (`autoplay muted loop playsinline`) |
| Canvas частиц | `canvas[data-anim="stars"]` | первый `canvas` внутри секции hero |
| Canvas огоньков | `.esd-map > canvas.map-lights-canvas` | нет (точки рисует Mapbox WebGL): `null` |
| Оболочка карты (скриншот, прокрутка) | `.map-shell` | `#ov-map-element` |
| Секция формы | `#light-form` | `#ov-light-form-container` |
| Секция About | `#about` | `#ov-about` |
| Секция-потолок fps (статичная) | `#quote` | `#ov-main-footer-content` |
| Якорь готовности страницы | `#quote` | `#ov-main-header` |
| Ссылки футера | `.site-footer__links a` (2) | не сравнивается |
| Label согласия и чекбокс | `#light-form label.lf-check`, `input.lf-checkbox` | не сравнивается |
| Подпись логотипа | `.site-header .wordmark__tagline` (ожидание `font-size` `10px`) | не сравнивается |
| Логотип-ссылка шапки | `a.site-header__brand` (в аудите до v1.2: 146×43) | не сравнивается |
| Обложки новостей | `.news-card__image` (первые 3: `width="480" height="360" decoding="async"`, первая `loading="eager" fetchpriority="high"`, остальные `lazy`) | не сравнивается |

Ожидания прода (из hero.css и спецификации). 1440×900: hero высотой 600 (`max(600px, 64vh)` = 600), видео `object-fit: contain`, `object-position: 100% 0%` (right top), `mix-blend-mode: screen`, `width: auto` при `height: 100%` и `aspect-ratio: 16 / 9` даёт прямоугольник 1066,67×600 с правым краем на `innerWidth` и верхом на верхнем крае hero; `mask-image` из двух `linear-gradient`. 390×844: hero `100svh` = 844, видео 390×844, `object-fit: cover`, `object-position: 50% 50%`, `filter: saturate(1.18) contrast(1.28) brightness(0.96)`, `mix-blend-mode: normal`. Видео 1920×1080, 12,79 с, `readyState` ≥ 2. Canvas частиц: `position: absolute; inset: 0`, размер равен hero, bitmap = размер × min(dpr, 1,75), `opacity: 0.72`, `mix-blend-mode: screen`; кадр рисуется, когда прошло ≥ 33,33 мс с прошлого (`FRAME_INTERVAL_MS = 1000 / 30`), поэтому при rAF 120 Гц измеренная частота лежит между 24 и 30 (4 или 5 интервалов по 8,33 мс), при 60 Гц ровно 30. Каждый кадр `drawScene` зовёт `ctx.clearRect` один раз.

Canvas огоньков: `data-light-count="942"`, `data-people="694"`, `data-groups="248"`, `data-new="0"`; `position: absolute`, `pointer-events: none`; bitmap = размер контейнера × min(dpr, `MAX_DPR`); кадр при `MIN_FRAME_GAP_MS = 33` (при 120 Гц ровно 30 кадров в секунду); цикл стоит по `IntersectionObserver` (threshold 0) на контейнере карты, в скрытой вкладке и при reduce (один статичный кадр). Каждый кадр зовёт `ctx.clearRect` один раз. `getContext("2d")` повторно отдаёт тот же контекст: счётчик ставится подменой `ctx.clearRect` на самом объекте.

Бюджет LIGHT-07: 390×844 при CPU×4 hero, map, light-form ≥ 55 fps; 1440×900 без троттлинга ≥ 100; `document.querySelectorAll("svg, svg *").length` < 1300. Замеры «до» (measurements.md): 1440 hero 65, map 66, form 69, about 76; 390 CPU×4 hero 30, map 30, form 30, about 73; оригинал 390 CPU×4 hero 116,8, map 116,8; потолок стенда 120.

Метод fps (из `map-probe.mjs` и `lights-probe.mjs`): в странице цикл `requestAnimationFrame` 2000 мс, `fps = frames · 1000 / sum(intervals)`, `maxGapMs`; три прогона, медиана. Троттлинг: `cdp = await context.newCDPSession(page)`, `cdp.send("Emulation.setCPUThrottlingRate", { rate: 4 })`, пауза 300 мс, замеры, `rate: 1`, `detach`.

Аудит целей касания: набор `a[href], button, input:not([type="hidden"]), select, textarea, label:has(input)`; видимые: `getClientRects().length > 0`, `visibility !== "hidden"`, не внутри `[aria-hidden="true"]`; исключения: прямоугольник ≤ 1×1 (визуально скрытые: radio `.lf-type input`, `a.skip-link`) идут в список `hidden`; `input[type="checkbox"]` внутри `label.lf-check` идёт в `viaLabel`, если высота label ≥ 44. Всё остальное с `min(width, height) < 44` попадает в `small` с тегом, id, классом, текстом (до 40 символов) и размерами.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Скрипты приёмки v12-measure.js и v12-run.mjs (режимы measure, fps с CPU×4 через CDP, shots) и пробный прогон на проде</name>
  <files>.planning/phases/17-integration-qa/qa/v12-measure.js, .planning/phases/17-integration-qa/qa/v12-run.mjs</files>
  <read_first>
    - .planning/milestones/v1.1-phases/13-integration-qa/qa/v11-run.mjs целиком (резолв playwright из кэша npx, `launchPersistentContext` с профилем в tmpdir, `openPage` с `waitUntil: "commit"` и повторами, `dismissConsent`, прогрев прокруткой, чтение файла-функции и `page.evaluate(call)`)
    - .planning/milestones/v1.1-phases/13-integration-qa/qa/v11-measure.js строки 1–140 (форма файла: одно выражение `async (site) => {...}`, таблица селекторов PROD/ORIG, хелперы `q`, `qa`, `cs`, `rect`, `r2`)
    - .planning/milestones/v1.1-phases/13-integration-qa/qa/v11-shots.mjs (`pageRect` в координатах документа, `page.screenshot` с `type: "jpeg"` и `quality`, `clip` при `fullPage`)
    - .planning/phases/15-lights-canvas/15-03-PLAN.md, Task 1 (режим fps с CDP-троттлингом, формат JSON, имя файла результата) и 15-03-SUMMARY.md (какие числа получены на preview, решение по `MAX_DPR`)
    - src/components/hero/heroParticles.ts строки 25–50 и 95–110 (`FRAME_INTERVAL_MS`, `MAX_PIXEL_RATIO`, `shouldDrawFrame`), src/components/hero/HeroParticles.tsx строки 20–30 (`getContext("2d", { alpha: true })`)
    - src/components/map/lightsCanvas.ts строки 24–60 (константы, `MAX_DPR`, `MIN_FRAME_GAP_MS`)
  </read_first>
  <action>
    Реализует инструментальную часть SHIP-03. Оба файла в `.planning/phases/17-integration-qa/qa/`, каталог `qa/results/` уже создан планом 17-01. Зависимости в репозиторий не добавлять: импорты только из `node:*`, playwright через `createRequire` из кэша npx (функции `playwrightRoots`, `loadPlaywright`, `unwrap` скопировать из `v11-run.mjs`, не импортировать: архив v1.1 не модуль).

    **`qa/v12-measure.js`**: одно выражение `async (site) => {...}` (`site` равен `"prod"` или `"orig"`), шапка-комментарий на русском о двух способах вызова (драйвер и `browser_evaluate`). Хелперы `q`, `qa`, `cs`, `rect`, `r2`, `wait` как в `v11-measure.js`; таблица селекторов из `<interfaces>`. Возвращает объект:
    - `meta`: `site`, `href`, `innerWidth`, `innerHeight`, `devicePixelRatio`, `scrollWidth`, `documentHeight`, `userAgent`, `nodes` (`document.querySelectorAll("*").length`), `takenAt`.
    - `globe`: по видео: `found`, `paused`, `ended`, `muted`, `autoplay`, `loop`, `playsInline`, `preload`, `readyState`, `videoWidth`, `videoHeight`, `duration` (2 знака), `currentSrc` (только имя файла после последнего `/`), `currentTime0`, затем `await wait(1500)`, `currentTime1`, `delta` (2 знака), `playing` (`!paused && !ended && delta >= 1`), `rect` видео, `heroRect`, `rightGap` (`innerWidth − (rect.x + rect.width)`, 2 знака), `topInHero` (`rect.y − heroRect.y`), `aspect` (`width / height`, 4 знака), computed `objectFit`, `objectPosition`, `mixBlendMode`, `filter`, `transformOrigin`, `hasMask` (`maskImage` или `webkitMaskImage` содержит `linear-gradient`), `maskLayers` (число вхождений `linear-gradient`), `sourceCount` (`video.querySelectorAll("source").length`), `attributes` (`disablepictureinpicture`, `disableremoteplayback`, `tabindex`, `aria-hidden`, `data-anim`).
    - `particles`: по canvas частиц: `found`, `rect`, `bitmap` `{ width, height }`, `expectedBitmap` (`rect × min(devicePixelRatio, 1.75)`, округлённо), computed `opacity`, `mixBlendMode`, `position`, `zIndex`, `drawsPerSecond`: подменить `ctx.clearRect` на объекте контекста (`ctx = canvas.getContext("2d")`) обёрткой со счётчиком, `await wait(2000)`, вернуть `count / 2` (1 знак), восстановить (`delete ctx.clearRect`, чтобы вернулся метод прототипа). Если контекста нет (WebGL у оригинала), `drawsPerSecond: null` и `contextType`.
    - `lights` (у оригинала `null`): `found`, `dataset` (`lightCount`, `people`, `groups`, `fresh` из `data-*`), `anim` (`data-anim`), `ariaHidden`, computed `position`, `pointerEvents`, `rect`, `bitmap`, `expectedBitmap` (`rect × min(devicePixelRatio, 2)`), `previousIsSvg` (`previousElementSibling.tagName === "svg"`), `mapCircles` (`.esd-map svg circle`), `legacyNodes` (`.map-lights, .light-bucket, .light-core, .light-ring, .esd-map svg defs`), `countries` (`.esd-map svg path`), `drawsPerSecond` тем же приёмом со счётчиком `clearRect` за 2000 мс (карта должна быть в кадре: перед этим `scrollIntoView({ block: "center" })` у `.map-shell`, `await wait(1200)`, после замера `window.scrollTo(0, 0)` и `await wait(600)`).
    - `svg`: `nodes` (`document.querySelectorAll("svg, svg *").length`), `roots` (`svg` count) на обоих сайтах.
    - `anim`: объект `{ значение: число узлов }` по всем `[data-anim]` в DOM (у оригинала пустой).
    - `touch`: аудит по правилам из `<interfaces>`: `checked` (число проверенных), `small` (список нарушений), `hidden` (список 1×1), `viaLabel` (чекбоксы внутри label ≥ 44), плюс `footerLinks` (массив `{ w, h }` для `.site-footer__links a`), `consentLabel` (`{ w, h }` для `label.lf-check`), `checkbox` (`{ w, h }` для `input.lf-checkbox`), `brand` (`{ w, h }` для `a.site-header__brand`), `taglineFontSize` (computed `font-size` у `.site-header .wordmark__tagline`), `news` (первые три `.news-card__image`: `width`, `height`, `loading`, `fetchpriority`, `decoding` атрибутами). У оригинала `touch` заполняется теми же правилами (для справки), поля прода `null`.
    Никаких кликов по форме и панелям: этот снимок не трогает состояние страницы, кроме прокрутки к карте и обратно.

    **`qa/v12-run.mjs`**: ESM, шапка-комментарий с режимами и примерами. CLI: первый позиционный аргумент режим `measure | fps | shots`; флаги `--site prod|orig` (по умолчанию `prod`), `--url` (переопределение), `--width` (1440), `--height` (900), `--cpu` (1; только `fps`), `--runs` (3; только `fps`), `--sections` (по умолчанию `hero,map,light-form,about,quote`; только `fps`), `--reduced` (контекст с `reducedMotion: "reduce"`), `--out <file>` (JSON для `measure`/`fps`; по умолчанию `results/<site>-<mode>-<width>[-cpu<cpu>][-reduced].json` рядом со скриптом) или `--out-dir <dir>` (для `shots`, по умолчанию `docs/qa`). Неизвестный аргумент: подсказка и код 2. URL: prod `https://thevladoss.github.io/esd-onevoice27/`, orig `https://onevoice27.org/`; якоря `#quote` и `#ov-main-header`; для orig таймаут ожидания якоря 150 000 мс и три попытки, для prod одна попытка с 60 000 мс. Браузер: `launchPersistentContext(process.env.PW_PROFILE ?? join(tmpdir(), "esd-v12-profile"), { channel: process.env.PW_CHANNEL ?? "chrome", headless: process.env.PW_HEADLESS === "1", viewport, deviceScaleFactor: 1, isMobile: width < 768, hasTouch: width < 768, reducedMotion: reduced ? "reduce" : "no-preference", args: ["--disable-blink-features=AutomationControlled"], ignoreDefaultArgs: ["--enable-automation"] })` для обоих сайтов (тот же профиль проходит challenge оригинала). После открытия: `waitForLoadState("load")`, `bringToFront`, `dismissConsent` (как в v11-run), прогрев прокруткой до низа и обратно (reveal, ленивые картинки, счётчики), `waitForTimeout(600)`. Для orig challenge не пройден: JSON `{ meta, unavailable: "vercel challenge", attempts }` в `--out`, код 3.
    - Режим `measure`: `page.evaluate(`(${src})(${JSON.stringify(site)})`)` с текстом `v12-measure.js`; добавить `probe` (`playwright`, `channel`, `url`, `attempt`, `consent`, `reduced`, `takenAt`); записать JSON, напечатать одну строку сводки: `playing`, размер видео, `objectFit`, `particles.drawsPerSecond`, `svg.nodes`, число `touch.small`.
    - Режим `fps`: для каждой секции из `--sections` прокрутить (`hero` → `window.scrollTo(0, 0)`; остальные `scrollIntoView({ block: "center" })` по таблице селекторов сайта), `waitForTimeout(2000)` (reveal 700 мс, счётчики 1600 мс); если `--cpu > 1`, CDP-сессия открыта один раз до первой секции и `rate` сброшен в `finally`; замер `--runs` раз по 2000 мс rAF в странице с одновременными счётчиками `clearRect` у canvas частиц и canvas огоньков (тот же приём подмены, оба контекста берутся один раз); на каждую секцию писать `{ runs: [{ fps, maxGapMs, frames, starsDraws, lightsDraws }], median, floor, pass, mapIntersects, scrollY }`, где `floor` = 55 при `cpu > 1`, иначе 100, а `pass` считается только для `hero`, `map`, `light-form` (`about` и `quote` информационные, `quote` служит потолком стенда). Итог: `{ meta: { site, width, height, cpu, dpr: 1, reduced }, sections, ceiling: median секции quote, passAll }`; вывод `PASS`/`FAIL` с кодом 0/1. Между секциями `waitForTimeout(300)`.
    - Режим `shots`: prod → `v12-hero-<width>.jpeg` (вьюпорт при `scrollY` 0 после `waitForTimeout(2500)`: видео идёт, частицы живут) и `v12-map-<width>.jpeg` (clip по `pageRect(".map-shell")` при `fullPage: true`, после прогрева); orig → `v12-orig-map-<width>.jpeg` (clip по `#ov-map-element`; hero оригинала уже лежит в `docs/research/v1.2/orig-hero-*.jpeg`). `page.screenshot({ type: "jpeg", quality: 70 })`. В конце напечатать имена и размеры файлов в байтах.
    Все ошибки навигации печатать в stderr с префиксом `#`. `process.exit` с кодом после `context.close()` в `finally`.

    **Пробный прогон**: `node --check` обоих файлов; `node .planning/phases/17-integration-qa/qa/v12-run.mjs` без аргументов → подсказка и код 2; `node .planning/phases/17-integration-qa/qa/v12-run.mjs measure --site prod --width 1440 --height 900 --out .planning/phases/17-integration-qa/qa/results/prod-measure-1440.json` → код 0, в JSON `globe.playing` true, `globe.rect.width` в пределах 1065…1068, `particles.drawsPerSecond` число, `lights.dataset.lightCount` `"942"`, `svg.nodes` число, `touch.checked` больше 0. Этот файл остаётся как результат Task 2 (перезаписывать не обязательно). Сводку записать в SUMMARY.
  </action>
  <verify>
    <automated>node --check .planning/phases/17-integration-qa/qa/v12-run.mjs && node -e "new Function('return ' + require('fs').readFileSync('.planning/phases/17-integration-qa/qa/v12-measure.js','utf8').trim().replace(/;$/,''))" && (node .planning/phases/17-integration-qa/qa/v12-run.mjs >/dev/null 2>&1; test $? -eq 2) && node -e "const j=JSON.parse(require('fs').readFileSync('.planning/phases/17-integration-qa/qa/results/prod-measure-1440.json','utf8'));if(!(j.globe.playing===true&&j.lights.dataset.lightCount==='942'&&typeof j.particles.drawsPerSecond==='number'&&j.svg.nodes<1300))process.exit(1);console.log('measure OK',j.globe.rect.width,j.particles.drawsPerSecond,j.svg.nodes)"</automated>
  </verify>
  <acceptance_criteria>
    - `qa/v12-measure.js` существует, одно выражение-функция; содержит `data-anim="globe"`, `data-anim="stars"`, `map-lights-canvas`, `clearRect`, `"svg, svg *"`, `label:has(input)`, `skip-link`, `lf-check`, `site-footer__links a`, `site-header__brand`, `wordmark__tagline`, `news-card__image`, `ov-main-header` или `ov-map-element`, `currentTime`, `objectFit`, `mixBlendMode`
    - `qa/v12-run.mjs` существует; содержит `createRequire`, `PW_ROOT`, `launchPersistentContext`, `AutomationControlled`, `newCDPSession`, `Emulation.setCPUThrottlingRate`, `rate: 1`, `reducedMotion`, `isMobile`, `hasTouch`, `ov-main-header`, `150_000` или `150000`, `quality: 70`, `v12-hero-`, `v12-map-`, `requestAnimationFrame`, `writeFileSync`; импортов кроме `node:*` нет; `node --check` код 0
    - Запуск без аргументов печатает режимы и завершается кодом 2
    - `qa/results/prod-measure-1440.json` существует: `globe.playing` true, `globe.delta` ≥ 1, `globe.currentSrc` `hero-globe.webm`, `lights.dataset.lightCount` `"942"`, `particles.drawsPerSecond` число, `svg.nodes` < 1300, `touch.checked` > 0, `probe.playwright` заполнен
    - В корне репозитория новых файлов нет; `git status --porcelain | grep -v HANDOFF.json` показывает только два скрипта и JSON
  </acceptance_criteria>
  <done>Драйвер и функция снимка написаны на образце v1.1, троттлинг через CDP и счётчики кадров обоих canvas встроены, пробный снимок прода на 1440 показывает живое видео и 942 огонька.</done>
</task>

<task type="auto">
  <name>Task 2: Матрица замеров на проде и оригинале, бюджет LIGHT-07, аудит целей касания, четыре скриншота</name>
  <files>.planning/phases/17-integration-qa/qa/results/prod-measure-1440.json, .planning/phases/17-integration-qa/qa/results/prod-measure-390.json, .planning/phases/17-integration-qa/qa/results/prod-measure-1440-reduced.json, .planning/phases/17-integration-qa/qa/results/orig-measure-1440.json, .planning/phases/17-integration-qa/qa/results/orig-measure-390.json, .planning/phases/17-integration-qa/qa/results/prod-fps-1440.json, .planning/phases/17-integration-qa/qa/results/prod-fps-390-cpu4.json, .planning/phases/17-integration-qa/qa/results/orig-fps-390-cpu4.json, docs/qa/v12-hero-1440.jpeg, docs/qa/v12-hero-390.jpeg, docs/qa/v12-map-1440.jpeg, docs/qa/v12-map-390.jpeg</files>
  <read_first>
    - docs/research/v1.2/measurements.md (стенд, числа «до», hero оригинала)
    - .planning/phases/15-lights-canvas/15-03-SUMMARY.md, раздел «Замеры LIGHT-07» (fps карты и формы на preview, решение по dpr)
    - .planning/phases/16-mobile/16-01-SUMMARY.md, «Next Phase Readiness» (что проверять на 390: ссылки футера 44px, label 44px, подпись 10px)
    - .planning/phases/14-hero-video-particles/14-VERIFICATION.md, «Deferred Items» (воспроизведение видео и 30 fps частиц закреплены за этой фазой)
    - .planning/milestones/v1.1-phases/13-integration-qa/13-02-SUMMARY.md, «Issues Encountered» (таймауты сети до Vercel, повторы)
  </read_first>
  <action>
    Реализует SHIP-03 и подтверждает LIGHT-07 (критерии 3 и 5 фазы 17). Переменные для команд: `Q=.planning/phases/17-integration-qa/qa`, `R=$Q/results`. Порядок: сначала прод (все режимы), потом оригинал (его вкладка с Mapbox WebGL до замеров прода не открывается).

    **Прод, снимки**: `node $Q/v12-run.mjs measure --site prod --width 1440 --height 900 --out $R/prod-measure-1440.json` (перезаписать пробный, если он снимался при открытых лишних вкладках); `node $Q/v12-run.mjs measure --site prod --width 390 --height 844 --out $R/prod-measure-390.json`; `node $Q/v12-run.mjs measure --site prod --width 1440 --height 900 --reduced --out $R/prod-measure-1440-reduced.json`.
    Ожидания 1440: `globe.playing` true, `delta` ≥ 1, `readyState` ≥ 2, `videoWidth` 1920, `videoHeight` 1080, `currentSrc` `hero-globe.webm`, `sourceCount` 2, `heroRect.height` 600 ± 1, `rect.width` 1066,67 ± 2, `rect.height` 600 ± 1, `rightGap` ≤ 1, `topInHero` ≤ 1, `objectFit` `contain`, `objectPosition` `100% 0%`, `mixBlendMode` `screen`, `hasMask` true, `maskLayers` 2; `particles.rect` равен `heroRect` (± 1), `opacity` `0.72`, `mixBlendMode` `screen`, `drawsPerSecond` 24…31, `bitmap` равен `expectedBitmap`; `lights.dataset` 942/694/248/0, `position` `absolute`, `pointerEvents` `none`, `previousIsSvg` true, `mapCircles` 0, `legacyNodes` 0, `countries` ≥ 177, `bitmap` равен `expectedBitmap`, `drawsPerSecond` 27…31; `svg.nodes` < 1300; `anim` содержит `stars: 1`, `globe: 1`, `pulse: 1`, `beam: 2`, `wave`, `halo`, `particles`, `atmosphere` и не содержит `new-light`; `meta.scrollWidth` 1440.
    Ожидания 390: `rect` 390×844 (± 1), `heroRect.height` 844 ± 1, `objectFit` `cover`, `objectPosition` `50% 50%`, `filter` `saturate(1.18) contrast(1.28) brightness(0.96)`, `mixBlendMode` `normal`, `playing` true; `particles.drawsPerSecond` 24…31; `svg.nodes` < 1300; `meta.scrollWidth` 390; `touch.small` пустой; `touch.hidden` содержит только radio `.lf-type` (2) и `a.skip-link`; `touch.viaLabel` содержит один чекбокс 20×20 при `consentLabel.h` ≥ 44; `footerLinks` оба `h` ≥ 44 и `w` ≥ 44; `taglineFontSize` `10px`; `news[0]` `width "480"`, `height "360"`, `loading "eager"`, `fetchpriority "high"`, `decoding "async"`, `news[1]` и `news[2]` `loading "lazy"` без `fetchpriority`.
    Ожидания reduced 1440: `globe.paused` true, `delta` 0 (пауза на первом кадре, отклонение 1 спецификации), `particles.drawsPerSecond` 0 (один статичный кадр нарисован до замера), `lights.drawsPerSecond` 0.

    **Правило для целей касания (единственная допустимая правка исходников в плане)**: если `touch.small` на 390 не пуст, каждый элемент списка это дефект приёмки, не отклонение. Известный кандидат: логотип-ссылка `a.site-header__brand` (146×43 в аудите до v1.2; подпись выросла до 10px в фазе 16, высота могла стать 44). Починка: в файле-владельце (`src/components/layout/Header.css` для шапки) задать боксу цели `display: inline-flex; align-items: center; min-height: 44px` без изменения кегля и раскладки, дописать проверку текста CSS в тест-владелец (`Header.test.tsx`), прогнать полный гейт из 17-01 Task 2, затем остановиться и вернуть оркестратору «нужен коммит и push: <файлы>»; после ответа `pushed <sha>` дождаться прогона (протокол 17-01 Task 4), `node $Q/prod-hashes.mjs --out $R/prod-hashes.txt` (перезаписать таблицу, отметить в SUMMARY), перемерить `prod-measure-390.json` и `prod-measure-1440.json`. Элементы ниже 44px ни по одному измерению не «принимаются»: ROADMAP допускает только визуально скрытые radio.

    **Прод, fps**: закрыть все вкладки с WebGL, не запускать параллельно `npm test`/`npm run build`. `node $Q/v12-run.mjs fps --site prod --width 390 --height 844 --cpu 4 --runs 3 --out $R/prod-fps-390-cpu4.json` → медианы `hero`, `map`, `light-form` ≥ 55, `about` и `quote` записаны; в секции `light-form` `mapIntersects` и `lightsDraws` записаны (если карта ушла с экрана, `lightsDraws` 0: цикл стоит, критерий 5 фазы 17; если карта частично в кадре на 844px высоты, `lightsDraws` около 60 за 2 с и это нормально, отметить в SUMMARY). `node $Q/v12-run.mjs fps --site prod --width 1440 --height 900 --runs 3 --out $R/prod-fps-1440.json` → медианы `hero`, `map`, `light-form` ≥ 100, `ceiling` (секция `quote`) записан. Если `ceiling` меньше 100, стенд не тот, на котором снимались числа `measurements.md` (там потолок 120): проверить частоту обновления дисплея и внешние мониторы, повторить на 120 Гц; порог не снижать. Провал бюджета в любой секции при потолке ≥ 100: не подстраивать порт частиц (GLOBE-04 фиксирует числа оригинала) и не менять `lightsCanvas.ts` тайком; записать числа в SUMMARY и в SMOKE как «бюджет не выдержан» с указанием секции, вернуть оркестратору как гап фазы (решение о доработке за ним).

    **Прод, скриншоты**: `node $Q/v12-run.mjs shots --site prod --width 1440 --height 900 --out-dir docs/qa` → `docs/qa/v12-hero-1440.jpeg`, `docs/qa/v12-map-1440.jpeg`; `node $Q/v12-run.mjs shots --site prod --width 390 --height 844 --out-dir docs/qa` → `v12-hero-390.jpeg`, `v12-map-390.jpeg`. Открыть каждый JPEG (Read) и сравнить с `docs/research/v1.2/orig-hero-1440.jpeg` и `orig-hero-390.jpeg`: на 1440 глобус справа вверху с растворённым левым краем и низом, частицы поверх, текстовая колонка слева внизу; на 390 глобус во весь экран по центру. Наблюдения (совпадения и различия словами) записать для SMOKE.

    **Оригинал** (после прода): `node $Q/v12-run.mjs measure --site orig --width 1440 --height 900 --out $R/orig-measure-1440.json`; `node $Q/v12-run.mjs measure --site orig --width 390 --height 844 --out $R/orig-measure-390.json`; `node $Q/v12-run.mjs fps --site orig --width 390 --height 844 --cpu 4 --runs 3 --sections hero,map --out $R/orig-fps-390-cpu4.json`; по желанию `shots --site orig` на 1440 и 390 (`v12-orig-map-*.jpeg`, в `files_modified` не входят, стадируются оркестратором, если сняты). Ожидания: 1440 видео около 1067×600 у правого верхнего края с `objectFit` `contain`; 390 около 390×844 с `cover`; `globe.playing` true; `particles.drawsPerSecond` 24…31 (у оригинала тот же порог кадра); hero при CPU×4 около 117. Разница размеров видео с продом ≤ 2px по каждой стороне; иное записать как отклонение с числами. Если после трёх попыток и повторного запуска скрипт вернул код 3 (`unavailable`), оставить JSON с пометкой и в SMOKE брать значения оригинала из `measurements.md` с записью «оригинал недоступен за Vercel Security Checkpoint в момент приёмки, значения из замеров 2026-09-06». Таймаут сети `net::ERR_TIMED_OUT` (13-02-SUMMARY) лечится повторным запуском.

    **Итог задачи**: восемь JSON в `$R/` (девятый `prod-hashes.txt` от 17-01 рядом), четыре JPEG в `docs/qa/`; корень репозитория чист; в SUMMARY таблица fps «до / после / оригинал / бюджет» по секциям и обоим вьюпортам, список `touch.small` (ожидается пустой), `svg.nodes` на обоих вьюпортах, размеры видео обеих сторон.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d='.planning/phases/17-integration-qa/qa/results/';const j=n=>JSON.parse(fs.readFileSync(d+n,'utf8'));const a=j('prod-measure-1440.json'),b=j('prod-measure-390.json'),f4=j('prod-fps-390-cpu4.json'),f1=j('prod-fps-1440.json'),r=j('prod-measure-1440-reduced.json');const ok=a.globe.playing&&a.globe.objectFit==='contain'&&a.globe.mixBlendMode==='screen'&&Math.abs(a.globe.rect.width-1066.67)<=2&&Math.abs(a.globe.rect.height-600)<=1&&a.particles.drawsPerSecond>=24&&a.particles.drawsPerSecond<=31&&a.svg.nodes<1300&&b.globe.playing&&b.globe.objectFit==='cover'&&Math.abs(b.globe.rect.width-390)<=1&&Math.abs(b.globe.rect.height-844)<=1&&b.touch.small.length===0&&b.touch.taglineFontSize==='10px'&&b.svg.nodes<1300&&['hero','map','light-form'].every(s=>f4.sections[s].median>=55)&&['hero','map','light-form'].every(s=>f1.sections[s].median>=100)&&r.globe.paused===true&&r.particles.drawsPerSecond===0;if(!ok)process.exit(1);console.log('matrix OK',{hero4:f4.sections.hero.median,map4:f4.sections.map.median,form4:f4.sections['light-form'].median,hero:f1.sections.hero.median,ceiling:f1.ceiling,svg:[a.svg.nodes,b.svg.nodes]})" && for f in v12-hero-1440 v12-hero-390 v12-map-1440 v12-map-390; do test -s "docs/qa/$f.jpeg" || exit 1; done && test "$(ls *.json *.jpeg *.png 2>/dev/null | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <acceptance_criteria>
    - `prod-measure-1440.json`: `globe.playing` true, `delta` ≥ 1, `currentSrc` `hero-globe.webm`, `videoWidth` 1920, `rect.width` 1066,67 ± 2, `rect.height` 600 ± 1, `rightGap` ≤ 1, `topInHero` ≤ 1, `objectFit` `contain`, `objectPosition` `100% 0%`, `mixBlendMode` `screen`, `hasMask` true; `particles.drawsPerSecond` 24…31, `opacity` `0.72`; `lights.dataset.lightCount` `"942"`, `mapCircles` 0, `legacyNodes` 0, `countries` ≥ 177, `drawsPerSecond` 27…31; `svg.nodes` < 1300; `anim` без `new-light`, `stars` 1, `globe` 1, `pulse` 1
    - `prod-measure-390.json`: `rect` 390×844 ± 1, `objectFit` `cover`, `objectPosition` `50% 50%`, `filter` `saturate(1.18) contrast(1.28) brightness(0.96)`, `playing` true; `particles.drawsPerSecond` 24…31; `svg.nodes` < 1300; `scrollWidth` 390; `touch.small` `[]`, `touch.hidden` только radio и skip-link, `viaLabel` один чекбокс, `footerLinks` оба ≥ 44 по высоте, `consentLabel.h` ≥ 44, `taglineFontSize` `10px`, `news[0]` eager/high 480×360, `news[1..2]` lazy
    - `prod-measure-1440-reduced.json`: `globe.paused` true, `particles.drawsPerSecond` 0, `lights.drawsPerSecond` 0
    - `prod-fps-390-cpu4.json`: `meta.cpu` 4, медианы `hero`, `map`, `light-form` ≥ 55 (`pass` true у всех трёх), `about` и `quote` записаны, `sections["light-form"].mapIntersects` и `lightsDraws` записаны, `passAll` true
    - `prod-fps-1440.json`: медианы `hero`, `map`, `light-form` ≥ 100, `ceiling` ≥ 100, `passAll` true
    - `orig-measure-1440.json`, `orig-measure-390.json`, `orig-fps-390-cpu4.json` существуют: либо с числами (видео оригинала 1067×600 `contain` на 1440 и 390×844 `cover` на 390 в пределах 2px от прода, hero при CPU×4 записан), либо с `unavailable: "vercel challenge"` и списком попыток
    - `docs/qa/v12-hero-1440.jpeg`, `v12-hero-390.jpeg`, `v12-map-1440.jpeg`, `v12-map-390.jpeg` существуют, размер каждого больше 10 000 байт; PNG в `docs/qa/` не добавлено; в корне репозитория нет `*.json`, `*.jpeg`, `*.png`
    - Если исходники правились по правилу целей касания: гейт зелёный, прогон deploy.yml на новом HEAD `success`, `prod-hashes.txt` перезаписан со всеми «да», замеры 390 и 1440 сняты заново после деплоя, правка записана в SUMMARY как deviation
    - `git status --porcelain | grep -v HANDOFF.json` показывает только файлы из `files_modified` планов 17-01 (незакоммиченный `prod-hashes.txt`) и 17-02
  </acceptance_criteria>
  <done>Видео и частицы живут на проде на обоих вьюпортах с размерами оригинала, бюджет LIGHT-07 подтверждён числами при CPU×4 и без троттлинга, узлов SVG меньше 1300, целей касания ниже 44px нет, четыре скриншота лежат в docs/qa, оригинал снят или его недоступность зафиксирована.</done>
</task>

<task type="auto">
  <name>Task 3: Раздел «Фаза 17 / v1.2» в docs/qa/SMOKE.md и правка README</name>
  <files>docs/qa/SMOKE.md, README.md</files>
  <read_first>
    - docs/qa/SMOKE.md строки 1–15 (шапка файла) и 173–380 (раздел «Фаза 13 / v1.1»: формат шапки-таблицы, «Гейт и деплой», «Стыки фаз», таблицы по пунктам с колонкой «Статус», «Принятые отклонения», «Скриншоты», «Итог»)
    - .planning/phases/17-integration-qa/17-01-SUMMARY.md (таблица «Гейт», число узлов SVG из jsdom, набор `data-anim`, id прогона, таблица хэшей) и `qa/results/prod-hashes.txt`
    - docs/superpowers/specs/2026-09-06-mobile-hero-perf-v1.2-design.md, «Принятые отклонения от оригинала» и «Диагноз лагов»
    - .planning/phases/14-hero-video-particles/14-VERIFICATION.md, таблица «Точность порта» (палитра частиц литералами спеки против вычисленных цветов оригинала)
    - README.md строки 40–75 («Проверка сборки»: «Одиннадцать проверок», `OK: 11 проверок`; «Браузерный smoke»)
    - JSON из `qa/results/` (источник чисел; читать через `node -e` выборкой ключей, не целиком)
  </read_first>
  <action>
    Реализует SHIP-04 в части документов и критерий 4 фазы 17.

    **`docs/qa/SMOKE.md`.** Добавить в конец файла раздел `## Фаза 17 / v1.2: приёмка глобуса, огоньков и мобильной адаптации` (заголовок содержит строку `Фаза 17 / v1.2`). Вводный абзац: сравнение прода с оригиналом по спецификации v1.2 (путь к спеке), числа сняты скриптами `qa/v12-measure.js` + `qa/v12-run.mjs` и лежат в `.planning/phases/17-integration-qa/qa/results/*.json`. Структура:
    1. Шапка-таблица «Поле | Значение»: дата, коммит прода (sha из `prod-hashes.txt`) и имена ассетов, прогон workflow со ссылкой `https://github.com/thevladoss/esd-onevoice27/actions/runs/<id>`, инструмент (Chrome под playwright из кэша npx с версией, вьюпорты 1440×900 и 390×844, DPR 1, CPU×4 через CDP `Emulation.setCPUThrottlingRate`, мобильная эмуляция `isMobile`/`hasTouch` на 390), оригинал (постоянный профиль без `AutomationControlled`, якорь `#ov-main-header`, баннер cookie снят; либо «недоступен: Vercel Security Checkpoint, значения из measurements.md»), исполнитель.
    2. `### Гейт и деплой (SHIP-01, SHIP-02)`: таблица «Шаг | Результат» из 17-01-SUMMARY: `npx tsc -b`, `npm test` (файлы/тесты), `npm run lint`, `npm run build` (ассеты с размерами), `node scripts/check-dist.mjs` (`OK: 12 проверок`), прогон workflow, побайтная сверка (все строки из `prod-hashes.txt`, включая `hero-globe.webm` и `hero-globe.mp4`, «N из N»). Ниже строка про инварианты: один блок reduce в `global.css`, реестр `data-anim` (восемь значений в DOM, `new-light` вне обязательных).
    3. `### Стыки фаз`: таблица «Стык | Проверка | jsdom (App.seams.test.tsx) | Прод» на четыре строки по блоку v1.2: 14 (видео + canvas частиц в hero), 15 + 8 (canvas огоньков, SVG без кругов, узлов SVG в jsdom и на проде), 16 + 9 (label 44px, ссылки футера 44px), 14 + 15 (набор `data-anim` в jsdom и в `anim` прода). Ниже две закрытые задачи 14-02: заглушки медиа в `setup.ts` (строк «Not implemented» до/после), комментарий в `HeroParticles.test.tsx`.
    4. `### GLOBE`: таблица «Проверка | Ожидание | Оригинал | Прод 1440 | Прод 390 | Статус»: видео воспроизводится (`paused`, прирост `currentTime` за 1,5 с), источник (`currentSrc`, 1920×1080, 12,79 с), размер и положение видео на 1440 (ширина, высота, `rightGap`, `topInHero`), `object-fit`/`object-position` на 1440 и 390, `mix-blend-mode`, маска (два градиента), фильтр на 390, высота hero (600 / 844), canvas частиц (размер = hero, `opacity` .72, `screen`, bitmap), частота кадров canvas (draws/s обеих сторон, порог 33,3 мс), reduced motion (видео на паузе, частиц 0 кадров), `saveData` (не воспроизводится в Playwright: ссылка на тест `Hero.test.tsx` «при saveData не подключает источники»). Значения оригинала на 390 в ячейке оригинала строкой «390: …».
    5. `### LIGHT`: таблица того же формата: canvas огоньков (атрибуты 942/694/248/0, `absolute`, `pointer-events: none`, bitmap = контейнер × min(dpr, MAX_DPR)), SVG без огоньков (`mapCircles` 0, `legacyNodes` 0, стран ≥ 177), узлы SVG на странице (`svg.nodes` на 1440 и 390 против 3109 «до» из measurements.md и порога 1300), частота цикла (draws/s около 30), пауза цикла вне экрана (`lightsDraws` на замере формы и `mapIntersects`), reduced motion (`lights.drawsPerSecond` 0). У оригинала в этих строках «Mapbox WebGL, не сравнивается».
    6. `### fps по секциям (LIGHT-07)`: таблица «Секция | Прод до v1.2, 1440 | Прод 1440 | Бюджет 1440 | Прод до v1.2, 390 CPU×4 | Прод 390 CPU×4 | Оригинал 390 CPU×4 | Бюджет 390 | Статус» на строки hero, карта, форма, about, потолок стенда (quote); числа «до» из `measurements.md` (65/66/69/76 и 30/30/30/73), оригинал 116,8 из measurements.md или из `orig-fps-390-cpu4.json`, бюджеты 100 и 55, у about и quote бюджет «нет». Под таблицей: три прогона по 2 с на секцию, медиана; `maxGapMs` худшего прогона hero и карты на 390.
    7. `### MOB`: таблица «Проверка | Ожидание | Прод 390 | Статус»: аудит целей касания (`checked`, `small` пуст, список `hidden` с размерами, `viaLabel`), ссылки футера (размеры обеих), label согласия и чекбокс (44 / 20×20), подпись логотипа (`10px`), логотип-ссылка (`brand` размеры), обложки новостей (атрибуты трёх первых), горизонтальная прокрутка (`scrollWidth` 390), плюс строка «Прод 1440» там, где значение отличается.
    8. `### Принятые отклонения`: нумерованный список: (1) видео на паузе при `prefers-reduced-motion` (оригинал крутит всегда) с числами из `prod-measure-1440-reduced.json`; (2) при `saveData` источники видео не подключаются (оригинал грузит всегда), проверено тестом; (3) свечение огоньков дышит синусом с периодом 2600 мс в пяти корзинах вместо `ease-in-out` keyframes оригинала; (4) палитра частиц литералами спецификации (`248 247 251`, `227 175 210`, `184 192 230`, `170 217 220`) вместо вычисленных брендовых цветов оригинала (см. 14-VERIFICATION); (5) частота canvas частиц измеряется 24…30 при 120 Гц из-за порога 33,3 мс, у оригинала тот же скрипт и та же частота (числа обеих сторон); (6) недоступность оригинала за challenge, если случилась; плюс любые расхождения из Task 2 с числами (например, разница размеров видео больше 1px). Ничего из списка не чинить: это запись.
    9. `### Скриншоты`: таблица «Файл | Вьюпорт | Что видно» на четыре строки `docs/qa/v12-*.jpeg` (и две `v12-orig-map-*.jpeg`, если сняты); ниже сравнение словами: `v12-hero-1440.jpeg` против `docs/research/v1.2/orig-hero-1440.jpeg`, `v12-hero-390.jpeg` против `orig-hero-390.jpeg`, `v12-map-1440.jpeg` против `docs/research/v1.1/orig-map-bottom.jpeg` (стык карты и формы) или `v12-orig-map-1440.jpeg`.
    10. `### Итог`: таблица «Поле | Значение»: вердикт («Принято» или «Принято с замечаниями» с перечислением), выполнение бюджета LIGHT-07 одной фразой с числами, замечания к методике (challenge, CPU×4 через CDP, потолок стенда 120 Гц, порог кадра canvas), найдено и починено при приёмке (если Task 2 правил исходники; иначе «ничего»), отложено на v2 (по REQUIREMENTS без изменений).
    Существующие разделы файла не менять. В вводном абзаце файла (строка про чеклист v1.1 в разделе «Фаза 13 / v1.1») дописать одну фразу: чеклист v1.2 в разделе «Фаза 17 / v1.2». В колонке «Статус» только «да», «отклонение №N» или «бюджет не выдержан»; пустых ячеек нет; числа переписываются из JSON без округления сверх двух знаков.

    **`README.md`.** В разделе «Проверка сборки» заменить «Одиннадцать проверок» на «Двенадцать проверок», дописать в перечень проверку видео глобуса (`hero-globe.webm` и `hero-globe.mp4` в `dist` и ссылки на них в бандле) и заменить `OK: 11 проверок` на `OK: 12 проверок`. В разделе «Браузерный smoke» после абзаца про v1.1 добавить абзац: раздел «Фаза 17 / v1.2» в `docs/qa/SMOKE.md` сравнивает прод с оригиналом по видео-глобусу и частицам, огонькам на canvas с бюджетом fps при CPU×4 и мобильным целям касания на 1440×900 и 390×844; скриншоты `docs/qa/v12-hero-1440.jpeg`, `v12-hero-390.jpeg`, `v12-map-1440.jpeg`, `v12-map-390.jpeg`; скрипты в `.planning/phases/17-integration-qa/qa/` с блоком команд: `QA=.planning/phases/17-integration-qa/qa`, `node $QA/v12-run.mjs measure --site prod --width 390 --height 844 --out $QA/results/prod-measure-390.json`, `node $QA/v12-run.mjs fps --site prod --width 390 --height 844 --cpu 4 --out $QA/results/prod-fps-390-cpu4.json`, `node $QA/v12-run.mjs shots --site prod --width 1440 --height 900 --out-dir docs/qa`, `node $QA/prod-hashes.mjs`. Блок команд v1.1 и путь `.planning/phases/13-integration-qa/qa/` заменить на архивный `.planning/milestones/v1.1-phases/13-integration-qa/qa/` (каталог переехал при закрытии v1.1). Остальной README не трогать.

    Проверить `npx prettier --check docs/qa/SMOKE.md README.md 2>/dev/null || true` (если prettier в проекте настроен на markdown; иначе пропустить) и `git status --porcelain | grep -v HANDOFF.json`: только файлы из `files_modified` планов 17-01 и 17-02. В SUMMARY: вердикт, число отклонений, ссылки на файлы; напоминание оркестратору о шагах из `<orchestrator>` (коммит, push, перепроверка хэшей, аудит milestone, архив, тег v1.2).
  </action>
  <verify>
    <automated>grep -c "^## Фаза 17 / v1.2" docs/qa/SMOKE.md | grep -qx 1 && for h in "### Гейт и деплой (SHIP-01, SHIP-02)" "### Стыки фаз" "### GLOBE" "### LIGHT" "### fps по секциям (LIGHT-07)" "### MOB" "### Принятые отклонения" "### Скриншоты" "### Итог"; do awk '/^## Фаза 17 \/ v1.2/,0' docs/qa/SMOKE.md | grep -qF "$h" || { echo "нет: $h"; exit 1; }; done && awk '/^## Фаза 17 \/ v1.2/,0' docs/qa/SMOKE.md | grep -q "hero-globe.webm" && awk '/^## Фаза 17 \/ v1.2/,0' docs/qa/SMOKE.md | grep -q "measurements.md" && for f in v12-hero-1440 v12-hero-390 v12-map-1440 v12-map-390; do grep -q "$f.jpeg" docs/qa/SMOKE.md || exit 1; done && grep -c "OK: 12 проверок" README.md | grep -qx 1 && grep -c "OK: 11 проверок" README.md | grep -qx 0 && grep -c "v12-" README.md | awk '{exit ($1 >= 1) ? 0 : 1}' && grep -c "Фаза 17 / v1.2" README.md | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Фаза 17 / v1.2" docs/qa/SMOKE.md` → 1; в новом разделе по одному разу подразделы `### Гейт и деплой (SHIP-01, SHIP-02)`, `### Стыки фаз`, `### GLOBE`, `### LIGHT`, `### fps по секциям (LIGHT-07)`, `### MOB`, `### Принятые отклонения`, `### Скриншоты`, `### Итог`
    - В новом разделе есть строки со значениями: `1066` или `1067`, `contain`, `cover`, `screen`, `saturate(1.18)`, `942`, `1300`, `≥ 55`, `≥ 100`, `116,8`, `sha256`, `hero-globe.webm`, `hero-globe.mp4`, `10px`, `44`, `Emulation.setCPUThrottlingRate`; пустых ячеек результата нет (`awk '/^## Фаза 17/,0' docs/qa/SMOKE.md | grep -c "|  |"` → 0)
    - `### Принятые отклонения` содержит не меньше пяти пунктов, среди них `reduced`, `saveData`, `2600`, `248 247 251`, `33`
    - `### Скриншоты` перечисляет `v12-hero-1440.jpeg`, `v12-hero-390.jpeg`, `v12-map-1440.jpeg`, `v12-map-390.jpeg` и ссылается на `orig-hero-1440.jpeg`, `orig-hero-390.jpeg`
    - Разделы SMOKE.md выше нового не изменились, кроме одной фразы во вводном абзаце (`git diff -- docs/qa/SMOKE.md` содержит удаления только в этой строке)
    - README: `grep -c "OK: 12 проверок" README.md` → 1; `grep -c "OK: 11 проверок" README.md` → 0; `grep -c "Двенадцать проверок" README.md` → 1; `grep -c "v12-" README.md` ≥ 1; `grep -c "Фаза 17 / v1.2" README.md` → 1; `grep -c "milestones/v1.1-phases/13-integration-qa/qa" README.md` ≥ 1; `grep -c "^.planning/phases/13-integration-qa" README.md` → 0
    - `git status --porcelain | grep -v HANDOFF.json` показывает только файлы из `files_modified` планов 17-01 и 17-02 (плюс `v12-orig-map-*.jpeg`, если снимались)
  </acceptance_criteria>
  <done>SMOKE.md содержит полную приёмку v1.2 с числами обеих сторон по GLOBE, LIGHT, fps и MOB, отклонениями и скриншотами; README указывает на раздел, скрипты и актуальное число проверок; рабочее дерево готово к коммиту и закрытию milestone оркестратором.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Браузер playwright → onevoice27.org (Vercel challenge, Mapbox WebGL) | чужой сайт читается только через страницу, форма не заполняется, данные не отправляются |
| Браузер → прод GitHub Pages | статический сайт, чтение, прокрутка, без отправки формы |
| CDP-сессия → Chrome (троттлинг CPU) | эмуляция влияет только на вкладку; сброс `rate: 1` обязателен |
| Постоянный профиль Chrome в tmpdir | cookie challenge оригинала живут вне репозитория |
| Репозиторий ← артефакты (JSON, JPEG) | случайный коммит служебных файлов из корня |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Information disclosure | скриншоты и JSON с оригинала | accept | публичные страницы без персональных данных; форма оригинала не трогается, snapshot без кликов |
| T-17-08 | Tampering | числа в таблицах SMOKE | mitigate | все значения переписываются из `qa/results/*.json`, verify Task 2 и Task 3 читают те же JSON и файл SMOKE |
| T-17-09 | Denial of service | fps-замер при открытом Mapbox оригинала или параллельной сборке | mitigate | прод меряется до сессии с оригиналом, WebGL-вкладки закрыты, три прогона и медиана, потолок стенда записан отдельной секцией |
| T-17-10 | Tampering | троттлинг CPU остаётся включённым после падения скрипта | mitigate | `rate: 1` и `detach` в `finally`; CDP-сессия на вкладку, а не на браузер |
| T-17-11 | Tampering | подмена `ctx.clearRect` в странице | mitigate | подмена только на объекте контекста, `delete` после замера возвращает метод прототипа; замеры не пересекаются во времени |
| T-17-12 | Tampering | файлы Playwright в корне репозитория | mitigate | JSON в `qa/results/`, JPEG в `docs/qa/`, verify проверяет пустой корень; профиль в `os.tmpdir()` |
| T-17-13 | Repudiation | прод после docs-пуша | mitigate | оркестратор перепроверяет `prod-hashes.mjs` после прогона (шаг 2 `<orchestrator>`) |
| T-17-SC | Tampering | npm/pip/cargo установки | accept | пакеты не добавляются; playwright из кэша npx или `PW_ROOT`; `package.json` и `package-lock.json` неизменны |
</threat_model>

<verification>
- `qa/results/prod-measure-1440.json`, `prod-measure-390.json`, `prod-measure-1440-reduced.json`, `prod-fps-390-cpu4.json`, `prod-fps-1440.json`, `orig-measure-1440.json`, `orig-measure-390.json`, `orig-fps-390-cpu4.json` существуют и проходят `node -e` из verify Task 2
- Пороги: видео `playing`, 1067×600 `contain` на 1440 и 390×844 `cover` на 390 (±2px), частицы 24…31 кадр/с, огоньки около 30 кадр/с, узлов SVG < 1300, fps ≥ 55 при CPU×4 на 390 и ≥ 100 на 1440 для hero/map/light-form, `touch.small` пуст на 390
- Четыре JPEG в `docs/qa/`, PNG нет, корень репозитория чист
- SMOKE.md с разделом «Фаза 17 / v1.2» из девяти подразделов, README с абзацем v1.2 и `OK: 12 проверок`
</verification>

<success_criteria>
- SHIP-03: Playwright-приёмка на 1440×900 и 390×844 подтверждает воспроизведение видео, размеры и `object-fit` как у оригинала, частоту canvas частиц, таблицу fps против бюджета LIGHT-07, аудит целей касания без элементов ниже 44px (кроме визуально скрытых), скриншоты hero и карты в `docs/qa/v12-*.jpeg`
- LIGHT-07 подтверждён на проде после слияния: hero, карта и форма ≥ 55 fps при CPU×4 и ≥ 100 без троттлинга, узлов SVG < 1300
- Критерий 5 фазы 17: форма под картой держит бюджет, цикл огоньков стоит при карте вне экрана (`lightsDraws`/`mapIntersects` записаны)
- SHIP-04 (документы): раздел «Фаза 17 / v1.2» с таблицами «оригинал / прод» по GLOBE, LIGHT и MOB, принятыми отклонениями и скриншотами; закрытие milestone (аудит, архив, тег `v1.2`) за оркестратором по шагам `<orchestrator>`
</success_criteria>

<output>
Create `.planning/phases/17-integration-qa/17-02-SUMMARY.md` when done: вердикт приёмки, таблица fps «до / после / оригинал / бюджет», размеры видео обеих сторон, список отклонений с номерами, доступность оригинала, список файлов для коммита оркестратора и напоминание о шагах закрытия milestone.
</output>
