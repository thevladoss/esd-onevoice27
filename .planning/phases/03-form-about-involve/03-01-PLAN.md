---
phase: 03-form-about-involve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validation.ts
  - src/lib/validation.test.ts
  - src/data/copy.form.ts
  - src/components/form/LightForm.tsx
  - src/components/form/LightForm.test.tsx
  - src/components/form/LightTypeChoice.tsx
  - src/components/form/FormField.tsx
  - src/components/form/ConsentCheckbox.tsx
  - src/components/form/SuccessToast.tsx
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03]
tags: [form, validation, react, testing-library]

must_haves:
  truths:
    - "Посетитель видит секцию #light-form с eyebrow «Участвуйте с нами», H2 «Зажгите свой свет» и двумя радио-карточками «Индивидуальный свет» / «Групповой маяк» с описаниями из 03-CONTEXT; карточка «Индивидуальный свет» выбрана по умолчанию"
    - "Посетитель заполняет имя, фамилию, страну (select из 12 стран ЕАД), город, email и чекбокс согласия; отправка пустой формы показывает ровно 6 сообщений об ошибках на русском, фокус переходит на поле «Имя»"
    - "После первой неудачной попытки поле перепроверяется на blur, а ошибка исчезает сразу, как только значение становится валидным; до первой попытки ошибок нет"
    - "Валидная отправка переводит кнопку в состояние «Зажигаем…» с aria-busy на 1200 мс, затем вызывает addLight({ type, countryId }) из useLights(), счётчик соответствующего типа растёт на 1, форма сбрасывается с сохранением выбранного типа, фокус возвращается на кнопку"
    - "После успешной отправки появляется тост role=\"status\" «Ваш свет зажжён! Огонёк уже на карте.», который закрывается через 4000 мс или по клику"
    - "Форма не делает сетевых запросов: у <form> нет action и method, onSubmit вызывает preventDefault"
  artifacts:
    - path: "src/lib/validation.ts"
      provides: "validateLightForm, типы LightFormValues/LightFormErrors, initialLightFormValues, toLightType"
      exports: ["validateLightForm", "initialLightFormValues", "toLightType", "EMAIL_RE"]
      min_lines: 30
    - path: "src/lib/validation.test.ts"
      provides: "unit-тесты шести правил и валидного набора"
      min_lines: 40
    - path: "src/data/copy.form.ts"
      provides: "все русские тексты секции формы"
      exports: ["formCopy"]
      contains: "Ваш свет зажжён! Огонёк уже на карте."
    - path: "src/components/form/LightForm.tsx"
      provides: "секция #light-form, состояние формы, submit, вызов addLight, тост"
      min_lines: 120
    - path: "src/components/form/LightTypeChoice.tsx"
      provides: "fieldset с двумя радио-карточками"
      min_lines: 30
    - path: "src/components/form/FormField.tsx"
      provides: "label + control + текст ошибки с aria-invalid/aria-describedby"
      min_lines: 25
    - path: "src/components/form/ConsentCheckbox.tsx"
      provides: "чекбокс согласия с кастомным боксом и ошибкой"
      min_lines: 25
    - path: "src/components/form/SuccessToast.tsx"
      provides: "тост role=status с автозакрытием и закрытием по клику"
      min_lines: 25
    - path: "src/components/form/LightForm.test.tsx"
      provides: "component-тесты формы: 6 ошибок, addLight, счётчик, тост, сброс, blur"
      min_lines: 120
  key_links:
    - from: "src/components/form/LightForm.tsx"
      to: "src/state/lights.tsx"
      via: "useLights().addLight"
      pattern: "addLight\\(\\{"
    - from: "src/components/form/LightForm.tsx"
      to: "src/lib/validation.ts"
      via: "validateLightForm при submit и blur"
      pattern: "validateLightForm\\("
    - from: "src/components/form/LightForm.tsx"
      to: "src/data/countries.ts"
      via: "опции select из массива стран ЕАД"
      pattern: "from \"\\.\\./\\.\\./data/countries\""
    - from: "src/components/form/SuccessToast.tsx"
      to: "a11y live region"
      via: "role=status"
      pattern: "role=\"status\""
    - from: "src/lib/validation.ts"
      to: "src/data/copy.form.ts"
      via: "тексты ошибок из formCopy.errors"
      pattern: "formCopy\\.errors"
---

## Phase Goal

**As a** посетитель лендинга, **I want to** понять суть «Единого голоса 27», увидеть путь от убеждения к действию и зажечь свой свет через форму, **so that** мой огонёк сразу появляется на карте и в счётчиках.

<objective>
Заменить заглушку `LightForm` фазы 1 на рабочую форму «Зажгите свой свет»: выбор типа света, шесть полей с клиентской валидацией на русском, мок-отправка в контекст огоньков фазы 2 (`addLight`), тост успеха и сброс. План покрывает поведение и тесты; визуальный контракт (CSS-токены полей, радио-карточек, тоста) добавляет план 03-04 той же фазы.

Purpose: это конверсионное ядро лендинга (FORM-01..03) и единственная точка, где посетитель меняет состояние карты и счётчиков.
Output: `src/lib/validation.ts` с тестами, `src/data/copy.form.ts`, компоненты в `src/components/form/`, `LightForm.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-form-about-involve/03-CONTEXT.md
@.planning/phases/03-form-about-involve/03-UI-SPEC.md
@.planning/phases/03-form-about-involve/03-RESEARCH.md
@.planning/phases/02-hero-and-map/02-CONTEXT.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md

<interfaces>
<!-- Ожидаемые контракты фаз 1–2 по их CONTEXT/UI-SPEC. Код фаз 1–2 пишется параллельно: перед использованием открой реальные файлы и сверь имена экспортов. Если имя отличается, используй реальное и зафиксируй расхождение в SUMMARY. -->

From src/state/lights.tsx (фаза 2, 02-CONTEXT «Данные и состояние»):
```typescript
export type Light = { id: string; type: "person" | "group"; countryId: number; lon: number; lat: number; isNew?: boolean };
export function LightsProvider(props: { children: React.ReactNode }): JSX.Element;
// useLights() отдаёт список огоньков, счётчики и действие добавления:
export function useLights(): { lights: Light[]; counts: { people: number; groups: number }; addLight(input: { type: "person" | "group"; countryId: number }): void };
```

From src/data/countries.ts (фаза 2):
```typescript
export interface EsdCountry { id: number; code: string; name: string; weight: number; center: [number, number] }
// массив из 12 стран, id по ISO-3166 numeric: 643 Россия, 112 Беларусь, 498 Молдова, 268 Грузия, 51 Армения, 31 Азербайджан, 398 Казахстан, 417 Кыргызстан, 762 Таджикистан, 795 Туркменистан, 860 Узбекистан, 4 Афганистан
```

From src/components/layout/* (фаза 1, 01-UI-SPEC «Component Inventory»):
```typescript
Section: { id: string; eyebrow?: string; title?: string; className?: string; children }
Eyebrow: { children }                       // <p> uppercase 12px horizon-200
GradientTitle: { as: "h1" | "h2"; variant: "hero" | "section"; children }
Button: { variant: "primary" | "ghost"; as: "a" | "button"; ...rest }  // rest пробрасывается в DOM-элемент
GlassCard: { className?: string; children }  // стекло, radius 16px, padding 24/32px
```

From src/components/map/Counters.tsx (фаза 2): рендерит подписи «Человек» и «Групп» и числа из `useLights().counts` через `useCountUp`; при `prefers-reduced-motion: reduce` показывает конечное значение сразу, старт по IntersectionObserver (threshold .4).

From src/test/setup.ts (фаза 1): моки `IntersectionObserver`, `ResizeObserver`, `window.matchMedia`, `HTMLCanvasElement.prototype.getContext`, `scrollTo`; `@testing-library/jest-dom/vitest`; `globals: true`, `css: false`.
</interfaces>
</context>

<class_contract>
Классы, которые компоненты этого плана обязаны выставить в разметке. План 03-04 пишет CSS ровно под эти имена, поэтому менять их нельзя. Раскладку (сетка, зазоры, ширина) в этом плане задавай утилитами Tailwind прямо в JSX; цвета, состояния, псевдоэлементы и токены оставь плану 03-04.

| Элемент | Класс |
|---------|-------|
| `Section` корень | `lf-section` |
| Блок заголовка (Eyebrow, H2, лид) | `lf-head`, лид `lf-lead` |
| `GlassCard` контейнер формы | `lf-card` |
| `<form>` | `lf-form` |
| `fieldset` типа света / `legend` | `lf-types` / `lf-legend` |
| `label` радио-карточки (атрибут `data-type="individual|group"`) | `lf-type`; заголовок `lf-type-title`, описание `lf-type-text`, точка `lf-type-dot` |
| Сетка полей | `lf-grid`; поле на всю ширину дополнительно `lf-span` |
| Обёртка поля / label / control / ошибка | `lf-field` / `lf-label` / `lf-control` / `lf-error` |
| Обёртка чекбокса / label / кастомный бокс / текст | `lf-consent` / `lf-check` / `lf-check-box` / `lf-check-text` |
| Кнопка отправки | `lf-submit` (плюс `id="light-form-submit"`) |
| Тост: внешний `div[role=status]` / внутренний `GlassCard` / иконка | `lf-toast` / `lf-toast-card` / `lf-toast-icon` |

Идентификаторы контролов: `light-form-firstName`, `light-form-lastName`, `light-form-countryId`, `light-form-city`, `light-form-email`, `light-form-consent`. Идентификатор ошибки: тот же плюс суффикс `-error` (например `light-form-email-error`).
</class_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Красные тесты валидации и формы</name>
  <files>src/lib/validation.test.ts, src/components/form/LightForm.test.tsx</files>
  <read_first>
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (раздел «Форма „Зажгите свой свет“»: правила валидации, тексты, поведение отправки, состав теста)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (разделы «Валидация», «Отправка и тост», «Copywriting Contract», «Accessibility Contract»)
    - .planning/phases/03-form-about-involve/03-RESEARCH.md (раздел «Форма без бэкенда»: сигнатура addLight, fake timers)
    - src/state/lights.tsx (реальная форма экспорта LightsProvider/useLights/addLight)
    - src/data/countries.ts (имя экспорта массива, название и id России)
    - src/components/map/Counters.tsx (как рендерятся подписи и числа, зависимость от matchMedia и IntersectionObserver)
    - src/test/setup.ts (что уже замокано; какие значения возвращает мок matchMedia)
    - package.json (есть ли `@testing-library/user-event`; если нет, используй `fireEvent` из `@testing-library/react`, новых зависимостей не добавлять)
  </read_first>
  <behavior>
    validation.test.ts (чистая функция, без DOM):
    - Test 1: `validateLightForm(initialLightFormValues)` возвращает объект ровно с шестью ключами `firstName, lastName, countryId, city, email, consent` и текстами «Введите имя», «Введите фамилию», «Выберите страну», «Укажите город», «Введите корректный email», «Нужно согласие на обработку данных».
    - Test 2: имя `" И "` (после trim короче 2 символов) даёт ошибку `firstName`; имя `"Иван"` ошибки не даёт.
    - Test 3: email `"ivan@"`, `"ivan@mail"`, `"ivan mail@x.ru"` дают «Введите корректный email»; `"ivan@example.org"` проходит.
    - Test 4: `countryId: ""` даёт «Выберите страну», `countryId: "643"` проходит; `consent: false` даёт ошибку, `true` проходит; город `"М"` даёт «Укажите город», `"Москва"` проходит.
    - Test 5: полный валидный набор (`type: "group"`, Иван, Иванов, "643", Москва, ivan@example.org, consent true) возвращает `{}`.
    - Test 6: `toLightType("individual") === "person"`, `toLightType("group") === "group"`.

    LightForm.test.tsx (рендер внутри `LightsProvider`, рядом `Counters` и тестовый `CountsProbe`, который через `useLights()` выводит `counts.groups`, `counts.people` и JSON последнего огонька `{ type, countryId }` в элементы с `data-testid="probe-groups"`, `probe-people`, `probe-last`; `window.matchMedia` в `beforeEach` переопределён так, что `(prefers-reduced-motion: reduce)` даёт `matches: true`; используются `vi.useFakeTimers()` и `act(() => vi.advanceTimersByTime(ms))`):
    - Test 1 (структура FORM-01): в секции `section#light-form` есть заголовок уровня 2 «Зажгите свой свет», текст «Участвуйте с нами», радио «Индивидуальный свет» (`checked`) и «Групповой маяк» с описаниями «Я лично посвящаю себя молитве, изучению и миссии там, где живу.» и «Церковь, школа, малая группа, семья или коллектив, которые идут вместе.»; `select#light-form-countryId` содержит 13 опций (первая «Выберите страну» с `value=""`, далее 12 стран, среди них «Россия» с `value="643"`); у `<form>` нет атрибутов `action` и `method`.
    - Test 2 (пустая отправка FORM-02): клик по кнопке «Зажечь свой свет» показывает ровно 6 элементов `[id^="light-form-"][id$="-error"]` с шестью текстами ошибок; ровно 6 контролов имеют `aria-invalid="true"`, у каждого `aria-describedby` равен id своей ошибки; `document.activeElement` равен `#light-form-firstName`; `probe-groups` остаётся `248`.
    - Test 3 (blur-перепроверка FORM-02): до первой отправки ввод «И» в имя и blur не показывают ошибок (0 элементов `-error`); после пустой отправки ввод «Иван» в имя убирает «Введите имя» сразу (без blur); ввод «И» и blur возвращают её.
    - Test 4 (успех FORM-03): выбрать «Групповой маяк», заполнить Иван / Иванов / «643» / Москва / ivan@example.org, отметить согласие, нажать кнопку. Сразу после клика кнопка `disabled`, `aria-busy="true"`, текст «Зажигаем…». После `advanceTimersByTime(1200)`: `probe-groups` = `249`, `probe-people` = `694`, `probe-last` = `{"type":"group","countryId":643}`; внутри `Counters` есть текст `249`; появился элемент `role="status"` с текстом «Ваш свет зажжён! Огонёк уже на карте.»; поля имени, фамилии, города, email пустые, select = `""`, чекбокс снят, радио «Групповой маяк» по-прежнему `checked`; кнопка снова активна с текстом «Зажечь свой свет», `document.activeElement` равен `#light-form-submit`.
    - Test 5 (тост FORM-03): после успешной отправки `advanceTimersByTime(4000)` убирает элемент `role="status"`; в отдельном прогоне клик по тосту убирает его до истечения 4000 мс.
  </behavior>
  <action>
    Создай два тестовых файла по блоку behavior. В `LightForm.test.tsx` импортируй `LightsProvider`/`useLights` из `../../state/lights`, `Counters` из `../map/Counters` и `LightForm` из `./LightForm`; определи `CountsProbe` прямо в тесте. Если мок `IntersectionObserver` из `src/test/setup.ts` не вызывает callback и `Counters` из-за этого не показывает значение, объяви локальный мок в этом тестовом файле (класс с `observe`, который синхронно вызывает callback с `[{ isIntersecting: true, intersectionRatio: 1, target }]`), не трогая `setup.ts`. События вводи через `fireEvent.change`/`fireEvent.click`/`fireEvent.blur`; кнопку ищи `getByRole("button", { name: /Зажечь свой свет/ })`. Тексты бери из `formCopy` (`../../data/copy.form`), чтобы тест не расходился с копирайтом. Запусти `npm test` и убедись, что оба файла падают из-за отсутствующих модулей (RED). Закоммить `test(03-01): add failing tests for light form and validation`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/lib/validation.test.ts src/components/form/LightForm.test.tsx 2>&1 | tail -20; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/validation.test.ts` существует и содержит строки `validateLightForm(` и все шесть текстов ошибок
    - `src/components/form/LightForm.test.tsx` существует, содержит `LightsProvider`, `Counters`, `role="status"`, `vi.useFakeTimers`, `advanceTimersByTime(1200)`, `advanceTimersByTime(4000)`, `probe-last`
    - `npx vitest run` по этим двум файлам завершается ненулевым кодом (RED), причина в отсутствии `./LightForm` и `../../lib/validation`
    - `git log -1 --format=%s` начинается с `test(03-01)`
  </acceptance_criteria>
  <done>Оба тестовых файла написаны по behavior, падают из-за отсутствующей реализации, закоммичены отдельным коммитом.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Валидация, тексты и рабочая форма с отправкой в контекст огоньков</name>
  <files>src/lib/validation.ts, src/data/copy.form.ts, src/components/form/LightTypeChoice.tsx, src/components/form/FormField.tsx, src/components/form/ConsentCheckbox.tsx, src/components/form/LightForm.tsx</files>
  <read_first>
    - src/components/form/LightForm.tsx (текущая заглушка фазы 1: как она использует Section, что из неё импортирует App.tsx; сигнатуру экспорта сохранить)
    - src/components/form/LightForm.test.tsx и src/lib/validation.test.ts (созданы в Task 1; ожидаемые id, тексты, порядок)
    - src/components/layout/Section.tsx, Eyebrow.tsx, GradientTitle.tsx, Button.tsx, GlassCard.tsx (реальные пропсы; проброс `className`, `id`, `type`, `disabled`, `aria-busy` у Button)
    - src/state/lights.tsx (сигнатура `addLight`, что возвращает `useLights`)
    - src/data/countries.ts (имя экспорта массива, поля `id` и `name`)
    - src/data/copy.ts (стиль объявления текстов фазы 1: `as const`, структура ключей; повторить, но в отдельном файле)
    - .planning/phases/03-form-about-involve/03-CONTEXT.md (раздел формы) и 03-UI-SPEC.md (разделы «Радио-карточки типа света», «Поля ввода и select», «Валидация», «Отправка и тост», «Accessibility Contract»)
  </read_first>
  <behavior>
    - Тесты Task 1 проходят, кроме проверок тоста в Test 4/5 (тост появляется в Task 3); остальные утверждения Test 4 (счётчики, сброс, фокус, busy) зелёные.
    - `validateLightForm` чистая: не читает DOM, не мутирует аргумент, для валидного набора возвращает `{}`.
  </behavior>
  <action>
    1. `src/data/copy.form.ts`: экспорт `formCopy` (`as const`) с ключами `eyebrow` «Участвуйте с нами», `title` «Зажгите свой свет», `lead` (абзац из 03-CONTEXT про приватность данных), `typeLegend` «Тип света», `types.individual` / `types.group` (`title`, `text` из Copywriting Contract), `fields.firstName|lastName|country|city|email` (`label`, `placeholder`; плейсхолдер страны «Выберите страну», остальные повторяют label), `consent` «Я согласен(на) на обработку персональных данных и получение рассылки», `submit` «Зажечь свой свет →», `submitting` «Зажигаем…», `success` «Ваш свет зажжён! Огонёк уже на карте.», `errors` с шестью текстами по ключам `firstName, lastName, countryId, city, email, consent`. Файл `src/data/copy.ts` не трогать (ограничение параллельного исполнения с фазой 4).

    2. `src/lib/validation.ts`: экспортируй `type LightType = "individual" | "group"`, `interface LightFormValues { type: LightType; firstName: string; lastName: string; countryId: string; city: string; email: string; consent: boolean }`, `type LightFormField = Exclude<keyof LightFormValues, "type">`, `type LightFormErrors = Partial<Record<LightFormField, string>>`, `const EMAIL_RE = /^[^\s@]+@[^\s@]+\.[^\s@]+$/`, `const LIGHT_FORM_FIELD_ORDER: LightFormField[]` в порядке firstName, lastName, countryId, city, email, consent, `const initialLightFormValues` (type "individual", строки пустые, consent false), `function toLightType(t: LightType): "person" | "group"`, `function validateLightForm(values): LightFormErrors`. Правила: `trim().length < 2` для имени, фамилии, города; `countryId === ""` для страны; `!EMAIL_RE.test(email.trim())` для email; `!consent` для согласия. Тексты только из `formCopy.errors` (D: «Введите имя», «Введите фамилию», «Выберите страну», «Укажите город», «Введите корректный email», «Нужно согласие на обработку данных»).

    3. `FormField.tsx`: пропсы `{ id: string; label: string; error?: string; className?: string; children: (control: { id: string; className: string; "aria-invalid": true | undefined; "aria-describedby": string | undefined }) => ReactNode }`. Рендер: `div.lf-field` (плюс `className`), `label.lf-label[htmlFor=id]`, `children({...})` с `className: "lf-control"`, `aria-invalid: error ? true : undefined`, `aria-describedby: error ? `${id}-error` : undefined`, и при наличии ошибки `p.lf-error#${id}-error`. Ошибка рендерится только когда есть текст (тест считает элементы `-error`).

    4. `LightTypeChoice.tsx`: пропсы `{ value: LightType; onChange(next: LightType): void }`. `fieldset.lf-types` с `legend.lf-legend` «Тип света», две `label.lf-type[data-type]`, внутри `input[type=radio].sr-only` с `name="lightType"`, `value`, `checked`, `onChange`; `span.lf-type-title` и `span.lf-type-text` из `formCopy.types`; `span.lf-type-dot[aria-hidden="true"]`. Раскладка утилитами: `grid gap-4 md:grid-cols-2`. Стили выбранного состояния и фокуса не пиши (план 03-04 делает `:has(input:checked)`).

    5. `ConsentCheckbox.tsx`: пропсы `{ checked: boolean; error?: string; onChange(next: boolean): void; onBlur(): void }`. `div.lf-consent`, `label.lf-check[htmlFor="light-form-consent"]` с `min-h-11 flex items-start gap-3`, `input[type=checkbox]#light-form-consent.sr-only` с `aria-invalid`/`aria-describedby` по тому же правилу, `span.lf-check-box[aria-hidden="true"]` с инлайн-SVG галочкой, `span.lf-check-text` с текстом согласия; ошибка `p.lf-error#light-form-consent-error` при наличии.

    6. `LightForm.tsx` (замена заглушки, тот же экспорт, который импортирует `App.tsx`): `Section id="light-form" className="lf-section"`. Внутри `div.lf-head` с `text-center`: `Eyebrow` «Участвуйте с нами», `GradientTitle as="h2" variant="section"` «Зажгите свой свет» (никаких собственных размеров и весов для H2; заголовок целиком задаёт примитив фазы 1), `p.lf-lead` с `max-w-[42rem] mx-auto`. Затем `GlassCard className="lf-card"` с `max-w-4xl mx-auto`, внутри `form.lf-form[noValidate]` без `action`/`method`, `onSubmit` с `preventDefault`. Состояние: `values: LightFormValues` (одним `useState`), `errors: LightFormErrors`, `attempted: boolean`, `submitting: boolean`, `toastOpen: boolean`. Блоки формы через `flex flex-col gap-8`: `LightTypeChoice`; `div.lf-grid` (`grid gap-4 md:grid-cols-2`) с полями Имя (`input`, `autocomplete="given-name"`), Фамилия (`family-name`), Страна (`select` с первой `option value=""` «Выберите страну» и 12 `option value={String(country.id)}` `{country.name}` из массива `src/data/countries.ts`, `autocomplete="country-name"`), Город (`address-level2`), Email (`type="email"`, `autocomplete="email"`, класс `lf-span md:col-span-2`); `ConsentCheckbox`; `Button variant="primary" as="button" type="submit" id="light-form-submit" className="lf-submit w-full"` с текстом `formCopy.submit` или `formCopy.submitting`, `disabled={submitting}`, `aria-busy={submitting || undefined}`. Плейсхолдеры из `formCopy.fields`.
       Обработчики: `onChange` поля обновляет `values`; если `attempted`, пересчитывает `validateLightForm(next)` и убирает ошибку этого поля, когда её больше нет (новые ошибки на change не добавляет). `onBlur` поля при `attempted` пересчитывает и выставляет ошибку этого поля из результата (или убирает). До первой отправки ни change, ни blur ошибок не показывают. `onSubmit`: `preventDefault`, `errs = validateLightForm(values)`, `setErrors(errs)`, `setAttempted(true)`; если ошибки есть, фокус на `document.getElementById(`light-form-${первый ключ по LIGHT_FORM_FIELD_ORDER}`)` и выход. Иначе `setSubmitting(true)` и `setTimeout(1200)`: `addLight({ type: toLightType(values.type), countryId: Number(values.countryId) })`, `setValues({ ...initialLightFormValues, type: values.type })`, `setErrors({})`, `setAttempted(false)`, `setSubmitting(false)`, `setToastOpen(true)`, фокус на `#light-form-submit`. Таймер хранить в `useRef` и очищать в cleanup `useEffect`. Тост рендерится в Task 3; сейчас оставь `toastOpen` в состоянии и место рендера с комментарием одной строкой.
       Ничего не отправляй по сети и не пиши в storage (T-03-02).

    Запусти `npm test` (валидационные тесты и Test 1–3 зелёные, Test 4 падает только на утверждениях про `role="status"`), затем `npm run build`. Закоммить `feat(03-01): light form with validation and addLight`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/lib/validation.test.ts 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/validation.test.ts` завершается кодом 0
    - `npm run build` завершается кодом 0
    - `grep -c "addLight({" src/components/form/LightForm.tsx` ≥ 1; `grep -c "validateLightForm(" src/components/form/LightForm.tsx` ≥ 2 (submit и blur/change)
    - `grep -c "noValidate" src/components/form/LightForm.tsx` = 1; `grep -c -E "action=|method=" src/components/form/LightForm.tsx` = 0
    - `grep -c "dangerouslySetInnerHTML" src/components/form/*.tsx` = 0 (файлы без совпадений дают 0 каждый)
    - `grep -c "formCopy.errors" src/lib/validation.ts` ≥ 1; `grep -c "document" src/lib/validation.ts` = 0
    - `grep -c 'autocomplete="given-name"\|autoComplete="given-name"' src/components/form/LightForm.tsx` ≥ 1
    - В `LightForm.test.tsx` при прогоне падают только утверждения с `role="status"` (Test 4 и Test 5); Test 1, 2, 3 зелёные
  </acceptance_criteria>
  <done>Форма рендерит типы, шесть полей и согласие, валидирует на submit и blur с текстами на русском, при валидной отправке вызывает `addLight` и сбрасывается с сохранением типа; билд зелёный.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Тост успеха и зелёный прогон всех тестов формы</name>
  <files>src/components/form/SuccessToast.tsx, src/components/form/LightForm.tsx</files>
  <read_first>
    - src/components/form/LightForm.tsx (место рендера тоста и состояние `toastOpen` из Task 2)
    - src/components/form/LightForm.test.tsx (Test 4 и Test 5: тексты, тайминги, ожидание исчезновения)
    - src/components/layout/GlassCard.tsx (пробрасывает ли `className`; используется как внутренняя поверхность тоста)
    - .planning/phases/03-form-about-involve/03-UI-SPEC.md (раздел «Отправка и тост», «Motion Contract» строки про тост, «Accessibility Contract» строка «Успех озвучен»)
    - .planning/phases/03-form-about-involve/03-RESEARCH.md (очистка таймера тоста в cleanup)
  </read_first>
  <behavior>
    - После успешной отправки в документе появляется ровно один элемент `role="status"` с `aria-live="polite"` и текстом `formCopy.success`.
    - Через 4000 мс элемент исчезает; клик по элементу убирает его раньше.
    - Повторная успешная отправка при открытом тосте не создаёт второго элемента (состояние `toastOpen` перезапускает таймер).
  </behavior>
  <action>
    `SuccessToast.tsx`: пропсы `{ open: boolean; message: string; onClose(): void; duration?: number }` (по умолчанию 4000). При `open === false` возвращает `null`. Рендер через `createPortal(..., document.body)`: `div.lf-toast[role="status"][aria-live="polite"]` с `onClick={onClose}`, внутри `GlassCard className="lf-toast-card"` с `span.lf-toast-icon[aria-hidden="true"]` (инлайн-SVG галочка 20px) и `span` с текстом. `useEffect` на `[open, duration, onClose]`: при `open` ставит `setTimeout(onClose, duration)` и очищает его в cleanup. Никаких кнопок внутри тоста (закрытие по клику в любое место, по UI-SPEC). Позиционирование (fixed, bottom 24px, центр, z-index 60) и анимацию появления добавит план 03-04; здесь только утилиты `fixed inset-x-0 bottom-6 z-[60] flex justify-center pointer-events-none` на внешнем элементе и `pointer-events-auto` на карточке, чтобы тост уже сейчас не перекрывал форму.
    В `LightForm.tsx` отрендери `<SuccessToast open={toastOpen} message={formCopy.success} onClose={() => setToastOpen(false)} />` после `GlassCard`; `onClose` оберни в `useCallback`. Запусти `npm test`: все тесты `LightForm.test.tsx` и `validation.test.ts` зелёные. `npm run build` зелёный. Закоммить `feat(03-01): success toast after lighting`.
  </action>
  <verify>
    <automated>cd /Users/thevladoss/devs/web/esd_cringe && npx vitest run src/components/form src/lib/validation.test.ts 2>&1 | tail -8 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/form src/lib/validation.test.ts` завершается кодом 0, в выводе нет `failed`
    - `npm test` (весь набор) завершается кодом 0
    - `grep -c 'role="status"' src/components/form/SuccessToast.tsx` = 1; `grep -c 'aria-live="polite"' src/components/form/SuccessToast.tsx` = 1
    - `grep -c "clearTimeout" src/components/form/SuccessToast.tsx` ≥ 1
    - `grep -c "<SuccessToast" src/components/form/LightForm.tsx` = 1
    - `grep -c "<button" src/components/form/SuccessToast.tsx` = 0
  </acceptance_criteria>
  <done>Тост появляется после успешной отправки, озвучивается live-region, закрывается по таймеру и клику; весь набор тестов проекта зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Пользовательский ввод → состояние React | Строки полей формы попадают в state и в DOM как текст |
| Форма → контекст огоньков | `addLight` получает только `type` и `countryId: number`; персональные данные в контекст не передаются |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LightForm поля, FormField ошибки | mitigate | Весь ввод рендерится текстовыми узлами React (автоэкранирование); `dangerouslySetInnerHTML` запрещён, проверка grep в Task 2 |
| T-03-02 | Information Disclosure | LightForm submit | mitigate | Нет сети, нет `localStorage`/`sessionStorage`; в `addLight` уходят только тип и числовой id страны; у `<form>` нет `action`/`method` |
| T-03-03 | Spoofing | Клиентская валидация | accept | Валидация только UX; без бэкенда нет доверенной стороны, документировано в PROJECT «Out of Scope» |
| T-03-04 | Denial of Service | Повторные отправки | mitigate | Кнопка `disabled` и `aria-busy` на 1200 мс; один таймер в `useRef`, очистка в cleanup |
| T-03-05 | Elevation of Privilege | Контекст огоньков | accept | Состояние живёт в памяти вкладки, привилегий нет |
| T-03-SC | Tampering | npm installs | accept | План не устанавливает пакеты; `package.json` не меняется (ограничение фазы) |
</threat_model>

<verification>
- `npm test` зелёный, включая `src/lib/validation.test.ts` и `src/components/form/LightForm.test.tsx`.
- `npm run build` зелёный, `tsc -b` без ошибок типов в `src/components/form` и `src/lib/validation.ts`.
- `git diff --name-only <base>..HEAD` содержит только файлы из `files_modified` этого плана: никаких правок `App.tsx`, `main.tsx`, `global.css`, `package.json`, `src/data/copy.ts`.
- Ручная проверка откладывается до плана 03-04 (визуальный контракт) и смоука фазы 5.
</verification>

<success_criteria>
- Секция `#light-form` содержит eyebrow, H2 через `GradientTitle`, лид, две радио-карточки с текстами 03-CONTEXT, шесть полей с label и согласие (FORM-01, FORM-02).
- Пустая отправка показывает шесть русских ошибок с `aria-invalid`/`aria-describedby` и переводит фокус на «Имя»; blur-перепроверка работает только после первой попытки (FORM-02).
- Валидная отправка вызывает `addLight` с нужным типом и страной, счётчик растёт на 1, тост `role="status"` появляется и исчезает через 4 с, форма сброшена с сохранением типа, сети нет (FORM-03).
- Все тесты и билд зелёные, три атомарных коммита (`test`, `feat`, `feat`).
</success_criteria>

<output>
Create `.planning/phases/03-form-about-involve/03-01-SUMMARY.md` when done. Зафиксируй в SUMMARY реальные имена экспортов `useLights`/массива стран, если они отличались от ожидаемых, и любые расхождения с UI-SPEC.
</output>
