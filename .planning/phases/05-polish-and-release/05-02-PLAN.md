---
phase: 05-polish-and-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/lib/scrollToSection.ts
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/layout/Button.tsx
  - src/components/layout/SkipLink.tsx
  - src/App.tsx
  - src/main.tsx
  - src/components/hero/Starfield.tsx
  - src/components/hero/GlobeCanvas.tsx
  - src/components/map/EsdMap.tsx
  - index.html
autonomous: true
requirements: [MOTION-02, MOTION-03]

must_haves:
  truths:
    - "При prefers-reduced-motion: reduce луч кнопки, пульсация огоньков, дрейф звёзд, глобус, волны и гало footer стоят: один блок @media в global.css гасит все слои через атрибут data-anim и оставляет статичные конечные состояния"
    - "Плавная прокрутка к якорям из меню при reduce идёт с behavior: auto"
    - "Первый фокусируемый элемент страницы: skip-link «Перейти к содержимому», ведёт на #main; main имеет id=main; header содержит nav с aria-label «Основная навигация»; footer содержит nav «Внешние ссылки» с rel=noopener noreferrer на внешних ссылках"
    - "Кольцо фокуса outline 2px solid var(--color-horizon-200) с offset 4px видно на каждом интерактивном элементе; outline: none в исходниках отсутствует"
    - "body держит overflow-x: clip, html держит scroll-padding-top: 96px; горизонтальный скролл от декоративных слоёв исключён на уровне оболочки"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Токены reveal в :root, единый блок reduced motion, глобальное кольцо фокуса, overflow-x: clip"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: "src/components/layout/SkipLink.tsx"
      provides: "Skip-link на #main"
      contains: "Перейти к содержимому"
    - path: "src/App.tsx"
      provides: "Ландмарк main#main и порядок SkipLink → Header → main → Footer"
      contains: "id=\"main\""
  key_links:
    - from: "src/styles/global.css"
      to: "[data-anim] в Starfield, GlobeCanvas, Button, EsdMap, Footer"
      via: "селекторы [data-anim], [data-anim]::before, [data-anim]::after внутри блока reduce"
      pattern: "\\[data-anim\\]"
    - from: "src/components/layout/SkipLink.tsx"
      to: "src/App.tsx <main id=\"main\">"
      via: "href=\"#main\""
      pattern: "href=\"#main\""
    - from: "src/lib/scrollToSection.ts или src/components/layout/Header.tsx"
      to: "window.matchMedia(\"(prefers-reduced-motion: reduce)\")"
      via: "behavior: reduce ? \"auto\" : \"smooth\""
      pattern: "prefers-reduced-motion"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Задачи построены срезами: после задачи 1 посетитель с включённым «Уменьшить движение» получает статичную страницу, после задачи 2 контракт `data-anim` покрывает все декоративные слои оболочки, после задачи 3 клавиатурный посетитель проходит страницу с skip-link и видимым фокусом.

<objective>
Свести политику reduced motion в один блок `global.css`, добавить токены reveal и защиту от горизонтального скролла, проставить `data-anim` на декоративные слои оболочки, hero и карты, довести оболочку до контракта доступности (skip-link, ландмарки, nav-метки, rel на внешних ссылках, глобальное кольцо фокуса).

Purpose: закрыть MOTION-02 целиком на уровне CSS и оболочки и часть MOTION-03 (фокус, ландмарки, защита от скролла). Секционные aria-атрибуты добирают планы 05-03 и 05-04.
Output: обновлённые `global.css`, `SkipLink.tsx`, `App.tsx`, `Header.tsx`, `Footer.tsx`, `Button.tsx`, `Starfield.tsx`, `GlobeCanvas.tsx`, `EsdMap.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/05-polish-and-release/05-RESEARCH.md
@.planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md
@.planning/research/PITFALLS.md

Четыре конфликта 05-CONTEXT.md с дизайн-системой решены в пользу фазы 1 (05-UI-SPEC.md «Разрешение конфликтов»): кольцо фокуса `var(--color-horizon-200)` (`#aad9dc`) с `outline-offset: 4px`, текст skip-link «Перейти к содержимому», цель `#main` и `<main id="main">`, непрозрачность текста на стекле остаётся `.8`. Файлы секций (`src/components/{map/MapSection,form,about,involve,news,resources,quote}`) в этой волне принадлежат плану 05-01: здесь их не трогать. `EsdMap.tsx`, `Starfield.tsx`, `GlobeCanvas.tsx` принадлежат этому плану.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Токены reveal, единый блок reduced motion и защита от горизонтального скролла в global.css</name>
  <files>src/styles/global.css, src/lib/scrollToSection.ts, src/components/layout/Header.tsx</files>
  <read_first>
    - src/styles/global.css (существующие `:root`, `@utility focus-ring`, `@keyframes footer-wave-drift`, `footer-halo-drift`, `beam`, `light-pulse`, `resources-particles`, уже имеющиеся блоки `@media (prefers-reduced-motion: reduce)`)
    - src/styles/tokens.css (реестр токенов `@theme`; новых токенов не добавлять)
    - src/**/*.css (любые дополнительные CSS-файлы секций с блоками reduced motion, которые нужно слить в единый блок)
    - src/lib/*.ts и src/components/layout/Header.tsx (где живёт `scrollToSection` и вызов `window.scrollTo`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, разделы «Motion Contract» → «Токены движения», «Reduced Motion Policy» → «CSS» и «JS», «Responsive Checklist» → «Защита от горизонтального скролла», «Accessibility Contract» → «Фокус»
  </read_first>
  <action>
    В `:root` рядом с `--dur-ui`, `--dur-header`, `--dur-underline` добавить `--dur-reveal: 700ms`, `--ease-reveal: cubic-bezier(0.22, 1, 0.36, 1)`, `--stagger-reveal: 80ms`, `--reveal-shift: 24px`; ниже `@media (max-width: 767px) { :root { --reveal-shift: 16px; } }`.

    В самом низу `global.css`, после всех утилит, разместить единственный блок `@media (prefers-reduced-motion: reduce)` с четырьмя группами правил из 05-UI-SPEC.md «Reduced Motion Policy → CSS»: (1) страховка `*, *::before, *::after` с `animation-duration: 0.01ms !important`, `animation-iteration-count: 1 !important`, `transition-duration: 0.01ms !important`, `scroll-behavior: auto !important`; (2) явное гашение `[data-anim], [data-anim]::before, [data-anim]::after { animation: none !important; }`; (3) статичные конечные состояния: `[data-anim="halo"] { transform: translateX(-50%); }`, `[data-anim="pulse"] circle { transform: none; opacity: .9; }`, `[data-anim="beam"]::before { background: var(--gradient-action); opacity: .55; }`, `[data-anim="particles"] { opacity: .28; }`; (4) `a:hover, button:hover, article:hover img { transform: none !important; }` и `html { scroll-behavior: auto; }`. Все ранее существовавшие блоки `@media (prefers-reduced-motion: reduce)` в `global.css` и в других `.css` внутри `src/` перенести сюда и удалить на прежних местах: после задачи в `src/**/*.css` остаётся ровно один такой блок. Смена цвета, границы и тени при hover и focus не гасятся.

    Защита от горизонтального скролла: `body { overflow-x: clip; }` (не `hidden`, иначе ломается `position: fixed` у header); убедиться, что `html { scroll-padding-top: 96px; }` присутствует.

    Фокус: `@utility focus-ring` должна давать `outline: 2px solid var(--color-horizon-200); outline-offset: 4px`. Добавить глобальное правило для `a:focus-visible, button:focus-visible, input:focus-visible, select:focus-visible, textarea:focus-visible, [tabindex]:focus-visible` с тем же кольцом, чтобы элементы фаз 2–4 без утилиты тоже получили кольцо. Удалить из `src/**` любые `outline: none`, `outline-width: 0` и классы `outline-none`, заменив на `focus-ring` или `focus-visible:` вариант с тем же кольцом (внутри `overflow: hidden` контейнеров допускается `outline-offset: -3px`, это делают планы 05-03/05-04 в своих файлах).

    `scrollToSection` (в `src/lib/scrollToSection.ts` или внутри `Header.tsx`, где найдётся): значение `behavior` вычислять как `window.matchMedia("(prefers-reduced-motion: reduce)").matches ? "auto" : "smooth"`; литерала `behavior: "smooth"` без условия не оставлять. Это единственное разрешённое место ручного `matchMedia` для reduce вне хука `useReducedMotion`.
  
    Яркость глобуса (docs/qa/SMOKE-phase2.md: средняя яркость canvas 10/255, освещено 7% сэмплов): в `src/components/hero/globe.ts` / `GlobeCanvas.tsx` поднять alpha точек передней стороны до ≥ .85 и задней до .25, размер точек 1.4–2.6px, включить `ctx.globalCompositeOperation = "lighter"` для свечения, при необходимости увеличить количество точек до 2200 и убрать лишнее затемнение скримом над областью глобуса (`.hero__scrim` не должен закрывать правую половину полностью). Критерий: сэмпл яркости той же области ≥ 35/255 и ≥ 30% освещённых точек, глобус читается как сфера на скриншоте 1440.
</action>
  <verify>
    <automated>npm test && npm run build && test "$(grep -rl "prefers-reduced-motion" src --include=*.css | wc -l | tr -d ' ')" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/hero/globe.ts` или `GlobeCanvas.tsx` содержит `globalCompositeOperation` и значение alpha передней стороны ≥ 0.85; замер яркости canvas оркестратором даёт avg ≥ 35 и ≥ 30% освещённых сэмплов
    - `grep -c "prefers-reduced-motion: reduce" src/styles/global.css` выводит 1; `grep -rl "prefers-reduced-motion" src --include=*.css | wc -l` выводит 1
    - `grep -c "\-\-dur-reveal: 700ms" src/styles/global.css` выводит 1; `grep -c "\-\-ease-reveal: cubic-bezier(0.22, 1, 0.36, 1)" src/styles/global.css` выводит 1; `grep -c "\-\-stagger-reveal: 80ms" src/styles/global.css` выводит 1; `grep -c "\-\-reveal-shift: 24px" src/styles/global.css` выводит 1; `grep -c "\-\-reveal-shift: 16px" src/styles/global.css` выводит 1
    - `grep -c "animation-duration: 0.01ms !important" src/styles/global.css` выводит 1; `grep -c "transition-duration: 0.01ms !important" src/styles/global.css` выводит 1
    - `grep -cE '^\s*\[data-anim\],?$|\[data-anim\]::before|\[data-anim\]::after' src/styles/global.css` выводит не меньше 2; `grep -c 'animation: none !important' src/styles/global.css` не меньше 1
    - `grep -c '\[data-anim="halo"\]' src/styles/global.css` не меньше 1; `grep -c '\[data-anim="pulse"\] circle' src/styles/global.css` не меньше 1; `grep -c '\[data-anim="beam"\]::before' src/styles/global.css` не меньше 1; `grep -c '\[data-anim="particles"\]' src/styles/global.css` не меньше 1
    - `grep -c "overflow-x: clip" src/styles/global.css` не меньше 1; `grep -c "scroll-padding-top: 96px" src/styles/global.css` не меньше 1
    - `grep -cE "outline: 2px solid var\(--color-horizon-200\)" src/styles/global.css` не меньше 2 (утилита и глобальное правило); `grep -c "outline-offset: 4px" src/styles/global.css` не меньше 1
    - `grep -rnE "outline:\s*none|outline-width:\s*0|\boutline-none\b" src --include=*.tsx --include=*.ts --include=*.css | wc -l` выводит 0
    - `grep -rn 'behavior: "smooth"' src --include=*.ts --include=*.tsx | grep -v "\.test\." | wc -l` выводит 0; `grep -rl "prefers-reduced-motion" src --include=*.ts --include=*.tsx | xargs grep -l "scrollTo" | wc -l` не меньше 1
    - `npm test` завершается 0; `npm run build` завершается 0
  </acceptance_criteria>
  <done>Один блок reduce в CSS проекта, токены reveal объявлены, кольцо фокуса глобально, `overflow-x: clip` на body, прокрутка к якорям уважает reduce.</done>
</task>

<task type="auto">
  <name>Task 2: Контракт data-anim и aria-hidden на декоративных слоях оболочки, hero и карты</name>
  <files>src/components/hero/Starfield.tsx, src/components/hero/GlobeCanvas.tsx, src/components/layout/Button.tsx, src/components/map/EsdMap.tsx, src/components/layout/Footer.tsx</files>
  <read_first>
    - src/components/hero/Starfield.tsx (корневой слой звёзд, есть ли `aria-hidden`, где дрейф 120s)
    - src/components/hero/GlobeCanvas.tsx (`<canvas>`, условие запуска rAF, проверка reduce, `document.hidden`, IntersectionObserver)
    - src/components/layout/Button.tsx (как рендерится `variant="primary"`, где `::before` луча)
    - src/components/map/*.tsx (слой огоньков: группа `<g>` пульсирующих кружков с классом `pulse`, кольцо `is-new` нового огонька)
    - src/components/layout/Footer.tsx (реализация волн: фон элемента; гало: `::before` или отдельный узел)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Reduced Motion Policy» → «Контракт разметки» (таблица девяти значений `data-anim`) и «JS»
    - .planning/research/FEATURES.md, таблица «Доступность и мобильное поведение по каждому анимированному элементу»
  </read_first>
  <action>
    Проставить атрибуты из закрытого списка `data-anim`, ровно там, где живёт анимация:

    `Starfield.tsx`: корневой слой звёзд получает `data-anim="stars"` и `aria-hidden="true"`; если слои звёзд и атмосферных пятен раздельные узлы, атрибут получает узел с анимацией дрейфа. `GlobeCanvas.tsx`: `<canvas data-anim="globe" aria-hidden="true">`; проверить, что при reduce (через `useReducedMotion()` или `matchMedia`) rAF не запускается, а рисуется один статичный кадр; если проверка отсутствует, добавить.

    `Button.tsx`: при `variant === "primary"` элемент получает `data-anim="beam"`; сам `::before` луча остаётся в `global.css`, блок reduce гасит его через `[data-anim="beam"]::before`. У `ghost` атрибута нет.

    `EsdMap.tsx` (или файл слоя огоньков в `src/components/map/`): группа `<g>`, содержащая пульсирующие огоньки, получает `data-anim="pulse"`; элемент кольца нового огонька (`is-new`) получает `data-anim="new-light"`. Атрибут ставится на группу, а не на каждый из сотен кружков. Проверить, что у SVG-карты есть `role="img"` и вложенный `<title>`; если нет, оставить план 05-03 (владелец aria карты), здесь только `data-anim`.

    `Footer.tsx`: волны рисуются фоном самого footer, поэтому footer-контейнер с анимацией `footer-wave-drift` получает `data-anim="wave"`. Гало: если оно реализовано `::before` того же элемента, вынести его в отдельный узел `<div aria-hidden="true" data-anim="halo">` с тем же позиционированием и стилем (иначе статичное правило `[data-anim="halo"] { transform: translateX(-50%) }` из блока reduce применится к footer, а не к гало). Если гало уже отдельный узел, только добавить атрибут и `aria-hidden`.

    Значения `particles` и `atmosphere` проставит план 05-04 в `src/components/resources/` (владелец файлов в этой волне: 05-01). Итоговый реестр после обеих волн: 9 значений.
  </action>
  <verify>
    <automated>npm test && test "$(grep -rhoE 'data-anim="[a-z-]+"' src | sort -u | wc -l | tr -d ' ')" -ge 7</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-anim="stars"' src/components/hero/Starfield.tsx` не меньше 1; `grep -c 'aria-hidden' src/components/hero/Starfield.tsx` не меньше 1
    - `grep -c 'data-anim="globe"' src/components/hero/GlobeCanvas.tsx` не меньше 1; `grep -c 'aria-hidden' src/components/hero/GlobeCanvas.tsx` не меньше 1; `grep -cE "useReducedMotion|prefers-reduced-motion" src/components/hero/GlobeCanvas.tsx` не меньше 1
    - `grep -c 'data-anim="beam"' src/components/layout/Button.tsx` не меньше 1
    - `grep -rl 'data-anim="pulse"' src/components/map | wc -l` не меньше 1; `grep -rl 'data-anim="new-light"' src/components/map | wc -l` не меньше 1
    - `grep -c 'data-anim="wave"' src/components/layout/Footer.tsx` не меньше 1; `grep -c 'data-anim="halo"' src/components/layout/Footer.tsx` не меньше 1
    - `grep -rhoE 'data-anim="[a-z-]+"' src | sort -u` содержит строки для каждого из значений `stars`, `globe`, `beam`, `pulse`, `new-light`, `wave`, `halo`
    - `grep -rhoE 'data-anim="[a-z-]+"' src | sort -u | grep -vcE '^data-anim="(stars|globe|beam|pulse|new-light|particles|atmosphere|wave|halo)"$'` выводит 0 (значений вне закрытого списка нет)
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Семь значений `data-anim` проставлены на реальных носителях анимации, декоративные слои скрыты от скринридера, тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Оболочка доступности: skip-link, ландмарки, nav-метки, внешние ссылки</name>
  <files>src/components/layout/SkipLink.tsx, src/App.tsx, src/main.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, index.html</files>
  <read_first>
    - src/App.tsx и src/main.tsx (порядок `SkipLink`, `Header`, `main`, `Footer`; провайдеры)
    - src/components/layout/SkipLink.tsx (текст, `href`, классы скрытия, z-index)
    - src/components/layout/Header.tsx (`<nav aria-label>`, бургер: `aria-expanded`, `aria-controls`, `aria-label`; оверлей: `role="dialog"`, `aria-modal`, `aria-label`; `aria-current` активного пункта)
    - src/components/layout/Footer.tsx (обёртка ссылок, `target`/`rel`, скрытый текст «(откроется в новой вкладке)»)
    - index.html (`lang="ru"`, `<title>`, `<noscript>`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Accessibility Contract» → «Ландмарки и заголовки», «aria по элементам», «Клавиатура»
    - .planning/phases/01-scaffold-and-deploy/01-UI-SPEC.md, разделы `Header`, `BurgerButton`, `MobileMenu`, `Footer`, `SkipLink`
  </read_first>
  <action>
    `SkipLink.tsx`: текст ровно «Перейти к содержимому», `href="#main"`, визуально скрыт до фокуса (`clip-path: inset(50%)` или `sr-only`), при `:focus-visible` виден в левом верхнем углу с `z-index: 60` (Tailwind `z-[60]` либо правило в `global.css` для класса skip-link), поверхность `midnight-900`, рамка `horizon-200`, padding 12px 24px, радиус 12px. `App.tsx`: `SkipLink` первым ребёнком корневого фрагмента, до `Header`; восемь секций внутри `<main id="main" tabIndex={-1}>` (`tabIndex={-1}` даёт цели skip-link принимать фокус во всех браузерах); `Footer` после `main`. Если корневой узел приложения в `main.tsx` уже содержит `main`, привести к одному `main` в `App.tsx`.

    `Header.tsx`: `<nav aria-label="Основная навигация">` вокруг меню; бургер с `aria-expanded`, `aria-controls="mobile-menu"`, `aria-label` «Открыть меню» / «Закрыть меню»; оверлей `id="mobile-menu" role="dialog" aria-modal="true" aria-label="Меню"`; пункт активной секции `aria-current="true"`; вордмарк `aria-label="Единый голос 27, на главную"`. Всё, что уже сделано фазой 1, оставить как есть; добавить только отсутствующее.

    `Footer.tsx`: ссылки внутри `<nav aria-label="Внешние ссылки">`; обе внешние ссылки несут `target="_blank" rel="noopener noreferrer"` в одной строке JSX (гейт ниже построчный); визуально скрытый текст «(откроется в новой вкладке)».

    `index.html`: подтвердить `lang="ru"`, `<title>Единый голос 27 — Евро-Азиатский дивизион</title>`, `<noscript>` с текстом «Для работы сайта нужен JavaScript. Включите его в настройках браузера и обновите страницу.»; при отсутствии добавить.

    Существующие тесты Header/Footer фазы 1 обновить только там, где изменилась разметка. `App.test.tsx` пишет план 05-05, здесь его не создавать.
  </action>
  <verify>
    <automated>npm test && npm run build && test "$(grep -rn 'target="_blank"' src --include=*.tsx | grep -vc 'noopener noreferrer' | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Перейти к содержимому" src/components/layout/SkipLink.tsx` не меньше 1; `grep -c 'href="#main"' src/components/layout/SkipLink.tsx` не меньше 1
    - `grep -cE "z-\[60\]|z-60|z-index: ?60" src/components/layout/SkipLink.tsx src/styles/global.css | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -cE '<main[^>]*id="main"' src/App.tsx` выводит 1; `grep -cE '<main[^>]*tabIndex=\{-1\}' src/App.tsx` выводит 1
    - `grep -n -m1 -E "<SkipLink|<Header" src/App.tsx` выводит строку, содержащую `<SkipLink`
    - `grep -c 'aria-label="Основная навигация"' src/components/layout/Header.tsx` не меньше 1; `grep -c 'aria-controls="mobile-menu"' src/components/layout/Header.tsx` не меньше 1; `grep -c 'role="dialog"' src/components/layout/Header.tsx` не меньше 1; `grep -c 'aria-modal="true"' src/components/layout/Header.tsx` не меньше 1; `grep -c "aria-expanded" src/components/layout/Header.tsx` не меньше 1; `grep -c "aria-current" src/components/layout/Header.tsx` не меньше 1
    - `grep -c 'aria-label="Внешние ссылки"' src/components/layout/Footer.tsx` не меньше 1
    - `grep -rn 'target="_blank"' src --include=*.tsx | grep -vc 'noopener noreferrer'` выводит 0
    - `grep -c 'lang="ru"' index.html` выводит 1; `grep -c "<title>Единый голос 27 — Евро-Азиатский дивизион</title>" index.html` выводит 1; `grep -c "<noscript>" index.html` выводит 1
    - `npm test` завершается 0; `npm run build` завершается 0
  </acceptance_criteria>
  <done>Порядок табуляции начинается со skip-link, ландмарки и nav-метки на месте, внешние ссылки безопасны, тесты и сборка зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Страница → внешние сайты (esd.adventist.org, onevoice27.org) | Переход по ссылке в новой вкладке: без `noopener` открытая страница получает `window.opener` |
| ОС посетителя → страница | Настройка `prefers-reduced-motion` читается через `matchMedia`, значение доверенное, только влияет на анимацию |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Внешние ссылки footer (reverse tabnabbing) | mitigate | `rel="noopener noreferrer"` на каждой `target="_blank"`, построчный grep-гейт в задаче 3 |
| T-05-04 | Information Disclosure | `rel=noreferrer` | mitigate | Referer к внешним сайтам не передаётся; входит в тот же атрибут |
| T-05-05 | Denial of Service | Бесконечные CSS-анимации при reduce (батарея, доступность) | mitigate | Единый блок `@media (prefers-reduced-motion: reduce)` гасит все `[data-anim]` слои |
| T-05-SC | Tampering | npm installs | mitigate | Установок в плане нет; `git diff -- package.json` без новых зависимостей |
</threat_model>

<verification>
- `grep -rl "prefers-reduced-motion" src --include=*.css | wc -l` равен 1
- Семь значений `data-anim` присутствуют, значений вне списка нет
- Skip-link первый в `App.tsx`, `main#main` один, nav-метки на месте, все `target="_blank"` с `rel="noopener noreferrer"`
- `outline: none` в `src/` отсутствует
- `npm test` и `npm run build` зелёные
</verification>

<success_criteria>
- Единый источник правды reduce в CSS с четырьмя группами правил из UI-SPEC
- Токены reveal в `:root`, `overflow-x: clip` на body, глобальное кольцо фокуса
- Оболочка доступности соответствует таблицам «Ландмарки и заголовки» и «aria по элементам» для header, footer, skip-link
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-02-SUMMARY.md` when done
</output>
