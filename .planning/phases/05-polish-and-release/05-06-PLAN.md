---
phase: 05-polish-and-release
plan: 06
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - vite.config.ts
  - package.json
  - scripts/check-dist.mjs
  - README.md
  - docs/qa/SMOKE.md
autonomous: true
requirements: [MOTION-04, QA-04]

must_haves:
  truths:
    - "npm run build завершается без предупреждения о размере чанков и без предупреждений deprecated: d3-*, topojson-client и world-atlas лежат в чанке vendor-map"
    - "В продакшн-коде src нет ни одного console.*; в консоли собранного билда нечему появляться от нашего кода"
    - "node scripts/check-dist.mjs подтверждает по dist: lang=ru, title, описание и og-теги, все ассеты под /esd-onevoice27/assets/ и существуют, чанк vendor-map присутствует, ни один JS-чанк не больше 500 КБ, восемь id секций присутствуют в бандле, внешние хосты в index.html только Google Fonts"
    - "README содержит раздел «Проверка» с командами тестов, сборки, check:dist, preview и ссылкой на docs/qa/SMOKE.md; docs/qa/SMOKE.md содержит полный чеклист smoke с ожидаемыми результатами и пустыми колонками результатов для preview и прода"
  artifacts:
    - path: "vite.config.ts"
      provides: "codeSplitting.groups с чанком vendor-map"
      contains: "vendor-map"
    - path: "scripts/check-dist.mjs"
      provides: "Node-проверка dist без зависимостей"
      min_lines: 60
    - path: "docs/qa/SMOKE.md"
      provides: "Приёмочный чеклист smoke"
      contains: "final-reduced-motion.jpeg"
    - path: "README.md"
      provides: "Раздел «Проверка»"
      contains: "## Проверка"
  key_links:
    - from: "package.json scripts.check:dist"
      to: "scripts/check-dist.mjs"
      via: "node scripts/check-dist.mjs"
      pattern: "check:dist"
    - from: "vite.config.ts"
      to: "node_modules/d3-*, topojson-client, world-atlas"
      via: "build.rolldownOptions.output.codeSplitting.groups[].test"
      pattern: "codeSplitting"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Срезы: после задачи 1 посетитель получает билд без лишних предупреждений и без утечек в консоль; после задачи 2 любой разработчик одной командой проверяет корректность `dist` под GitHub Pages; после задачи 3 у приёмки есть чеклист, по которому оркестратор прогоняет браузерный smoke.

<objective>
Разбить бандл (чанк `vendor-map`), вычистить `console.*` из продакшн-кода, добавить Node-проверку `scripts/check-dist.mjs`, дополнить README разделом «Проверка» и подготовить `docs/qa/SMOKE.md` как приёмочный чеклист.

Purpose: закрыть MOTION-04 (чистая консоль, билд без предупреждений) и подготовить инструменты для QA-04 (smoke по preview и проду выполняет 05-07).
Output: `vite.config.ts`, `scripts/check-dist.mjs`, скрипт `check:dist` в `package.json`, `README.md`, `docs/qa/SMOKE.md`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/05-polish-and-release/05-RESEARCH.md
@.planning/research/PITFALLS.md

Vite 8 собирает через Rolldown: `build.rollupOptions.output.manualChunks` помечен deprecated и проксируется; актуальный ключ `build.rolldownOptions.output.codeSplitting.groups` (массив `{ name, test }`, `test` строка или RegExp по пути модуля). Проверено по документации Vite 8.0.x во время планирования. CONTEXT называл `manualChunks`; способ разбиения отдан на усмотрение, берём актуальный API. Playwright в зависимости не добавляется: браузерный smoke выполняет оркестратор через Playwright MCP по чеклисту `docs/qa/SMOKE.md` (план 05-07). Параллельный план 05-05 владеет `*.test.tsx` и `src/test/setup.ts`: здесь их не трогать; зачистка `console.*` идёт по `src/**` без тестов.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Чанк vendor-map, зачистка console.* и сборка без предупреждений</name>
  <files>vite.config.ts</files>
  <read_first>
    - vite.config.ts (текущие `base`, `plugins`, `test`; отсутствие `build`)
    - package.json (скрипт `build`: `tsc -b && vite build`; версии `vite`, `d3-*`, `topojson-client`, `world-atlas`)
    - Результат `grep -rnE "console\.(log|warn|error|info|debug|table|trace|dir)" src --include=*.ts --include=*.tsx | grep -vE "\.test\.|src/test/"` (места для зачистки)
    - .planning/phases/05-polish-and-release/05-RESEARCH.md, «Размер бандла»; 05-UI-SPEC.md, «Acceptance Smoke Checklist» → «Команды»
  </read_first>
  <action>
    В `vite.config.ts` добавить `build.rolldownOptions.output.codeSplitting.groups` с одной группой `{ name: "vendor-map", test: /node_modules[\\/](d3-[a-z-]+|topojson-client|world-atlas)[\\/]/ }` (транзитивные `d3-dispatch`, `d3-drag`, `d3-interpolate`, `d3-timer`, `d3-ease`, `d3-transition` попадают в тот же чанк по маске, это решение в рамках усмотрения по способу разбиения). `manualChunks` и `chunkSizeWarningLimit` не использовать: порог предупреждения остаётся стандартным (500 КБ), скрывать его запрещено. Если после сборки предупреждение о размере остаётся, добавить вторую группу `{ name: "vendor-motion", test: /node_modules[\\/]motion[\\/]/ }`; если и этого мало, третью `{ name: "vendor-react", test: /node_modules[\\/](react|react-dom|scheduler)[\\/]/ }`. Записать итоговый набор групп и размеры чанков из вывода сборки в SUMMARY.

    Зачистить `console.*` из `src/**` вне тестов и `src/test/`: отладочные вызовы удалить; в обработчиках ошибок (например, блок «Карта не загрузилась») оставить пользовательское сообщение в UI и убрать вывод в консоль. Полагаться на `dropConsole` минификатора запрещено: правило UI-SPEC требует отсутствия `console.*` в исходниках.

    Прогнать `npm run build 2>&1 | tee /tmp/build-05-06.log`: код выхода 0, в логе нет строк с `larger than`, `deprecated`, `(!)`; в `dist/assets` ровно один файл `vendor-map-*.js`.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tee /tmp/build-05-06.log >/dev/null; test "${PIPESTATUS[0]}" = "0" && test "$(grep -ci "larger than" /tmp/build-05-06.log)" = "0" && test "$(grep -ci "deprecated" /tmp/build-05-06.log)" = "0" && test "$(ls dist/assets | grep -c '^vendor-map-.*\.js$')" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "codeSplitting" vite.config.ts` не меньше 1; `grep -c '"vendor-map"' vite.config.ts` выводит 1; `grep -c "rolldownOptions" vite.config.ts` не меньше 1
    - `grep -c "manualChunks" vite.config.ts` выводит 0; `grep -c "chunkSizeWarningLimit" vite.config.ts` выводит 0
    - `grep -rnE "console\.(log|warn|error|info|debug|table|trace|dir)" src --include=*.ts --include=*.tsx | grep -vE "\.test\.|src/test/" | wc -l` выводит 0
    - `npm run build` завершается 0; `npm run build 2>&1 | grep -ci "larger than"` выводит 0; `npm run build 2>&1 | grep -ci "deprecated"` выводит 0; `npm run build 2>&1 | grep -c "(!)"` выводит 0
    - `ls dist/assets | grep -c '^vendor-map-.*\.js$'` выводит 1
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Сборка чистая, чанк `vendor-map` на месте, `console.*` в продакшн-коде нет.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/check-dist.mjs: Node-проверка собранного dist</name>
  <files>scripts/check-dist.mjs, package.json</files>
  <read_first>
    - dist/index.html после `npm run build` (структура: `lang`, `<title>`, meta, `<link>`/`<script>` с `/esd-onevoice27/assets/…`, `<noscript>`)
    - index.html в корне (какие внешние хосты объявлены: `fonts.googleapis.com`, `fonts.gstatic.com`, `og:url` на `thevladoss.github.io`)
    - package.json (блок `scripts`, поле `type`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Copywriting Contract» → «Метаданные index.html» (01-UI-SPEC), «Acceptance Smoke Checklist»
  </read_first>
  <behavior>
    - `node scripts/check-dist.mjs` после `npm run build` выводит по строке `OK <проверка>` на каждую проверку, завершается 0 и печатает итог `OK: N проверок`
    - `node scripts/check-dist.mjs --dist /nonexistent` выводит `FAIL index.html: файл не найден` и завершается 1
    - Проверки: (1) `dist/index.html` существует; (2) содержит `<html lang="ru"`; (3) содержит `<title>Единый голос 27 — Евро-Азиатский дивизион</title>`; (4) содержит `<meta name="description"`, `property="og:title"`, `property="og:url" content="https://thevladoss.github.io/esd-onevoice27/"`; (5) каждый `src`/`href`, ссылающийся на `assets/`, начинается с `/esd-onevoice27/assets/` и соответствующий файл существует в `dist`; (6) нет ссылок, начинающихся с `/assets/` (потерянный base); (7) в `dist/assets` есть файл по маске `vendor-map-*.js`; (8) ни один `*.js` в `dist/assets` не больше 500 × 1024 байт; (9) конкатенация всех `dist/assets/*.js` содержит каждую из строк `"hero"`, `"map"`, `"light-form"`, `"about"`, `"involve"`, `"news"`, `"resources"`, `"quote"` в двойных кавычках (приложение рендерится на клиенте, id секций живут в бандле, а не в `index.html`; наличие в DOM подтверждают `App.test.tsx` и smoke); (10) `<noscript>` присутствует; (11) все `https?://` хосты в `index.html` входят в список `fonts.googleapis.com`, `fonts.gstatic.com`, `thevladoss.github.io`
    - Любая неудача печатает `FAIL <проверка>: <детали>` и в конце завершает процесс кодом 1; остальные проверки при этом всё равно выполняются и печатаются
  </behavior>
  <action>
    Создать `scripts/check-dist.mjs`: ESM, только `node:fs`, `node:path`, `node:process`; разбор аргумента `--dist <path>` (по умолчанию `dist`); функции-проверки возвращают `{ name, ok, detail }`; вывод на русском; никаких зависимостей и сети. Добавить в `package.json` скрипт `"check:dist": "node scripts/check-dist.mjs"`. Файл не трогает `dist`, только читает.

    Сначала запустить скрипт на несуществующей папке (RED: ожидаем код 1 и `FAIL`), затем на реальном `dist` после `npm run build` (GREEN: код 0).
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && node scripts/check-dist.mjs && ! node scripts/check-dist.mjs --dist /nonexistent-dist-05-06 >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/check-dist.mjs` завершается 0; `wc -l < scripts/check-dist.mjs` не меньше 60
    - `grep -c '"check:dist": "node scripts/check-dist.mjs"' package.json` выводит 1
    - `grep -E "from ['\"]" scripts/check-dist.mjs | grep -vc "node:"` выводит 0 (только встроенные модули)
    - `grep -c "/esd-onevoice27/assets/" scripts/check-dist.mjs` не меньше 1; `grep -c "vendor-map" scripts/check-dist.mjs` не меньше 1; `grep -c "light-form" scripts/check-dist.mjs` не меньше 1; `grep -c "fonts.gstatic.com" scripts/check-dist.mjs` не меньше 1; `grep -c "512000\|500 \* 1024" scripts/check-dist.mjs` не меньше 1
    - После `npm run build`: `node scripts/check-dist.mjs` завершается 0 и вывод содержит не меньше 11 строк, начинающихся с `OK `
    - `node scripts/check-dist.mjs --dist /nonexistent-dist-05-06; echo $?` выводит 1 и строку `FAIL`
  </acceptance_criteria>
  <done>Одна команда подтверждает корректность `dist` под base path GitHub Pages и состав бандла.</done>
</task>

<task type="auto">
  <name>Task 3: README «Проверка» и приёмочный чеклист docs/qa/SMOKE.md</name>
  <files>README.md, docs/qa/SMOKE.md</files>
  <read_first>
    - README.md (структура из фазы 1: что это, как запустить, деплой)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Acceptance Smoke Checklist» (таблицы Playwright: preview, скриншоты, прод), «Reduced Motion Policy» → «Проверка», «Responsive Checklist» → «Правила измерения», «Color» → «Контраст: что фаза 5 обязана измерить», «Взаимодействие reveal со стеклом» (пункт 2 про `transform: none`)
    - .planning/phases/05-polish-and-release/05-RESEARCH.md, «Smoke через Playwright MCP»
    - .planning/phases/05-polish-and-release/05-CONTEXT.md, «Финальный smoke и деплой (QA-04)»
  </read_first>
  <action>
    README.md: добавить раздел `## Проверка` с подпунктами: тесты (`npm test`, ожидание: зелёный, без предупреждений `act`), сборка (`npm run build`, ожидание: без предупреждений о чанках, чанк `vendor-map`), проверка dist (`npm run check:dist`), локальный просмотр (`npx vite preview --port 4173 --strictPort`, адрес `http://localhost:4173/esd-onevoice27/`), браузерный smoke по чеклисту `docs/qa/SMOKE.md` (через Playwright MCP или вручную в DevTools), деплой (пуш в `main`, `gh run watch`, `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1`). Текст на русском, команды дословно.

    `docs/qa/SMOKE.md`: шапка с полями «Дата», «Коммит», «Исполнитель», «Инструмент» (пустые, заполняет 05-07); раздел «Как запустить» (команды выше, `emulateMedia({ reducedMotion: "reduce" })` для второго прогона); таблицы проверок с колонками «Проверка», «Критерий», «Preview», «Прод»: (1) Секции: `#hero #map #light-form #about #involve #news #resources #quote` присутствуют и видимы, плюс `header` и `footer`; (2) Консоль: 0 сообщений уровня `error`, 0 необработанных исключений; (3) Сеть: 0 ответов 404 и 5xx, включая шрифты и превью YouTube; внешние хосты только `fonts.googleapis.com`, `fonts.gstatic.com`, `img.youtube.com`, `youtube-nocookie.com`; (4) Горизонтальный скролл: `document.documentElement.scrollWidth <= window.innerWidth + 1` на 320, 390, 768, 1024, 1440 до и после прокрутки до конца; (5) Reveal: после прокрутки до низа у каждой обёртки `getComputedStyle(el).opacity === "1"` и `transform === "none"`; (6) Reduced motion: секции видны, `opacity` обёрток 1, высота документа отличается от обычного прогона не больше чем на 2%, `animationName === "none"` у элементов `[data-anim]`; (7) Якоря: клик по четырём пунктам меню приводит верх целевого `<h2>` ниже нижней грани пилюли минимум на 16px; (8) Фокус: Tab от начала страницы, первый стоп skip-link, кольцо видно на каждом шаге, порядок совпадает с DOM; (9) Мобильное меню на 390: бургер открывает, Esc закрывает, фокус возвращается; (10) FPS: сэмпл при прокрутке на 1440, ориентир ≥ 45, информативно; (11) Контраст: пары из таблицы UI-SPEC с ожидаемыми значениями (paper/.8 на стекле ≥ 4.5, paper/.72 на карточках ≥ 4.5, дата/заголовок новости поверх обложки ≥ 4.5, текст цитаты ≥ 4.5, `#fca5a5` на поле ≥ 4.5, кольцо `#aad9dc` ≥ 3); раздел «Скриншоты» со списком `docs/qa/final-desktop.jpeg` (1440×900), `docs/qa/final-mobile.jpeg` (390×844), `docs/qa/final-full.jpeg` (1440, fullPage), `docs/qa/final-reduced-motion.jpeg` (1440×900, reduce) и заметкой о сравнении `final-full.jpeg` с `docs/research/orig-full.jpeg`; раздел «Прод»: `<title>` совпадает с preview, число секций 8, консоль чистая, 0 ответов 404, список ассетов `/esd-onevoice27/assets/*` совпадает с `dist/index.html`; раздел «Итог» с полем «Вердикт» (пусто). Файл `scripts/smoke.md` из CONTEXT не создаётся: единый документ живёт в `docs/qa/SMOKE.md` по UI-SPEC.
  </action>
  <verify>
    <automated>test -f docs/qa/SMOKE.md && test "$(grep -c '^## Проверка' README.md)" = "1" && test "$(grep -cE '#(hero|map|light-form|about|involve|news|resources|quote)\b' docs/qa/SMOKE.md)" -ge 1 && test "$(grep -c 'final-reduced-motion.jpeg' docs/qa/SMOKE.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Проверка" README.md` выводит 1; `grep -c "npm run check:dist" README.md` не меньше 1; `grep -c "docs/qa/SMOKE.md" README.md` не меньше 1; `grep -c "vite preview --port 4173" README.md` не меньше 1; `grep -c "https://thevladoss.github.io/esd-onevoice27/" README.md` не меньше 1
    - `test -f docs/qa/SMOKE.md` завершается 0
    - Для каждого id из `hero map light-form about involve news resources quote` команда `grep -c "#<id>" docs/qa/SMOKE.md` выводит не меньше 1
    - `grep -c "http://localhost:4173/esd-onevoice27/" docs/qa/SMOKE.md` не меньше 1; `grep -c "https://thevladoss.github.io/esd-onevoice27/" docs/qa/SMOKE.md` не меньше 1
    - `grep -c 'reducedMotion: "reduce"' docs/qa/SMOKE.md` не меньше 1; `grep -c "scrollWidth" docs/qa/SMOKE.md` не меньше 1; `grep -cE "\b320\b" docs/qa/SMOKE.md` не меньше 1; `grep -cE "\b1440\b" docs/qa/SMOKE.md` не меньше 1
    - Для каждого файла из `final-desktop.jpeg final-mobile.jpeg final-full.jpeg final-reduced-motion.jpeg` команда `grep -c "<file>" docs/qa/SMOKE.md` выводит не меньше 1
    - `grep -c "transform" docs/qa/SMOKE.md` не меньше 1; `grep -c "Контраст" docs/qa/SMOKE.md` не меньше 1; `grep -c "Вердикт" docs/qa/SMOKE.md` не меньше 1
    - `test ! -e scripts/smoke.md` завершается 0
  </acceptance_criteria>
  <done>README ведёт разработчика по проверке, чеклист smoke готов к заполнению планом 05-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Исходники → бандл → консоль посетителя | `console.*` из кода утекает в DevTools любого посетителя |
| `dist` → GitHub Pages | Неверный base path даёт 404 на ассеты и белый экран |
| Сборка → npm | Новые пакеты не добавляются |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure | `console.*` в продакшн-коде | mitigate | Grep-гейт нулевых вхождений в `src/**` вне тестов (задача 1), не `dropConsole` |
| T-05-14 | Denial of Service | Потерянный base path, 404 на ассеты | mitigate | `check-dist.mjs` проверяет префикс `/esd-onevoice27/assets/` и существование файлов (задача 2) |
| T-05-15 | Tampering | Лишние внешние хосты в `index.html` | mitigate | Белый список хостов в `check-dist.mjs` (проверка 11) |
| T-05-SC | Tampering | npm installs | mitigate | Установок нет; Playwright как зависимость не добавляется; `git diff -- package.json` меняет только `scripts` |
</threat_model>

<verification>
- `npm run build` без `larger than`, `deprecated`, `(!)`; `vendor-map-*.js` один
- `grep console.*` по `src/**` вне тестов даёт 0
- `npm run check:dist` завершается 0 с не меньше 11 `OK`
- README и SMOKE.md содержат все обязательные разделы
</verification>

<success_criteria>
- MOTION-04 на уровне исходников и сборки закрыт: чистый код, чистая сборка, проверяемый `dist`
- Инструменты QA-04 готовы: команда `check:dist` и чеклист `docs/qa/SMOKE.md` для 05-07
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-06-SUMMARY.md` when done
</output>
