---
phase: 05-polish-and-release
plan: 07
type: execute
wave: 4
depends_on: ["05-05", "05-06"]
files_modified:
  - docs/qa/SMOKE.md
  - docs/qa/final-desktop.jpeg
  - docs/qa/final-mobile.jpeg
  - docs/qa/final-full.jpeg
  - docs/qa/final-reduced-motion.jpeg
autonomous: false
requirements: [QA-04, MOTION-04]

must_haves:
  truths:
    - "main запушен, GitHub Actions прогнал npm ci, npm test, npm run build и деплой; https://thevladoss.github.io/esd-onevoice27/ отвечает 200 с тем же title и тем же списком ассетов, что локальный dist"
    - "Playwright smoke по preview и по проду на 1440×900 и 390×844 подтверждает восемь секций, header и footer, 0 ошибок консоли, 0 ответов 404/5xx, отсутствие горизонтального скролла на 320/390/768/1024/1440, opacity 1 и transform none у обёрток reveal после прокрутки, работу reduced motion"
    - "Четыре скриншота лежат в docs/qa, результаты прогона записаны в docs/qa/SMOKE.md с датой и коммитом"
  artifacts:
    - path: "docs/qa/SMOKE.md"
      provides: "Заполненные результаты smoke для preview и прода"
      contains: "Вердикт"
    - path: "docs/qa/final-full.jpeg"
      provides: "Полный скриншот 1440 для сравнения с docs/research/orig-full.jpeg"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "GitHub Pages"
      via: "actions/deploy-pages@v4 с permissions contents: read, pages: write, id-token: write"
      pattern: "id-token: write"
    - from: "dist/index.html"
      to: "https://thevladoss.github.io/esd-onevoice27/"
      via: "одинаковый список /esd-onevoice27/assets/*"
      pattern: "/esd-onevoice27/assets/"
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Срез: после задачи 1 посетитель открывает финальную версию по публичному адресу; после задачи 2 приёмка подтверждена браузером и зафиксирована в репозитории.

<objective>
Финальный деплой и приёмочный smoke: предполётные проверки, пуш `main`, ожидание зелёного workflow, сверка прода с локальной сборкой, затем браузерный прогон по `docs/qa/SMOKE.md` через Playwright MCP (выполняет оркестратор) со скриншотами и записью результатов.

Purpose: закрыть QA-04 и подтвердить MOTION-04 в реальном браузере (чистая консоль и отсутствие 404 на собранном билде и на проде).
Output: зелёный деплой, заполненный `docs/qa/SMOKE.md`, четыре скриншота в `docs/qa/`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/05-polish-and-release/05-RESEARCH.md
@.planning/research/PITFALLS.md

Браузер доступен только оркестратору через Playwright MCP; исполнитель плана делает всё, что можно сделать из CLI (задача 1), и передаёт браузерную часть чекпоинтом (задача 2). Найденные в smoke дефекты в этом плане не исправляются: они фиксируются в `docs/qa/SMOKE.md` и уходят в verifier фазы как gaps. Файлы `src/**` этот план не меняет.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Предполётные проверки, деплой, сверка прода с локальной сборкой</name>
  <files>docs/qa/SMOKE.md</files>
  <read_first>
    - .github/workflows/deploy.yml (`permissions`, `concurrency`, шаги `npm ci`, `npm test`, `npm run build`, `upload-pages-artifact`, `deploy-pages`)
    - docs/qa/SMOKE.md (шапка: поля «Дата», «Коммит»; раздел «Прод»)
    - .planning/phases/01-scaffold-and-deploy/01-RESEARCH.md, раздел «GitHub Pages» (команды `gh run list`, `gh run watch`, `curl`)
    - .planning/research/PITFALLS.md, «Pitfall 2» (base path) и «Pitfall 5» (права deploy-pages)
  </read_first>
  <action>
    Предполёт: рабочее дерево чистое (`git status --porcelain` пуст; иначе закоммитить остатки предыдущих планов); `npm test`, `npm run build`, `npm run check:dist` завершаются 0. Права workflow минимальны: блок `permissions` в `.github/workflows/deploy.yml` содержит ровно `contents: read`, `pages: write`, `id-token: write`, без `write-all` и без `contents: write`; `concurrency.group` равен `pages`. При расхождении привести к этому виду и закоммитить (`ci: минимальные права деплоя`).

    Деплой: `git push origin main`; найти запуск по коммиту: `gh run list --commit "$(git rev-parse HEAD)" --json databaseId --jq '.[0].databaseId'` (повторять до 12 раз с паузой 5 с, пока id не появится); `gh run watch <id> --exit-status` завершается 0.

    Сверка прода: `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` содержит `200`; `curl -s https://thevladoss.github.io/esd-onevoice27/ | grep -c "<title>Единый голос 27 — Евро-Азиатский дивизион</title>"` равен 1; список ассетов прода `curl -s <прод> | grep -oE '/esd-onevoice27/assets/[^"]+' | sort -u` побайтно совпадает с `grep -oE '/esd-onevoice27/assets/[^"]+' dist/index.html | sort -u` (`diff` пуст; при расхождении подождать 60 с и повторить до 5 раз: кэш Pages). Каждый ассет из списка отвечает 200 по `curl -sI`.

    Записать в шапку `docs/qa/SMOKE.md` дату, короткий sha (`git rev-parse --short HEAD`), url запуска workflow (`gh run view <id> --json url --jq .url`) и результаты сверки в разделе «Прод» (title, ассеты, коды ответов). Закоммитить `docs(05): deploy verified`.
  </action>
  <verify>
    <automated>curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1 | grep -q " 200" && diff <(curl -s https://thevladoss.github.io/esd-onevoice27/ | grep -oE '/esd-onevoice27/assets/[^"]+' | sort -u) <(grep -oE '/esd-onevoice27/assets/[^"]+' dist/index.html | sort -u)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*(contents: read|pages: write|id-token: write)$" .github/workflows/deploy.yml` выводит 3; `grep -cE "write-all|contents: write" .github/workflows/deploy.yml` выводит 0; `grep -c "group: pages" .github/workflows/deploy.yml` выводит 1
    - `npm test`, `npm run build`, `npm run check:dist` завершаются 0
    - `git status --porcelain | wc -l` выводит 0 после пуша; `git rev-parse HEAD` равен `git rev-parse origin/main`
    - `gh run watch <id> --exit-status` завершается 0; `gh run view <id> --json conclusion --jq .conclusion` выводит `success`
    - `curl -sI https://thevladoss.github.io/esd-onevoice27/ | head -1` содержит `200`
    - `curl -s https://thevladoss.github.io/esd-onevoice27/ | grep -c "<title>Единый голос 27 — Евро-Азиатский дивизион</title>"` выводит 1
    - `diff` списков ассетов прода и `dist/index.html` пуст; каждый ассет из списка отвечает 200
    - `grep -c "$(git rev-parse --short HEAD)" docs/qa/SMOKE.md` не меньше 1
  </acceptance_criteria>
  <done>Прод отдаёт ту же сборку, что локальный dist, workflow зелёный, шапка SMOKE.md заполнена.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Браузерный smoke по preview и проду через Playwright MCP (выполняется оркестратором)</name>
  <files>docs/qa/SMOKE.md, docs/qa/final-desktop.jpeg, docs/qa/final-mobile.jpeg, docs/qa/final-full.jpeg, docs/qa/final-reduced-motion.jpeg</files>
  <read_first>
    - docs/qa/SMOKE.md (чеклист с критериями и пустыми колонками «Preview» и «Прод»)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Acceptance Smoke Checklist», «Reduced Motion Policy» → «Проверка», «Responsive Checklist» → «Правила измерения»
    - .planning/phases/05-polish-and-release/05-RESEARCH.md, «Smoke через Playwright MCP» (куда Playwright MCP пишет скриншоты)
    - docs/research/orig-full.jpeg (эталон для сравнения ритма секций)
  </read_first>
  <action>
    Браузерная часть приёмки. Исполнитель плана запускает `npx vite preview --port 4173 --strictPort` в фоне и передаёт управление оркестратору: браузер доступен только через Playwright MCP оркестратора, Playwright в зависимости проекта не добавляется. Оркестратор проходит шаги из `how-to-verify` ниже по чеклисту `docs/qa/SMOKE.md`, переносит скриншоты из `.playwright-mcp/` в `docs/qa/` под четырьмя именами из `files`, заполняет колонки «Preview» и «Прод», поле «Инструмент: Playwright MCP», поле «Вердикт», коммитит `docs(05): smoke results and screenshots` и пушит `main`. Дефекты записываются в SMOKE.md шагами воспроизведения и ожидаемым результатом и уходят в verifier фазы как gaps; код в этом плане не правится.
  </action>
  <what-built>Финальная сборка лендинга задеплоена на https://thevladoss.github.io/esd-onevoice27/ и совпадает с локальным `dist`. Все автоматические гейты фазы пройдены: `npm test`, `npm run build`, `npm run check:dist`, зелёный workflow. Осталась браузерная часть приёмки, для которой у исполнителя нет Playwright; её выполняет оркестратор через Playwright MCP по чеклисту `docs/qa/SMOKE.md`.</what-built>
  <how-to-verify>
    1. В корне проекта запустить в фоне `npx vite preview --port 4173 --strictPort` (сборка `dist` уже актуальна после задачи 1); адрес `http://localhost:4173/esd-onevoice27/`.
    2. Playwright MCP, viewport 1440×900, открыть preview. Подписаться на консоль и ответы сети. Ожидаемо: `page.locator("#hero, #map, #light-form, #about, #involve, #news, #resources, #quote").count()` равен 8, `header` и `footer` в DOM, 0 сообщений уровня `error`, 0 ответов со статусом 404 и 5xx, внешние хосты только `fonts.googleapis.com`, `fonts.gstatic.com`, `img.youtube.com`, `youtube-nocookie.com`.
    3. Прокрутить страницу до низа с шагом 600px. Ожидаемо: у каждой обёртки reveal (элементы с inline `opacity`, вложенные в секции) `getComputedStyle(el).opacity === "1"` и `transform === "none"`; `document.documentElement.scrollWidth <= window.innerWidth + 1` до и после прокрутки. Снять `docs/qa/final-full.jpeg` (`fullPage: true`) и `docs/qa/final-desktop.jpeg` (первый экран).
    4. Повторить пункт 3 на 1024×900, 768×1024, 390×844 и 320×844: горизонтального скролла нет ни на одной ширине; на 390 счётчики стоят над картой, форма в одну колонку, новости в одну колонку, видео-сетка в две. На 390 снять `docs/qa/final-mobile.jpeg` (первый экран). На 390 открыть бургер, нажать Esc: меню закрылось, фокус на бургере.
    5. На 1440 кликнуть по четырём пунктам меню: верх целевого `<h2>` ниже нижней грани пилюли header минимум на 16px. Пройти Tab от начала страницы: первый стоп skip-link «Перейти к содержимому», кольцо фокуса видно на каждом стопе, порядок совпадает с DOM. Сэмпл FPS при прокрутке (информативно, ориентир ≥ 45).
    6. `page.emulateMedia({ reducedMotion: "reduce" })`, перезагрузить preview на 1440×900. Ожидаемо: все восемь секций видны без прокрутки-анимаций (`opacity` обёрток 1), у элементов `[data-anim]` `getComputedStyle(el).animationName === "none"`, высота документа отличается от прогона без reduce не больше чем на 2%. Снять `docs/qa/final-reduced-motion.jpeg`.
    7. Открыть https://thevladoss.github.io/esd-onevoice27/ на 1440×900 и 390×844: тот же `<title>`, 8 секций, 0 ошибок консоли, 0 ответов 404/5xx.
    8. Перенести скриншоты из `.playwright-mcp/` в `docs/qa/` под именами из пунктов 3, 4, 6. Заполнить колонки «Preview» и «Прод» в `docs/qa/SMOKE.md`, поле «Вердикт» и «Инструмент: Playwright MCP». Сравнить `docs/qa/final-full.jpeg` с `docs/research/orig-full.jpeg` по ритму секций, скосам, стеклу и градиентам; заметку записать в SMOKE.md. Закоммитить `docs(05): smoke results and screenshots` и запушить `main`.
    9. Каждый провал записать в SMOKE.md с шагом воспроизведения и ожидаемым результатом: он уходит в verifier фазы как gap.
  </how-to-verify>
  <verify>
    <automated>test -f docs/qa/final-desktop.jpeg && test -f docs/qa/final-mobile.jpeg && test -f docs/qa/final-full.jpeg && test -f docs/qa/final-reduced-motion.jpeg && grep -q "Playwright MCP" docs/qa/SMOKE.md && test "$(git status --porcelain docs/qa | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Для каждого файла из `final-desktop.jpeg final-mobile.jpeg final-full.jpeg final-reduced-motion.jpeg` команда `test -s docs/qa/<file>` завершается 0
    - `grep -c "Playwright MCP" docs/qa/SMOKE.md` не меньше 1; `grep -c "Вердикт" docs/qa/SMOKE.md` не меньше 1; строка «Вердикт» содержит непустое значение
    - В `docs/qa/SMOKE.md` заполнены колонки «Preview» и «Прод» для строк «Секции», «Консоль», «Сеть», «Горизонтальный скролл», «Reveal», «Reduced motion», «Якоря», «Фокус»
    - `git status --porcelain docs/qa | wc -l` выводит 0; `git log -1 --format=%s` содержит `smoke`; `git rev-parse HEAD` равен `git rev-parse origin/main`
    - `ls .playwright-mcp 2>/dev/null | grep -c jpeg` выводит 0 (скриншоты перенесены, папка в .gitignore)
  </acceptance_criteria>
  <resume-signal>Напишите «approved», если все пункты 2–7 прошли и файлы из пункта 8 закоммичены; иначе перечислите провалившиеся пункты.</resume-signal>
  <done>Четыре скриншота и заполненный SMOKE.md закоммичены, прод и preview прошли чеклист без ошибок консоли и 404.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → GitHub Pages | Деплой через OIDC-токен (`id-token: write`), без долгоживущих секретов |
| Локальная машина → origin | Пуш в `main` из авторизованного `gh`/git |
| Браузер приёмки → прод и внешние хосты | Smoke фиксирует фактический список внешних доменов |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Elevation of Privilege | Права workflow deploy | mitigate | Grep-гейт: ровно `contents: read`, `pages: write`, `id-token: write`; без `write-all` (задача 1) |
| T-05-17 | Tampering | Расхождение прода и локального dist (кэш, чужой артефакт) | mitigate | Побайтная сверка списка ассетов и title, проверка 200 на каждый ассет (задача 1) |
| T-05-18 | Information Disclosure | Ошибки консоли и 404 на проде | mitigate | Smoke считает `console.error` и ответы ≥ 400 на preview и на проде (задача 2) |
| T-05-19 | Spoofing | Неожиданные внешние домены на странице | mitigate | Smoke сверяет хосты сети с белым списком из четырёх доменов |
| T-05-SC | Tampering | npm installs | mitigate | Установок нет; Playwright не добавляется в зависимости, браузер даёт оркестратор |
</threat_model>

<verification>
- Workflow зелёный, прод отвечает 200, title и ассеты совпадают с `dist/index.html`
- Smoke по preview и проду на 1440 и 390 без ошибок консоли и без 404; горизонтального скролла нет на 320/390/768/1024/1440
- Четыре скриншота в `docs/qa/`, `docs/qa/SMOKE.md` заполнен и закоммичен
</verification>

<success_criteria>
- QA-04 закрыт: сайт доступен по прод-URL, совпадает с локальным preview, Playwright smoke подтвердил секции и чистую консоль на 1440 и 390
- MOTION-04 подтверждён в браузере: 0 ошибок консоли, 0 ответов 404 на собранном билде и на проде
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-07-SUMMARY.md` when done
</output>
