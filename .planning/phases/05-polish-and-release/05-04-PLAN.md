---
phase: 05-polish-and-release
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/about/VideoEmbed.tsx
  - src/components/about/VideoEmbed.test.tsx
  - src/components/resources/VideoFacade.tsx
  - src/components/resources/VideoFacade.test.tsx
  - src/components/resources/Resources.tsx
  - src/components/resources/ResourceCard.tsx
  - src/components/resources/ResourcePanel.tsx
  - src/components/news/News.tsx
  - src/components/news/NewsCard.tsx
  - src/components/news/NewsPagination.tsx
  - src/components/quote/Quote.tsx
autonomous: true
requirements: [MOTION-02, MOTION-03]

must_haves:
  truths:
    - "Один компонент видео-фасада на проект: components/about/VideoEmbed.tsx; VideoFacade удалён, панель «Видео» и блок «О проекте» рендерят один и тот же компонент с кнопкой «Смотреть видео: {название}» и iframe youtube-nocookie по клику"
    - "Секции news, resources, quote несут aria-labelledby на news-title, resources-title, quote-title; у quote есть h2 (визуально скрытый, если дизайн его не показывает)"
    - "Карточки ресурсов: button с aria-expanded и aria-controls; панель получает фокус при открытии и закрывается по Esc; кнопка «Свернуть» с aria-label; частицы и слой атмосферы несут data-anim particles и atmosphere, итого девять значений data-anim в проекте"
    - "Новости: вся карточка ссылка с rel=noopener noreferrer и кольцом фокуса внутри обрезанного контейнера; пагинация в nav с aria-label, текущая страница aria-current=page, стрелка с aria-label; обложки object-fit cover 4:5 не шире контейнера"
    - "На 390px новости в одну колонку, ресурсы стеком «текст, три карточки», сетка видео в две колонки, цитата не выходит за край; секции resources и quote держат overflow hidden"
  artifacts:
    - path: "src/components/about/VideoEmbed.tsx"
      provides: "Единый видео-фасад с aria-label и youtube-nocookie"
      contains: "youtube-nocookie.com"
    - path: "src/components/resources/Resources.tsx"
      provides: "data-anim particles/atmosphere, aria-labelledby resources-title"
      contains: "data-anim=\"particles\""
    - path: "src/components/quote/Quote.tsx"
      provides: "aria-labelledby quote-title, figure/blockquote/figcaption/cite"
      contains: "quote-title"
  key_links:
    - from: "src/components/resources/ResourcePanel.tsx"
      to: "src/components/about/VideoEmbed.tsx"
      via: "import VideoEmbed"
      pattern: "about/VideoEmbed"
    - from: "src/components/resources/ResourceCard.tsx"
      to: "ResourcePanel id"
      via: "aria-controls"
      pattern: "aria-controls"
    - from: "src/styles/global.css блок reduce"
      to: "src/components/resources/*.tsx"
      via: "[data-anim=\"particles\"] { opacity: .28 }"
      pattern: "data-anim=\"particles\""
---

## Phase Goal

Цель фазы из ROADMAP.md (дословно): «Секции лендинга плавно появляются при скролле, страница безупречно работает на всех целевых ширинах без ошибок в консоли, а финальный деплой подтверждён Playwright smoke-тестами».

Строка `**Goal:**` в ROADMAP.md не приведена к формату user story; историю не придумываем, для переформатирования запустите `/bm:mvp-phase 5`. Срезы: после задачи 1 посетитель запускает видео одинаково в двух местах страницы и скринридер называет ролик по имени; после задачи 2 новости и цитата читаемы и проходимы с клавиатуры на 390px; после задачи 3 панели ресурсов управляются с клавиатуры, а частицы замирают при reduce.

<objective>
Свести два видео-фасада к одному `VideoEmbed`, довести секции news, resources, quote до «Accessibility Contract» и «Responsive Checklist», проставить `data-anim="particles"` и `data-anim="atmosphere"` (последние два значения контракта reduced motion).

Purpose: закрыть MOTION-03 для трёх секций и завершить MOTION-02 на уровне разметки (девять значений `data-anim`).
Output: удалённый `VideoFacade.tsx`, правки атрибутов и классов в `components/{news,resources,quote}`, `VideoEmbed.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release/05-CONTEXT.md
@.planning/phases/05-polish-and-release/05-UI-SPEC.md
@.planning/phases/04-news-resources-quote/04-CONTEXT.md
@.planning/research/PITFALLS.md

В этой волне план 05-03 владеет `src/components/{hero,map,form,involve}/**`, `src/components/about/About.tsx`, `src/components/about/StepCard.tsx`, `src/components/layout/Section.tsx` и `src/data/copy.ts`. Здесь их не трогать: новые aria-строки объявлять константами в файле компонента (отступление от паттерна «тексты в data/copy.ts» ради непересечения файлов; записать в SUMMARY). `global.css` принадлежит 05-02 (выполнен): правки адаптива только классами в файлах секций. Обёртки `Reveal` из 05-01 не переставлять.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Один видео-фасад: слияние VideoFacade в VideoEmbed и его aria-контракт</name>
  <files>src/components/about/VideoEmbed.tsx, src/components/about/VideoEmbed.test.tsx, src/components/resources/VideoFacade.tsx, src/components/resources/VideoFacade.test.tsx, src/components/resources/ResourcePanel.tsx</files>
  <read_first>
    - src/components/about/VideoEmbed.tsx (пропы `videoId`, `title`, разметка кнопки, `img` постера, `iframe`)
    - src/components/resources/VideoFacade.tsx, если существует (отличия API: подпись, размеры, классы), и все места импорта: `grep -rn "VideoFacade" src`
    - src/components/resources/ResourcePanel.tsx (сетка 16 роликов, подпись под фасадом)
    - src/components/about/VideoEmbed.test.tsx и src/components/resources/VideoFacade.test.tsx, если существуют
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «aria по элементам» → «Видео-фасад»; .planning/phases/04-news-resources-quote/04-CONTEXT.md, подраздел «Видео»
  </read_first>
  <action>
    Если `src/components/resources/VideoFacade.tsx` существует: перенести недостающие возможности (например, проп `caption` или компактный размер кнопки play) в `VideoEmbed` как необязательные пропы, переключить все импорты на `components/about/VideoEmbed`, удалить `VideoFacade.tsx` и его тест, а уникальные проверки из него перенести в `VideoEmbed.test.tsx`. Если `VideoFacade.tsx` отсутствует, задача сводится к аудиту `VideoEmbed`.

    Контракт `VideoEmbed`: корневой элемент фасада `<button type="button" aria-label={`Смотреть видео: ${title}`}>` (aria-label формируется из пропа `title`), постер `https://img.youtube.com/vi/${videoId}/hqdefault.jpg` с `alt=""` (подпись дублирует название) и `loading="lazy"`, `object-fit: cover`, контейнер `aspect-ratio: 16 / 9` и `max-width: 100%`; по клику монтируется `<iframe src="https://www.youtube-nocookie.com/embed/${videoId}?autoplay=1&rel=0" title={title} allow="autoplay; encrypted-media; picture-in-picture" allowFullScreen>`; фокус после клика остаётся внутри контейнера (iframe получает фокус или контейнер `tabIndex={-1}`); `console.*` в компоненте нет.

    `VideoEmbed.test.tsx` (создать, если нет): (1) рендерит кнопку с именем «Смотреть видео: Единый голос 27» при `title="Единый голос 27"`; (2) до клика `iframe` в DOM нет; (3) после клика `iframe` появляется, `src` начинается с `https://www.youtube-nocookie.com/embed/YpLD6p-z00g`, `title` равен пропу.
  </action>
  <verify>
    <automated>npm test && test ! -e src/components/resources/VideoFacade.tsx && test "$(grep -rl "VideoFacade" src | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e src/components/resources/VideoFacade.tsx && test ! -e src/components/resources/VideoFacade.test.tsx` завершается 0
    - `grep -rl "VideoFacade" src | wc -l` выводит 0
    - `grep -rl "about/VideoEmbed" src/components/resources | wc -l` не меньше 1
    - `grep -c "Смотреть видео: " src/components/about/VideoEmbed.tsx` не меньше 1; `grep -c "youtube-nocookie.com/embed/" src/components/about/VideoEmbed.tsx` не меньше 1; `grep -c "img.youtube.com/vi/" src/components/about/VideoEmbed.tsx` не меньше 1; `grep -c "allowFullScreen" src/components/about/VideoEmbed.tsx` не меньше 1
    - `grep -cE '<button' src/components/about/VideoEmbed.tsx` не меньше 1
    - `test -f src/components/about/VideoEmbed.test.tsx` завершается 0; `npx vitest run src/components/about/VideoEmbed.test.tsx` завершается 0 и показывает не меньше 3 passed
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Единственный видео-фасад с полным aria-контрактом, тесты на него зелёные, упоминаний VideoFacade в src нет.</done>
</task>

<task type="auto">
  <name>Task 2: Новости и цитата: aria, фокус в обрезанных карточках, контраст, раскладка 390px</name>
  <files>src/components/news/News.tsx, src/components/news/NewsCard.tsx, src/components/news/NewsPagination.tsx, src/components/quote/Quote.tsx</files>
  <read_first>
    - src/components/news/*.tsx (карточка-ссылка: `target`/`rel`, `overflow: hidden`, обложка и скрим, hover-зум; пагинация: обёртка, `aria-current`, `aria-label`, размеры кнопок; заголовок секции и его id)
    - src/components/quote/Quote.tsx (`figure`/`blockquote`/`figcaption`/`cite`, SVG-силуэт карты, непрозрачность текста, заголовок или его отсутствие)
    - src/data/news.ts (поля `title`, `href`, `cover`)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «aria по элементам» (Пагинация новостей, Декоративная графика, Внешние ссылки), «Фокус» (`outline-offset: -3px`), «Color» → «Контраст» (Дата и заголовок новости, Текст цитаты), «Responsive Checklist» → «390 × 844» (Новости, Цитата)
  </read_first>
  <action>
    News: `<section id="news" aria-labelledby="news-title">`, `id="news-title"` на `<h2>`. `NewsCard`: `<article>` со ссылкой на всю карточку, `target="_blank" rel="noopener noreferrer"` в одной строке JSX; обложка `object-fit: cover`, `aspect-ratio: 4 / 5`, `max-width: 100%`, `loading="lazy"`, `alt` с названием новости (или `alt=""` при видимом заголовке рядом); кольцо фокуса внутри обрезанного контейнера через `outline-offset: -3px` (класс `focus-visible:-outline-offset-3` либо локальный стиль) на элементе, который несёт `overflow: hidden`; hover-зум обложки только внутри `@media (hover: hover) and (pointer: fine)`; скрим снизу обязателен, при сомнении в контрасте даты и заголовка поверх самой светлой точки обложки поднять непрозрачность нижней части скрима до .9 (правило UI-SPEC). `NewsPagination`: `<nav aria-label="Страницы новостей">` (уже существующая метка «Пагинация новостей» из фазы 4 тоже принимается, менять её не обязательно), кнопки `type="button"` не меньше 44×44px (`min-w-11 min-h-11`), у активной `aria-current="page"`, у стрелки `aria-label="Следующая страница"` и `disabled` на последней странице. Сетка: одна колонка ниже 768px, две от 768px, три от 1024px; карточки `min-width: 0`.

    Quote: `<section id="quote" aria-labelledby="quote-title">`; если дизайн секции не содержит `<h2>`, добавить визуально скрытый `<h2 id="quote-title" className="sr-only">` с текстом eyebrow «Слово на дорогу» (видимый `Eyebrow` остаётся `<p>`); разметка `<figure><blockquote><p/><p/></blockquote><figcaption><cite>…</cite></figcaption></figure>`; SVG-силуэт карты `aria-hidden="true"` и `pointer-events: none`, обрезан контейнером (`overflow: hidden` на секции-хосте); текст цитаты непрозрачностью 1.0 или не ниже .9 на самой светлой точке градиента (правило контраста UI-SPEC: при провале поднять до 1.0 и притемнить подложку, палитру не менять); на 390px контент `max-w-3xl` с боковыми отступами, текст не уезжает под край.

    Непрозрачность текста в обоих разделах не ниже .72.
  </action>
  <verify>
    <automated>npm test && test "$(grep -rn 'target="_blank"' src/components/news | grep -vc 'noopener noreferrer' | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-labelledby="news-title"' src/components/news/News.tsx` выводит 1; `grep -c 'id="news-title"' src/components/news/News.tsx` выводит 1
    - `grep -c 'aria-labelledby="quote-title"' src/components/quote/Quote.tsx` выводит 1; `grep -c 'id="quote-title"' src/components/quote/Quote.tsx` выводит 1; `grep -cE "<h2" src/components/quote/Quote.tsx` не меньше 1
    - `grep -c "<figure" src/components/quote/Quote.tsx` не меньше 1; `grep -c "<blockquote" src/components/quote/Quote.tsx` не меньше 1; `grep -c "<cite" src/components/quote/Quote.tsx` не меньше 1; `grep -c 'aria-hidden' src/components/quote/Quote.tsx` не меньше 1
    - `grep -rn 'target="_blank"' src/components/news | grep -vc 'noopener noreferrer'` выводит 0
    - `grep -rcE 'aria-label="(Страницы новостей|Пагинация новостей)"' src/components/news src/data/copy.ts | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc 'aria-current="page"' src/components/news | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc "Следующая страница" src/components/news src/data/copy.ts | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "aspect-\[4/5\]|aspect-ratio: ?4 ?/ ?5" src/components/news | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rcE "object-cover|object-fit: ?cover" src/components/news | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "outline-offset-3|outline-offset: ?-3px" src/components/news | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "overflow-hidden|overflow: ?hidden|overflow-clip" src/components/quote/Quote.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rnE "text-paper/([0-6][0-9]?|7[01])\b|248 247 251 / \.?(0?[0-6][0-9]?|7[01])\)" src/components/news src/components/quote | grep -v placeholder | wc -l` выводит 0
    - `npm test` завершается 0
  </acceptance_criteria>
  <done>Новости и цитата именованы, проходимы с клавиатуры с видимым кольцом, обложки не растягиваются, контраст соблюдён по правилам UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Ресурсы: data-anim частиц, aria панелей, фокус и Esc, раскладка</name>
  <files>src/components/resources/Resources.tsx, src/components/resources/ResourceCard.tsx, src/components/resources/ResourcePanel.tsx</files>
  <read_first>
    - src/components/resources/*.tsx (носитель псевдослоёв частиц `resources-particles`, слой атмосферного зума карточки, карточка-кнопка: `aria-expanded`, `aria-controls`; панель: `id`, `tabIndex`, фокус при открытии, Esc; кнопка «Свернуть»; сетка 12 колонок и её мобильный стек; сетка видео)
    - src/data/materials.ts (5 ссылок, внешние)
    - .planning/phases/05-polish-and-release/05-UI-SPEC.md, «Reduced Motion Policy» → «Контракт разметки» (`particles`, `atmosphere`), «aria по элементам» (Карточки ресурсов, Кнопка «Свернуть», Внешние ссылки), «Клавиатура», «Responsive Checklist» → «390 × 844» (Ресурсы), «1024 × 900»
    - .planning/research/FEATURES.md, строки «Атмосферный зум и частицы ресурсов», «Открытие панели ресурсов»
  </read_first>
  <action>
    `data-anim`: элемент, на чьих `::before`/`::after` (или дочерних слоях) живёт `@keyframes resources-particles`, получает `data-anim="particles"`; если частицы разнесены по трём отдельным узлам, атрибут получает каждый узел; слой атмосферного зума карточки получает `data-anim="atmosphere"`. Оба слоя `aria-hidden="true"` и `pointer-events: none`. После этого в проекте ровно девять значений `data-anim`.

    Aria и клавиатура: `<section id="resources" aria-labelledby="resources-title">`, `id="resources-title"` на `<h2>`; каждая карточка `<button type="button" aria-expanded={isActive} aria-controls="resources-panel">` (или уникальный id панели); панель с тем же `id`, `tabIndex={-1}`, получает фокус при открытии (`ref.current.focus()` в эффекте после смены `activePanel`), Esc внутри панели закрывает её и возвращает фокус на активную карточку; кнопка «Свернуть» с `aria-label="Свернуть панель"`; при reduce раскрытие мгновенное (проверить, что `grid-template-rows` переход отключён блоком reduce через `transition-duration: 0.01ms`, дополнительного кода не нужно). Внешние ссылки материалов `target="_blank" rel="noopener noreferrer"` в одной строке JSX; текст ссылки называет, куда она ведёт.

    Раскладка: ниже 768px стек «текст, затем три карточки», панель на всю ширину, сетка видео в две колонки, кнопка «Свернуть» доступна без горизонтального скролла; от 1024px 12-колоночная раскладка, отрицательные отступы карточек не выходят за границу секции; секция-хост `overflow: hidden`; карточки `min-width: 0`; кольцо фокуса на карточках внутри `overflow: hidden` через `outline-offset: -3px`.

    Тест `Resources.test.tsx` фазы 4 остаётся зелёным; новые проверки (Esc, фокус на панели) добавляет план 05-05.
  </action>
  <verify>
    <automated>npm test && npm run build && test "$(grep -rhoE 'data-anim="[a-z-]+"' src | sort -u | wc -l | tr -d ' ')" = "9"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'data-anim="particles"' src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc 'data-anim="atmosphere"' src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rhoE 'data-anim="[a-z-]+"' src | sort -u | wc -l` выводит 9
    - `grep -c 'aria-labelledby="resources-title"' src/components/resources/Resources.tsx` выводит 1; `grep -rc 'id="resources-title"' src/components/resources | awk -F: '{s+=$2} END {print s}'` выводит 1
    - `grep -rc "aria-expanded" src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rc "aria-controls" src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "tabIndex=\{-1\}" src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1; `grep -rcE "Escape" src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rc 'aria-label="Свернуть панель"' src/components/resources | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rn 'target="_blank"' src/components/resources src/data/materials.ts | grep -vc 'noopener noreferrer'` выводит 0
    - `grep -rcE "overflow-hidden|overflow: ?hidden|overflow-clip" src/components/resources/Resources.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `grep -rcE "grid-cols-2" src/components/resources/ResourcePanel.tsx | awk -F: '{s+=$2} END {print s}'` не меньше 1
    - `npm test` завершается 0; `npm run build` завершается 0
  </acceptance_criteria>
  <done>Девять значений `data-anim` в проекте, панели ресурсов управляются с клавиатуры, раскладка стековая на мобильном, тесты и сборка зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Страница → YouTube (`img.youtube.com`, `youtube-nocookie.com`) | Постеры грузятся сразу, iframe только по клику пользователя; третья сторона получает запрос лишь после явного действия |
| Страница → внешние материалы ЕАД, DigitalOcean Spaces, SharePoint | Ссылки в новой вкладке |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Information Disclosure | iframe YouTube | mitigate | Домен `youtube-nocookie.com`, монтирование только по клику, `allow` ограничен `autoplay; encrypted-media; picture-in-picture` |
| T-05-09 | Tampering | Внешние ссылки материалов и новостей (reverse tabnabbing) | mitigate | `rel="noopener noreferrer"` на каждой `target="_blank"`, построчный grep-гейт |
| T-05-10 | Spoofing | Кнопка видео без имени | mitigate | `aria-label` «Смотреть видео: {название}» из данных, не из пользовательского ввода |
| T-05-SC | Tampering | npm installs | mitigate | Установок в плане нет; `git diff -- package.json` без новых зависимостей |
</threat_model>

<verification>
- `VideoFacade` отсутствует, `VideoEmbed` один, тесты на него зелёные
- Три `aria-labelledby` (news-title, resources-title, quote-title), девять значений `data-anim`
- Все `target="_blank"` в трёх секциях с `rel="noopener noreferrer"`
- `npm test` и `npm run build` зелёные
</verification>

<success_criteria>
- «Accessibility Contract» выполнен для news, resources, quote по строкам «aria по элементам» и «Клавиатура»
- «Reduced Motion Policy → Контракт разметки» закрыт: все девять значений присутствуют
- «Responsive Checklist» 390/768/1024 для трёх секций выполнен классами; визуальное подтверждение даёт smoke 05-07
</success_criteria>

<output>
Create `.planning/phases/05-polish-and-release/05-04-SUMMARY.md` when done
</output>
