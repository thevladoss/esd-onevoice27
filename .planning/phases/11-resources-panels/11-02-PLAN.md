---
phase: 11-resources-panels
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/resources/Resources.tsx
  - src/components/resources/ResourceCard.tsx
  - src/components/resources/resources.css
  - src/components/resources/Resources.test.tsx
autonomous: true
requirements: [RES-01, RES-02]
must_haves:
  truths:
    - "На ≥1024px блоки стоят по сетке minmax(0, 320fr) minmax(0, 528fr) minmax(0, 272fr) с gap 16px: текст в центре с пунктирной рамкой, музыка слева, материалы справа у низа первой строки, видео во второй строке под текстом шириной min(69.697%, 368px)"
    - "На <1024px блоки идут колонкой по центру: текст первым, карточки min(100%, 328px) × 248px с gap 24px"
    - "Карточка: строка индикатора с подписью и точкой 16px цвета акцента, контент прижат к низу, действие с подчёркиванием scaleX(.34), на ховер/фокус подчёркивание scaleX(1), карточка поднимается на 4px и получает рамку акцента за 420ms"
    - "Карточка несёт классы glass и glass-resource, собственного фона в resources.css нет; кнопка-слой на всю карточку и aria-атрибуты сохранены"
    - "Reveal-каскад появления (RevealGroup / Reveal / RevealItem) сохранён, атмосфера и частицы с data-anim на месте"
  artifacts:
    - path: src/components/resources/resources.css
      provides: "правила .resources-grid, .resources-copy, .resources-cell--*, .resource-card* по RES-01/RES-02"
      contains: "minmax(0, 320fr) minmax(0, 528fr) minmax(0, 272fr)"
    - path: src/components/resources/ResourceCard.tsx
      provides: "разметка карточки: indicator + dot 16px, content, actions/trigger, overlay button"
      contains: "glass-resource"
    - path: src/components/resources/Resources.tsx
      provides: "сетка секции классами из resources.css вместо Tailwind-классов"
      contains: "resources-grid"
    - path: src/components/resources/Resources.test.tsx
      provides: "тесты структуры карточек, порядка блоков и значений CSS"
      min_lines: 300
  key_links:
    - from: src/components/resources/Resources.tsx
      to: src/components/resources/resources.css
      via: "className resources-grid / resources-copy / resources-cell--music|materials|video на обёртках Reveal"
      pattern: "resources-cell--video"
    - from: src/components/resources/ResourceCard.tsx
      to: src/data/copy.resources.ts
      via: "style --accent из resourcesCopy.cards[kind].accent"
      pattern: "\"--accent\": card\\.accent"
    - from: src/components/resources/resources.css
      to: ".resource-card__trigger::before"
      via: "подчёркивание scaleX(.34) → scaleX(1) на :hover и :has(:focus-visible)"
      pattern: "scaleX\\(\\.34\\)"
---

<objective>
Перевести сетку и карточки секции «Всё, что нужно для старта» на раскладку оригинала: трёхколоночная сетка `320fr / 528fr / 272fr` с пропорциями блоков на ≥1024px, колонка по центру на узких экранах, карточка с индикатором, контентом внизу и подчёркнутым действием, ховер-подъём на 4px. Поверхность карточки отдаётся утилите `glass-resource` фазы 7.

Purpose: пункт 5 правок пользователя («блок другой, изучить размеры и анимации»). После этого плана секция визуально совпадает с оригиналом по размерам и поведению карточек; панели остаются старыми до плана 11-03.
Output: правки `Resources.tsx`, `ResourceCard.tsx`, `resources.css`, `Resources.test.tsx`.
</objective>

<execution_context>
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/thevladoss/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-resources-panels/11-CONTEXT.md
@docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md
@docs/research/v1.1/orig-rules.css
@docs/research/v1.1/orig-resources.jpeg
@src/components/resources/Resources.tsx
@src/components/resources/ResourceCard.tsx
@src/components/resources/resources.css
@src/components/resources/Resources.test.tsx
@src/components/layout/Reveal.tsx
@src/styles/motionPolicy.test.ts

<ownership>
Фаза 11 идёт параллельно с фазами 7–10 и 12 в отдельном worktree. Этот план правит только четыре файла из `files_modified`. Не трогать: `src/styles/global.css` (утилиту `glass-resource` создаёт фаза 7; здесь класс только вешается, правил для него в `resources.css` не писать), `src/components/layout/primitives.css`, `src/components/layout/Header.css`, `src/components/layout/Reveal.tsx`, `src/components/about/VideoEmbed.tsx`, `src/components/about/*`, `src/components/news/*`, `src/data/*` (данные правит план 11-01 в той же волне: `copy.resources.ts` не открывать на запись). Файлы панели (`ResourcePanel.tsx`, `MaterialsList.tsx`, `MusicPlaceholder.tsx`, `VideoGrid.tsx`, `VideoGrid.test.tsx`) в этом плане не менять: их переписывает 11-03.

Reduced motion: в `resources.css` не добавлять `@media (prefers-reduced-motion: reduce)` (тест `src/styles/motionPolicy.test.ts` допускает единственный блок в `global.css`). Переходы карточки гасит глобальное правило `transition-duration: 0.01ms`. Атрибуты `data-anim="particles"` и `data-anim="atmosphere"` сохранить, новых значений `data-anim` не заводить.

Цвета в CSS: литералы `rgb(r g b / a)` из спецификации, токены `--color-*` не использовать.
</ownership>

<interfaces>
<!-- Контракты, против которых пишется план. Использовать напрямую. -->

Из src/components/layout/Reveal.tsx:
```typescript
type RevealBaseProps<T extends RevealTag = "div"> = AriaAttributes & { as?: T; className?: string; children: ReactNode; ref?: Ref<TagElement[T]> };
export function Reveal(props: RevealBaseProps & { delay?: number });   // одиночный блок, motion.div с whileInView
export function RevealGroup(props: RevealBaseProps);                     // контейнер каскада (staggerChildren), сам без transform
export function RevealItem(props: RevealBaseProps);                      // элемент каскада, motion.div с transform во время появления
```
Все три принимают `className` и рендерят его на корневом узле (при reduced motion рендерят обычный Tag с тем же className).

Из src/data/copy.resources.ts (после 11-01 accent становится литералом, форма не меняется):
```typescript
export type ResourceKey = "music" | "materials" | "video";
resourcesCopy.cards[kind]: { label: string; title: string; description: string; cta: string; accent: string }
```

Из src/components/resources/ResourceCard.tsx (сигнатура сохраняется):
```typescript
export function ResourceCard({ kind, isOpen, onToggle, ref }: { kind: ResourceKey; isOpen: boolean; onToggle: () => void; ref?: Ref<HTMLButtonElement> });
```

Из src/styles/global.css: утилита `glass` даёт `border: 1px solid var(--glass-border)`, `border-radius: var(--radius-card)`, `box-shadow: var(--shadow-card)`, backdrop-filter и градиент поверхности. `resources.css` импортируется компонентом и живёт вне слоёв Tailwind, поэтому его правила побеждают утилиты при равной специфичности.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Правила сетки, ячеек и карточки в resources.css</name>
  <files>src/components/resources/resources.css</files>
  <read_first>
    - src/components/resources/resources.css (полностью: подложка, атмосфера, частицы, .resource-card, .resources-panel-wrap)
    - docs/superpowers/specs/2026-09-06-design-fixes-v1.1-design.md, раздел 5: вступление (сетка и padding-block), RES-01, RES-02
    - docs/research/v1.1/orig-rules.css: блоки `#ov-resources-content`, `#ov-resources-copy`, `.ov-resources-card*`, `.ov-resources-panel-trigger`, медиа-блок с `grid-template-columns`
  </read_first>
  <action>
    Оставить без изменений блоки `.resources` (подложка и `is-*-active`), `.resources-atmosphere*`, `@keyframes resources-zoom`, `.resources-particles`, `@keyframes resources-particles`, `.resources-panel-wrap*` и `.resources-panel` (старую панель снимает 11-03). Удалить блок `.resource-card { background: …; backdrop-filter: … }`: поверхность теперь у утилит `glass` и `glass-resource` (GLASS-03, фаза 7). Добавить правила ниже; значения дословно из спецификации, единица измерения медиа-запроса `64rem` как в оригинале.

    Контейнер `.resources-content`: `padding-block: clamp(88px, 10vw, 144px) clamp(88px, 10vw, 152px)`, `display: flex; flex-direction: column; align-items: center; width: 100%`.

    Сетка `.resources-grid` (класс на RevealGroup): мобильный вид `display: flex; flex-direction: column; align-items: center; gap: 24px; width: calc(100% - 32px); max-width: 72rem; margin-inline: auto`. В `@media (min-width: 64rem)`: `display: grid; grid-template-columns: minmax(0, 320fr) minmax(0, 528fr) minmax(0, 272fr); grid-template-rows: auto auto; justify-content: center; align-items: start; column-gap: 16px; row-gap: 16px`.

    Текстовый блок `.resources-copy` (класс на Reveal): мобильный вид `display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 10px; width: min(100%, 528px); margin: 0 0 28px; padding: 0; text-align: center`. В `@media (min-width: 64rem)`: `grid-area: 1 / 2; width: 100%; max-width: 528px; max-height: 523px; aspect-ratio: 528 / 523; margin: 0; padding: 32px; border: 1px dotted rgb(84 164 172 / .25); border-radius: 16px; background: rgb(84 164 172 / .05)`.

    Ячейки карточек `.resources-cell` (класс на RevealItem): мобильный вид `width: min(100%, 328px); height: 248px`. В `@media (min-width: 64rem)` общее `height: auto; width: 100%` и модификаторы: `.resources-cell--music { grid-area: 1 / 1; max-width: 320px; max-height: 296px; aspect-ratio: 320 / 296 }`, `.resources-cell--materials { grid-area: 1 / 3; align-self: end; max-width: 272px; max-height: 336px; aspect-ratio: 272 / 336 }`, `.resources-cell--video { grid-area: 2 / 2; justify-self: start; width: min(69.697%, 368px); max-height: 256px; aspect-ratio: 368 / 256 }`.

    Карточка `.resource-card`: `position: relative; isolation: isolate; display: flex; flex-direction: column; justify-content: space-between; width: 100%; height: 100%; overflow: hidden; padding: 24px; color: rgb(248 247 251 / .94); transform: translateY(0); transition: border-color 420ms ease, box-shadow 420ms ease, transform 420ms cubic-bezier(.22, 1, .36, 1)`; в медиа ≥64rem `padding: 20px`. Состояния `.resource-card:hover, .resource-card:has(:focus-visible), .resource-card[data-open="true"] { border-color: var(--accent); transform: translateY(-4px) }` (фокус ловится через `:has`, потому что кнопка внутри карточки прозрачный слой). Фон и backdrop-filter не задавать.

    Индикатор `.resource-card__indicator`: `display: flex; align-items: center; justify-content: space-between; width: 100%; color: rgb(239 237 245 / .76); font-family: var(--font-body); font-size: 12px; font-weight: 700; line-height: 1.4; letter-spacing: .08em; text-transform: uppercase`. Точка `.resource-card__dot`: `flex: 0 0 16px; width: 16px; height: 16px; margin-left: 16px; border-radius: 50%; background: var(--accent)`; сохранить текущее правило свечения `.resource-card[data-open="true"] .resource-card__dot { box-shadow: 0 0 0 6px color-mix(in oklab, var(--accent) 18%, transparent) }`.

    Контент `.resource-card__content`: `display: flex; flex-direction: column; align-items: flex-start; width: 100%; margin-top: auto; gap: 7px`. Заголовок `.resource-card__title`: `margin: 0; font-family: var(--font-display); font-size: clamp(1.25rem, 6vw, 1.35rem); font-weight: 800; letter-spacing: -0.03em; line-height: 1.05; color: rgb(248 247 251 / .94)`. Описание `.resource-card__description`: `margin: 0; font-family: var(--font-body); font-size: .875rem; line-height: 1.45; color: rgb(239 237 245 / .76)`. Блок действия `.resource-card__actions { margin-top: 12px }`, в медиа ≥64rem `margin-top: 4px`.

    Триггер `.resource-card__trigger`: `position: relative; display: inline-block; padding: 10px 0; font-family: var(--font-body); font-size: .875rem; font-weight: 700; letter-spacing: .08em; text-transform: uppercase; color: rgb(248 247 251 / .94)`. Подчёркивание `.resource-card__trigger::before`: `content: ""; position: absolute; right: 0; bottom: 5px; left: 0; height: 1px; background: linear-gradient(90deg, var(--accent), transparent); transform: scaleX(.34); transform-origin: left center; transition: transform 420ms cubic-bezier(.22, 1, .36, 1)`. Раскрытие `.resource-card:hover .resource-card__trigger::before, .resource-card:has(:focus-visible) .resource-card__trigger::before { transform: scaleX(1) }`. Старые правила `.resource-card__trigger::after` удалить.

    Комментарии в файле на русском: почему поверхность ушла в утилиту, почему размеры лежат на ячейке (RevealItem и есть дочерний элемент сетки), почему нет reduce-блока.
  </action>
  <verify>
    <automated>grep -q "minmax(0, 320fr) minmax(0, 528fr) minmax(0, 272fr)" src/components/resources/resources.css && grep -q "width: min(69.697%, 368px)" src/components/resources/resources.css && ! grep -q "prefers-reduced-motion" src/components/resources/resources.css && awk 'BEGIN{RS="}"} /\.resource-card \{/ && /background:/ {bad=1} END{exit bad}' src/components/resources/resources.css</automated>
  </verify>
  <acceptance_criteria>
    - В `resources.css` есть строки `grid-template-columns: minmax(0, 320fr) minmax(0, 528fr) minmax(0, 272fr)`, `aspect-ratio: 528 / 523`, `aspect-ratio: 320 / 296`, `aspect-ratio: 272 / 336`, `aspect-ratio: 368 / 256`, `width: min(69.697%, 368px)`, `width: min(100%, 328px)`, `height: 248px`, `border: 1px dotted rgb(84 164 172 / .25)`, `background: rgb(84 164 172 / .05)`, `transform: scaleX(.34)`, `transform: translateY(-4px)`, `420ms cubic-bezier(.22, 1, .36, 1)`, `padding-block: clamp(88px, 10vw, 144px) clamp(88px, 10vw, 152px)`
    - Блок `.resource-card {` не содержит `background` и `backdrop-filter`
    - В файле нет `prefers-reduced-motion`, нет `var(--color-`, нет `outline: none`
    - Блоки `.resources-atmosphere`, `.resources-particles`, `.resources-panel-wrap`, `.resources-panel` сохранены без изменений
  </acceptance_criteria>
  <done>CSS секции задаёт сетку, ячейки и карточку по RES-01/RES-02, поверхность карточки отдана утилитам.</done>
</task>

<task type="auto">
  <name>Task 2: Разметка сетки в Resources.tsx и карточки в ResourceCard.tsx</name>
  <files>src/components/resources/Resources.tsx, src/components/resources/ResourceCard.tsx</files>
  <read_first>
    - src/components/resources/Resources.tsx (весь JSX секции, эффекты deep link не трогать)
    - src/components/resources/ResourceCard.tsx (полностью: overlay-кнопка, aria-labelledby, aria-controls)
    - src/components/layout/Reveal.tsx (строки 1–60 и сигнатуры Reveal/RevealGroup/RevealItem)
    - docs/research/v1.1/dom-ov-resources.html (порядок узлов карточки: indicator → content → actions)
  </read_first>
  <action>
    **Resources.tsx.** Заменить обёртку `div.mx-auto.max-w-[72rem].px-4.py-16.md:px-8.md:py-24` на `div.resources-content`. У `RevealGroup` вместо Tailwind-сетки поставить `className="resources-grid"`. У `Reveal` текстового блока оставить только `className="resources-copy"` (убрать `order-first`, рамку, фон, `p-8`, `md:col-*`, `lg:*`); внутри убрать `mt-2` у `GradientTitle` и `mt-4` у абзаца, отступы даёт `gap: 10px`; `<span id="resources-title">` внутри заголовка и `aria-labelledby="resources-title"` секции сохранить. Трём `RevealItem` поставить `className="resources-cell resources-cell--music"`, `"resources-cell resources-cell--materials"`, `"resources-cell resources-cell--video"`; порядок в DOM остаётся copy → music → materials → video (на узких экранах он и есть порядок колонки). Всё остальное (`section#resources` с `is-${active}-active`, атмосфера с `data-anim="atmosphere"`, частицы с `data-anim="particles"`, `cardRefs`, `toggle`, `close`, эффект хэша и делегированного клика, старая обёртка `.resources-panel-wrap` с `ResourcePanel`) не менять: панель переписывает 11-03.

    **ResourceCard.tsx.** Корневой `div`: `className="resource-card glass glass-resource"` плюс `data-open`, `data-kind={kind}` и `style={{ "--accent": card.accent }}` (per RES-02 акцент приходит литералом из `copy.resources.ts`, план 11-01). Все Tailwind-классы поведения (`min-h-64`, `p-6`, `transition-*`, `duration-[420ms]`, `hover:border-*`, `motion-safe:hover:-translate-y-1`, `data-[open=true]:border-*`) убрать: их заменили правила `resources.css`. Структура:
    1. `<p className="resource-card__indicator">` с `<span>{card.label}</span>` и `<span aria-hidden="true" className="resource-card__dot" />` (16px задаёт CSS, Tailwind `h-3 w-3` убрать).
    2. `<div className="resource-card__content">` с `<h3 id={titleId} className="resource-card__title">`, `<p className="resource-card__description">`, `<div className="resource-card__actions"><span className="resource-card__trigger">{card.cta}</span></div>`.
    3. Overlay-кнопка без изменений: `type="button"`, `ref`, `data-kind`, `aria-expanded`, `aria-labelledby={titleId}`, `aria-controls={isOpen ? "resources-panel" : undefined}`, `onClick`, классы `absolute inset-0 rounded-card focus-visible:outline-2 focus-visible:-outline-offset-3 focus-visible:outline-[color:var(--accent)]`.
    Комментарий модуля о модели содержимого `<button>` сохранить.
  </action>
  <verify>
    <automated>npx tsc -b && grep -q 'resource-card glass glass-resource' src/components/resources/ResourceCard.tsx && grep -q 'className="resources-grid"' src/components/resources/Resources.tsx && npx vitest run src/components/resources/Resources.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `Resources.tsx` содержит `resources-content`, `resources-grid`, `resources-copy`, `resources-cell--music`, `resources-cell--materials`, `resources-cell--video`; не содержит `lg:col-start`, `md:col-start`, `max-w-[72rem]`
    - `ResourceCard.tsx` содержит классы `resource-card glass glass-resource`, `resource-card__indicator`, `resource-card__dot`, `resource-card__content`, `resource-card__title`, `resource-card__description`, `resource-card__actions`, `resource-card__trigger`; не содержит `motion-safe:hover:-translate-y-1`, `min-h-64`, `h-3 w-3`
    - Overlay-кнопка сохраняет `aria-expanded`, `aria-labelledby`, `aria-controls` (только при `isOpen`), `data-kind`
    - Существующие тесты `Resources.test.tsx` зелёные без правок (структура кнопок и заголовков не изменилась)
    - `npx tsc -b` без ошибок
  </acceptance_criteria>
  <done>Секция и карточка размечены классами из resources.css, доступность карточки сохранена, старые тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Тесты сетки, карточки и значений CSS</name>
  <files>src/components/resources/Resources.test.tsx</files>
  <read_first>
    - src/components/resources/Resources.test.tsx (хелперы cardButtons, resourcesSection, существующие describe)
    - src/styles/motionPolicy.test.ts (чтение CSS с диска через readFileSync: vitest настроен с `css: false`, и `?raw` отдаёт пустую строку)
    - src/components/layout/Header.test.tsx, строки 700–715 (пример проверок CSS регулярками)
  </read_first>
  <action>
    Добавить в `Resources.test.tsx` describe «сетка и карточки по оригиналу». Константу `RESOURCES_CSS` читать через `readFileSync(resolve(process.cwd(), "src/components/resources/resources.css"), "utf8")`. Сценарии:
    1. «держит порядок блоков copy → music → materials → video на обёртках сетки»: `section.querySelector(".resources-grid")` не null; `Array.from(grid.children).map(el => el.className)` по порядку содержит `resources-copy`, `resources-cell--music`, `resources-cell--materials`, `resources-cell--video`; у каждой ячейки есть класс `resources-cell`.
    2. «размечает карточку: индикатор с точкой, контент внизу, действие с подчёркиванием»: для каждой `.resource-card` есть `glass` и `glass-resource` в `classList`; первый ребёнок `.resource-card__indicator` с текстом `label` и `.resource-card__dot[aria-hidden="true"]`; есть `.resource-card__content > h3.resource-card__title`, `p.resource-card__description`, `.resource-card__actions .resource-card__trigger` с текстом `cta`; `style.getPropertyValue("--accent")` равен `resourcesCopy.cards[kind].accent`.
    3. «задаёт сетку и пропорции оригинала в CSS»: `RESOURCES_CSS` содержит каждую строку из acceptance criteria Task 1 (сетка, четыре aspect-ratio, `min(69.697%, 368px)`, `min(100%, 328px)`, `height: 248px`, пунктирная рамка, `scaleX(.34)`, `translateY(-4px)`, `420ms cubic-bezier(.22, 1, .36, 1)`, `padding: 20px`, `padding: 24px`).
    4. «не рисует собственный фон карточки и не заводит reduce-блок»: `RESOURCES_CSS` не матчится на `/\.resource-card \{[^}]*background/`, не содержит `prefers-reduced-motion`, не содержит `var(--color-`.
    Существующие сценарии не менять. Импорт `resourcesCopy` из `../../data/copy.resources` добавить.
  </action>
  <verify>
    <automated>npx vitest run src/components/resources/Resources.test.tsx src/styles/motionPolicy.test.ts && npx tsc -b && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/resources/Resources.test.tsx` зелёный, добавлено не меньше 4 сценариев
    - `npx vitest run src/styles/motionPolicy.test.ts` зелёный (единственный reduce-блок остался в global.css, реестр data-anim без новых значений)
    - `npx tsc -b` и `npm run lint` без ошибок
  </acceptance_criteria>
  <done>Структура сетки и карточек, значения CSS и отсутствие собственного фона закреплены тестами.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Разметка → CSS custom property | `--accent` приходит из статического `copy.resources.ts`, пользовательского ввода нет |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | style `--accent` в ResourceCard | accept | значение из типизированного модуля данных, литерал `rgb(...)`; тест сверяет строку |
| T-11-04 | Elevation of privilege | overlay-кнопка карточки | mitigate | кнопка остаётся `<button type="button">` без вложенных интерактивных узлов; тест «не держит вложенных ссылок и кнопок» сохранён |
| T-11-SC | Tampering | npm-установки | accept | план не устанавливает пакетов |
</threat_model>

<verification>
- `npx vitest run src/components/resources/Resources.test.tsx src/styles/motionPolicy.test.ts` зелёный
- `npx tsc -b` и `npm run lint` без ошибок
- Визуально (по желанию, не блокирует): `npx vite build && npx vite preview`, ширина 1440: музыка 320×296 слева, текст 528×523 в центре с пунктиром, материалы 272×336 справа у низа, видео 368×256 во второй строке под текстом; ширина 390: колонка, карточки 328×248
</verification>

<success_criteria>
- Сетка и пропорции блоков соответствуют RES-01 на ≥1024px и <1024px
- Карточка соответствует RES-02: индикатор с точкой 16px, контент внизу, подчёркивание, ховер-подъём 4px за 420ms, классы `glass glass-resource`, собственного фона нет
- Reveal-каскад, атмосфера, частицы и доступность карточек сохранены
- Тесты, `tsc -b`, `lint` зелёные
</success_criteria>

<output>
Create `.planning/phases/11-resources-panels/11-02-SUMMARY.md` when done
</output>
